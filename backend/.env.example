DATABASE_PROVIDER=sqlite
DATABASE_URL="file:./dev.db"
PORT=4000
NODE_ENV=development

# Auth & security
COOKIE_SECRET=dev-cookie-secret
CSRF_SECRET=dev-csrf-secret
JWT_ACCESS_SECRET=dev-access-secret
JWT_REFRESH_SECRET=dev-refresh-secret
COOKIE_DOMAIN=
CORS_ORIGIN=http://localhost:5173

# OAuth providers (Phase 1)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Email provider (for verification/reset)
EMAIL_PROVIDER_API_KEY=

# TTLs
# Access token lifetime (e.g. 15m)
ACCESS_TTL=15m
# Refresh token life in days
REFRESH_DAYS=30
# Email verification token TTL in minutes
EMAIL_VERIFICATION_TTL_MIN=1440
# Password reset token TTL in minutes
PASSWORD_RESET_TTL_MIN=60

# CORS origin for frontend dev
CORS_ORIGIN=http://localhost:5173
# Cookie signing secret (change in prod)
COOKIE_SECRET=please-change-me
# CSRF signing secret (andi-CSRF)
CSRF_SECRET=please-change-me

# JWT secrets (change in prod)
JWT_ACCESS_SECRET=dev-access-secret
JWT_REFRESH_SECRET=dev-refresh-secret

# OAuth provider credentials (Phase 1)
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
GITHUB_CLIENT_ID=
GITHUB_CLIENT_SECRET=

# Optional email provider for verification/reset
EMAIL_PROVIDER_API_KEY=
