import {
  clsx_default,
  init_clsx
} from "./chunk-7DAJWA5W.js";
import {
  require_shim
} from "./chunk-Q6WLQPLN.js";
import {
  __assign,
  __spreadArray,
  init_tslib_es6
} from "./chunk-W4KUXUUW.js";
import {
  require_react_dom
} from "./chunk-PUAIFRKT.js";
import {
  require_react
} from "./chunk-RVTP7VLF.js";
import {
  __commonJS,
  __esm,
  __export,
  __require,
  __toCommonJS,
  __toESM
} from "./chunk-PLDDJCW6.js";

// node_modules/lunr/lunr.js
var require_lunr = __commonJS({
  "node_modules/lunr/lunr.js"(exports, module) {
    (function() {
      var lunr = function(config) {
        var builder = new lunr.Builder();
        builder.pipeline.add(
          lunr.trimmer,
          lunr.stopWordFilter,
          lunr.stemmer
        );
        builder.searchPipeline.add(
          lunr.stemmer
        );
        config.call(builder, builder);
        return builder.build();
      };
      lunr.version = "2.3.9";
      lunr.utils = {};
      lunr.utils.warn = /* @__PURE__ */ (function(global2) {
        return function(message) {
          if (global2.console && console.warn) {
            console.warn(message);
          }
        };
      })(this);
      lunr.utils.asString = function(obj) {
        if (obj === void 0 || obj === null) {
          return "";
        } else {
          return obj.toString();
        }
      };
      lunr.utils.clone = function(obj) {
        if (obj === null || obj === void 0) {
          return obj;
        }
        var clone = /* @__PURE__ */ Object.create(null), keys2 = Object.keys(obj);
        for (var i2 = 0; i2 < keys2.length; i2++) {
          var key2 = keys2[i2], val = obj[key2];
          if (Array.isArray(val)) {
            clone[key2] = val.slice();
            continue;
          }
          if (typeof val === "string" || typeof val === "number" || typeof val === "boolean") {
            clone[key2] = val;
            continue;
          }
          throw new TypeError("clone is not deep and does not support nested objects");
        }
        return clone;
      };
      lunr.FieldRef = function(docRef, fieldName, stringValue) {
        this.docRef = docRef;
        this.fieldName = fieldName;
        this._stringValue = stringValue;
      };
      lunr.FieldRef.joiner = "/";
      lunr.FieldRef.fromString = function(s2) {
        var n = s2.indexOf(lunr.FieldRef.joiner);
        if (n === -1) {
          throw "malformed field ref string";
        }
        var fieldRef = s2.slice(0, n), docRef = s2.slice(n + 1);
        return new lunr.FieldRef(docRef, fieldRef, s2);
      };
      lunr.FieldRef.prototype.toString = function() {
        if (this._stringValue == void 0) {
          this._stringValue = this.fieldName + lunr.FieldRef.joiner + this.docRef;
        }
        return this._stringValue;
      };
      lunr.Set = function(elements) {
        this.elements = /* @__PURE__ */ Object.create(null);
        if (elements) {
          this.length = elements.length;
          for (var i2 = 0; i2 < this.length; i2++) {
            this.elements[elements[i2]] = true;
          }
        } else {
          this.length = 0;
        }
      };
      lunr.Set.complete = {
        intersect: function(other) {
          return other;
        },
        union: function() {
          return this;
        },
        contains: function() {
          return true;
        }
      };
      lunr.Set.empty = {
        intersect: function() {
          return this;
        },
        union: function(other) {
          return other;
        },
        contains: function() {
          return false;
        }
      };
      lunr.Set.prototype.contains = function(object2) {
        return !!this.elements[object2];
      };
      lunr.Set.prototype.intersect = function(other) {
        var a2, b2, elements, intersection = [];
        if (other === lunr.Set.complete) {
          return this;
        }
        if (other === lunr.Set.empty) {
          return other;
        }
        if (this.length < other.length) {
          a2 = this;
          b2 = other;
        } else {
          a2 = other;
          b2 = this;
        }
        elements = Object.keys(a2.elements);
        for (var i2 = 0; i2 < elements.length; i2++) {
          var element = elements[i2];
          if (element in b2.elements) {
            intersection.push(element);
          }
        }
        return new lunr.Set(intersection);
      };
      lunr.Set.prototype.union = function(other) {
        if (other === lunr.Set.complete) {
          return lunr.Set.complete;
        }
        if (other === lunr.Set.empty) {
          return this;
        }
        return new lunr.Set(Object.keys(this.elements).concat(Object.keys(other.elements)));
      };
      lunr.idf = function(posting, documentCount) {
        var documentsWithTerm = 0;
        for (var fieldName in posting) {
          if (fieldName == "_index") continue;
          documentsWithTerm += Object.keys(posting[fieldName]).length;
        }
        var x2 = (documentCount - documentsWithTerm + 0.5) / (documentsWithTerm + 0.5);
        return Math.log(1 + Math.abs(x2));
      };
      lunr.Token = function(str, metadata) {
        this.str = str || "";
        this.metadata = metadata || {};
      };
      lunr.Token.prototype.toString = function() {
        return this.str;
      };
      lunr.Token.prototype.update = function(fn) {
        this.str = fn(this.str, this.metadata);
        return this;
      };
      lunr.Token.prototype.clone = function(fn) {
        fn = fn || function(s2) {
          return s2;
        };
        return new lunr.Token(fn(this.str, this.metadata), this.metadata);
      };
      lunr.tokenizer = function(obj, metadata) {
        if (obj == null || obj == void 0) {
          return [];
        }
        if (Array.isArray(obj)) {
          return obj.map(function(t) {
            return new lunr.Token(
              lunr.utils.asString(t).toLowerCase(),
              lunr.utils.clone(metadata)
            );
          });
        }
        var str = obj.toString().toLowerCase(), len = str.length, tokens = [];
        for (var sliceEnd = 0, sliceStart = 0; sliceEnd <= len; sliceEnd++) {
          var char2 = str.charAt(sliceEnd), sliceLength = sliceEnd - sliceStart;
          if (char2.match(lunr.tokenizer.separator) || sliceEnd == len) {
            if (sliceLength > 0) {
              var tokenMetadata = lunr.utils.clone(metadata) || {};
              tokenMetadata["position"] = [sliceStart, sliceLength];
              tokenMetadata["index"] = tokens.length;
              tokens.push(
                new lunr.Token(
                  str.slice(sliceStart, sliceEnd),
                  tokenMetadata
                )
              );
            }
            sliceStart = sliceEnd + 1;
          }
        }
        return tokens;
      };
      lunr.tokenizer.separator = /[\s\-]+/;
      lunr.Pipeline = function() {
        this._stack = [];
      };
      lunr.Pipeline.registeredFunctions = /* @__PURE__ */ Object.create(null);
      lunr.Pipeline.registerFunction = function(fn, label) {
        if (label in this.registeredFunctions) {
          lunr.utils.warn("Overwriting existing registered function: " + label);
        }
        fn.label = label;
        lunr.Pipeline.registeredFunctions[fn.label] = fn;
      };
      lunr.Pipeline.warnIfFunctionNotRegistered = function(fn) {
        var isRegistered = fn.label && fn.label in this.registeredFunctions;
        if (!isRegistered) {
          lunr.utils.warn("Function is not registered with pipeline. This may cause problems when serialising the index.\n", fn);
        }
      };
      lunr.Pipeline.load = function(serialised) {
        var pipeline = new lunr.Pipeline();
        serialised.forEach(function(fnName) {
          var fn = lunr.Pipeline.registeredFunctions[fnName];
          if (fn) {
            pipeline.add(fn);
          } else {
            throw new Error("Cannot load unregistered function: " + fnName);
          }
        });
        return pipeline;
      };
      lunr.Pipeline.prototype.add = function() {
        var fns = Array.prototype.slice.call(arguments);
        fns.forEach(function(fn) {
          lunr.Pipeline.warnIfFunctionNotRegistered(fn);
          this._stack.push(fn);
        }, this);
      };
      lunr.Pipeline.prototype.after = function(existingFn, newFn) {
        lunr.Pipeline.warnIfFunctionNotRegistered(newFn);
        var pos = this._stack.indexOf(existingFn);
        if (pos == -1) {
          throw new Error("Cannot find existingFn");
        }
        pos = pos + 1;
        this._stack.splice(pos, 0, newFn);
      };
      lunr.Pipeline.prototype.before = function(existingFn, newFn) {
        lunr.Pipeline.warnIfFunctionNotRegistered(newFn);
        var pos = this._stack.indexOf(existingFn);
        if (pos == -1) {
          throw new Error("Cannot find existingFn");
        }
        this._stack.splice(pos, 0, newFn);
      };
      lunr.Pipeline.prototype.remove = function(fn) {
        var pos = this._stack.indexOf(fn);
        if (pos == -1) {
          return;
        }
        this._stack.splice(pos, 1);
      };
      lunr.Pipeline.prototype.run = function(tokens) {
        var stackLength = this._stack.length;
        for (var i2 = 0; i2 < stackLength; i2++) {
          var fn = this._stack[i2];
          var memo2 = [];
          for (var j2 = 0; j2 < tokens.length; j2++) {
            var result = fn(tokens[j2], j2, tokens);
            if (result === null || result === void 0 || result === "") continue;
            if (Array.isArray(result)) {
              for (var k2 = 0; k2 < result.length; k2++) {
                memo2.push(result[k2]);
              }
            } else {
              memo2.push(result);
            }
          }
          tokens = memo2;
        }
        return tokens;
      };
      lunr.Pipeline.prototype.runString = function(str, metadata) {
        var token2 = new lunr.Token(str, metadata);
        return this.run([token2]).map(function(t) {
          return t.toString();
        });
      };
      lunr.Pipeline.prototype.reset = function() {
        this._stack = [];
      };
      lunr.Pipeline.prototype.toJSON = function() {
        return this._stack.map(function(fn) {
          lunr.Pipeline.warnIfFunctionNotRegistered(fn);
          return fn.label;
        });
      };
      lunr.Vector = function(elements) {
        this._magnitude = 0;
        this.elements = elements || [];
      };
      lunr.Vector.prototype.positionForIndex = function(index) {
        if (this.elements.length == 0) {
          return 0;
        }
        var start = 0, end = this.elements.length / 2, sliceLength = end - start, pivotPoint = Math.floor(sliceLength / 2), pivotIndex = this.elements[pivotPoint * 2];
        while (sliceLength > 1) {
          if (pivotIndex < index) {
            start = pivotPoint;
          }
          if (pivotIndex > index) {
            end = pivotPoint;
          }
          if (pivotIndex == index) {
            break;
          }
          sliceLength = end - start;
          pivotPoint = start + Math.floor(sliceLength / 2);
          pivotIndex = this.elements[pivotPoint * 2];
        }
        if (pivotIndex == index) {
          return pivotPoint * 2;
        }
        if (pivotIndex > index) {
          return pivotPoint * 2;
        }
        if (pivotIndex < index) {
          return (pivotPoint + 1) * 2;
        }
      };
      lunr.Vector.prototype.insert = function(insertIdx, val) {
        this.upsert(insertIdx, val, function() {
          throw "duplicate index";
        });
      };
      lunr.Vector.prototype.upsert = function(insertIdx, val, fn) {
        this._magnitude = 0;
        var position3 = this.positionForIndex(insertIdx);
        if (this.elements[position3] == insertIdx) {
          this.elements[position3 + 1] = fn(this.elements[position3 + 1], val);
        } else {
          this.elements.splice(position3, 0, insertIdx, val);
        }
      };
      lunr.Vector.prototype.magnitude = function() {
        if (this._magnitude) return this._magnitude;
        var sumOfSquares = 0, elementsLength = this.elements.length;
        for (var i2 = 1; i2 < elementsLength; i2 += 2) {
          var val = this.elements[i2];
          sumOfSquares += val * val;
        }
        return this._magnitude = Math.sqrt(sumOfSquares);
      };
      lunr.Vector.prototype.dot = function(otherVector) {
        var dotProduct = 0, a2 = this.elements, b2 = otherVector.elements, aLen = a2.length, bLen = b2.length, aVal = 0, bVal = 0, i2 = 0, j2 = 0;
        while (i2 < aLen && j2 < bLen) {
          aVal = a2[i2], bVal = b2[j2];
          if (aVal < bVal) {
            i2 += 2;
          } else if (aVal > bVal) {
            j2 += 2;
          } else if (aVal == bVal) {
            dotProduct += a2[i2 + 1] * b2[j2 + 1];
            i2 += 2;
            j2 += 2;
          }
        }
        return dotProduct;
      };
      lunr.Vector.prototype.similarity = function(otherVector) {
        return this.dot(otherVector) / this.magnitude() || 0;
      };
      lunr.Vector.prototype.toArray = function() {
        var output = new Array(this.elements.length / 2);
        for (var i2 = 1, j2 = 0; i2 < this.elements.length; i2 += 2, j2++) {
          output[j2] = this.elements[i2];
        }
        return output;
      };
      lunr.Vector.prototype.toJSON = function() {
        return this.elements;
      };
      lunr.stemmer = (function() {
        var step2list = {
          "ational": "ate",
          "tional": "tion",
          "enci": "ence",
          "anci": "ance",
          "izer": "ize",
          "bli": "ble",
          "alli": "al",
          "entli": "ent",
          "eli": "e",
          "ousli": "ous",
          "ization": "ize",
          "ation": "ate",
          "ator": "ate",
          "alism": "al",
          "iveness": "ive",
          "fulness": "ful",
          "ousness": "ous",
          "aliti": "al",
          "iviti": "ive",
          "biliti": "ble",
          "logi": "log"
        }, step3list = {
          "icate": "ic",
          "ative": "",
          "alize": "al",
          "iciti": "ic",
          "ical": "ic",
          "ful": "",
          "ness": ""
        }, c2 = "[^aeiou]", v2 = "[aeiouy]", C2 = c2 + "[^aeiouy]*", V2 = v2 + "[aeiou]*", mgr0 = "^(" + C2 + ")?" + V2 + C2, meq1 = "^(" + C2 + ")?" + V2 + C2 + "(" + V2 + ")?$", mgr1 = "^(" + C2 + ")?" + V2 + C2 + V2 + C2, s_v = "^(" + C2 + ")?" + v2;
        var re_mgr0 = new RegExp(mgr0);
        var re_mgr1 = new RegExp(mgr1);
        var re_meq1 = new RegExp(meq1);
        var re_s_v = new RegExp(s_v);
        var re_1a = /^(.+?)(ss|i)es$/;
        var re2_1a = /^(.+?)([^s])s$/;
        var re_1b = /^(.+?)eed$/;
        var re2_1b = /^(.+?)(ed|ing)$/;
        var re_1b_2 = /.$/;
        var re2_1b_2 = /(at|bl|iz)$/;
        var re3_1b_2 = new RegExp("([^aeiouylsz])\\1$");
        var re4_1b_2 = new RegExp("^" + C2 + v2 + "[^aeiouwxy]$");
        var re_1c = /^(.+?[^aeiou])y$/;
        var re_2 = /^(.+?)(ational|tional|enci|anci|izer|bli|alli|entli|eli|ousli|ization|ation|ator|alism|iveness|fulness|ousness|aliti|iviti|biliti|logi)$/;
        var re_3 = /^(.+?)(icate|ative|alize|iciti|ical|ful|ness)$/;
        var re_4 = /^(.+?)(al|ance|ence|er|ic|able|ible|ant|ement|ment|ent|ou|ism|ate|iti|ous|ive|ize)$/;
        var re2_4 = /^(.+?)(s|t)(ion)$/;
        var re_5 = /^(.+?)e$/;
        var re_5_1 = /ll$/;
        var re3_5 = new RegExp("^" + C2 + v2 + "[^aeiouwxy]$");
        var porterStemmer = function porterStemmer2(w2) {
          var stem, suffix, firstch, re2, re22, re3, re4;
          if (w2.length < 3) {
            return w2;
          }
          firstch = w2.substr(0, 1);
          if (firstch == "y") {
            w2 = firstch.toUpperCase() + w2.substr(1);
          }
          re2 = re_1a;
          re22 = re2_1a;
          if (re2.test(w2)) {
            w2 = w2.replace(re2, "$1$2");
          } else if (re22.test(w2)) {
            w2 = w2.replace(re22, "$1$2");
          }
          re2 = re_1b;
          re22 = re2_1b;
          if (re2.test(w2)) {
            var fp = re2.exec(w2);
            re2 = re_mgr0;
            if (re2.test(fp[1])) {
              re2 = re_1b_2;
              w2 = w2.replace(re2, "");
            }
          } else if (re22.test(w2)) {
            var fp = re22.exec(w2);
            stem = fp[1];
            re22 = re_s_v;
            if (re22.test(stem)) {
              w2 = stem;
              re22 = re2_1b_2;
              re3 = re3_1b_2;
              re4 = re4_1b_2;
              if (re22.test(w2)) {
                w2 = w2 + "e";
              } else if (re3.test(w2)) {
                re2 = re_1b_2;
                w2 = w2.replace(re2, "");
              } else if (re4.test(w2)) {
                w2 = w2 + "e";
              }
            }
          }
          re2 = re_1c;
          if (re2.test(w2)) {
            var fp = re2.exec(w2);
            stem = fp[1];
            w2 = stem + "i";
          }
          re2 = re_2;
          if (re2.test(w2)) {
            var fp = re2.exec(w2);
            stem = fp[1];
            suffix = fp[2];
            re2 = re_mgr0;
            if (re2.test(stem)) {
              w2 = stem + step2list[suffix];
            }
          }
          re2 = re_3;
          if (re2.test(w2)) {
            var fp = re2.exec(w2);
            stem = fp[1];
            suffix = fp[2];
            re2 = re_mgr0;
            if (re2.test(stem)) {
              w2 = stem + step3list[suffix];
            }
          }
          re2 = re_4;
          re22 = re2_4;
          if (re2.test(w2)) {
            var fp = re2.exec(w2);
            stem = fp[1];
            re2 = re_mgr1;
            if (re2.test(stem)) {
              w2 = stem;
            }
          } else if (re22.test(w2)) {
            var fp = re22.exec(w2);
            stem = fp[1] + fp[2];
            re22 = re_mgr1;
            if (re22.test(stem)) {
              w2 = stem;
            }
          }
          re2 = re_5;
          if (re2.test(w2)) {
            var fp = re2.exec(w2);
            stem = fp[1];
            re2 = re_mgr1;
            re22 = re_meq1;
            re3 = re3_5;
            if (re2.test(stem) || re22.test(stem) && !re3.test(stem)) {
              w2 = stem;
            }
          }
          re2 = re_5_1;
          re22 = re_mgr1;
          if (re2.test(w2) && re22.test(w2)) {
            re2 = re_1b_2;
            w2 = w2.replace(re2, "");
          }
          if (firstch == "y") {
            w2 = firstch.toLowerCase() + w2.substr(1);
          }
          return w2;
        };
        return function(token2) {
          return token2.update(porterStemmer);
        };
      })();
      lunr.Pipeline.registerFunction(lunr.stemmer, "stemmer");
      lunr.generateStopWordFilter = function(stopWords) {
        var words = stopWords.reduce(function(memo2, stopWord) {
          memo2[stopWord] = stopWord;
          return memo2;
        }, {});
        return function(token2) {
          if (token2 && words[token2.toString()] !== token2.toString()) return token2;
        };
      };
      lunr.stopWordFilter = lunr.generateStopWordFilter([
        "a",
        "able",
        "about",
        "across",
        "after",
        "all",
        "almost",
        "also",
        "am",
        "among",
        "an",
        "and",
        "any",
        "are",
        "as",
        "at",
        "be",
        "because",
        "been",
        "but",
        "by",
        "can",
        "cannot",
        "could",
        "dear",
        "did",
        "do",
        "does",
        "either",
        "else",
        "ever",
        "every",
        "for",
        "from",
        "get",
        "got",
        "had",
        "has",
        "have",
        "he",
        "her",
        "hers",
        "him",
        "his",
        "how",
        "however",
        "i",
        "if",
        "in",
        "into",
        "is",
        "it",
        "its",
        "just",
        "least",
        "let",
        "like",
        "likely",
        "may",
        "me",
        "might",
        "most",
        "must",
        "my",
        "neither",
        "no",
        "nor",
        "not",
        "of",
        "off",
        "often",
        "on",
        "only",
        "or",
        "other",
        "our",
        "own",
        "rather",
        "said",
        "say",
        "says",
        "she",
        "should",
        "since",
        "so",
        "some",
        "than",
        "that",
        "the",
        "their",
        "them",
        "then",
        "there",
        "these",
        "they",
        "this",
        "tis",
        "to",
        "too",
        "twas",
        "us",
        "wants",
        "was",
        "we",
        "were",
        "what",
        "when",
        "where",
        "which",
        "while",
        "who",
        "whom",
        "why",
        "will",
        "with",
        "would",
        "yet",
        "you",
        "your"
      ]);
      lunr.Pipeline.registerFunction(lunr.stopWordFilter, "stopWordFilter");
      lunr.trimmer = function(token2) {
        return token2.update(function(s2) {
          return s2.replace(/^\W+/, "").replace(/\W+$/, "");
        });
      };
      lunr.Pipeline.registerFunction(lunr.trimmer, "trimmer");
      lunr.TokenSet = function() {
        this.final = false;
        this.edges = {};
        this.id = lunr.TokenSet._nextId;
        lunr.TokenSet._nextId += 1;
      };
      lunr.TokenSet._nextId = 1;
      lunr.TokenSet.fromArray = function(arr) {
        var builder = new lunr.TokenSet.Builder();
        for (var i2 = 0, len = arr.length; i2 < len; i2++) {
          builder.insert(arr[i2]);
        }
        builder.finish();
        return builder.root;
      };
      lunr.TokenSet.fromClause = function(clause) {
        if ("editDistance" in clause) {
          return lunr.TokenSet.fromFuzzyString(clause.term, clause.editDistance);
        } else {
          return lunr.TokenSet.fromString(clause.term);
        }
      };
      lunr.TokenSet.fromFuzzyString = function(str, editDistance) {
        var root = new lunr.TokenSet();
        var stack = [{
          node: root,
          editsRemaining: editDistance,
          str
        }];
        while (stack.length) {
          var frame = stack.pop();
          if (frame.str.length > 0) {
            var char2 = frame.str.charAt(0), noEditNode;
            if (char2 in frame.node.edges) {
              noEditNode = frame.node.edges[char2];
            } else {
              noEditNode = new lunr.TokenSet();
              frame.node.edges[char2] = noEditNode;
            }
            if (frame.str.length == 1) {
              noEditNode.final = true;
            }
            stack.push({
              node: noEditNode,
              editsRemaining: frame.editsRemaining,
              str: frame.str.slice(1)
            });
          }
          if (frame.editsRemaining == 0) {
            continue;
          }
          if ("*" in frame.node.edges) {
            var insertionNode = frame.node.edges["*"];
          } else {
            var insertionNode = new lunr.TokenSet();
            frame.node.edges["*"] = insertionNode;
          }
          if (frame.str.length == 0) {
            insertionNode.final = true;
          }
          stack.push({
            node: insertionNode,
            editsRemaining: frame.editsRemaining - 1,
            str: frame.str
          });
          if (frame.str.length > 1) {
            stack.push({
              node: frame.node,
              editsRemaining: frame.editsRemaining - 1,
              str: frame.str.slice(1)
            });
          }
          if (frame.str.length == 1) {
            frame.node.final = true;
          }
          if (frame.str.length >= 1) {
            if ("*" in frame.node.edges) {
              var substitutionNode = frame.node.edges["*"];
            } else {
              var substitutionNode = new lunr.TokenSet();
              frame.node.edges["*"] = substitutionNode;
            }
            if (frame.str.length == 1) {
              substitutionNode.final = true;
            }
            stack.push({
              node: substitutionNode,
              editsRemaining: frame.editsRemaining - 1,
              str: frame.str.slice(1)
            });
          }
          if (frame.str.length > 1) {
            var charA = frame.str.charAt(0), charB = frame.str.charAt(1), transposeNode;
            if (charB in frame.node.edges) {
              transposeNode = frame.node.edges[charB];
            } else {
              transposeNode = new lunr.TokenSet();
              frame.node.edges[charB] = transposeNode;
            }
            if (frame.str.length == 1) {
              transposeNode.final = true;
            }
            stack.push({
              node: transposeNode,
              editsRemaining: frame.editsRemaining - 1,
              str: charA + frame.str.slice(2)
            });
          }
        }
        return root;
      };
      lunr.TokenSet.fromString = function(str) {
        var node2 = new lunr.TokenSet(), root = node2;
        for (var i2 = 0, len = str.length; i2 < len; i2++) {
          var char2 = str[i2], final = i2 == len - 1;
          if (char2 == "*") {
            node2.edges[char2] = node2;
            node2.final = final;
          } else {
            var next2 = new lunr.TokenSet();
            next2.final = final;
            node2.edges[char2] = next2;
            node2 = next2;
          }
        }
        return root;
      };
      lunr.TokenSet.prototype.toArray = function() {
        var words = [];
        var stack = [{
          prefix: "",
          node: this
        }];
        while (stack.length) {
          var frame = stack.pop(), edges = Object.keys(frame.node.edges), len = edges.length;
          if (frame.node.final) {
            frame.prefix.charAt(0);
            words.push(frame.prefix);
          }
          for (var i2 = 0; i2 < len; i2++) {
            var edge = edges[i2];
            stack.push({
              prefix: frame.prefix.concat(edge),
              node: frame.node.edges[edge]
            });
          }
        }
        return words;
      };
      lunr.TokenSet.prototype.toString = function() {
        if (this._str) {
          return this._str;
        }
        var str = this.final ? "1" : "0", labels = Object.keys(this.edges).sort(), len = labels.length;
        for (var i2 = 0; i2 < len; i2++) {
          var label = labels[i2], node2 = this.edges[label];
          str = str + label + node2.id;
        }
        return str;
      };
      lunr.TokenSet.prototype.intersect = function(b2) {
        var output = new lunr.TokenSet(), frame = void 0;
        var stack = [{
          qNode: b2,
          output,
          node: this
        }];
        while (stack.length) {
          frame = stack.pop();
          var qEdges = Object.keys(frame.qNode.edges), qLen = qEdges.length, nEdges = Object.keys(frame.node.edges), nLen = nEdges.length;
          for (var q2 = 0; q2 < qLen; q2++) {
            var qEdge = qEdges[q2];
            for (var n = 0; n < nLen; n++) {
              var nEdge = nEdges[n];
              if (nEdge == qEdge || qEdge == "*") {
                var node2 = frame.node.edges[nEdge], qNode = frame.qNode.edges[qEdge], final = node2.final && qNode.final, next2 = void 0;
                if (nEdge in frame.output.edges) {
                  next2 = frame.output.edges[nEdge];
                  next2.final = next2.final || final;
                } else {
                  next2 = new lunr.TokenSet();
                  next2.final = final;
                  frame.output.edges[nEdge] = next2;
                }
                stack.push({
                  qNode,
                  output: next2,
                  node: node2
                });
              }
            }
          }
        }
        return output;
      };
      lunr.TokenSet.Builder = function() {
        this.previousWord = "";
        this.root = new lunr.TokenSet();
        this.uncheckedNodes = [];
        this.minimizedNodes = {};
      };
      lunr.TokenSet.Builder.prototype.insert = function(word) {
        var node2, commonPrefix = 0;
        if (word < this.previousWord) {
          throw new Error("Out of order word insertion");
        }
        for (var i2 = 0; i2 < word.length && i2 < this.previousWord.length; i2++) {
          if (word[i2] != this.previousWord[i2]) break;
          commonPrefix++;
        }
        this.minimize(commonPrefix);
        if (this.uncheckedNodes.length == 0) {
          node2 = this.root;
        } else {
          node2 = this.uncheckedNodes[this.uncheckedNodes.length - 1].child;
        }
        for (var i2 = commonPrefix; i2 < word.length; i2++) {
          var nextNode = new lunr.TokenSet(), char2 = word[i2];
          node2.edges[char2] = nextNode;
          this.uncheckedNodes.push({
            parent: node2,
            char: char2,
            child: nextNode
          });
          node2 = nextNode;
        }
        node2.final = true;
        this.previousWord = word;
      };
      lunr.TokenSet.Builder.prototype.finish = function() {
        this.minimize(0);
      };
      lunr.TokenSet.Builder.prototype.minimize = function(downTo) {
        for (var i2 = this.uncheckedNodes.length - 1; i2 >= downTo; i2--) {
          var node2 = this.uncheckedNodes[i2], childKey = node2.child.toString();
          if (childKey in this.minimizedNodes) {
            node2.parent.edges[node2.char] = this.minimizedNodes[childKey];
          } else {
            node2.child._str = childKey;
            this.minimizedNodes[childKey] = node2.child;
          }
          this.uncheckedNodes.pop();
        }
      };
      lunr.Index = function(attrs) {
        this.invertedIndex = attrs.invertedIndex;
        this.fieldVectors = attrs.fieldVectors;
        this.tokenSet = attrs.tokenSet;
        this.fields = attrs.fields;
        this.pipeline = attrs.pipeline;
      };
      lunr.Index.prototype.search = function(queryString) {
        return this.query(function(query) {
          var parser = new lunr.QueryParser(queryString, query);
          parser.parse();
        });
      };
      lunr.Index.prototype.query = function(fn) {
        var query = new lunr.Query(this.fields), matchingFields = /* @__PURE__ */ Object.create(null), queryVectors = /* @__PURE__ */ Object.create(null), termFieldCache = /* @__PURE__ */ Object.create(null), requiredMatches = /* @__PURE__ */ Object.create(null), prohibitedMatches = /* @__PURE__ */ Object.create(null);
        for (var i2 = 0; i2 < this.fields.length; i2++) {
          queryVectors[this.fields[i2]] = new lunr.Vector();
        }
        fn.call(query, query);
        for (var i2 = 0; i2 < query.clauses.length; i2++) {
          var clause = query.clauses[i2], terms = null, clauseMatches = lunr.Set.empty;
          if (clause.usePipeline) {
            terms = this.pipeline.runString(clause.term, {
              fields: clause.fields
            });
          } else {
            terms = [clause.term];
          }
          for (var m2 = 0; m2 < terms.length; m2++) {
            var term = terms[m2];
            clause.term = term;
            var termTokenSet = lunr.TokenSet.fromClause(clause), expandedTerms = this.tokenSet.intersect(termTokenSet).toArray();
            if (expandedTerms.length === 0 && clause.presence === lunr.Query.presence.REQUIRED) {
              for (var k2 = 0; k2 < clause.fields.length; k2++) {
                var field = clause.fields[k2];
                requiredMatches[field] = lunr.Set.empty;
              }
              break;
            }
            for (var j2 = 0; j2 < expandedTerms.length; j2++) {
              var expandedTerm = expandedTerms[j2], posting = this.invertedIndex[expandedTerm], termIndex = posting._index;
              for (var k2 = 0; k2 < clause.fields.length; k2++) {
                var field = clause.fields[k2], fieldPosting = posting[field], matchingDocumentRefs = Object.keys(fieldPosting), termField = expandedTerm + "/" + field, matchingDocumentsSet = new lunr.Set(matchingDocumentRefs);
                if (clause.presence == lunr.Query.presence.REQUIRED) {
                  clauseMatches = clauseMatches.union(matchingDocumentsSet);
                  if (requiredMatches[field] === void 0) {
                    requiredMatches[field] = lunr.Set.complete;
                  }
                }
                if (clause.presence == lunr.Query.presence.PROHIBITED) {
                  if (prohibitedMatches[field] === void 0) {
                    prohibitedMatches[field] = lunr.Set.empty;
                  }
                  prohibitedMatches[field] = prohibitedMatches[field].union(matchingDocumentsSet);
                  continue;
                }
                queryVectors[field].upsert(termIndex, clause.boost, function(a2, b2) {
                  return a2 + b2;
                });
                if (termFieldCache[termField]) {
                  continue;
                }
                for (var l2 = 0; l2 < matchingDocumentRefs.length; l2++) {
                  var matchingDocumentRef = matchingDocumentRefs[l2], matchingFieldRef = new lunr.FieldRef(matchingDocumentRef, field), metadata = fieldPosting[matchingDocumentRef], fieldMatch;
                  if ((fieldMatch = matchingFields[matchingFieldRef]) === void 0) {
                    matchingFields[matchingFieldRef] = new lunr.MatchData(expandedTerm, field, metadata);
                  } else {
                    fieldMatch.add(expandedTerm, field, metadata);
                  }
                }
                termFieldCache[termField] = true;
              }
            }
          }
          if (clause.presence === lunr.Query.presence.REQUIRED) {
            for (var k2 = 0; k2 < clause.fields.length; k2++) {
              var field = clause.fields[k2];
              requiredMatches[field] = requiredMatches[field].intersect(clauseMatches);
            }
          }
        }
        var allRequiredMatches = lunr.Set.complete, allProhibitedMatches = lunr.Set.empty;
        for (var i2 = 0; i2 < this.fields.length; i2++) {
          var field = this.fields[i2];
          if (requiredMatches[field]) {
            allRequiredMatches = allRequiredMatches.intersect(requiredMatches[field]);
          }
          if (prohibitedMatches[field]) {
            allProhibitedMatches = allProhibitedMatches.union(prohibitedMatches[field]);
          }
        }
        var matchingFieldRefs = Object.keys(matchingFields), results = [], matches2 = /* @__PURE__ */ Object.create(null);
        if (query.isNegated()) {
          matchingFieldRefs = Object.keys(this.fieldVectors);
          for (var i2 = 0; i2 < matchingFieldRefs.length; i2++) {
            var matchingFieldRef = matchingFieldRefs[i2];
            var fieldRef = lunr.FieldRef.fromString(matchingFieldRef);
            matchingFields[matchingFieldRef] = new lunr.MatchData();
          }
        }
        for (var i2 = 0; i2 < matchingFieldRefs.length; i2++) {
          var fieldRef = lunr.FieldRef.fromString(matchingFieldRefs[i2]), docRef = fieldRef.docRef;
          if (!allRequiredMatches.contains(docRef)) {
            continue;
          }
          if (allProhibitedMatches.contains(docRef)) {
            continue;
          }
          var fieldVector = this.fieldVectors[fieldRef], score = queryVectors[fieldRef.fieldName].similarity(fieldVector), docMatch;
          if ((docMatch = matches2[docRef]) !== void 0) {
            docMatch.score += score;
            docMatch.matchData.combine(matchingFields[fieldRef]);
          } else {
            var match2 = {
              ref: docRef,
              score,
              matchData: matchingFields[fieldRef]
            };
            matches2[docRef] = match2;
            results.push(match2);
          }
        }
        return results.sort(function(a2, b2) {
          return b2.score - a2.score;
        });
      };
      lunr.Index.prototype.toJSON = function() {
        var invertedIndex = Object.keys(this.invertedIndex).sort().map(function(term) {
          return [term, this.invertedIndex[term]];
        }, this);
        var fieldVectors = Object.keys(this.fieldVectors).map(function(ref) {
          return [ref, this.fieldVectors[ref].toJSON()];
        }, this);
        return {
          version: lunr.version,
          fields: this.fields,
          fieldVectors,
          invertedIndex,
          pipeline: this.pipeline.toJSON()
        };
      };
      lunr.Index.load = function(serializedIndex) {
        var attrs = {}, fieldVectors = {}, serializedVectors = serializedIndex.fieldVectors, invertedIndex = /* @__PURE__ */ Object.create(null), serializedInvertedIndex = serializedIndex.invertedIndex, tokenSetBuilder = new lunr.TokenSet.Builder(), pipeline = lunr.Pipeline.load(serializedIndex.pipeline);
        if (serializedIndex.version != lunr.version) {
          lunr.utils.warn("Version mismatch when loading serialised index. Current version of lunr '" + lunr.version + "' does not match serialized index '" + serializedIndex.version + "'");
        }
        for (var i2 = 0; i2 < serializedVectors.length; i2++) {
          var tuple = serializedVectors[i2], ref = tuple[0], elements = tuple[1];
          fieldVectors[ref] = new lunr.Vector(elements);
        }
        for (var i2 = 0; i2 < serializedInvertedIndex.length; i2++) {
          var tuple = serializedInvertedIndex[i2], term = tuple[0], posting = tuple[1];
          tokenSetBuilder.insert(term);
          invertedIndex[term] = posting;
        }
        tokenSetBuilder.finish();
        attrs.fields = serializedIndex.fields;
        attrs.fieldVectors = fieldVectors;
        attrs.invertedIndex = invertedIndex;
        attrs.tokenSet = tokenSetBuilder.root;
        attrs.pipeline = pipeline;
        return new lunr.Index(attrs);
      };
      lunr.Builder = function() {
        this._ref = "id";
        this._fields = /* @__PURE__ */ Object.create(null);
        this._documents = /* @__PURE__ */ Object.create(null);
        this.invertedIndex = /* @__PURE__ */ Object.create(null);
        this.fieldTermFrequencies = {};
        this.fieldLengths = {};
        this.tokenizer = lunr.tokenizer;
        this.pipeline = new lunr.Pipeline();
        this.searchPipeline = new lunr.Pipeline();
        this.documentCount = 0;
        this._b = 0.75;
        this._k1 = 1.2;
        this.termIndex = 0;
        this.metadataWhitelist = [];
      };
      lunr.Builder.prototype.ref = function(ref) {
        this._ref = ref;
      };
      lunr.Builder.prototype.field = function(fieldName, attributes) {
        if (/\//.test(fieldName)) {
          throw new RangeError("Field '" + fieldName + "' contains illegal character '/'");
        }
        this._fields[fieldName] = attributes || {};
      };
      lunr.Builder.prototype.b = function(number) {
        if (number < 0) {
          this._b = 0;
        } else if (number > 1) {
          this._b = 1;
        } else {
          this._b = number;
        }
      };
      lunr.Builder.prototype.k1 = function(number) {
        this._k1 = number;
      };
      lunr.Builder.prototype.add = function(doc, attributes) {
        var docRef = doc[this._ref], fields = Object.keys(this._fields);
        this._documents[docRef] = attributes || {};
        this.documentCount += 1;
        for (var i2 = 0; i2 < fields.length; i2++) {
          var fieldName = fields[i2], extractor = this._fields[fieldName].extractor, field = extractor ? extractor(doc) : doc[fieldName], tokens = this.tokenizer(field, {
            fields: [fieldName]
          }), terms = this.pipeline.run(tokens), fieldRef = new lunr.FieldRef(docRef, fieldName), fieldTerms = /* @__PURE__ */ Object.create(null);
          this.fieldTermFrequencies[fieldRef] = fieldTerms;
          this.fieldLengths[fieldRef] = 0;
          this.fieldLengths[fieldRef] += terms.length;
          for (var j2 = 0; j2 < terms.length; j2++) {
            var term = terms[j2];
            if (fieldTerms[term] == void 0) {
              fieldTerms[term] = 0;
            }
            fieldTerms[term] += 1;
            if (this.invertedIndex[term] == void 0) {
              var posting = /* @__PURE__ */ Object.create(null);
              posting["_index"] = this.termIndex;
              this.termIndex += 1;
              for (var k2 = 0; k2 < fields.length; k2++) {
                posting[fields[k2]] = /* @__PURE__ */ Object.create(null);
              }
              this.invertedIndex[term] = posting;
            }
            if (this.invertedIndex[term][fieldName][docRef] == void 0) {
              this.invertedIndex[term][fieldName][docRef] = /* @__PURE__ */ Object.create(null);
            }
            for (var l2 = 0; l2 < this.metadataWhitelist.length; l2++) {
              var metadataKey = this.metadataWhitelist[l2], metadata = term.metadata[metadataKey];
              if (this.invertedIndex[term][fieldName][docRef][metadataKey] == void 0) {
                this.invertedIndex[term][fieldName][docRef][metadataKey] = [];
              }
              this.invertedIndex[term][fieldName][docRef][metadataKey].push(metadata);
            }
          }
        }
      };
      lunr.Builder.prototype.calculateAverageFieldLengths = function() {
        var fieldRefs = Object.keys(this.fieldLengths), numberOfFields = fieldRefs.length, accumulator = {}, documentsWithField = {};
        for (var i2 = 0; i2 < numberOfFields; i2++) {
          var fieldRef = lunr.FieldRef.fromString(fieldRefs[i2]), field = fieldRef.fieldName;
          documentsWithField[field] || (documentsWithField[field] = 0);
          documentsWithField[field] += 1;
          accumulator[field] || (accumulator[field] = 0);
          accumulator[field] += this.fieldLengths[fieldRef];
        }
        var fields = Object.keys(this._fields);
        for (var i2 = 0; i2 < fields.length; i2++) {
          var fieldName = fields[i2];
          accumulator[fieldName] = accumulator[fieldName] / documentsWithField[fieldName];
        }
        this.averageFieldLength = accumulator;
      };
      lunr.Builder.prototype.createFieldVectors = function() {
        var fieldVectors = {}, fieldRefs = Object.keys(this.fieldTermFrequencies), fieldRefsLength = fieldRefs.length, termIdfCache = /* @__PURE__ */ Object.create(null);
        for (var i2 = 0; i2 < fieldRefsLength; i2++) {
          var fieldRef = lunr.FieldRef.fromString(fieldRefs[i2]), fieldName = fieldRef.fieldName, fieldLength = this.fieldLengths[fieldRef], fieldVector = new lunr.Vector(), termFrequencies = this.fieldTermFrequencies[fieldRef], terms = Object.keys(termFrequencies), termsLength = terms.length;
          var fieldBoost = this._fields[fieldName].boost || 1, docBoost = this._documents[fieldRef.docRef].boost || 1;
          for (var j2 = 0; j2 < termsLength; j2++) {
            var term = terms[j2], tf = termFrequencies[term], termIndex = this.invertedIndex[term]._index, idf, score, scoreWithPrecision;
            if (termIdfCache[term] === void 0) {
              idf = lunr.idf(this.invertedIndex[term], this.documentCount);
              termIdfCache[term] = idf;
            } else {
              idf = termIdfCache[term];
            }
            score = idf * ((this._k1 + 1) * tf) / (this._k1 * (1 - this._b + this._b * (fieldLength / this.averageFieldLength[fieldName])) + tf);
            score *= fieldBoost;
            score *= docBoost;
            scoreWithPrecision = Math.round(score * 1e3) / 1e3;
            fieldVector.insert(termIndex, scoreWithPrecision);
          }
          fieldVectors[fieldRef] = fieldVector;
        }
        this.fieldVectors = fieldVectors;
      };
      lunr.Builder.prototype.createTokenSet = function() {
        this.tokenSet = lunr.TokenSet.fromArray(
          Object.keys(this.invertedIndex).sort()
        );
      };
      lunr.Builder.prototype.build = function() {
        this.calculateAverageFieldLengths();
        this.createFieldVectors();
        this.createTokenSet();
        return new lunr.Index({
          invertedIndex: this.invertedIndex,
          fieldVectors: this.fieldVectors,
          tokenSet: this.tokenSet,
          fields: Object.keys(this._fields),
          pipeline: this.searchPipeline
        });
      };
      lunr.Builder.prototype.use = function(fn) {
        var args = Array.prototype.slice.call(arguments, 1);
        args.unshift(this);
        fn.apply(this, args);
      };
      lunr.MatchData = function(term, field, metadata) {
        var clonedMetadata = /* @__PURE__ */ Object.create(null), metadataKeys = Object.keys(metadata || {});
        for (var i2 = 0; i2 < metadataKeys.length; i2++) {
          var key2 = metadataKeys[i2];
          clonedMetadata[key2] = metadata[key2].slice();
        }
        this.metadata = /* @__PURE__ */ Object.create(null);
        if (term !== void 0) {
          this.metadata[term] = /* @__PURE__ */ Object.create(null);
          this.metadata[term][field] = clonedMetadata;
        }
      };
      lunr.MatchData.prototype.combine = function(otherMatchData) {
        var terms = Object.keys(otherMatchData.metadata);
        for (var i2 = 0; i2 < terms.length; i2++) {
          var term = terms[i2], fields = Object.keys(otherMatchData.metadata[term]);
          if (this.metadata[term] == void 0) {
            this.metadata[term] = /* @__PURE__ */ Object.create(null);
          }
          for (var j2 = 0; j2 < fields.length; j2++) {
            var field = fields[j2], keys2 = Object.keys(otherMatchData.metadata[term][field]);
            if (this.metadata[term][field] == void 0) {
              this.metadata[term][field] = /* @__PURE__ */ Object.create(null);
            }
            for (var k2 = 0; k2 < keys2.length; k2++) {
              var key2 = keys2[k2];
              if (this.metadata[term][field][key2] == void 0) {
                this.metadata[term][field][key2] = otherMatchData.metadata[term][field][key2];
              } else {
                this.metadata[term][field][key2] = this.metadata[term][field][key2].concat(otherMatchData.metadata[term][field][key2]);
              }
            }
          }
        }
      };
      lunr.MatchData.prototype.add = function(term, field, metadata) {
        if (!(term in this.metadata)) {
          this.metadata[term] = /* @__PURE__ */ Object.create(null);
          this.metadata[term][field] = metadata;
          return;
        }
        if (!(field in this.metadata[term])) {
          this.metadata[term][field] = metadata;
          return;
        }
        var metadataKeys = Object.keys(metadata);
        for (var i2 = 0; i2 < metadataKeys.length; i2++) {
          var key2 = metadataKeys[i2];
          if (key2 in this.metadata[term][field]) {
            this.metadata[term][field][key2] = this.metadata[term][field][key2].concat(metadata[key2]);
          } else {
            this.metadata[term][field][key2] = metadata[key2];
          }
        }
      };
      lunr.Query = function(allFields) {
        this.clauses = [];
        this.allFields = allFields;
      };
      lunr.Query.wildcard = new String("*");
      lunr.Query.wildcard.NONE = 0;
      lunr.Query.wildcard.LEADING = 1;
      lunr.Query.wildcard.TRAILING = 2;
      lunr.Query.presence = {
        /**
         * Term's presence in a document is optional, this is the default value.
         */
        OPTIONAL: 1,
        /**
         * Term's presence in a document is required, documents that do not contain
         * this term will not be returned.
         */
        REQUIRED: 2,
        /**
         * Term's presence in a document is prohibited, documents that do contain
         * this term will not be returned.
         */
        PROHIBITED: 3
      };
      lunr.Query.prototype.clause = function(clause) {
        if (!("fields" in clause)) {
          clause.fields = this.allFields;
        }
        if (!("boost" in clause)) {
          clause.boost = 1;
        }
        if (!("usePipeline" in clause)) {
          clause.usePipeline = true;
        }
        if (!("wildcard" in clause)) {
          clause.wildcard = lunr.Query.wildcard.NONE;
        }
        if (clause.wildcard & lunr.Query.wildcard.LEADING && clause.term.charAt(0) != lunr.Query.wildcard) {
          clause.term = "*" + clause.term;
        }
        if (clause.wildcard & lunr.Query.wildcard.TRAILING && clause.term.slice(-1) != lunr.Query.wildcard) {
          clause.term = "" + clause.term + "*";
        }
        if (!("presence" in clause)) {
          clause.presence = lunr.Query.presence.OPTIONAL;
        }
        this.clauses.push(clause);
        return this;
      };
      lunr.Query.prototype.isNegated = function() {
        for (var i2 = 0; i2 < this.clauses.length; i2++) {
          if (this.clauses[i2].presence != lunr.Query.presence.PROHIBITED) {
            return false;
          }
        }
        return true;
      };
      lunr.Query.prototype.term = function(term, options) {
        if (Array.isArray(term)) {
          term.forEach(function(t) {
            this.term(t, lunr.utils.clone(options));
          }, this);
          return this;
        }
        var clause = options || {};
        clause.term = term.toString();
        this.clause(clause);
        return this;
      };
      lunr.QueryParseError = function(message, start, end) {
        this.name = "QueryParseError";
        this.message = message;
        this.start = start;
        this.end = end;
      };
      lunr.QueryParseError.prototype = new Error();
      lunr.QueryLexer = function(str) {
        this.lexemes = [];
        this.str = str;
        this.length = str.length;
        this.pos = 0;
        this.start = 0;
        this.escapeCharPositions = [];
      };
      lunr.QueryLexer.prototype.run = function() {
        var state = lunr.QueryLexer.lexText;
        while (state) {
          state = state(this);
        }
      };
      lunr.QueryLexer.prototype.sliceString = function() {
        var subSlices = [], sliceStart = this.start, sliceEnd = this.pos;
        for (var i2 = 0; i2 < this.escapeCharPositions.length; i2++) {
          sliceEnd = this.escapeCharPositions[i2];
          subSlices.push(this.str.slice(sliceStart, sliceEnd));
          sliceStart = sliceEnd + 1;
        }
        subSlices.push(this.str.slice(sliceStart, this.pos));
        this.escapeCharPositions.length = 0;
        return subSlices.join("");
      };
      lunr.QueryLexer.prototype.emit = function(type) {
        this.lexemes.push({
          type,
          str: this.sliceString(),
          start: this.start,
          end: this.pos
        });
        this.start = this.pos;
      };
      lunr.QueryLexer.prototype.escapeCharacter = function() {
        this.escapeCharPositions.push(this.pos - 1);
        this.pos += 1;
      };
      lunr.QueryLexer.prototype.next = function() {
        if (this.pos >= this.length) {
          return lunr.QueryLexer.EOS;
        }
        var char2 = this.str.charAt(this.pos);
        this.pos += 1;
        return char2;
      };
      lunr.QueryLexer.prototype.width = function() {
        return this.pos - this.start;
      };
      lunr.QueryLexer.prototype.ignore = function() {
        if (this.start == this.pos) {
          this.pos += 1;
        }
        this.start = this.pos;
      };
      lunr.QueryLexer.prototype.backup = function() {
        this.pos -= 1;
      };
      lunr.QueryLexer.prototype.acceptDigitRun = function() {
        var char2, charCode;
        do {
          char2 = this.next();
          charCode = char2.charCodeAt(0);
        } while (charCode > 47 && charCode < 58);
        if (char2 != lunr.QueryLexer.EOS) {
          this.backup();
        }
      };
      lunr.QueryLexer.prototype.more = function() {
        return this.pos < this.length;
      };
      lunr.QueryLexer.EOS = "EOS";
      lunr.QueryLexer.FIELD = "FIELD";
      lunr.QueryLexer.TERM = "TERM";
      lunr.QueryLexer.EDIT_DISTANCE = "EDIT_DISTANCE";
      lunr.QueryLexer.BOOST = "BOOST";
      lunr.QueryLexer.PRESENCE = "PRESENCE";
      lunr.QueryLexer.lexField = function(lexer) {
        lexer.backup();
        lexer.emit(lunr.QueryLexer.FIELD);
        lexer.ignore();
        return lunr.QueryLexer.lexText;
      };
      lunr.QueryLexer.lexTerm = function(lexer) {
        if (lexer.width() > 1) {
          lexer.backup();
          lexer.emit(lunr.QueryLexer.TERM);
        }
        lexer.ignore();
        if (lexer.more()) {
          return lunr.QueryLexer.lexText;
        }
      };
      lunr.QueryLexer.lexEditDistance = function(lexer) {
        lexer.ignore();
        lexer.acceptDigitRun();
        lexer.emit(lunr.QueryLexer.EDIT_DISTANCE);
        return lunr.QueryLexer.lexText;
      };
      lunr.QueryLexer.lexBoost = function(lexer) {
        lexer.ignore();
        lexer.acceptDigitRun();
        lexer.emit(lunr.QueryLexer.BOOST);
        return lunr.QueryLexer.lexText;
      };
      lunr.QueryLexer.lexEOS = function(lexer) {
        if (lexer.width() > 0) {
          lexer.emit(lunr.QueryLexer.TERM);
        }
      };
      lunr.QueryLexer.termSeparator = lunr.tokenizer.separator;
      lunr.QueryLexer.lexText = function(lexer) {
        while (true) {
          var char2 = lexer.next();
          if (char2 == lunr.QueryLexer.EOS) {
            return lunr.QueryLexer.lexEOS;
          }
          if (char2.charCodeAt(0) == 92) {
            lexer.escapeCharacter();
            continue;
          }
          if (char2 == ":") {
            return lunr.QueryLexer.lexField;
          }
          if (char2 == "~") {
            lexer.backup();
            if (lexer.width() > 0) {
              lexer.emit(lunr.QueryLexer.TERM);
            }
            return lunr.QueryLexer.lexEditDistance;
          }
          if (char2 == "^") {
            lexer.backup();
            if (lexer.width() > 0) {
              lexer.emit(lunr.QueryLexer.TERM);
            }
            return lunr.QueryLexer.lexBoost;
          }
          if (char2 == "+" && lexer.width() === 1) {
            lexer.emit(lunr.QueryLexer.PRESENCE);
            return lunr.QueryLexer.lexText;
          }
          if (char2 == "-" && lexer.width() === 1) {
            lexer.emit(lunr.QueryLexer.PRESENCE);
            return lunr.QueryLexer.lexText;
          }
          if (char2.match(lunr.QueryLexer.termSeparator)) {
            return lunr.QueryLexer.lexTerm;
          }
        }
      };
      lunr.QueryParser = function(str, query) {
        this.lexer = new lunr.QueryLexer(str);
        this.query = query;
        this.currentClause = {};
        this.lexemeIdx = 0;
      };
      lunr.QueryParser.prototype.parse = function() {
        this.lexer.run();
        this.lexemes = this.lexer.lexemes;
        var state = lunr.QueryParser.parseClause;
        while (state) {
          state = state(this);
        }
        return this.query;
      };
      lunr.QueryParser.prototype.peekLexeme = function() {
        return this.lexemes[this.lexemeIdx];
      };
      lunr.QueryParser.prototype.consumeLexeme = function() {
        var lexeme = this.peekLexeme();
        this.lexemeIdx += 1;
        return lexeme;
      };
      lunr.QueryParser.prototype.nextClause = function() {
        var completedClause = this.currentClause;
        this.query.clause(completedClause);
        this.currentClause = {};
      };
      lunr.QueryParser.parseClause = function(parser) {
        var lexeme = parser.peekLexeme();
        if (lexeme == void 0) {
          return;
        }
        switch (lexeme.type) {
          case lunr.QueryLexer.PRESENCE:
            return lunr.QueryParser.parsePresence;
          case lunr.QueryLexer.FIELD:
            return lunr.QueryParser.parseField;
          case lunr.QueryLexer.TERM:
            return lunr.QueryParser.parseTerm;
          default:
            var errorMessage = "expected either a field or a term, found " + lexeme.type;
            if (lexeme.str.length >= 1) {
              errorMessage += " with value '" + lexeme.str + "'";
            }
            throw new lunr.QueryParseError(errorMessage, lexeme.start, lexeme.end);
        }
      };
      lunr.QueryParser.parsePresence = function(parser) {
        var lexeme = parser.consumeLexeme();
        if (lexeme == void 0) {
          return;
        }
        switch (lexeme.str) {
          case "-":
            parser.currentClause.presence = lunr.Query.presence.PROHIBITED;
            break;
          case "+":
            parser.currentClause.presence = lunr.Query.presence.REQUIRED;
            break;
          default:
            var errorMessage = "unrecognised presence operator'" + lexeme.str + "'";
            throw new lunr.QueryParseError(errorMessage, lexeme.start, lexeme.end);
        }
        var nextLexeme = parser.peekLexeme();
        if (nextLexeme == void 0) {
          var errorMessage = "expecting term or field, found nothing";
          throw new lunr.QueryParseError(errorMessage, lexeme.start, lexeme.end);
        }
        switch (nextLexeme.type) {
          case lunr.QueryLexer.FIELD:
            return lunr.QueryParser.parseField;
          case lunr.QueryLexer.TERM:
            return lunr.QueryParser.parseTerm;
          default:
            var errorMessage = "expecting term or field, found '" + nextLexeme.type + "'";
            throw new lunr.QueryParseError(errorMessage, nextLexeme.start, nextLexeme.end);
        }
      };
      lunr.QueryParser.parseField = function(parser) {
        var lexeme = parser.consumeLexeme();
        if (lexeme == void 0) {
          return;
        }
        if (parser.query.allFields.indexOf(lexeme.str) == -1) {
          var possibleFields = parser.query.allFields.map(function(f2) {
            return "'" + f2 + "'";
          }).join(", "), errorMessage = "unrecognised field '" + lexeme.str + "', possible fields: " + possibleFields;
          throw new lunr.QueryParseError(errorMessage, lexeme.start, lexeme.end);
        }
        parser.currentClause.fields = [lexeme.str];
        var nextLexeme = parser.peekLexeme();
        if (nextLexeme == void 0) {
          var errorMessage = "expecting term, found nothing";
          throw new lunr.QueryParseError(errorMessage, lexeme.start, lexeme.end);
        }
        switch (nextLexeme.type) {
          case lunr.QueryLexer.TERM:
            return lunr.QueryParser.parseTerm;
          default:
            var errorMessage = "expecting term, found '" + nextLexeme.type + "'";
            throw new lunr.QueryParseError(errorMessage, nextLexeme.start, nextLexeme.end);
        }
      };
      lunr.QueryParser.parseTerm = function(parser) {
        var lexeme = parser.consumeLexeme();
        if (lexeme == void 0) {
          return;
        }
        parser.currentClause.term = lexeme.str.toLowerCase();
        if (lexeme.str.indexOf("*") != -1) {
          parser.currentClause.usePipeline = false;
        }
        var nextLexeme = parser.peekLexeme();
        if (nextLexeme == void 0) {
          parser.nextClause();
          return;
        }
        switch (nextLexeme.type) {
          case lunr.QueryLexer.TERM:
            parser.nextClause();
            return lunr.QueryParser.parseTerm;
          case lunr.QueryLexer.FIELD:
            parser.nextClause();
            return lunr.QueryParser.parseField;
          case lunr.QueryLexer.EDIT_DISTANCE:
            return lunr.QueryParser.parseEditDistance;
          case lunr.QueryLexer.BOOST:
            return lunr.QueryParser.parseBoost;
          case lunr.QueryLexer.PRESENCE:
            parser.nextClause();
            return lunr.QueryParser.parsePresence;
          default:
            var errorMessage = "Unexpected lexeme type '" + nextLexeme.type + "'";
            throw new lunr.QueryParseError(errorMessage, nextLexeme.start, nextLexeme.end);
        }
      };
      lunr.QueryParser.parseEditDistance = function(parser) {
        var lexeme = parser.consumeLexeme();
        if (lexeme == void 0) {
          return;
        }
        var editDistance = parseInt(lexeme.str, 10);
        if (isNaN(editDistance)) {
          var errorMessage = "edit distance must be numeric";
          throw new lunr.QueryParseError(errorMessage, lexeme.start, lexeme.end);
        }
        parser.currentClause.editDistance = editDistance;
        var nextLexeme = parser.peekLexeme();
        if (nextLexeme == void 0) {
          parser.nextClause();
          return;
        }
        switch (nextLexeme.type) {
          case lunr.QueryLexer.TERM:
            parser.nextClause();
            return lunr.QueryParser.parseTerm;
          case lunr.QueryLexer.FIELD:
            parser.nextClause();
            return lunr.QueryParser.parseField;
          case lunr.QueryLexer.EDIT_DISTANCE:
            return lunr.QueryParser.parseEditDistance;
          case lunr.QueryLexer.BOOST:
            return lunr.QueryParser.parseBoost;
          case lunr.QueryLexer.PRESENCE:
            parser.nextClause();
            return lunr.QueryParser.parsePresence;
          default:
            var errorMessage = "Unexpected lexeme type '" + nextLexeme.type + "'";
            throw new lunr.QueryParseError(errorMessage, nextLexeme.start, nextLexeme.end);
        }
      };
      lunr.QueryParser.parseBoost = function(parser) {
        var lexeme = parser.consumeLexeme();
        if (lexeme == void 0) {
          return;
        }
        var boost = parseInt(lexeme.str, 10);
        if (isNaN(boost)) {
          var errorMessage = "boost must be numeric";
          throw new lunr.QueryParseError(errorMessage, lexeme.start, lexeme.end);
        }
        parser.currentClause.boost = boost;
        var nextLexeme = parser.peekLexeme();
        if (nextLexeme == void 0) {
          parser.nextClause();
          return;
        }
        switch (nextLexeme.type) {
          case lunr.QueryLexer.TERM:
            parser.nextClause();
            return lunr.QueryParser.parseTerm;
          case lunr.QueryLexer.FIELD:
            parser.nextClause();
            return lunr.QueryParser.parseField;
          case lunr.QueryLexer.EDIT_DISTANCE:
            return lunr.QueryParser.parseEditDistance;
          case lunr.QueryLexer.BOOST:
            return lunr.QueryParser.parseBoost;
          case lunr.QueryLexer.PRESENCE:
            parser.nextClause();
            return lunr.QueryParser.parsePresence;
          default:
            var errorMessage = "Unexpected lexeme type '" + nextLexeme.type + "'";
            throw new lunr.QueryParseError(errorMessage, nextLexeme.start, nextLexeme.end);
        }
      };
      (function(root, factory) {
        if (typeof define === "function" && define.amd) {
          define(factory);
        } else if (typeof exports === "object") {
          module.exports = factory();
        } else {
          root.lunr = factory();
        }
      })(this, function() {
        return lunr;
      });
    })();
  }
});

// node_modules/call-me-maybe/src/next.js
var require_next = __commonJS({
  "node_modules/call-me-maybe/src/next.js"(exports, module) {
    "use strict";
    function makeNext() {
      if (typeof process === "object" && typeof process.nextTick === "function") {
        return process.nextTick;
      } else if (typeof setImmediate === "function") {
        return setImmediate;
      } else {
        return function next2(f2) {
          setTimeout(f2, 0);
        };
      }
    }
    module.exports = makeNext();
  }
});

// node_modules/call-me-maybe/src/maybe.js
var require_maybe = __commonJS({
  "node_modules/call-me-maybe/src/maybe.js"(exports, module) {
    "use strict";
    var next2 = require_next();
    module.exports = function maybe(cb, promise) {
      if (cb) {
        promise.then(function(result) {
          next2(function() {
            cb(null, result);
          });
        }, function(err) {
          next2(function() {
            cb(err);
          });
        });
        return void 0;
      } else {
        return promise;
      }
    };
  }
});

// node_modules/fast-safe-stringify/index.js
var require_fast_safe_stringify = __commonJS({
  "node_modules/fast-safe-stringify/index.js"(exports, module) {
    module.exports = stringify5;
    stringify5.default = stringify5;
    stringify5.stable = deterministicStringify;
    stringify5.stableStringify = deterministicStringify;
    var LIMIT_REPLACE_NODE = "[...]";
    var CIRCULAR_REPLACE_NODE = "[Circular]";
    var arr = [];
    var replacerStack = [];
    function defaultOptions() {
      return {
        depthLimit: Number.MAX_SAFE_INTEGER,
        edgesLimit: Number.MAX_SAFE_INTEGER
      };
    }
    function stringify5(obj, replacer, spacer, options) {
      if (typeof options === "undefined") {
        options = defaultOptions();
      }
      decirc(obj, "", 0, [], void 0, 0, options);
      var res;
      try {
        if (replacerStack.length === 0) {
          res = JSON.stringify(obj, replacer, spacer);
        } else {
          res = JSON.stringify(obj, replaceGetterValues(replacer), spacer);
        }
      } catch (_15) {
        return JSON.stringify("[unable to serialize, circular reference is too complex to analyze]");
      } finally {
        while (arr.length !== 0) {
          var part = arr.pop();
          if (part.length === 4) {
            Object.defineProperty(part[0], part[1], part[3]);
          } else {
            part[0][part[1]] = part[2];
          }
        }
      }
      return res;
    }
    function setReplace(replace3, val, k2, parent) {
      var propertyDescriptor = Object.getOwnPropertyDescriptor(parent, k2);
      if (propertyDescriptor.get !== void 0) {
        if (propertyDescriptor.configurable) {
          Object.defineProperty(parent, k2, { value: replace3 });
          arr.push([parent, k2, val, propertyDescriptor]);
        } else {
          replacerStack.push([val, k2, replace3]);
        }
      } else {
        parent[k2] = replace3;
        arr.push([parent, k2, val]);
      }
    }
    function decirc(val, k2, edgeIndex, stack, parent, depth, options) {
      depth += 1;
      var i2;
      if (typeof val === "object" && val !== null) {
        for (i2 = 0; i2 < stack.length; i2++) {
          if (stack[i2] === val) {
            setReplace(CIRCULAR_REPLACE_NODE, val, k2, parent);
            return;
          }
        }
        if (typeof options.depthLimit !== "undefined" && depth > options.depthLimit) {
          setReplace(LIMIT_REPLACE_NODE, val, k2, parent);
          return;
        }
        if (typeof options.edgesLimit !== "undefined" && edgeIndex + 1 > options.edgesLimit) {
          setReplace(LIMIT_REPLACE_NODE, val, k2, parent);
          return;
        }
        stack.push(val);
        if (Array.isArray(val)) {
          for (i2 = 0; i2 < val.length; i2++) {
            decirc(val[i2], i2, i2, stack, val, depth, options);
          }
        } else {
          var keys2 = Object.keys(val);
          for (i2 = 0; i2 < keys2.length; i2++) {
            var key2 = keys2[i2];
            decirc(val[key2], key2, i2, stack, val, depth, options);
          }
        }
        stack.pop();
      }
    }
    function compareFunction(a2, b2) {
      if (a2 < b2) {
        return -1;
      }
      if (a2 > b2) {
        return 1;
      }
      return 0;
    }
    function deterministicStringify(obj, replacer, spacer, options) {
      if (typeof options === "undefined") {
        options = defaultOptions();
      }
      var tmp = deterministicDecirc(obj, "", 0, [], void 0, 0, options) || obj;
      var res;
      try {
        if (replacerStack.length === 0) {
          res = JSON.stringify(tmp, replacer, spacer);
        } else {
          res = JSON.stringify(tmp, replaceGetterValues(replacer), spacer);
        }
      } catch (_15) {
        return JSON.stringify("[unable to serialize, circular reference is too complex to analyze]");
      } finally {
        while (arr.length !== 0) {
          var part = arr.pop();
          if (part.length === 4) {
            Object.defineProperty(part[0], part[1], part[3]);
          } else {
            part[0][part[1]] = part[2];
          }
        }
      }
      return res;
    }
    function deterministicDecirc(val, k2, edgeIndex, stack, parent, depth, options) {
      depth += 1;
      var i2;
      if (typeof val === "object" && val !== null) {
        for (i2 = 0; i2 < stack.length; i2++) {
          if (stack[i2] === val) {
            setReplace(CIRCULAR_REPLACE_NODE, val, k2, parent);
            return;
          }
        }
        try {
          if (typeof val.toJSON === "function") {
            return;
          }
        } catch (_15) {
          return;
        }
        if (typeof options.depthLimit !== "undefined" && depth > options.depthLimit) {
          setReplace(LIMIT_REPLACE_NODE, val, k2, parent);
          return;
        }
        if (typeof options.edgesLimit !== "undefined" && edgeIndex + 1 > options.edgesLimit) {
          setReplace(LIMIT_REPLACE_NODE, val, k2, parent);
          return;
        }
        stack.push(val);
        if (Array.isArray(val)) {
          for (i2 = 0; i2 < val.length; i2++) {
            deterministicDecirc(val[i2], i2, i2, stack, val, depth, options);
          }
        } else {
          var tmp = {};
          var keys2 = Object.keys(val).sort(compareFunction);
          for (i2 = 0; i2 < keys2.length; i2++) {
            var key2 = keys2[i2];
            deterministicDecirc(val[key2], key2, i2, stack, val, depth, options);
            tmp[key2] = val[key2];
          }
          if (typeof parent !== "undefined") {
            arr.push([parent, k2, val]);
            parent[k2] = tmp;
          } else {
            return tmp;
          }
        }
        stack.pop();
      }
    }
    function replaceGetterValues(replacer) {
      replacer = typeof replacer !== "undefined" ? replacer : function(k2, v2) {
        return v2;
      };
      return function(key2, val) {
        if (replacerStack.length > 0) {
          for (var i2 = 0; i2 < replacerStack.length; i2++) {
            var part = replacerStack[i2];
            if (part[1] === key2 && part[0] === val) {
              val = part[2];
              replacerStack.splice(i2, 1);
              break;
            }
          }
        }
        return replacer.call(this, key2, val);
      };
    }
  }
});

// node_modules/stickyfill/index.js
var require_stickyfill = __commonJS({
  "node_modules/stickyfill/index.js"(exports, module) {
    module.exports = (function(doc, win) {
      if (!doc) {
        doc = document;
      }
      if (!win) {
        win = window;
      }
      var watchArray = [], scroll, initialized = false, html = doc.documentElement, noop3 = function() {
      }, checkTimer, hiddenPropertyName = "hidden", visibilityChangeEventName = "visibilitychange";
      if (doc.webkitHidden !== void 0) {
        hiddenPropertyName = "webkitHidden";
        visibilityChangeEventName = "webkitvisibilitychange";
      }
      if (!win.getComputedStyle) {
        seppuku();
      }
      var prefixes = ["", "-webkit-", "-moz-", "-ms-"], block = document.createElement("div");
      for (var i2 = prefixes.length - 1; i2 >= 0; i2--) {
        try {
          block.style.position = prefixes[i2] + "sticky";
        } catch (e) {
        }
        if (block.style.position != "") {
          seppuku();
        }
      }
      updateScrollPos();
      function seppuku() {
        init = add = rebuild = pause = stop = kill = noop3;
      }
      function mergeObjects(targetObj, sourceObject) {
        for (key in sourceObject) {
          if (sourceObject.hasOwnProperty(key)) {
            targetObj[key] = sourceObject[key];
          }
        }
      }
      function parseNumeric(val) {
        return parseFloat(val) || 0;
      }
      function updateScrollPos() {
        scroll = {
          top: win.pageYOffset,
          left: win.pageXOffset
        };
      }
      function onScroll2() {
        if (win.pageXOffset != scroll.left) {
          updateScrollPos();
          rebuild();
          return;
        }
        if (win.pageYOffset != scroll.top) {
          updateScrollPos();
          recalcAllPos();
        }
      }
      function onWheel(event) {
        setTimeout(function() {
          if (win.pageYOffset != scroll.top) {
            scroll.top = win.pageYOffset;
            recalcAllPos();
          }
        }, 0);
      }
      function recalcAllPos() {
        for (var i3 = watchArray.length - 1; i3 >= 0; i3--) {
          recalcElementPos(watchArray[i3]);
        }
      }
      function recalcElementPos(el) {
        if (!el.inited) return;
        var currentMode = scroll.top <= el.limit.start ? 0 : scroll.top >= el.limit.end ? 2 : 1;
        if (el.mode != currentMode) {
          switchElementMode(el, currentMode);
        }
      }
      function fastCheck() {
        for (var i3 = watchArray.length - 1; i3 >= 0; i3--) {
          if (!watchArray[i3].inited) continue;
          var deltaTop = Math.abs(getDocOffsetTop(watchArray[i3].clone) - watchArray[i3].docOffsetTop), deltaHeight = Math.abs(watchArray[i3].parent.node.offsetHeight - watchArray[i3].parent.height);
          if (deltaTop >= 2 || deltaHeight >= 2) return false;
        }
        return true;
      }
      function initElement(el) {
        if (isNaN(parseFloat(el.computed.top)) || el.isCell) return;
        el.inited = true;
        if (!el.clone) clone(el);
        if (el.parent.computed.position != "absolute" && el.parent.computed.position != "relative") el.parent.node.style.position = "relative";
        recalcElementPos(el);
        el.parent.height = el.parent.node.offsetHeight;
        el.docOffsetTop = getDocOffsetTop(el.clone);
      }
      function deinitElement(el) {
        var deinitParent = true;
        el.clone && killClone(el);
        mergeObjects(el.node.style, el.css);
        for (var i3 = watchArray.length - 1; i3 >= 0; i3--) {
          if (watchArray[i3].node !== el.node && watchArray[i3].parent.node === el.parent.node) {
            deinitParent = false;
            break;
          }
        }
        ;
        if (deinitParent) el.parent.node.style.position = el.parent.css.position;
        el.mode = -1;
      }
      function initAll() {
        for (var i3 = watchArray.length - 1; i3 >= 0; i3--) {
          initElement(watchArray[i3]);
        }
      }
      function deinitAll() {
        for (var i3 = watchArray.length - 1; i3 >= 0; i3--) {
          deinitElement(watchArray[i3]);
        }
      }
      function switchElementMode(el, mode) {
        var nodeStyle = el.node.style;
        switch (mode) {
          case 0:
            nodeStyle.position = "absolute";
            nodeStyle.left = el.offset.left + "px";
            nodeStyle.right = el.offset.right + "px";
            nodeStyle.top = el.offset.top + "px";
            nodeStyle.bottom = "auto";
            nodeStyle.width = "auto";
            nodeStyle.marginLeft = 0;
            nodeStyle.marginRight = 0;
            nodeStyle.marginTop = 0;
            break;
          case 1:
            nodeStyle.position = "fixed";
            nodeStyle.left = el.box.left + "px";
            nodeStyle.right = el.box.right + "px";
            nodeStyle.top = el.css.top;
            nodeStyle.bottom = "auto";
            nodeStyle.width = "auto";
            nodeStyle.marginLeft = 0;
            nodeStyle.marginRight = 0;
            nodeStyle.marginTop = 0;
            break;
          case 2:
            nodeStyle.position = "absolute";
            nodeStyle.left = el.offset.left + "px";
            nodeStyle.right = el.offset.right + "px";
            nodeStyle.top = "auto";
            nodeStyle.bottom = 0;
            nodeStyle.width = "auto";
            nodeStyle.marginLeft = 0;
            nodeStyle.marginRight = 0;
            break;
        }
        el.mode = mode;
      }
      function clone(el) {
        el.clone = document.createElement("div");
        var refElement = el.node.nextSibling || el.node, cloneStyle = el.clone.style;
        cloneStyle.height = el.height + "px";
        cloneStyle.width = el.width + "px";
        cloneStyle.marginTop = el.computed.marginTop;
        cloneStyle.marginBottom = el.computed.marginBottom;
        cloneStyle.marginLeft = el.computed.marginLeft;
        cloneStyle.marginRight = el.computed.marginRight;
        cloneStyle.padding = cloneStyle.border = cloneStyle.borderSpacing = 0;
        cloneStyle.fontSize = "1em";
        cloneStyle.position = "static";
        cloneStyle.cssFloat = el.computed.cssFloat;
        el.node.parentNode.insertBefore(el.clone, refElement);
      }
      function killClone(el) {
        el.clone.parentNode.removeChild(el.clone);
        el.clone = void 0;
      }
      function getElementParams(node2) {
        var computedStyle = getComputedStyle(node2), parentNode = node2.parentNode, parentComputedStyle = getComputedStyle(parentNode), cachedPosition = node2.style.position;
        node2.style.position = "relative";
        var computed3 = {
          top: computedStyle.top,
          marginTop: computedStyle.marginTop,
          marginBottom: computedStyle.marginBottom,
          marginLeft: computedStyle.marginLeft,
          marginRight: computedStyle.marginRight,
          cssFloat: computedStyle.cssFloat
        }, numeric = {
          top: parseNumeric(computedStyle.top),
          marginBottom: parseNumeric(computedStyle.marginBottom),
          paddingLeft: parseNumeric(computedStyle.paddingLeft),
          paddingRight: parseNumeric(computedStyle.paddingRight),
          borderLeftWidth: parseNumeric(computedStyle.borderLeftWidth),
          borderRightWidth: parseNumeric(computedStyle.borderRightWidth)
        };
        node2.style.position = cachedPosition;
        var css = {
          position: node2.style.position,
          top: node2.style.top,
          bottom: node2.style.bottom,
          left: node2.style.left,
          right: node2.style.right,
          width: node2.style.width,
          marginTop: node2.style.marginTop,
          marginLeft: node2.style.marginLeft,
          marginRight: node2.style.marginRight
        }, nodeOffset = getElementOffset(node2), parentOffset = getElementOffset(parentNode), parent = {
          node: parentNode,
          css: {
            position: parentNode.style.position
          },
          computed: {
            position: parentComputedStyle.position
          },
          numeric: {
            borderLeftWidth: parseNumeric(parentComputedStyle.borderLeftWidth),
            borderRightWidth: parseNumeric(parentComputedStyle.borderRightWidth),
            borderTopWidth: parseNumeric(parentComputedStyle.borderTopWidth),
            borderBottomWidth: parseNumeric(parentComputedStyle.borderBottomWidth)
          }
        }, el = {
          node: node2,
          box: {
            left: nodeOffset.win.left,
            right: html.clientWidth - nodeOffset.win.right
          },
          offset: {
            top: nodeOffset.win.top - parentOffset.win.top - parent.numeric.borderTopWidth,
            left: nodeOffset.win.left - parentOffset.win.left - parent.numeric.borderLeftWidth,
            right: -nodeOffset.win.right + parentOffset.win.right - parent.numeric.borderRightWidth
          },
          css,
          isCell: computedStyle.display == "table-cell",
          computed: computed3,
          numeric,
          width: nodeOffset.win.right - nodeOffset.win.left,
          height: nodeOffset.win.bottom - nodeOffset.win.top,
          mode: -1,
          inited: false,
          parent,
          limit: {
            start: nodeOffset.doc.top - numeric.top,
            end: parentOffset.doc.top + parentNode.offsetHeight - parent.numeric.borderBottomWidth - node2.offsetHeight - numeric.top - numeric.marginBottom
          }
        };
        return el;
      }
      function getDocOffsetTop(node2) {
        var docOffsetTop = 0;
        while (node2) {
          docOffsetTop += node2.offsetTop;
          node2 = node2.offsetParent;
        }
        return docOffsetTop;
      }
      function getElementOffset(node2) {
        var box2 = node2.getBoundingClientRect();
        return {
          doc: {
            top: box2.top + win.pageYOffset,
            left: box2.left + win.pageXOffset
          },
          win: box2
        };
      }
      function startFastCheckTimer() {
        checkTimer = setInterval(function() {
          !fastCheck() && rebuild();
        }, 500);
      }
      function stopFastCheckTimer() {
        clearInterval(checkTimer);
      }
      function handlePageVisibilityChange() {
        if (!initialized) return;
        if (document[hiddenPropertyName]) {
          stopFastCheckTimer();
        } else {
          startFastCheckTimer();
        }
      }
      function init() {
        if (initialized) return;
        updateScrollPos();
        initAll();
        win.addEventListener("scroll", onScroll2);
        win.addEventListener("wheel", onWheel);
        win.addEventListener("resize", rebuild);
        win.addEventListener("orientationchange", rebuild);
        doc.addEventListener(visibilityChangeEventName, handlePageVisibilityChange);
        startFastCheckTimer();
        initialized = true;
      }
      function rebuild() {
        if (!initialized) return;
        deinitAll();
        for (var i3 = watchArray.length - 1; i3 >= 0; i3--) {
          watchArray[i3] = getElementParams(watchArray[i3].node);
        }
        initAll();
      }
      function pause() {
        win.removeEventListener("scroll", onScroll2);
        win.removeEventListener("wheel", onWheel);
        win.removeEventListener("resize", rebuild);
        win.removeEventListener("orientationchange", rebuild);
        doc.removeEventListener(visibilityChangeEventName, handlePageVisibilityChange);
        stopFastCheckTimer();
        initialized = false;
      }
      function stop() {
        pause();
        deinitAll();
      }
      function kill() {
        stop();
        while (watchArray.length) {
          watchArray.pop();
        }
      }
      function add(node2) {
        for (var i3 = watchArray.length - 1; i3 >= 0; i3--) {
          if (watchArray[i3].node === node2) return;
        }
        ;
        var el = getElementParams(node2);
        watchArray.push(el);
        if (!initialized) {
          init();
        } else {
          initElement(el);
        }
      }
      function remove4(node2) {
        for (var i3 = watchArray.length - 1; i3 >= 0; i3--) {
          if (watchArray[i3].node === node2) {
            deinitElement(watchArray[i3]);
            watchArray.splice(i3, 1);
          }
        }
        ;
      }
      return {
        stickies: watchArray,
        add,
        remove: remove4,
        init,
        rebuild,
        pause,
        stop,
        kill
      };
    });
  }
});

// node_modules/yaml/browser/dist/nodes/identity.js
function isCollection(node2) {
  if (node2 && typeof node2 === "object")
    switch (node2[NODE_TYPE]) {
      case MAP:
      case SEQ:
        return true;
    }
  return false;
}
function isNode(node2) {
  if (node2 && typeof node2 === "object")
    switch (node2[NODE_TYPE]) {
      case ALIAS:
      case MAP:
      case SCALAR:
      case SEQ:
        return true;
    }
  return false;
}
var ALIAS, DOC, MAP, PAIR, SCALAR, SEQ, NODE_TYPE, isAlias, isDocument, isMap, isPair, isScalar, isSeq, hasAnchor;
var init_identity = __esm({
  "node_modules/yaml/browser/dist/nodes/identity.js"() {
    ALIAS = Symbol.for("yaml.alias");
    DOC = Symbol.for("yaml.document");
    MAP = Symbol.for("yaml.map");
    PAIR = Symbol.for("yaml.pair");
    SCALAR = Symbol.for("yaml.scalar");
    SEQ = Symbol.for("yaml.seq");
    NODE_TYPE = Symbol.for("yaml.node.type");
    isAlias = (node2) => !!node2 && typeof node2 === "object" && node2[NODE_TYPE] === ALIAS;
    isDocument = (node2) => !!node2 && typeof node2 === "object" && node2[NODE_TYPE] === DOC;
    isMap = (node2) => !!node2 && typeof node2 === "object" && node2[NODE_TYPE] === MAP;
    isPair = (node2) => !!node2 && typeof node2 === "object" && node2[NODE_TYPE] === PAIR;
    isScalar = (node2) => !!node2 && typeof node2 === "object" && node2[NODE_TYPE] === SCALAR;
    isSeq = (node2) => !!node2 && typeof node2 === "object" && node2[NODE_TYPE] === SEQ;
    hasAnchor = (node2) => (isScalar(node2) || isCollection(node2)) && !!node2.anchor;
  }
});

// node_modules/yaml/browser/dist/visit.js
function visit(node2, visitor) {
  const visitor_ = initVisitor(visitor);
  if (isDocument(node2)) {
    const cd = visit_(null, node2.contents, visitor_, Object.freeze([node2]));
    if (cd === REMOVE)
      node2.contents = null;
  } else
    visit_(null, node2, visitor_, Object.freeze([]));
}
function visit_(key2, node2, visitor, path) {
  const ctrl = callVisitor(key2, node2, visitor, path);
  if (isNode(ctrl) || isPair(ctrl)) {
    replaceNode(key2, path, ctrl);
    return visit_(key2, ctrl, visitor, path);
  }
  if (typeof ctrl !== "symbol") {
    if (isCollection(node2)) {
      path = Object.freeze(path.concat(node2));
      for (let i2 = 0; i2 < node2.items.length; ++i2) {
        const ci = visit_(i2, node2.items[i2], visitor, path);
        if (typeof ci === "number")
          i2 = ci - 1;
        else if (ci === BREAK)
          return BREAK;
        else if (ci === REMOVE) {
          node2.items.splice(i2, 1);
          i2 -= 1;
        }
      }
    } else if (isPair(node2)) {
      path = Object.freeze(path.concat(node2));
      const ck = visit_("key", node2.key, visitor, path);
      if (ck === BREAK)
        return BREAK;
      else if (ck === REMOVE)
        node2.key = null;
      const cv = visit_("value", node2.value, visitor, path);
      if (cv === BREAK)
        return BREAK;
      else if (cv === REMOVE)
        node2.value = null;
    }
  }
  return ctrl;
}
async function visitAsync(node2, visitor) {
  const visitor_ = initVisitor(visitor);
  if (isDocument(node2)) {
    const cd = await visitAsync_(null, node2.contents, visitor_, Object.freeze([node2]));
    if (cd === REMOVE)
      node2.contents = null;
  } else
    await visitAsync_(null, node2, visitor_, Object.freeze([]));
}
async function visitAsync_(key2, node2, visitor, path) {
  const ctrl = await callVisitor(key2, node2, visitor, path);
  if (isNode(ctrl) || isPair(ctrl)) {
    replaceNode(key2, path, ctrl);
    return visitAsync_(key2, ctrl, visitor, path);
  }
  if (typeof ctrl !== "symbol") {
    if (isCollection(node2)) {
      path = Object.freeze(path.concat(node2));
      for (let i2 = 0; i2 < node2.items.length; ++i2) {
        const ci = await visitAsync_(i2, node2.items[i2], visitor, path);
        if (typeof ci === "number")
          i2 = ci - 1;
        else if (ci === BREAK)
          return BREAK;
        else if (ci === REMOVE) {
          node2.items.splice(i2, 1);
          i2 -= 1;
        }
      }
    } else if (isPair(node2)) {
      path = Object.freeze(path.concat(node2));
      const ck = await visitAsync_("key", node2.key, visitor, path);
      if (ck === BREAK)
        return BREAK;
      else if (ck === REMOVE)
        node2.key = null;
      const cv = await visitAsync_("value", node2.value, visitor, path);
      if (cv === BREAK)
        return BREAK;
      else if (cv === REMOVE)
        node2.value = null;
    }
  }
  return ctrl;
}
function initVisitor(visitor) {
  if (typeof visitor === "object" && (visitor.Collection || visitor.Node || visitor.Value)) {
    return Object.assign({
      Alias: visitor.Node,
      Map: visitor.Node,
      Scalar: visitor.Node,
      Seq: visitor.Node
    }, visitor.Value && {
      Map: visitor.Value,
      Scalar: visitor.Value,
      Seq: visitor.Value
    }, visitor.Collection && {
      Map: visitor.Collection,
      Seq: visitor.Collection
    }, visitor);
  }
  return visitor;
}
function callVisitor(key2, node2, visitor, path) {
  if (typeof visitor === "function")
    return visitor(key2, node2, path);
  if (isMap(node2))
    return visitor.Map?.(key2, node2, path);
  if (isSeq(node2))
    return visitor.Seq?.(key2, node2, path);
  if (isPair(node2))
    return visitor.Pair?.(key2, node2, path);
  if (isScalar(node2))
    return visitor.Scalar?.(key2, node2, path);
  if (isAlias(node2))
    return visitor.Alias?.(key2, node2, path);
  return void 0;
}
function replaceNode(key2, path, node2) {
  const parent = path[path.length - 1];
  if (isCollection(parent)) {
    parent.items[key2] = node2;
  } else if (isPair(parent)) {
    if (key2 === "key")
      parent.key = node2;
    else
      parent.value = node2;
  } else if (isDocument(parent)) {
    parent.contents = node2;
  } else {
    const pt2 = isAlias(parent) ? "alias" : "scalar";
    throw new Error(`Cannot replace node with ${pt2} parent`);
  }
}
var BREAK, SKIP, REMOVE;
var init_visit = __esm({
  "node_modules/yaml/browser/dist/visit.js"() {
    init_identity();
    BREAK = Symbol("break visit");
    SKIP = Symbol("skip children");
    REMOVE = Symbol("remove node");
    visit.BREAK = BREAK;
    visit.SKIP = SKIP;
    visit.REMOVE = REMOVE;
    visitAsync.BREAK = BREAK;
    visitAsync.SKIP = SKIP;
    visitAsync.REMOVE = REMOVE;
  }
});

// node_modules/yaml/browser/dist/doc/directives.js
var escapeChars, escapeTagName, Directives;
var init_directives = __esm({
  "node_modules/yaml/browser/dist/doc/directives.js"() {
    init_identity();
    init_visit();
    escapeChars = {
      "!": "%21",
      ",": "%2C",
      "[": "%5B",
      "]": "%5D",
      "{": "%7B",
      "}": "%7D"
    };
    escapeTagName = (tn) => tn.replace(/[!,[\]{}]/g, (ch) => escapeChars[ch]);
    Directives = class _Directives {
      constructor(yaml, tags) {
        this.docStart = null;
        this.docEnd = false;
        this.yaml = Object.assign({}, _Directives.defaultYaml, yaml);
        this.tags = Object.assign({}, _Directives.defaultTags, tags);
      }
      clone() {
        const copy2 = new _Directives(this.yaml, this.tags);
        copy2.docStart = this.docStart;
        return copy2;
      }
      /**
       * During parsing, get a Directives instance for the current document and
       * update the stream state according to the current version's spec.
       */
      atDocument() {
        const res = new _Directives(this.yaml, this.tags);
        switch (this.yaml.version) {
          case "1.1":
            this.atNextDocument = true;
            break;
          case "1.2":
            this.atNextDocument = false;
            this.yaml = {
              explicit: _Directives.defaultYaml.explicit,
              version: "1.2"
            };
            this.tags = Object.assign({}, _Directives.defaultTags);
            break;
        }
        return res;
      }
      /**
       * @param onError - May be called even if the action was successful
       * @returns `true` on success
       */
      add(line2, onError) {
        if (this.atNextDocument) {
          this.yaml = { explicit: _Directives.defaultYaml.explicit, version: "1.1" };
          this.tags = Object.assign({}, _Directives.defaultTags);
          this.atNextDocument = false;
        }
        const parts = line2.trim().split(/[ \t]+/);
        const name = parts.shift();
        switch (name) {
          case "%TAG": {
            if (parts.length !== 2) {
              onError(0, "%TAG directive should contain exactly two parts");
              if (parts.length < 2)
                return false;
            }
            const [handle, prefix2] = parts;
            this.tags[handle] = prefix2;
            return true;
          }
          case "%YAML": {
            this.yaml.explicit = true;
            if (parts.length !== 1) {
              onError(0, "%YAML directive should contain exactly one part");
              return false;
            }
            const [version] = parts;
            if (version === "1.1" || version === "1.2") {
              this.yaml.version = version;
              return true;
            } else {
              const isValid = /^\d+\.\d+$/.test(version);
              onError(6, `Unsupported YAML version ${version}`, isValid);
              return false;
            }
          }
          default:
            onError(0, `Unknown directive ${name}`, true);
            return false;
        }
      }
      /**
       * Resolves a tag, matching handles to those defined in %TAG directives.
       *
       * @returns Resolved tag, which may also be the non-specific tag `'!'` or a
       *   `'!local'` tag, or `null` if unresolvable.
       */
      tagName(source, onError) {
        if (source === "!")
          return "!";
        if (source[0] !== "!") {
          onError(`Not a valid tag: ${source}`);
          return null;
        }
        if (source[1] === "<") {
          const verbatim = source.slice(2, -1);
          if (verbatim === "!" || verbatim === "!!") {
            onError(`Verbatim tags aren't resolved, so ${source} is invalid.`);
            return null;
          }
          if (source[source.length - 1] !== ">")
            onError("Verbatim tags must end with a >");
          return verbatim;
        }
        const [, handle, suffix] = source.match(/^(.*!)([^!]*)$/s);
        if (!suffix)
          onError(`The ${source} tag has no suffix`);
        const prefix2 = this.tags[handle];
        if (prefix2) {
          try {
            return prefix2 + decodeURIComponent(suffix);
          } catch (error) {
            onError(String(error));
            return null;
          }
        }
        if (handle === "!")
          return source;
        onError(`Could not resolve tag: ${source}`);
        return null;
      }
      /**
       * Given a fully resolved tag, returns its printable string form,
       * taking into account current tag prefixes and defaults.
       */
      tagString(tag) {
        for (const [handle, prefix2] of Object.entries(this.tags)) {
          if (tag.startsWith(prefix2))
            return handle + escapeTagName(tag.substring(prefix2.length));
        }
        return tag[0] === "!" ? tag : `!<${tag}>`;
      }
      toString(doc) {
        const lines = this.yaml.explicit ? [`%YAML ${this.yaml.version || "1.2"}`] : [];
        const tagEntries = Object.entries(this.tags);
        let tagNames;
        if (doc && tagEntries.length > 0 && isNode(doc.contents)) {
          const tags = {};
          visit(doc.contents, (_key, node2) => {
            if (isNode(node2) && node2.tag)
              tags[node2.tag] = true;
          });
          tagNames = Object.keys(tags);
        } else
          tagNames = [];
        for (const [handle, prefix2] of tagEntries) {
          if (handle === "!!" && prefix2 === "tag:yaml.org,2002:")
            continue;
          if (!doc || tagNames.some((tn) => tn.startsWith(prefix2)))
            lines.push(`%TAG ${handle} ${prefix2}`);
        }
        return lines.join("\n");
      }
    };
    Directives.defaultYaml = { explicit: false, version: "1.2" };
    Directives.defaultTags = { "!!": "tag:yaml.org,2002:" };
  }
});

// node_modules/yaml/browser/dist/doc/anchors.js
function anchorIsValid(anchor) {
  if (/[\x00-\x19\s,[\]{}]/.test(anchor)) {
    const sa = JSON.stringify(anchor);
    const msg = `Anchor must not contain whitespace or control characters: ${sa}`;
    throw new Error(msg);
  }
  return true;
}
function anchorNames(root) {
  const anchors = /* @__PURE__ */ new Set();
  visit(root, {
    Value(_key, node2) {
      if (node2.anchor)
        anchors.add(node2.anchor);
    }
  });
  return anchors;
}
function findNewAnchor(prefix2, exclude) {
  for (let i2 = 1; true; ++i2) {
    const name = `${prefix2}${i2}`;
    if (!exclude.has(name))
      return name;
  }
}
function createNodeAnchors(doc, prefix2) {
  const aliasObjects = [];
  const sourceObjects = /* @__PURE__ */ new Map();
  let prevAnchors = null;
  return {
    onAnchor: (source) => {
      aliasObjects.push(source);
      prevAnchors ?? (prevAnchors = anchorNames(doc));
      const anchor = findNewAnchor(prefix2, prevAnchors);
      prevAnchors.add(anchor);
      return anchor;
    },
    /**
     * With circular references, the source node is only resolved after all
     * of its child nodes are. This is why anchors are set only after all of
     * the nodes have been created.
     */
    setAnchors: () => {
      for (const source of aliasObjects) {
        const ref = sourceObjects.get(source);
        if (typeof ref === "object" && ref.anchor && (isScalar(ref.node) || isCollection(ref.node))) {
          ref.node.anchor = ref.anchor;
        } else {
          const error = new Error("Failed to resolve repeated object (this should not happen)");
          error.source = source;
          throw error;
        }
      }
    },
    sourceObjects
  };
}
var init_anchors = __esm({
  "node_modules/yaml/browser/dist/doc/anchors.js"() {
    init_identity();
    init_visit();
  }
});

// node_modules/yaml/browser/dist/doc/applyReviver.js
function applyReviver(reviver, obj, key2, val) {
  if (val && typeof val === "object") {
    if (Array.isArray(val)) {
      for (let i2 = 0, len = val.length; i2 < len; ++i2) {
        const v0 = val[i2];
        const v1 = applyReviver(reviver, val, String(i2), v0);
        if (v1 === void 0)
          delete val[i2];
        else if (v1 !== v0)
          val[i2] = v1;
      }
    } else if (val instanceof Map) {
      for (const k2 of Array.from(val.keys())) {
        const v0 = val.get(k2);
        const v1 = applyReviver(reviver, val, k2, v0);
        if (v1 === void 0)
          val.delete(k2);
        else if (v1 !== v0)
          val.set(k2, v1);
      }
    } else if (val instanceof Set) {
      for (const v0 of Array.from(val)) {
        const v1 = applyReviver(reviver, val, v0, v0);
        if (v1 === void 0)
          val.delete(v0);
        else if (v1 !== v0) {
          val.delete(v0);
          val.add(v1);
        }
      }
    } else {
      for (const [k2, v0] of Object.entries(val)) {
        const v1 = applyReviver(reviver, val, k2, v0);
        if (v1 === void 0)
          delete val[k2];
        else if (v1 !== v0)
          val[k2] = v1;
      }
    }
  }
  return reviver.call(obj, key2, val);
}
var init_applyReviver = __esm({
  "node_modules/yaml/browser/dist/doc/applyReviver.js"() {
  }
});

// node_modules/yaml/browser/dist/nodes/toJS.js
function toJS(value, arg, ctx) {
  if (Array.isArray(value))
    return value.map((v2, i2) => toJS(v2, String(i2), ctx));
  if (value && typeof value.toJSON === "function") {
    if (!ctx || !hasAnchor(value))
      return value.toJSON(arg, ctx);
    const data = { aliasCount: 0, count: 1, res: void 0 };
    ctx.anchors.set(value, data);
    ctx.onCreate = (res2) => {
      data.res = res2;
      delete ctx.onCreate;
    };
    const res = value.toJSON(arg, ctx);
    if (ctx.onCreate)
      ctx.onCreate(res);
    return res;
  }
  if (typeof value === "bigint" && !ctx?.keep)
    return Number(value);
  return value;
}
var init_toJS = __esm({
  "node_modules/yaml/browser/dist/nodes/toJS.js"() {
    init_identity();
  }
});

// node_modules/yaml/browser/dist/nodes/Node.js
var NodeBase;
var init_Node = __esm({
  "node_modules/yaml/browser/dist/nodes/Node.js"() {
    init_applyReviver();
    init_identity();
    init_toJS();
    NodeBase = class {
      constructor(type) {
        Object.defineProperty(this, NODE_TYPE, { value: type });
      }
      /** Create a copy of this node.  */
      clone() {
        const copy2 = Object.create(Object.getPrototypeOf(this), Object.getOwnPropertyDescriptors(this));
        if (this.range)
          copy2.range = this.range.slice();
        return copy2;
      }
      /** A plain JavaScript representation of this node. */
      toJS(doc, { mapAsMap, maxAliasCount, onAnchor, reviver } = {}) {
        if (!isDocument(doc))
          throw new TypeError("A document argument is required");
        const ctx = {
          anchors: /* @__PURE__ */ new Map(),
          doc,
          keep: true,
          mapAsMap: mapAsMap === true,
          mapKeyWarned: false,
          maxAliasCount: typeof maxAliasCount === "number" ? maxAliasCount : 100
        };
        const res = toJS(this, "", ctx);
        if (typeof onAnchor === "function")
          for (const { count, res: res2 } of ctx.anchors.values())
            onAnchor(res2, count);
        return typeof reviver === "function" ? applyReviver(reviver, { "": res }, "", res) : res;
      }
    };
  }
});

// node_modules/yaml/browser/dist/nodes/Alias.js
function getAliasCount(doc, node2, anchors) {
  if (isAlias(node2)) {
    const source = node2.resolve(doc);
    const anchor = anchors && source && anchors.get(source);
    return anchor ? anchor.count * anchor.aliasCount : 0;
  } else if (isCollection(node2)) {
    let count = 0;
    for (const item of node2.items) {
      const c2 = getAliasCount(doc, item, anchors);
      if (c2 > count)
        count = c2;
    }
    return count;
  } else if (isPair(node2)) {
    const kc = getAliasCount(doc, node2.key, anchors);
    const vc = getAliasCount(doc, node2.value, anchors);
    return Math.max(kc, vc);
  }
  return 1;
}
var Alias;
var init_Alias = __esm({
  "node_modules/yaml/browser/dist/nodes/Alias.js"() {
    init_anchors();
    init_visit();
    init_identity();
    init_Node();
    init_toJS();
    Alias = class extends NodeBase {
      constructor(source) {
        super(ALIAS);
        this.source = source;
        Object.defineProperty(this, "tag", {
          set() {
            throw new Error("Alias nodes cannot have tags");
          }
        });
      }
      /**
       * Resolve the value of this alias within `doc`, finding the last
       * instance of the `source` anchor before this node.
       */
      resolve(doc, ctx) {
        let nodes;
        if (ctx?.aliasResolveCache) {
          nodes = ctx.aliasResolveCache;
        } else {
          nodes = [];
          visit(doc, {
            Node: (_key, node2) => {
              if (isAlias(node2) || hasAnchor(node2))
                nodes.push(node2);
            }
          });
          if (ctx)
            ctx.aliasResolveCache = nodes;
        }
        let found = void 0;
        for (const node2 of nodes) {
          if (node2 === this)
            break;
          if (node2.anchor === this.source)
            found = node2;
        }
        return found;
      }
      toJSON(_arg, ctx) {
        if (!ctx)
          return { source: this.source };
        const { anchors, doc, maxAliasCount } = ctx;
        const source = this.resolve(doc, ctx);
        if (!source) {
          const msg = `Unresolved alias (the anchor must be set before the alias): ${this.source}`;
          throw new ReferenceError(msg);
        }
        let data = anchors.get(source);
        if (!data) {
          toJS(source, null, ctx);
          data = anchors.get(source);
        }
        if (!data || data.res === void 0) {
          const msg = "This should not happen: Alias anchor was not resolved?";
          throw new ReferenceError(msg);
        }
        if (maxAliasCount >= 0) {
          data.count += 1;
          if (data.aliasCount === 0)
            data.aliasCount = getAliasCount(doc, source, anchors);
          if (data.count * data.aliasCount > maxAliasCount) {
            const msg = "Excessive alias count indicates a resource exhaustion attack";
            throw new ReferenceError(msg);
          }
        }
        return data.res;
      }
      toString(ctx, _onComment, _onChompKeep) {
        const src = `*${this.source}`;
        if (ctx) {
          anchorIsValid(this.source);
          if (ctx.options.verifyAliasOrder && !ctx.anchors.has(this.source)) {
            const msg = `Unresolved alias (the anchor must be set before the alias): ${this.source}`;
            throw new Error(msg);
          }
          if (ctx.implicitKey)
            return `${src} `;
        }
        return src;
      }
    };
  }
});

// node_modules/yaml/browser/dist/nodes/Scalar.js
var isScalarValue, Scalar;
var init_Scalar = __esm({
  "node_modules/yaml/browser/dist/nodes/Scalar.js"() {
    init_identity();
    init_Node();
    init_toJS();
    isScalarValue = (value) => !value || typeof value !== "function" && typeof value !== "object";
    Scalar = class extends NodeBase {
      constructor(value) {
        super(SCALAR);
        this.value = value;
      }
      toJSON(arg, ctx) {
        return ctx?.keep ? this.value : toJS(this.value, arg, ctx);
      }
      toString() {
        return String(this.value);
      }
    };
    Scalar.BLOCK_FOLDED = "BLOCK_FOLDED";
    Scalar.BLOCK_LITERAL = "BLOCK_LITERAL";
    Scalar.PLAIN = "PLAIN";
    Scalar.QUOTE_DOUBLE = "QUOTE_DOUBLE";
    Scalar.QUOTE_SINGLE = "QUOTE_SINGLE";
  }
});

// node_modules/yaml/browser/dist/doc/createNode.js
function findTagObject(value, tagName, tags) {
  if (tagName) {
    const match2 = tags.filter((t) => t.tag === tagName);
    const tagObj = match2.find((t) => !t.format) ?? match2[0];
    if (!tagObj)
      throw new Error(`Tag ${tagName} not found`);
    return tagObj;
  }
  return tags.find((t) => t.identify?.(value) && !t.format);
}
function createNode(value, tagName, ctx) {
  if (isDocument(value))
    value = value.contents;
  if (isNode(value))
    return value;
  if (isPair(value)) {
    const map3 = ctx.schema[MAP].createNode?.(ctx.schema, null, ctx);
    map3.items.push(value);
    return map3;
  }
  if (value instanceof String || value instanceof Number || value instanceof Boolean || typeof BigInt !== "undefined" && value instanceof BigInt) {
    value = value.valueOf();
  }
  const { aliasDuplicateObjects, onAnchor, onTagObj, schema: schema4, sourceObjects } = ctx;
  let ref = void 0;
  if (aliasDuplicateObjects && value && typeof value === "object") {
    ref = sourceObjects.get(value);
    if (ref) {
      ref.anchor ?? (ref.anchor = onAnchor(value));
      return new Alias(ref.anchor);
    } else {
      ref = { anchor: null, node: null };
      sourceObjects.set(value, ref);
    }
  }
  if (tagName?.startsWith("!!"))
    tagName = defaultTagPrefix + tagName.slice(2);
  let tagObj = findTagObject(value, tagName, schema4.tags);
  if (!tagObj) {
    if (value && typeof value.toJSON === "function") {
      value = value.toJSON();
    }
    if (!value || typeof value !== "object") {
      const node3 = new Scalar(value);
      if (ref)
        ref.node = node3;
      return node3;
    }
    tagObj = value instanceof Map ? schema4[MAP] : Symbol.iterator in Object(value) ? schema4[SEQ] : schema4[MAP];
  }
  if (onTagObj) {
    onTagObj(tagObj);
    delete ctx.onTagObj;
  }
  const node2 = tagObj?.createNode ? tagObj.createNode(ctx.schema, value, ctx) : typeof tagObj?.nodeClass?.from === "function" ? tagObj.nodeClass.from(ctx.schema, value, ctx) : new Scalar(value);
  if (tagName)
    node2.tag = tagName;
  else if (!tagObj.default)
    node2.tag = tagObj.tag;
  if (ref)
    ref.node = node2;
  return node2;
}
var defaultTagPrefix;
var init_createNode = __esm({
  "node_modules/yaml/browser/dist/doc/createNode.js"() {
    init_Alias();
    init_identity();
    init_Scalar();
    defaultTagPrefix = "tag:yaml.org,2002:";
  }
});

// node_modules/yaml/browser/dist/nodes/Collection.js
function collectionFromPath(schema4, path, value) {
  let v2 = value;
  for (let i2 = path.length - 1; i2 >= 0; --i2) {
    const k2 = path[i2];
    if (typeof k2 === "number" && Number.isInteger(k2) && k2 >= 0) {
      const a2 = [];
      a2[k2] = v2;
      v2 = a2;
    } else {
      v2 = /* @__PURE__ */ new Map([[k2, v2]]);
    }
  }
  return createNode(v2, void 0, {
    aliasDuplicateObjects: false,
    keepUndefined: false,
    onAnchor: () => {
      throw new Error("This should not happen, please report a bug.");
    },
    schema: schema4,
    sourceObjects: /* @__PURE__ */ new Map()
  });
}
var isEmptyPath, Collection;
var init_Collection = __esm({
  "node_modules/yaml/browser/dist/nodes/Collection.js"() {
    init_createNode();
    init_identity();
    init_Node();
    isEmptyPath = (path) => path == null || typeof path === "object" && !!path[Symbol.iterator]().next().done;
    Collection = class extends NodeBase {
      constructor(type, schema4) {
        super(type);
        Object.defineProperty(this, "schema", {
          value: schema4,
          configurable: true,
          enumerable: false,
          writable: true
        });
      }
      /**
       * Create a copy of this collection.
       *
       * @param schema - If defined, overwrites the original's schema
       */
      clone(schema4) {
        const copy2 = Object.create(Object.getPrototypeOf(this), Object.getOwnPropertyDescriptors(this));
        if (schema4)
          copy2.schema = schema4;
        copy2.items = copy2.items.map((it2) => isNode(it2) || isPair(it2) ? it2.clone(schema4) : it2);
        if (this.range)
          copy2.range = this.range.slice();
        return copy2;
      }
      /**
       * Adds a value to the collection. For `!!map` and `!!omap` the value must
       * be a Pair instance or a `{ key, value }` object, which may not have a key
       * that already exists in the map.
       */
      addIn(path, value) {
        if (isEmptyPath(path))
          this.add(value);
        else {
          const [key2, ...rest] = path;
          const node2 = this.get(key2, true);
          if (isCollection(node2))
            node2.addIn(rest, value);
          else if (node2 === void 0 && this.schema)
            this.set(key2, collectionFromPath(this.schema, rest, value));
          else
            throw new Error(`Expected YAML collection at ${key2}. Remaining path: ${rest}`);
        }
      }
      /**
       * Removes a value from the collection.
       * @returns `true` if the item was found and removed.
       */
      deleteIn(path) {
        const [key2, ...rest] = path;
        if (rest.length === 0)
          return this.delete(key2);
        const node2 = this.get(key2, true);
        if (isCollection(node2))
          return node2.deleteIn(rest);
        else
          throw new Error(`Expected YAML collection at ${key2}. Remaining path: ${rest}`);
      }
      /**
       * Returns item at `key`, or `undefined` if not found. By default unwraps
       * scalar values from their surrounding node; to disable set `keepScalar` to
       * `true` (collections are always returned intact).
       */
      getIn(path, keepScalar) {
        const [key2, ...rest] = path;
        const node2 = this.get(key2, true);
        if (rest.length === 0)
          return !keepScalar && isScalar(node2) ? node2.value : node2;
        else
          return isCollection(node2) ? node2.getIn(rest, keepScalar) : void 0;
      }
      hasAllNullValues(allowScalar) {
        return this.items.every((node2) => {
          if (!isPair(node2))
            return false;
          const n = node2.value;
          return n == null || allowScalar && isScalar(n) && n.value == null && !n.commentBefore && !n.comment && !n.tag;
        });
      }
      /**
       * Checks if the collection includes a value with the key `key`.
       */
      hasIn(path) {
        const [key2, ...rest] = path;
        if (rest.length === 0)
          return this.has(key2);
        const node2 = this.get(key2, true);
        return isCollection(node2) ? node2.hasIn(rest) : false;
      }
      /**
       * Sets a value in this collection. For `!!set`, `value` needs to be a
       * boolean to add/remove the item from the set.
       */
      setIn(path, value) {
        const [key2, ...rest] = path;
        if (rest.length === 0) {
          this.set(key2, value);
        } else {
          const node2 = this.get(key2, true);
          if (isCollection(node2))
            node2.setIn(rest, value);
          else if (node2 === void 0 && this.schema)
            this.set(key2, collectionFromPath(this.schema, rest, value));
          else
            throw new Error(`Expected YAML collection at ${key2}. Remaining path: ${rest}`);
        }
      }
    };
  }
});

// node_modules/yaml/browser/dist/stringify/stringifyComment.js
function indentComment(comment2, indent) {
  if (/^\n+$/.test(comment2))
    return comment2.substring(1);
  return indent ? comment2.replace(/^(?! *$)/gm, indent) : comment2;
}
var stringifyComment, lineComment;
var init_stringifyComment = __esm({
  "node_modules/yaml/browser/dist/stringify/stringifyComment.js"() {
    stringifyComment = (str) => str.replace(/^(?!$)(?: $)?/gm, "#");
    lineComment = (str, indent, comment2) => str.endsWith("\n") ? indentComment(comment2, indent) : comment2.includes("\n") ? "\n" + indentComment(comment2, indent) : (str.endsWith(" ") ? "" : " ") + comment2;
  }
});

// node_modules/yaml/browser/dist/stringify/foldFlowLines.js
function foldFlowLines(text, indent, mode = "flow", { indentAtStart, lineWidth = 80, minContentWidth = 20, onFold, onOverflow } = {}) {
  if (!lineWidth || lineWidth < 0)
    return text;
  if (lineWidth < minContentWidth)
    minContentWidth = 0;
  const endStep = Math.max(1 + minContentWidth, 1 + lineWidth - indent.length);
  if (text.length <= endStep)
    return text;
  const folds = [];
  const escapedFolds = {};
  let end = lineWidth - indent.length;
  if (typeof indentAtStart === "number") {
    if (indentAtStart > lineWidth - Math.max(2, minContentWidth))
      folds.push(0);
    else
      end = lineWidth - indentAtStart;
  }
  let split = void 0;
  let prev2 = void 0;
  let overflow = false;
  let i2 = -1;
  let escStart = -1;
  let escEnd = -1;
  if (mode === FOLD_BLOCK) {
    i2 = consumeMoreIndentedLines(text, i2, indent.length);
    if (i2 !== -1)
      end = i2 + endStep;
  }
  for (let ch; ch = text[i2 += 1]; ) {
    if (mode === FOLD_QUOTED && ch === "\\") {
      escStart = i2;
      switch (text[i2 + 1]) {
        case "x":
          i2 += 3;
          break;
        case "u":
          i2 += 5;
          break;
        case "U":
          i2 += 9;
          break;
        default:
          i2 += 1;
      }
      escEnd = i2;
    }
    if (ch === "\n") {
      if (mode === FOLD_BLOCK)
        i2 = consumeMoreIndentedLines(text, i2, indent.length);
      end = i2 + indent.length + endStep;
      split = void 0;
    } else {
      if (ch === " " && prev2 && prev2 !== " " && prev2 !== "\n" && prev2 !== "	") {
        const next2 = text[i2 + 1];
        if (next2 && next2 !== " " && next2 !== "\n" && next2 !== "	")
          split = i2;
      }
      if (i2 >= end) {
        if (split) {
          folds.push(split);
          end = split + endStep;
          split = void 0;
        } else if (mode === FOLD_QUOTED) {
          while (prev2 === " " || prev2 === "	") {
            prev2 = ch;
            ch = text[i2 += 1];
            overflow = true;
          }
          const j2 = i2 > escEnd + 1 ? i2 - 2 : escStart - 1;
          if (escapedFolds[j2])
            return text;
          folds.push(j2);
          escapedFolds[j2] = true;
          end = j2 + endStep;
          split = void 0;
        } else {
          overflow = true;
        }
      }
    }
    prev2 = ch;
  }
  if (overflow && onOverflow)
    onOverflow();
  if (folds.length === 0)
    return text;
  if (onFold)
    onFold();
  let res = text.slice(0, folds[0]);
  for (let i3 = 0; i3 < folds.length; ++i3) {
    const fold = folds[i3];
    const end2 = folds[i3 + 1] || text.length;
    if (fold === 0)
      res = `
${indent}${text.slice(0, end2)}`;
    else {
      if (mode === FOLD_QUOTED && escapedFolds[fold])
        res += `${text[fold]}\\`;
      res += `
${indent}${text.slice(fold + 1, end2)}`;
    }
  }
  return res;
}
function consumeMoreIndentedLines(text, i2, indent) {
  let end = i2;
  let start = i2 + 1;
  let ch = text[start];
  while (ch === " " || ch === "	") {
    if (i2 < start + indent) {
      ch = text[++i2];
    } else {
      do {
        ch = text[++i2];
      } while (ch && ch !== "\n");
      end = i2;
      start = i2 + 1;
      ch = text[start];
    }
  }
  return end;
}
var FOLD_FLOW, FOLD_BLOCK, FOLD_QUOTED;
var init_foldFlowLines = __esm({
  "node_modules/yaml/browser/dist/stringify/foldFlowLines.js"() {
    FOLD_FLOW = "flow";
    FOLD_BLOCK = "block";
    FOLD_QUOTED = "quoted";
  }
});

// node_modules/yaml/browser/dist/stringify/stringifyString.js
function lineLengthOverLimit(str, lineWidth, indentLength) {
  if (!lineWidth || lineWidth < 0)
    return false;
  const limit = lineWidth - indentLength;
  const strLen = str.length;
  if (strLen <= limit)
    return false;
  for (let i2 = 0, start = 0; i2 < strLen; ++i2) {
    if (str[i2] === "\n") {
      if (i2 - start > limit)
        return true;
      start = i2 + 1;
      if (strLen - start <= limit)
        return false;
    }
  }
  return true;
}
function doubleQuotedString(value, ctx) {
  const json = JSON.stringify(value);
  if (ctx.options.doubleQuotedAsJSON)
    return json;
  const { implicitKey } = ctx;
  const minMultiLineLength = ctx.options.doubleQuotedMinMultiLineLength;
  const indent = ctx.indent || (containsDocumentMarker(value) ? "  " : "");
  let str = "";
  let start = 0;
  for (let i2 = 0, ch = json[i2]; ch; ch = json[++i2]) {
    if (ch === " " && json[i2 + 1] === "\\" && json[i2 + 2] === "n") {
      str += json.slice(start, i2) + "\\ ";
      i2 += 1;
      start = i2;
      ch = "\\";
    }
    if (ch === "\\")
      switch (json[i2 + 1]) {
        case "u":
          {
            str += json.slice(start, i2);
            const code = json.substr(i2 + 2, 4);
            switch (code) {
              case "0000":
                str += "\\0";
                break;
              case "0007":
                str += "\\a";
                break;
              case "000b":
                str += "\\v";
                break;
              case "001b":
                str += "\\e";
                break;
              case "0085":
                str += "\\N";
                break;
              case "00a0":
                str += "\\_";
                break;
              case "2028":
                str += "\\L";
                break;
              case "2029":
                str += "\\P";
                break;
              default:
                if (code.substr(0, 2) === "00")
                  str += "\\x" + code.substr(2);
                else
                  str += json.substr(i2, 6);
            }
            i2 += 5;
            start = i2 + 1;
          }
          break;
        case "n":
          if (implicitKey || json[i2 + 2] === '"' || json.length < minMultiLineLength) {
            i2 += 1;
          } else {
            str += json.slice(start, i2) + "\n\n";
            while (json[i2 + 2] === "\\" && json[i2 + 3] === "n" && json[i2 + 4] !== '"') {
              str += "\n";
              i2 += 2;
            }
            str += indent;
            if (json[i2 + 2] === " ")
              str += "\\";
            i2 += 1;
            start = i2 + 1;
          }
          break;
        default:
          i2 += 1;
      }
  }
  str = start ? str + json.slice(start) : json;
  return implicitKey ? str : foldFlowLines(str, indent, FOLD_QUOTED, getFoldOptions(ctx, false));
}
function singleQuotedString(value, ctx) {
  if (ctx.options.singleQuote === false || ctx.implicitKey && value.includes("\n") || /[ \t]\n|\n[ \t]/.test(value))
    return doubleQuotedString(value, ctx);
  const indent = ctx.indent || (containsDocumentMarker(value) ? "  " : "");
  const res = "'" + value.replace(/'/g, "''").replace(/\n+/g, `$&
${indent}`) + "'";
  return ctx.implicitKey ? res : foldFlowLines(res, indent, FOLD_FLOW, getFoldOptions(ctx, false));
}
function quotedString(value, ctx) {
  const { singleQuote } = ctx.options;
  let qs;
  if (singleQuote === false)
    qs = doubleQuotedString;
  else {
    const hasDouble = value.includes('"');
    const hasSingle = value.includes("'");
    if (hasDouble && !hasSingle)
      qs = singleQuotedString;
    else if (hasSingle && !hasDouble)
      qs = doubleQuotedString;
    else
      qs = singleQuote ? singleQuotedString : doubleQuotedString;
  }
  return qs(value, ctx);
}
function blockString({ comment: comment2, type, value }, ctx, onComment, onChompKeep) {
  const { blockQuote, commentString, lineWidth } = ctx.options;
  if (!blockQuote || /\n[\t ]+$/.test(value)) {
    return quotedString(value, ctx);
  }
  const indent = ctx.indent || (ctx.forceBlockIndent || containsDocumentMarker(value) ? "  " : "");
  const literal = blockQuote === "literal" ? true : blockQuote === "folded" || type === Scalar.BLOCK_FOLDED ? false : type === Scalar.BLOCK_LITERAL ? true : !lineLengthOverLimit(value, lineWidth, indent.length);
  if (!value)
    return literal ? "|\n" : ">\n";
  let chomp;
  let endStart;
  for (endStart = value.length; endStart > 0; --endStart) {
    const ch = value[endStart - 1];
    if (ch !== "\n" && ch !== "	" && ch !== " ")
      break;
  }
  let end = value.substring(endStart);
  const endNlPos = end.indexOf("\n");
  if (endNlPos === -1) {
    chomp = "-";
  } else if (value === end || endNlPos !== end.length - 1) {
    chomp = "+";
    if (onChompKeep)
      onChompKeep();
  } else {
    chomp = "";
  }
  if (end) {
    value = value.slice(0, -end.length);
    if (end[end.length - 1] === "\n")
      end = end.slice(0, -1);
    end = end.replace(blockEndNewlines, `$&${indent}`);
  }
  let startWithSpace = false;
  let startEnd;
  let startNlPos = -1;
  for (startEnd = 0; startEnd < value.length; ++startEnd) {
    const ch = value[startEnd];
    if (ch === " ")
      startWithSpace = true;
    else if (ch === "\n")
      startNlPos = startEnd;
    else
      break;
  }
  let start = value.substring(0, startNlPos < startEnd ? startNlPos + 1 : startEnd);
  if (start) {
    value = value.substring(start.length);
    start = start.replace(/\n+/g, `$&${indent}`);
  }
  const indentSize = indent ? "2" : "1";
  let header = (startWithSpace ? indentSize : "") + chomp;
  if (comment2) {
    header += " " + commentString(comment2.replace(/ ?[\r\n]+/g, " "));
    if (onComment)
      onComment();
  }
  if (!literal) {
    const foldedValue = value.replace(/\n+/g, "\n$&").replace(/(?:^|\n)([\t ].*)(?:([\n\t ]*)\n(?![\n\t ]))?/g, "$1$2").replace(/\n+/g, `$&${indent}`);
    let literalFallback = false;
    const foldOptions = getFoldOptions(ctx, true);
    if (blockQuote !== "folded" && type !== Scalar.BLOCK_FOLDED) {
      foldOptions.onOverflow = () => {
        literalFallback = true;
      };
    }
    const body = foldFlowLines(`${start}${foldedValue}${end}`, indent, FOLD_BLOCK, foldOptions);
    if (!literalFallback)
      return `>${header}
${indent}${body}`;
  }
  value = value.replace(/\n+/g, `$&${indent}`);
  return `|${header}
${indent}${start}${value}${end}`;
}
function plainString(item, ctx, onComment, onChompKeep) {
  const { type, value } = item;
  const { actualString, implicitKey, indent, indentStep, inFlow } = ctx;
  if (implicitKey && value.includes("\n") || inFlow && /[[\]{},]/.test(value)) {
    return quotedString(value, ctx);
  }
  if (/^[\n\t ,[\]{}#&*!|>'"%@`]|^[?-]$|^[?-][ \t]|[\n:][ \t]|[ \t]\n|[\n\t ]#|[\n\t :]$/.test(value)) {
    return implicitKey || inFlow || !value.includes("\n") ? quotedString(value, ctx) : blockString(item, ctx, onComment, onChompKeep);
  }
  if (!implicitKey && !inFlow && type !== Scalar.PLAIN && value.includes("\n")) {
    return blockString(item, ctx, onComment, onChompKeep);
  }
  if (containsDocumentMarker(value)) {
    if (indent === "") {
      ctx.forceBlockIndent = true;
      return blockString(item, ctx, onComment, onChompKeep);
    } else if (implicitKey && indent === indentStep) {
      return quotedString(value, ctx);
    }
  }
  const str = value.replace(/\n+/g, `$&
${indent}`);
  if (actualString) {
    const test = (tag) => tag.default && tag.tag !== "tag:yaml.org,2002:str" && tag.test?.test(str);
    const { compat, tags } = ctx.doc.schema;
    if (tags.some(test) || compat?.some(test))
      return quotedString(value, ctx);
  }
  return implicitKey ? str : foldFlowLines(str, indent, FOLD_FLOW, getFoldOptions(ctx, false));
}
function stringifyString(item, ctx, onComment, onChompKeep) {
  const { implicitKey, inFlow } = ctx;
  const ss = typeof item.value === "string" ? item : Object.assign({}, item, { value: String(item.value) });
  let { type } = item;
  if (type !== Scalar.QUOTE_DOUBLE) {
    if (/[\x00-\x08\x0b-\x1f\x7f-\x9f\u{D800}-\u{DFFF}]/u.test(ss.value))
      type = Scalar.QUOTE_DOUBLE;
  }
  const _stringify = (_type) => {
    switch (_type) {
      case Scalar.BLOCK_FOLDED:
      case Scalar.BLOCK_LITERAL:
        return implicitKey || inFlow ? quotedString(ss.value, ctx) : blockString(ss, ctx, onComment, onChompKeep);
      case Scalar.QUOTE_DOUBLE:
        return doubleQuotedString(ss.value, ctx);
      case Scalar.QUOTE_SINGLE:
        return singleQuotedString(ss.value, ctx);
      case Scalar.PLAIN:
        return plainString(ss, ctx, onComment, onChompKeep);
      default:
        return null;
    }
  };
  let res = _stringify(type);
  if (res === null) {
    const { defaultKeyType, defaultStringType } = ctx.options;
    const t = implicitKey && defaultKeyType || defaultStringType;
    res = _stringify(t);
    if (res === null)
      throw new Error(`Unsupported default string type ${t}`);
  }
  return res;
}
var getFoldOptions, containsDocumentMarker, blockEndNewlines;
var init_stringifyString = __esm({
  "node_modules/yaml/browser/dist/stringify/stringifyString.js"() {
    init_Scalar();
    init_foldFlowLines();
    getFoldOptions = (ctx, isBlock2) => ({
      indentAtStart: isBlock2 ? ctx.indent.length : ctx.indentAtStart,
      lineWidth: ctx.options.lineWidth,
      minContentWidth: ctx.options.minContentWidth
    });
    containsDocumentMarker = (str) => /^(%|---|\.\.\.)/m.test(str);
    try {
      blockEndNewlines = new RegExp("(^|(?<!\n))\n+(?!\n|$)", "g");
    } catch {
      blockEndNewlines = /\n+(?!\n|$)/g;
    }
  }
});

// node_modules/yaml/browser/dist/stringify/stringify.js
function createStringifyContext(doc, options) {
  const opt = Object.assign({
    blockQuote: true,
    commentString: stringifyComment,
    defaultKeyType: null,
    defaultStringType: "PLAIN",
    directives: null,
    doubleQuotedAsJSON: false,
    doubleQuotedMinMultiLineLength: 40,
    falseStr: "false",
    flowCollectionPadding: true,
    indentSeq: true,
    lineWidth: 80,
    minContentWidth: 20,
    nullStr: "null",
    simpleKeys: false,
    singleQuote: null,
    trueStr: "true",
    verifyAliasOrder: true
  }, doc.schema.toStringOptions, options);
  let inFlow;
  switch (opt.collectionStyle) {
    case "block":
      inFlow = false;
      break;
    case "flow":
      inFlow = true;
      break;
    default:
      inFlow = null;
  }
  return {
    anchors: /* @__PURE__ */ new Set(),
    doc,
    flowCollectionPadding: opt.flowCollectionPadding ? " " : "",
    indent: "",
    indentStep: typeof opt.indent === "number" ? " ".repeat(opt.indent) : "  ",
    inFlow,
    options: opt
  };
}
function getTagObject(tags, item) {
  if (item.tag) {
    const match2 = tags.filter((t) => t.tag === item.tag);
    if (match2.length > 0)
      return match2.find((t) => t.format === item.format) ?? match2[0];
  }
  let tagObj = void 0;
  let obj;
  if (isScalar(item)) {
    obj = item.value;
    let match2 = tags.filter((t) => t.identify?.(obj));
    if (match2.length > 1) {
      const testMatch = match2.filter((t) => t.test);
      if (testMatch.length > 0)
        match2 = testMatch;
    }
    tagObj = match2.find((t) => t.format === item.format) ?? match2.find((t) => !t.format);
  } else {
    obj = item;
    tagObj = tags.find((t) => t.nodeClass && obj instanceof t.nodeClass);
  }
  if (!tagObj) {
    const name = obj?.constructor?.name ?? (obj === null ? "null" : typeof obj);
    throw new Error(`Tag not resolved for ${name} value`);
  }
  return tagObj;
}
function stringifyProps(node2, tagObj, { anchors, doc }) {
  if (!doc.directives)
    return "";
  const props = [];
  const anchor = (isScalar(node2) || isCollection(node2)) && node2.anchor;
  if (anchor && anchorIsValid(anchor)) {
    anchors.add(anchor);
    props.push(`&${anchor}`);
  }
  const tag = node2.tag ?? (tagObj.default ? null : tagObj.tag);
  if (tag)
    props.push(doc.directives.tagString(tag));
  return props.join(" ");
}
function stringify(item, ctx, onComment, onChompKeep) {
  if (isPair(item))
    return item.toString(ctx, onComment, onChompKeep);
  if (isAlias(item)) {
    if (ctx.doc.directives)
      return item.toString(ctx);
    if (ctx.resolvedAliases?.has(item)) {
      throw new TypeError(`Cannot stringify circular structure without alias nodes`);
    } else {
      if (ctx.resolvedAliases)
        ctx.resolvedAliases.add(item);
      else
        ctx.resolvedAliases = /* @__PURE__ */ new Set([item]);
      item = item.resolve(ctx.doc);
    }
  }
  let tagObj = void 0;
  const node2 = isNode(item) ? item : ctx.doc.createNode(item, { onTagObj: (o2) => tagObj = o2 });
  tagObj ?? (tagObj = getTagObject(ctx.doc.schema.tags, node2));
  const props = stringifyProps(node2, tagObj, ctx);
  if (props.length > 0)
    ctx.indentAtStart = (ctx.indentAtStart ?? 0) + props.length + 1;
  const str = typeof tagObj.stringify === "function" ? tagObj.stringify(node2, ctx, onComment, onChompKeep) : isScalar(node2) ? stringifyString(node2, ctx, onComment, onChompKeep) : node2.toString(ctx, onComment, onChompKeep);
  if (!props)
    return str;
  return isScalar(node2) || str[0] === "{" || str[0] === "[" ? `${props} ${str}` : `${props}
${ctx.indent}${str}`;
}
var init_stringify = __esm({
  "node_modules/yaml/browser/dist/stringify/stringify.js"() {
    init_anchors();
    init_identity();
    init_stringifyComment();
    init_stringifyString();
  }
});

// node_modules/yaml/browser/dist/stringify/stringifyPair.js
function stringifyPair({ key: key2, value }, ctx, onComment, onChompKeep) {
  const { allNullValues, doc, indent, indentStep, options: { commentString, indentSeq, simpleKeys } } = ctx;
  let keyComment = isNode(key2) && key2.comment || null;
  if (simpleKeys) {
    if (keyComment) {
      throw new Error("With simple keys, key nodes cannot have comments");
    }
    if (isCollection(key2) || !isNode(key2) && typeof key2 === "object") {
      const msg = "With simple keys, collection cannot be used as a key value";
      throw new Error(msg);
    }
  }
  let explicitKey = !simpleKeys && (!key2 || keyComment && value == null && !ctx.inFlow || isCollection(key2) || (isScalar(key2) ? key2.type === Scalar.BLOCK_FOLDED || key2.type === Scalar.BLOCK_LITERAL : typeof key2 === "object"));
  ctx = Object.assign({}, ctx, {
    allNullValues: false,
    implicitKey: !explicitKey && (simpleKeys || !allNullValues),
    indent: indent + indentStep
  });
  let keyCommentDone = false;
  let chompKeep = false;
  let str = stringify(key2, ctx, () => keyCommentDone = true, () => chompKeep = true);
  if (!explicitKey && !ctx.inFlow && str.length > 1024) {
    if (simpleKeys)
      throw new Error("With simple keys, single line scalar must not span more than 1024 characters");
    explicitKey = true;
  }
  if (ctx.inFlow) {
    if (allNullValues || value == null) {
      if (keyCommentDone && onComment)
        onComment();
      return str === "" ? "?" : explicitKey ? `? ${str}` : str;
    }
  } else if (allNullValues && !simpleKeys || value == null && explicitKey) {
    str = `? ${str}`;
    if (keyComment && !keyCommentDone) {
      str += lineComment(str, ctx.indent, commentString(keyComment));
    } else if (chompKeep && onChompKeep)
      onChompKeep();
    return str;
  }
  if (keyCommentDone)
    keyComment = null;
  if (explicitKey) {
    if (keyComment)
      str += lineComment(str, ctx.indent, commentString(keyComment));
    str = `? ${str}
${indent}:`;
  } else {
    str = `${str}:`;
    if (keyComment)
      str += lineComment(str, ctx.indent, commentString(keyComment));
  }
  let vsb, vcb, valueComment;
  if (isNode(value)) {
    vsb = !!value.spaceBefore;
    vcb = value.commentBefore;
    valueComment = value.comment;
  } else {
    vsb = false;
    vcb = null;
    valueComment = null;
    if (value && typeof value === "object")
      value = doc.createNode(value);
  }
  ctx.implicitKey = false;
  if (!explicitKey && !keyComment && isScalar(value))
    ctx.indentAtStart = str.length + 1;
  chompKeep = false;
  if (!indentSeq && indentStep.length >= 2 && !ctx.inFlow && !explicitKey && isSeq(value) && !value.flow && !value.tag && !value.anchor) {
    ctx.indent = ctx.indent.substring(2);
  }
  let valueCommentDone = false;
  const valueStr = stringify(value, ctx, () => valueCommentDone = true, () => chompKeep = true);
  let ws = " ";
  if (keyComment || vsb || vcb) {
    ws = vsb ? "\n" : "";
    if (vcb) {
      const cs = commentString(vcb);
      ws += `
${indentComment(cs, ctx.indent)}`;
    }
    if (valueStr === "" && !ctx.inFlow) {
      if (ws === "\n")
        ws = "\n\n";
    } else {
      ws += `
${ctx.indent}`;
    }
  } else if (!explicitKey && isCollection(value)) {
    const vs0 = valueStr[0];
    const nl0 = valueStr.indexOf("\n");
    const hasNewline = nl0 !== -1;
    const flow3 = ctx.inFlow ?? value.flow ?? value.items.length === 0;
    if (hasNewline || !flow3) {
      let hasPropsLine = false;
      if (hasNewline && (vs0 === "&" || vs0 === "!")) {
        let sp0 = valueStr.indexOf(" ");
        if (vs0 === "&" && sp0 !== -1 && sp0 < nl0 && valueStr[sp0 + 1] === "!") {
          sp0 = valueStr.indexOf(" ", sp0 + 1);
        }
        if (sp0 === -1 || nl0 < sp0)
          hasPropsLine = true;
      }
      if (!hasPropsLine)
        ws = `
${ctx.indent}`;
    }
  } else if (valueStr === "" || valueStr[0] === "\n") {
    ws = "";
  }
  str += ws + valueStr;
  if (ctx.inFlow) {
    if (valueCommentDone && onComment)
      onComment();
  } else if (valueComment && !valueCommentDone) {
    str += lineComment(str, ctx.indent, commentString(valueComment));
  } else if (chompKeep && onChompKeep) {
    onChompKeep();
  }
  return str;
}
var init_stringifyPair = __esm({
  "node_modules/yaml/browser/dist/stringify/stringifyPair.js"() {
    init_identity();
    init_Scalar();
    init_stringify();
    init_stringifyComment();
  }
});

// node_modules/yaml/browser/dist/log.js
function warn(logLevel, warning) {
  if (logLevel === "debug" || logLevel === "warn") {
    console.warn(warning);
  }
}
var init_log = __esm({
  "node_modules/yaml/browser/dist/log.js"() {
  }
});

// node_modules/yaml/browser/dist/schema/yaml-1.1/merge.js
function addMergeToJSMap(ctx, map3, value) {
  value = ctx && isAlias(value) ? value.resolve(ctx.doc) : value;
  if (isSeq(value))
    for (const it2 of value.items)
      mergeValue(ctx, map3, it2);
  else if (Array.isArray(value))
    for (const it2 of value)
      mergeValue(ctx, map3, it2);
  else
    mergeValue(ctx, map3, value);
}
function mergeValue(ctx, map3, value) {
  const source = ctx && isAlias(value) ? value.resolve(ctx.doc) : value;
  if (!isMap(source))
    throw new Error("Merge sources must be maps or map aliases");
  const srcMap = source.toJSON(null, ctx, Map);
  for (const [key2, value2] of srcMap) {
    if (map3 instanceof Map) {
      if (!map3.has(key2))
        map3.set(key2, value2);
    } else if (map3 instanceof Set) {
      map3.add(key2);
    } else if (!Object.prototype.hasOwnProperty.call(map3, key2)) {
      Object.defineProperty(map3, key2, {
        value: value2,
        writable: true,
        enumerable: true,
        configurable: true
      });
    }
  }
  return map3;
}
var MERGE_KEY, merge, isMergeKey;
var init_merge = __esm({
  "node_modules/yaml/browser/dist/schema/yaml-1.1/merge.js"() {
    init_identity();
    init_Scalar();
    MERGE_KEY = "<<";
    merge = {
      identify: (value) => value === MERGE_KEY || typeof value === "symbol" && value.description === MERGE_KEY,
      default: "key",
      tag: "tag:yaml.org,2002:merge",
      test: /^<<$/,
      resolve: () => Object.assign(new Scalar(Symbol(MERGE_KEY)), {
        addToJSMap: addMergeToJSMap
      }),
      stringify: () => MERGE_KEY
    };
    isMergeKey = (ctx, key2) => (merge.identify(key2) || isScalar(key2) && (!key2.type || key2.type === Scalar.PLAIN) && merge.identify(key2.value)) && ctx?.doc.schema.tags.some((tag) => tag.tag === merge.tag && tag.default);
  }
});

// node_modules/yaml/browser/dist/nodes/addPairToJSMap.js
function addPairToJSMap(ctx, map3, { key: key2, value }) {
  if (isNode(key2) && key2.addToJSMap)
    key2.addToJSMap(ctx, map3, value);
  else if (isMergeKey(ctx, key2))
    addMergeToJSMap(ctx, map3, value);
  else {
    const jsKey = toJS(key2, "", ctx);
    if (map3 instanceof Map) {
      map3.set(jsKey, toJS(value, jsKey, ctx));
    } else if (map3 instanceof Set) {
      map3.add(jsKey);
    } else {
      const stringKey = stringifyKey(key2, jsKey, ctx);
      const jsValue = toJS(value, stringKey, ctx);
      if (stringKey in map3)
        Object.defineProperty(map3, stringKey, {
          value: jsValue,
          writable: true,
          enumerable: true,
          configurable: true
        });
      else
        map3[stringKey] = jsValue;
    }
  }
  return map3;
}
function stringifyKey(key2, jsKey, ctx) {
  if (jsKey === null)
    return "";
  if (typeof jsKey !== "object")
    return String(jsKey);
  if (isNode(key2) && ctx?.doc) {
    const strCtx = createStringifyContext(ctx.doc, {});
    strCtx.anchors = /* @__PURE__ */ new Set();
    for (const node2 of ctx.anchors.keys())
      strCtx.anchors.add(node2.anchor);
    strCtx.inFlow = true;
    strCtx.inStringifyKey = true;
    const strKey = key2.toString(strCtx);
    if (!ctx.mapKeyWarned) {
      let jsonStr = JSON.stringify(strKey);
      if (jsonStr.length > 40)
        jsonStr = jsonStr.substring(0, 36) + '..."';
      warn(ctx.doc.options.logLevel, `Keys with collection values will be stringified due to JS Object restrictions: ${jsonStr}. Set mapAsMap: true to use object keys.`);
      ctx.mapKeyWarned = true;
    }
    return strKey;
  }
  return JSON.stringify(jsKey);
}
var init_addPairToJSMap = __esm({
  "node_modules/yaml/browser/dist/nodes/addPairToJSMap.js"() {
    init_log();
    init_merge();
    init_stringify();
    init_identity();
    init_toJS();
  }
});

// node_modules/yaml/browser/dist/nodes/Pair.js
function createPair(key2, value, ctx) {
  const k2 = createNode(key2, void 0, ctx);
  const v2 = createNode(value, void 0, ctx);
  return new Pair(k2, v2);
}
var Pair;
var init_Pair = __esm({
  "node_modules/yaml/browser/dist/nodes/Pair.js"() {
    init_createNode();
    init_stringifyPair();
    init_addPairToJSMap();
    init_identity();
    Pair = class _Pair {
      constructor(key2, value = null) {
        Object.defineProperty(this, NODE_TYPE, { value: PAIR });
        this.key = key2;
        this.value = value;
      }
      clone(schema4) {
        let { key: key2, value } = this;
        if (isNode(key2))
          key2 = key2.clone(schema4);
        if (isNode(value))
          value = value.clone(schema4);
        return new _Pair(key2, value);
      }
      toJSON(_15, ctx) {
        const pair = ctx?.mapAsMap ? /* @__PURE__ */ new Map() : {};
        return addPairToJSMap(ctx, pair, this);
      }
      toString(ctx, onComment, onChompKeep) {
        return ctx?.doc ? stringifyPair(this, ctx, onComment, onChompKeep) : JSON.stringify(this);
      }
    };
  }
});

// node_modules/yaml/browser/dist/stringify/stringifyCollection.js
function stringifyCollection(collection, ctx, options) {
  const flow3 = ctx.inFlow ?? collection.flow;
  const stringify5 = flow3 ? stringifyFlowCollection : stringifyBlockCollection;
  return stringify5(collection, ctx, options);
}
function stringifyBlockCollection({ comment: comment2, items }, ctx, { blockItemPrefix, flowChars, itemIndent, onChompKeep, onComment }) {
  const { indent, options: { commentString } } = ctx;
  const itemCtx = Object.assign({}, ctx, { indent: itemIndent, type: null });
  let chompKeep = false;
  const lines = [];
  for (let i2 = 0; i2 < items.length; ++i2) {
    const item = items[i2];
    let comment3 = null;
    if (isNode(item)) {
      if (!chompKeep && item.spaceBefore)
        lines.push("");
      addCommentBefore(ctx, lines, item.commentBefore, chompKeep);
      if (item.comment)
        comment3 = item.comment;
    } else if (isPair(item)) {
      const ik = isNode(item.key) ? item.key : null;
      if (ik) {
        if (!chompKeep && ik.spaceBefore)
          lines.push("");
        addCommentBefore(ctx, lines, ik.commentBefore, chompKeep);
      }
    }
    chompKeep = false;
    let str2 = stringify(item, itemCtx, () => comment3 = null, () => chompKeep = true);
    if (comment3)
      str2 += lineComment(str2, itemIndent, commentString(comment3));
    if (chompKeep && comment3)
      chompKeep = false;
    lines.push(blockItemPrefix + str2);
  }
  let str;
  if (lines.length === 0) {
    str = flowChars.start + flowChars.end;
  } else {
    str = lines[0];
    for (let i2 = 1; i2 < lines.length; ++i2) {
      const line2 = lines[i2];
      str += line2 ? `
${indent}${line2}` : "\n";
    }
  }
  if (comment2) {
    str += "\n" + indentComment(commentString(comment2), indent);
    if (onComment)
      onComment();
  } else if (chompKeep && onChompKeep)
    onChompKeep();
  return str;
}
function stringifyFlowCollection({ items }, ctx, { flowChars, itemIndent }) {
  const { indent, indentStep, flowCollectionPadding: fcPadding, options: { commentString } } = ctx;
  itemIndent += indentStep;
  const itemCtx = Object.assign({}, ctx, {
    indent: itemIndent,
    inFlow: true,
    type: null
  });
  let reqNewline = false;
  let linesAtValue = 0;
  const lines = [];
  for (let i2 = 0; i2 < items.length; ++i2) {
    const item = items[i2];
    let comment2 = null;
    if (isNode(item)) {
      if (item.spaceBefore)
        lines.push("");
      addCommentBefore(ctx, lines, item.commentBefore, false);
      if (item.comment)
        comment2 = item.comment;
    } else if (isPair(item)) {
      const ik = isNode(item.key) ? item.key : null;
      if (ik) {
        if (ik.spaceBefore)
          lines.push("");
        addCommentBefore(ctx, lines, ik.commentBefore, false);
        if (ik.comment)
          reqNewline = true;
      }
      const iv = isNode(item.value) ? item.value : null;
      if (iv) {
        if (iv.comment)
          comment2 = iv.comment;
        if (iv.commentBefore)
          reqNewline = true;
      } else if (item.value == null && ik?.comment) {
        comment2 = ik.comment;
      }
    }
    if (comment2)
      reqNewline = true;
    let str = stringify(item, itemCtx, () => comment2 = null);
    if (i2 < items.length - 1)
      str += ",";
    if (comment2)
      str += lineComment(str, itemIndent, commentString(comment2));
    if (!reqNewline && (lines.length > linesAtValue || str.includes("\n")))
      reqNewline = true;
    lines.push(str);
    linesAtValue = lines.length;
  }
  const { start, end } = flowChars;
  if (lines.length === 0) {
    return start + end;
  } else {
    if (!reqNewline) {
      const len = lines.reduce((sum, line2) => sum + line2.length + 2, 2);
      reqNewline = ctx.options.lineWidth > 0 && len > ctx.options.lineWidth;
    }
    if (reqNewline) {
      let str = start;
      for (const line2 of lines)
        str += line2 ? `
${indentStep}${indent}${line2}` : "\n";
      return `${str}
${indent}${end}`;
    } else {
      return `${start}${fcPadding}${lines.join(" ")}${fcPadding}${end}`;
    }
  }
}
function addCommentBefore({ indent, options: { commentString } }, lines, comment2, chompKeep) {
  if (comment2 && chompKeep)
    comment2 = comment2.replace(/^\n+/, "");
  if (comment2) {
    const ic = indentComment(commentString(comment2), indent);
    lines.push(ic.trimStart());
  }
}
var init_stringifyCollection = __esm({
  "node_modules/yaml/browser/dist/stringify/stringifyCollection.js"() {
    init_identity();
    init_stringify();
    init_stringifyComment();
  }
});

// node_modules/yaml/browser/dist/nodes/YAMLMap.js
function findPair(items, key2) {
  const k2 = isScalar(key2) ? key2.value : key2;
  for (const it2 of items) {
    if (isPair(it2)) {
      if (it2.key === key2 || it2.key === k2)
        return it2;
      if (isScalar(it2.key) && it2.key.value === k2)
        return it2;
    }
  }
  return void 0;
}
var YAMLMap;
var init_YAMLMap = __esm({
  "node_modules/yaml/browser/dist/nodes/YAMLMap.js"() {
    init_stringifyCollection();
    init_addPairToJSMap();
    init_Collection();
    init_identity();
    init_Pair();
    init_Scalar();
    YAMLMap = class extends Collection {
      static get tagName() {
        return "tag:yaml.org,2002:map";
      }
      constructor(schema4) {
        super(MAP, schema4);
        this.items = [];
      }
      /**
       * A generic collection parsing method that can be extended
       * to other node classes that inherit from YAMLMap
       */
      static from(schema4, obj, ctx) {
        const { keepUndefined, replacer } = ctx;
        const map3 = new this(schema4);
        const add = (key2, value) => {
          if (typeof replacer === "function")
            value = replacer.call(obj, key2, value);
          else if (Array.isArray(replacer) && !replacer.includes(key2))
            return;
          if (value !== void 0 || keepUndefined)
            map3.items.push(createPair(key2, value, ctx));
        };
        if (obj instanceof Map) {
          for (const [key2, value] of obj)
            add(key2, value);
        } else if (obj && typeof obj === "object") {
          for (const key2 of Object.keys(obj))
            add(key2, obj[key2]);
        }
        if (typeof schema4.sortMapEntries === "function") {
          map3.items.sort(schema4.sortMapEntries);
        }
        return map3;
      }
      /**
       * Adds a value to the collection.
       *
       * @param overwrite - If not set `true`, using a key that is already in the
       *   collection will throw. Otherwise, overwrites the previous value.
       */
      add(pair, overwrite) {
        let _pair;
        if (isPair(pair))
          _pair = pair;
        else if (!pair || typeof pair !== "object" || !("key" in pair)) {
          _pair = new Pair(pair, pair?.value);
        } else
          _pair = new Pair(pair.key, pair.value);
        const prev2 = findPair(this.items, _pair.key);
        const sortEntries = this.schema?.sortMapEntries;
        if (prev2) {
          if (!overwrite)
            throw new Error(`Key ${_pair.key} already set`);
          if (isScalar(prev2.value) && isScalarValue(_pair.value))
            prev2.value.value = _pair.value;
          else
            prev2.value = _pair.value;
        } else if (sortEntries) {
          const i2 = this.items.findIndex((item) => sortEntries(_pair, item) < 0);
          if (i2 === -1)
            this.items.push(_pair);
          else
            this.items.splice(i2, 0, _pair);
        } else {
          this.items.push(_pair);
        }
      }
      delete(key2) {
        const it2 = findPair(this.items, key2);
        if (!it2)
          return false;
        const del = this.items.splice(this.items.indexOf(it2), 1);
        return del.length > 0;
      }
      get(key2, keepScalar) {
        const it2 = findPair(this.items, key2);
        const node2 = it2?.value;
        return (!keepScalar && isScalar(node2) ? node2.value : node2) ?? void 0;
      }
      has(key2) {
        return !!findPair(this.items, key2);
      }
      set(key2, value) {
        this.add(new Pair(key2, value), true);
      }
      /**
       * @param ctx - Conversion context, originally set in Document#toJS()
       * @param {Class} Type - If set, forces the returned collection type
       * @returns Instance of Type, Map, or Object
       */
      toJSON(_15, ctx, Type) {
        const map3 = Type ? new Type() : ctx?.mapAsMap ? /* @__PURE__ */ new Map() : {};
        if (ctx?.onCreate)
          ctx.onCreate(map3);
        for (const item of this.items)
          addPairToJSMap(ctx, map3, item);
        return map3;
      }
      toString(ctx, onComment, onChompKeep) {
        if (!ctx)
          return JSON.stringify(this);
        for (const item of this.items) {
          if (!isPair(item))
            throw new Error(`Map items must all be pairs; found ${JSON.stringify(item)} instead`);
        }
        if (!ctx.allNullValues && this.hasAllNullValues(false))
          ctx = Object.assign({}, ctx, { allNullValues: true });
        return stringifyCollection(this, ctx, {
          blockItemPrefix: "",
          flowChars: { start: "{", end: "}" },
          itemIndent: ctx.indent || "",
          onChompKeep,
          onComment
        });
      }
    };
  }
});

// node_modules/yaml/browser/dist/schema/common/map.js
var map;
var init_map = __esm({
  "node_modules/yaml/browser/dist/schema/common/map.js"() {
    init_identity();
    init_YAMLMap();
    map = {
      collection: "map",
      default: true,
      nodeClass: YAMLMap,
      tag: "tag:yaml.org,2002:map",
      resolve(map3, onError) {
        if (!isMap(map3))
          onError("Expected a mapping for this tag");
        return map3;
      },
      createNode: (schema4, obj, ctx) => YAMLMap.from(schema4, obj, ctx)
    };
  }
});

// node_modules/yaml/browser/dist/nodes/YAMLSeq.js
function asItemIndex(key2) {
  let idx = isScalar(key2) ? key2.value : key2;
  if (idx && typeof idx === "string")
    idx = Number(idx);
  return typeof idx === "number" && Number.isInteger(idx) && idx >= 0 ? idx : null;
}
var YAMLSeq;
var init_YAMLSeq = __esm({
  "node_modules/yaml/browser/dist/nodes/YAMLSeq.js"() {
    init_createNode();
    init_stringifyCollection();
    init_Collection();
    init_identity();
    init_Scalar();
    init_toJS();
    YAMLSeq = class extends Collection {
      static get tagName() {
        return "tag:yaml.org,2002:seq";
      }
      constructor(schema4) {
        super(SEQ, schema4);
        this.items = [];
      }
      add(value) {
        this.items.push(value);
      }
      /**
       * Removes a value from the collection.
       *
       * `key` must contain a representation of an integer for this to succeed.
       * It may be wrapped in a `Scalar`.
       *
       * @returns `true` if the item was found and removed.
       */
      delete(key2) {
        const idx = asItemIndex(key2);
        if (typeof idx !== "number")
          return false;
        const del = this.items.splice(idx, 1);
        return del.length > 0;
      }
      get(key2, keepScalar) {
        const idx = asItemIndex(key2);
        if (typeof idx !== "number")
          return void 0;
        const it2 = this.items[idx];
        return !keepScalar && isScalar(it2) ? it2.value : it2;
      }
      /**
       * Checks if the collection includes a value with the key `key`.
       *
       * `key` must contain a representation of an integer for this to succeed.
       * It may be wrapped in a `Scalar`.
       */
      has(key2) {
        const idx = asItemIndex(key2);
        return typeof idx === "number" && idx < this.items.length;
      }
      /**
       * Sets a value in this collection. For `!!set`, `value` needs to be a
       * boolean to add/remove the item from the set.
       *
       * If `key` does not contain a representation of an integer, this will throw.
       * It may be wrapped in a `Scalar`.
       */
      set(key2, value) {
        const idx = asItemIndex(key2);
        if (typeof idx !== "number")
          throw new Error(`Expected a valid index, not ${key2}.`);
        const prev2 = this.items[idx];
        if (isScalar(prev2) && isScalarValue(value))
          prev2.value = value;
        else
          this.items[idx] = value;
      }
      toJSON(_15, ctx) {
        const seq2 = [];
        if (ctx?.onCreate)
          ctx.onCreate(seq2);
        let i2 = 0;
        for (const item of this.items)
          seq2.push(toJS(item, String(i2++), ctx));
        return seq2;
      }
      toString(ctx, onComment, onChompKeep) {
        if (!ctx)
          return JSON.stringify(this);
        return stringifyCollection(this, ctx, {
          blockItemPrefix: "- ",
          flowChars: { start: "[", end: "]" },
          itemIndent: (ctx.indent || "") + "  ",
          onChompKeep,
          onComment
        });
      }
      static from(schema4, obj, ctx) {
        const { replacer } = ctx;
        const seq2 = new this(schema4);
        if (obj && Symbol.iterator in Object(obj)) {
          let i2 = 0;
          for (let it2 of obj) {
            if (typeof replacer === "function") {
              const key2 = obj instanceof Set ? it2 : String(i2++);
              it2 = replacer.call(obj, key2, it2);
            }
            seq2.items.push(createNode(it2, void 0, ctx));
          }
        }
        return seq2;
      }
    };
  }
});

// node_modules/yaml/browser/dist/schema/common/seq.js
var seq;
var init_seq = __esm({
  "node_modules/yaml/browser/dist/schema/common/seq.js"() {
    init_identity();
    init_YAMLSeq();
    seq = {
      collection: "seq",
      default: true,
      nodeClass: YAMLSeq,
      tag: "tag:yaml.org,2002:seq",
      resolve(seq2, onError) {
        if (!isSeq(seq2))
          onError("Expected a sequence for this tag");
        return seq2;
      },
      createNode: (schema4, obj, ctx) => YAMLSeq.from(schema4, obj, ctx)
    };
  }
});

// node_modules/yaml/browser/dist/schema/common/string.js
var string;
var init_string = __esm({
  "node_modules/yaml/browser/dist/schema/common/string.js"() {
    init_stringifyString();
    string = {
      identify: (value) => typeof value === "string",
      default: true,
      tag: "tag:yaml.org,2002:str",
      resolve: (str) => str,
      stringify(item, ctx, onComment, onChompKeep) {
        ctx = Object.assign({ actualString: true }, ctx);
        return stringifyString(item, ctx, onComment, onChompKeep);
      }
    };
  }
});

// node_modules/yaml/browser/dist/schema/common/null.js
var nullTag;
var init_null = __esm({
  "node_modules/yaml/browser/dist/schema/common/null.js"() {
    init_Scalar();
    nullTag = {
      identify: (value) => value == null,
      createNode: () => new Scalar(null),
      default: true,
      tag: "tag:yaml.org,2002:null",
      test: /^(?:~|[Nn]ull|NULL)?$/,
      resolve: () => new Scalar(null),
      stringify: ({ source }, ctx) => typeof source === "string" && nullTag.test.test(source) ? source : ctx.options.nullStr
    };
  }
});

// node_modules/yaml/browser/dist/schema/core/bool.js
var boolTag;
var init_bool = __esm({
  "node_modules/yaml/browser/dist/schema/core/bool.js"() {
    init_Scalar();
    boolTag = {
      identify: (value) => typeof value === "boolean",
      default: true,
      tag: "tag:yaml.org,2002:bool",
      test: /^(?:[Tt]rue|TRUE|[Ff]alse|FALSE)$/,
      resolve: (str) => new Scalar(str[0] === "t" || str[0] === "T"),
      stringify({ source, value }, ctx) {
        if (source && boolTag.test.test(source)) {
          const sv = source[0] === "t" || source[0] === "T";
          if (value === sv)
            return source;
        }
        return value ? ctx.options.trueStr : ctx.options.falseStr;
      }
    };
  }
});

// node_modules/yaml/browser/dist/stringify/stringifyNumber.js
function stringifyNumber({ format: format2, minFractionDigits, tag, value }) {
  if (typeof value === "bigint")
    return String(value);
  const num = typeof value === "number" ? value : Number(value);
  if (!isFinite(num))
    return isNaN(num) ? ".nan" : num < 0 ? "-.inf" : ".inf";
  let n = JSON.stringify(value);
  if (!format2 && minFractionDigits && (!tag || tag === "tag:yaml.org,2002:float") && /^\d/.test(n)) {
    let i2 = n.indexOf(".");
    if (i2 < 0) {
      i2 = n.length;
      n += ".";
    }
    let d = minFractionDigits - (n.length - i2 - 1);
    while (d-- > 0)
      n += "0";
  }
  return n;
}
var init_stringifyNumber = __esm({
  "node_modules/yaml/browser/dist/stringify/stringifyNumber.js"() {
  }
});

// node_modules/yaml/browser/dist/schema/core/float.js
var floatNaN, floatExp, float;
var init_float = __esm({
  "node_modules/yaml/browser/dist/schema/core/float.js"() {
    init_Scalar();
    init_stringifyNumber();
    floatNaN = {
      identify: (value) => typeof value === "number",
      default: true,
      tag: "tag:yaml.org,2002:float",
      test: /^(?:[-+]?\.(?:inf|Inf|INF)|\.nan|\.NaN|\.NAN)$/,
      resolve: (str) => str.slice(-3).toLowerCase() === "nan" ? NaN : str[0] === "-" ? Number.NEGATIVE_INFINITY : Number.POSITIVE_INFINITY,
      stringify: stringifyNumber
    };
    floatExp = {
      identify: (value) => typeof value === "number",
      default: true,
      tag: "tag:yaml.org,2002:float",
      format: "EXP",
      test: /^[-+]?(?:\.[0-9]+|[0-9]+(?:\.[0-9]*)?)[eE][-+]?[0-9]+$/,
      resolve: (str) => parseFloat(str),
      stringify(node2) {
        const num = Number(node2.value);
        return isFinite(num) ? num.toExponential() : stringifyNumber(node2);
      }
    };
    float = {
      identify: (value) => typeof value === "number",
      default: true,
      tag: "tag:yaml.org,2002:float",
      test: /^[-+]?(?:\.[0-9]+|[0-9]+\.[0-9]*)$/,
      resolve(str) {
        const node2 = new Scalar(parseFloat(str));
        const dot = str.indexOf(".");
        if (dot !== -1 && str[str.length - 1] === "0")
          node2.minFractionDigits = str.length - dot - 1;
        return node2;
      },
      stringify: stringifyNumber
    };
  }
});

// node_modules/yaml/browser/dist/schema/core/int.js
function intStringify(node2, radix, prefix2) {
  const { value } = node2;
  if (intIdentify(value) && value >= 0)
    return prefix2 + value.toString(radix);
  return stringifyNumber(node2);
}
var intIdentify, intResolve, intOct, int, intHex;
var init_int = __esm({
  "node_modules/yaml/browser/dist/schema/core/int.js"() {
    init_stringifyNumber();
    intIdentify = (value) => typeof value === "bigint" || Number.isInteger(value);
    intResolve = (str, offset, radix, { intAsBigInt }) => intAsBigInt ? BigInt(str) : parseInt(str.substring(offset), radix);
    intOct = {
      identify: (value) => intIdentify(value) && value >= 0,
      default: true,
      tag: "tag:yaml.org,2002:int",
      format: "OCT",
      test: /^0o[0-7]+$/,
      resolve: (str, _onError, opt) => intResolve(str, 2, 8, opt),
      stringify: (node2) => intStringify(node2, 8, "0o")
    };
    int = {
      identify: intIdentify,
      default: true,
      tag: "tag:yaml.org,2002:int",
      test: /^[-+]?[0-9]+$/,
      resolve: (str, _onError, opt) => intResolve(str, 0, 10, opt),
      stringify: stringifyNumber
    };
    intHex = {
      identify: (value) => intIdentify(value) && value >= 0,
      default: true,
      tag: "tag:yaml.org,2002:int",
      format: "HEX",
      test: /^0x[0-9a-fA-F]+$/,
      resolve: (str, _onError, opt) => intResolve(str, 2, 16, opt),
      stringify: (node2) => intStringify(node2, 16, "0x")
    };
  }
});

// node_modules/yaml/browser/dist/schema/core/schema.js
var schema;
var init_schema = __esm({
  "node_modules/yaml/browser/dist/schema/core/schema.js"() {
    init_map();
    init_null();
    init_seq();
    init_string();
    init_bool();
    init_float();
    init_int();
    schema = [
      map,
      seq,
      string,
      nullTag,
      boolTag,
      intOct,
      int,
      intHex,
      floatNaN,
      floatExp,
      float
    ];
  }
});

// node_modules/yaml/browser/dist/schema/json/schema.js
function intIdentify2(value) {
  return typeof value === "bigint" || Number.isInteger(value);
}
var stringifyJSON, jsonScalars, jsonError, schema2;
var init_schema2 = __esm({
  "node_modules/yaml/browser/dist/schema/json/schema.js"() {
    init_Scalar();
    init_map();
    init_seq();
    stringifyJSON = ({ value }) => JSON.stringify(value);
    jsonScalars = [
      {
        identify: (value) => typeof value === "string",
        default: true,
        tag: "tag:yaml.org,2002:str",
        resolve: (str) => str,
        stringify: stringifyJSON
      },
      {
        identify: (value) => value == null,
        createNode: () => new Scalar(null),
        default: true,
        tag: "tag:yaml.org,2002:null",
        test: /^null$/,
        resolve: () => null,
        stringify: stringifyJSON
      },
      {
        identify: (value) => typeof value === "boolean",
        default: true,
        tag: "tag:yaml.org,2002:bool",
        test: /^true$|^false$/,
        resolve: (str) => str === "true",
        stringify: stringifyJSON
      },
      {
        identify: intIdentify2,
        default: true,
        tag: "tag:yaml.org,2002:int",
        test: /^-?(?:0|[1-9][0-9]*)$/,
        resolve: (str, _onError, { intAsBigInt }) => intAsBigInt ? BigInt(str) : parseInt(str, 10),
        stringify: ({ value }) => intIdentify2(value) ? value.toString() : JSON.stringify(value)
      },
      {
        identify: (value) => typeof value === "number",
        default: true,
        tag: "tag:yaml.org,2002:float",
        test: /^-?(?:0|[1-9][0-9]*)(?:\.[0-9]*)?(?:[eE][-+]?[0-9]+)?$/,
        resolve: (str) => parseFloat(str),
        stringify: stringifyJSON
      }
    ];
    jsonError = {
      default: true,
      tag: "",
      test: /^/,
      resolve(str, onError) {
        onError(`Unresolved plain scalar ${JSON.stringify(str)}`);
        return str;
      }
    };
    schema2 = [map, seq].concat(jsonScalars, jsonError);
  }
});

// node_modules/yaml/browser/dist/schema/yaml-1.1/binary.js
var binary;
var init_binary = __esm({
  "node_modules/yaml/browser/dist/schema/yaml-1.1/binary.js"() {
    init_Scalar();
    init_stringifyString();
    binary = {
      identify: (value) => value instanceof Uint8Array,
      // Buffer inherits from Uint8Array
      default: false,
      tag: "tag:yaml.org,2002:binary",
      /**
       * Returns a Buffer in node and an Uint8Array in browsers
       *
       * To use the resulting buffer as an image, you'll want to do something like:
       *
       *   const blob = new Blob([buffer], { type: 'image/jpeg' })
       *   document.querySelector('#photo').src = URL.createObjectURL(blob)
       */
      resolve(src, onError) {
        if (typeof atob === "function") {
          const str = atob(src.replace(/[\n\r]/g, ""));
          const buffer = new Uint8Array(str.length);
          for (let i2 = 0; i2 < str.length; ++i2)
            buffer[i2] = str.charCodeAt(i2);
          return buffer;
        } else {
          onError("This environment does not support reading binary tags; either Buffer or atob is required");
          return src;
        }
      },
      stringify({ comment: comment2, type, value }, ctx, onComment, onChompKeep) {
        if (!value)
          return "";
        const buf = value;
        let str;
        if (typeof btoa === "function") {
          let s2 = "";
          for (let i2 = 0; i2 < buf.length; ++i2)
            s2 += String.fromCharCode(buf[i2]);
          str = btoa(s2);
        } else {
          throw new Error("This environment does not support writing binary tags; either Buffer or btoa is required");
        }
        type ?? (type = Scalar.BLOCK_LITERAL);
        if (type !== Scalar.QUOTE_DOUBLE) {
          const lineWidth = Math.max(ctx.options.lineWidth - ctx.indent.length, ctx.options.minContentWidth);
          const n = Math.ceil(str.length / lineWidth);
          const lines = new Array(n);
          for (let i2 = 0, o2 = 0; i2 < n; ++i2, o2 += lineWidth) {
            lines[i2] = str.substr(o2, lineWidth);
          }
          str = lines.join(type === Scalar.BLOCK_LITERAL ? "\n" : " ");
        }
        return stringifyString({ comment: comment2, type, value: str }, ctx, onComment, onChompKeep);
      }
    };
  }
});

// node_modules/yaml/browser/dist/schema/yaml-1.1/pairs.js
function resolvePairs(seq2, onError) {
  if (isSeq(seq2)) {
    for (let i2 = 0; i2 < seq2.items.length; ++i2) {
      let item = seq2.items[i2];
      if (isPair(item))
        continue;
      else if (isMap(item)) {
        if (item.items.length > 1)
          onError("Each pair must have its own sequence indicator");
        const pair = item.items[0] || new Pair(new Scalar(null));
        if (item.commentBefore)
          pair.key.commentBefore = pair.key.commentBefore ? `${item.commentBefore}
${pair.key.commentBefore}` : item.commentBefore;
        if (item.comment) {
          const cn = pair.value ?? pair.key;
          cn.comment = cn.comment ? `${item.comment}
${cn.comment}` : item.comment;
        }
        item = pair;
      }
      seq2.items[i2] = isPair(item) ? item : new Pair(item);
    }
  } else
    onError("Expected a sequence for this tag");
  return seq2;
}
function createPairs(schema4, iterable, ctx) {
  const { replacer } = ctx;
  const pairs2 = new YAMLSeq(schema4);
  pairs2.tag = "tag:yaml.org,2002:pairs";
  let i2 = 0;
  if (iterable && Symbol.iterator in Object(iterable))
    for (let it2 of iterable) {
      if (typeof replacer === "function")
        it2 = replacer.call(iterable, String(i2++), it2);
      let key2, value;
      if (Array.isArray(it2)) {
        if (it2.length === 2) {
          key2 = it2[0];
          value = it2[1];
        } else
          throw new TypeError(`Expected [key, value] tuple: ${it2}`);
      } else if (it2 && it2 instanceof Object) {
        const keys2 = Object.keys(it2);
        if (keys2.length === 1) {
          key2 = keys2[0];
          value = it2[key2];
        } else {
          throw new TypeError(`Expected tuple with one key, not ${keys2.length} keys`);
        }
      } else {
        key2 = it2;
      }
      pairs2.items.push(createPair(key2, value, ctx));
    }
  return pairs2;
}
var pairs;
var init_pairs = __esm({
  "node_modules/yaml/browser/dist/schema/yaml-1.1/pairs.js"() {
    init_identity();
    init_Pair();
    init_Scalar();
    init_YAMLSeq();
    pairs = {
      collection: "seq",
      default: false,
      tag: "tag:yaml.org,2002:pairs",
      resolve: resolvePairs,
      createNode: createPairs
    };
  }
});

// node_modules/yaml/browser/dist/schema/yaml-1.1/omap.js
var YAMLOMap, omap;
var init_omap = __esm({
  "node_modules/yaml/browser/dist/schema/yaml-1.1/omap.js"() {
    init_identity();
    init_toJS();
    init_YAMLMap();
    init_YAMLSeq();
    init_pairs();
    YAMLOMap = class _YAMLOMap extends YAMLSeq {
      constructor() {
        super();
        this.add = YAMLMap.prototype.add.bind(this);
        this.delete = YAMLMap.prototype.delete.bind(this);
        this.get = YAMLMap.prototype.get.bind(this);
        this.has = YAMLMap.prototype.has.bind(this);
        this.set = YAMLMap.prototype.set.bind(this);
        this.tag = _YAMLOMap.tag;
      }
      /**
       * If `ctx` is given, the return type is actually `Map<unknown, unknown>`,
       * but TypeScript won't allow widening the signature of a child method.
       */
      toJSON(_15, ctx) {
        if (!ctx)
          return super.toJSON(_15);
        const map3 = /* @__PURE__ */ new Map();
        if (ctx?.onCreate)
          ctx.onCreate(map3);
        for (const pair of this.items) {
          let key2, value;
          if (isPair(pair)) {
            key2 = toJS(pair.key, "", ctx);
            value = toJS(pair.value, key2, ctx);
          } else {
            key2 = toJS(pair, "", ctx);
          }
          if (map3.has(key2))
            throw new Error("Ordered maps must not include duplicate keys");
          map3.set(key2, value);
        }
        return map3;
      }
      static from(schema4, iterable, ctx) {
        const pairs2 = createPairs(schema4, iterable, ctx);
        const omap2 = new this();
        omap2.items = pairs2.items;
        return omap2;
      }
    };
    YAMLOMap.tag = "tag:yaml.org,2002:omap";
    omap = {
      collection: "seq",
      identify: (value) => value instanceof Map,
      nodeClass: YAMLOMap,
      default: false,
      tag: "tag:yaml.org,2002:omap",
      resolve(seq2, onError) {
        const pairs2 = resolvePairs(seq2, onError);
        const seenKeys = [];
        for (const { key: key2 } of pairs2.items) {
          if (isScalar(key2)) {
            if (seenKeys.includes(key2.value)) {
              onError(`Ordered maps must not include duplicate keys: ${key2.value}`);
            } else {
              seenKeys.push(key2.value);
            }
          }
        }
        return Object.assign(new YAMLOMap(), pairs2);
      },
      createNode: (schema4, iterable, ctx) => YAMLOMap.from(schema4, iterable, ctx)
    };
  }
});

// node_modules/yaml/browser/dist/schema/yaml-1.1/bool.js
function boolStringify({ value, source }, ctx) {
  const boolObj = value ? trueTag : falseTag;
  if (source && boolObj.test.test(source))
    return source;
  return value ? ctx.options.trueStr : ctx.options.falseStr;
}
var trueTag, falseTag;
var init_bool2 = __esm({
  "node_modules/yaml/browser/dist/schema/yaml-1.1/bool.js"() {
    init_Scalar();
    trueTag = {
      identify: (value) => value === true,
      default: true,
      tag: "tag:yaml.org,2002:bool",
      test: /^(?:Y|y|[Yy]es|YES|[Tt]rue|TRUE|[Oo]n|ON)$/,
      resolve: () => new Scalar(true),
      stringify: boolStringify
    };
    falseTag = {
      identify: (value) => value === false,
      default: true,
      tag: "tag:yaml.org,2002:bool",
      test: /^(?:N|n|[Nn]o|NO|[Ff]alse|FALSE|[Oo]ff|OFF)$/,
      resolve: () => new Scalar(false),
      stringify: boolStringify
    };
  }
});

// node_modules/yaml/browser/dist/schema/yaml-1.1/float.js
var floatNaN2, floatExp2, float2;
var init_float2 = __esm({
  "node_modules/yaml/browser/dist/schema/yaml-1.1/float.js"() {
    init_Scalar();
    init_stringifyNumber();
    floatNaN2 = {
      identify: (value) => typeof value === "number",
      default: true,
      tag: "tag:yaml.org,2002:float",
      test: /^(?:[-+]?\.(?:inf|Inf|INF)|\.nan|\.NaN|\.NAN)$/,
      resolve: (str) => str.slice(-3).toLowerCase() === "nan" ? NaN : str[0] === "-" ? Number.NEGATIVE_INFINITY : Number.POSITIVE_INFINITY,
      stringify: stringifyNumber
    };
    floatExp2 = {
      identify: (value) => typeof value === "number",
      default: true,
      tag: "tag:yaml.org,2002:float",
      format: "EXP",
      test: /^[-+]?(?:[0-9][0-9_]*)?(?:\.[0-9_]*)?[eE][-+]?[0-9]+$/,
      resolve: (str) => parseFloat(str.replace(/_/g, "")),
      stringify(node2) {
        const num = Number(node2.value);
        return isFinite(num) ? num.toExponential() : stringifyNumber(node2);
      }
    };
    float2 = {
      identify: (value) => typeof value === "number",
      default: true,
      tag: "tag:yaml.org,2002:float",
      test: /^[-+]?(?:[0-9][0-9_]*)?\.[0-9_]*$/,
      resolve(str) {
        const node2 = new Scalar(parseFloat(str.replace(/_/g, "")));
        const dot = str.indexOf(".");
        if (dot !== -1) {
          const f2 = str.substring(dot + 1).replace(/_/g, "");
          if (f2[f2.length - 1] === "0")
            node2.minFractionDigits = f2.length;
        }
        return node2;
      },
      stringify: stringifyNumber
    };
  }
});

// node_modules/yaml/browser/dist/schema/yaml-1.1/int.js
function intResolve2(str, offset, radix, { intAsBigInt }) {
  const sign = str[0];
  if (sign === "-" || sign === "+")
    offset += 1;
  str = str.substring(offset).replace(/_/g, "");
  if (intAsBigInt) {
    switch (radix) {
      case 2:
        str = `0b${str}`;
        break;
      case 8:
        str = `0o${str}`;
        break;
      case 16:
        str = `0x${str}`;
        break;
    }
    const n2 = BigInt(str);
    return sign === "-" ? BigInt(-1) * n2 : n2;
  }
  const n = parseInt(str, radix);
  return sign === "-" ? -1 * n : n;
}
function intStringify2(node2, radix, prefix2) {
  const { value } = node2;
  if (intIdentify3(value)) {
    const str = value.toString(radix);
    return value < 0 ? "-" + prefix2 + str.substr(1) : prefix2 + str;
  }
  return stringifyNumber(node2);
}
var intIdentify3, intBin, intOct2, int2, intHex2;
var init_int2 = __esm({
  "node_modules/yaml/browser/dist/schema/yaml-1.1/int.js"() {
    init_stringifyNumber();
    intIdentify3 = (value) => typeof value === "bigint" || Number.isInteger(value);
    intBin = {
      identify: intIdentify3,
      default: true,
      tag: "tag:yaml.org,2002:int",
      format: "BIN",
      test: /^[-+]?0b[0-1_]+$/,
      resolve: (str, _onError, opt) => intResolve2(str, 2, 2, opt),
      stringify: (node2) => intStringify2(node2, 2, "0b")
    };
    intOct2 = {
      identify: intIdentify3,
      default: true,
      tag: "tag:yaml.org,2002:int",
      format: "OCT",
      test: /^[-+]?0[0-7_]+$/,
      resolve: (str, _onError, opt) => intResolve2(str, 1, 8, opt),
      stringify: (node2) => intStringify2(node2, 8, "0")
    };
    int2 = {
      identify: intIdentify3,
      default: true,
      tag: "tag:yaml.org,2002:int",
      test: /^[-+]?[0-9][0-9_]*$/,
      resolve: (str, _onError, opt) => intResolve2(str, 0, 10, opt),
      stringify: stringifyNumber
    };
    intHex2 = {
      identify: intIdentify3,
      default: true,
      tag: "tag:yaml.org,2002:int",
      format: "HEX",
      test: /^[-+]?0x[0-9a-fA-F_]+$/,
      resolve: (str, _onError, opt) => intResolve2(str, 2, 16, opt),
      stringify: (node2) => intStringify2(node2, 16, "0x")
    };
  }
});

// node_modules/yaml/browser/dist/schema/yaml-1.1/set.js
var YAMLSet, set;
var init_set = __esm({
  "node_modules/yaml/browser/dist/schema/yaml-1.1/set.js"() {
    init_identity();
    init_Pair();
    init_YAMLMap();
    YAMLSet = class _YAMLSet extends YAMLMap {
      constructor(schema4) {
        super(schema4);
        this.tag = _YAMLSet.tag;
      }
      add(key2) {
        let pair;
        if (isPair(key2))
          pair = key2;
        else if (key2 && typeof key2 === "object" && "key" in key2 && "value" in key2 && key2.value === null)
          pair = new Pair(key2.key, null);
        else
          pair = new Pair(key2, null);
        const prev2 = findPair(this.items, pair.key);
        if (!prev2)
          this.items.push(pair);
      }
      /**
       * If `keepPair` is `true`, returns the Pair matching `key`.
       * Otherwise, returns the value of that Pair's key.
       */
      get(key2, keepPair) {
        const pair = findPair(this.items, key2);
        return !keepPair && isPair(pair) ? isScalar(pair.key) ? pair.key.value : pair.key : pair;
      }
      set(key2, value) {
        if (typeof value !== "boolean")
          throw new Error(`Expected boolean value for set(key, value) in a YAML set, not ${typeof value}`);
        const prev2 = findPair(this.items, key2);
        if (prev2 && !value) {
          this.items.splice(this.items.indexOf(prev2), 1);
        } else if (!prev2 && value) {
          this.items.push(new Pair(key2));
        }
      }
      toJSON(_15, ctx) {
        return super.toJSON(_15, ctx, Set);
      }
      toString(ctx, onComment, onChompKeep) {
        if (!ctx)
          return JSON.stringify(this);
        if (this.hasAllNullValues(true))
          return super.toString(Object.assign({}, ctx, { allNullValues: true }), onComment, onChompKeep);
        else
          throw new Error("Set items must all have null values");
      }
      static from(schema4, iterable, ctx) {
        const { replacer } = ctx;
        const set7 = new this(schema4);
        if (iterable && Symbol.iterator in Object(iterable))
          for (let value of iterable) {
            if (typeof replacer === "function")
              value = replacer.call(iterable, value, value);
            set7.items.push(createPair(value, null, ctx));
          }
        return set7;
      }
    };
    YAMLSet.tag = "tag:yaml.org,2002:set";
    set = {
      collection: "map",
      identify: (value) => value instanceof Set,
      nodeClass: YAMLSet,
      default: false,
      tag: "tag:yaml.org,2002:set",
      createNode: (schema4, iterable, ctx) => YAMLSet.from(schema4, iterable, ctx),
      resolve(map3, onError) {
        if (isMap(map3)) {
          if (map3.hasAllNullValues(true))
            return Object.assign(new YAMLSet(), map3);
          else
            onError("Set items must all have null values");
        } else
          onError("Expected a mapping for this tag");
        return map3;
      }
    };
  }
});

// node_modules/yaml/browser/dist/schema/yaml-1.1/timestamp.js
function parseSexagesimal(str, asBigInt) {
  const sign = str[0];
  const parts = sign === "-" || sign === "+" ? str.substring(1) : str;
  const num = (n) => asBigInt ? BigInt(n) : Number(n);
  const res = parts.replace(/_/g, "").split(":").reduce((res2, p2) => res2 * num(60) + num(p2), num(0));
  return sign === "-" ? num(-1) * res : res;
}
function stringifySexagesimal(node2) {
  let { value } = node2;
  let num = (n) => n;
  if (typeof value === "bigint")
    num = (n) => BigInt(n);
  else if (isNaN(value) || !isFinite(value))
    return stringifyNumber(node2);
  let sign = "";
  if (value < 0) {
    sign = "-";
    value *= num(-1);
  }
  const _60 = num(60);
  const parts = [value % _60];
  if (value < 60) {
    parts.unshift(0);
  } else {
    value = (value - parts[0]) / _60;
    parts.unshift(value % _60);
    if (value >= 60) {
      value = (value - parts[0]) / _60;
      parts.unshift(value);
    }
  }
  return sign + parts.map((n) => String(n).padStart(2, "0")).join(":").replace(/000000\d*$/, "");
}
var intTime, floatTime, timestamp;
var init_timestamp = __esm({
  "node_modules/yaml/browser/dist/schema/yaml-1.1/timestamp.js"() {
    init_stringifyNumber();
    intTime = {
      identify: (value) => typeof value === "bigint" || Number.isInteger(value),
      default: true,
      tag: "tag:yaml.org,2002:int",
      format: "TIME",
      test: /^[-+]?[0-9][0-9_]*(?::[0-5]?[0-9])+$/,
      resolve: (str, _onError, { intAsBigInt }) => parseSexagesimal(str, intAsBigInt),
      stringify: stringifySexagesimal
    };
    floatTime = {
      identify: (value) => typeof value === "number",
      default: true,
      tag: "tag:yaml.org,2002:float",
      format: "TIME",
      test: /^[-+]?[0-9][0-9_]*(?::[0-5]?[0-9])+\.[0-9_]*$/,
      resolve: (str) => parseSexagesimal(str, false),
      stringify: stringifySexagesimal
    };
    timestamp = {
      identify: (value) => value instanceof Date,
      default: true,
      tag: "tag:yaml.org,2002:timestamp",
      // If the time zone is omitted, the timestamp is assumed to be specified in UTC. The time part
      // may be omitted altogether, resulting in a date format. In such a case, the time part is
      // assumed to be 00:00:00Z (start of day, UTC).
      test: RegExp("^([0-9]{4})-([0-9]{1,2})-([0-9]{1,2})(?:(?:t|T|[ \\t]+)([0-9]{1,2}):([0-9]{1,2}):([0-9]{1,2}(\\.[0-9]+)?)(?:[ \\t]*(Z|[-+][012]?[0-9](?::[0-9]{2})?))?)?$"),
      resolve(str) {
        const match2 = str.match(timestamp.test);
        if (!match2)
          throw new Error("!!timestamp expects a date, starting with yyyy-mm-dd");
        const [, year, month, day, hour, minute, second] = match2.map(Number);
        const millisec = match2[7] ? Number((match2[7] + "00").substr(1, 3)) : 0;
        let date = Date.UTC(year, month - 1, day, hour || 0, minute || 0, second || 0, millisec);
        const tz = match2[8];
        if (tz && tz !== "Z") {
          let d = parseSexagesimal(tz, false);
          if (Math.abs(d) < 30)
            d *= 60;
          date -= 6e4 * d;
        }
        return new Date(date);
      },
      stringify: ({ value }) => value?.toISOString().replace(/(T00:00:00)?\.000Z$/, "") ?? ""
    };
  }
});

// node_modules/yaml/browser/dist/schema/yaml-1.1/schema.js
var schema3;
var init_schema3 = __esm({
  "node_modules/yaml/browser/dist/schema/yaml-1.1/schema.js"() {
    init_map();
    init_null();
    init_seq();
    init_string();
    init_binary();
    init_bool2();
    init_float2();
    init_int2();
    init_merge();
    init_omap();
    init_pairs();
    init_set();
    init_timestamp();
    schema3 = [
      map,
      seq,
      string,
      nullTag,
      trueTag,
      falseTag,
      intBin,
      intOct2,
      int2,
      intHex2,
      floatNaN2,
      floatExp2,
      float2,
      binary,
      merge,
      omap,
      pairs,
      set,
      intTime,
      floatTime,
      timestamp
    ];
  }
});

// node_modules/yaml/browser/dist/schema/tags.js
function getTags(customTags, schemaName, addMergeTag) {
  const schemaTags = schemas.get(schemaName);
  if (schemaTags && !customTags) {
    return addMergeTag && !schemaTags.includes(merge) ? schemaTags.concat(merge) : schemaTags.slice();
  }
  let tags = schemaTags;
  if (!tags) {
    if (Array.isArray(customTags))
      tags = [];
    else {
      const keys2 = Array.from(schemas.keys()).filter((key2) => key2 !== "yaml11").map((key2) => JSON.stringify(key2)).join(", ");
      throw new Error(`Unknown schema "${schemaName}"; use one of ${keys2} or define customTags array`);
    }
  }
  if (Array.isArray(customTags)) {
    for (const tag of customTags)
      tags = tags.concat(tag);
  } else if (typeof customTags === "function") {
    tags = customTags(tags.slice());
  }
  if (addMergeTag)
    tags = tags.concat(merge);
  return tags.reduce((tags2, tag) => {
    const tagObj = typeof tag === "string" ? tagsByName[tag] : tag;
    if (!tagObj) {
      const tagName = JSON.stringify(tag);
      const keys2 = Object.keys(tagsByName).map((key2) => JSON.stringify(key2)).join(", ");
      throw new Error(`Unknown custom tag ${tagName}; use one of ${keys2}`);
    }
    if (!tags2.includes(tagObj))
      tags2.push(tagObj);
    return tags2;
  }, []);
}
var schemas, tagsByName, coreKnownTags;
var init_tags = __esm({
  "node_modules/yaml/browser/dist/schema/tags.js"() {
    init_map();
    init_null();
    init_seq();
    init_string();
    init_bool();
    init_float();
    init_int();
    init_schema();
    init_schema2();
    init_binary();
    init_merge();
    init_omap();
    init_pairs();
    init_schema3();
    init_set();
    init_timestamp();
    schemas = /* @__PURE__ */ new Map([
      ["core", schema],
      ["failsafe", [map, seq, string]],
      ["json", schema2],
      ["yaml11", schema3],
      ["yaml-1.1", schema3]
    ]);
    tagsByName = {
      binary,
      bool: boolTag,
      float,
      floatExp,
      floatNaN,
      floatTime,
      int,
      intHex,
      intOct,
      intTime,
      map,
      merge,
      null: nullTag,
      omap,
      pairs,
      seq,
      set,
      timestamp
    };
    coreKnownTags = {
      "tag:yaml.org,2002:binary": binary,
      "tag:yaml.org,2002:merge": merge,
      "tag:yaml.org,2002:omap": omap,
      "tag:yaml.org,2002:pairs": pairs,
      "tag:yaml.org,2002:set": set,
      "tag:yaml.org,2002:timestamp": timestamp
    };
  }
});

// node_modules/yaml/browser/dist/schema/Schema.js
var sortMapEntriesByKey, Schema;
var init_Schema = __esm({
  "node_modules/yaml/browser/dist/schema/Schema.js"() {
    init_identity();
    init_map();
    init_seq();
    init_string();
    init_tags();
    sortMapEntriesByKey = (a2, b2) => a2.key < b2.key ? -1 : a2.key > b2.key ? 1 : 0;
    Schema = class _Schema {
      constructor({ compat, customTags, merge: merge2, resolveKnownTags, schema: schema4, sortMapEntries, toStringDefaults }) {
        this.compat = Array.isArray(compat) ? getTags(compat, "compat") : compat ? getTags(null, compat) : null;
        this.name = typeof schema4 === "string" && schema4 || "core";
        this.knownTags = resolveKnownTags ? coreKnownTags : {};
        this.tags = getTags(customTags, this.name, merge2);
        this.toStringOptions = toStringDefaults ?? null;
        Object.defineProperty(this, MAP, { value: map });
        Object.defineProperty(this, SCALAR, { value: string });
        Object.defineProperty(this, SEQ, { value: seq });
        this.sortMapEntries = typeof sortMapEntries === "function" ? sortMapEntries : sortMapEntries === true ? sortMapEntriesByKey : null;
      }
      clone() {
        const copy2 = Object.create(_Schema.prototype, Object.getOwnPropertyDescriptors(this));
        copy2.tags = this.tags.slice();
        return copy2;
      }
    };
  }
});

// node_modules/yaml/browser/dist/stringify/stringifyDocument.js
function stringifyDocument(doc, options) {
  const lines = [];
  let hasDirectives = options.directives === true;
  if (options.directives !== false && doc.directives) {
    const dir = doc.directives.toString(doc);
    if (dir) {
      lines.push(dir);
      hasDirectives = true;
    } else if (doc.directives.docStart)
      hasDirectives = true;
  }
  if (hasDirectives)
    lines.push("---");
  const ctx = createStringifyContext(doc, options);
  const { commentString } = ctx.options;
  if (doc.commentBefore) {
    if (lines.length !== 1)
      lines.unshift("");
    const cs = commentString(doc.commentBefore);
    lines.unshift(indentComment(cs, ""));
  }
  let chompKeep = false;
  let contentComment = null;
  if (doc.contents) {
    if (isNode(doc.contents)) {
      if (doc.contents.spaceBefore && hasDirectives)
        lines.push("");
      if (doc.contents.commentBefore) {
        const cs = commentString(doc.contents.commentBefore);
        lines.push(indentComment(cs, ""));
      }
      ctx.forceBlockIndent = !!doc.comment;
      contentComment = doc.contents.comment;
    }
    const onChompKeep = contentComment ? void 0 : () => chompKeep = true;
    let body = stringify(doc.contents, ctx, () => contentComment = null, onChompKeep);
    if (contentComment)
      body += lineComment(body, "", commentString(contentComment));
    if ((body[0] === "|" || body[0] === ">") && lines[lines.length - 1] === "---") {
      lines[lines.length - 1] = `--- ${body}`;
    } else
      lines.push(body);
  } else {
    lines.push(stringify(doc.contents, ctx));
  }
  if (doc.directives?.docEnd) {
    if (doc.comment) {
      const cs = commentString(doc.comment);
      if (cs.includes("\n")) {
        lines.push("...");
        lines.push(indentComment(cs, ""));
      } else {
        lines.push(`... ${cs}`);
      }
    } else {
      lines.push("...");
    }
  } else {
    let dc = doc.comment;
    if (dc && chompKeep)
      dc = dc.replace(/^\n+/, "");
    if (dc) {
      if ((!chompKeep || contentComment) && lines[lines.length - 1] !== "")
        lines.push("");
      lines.push(indentComment(commentString(dc), ""));
    }
  }
  return lines.join("\n") + "\n";
}
var init_stringifyDocument = __esm({
  "node_modules/yaml/browser/dist/stringify/stringifyDocument.js"() {
    init_identity();
    init_stringify();
    init_stringifyComment();
  }
});

// node_modules/yaml/browser/dist/doc/Document.js
function assertCollection(contents) {
  if (isCollection(contents))
    return true;
  throw new Error("Expected a YAML collection as document contents");
}
var Document;
var init_Document = __esm({
  "node_modules/yaml/browser/dist/doc/Document.js"() {
    init_Alias();
    init_Collection();
    init_identity();
    init_Pair();
    init_toJS();
    init_Schema();
    init_stringifyDocument();
    init_anchors();
    init_applyReviver();
    init_createNode();
    init_directives();
    Document = class _Document {
      constructor(value, replacer, options) {
        this.commentBefore = null;
        this.comment = null;
        this.errors = [];
        this.warnings = [];
        Object.defineProperty(this, NODE_TYPE, { value: DOC });
        let _replacer = null;
        if (typeof replacer === "function" || Array.isArray(replacer)) {
          _replacer = replacer;
        } else if (options === void 0 && replacer) {
          options = replacer;
          replacer = void 0;
        }
        const opt = Object.assign({
          intAsBigInt: false,
          keepSourceTokens: false,
          logLevel: "warn",
          prettyErrors: true,
          strict: true,
          stringKeys: false,
          uniqueKeys: true,
          version: "1.2"
        }, options);
        this.options = opt;
        let { version } = opt;
        if (options?._directives) {
          this.directives = options._directives.atDocument();
          if (this.directives.yaml.explicit)
            version = this.directives.yaml.version;
        } else
          this.directives = new Directives({ version });
        this.setSchema(version, options);
        this.contents = value === void 0 ? null : this.createNode(value, _replacer, options);
      }
      /**
       * Create a deep copy of this Document and its contents.
       *
       * Custom Node values that inherit from `Object` still refer to their original instances.
       */
      clone() {
        const copy2 = Object.create(_Document.prototype, {
          [NODE_TYPE]: { value: DOC }
        });
        copy2.commentBefore = this.commentBefore;
        copy2.comment = this.comment;
        copy2.errors = this.errors.slice();
        copy2.warnings = this.warnings.slice();
        copy2.options = Object.assign({}, this.options);
        if (this.directives)
          copy2.directives = this.directives.clone();
        copy2.schema = this.schema.clone();
        copy2.contents = isNode(this.contents) ? this.contents.clone(copy2.schema) : this.contents;
        if (this.range)
          copy2.range = this.range.slice();
        return copy2;
      }
      /** Adds a value to the document. */
      add(value) {
        if (assertCollection(this.contents))
          this.contents.add(value);
      }
      /** Adds a value to the document. */
      addIn(path, value) {
        if (assertCollection(this.contents))
          this.contents.addIn(path, value);
      }
      /**
       * Create a new `Alias` node, ensuring that the target `node` has the required anchor.
       *
       * If `node` already has an anchor, `name` is ignored.
       * Otherwise, the `node.anchor` value will be set to `name`,
       * or if an anchor with that name is already present in the document,
       * `name` will be used as a prefix for a new unique anchor.
       * If `name` is undefined, the generated anchor will use 'a' as a prefix.
       */
      createAlias(node2, name) {
        if (!node2.anchor) {
          const prev2 = anchorNames(this);
          node2.anchor = // eslint-disable-next-line @typescript-eslint/prefer-nullish-coalescing
          !name || prev2.has(name) ? findNewAnchor(name || "a", prev2) : name;
        }
        return new Alias(node2.anchor);
      }
      createNode(value, replacer, options) {
        let _replacer = void 0;
        if (typeof replacer === "function") {
          value = replacer.call({ "": value }, "", value);
          _replacer = replacer;
        } else if (Array.isArray(replacer)) {
          const keyToStr = (v2) => typeof v2 === "number" || v2 instanceof String || v2 instanceof Number;
          const asStr = replacer.filter(keyToStr).map(String);
          if (asStr.length > 0)
            replacer = replacer.concat(asStr);
          _replacer = replacer;
        } else if (options === void 0 && replacer) {
          options = replacer;
          replacer = void 0;
        }
        const { aliasDuplicateObjects, anchorPrefix, flow: flow3, keepUndefined, onTagObj, tag } = options ?? {};
        const { onAnchor, setAnchors, sourceObjects } = createNodeAnchors(
          this,
          // eslint-disable-next-line @typescript-eslint/prefer-nullish-coalescing
          anchorPrefix || "a"
        );
        const ctx = {
          aliasDuplicateObjects: aliasDuplicateObjects ?? true,
          keepUndefined: keepUndefined ?? false,
          onAnchor,
          onTagObj,
          replacer: _replacer,
          schema: this.schema,
          sourceObjects
        };
        const node2 = createNode(value, tag, ctx);
        if (flow3 && isCollection(node2))
          node2.flow = true;
        setAnchors();
        return node2;
      }
      /**
       * Convert a key and a value into a `Pair` using the current schema,
       * recursively wrapping all values as `Scalar` or `Collection` nodes.
       */
      createPair(key2, value, options = {}) {
        const k2 = this.createNode(key2, null, options);
        const v2 = this.createNode(value, null, options);
        return new Pair(k2, v2);
      }
      /**
       * Removes a value from the document.
       * @returns `true` if the item was found and removed.
       */
      delete(key2) {
        return assertCollection(this.contents) ? this.contents.delete(key2) : false;
      }
      /**
       * Removes a value from the document.
       * @returns `true` if the item was found and removed.
       */
      deleteIn(path) {
        if (isEmptyPath(path)) {
          if (this.contents == null)
            return false;
          this.contents = null;
          return true;
        }
        return assertCollection(this.contents) ? this.contents.deleteIn(path) : false;
      }
      /**
       * Returns item at `key`, or `undefined` if not found. By default unwraps
       * scalar values from their surrounding node; to disable set `keepScalar` to
       * `true` (collections are always returned intact).
       */
      get(key2, keepScalar) {
        return isCollection(this.contents) ? this.contents.get(key2, keepScalar) : void 0;
      }
      /**
       * Returns item at `path`, or `undefined` if not found. By default unwraps
       * scalar values from their surrounding node; to disable set `keepScalar` to
       * `true` (collections are always returned intact).
       */
      getIn(path, keepScalar) {
        if (isEmptyPath(path))
          return !keepScalar && isScalar(this.contents) ? this.contents.value : this.contents;
        return isCollection(this.contents) ? this.contents.getIn(path, keepScalar) : void 0;
      }
      /**
       * Checks if the document includes a value with the key `key`.
       */
      has(key2) {
        return isCollection(this.contents) ? this.contents.has(key2) : false;
      }
      /**
       * Checks if the document includes a value at `path`.
       */
      hasIn(path) {
        if (isEmptyPath(path))
          return this.contents !== void 0;
        return isCollection(this.contents) ? this.contents.hasIn(path) : false;
      }
      /**
       * Sets a value in this document. For `!!set`, `value` needs to be a
       * boolean to add/remove the item from the set.
       */
      set(key2, value) {
        if (this.contents == null) {
          this.contents = collectionFromPath(this.schema, [key2], value);
        } else if (assertCollection(this.contents)) {
          this.contents.set(key2, value);
        }
      }
      /**
       * Sets a value in this document. For `!!set`, `value` needs to be a
       * boolean to add/remove the item from the set.
       */
      setIn(path, value) {
        if (isEmptyPath(path)) {
          this.contents = value;
        } else if (this.contents == null) {
          this.contents = collectionFromPath(this.schema, Array.from(path), value);
        } else if (assertCollection(this.contents)) {
          this.contents.setIn(path, value);
        }
      }
      /**
       * Change the YAML version and schema used by the document.
       * A `null` version disables support for directives, explicit tags, anchors, and aliases.
       * It also requires the `schema` option to be given as a `Schema` instance value.
       *
       * Overrides all previously set schema options.
       */
      setSchema(version, options = {}) {
        if (typeof version === "number")
          version = String(version);
        let opt;
        switch (version) {
          case "1.1":
            if (this.directives)
              this.directives.yaml.version = "1.1";
            else
              this.directives = new Directives({ version: "1.1" });
            opt = { resolveKnownTags: false, schema: "yaml-1.1" };
            break;
          case "1.2":
          case "next":
            if (this.directives)
              this.directives.yaml.version = version;
            else
              this.directives = new Directives({ version });
            opt = { resolveKnownTags: true, schema: "core" };
            break;
          case null:
            if (this.directives)
              delete this.directives;
            opt = null;
            break;
          default: {
            const sv = JSON.stringify(version);
            throw new Error(`Expected '1.1', '1.2' or null as first argument, but found: ${sv}`);
          }
        }
        if (options.schema instanceof Object)
          this.schema = options.schema;
        else if (opt)
          this.schema = new Schema(Object.assign(opt, options));
        else
          throw new Error(`With a null YAML version, the { schema: Schema } option is required`);
      }
      // json & jsonArg are only used from toJSON()
      toJS({ json, jsonArg, mapAsMap, maxAliasCount, onAnchor, reviver } = {}) {
        const ctx = {
          anchors: /* @__PURE__ */ new Map(),
          doc: this,
          keep: !json,
          mapAsMap: mapAsMap === true,
          mapKeyWarned: false,
          maxAliasCount: typeof maxAliasCount === "number" ? maxAliasCount : 100
        };
        const res = toJS(this.contents, jsonArg ?? "", ctx);
        if (typeof onAnchor === "function")
          for (const { count, res: res2 } of ctx.anchors.values())
            onAnchor(res2, count);
        return typeof reviver === "function" ? applyReviver(reviver, { "": res }, "", res) : res;
      }
      /**
       * A JSON representation of the document `contents`.
       *
       * @param jsonArg Used by `JSON.stringify` to indicate the array index or
       *   property name.
       */
      toJSON(jsonArg, onAnchor) {
        return this.toJS({ json: true, jsonArg, mapAsMap: false, onAnchor });
      }
      /** A YAML representation of the document. */
      toString(options = {}) {
        if (this.errors.length > 0)
          throw new Error("Document with errors cannot be stringified");
        if ("indent" in options && (!Number.isInteger(options.indent) || Number(options.indent) <= 0)) {
          const s2 = JSON.stringify(options.indent);
          throw new Error(`"indent" option must be a positive integer, not ${s2}`);
        }
        return stringifyDocument(this, options);
      }
    };
  }
});

// node_modules/yaml/browser/dist/errors.js
var YAMLError, YAMLParseError, YAMLWarning, prettifyError;
var init_errors = __esm({
  "node_modules/yaml/browser/dist/errors.js"() {
    YAMLError = class extends Error {
      constructor(name, pos, code, message) {
        super();
        this.name = name;
        this.code = code;
        this.message = message;
        this.pos = pos;
      }
    };
    YAMLParseError = class extends YAMLError {
      constructor(pos, code, message) {
        super("YAMLParseError", pos, code, message);
      }
    };
    YAMLWarning = class extends YAMLError {
      constructor(pos, code, message) {
        super("YAMLWarning", pos, code, message);
      }
    };
    prettifyError = (src, lc) => (error) => {
      if (error.pos[0] === -1)
        return;
      error.linePos = error.pos.map((pos) => lc.linePos(pos));
      const { line: line2, col } = error.linePos[0];
      error.message += ` at line ${line2}, column ${col}`;
      let ci = col - 1;
      let lineStr = src.substring(lc.lineStarts[line2 - 1], lc.lineStarts[line2]).replace(/[\n\r]+$/, "");
      if (ci >= 60 && lineStr.length > 80) {
        const trimStart = Math.min(ci - 39, lineStr.length - 79);
        lineStr = "…" + lineStr.substring(trimStart);
        ci -= trimStart - 1;
      }
      if (lineStr.length > 80)
        lineStr = lineStr.substring(0, 79) + "…";
      if (line2 > 1 && /^ *$/.test(lineStr.substring(0, ci))) {
        let prev2 = src.substring(lc.lineStarts[line2 - 2], lc.lineStarts[line2 - 1]);
        if (prev2.length > 80)
          prev2 = prev2.substring(0, 79) + "…\n";
        lineStr = prev2 + lineStr;
      }
      if (/[^ ]/.test(lineStr)) {
        let count = 1;
        const end = error.linePos[1];
        if (end && end.line === line2 && end.col > col) {
          count = Math.max(1, Math.min(end.col - col, 80 - ci));
        }
        const pointer = " ".repeat(ci) + "^".repeat(count);
        error.message += `:

${lineStr}
${pointer}
`;
      }
    };
  }
});

// node_modules/yaml/browser/dist/compose/resolve-props.js
function resolveProps(tokens, { flow: flow3, indicator, next: next2, offset, onError, parentIndent, startOnNewline }) {
  let spaceBefore = false;
  let atNewline = startOnNewline;
  let hasSpace = startOnNewline;
  let comment2 = "";
  let commentSep = "";
  let hasNewline = false;
  let reqSpace = false;
  let tab = null;
  let anchor = null;
  let tag = null;
  let newlineAfterProp = null;
  let comma2 = null;
  let found = null;
  let start = null;
  for (const token2 of tokens) {
    if (reqSpace) {
      if (token2.type !== "space" && token2.type !== "newline" && token2.type !== "comma")
        onError(token2.offset, "MISSING_CHAR", "Tags and anchors must be separated from the next token by white space");
      reqSpace = false;
    }
    if (tab) {
      if (atNewline && token2.type !== "comment" && token2.type !== "newline") {
        onError(tab, "TAB_AS_INDENT", "Tabs are not allowed as indentation");
      }
      tab = null;
    }
    switch (token2.type) {
      case "space":
        if (!flow3 && (indicator !== "doc-start" || next2?.type !== "flow-collection") && token2.source.includes("	")) {
          tab = token2;
        }
        hasSpace = true;
        break;
      case "comment": {
        if (!hasSpace)
          onError(token2, "MISSING_CHAR", "Comments must be separated from other tokens by white space characters");
        const cb = token2.source.substring(1) || " ";
        if (!comment2)
          comment2 = cb;
        else
          comment2 += commentSep + cb;
        commentSep = "";
        atNewline = false;
        break;
      }
      case "newline":
        if (atNewline) {
          if (comment2)
            comment2 += token2.source;
          else if (!found || indicator !== "seq-item-ind")
            spaceBefore = true;
        } else
          commentSep += token2.source;
        atNewline = true;
        hasNewline = true;
        if (anchor || tag)
          newlineAfterProp = token2;
        hasSpace = true;
        break;
      case "anchor":
        if (anchor)
          onError(token2, "MULTIPLE_ANCHORS", "A node can have at most one anchor");
        if (token2.source.endsWith(":"))
          onError(token2.offset + token2.source.length - 1, "BAD_ALIAS", "Anchor ending in : is ambiguous", true);
        anchor = token2;
        start ?? (start = token2.offset);
        atNewline = false;
        hasSpace = false;
        reqSpace = true;
        break;
      case "tag": {
        if (tag)
          onError(token2, "MULTIPLE_TAGS", "A node can have at most one tag");
        tag = token2;
        start ?? (start = token2.offset);
        atNewline = false;
        hasSpace = false;
        reqSpace = true;
        break;
      }
      case indicator:
        if (anchor || tag)
          onError(token2, "BAD_PROP_ORDER", `Anchors and tags must be after the ${token2.source} indicator`);
        if (found)
          onError(token2, "UNEXPECTED_TOKEN", `Unexpected ${token2.source} in ${flow3 ?? "collection"}`);
        found = token2;
        atNewline = indicator === "seq-item-ind" || indicator === "explicit-key-ind";
        hasSpace = false;
        break;
      case "comma":
        if (flow3) {
          if (comma2)
            onError(token2, "UNEXPECTED_TOKEN", `Unexpected , in ${flow3}`);
          comma2 = token2;
          atNewline = false;
          hasSpace = false;
          break;
        }
      // else fallthrough
      default:
        onError(token2, "UNEXPECTED_TOKEN", `Unexpected ${token2.type} token`);
        atNewline = false;
        hasSpace = false;
    }
  }
  const last2 = tokens[tokens.length - 1];
  const end = last2 ? last2.offset + last2.source.length : offset;
  if (reqSpace && next2 && next2.type !== "space" && next2.type !== "newline" && next2.type !== "comma" && (next2.type !== "scalar" || next2.source !== "")) {
    onError(next2.offset, "MISSING_CHAR", "Tags and anchors must be separated from the next token by white space");
  }
  if (tab && (atNewline && tab.indent <= parentIndent || next2?.type === "block-map" || next2?.type === "block-seq"))
    onError(tab, "TAB_AS_INDENT", "Tabs are not allowed as indentation");
  return {
    comma: comma2,
    found,
    spaceBefore,
    comment: comment2,
    hasNewline,
    anchor,
    tag,
    newlineAfterProp,
    end,
    start: start ?? end
  };
}
var init_resolve_props = __esm({
  "node_modules/yaml/browser/dist/compose/resolve-props.js"() {
  }
});

// node_modules/yaml/browser/dist/compose/util-contains-newline.js
function containsNewline(key2) {
  if (!key2)
    return null;
  switch (key2.type) {
    case "alias":
    case "scalar":
    case "double-quoted-scalar":
    case "single-quoted-scalar":
      if (key2.source.includes("\n"))
        return true;
      if (key2.end) {
        for (const st2 of key2.end)
          if (st2.type === "newline")
            return true;
      }
      return false;
    case "flow-collection":
      for (const it2 of key2.items) {
        for (const st2 of it2.start)
          if (st2.type === "newline")
            return true;
        if (it2.sep) {
          for (const st2 of it2.sep)
            if (st2.type === "newline")
              return true;
        }
        if (containsNewline(it2.key) || containsNewline(it2.value))
          return true;
      }
      return false;
    default:
      return true;
  }
}
var init_util_contains_newline = __esm({
  "node_modules/yaml/browser/dist/compose/util-contains-newline.js"() {
  }
});

// node_modules/yaml/browser/dist/compose/util-flow-indent-check.js
function flowIndentCheck(indent, fc, onError) {
  if (fc?.type === "flow-collection") {
    const end = fc.end[0];
    if (end.indent === indent && (end.source === "]" || end.source === "}") && containsNewline(fc)) {
      const msg = "Flow end indicator should be more indented than parent";
      onError(end, "BAD_INDENT", msg, true);
    }
  }
}
var init_util_flow_indent_check = __esm({
  "node_modules/yaml/browser/dist/compose/util-flow-indent-check.js"() {
    init_util_contains_newline();
  }
});

// node_modules/yaml/browser/dist/compose/util-map-includes.js
function mapIncludes(ctx, items, search) {
  const { uniqueKeys } = ctx.options;
  if (uniqueKeys === false)
    return false;
  const isEqual = typeof uniqueKeys === "function" ? uniqueKeys : (a2, b2) => a2 === b2 || isScalar(a2) && isScalar(b2) && a2.value === b2.value;
  return items.some((pair) => isEqual(pair.key, search));
}
var init_util_map_includes = __esm({
  "node_modules/yaml/browser/dist/compose/util-map-includes.js"() {
    init_identity();
  }
});

// node_modules/yaml/browser/dist/compose/resolve-block-map.js
function resolveBlockMap({ composeNode: composeNode2, composeEmptyNode: composeEmptyNode2 }, ctx, bm, onError, tag) {
  const NodeClass = tag?.nodeClass ?? YAMLMap;
  const map3 = new NodeClass(ctx.schema);
  if (ctx.atRoot)
    ctx.atRoot = false;
  let offset = bm.offset;
  let commentEnd = null;
  for (const collItem of bm.items) {
    const { start, key: key2, sep, value } = collItem;
    const keyProps = resolveProps(start, {
      indicator: "explicit-key-ind",
      next: key2 ?? sep?.[0],
      offset,
      onError,
      parentIndent: bm.indent,
      startOnNewline: true
    });
    const implicitKey = !keyProps.found;
    if (implicitKey) {
      if (key2) {
        if (key2.type === "block-seq")
          onError(offset, "BLOCK_AS_IMPLICIT_KEY", "A block sequence may not be used as an implicit map key");
        else if ("indent" in key2 && key2.indent !== bm.indent)
          onError(offset, "BAD_INDENT", startColMsg);
      }
      if (!keyProps.anchor && !keyProps.tag && !sep) {
        commentEnd = keyProps.end;
        if (keyProps.comment) {
          if (map3.comment)
            map3.comment += "\n" + keyProps.comment;
          else
            map3.comment = keyProps.comment;
        }
        continue;
      }
      if (keyProps.newlineAfterProp || containsNewline(key2)) {
        onError(key2 ?? start[start.length - 1], "MULTILINE_IMPLICIT_KEY", "Implicit keys need to be on a single line");
      }
    } else if (keyProps.found?.indent !== bm.indent) {
      onError(offset, "BAD_INDENT", startColMsg);
    }
    ctx.atKey = true;
    const keyStart = keyProps.end;
    const keyNode = key2 ? composeNode2(ctx, key2, keyProps, onError) : composeEmptyNode2(ctx, keyStart, start, null, keyProps, onError);
    if (ctx.schema.compat)
      flowIndentCheck(bm.indent, key2, onError);
    ctx.atKey = false;
    if (mapIncludes(ctx, map3.items, keyNode))
      onError(keyStart, "DUPLICATE_KEY", "Map keys must be unique");
    const valueProps = resolveProps(sep ?? [], {
      indicator: "map-value-ind",
      next: value,
      offset: keyNode.range[2],
      onError,
      parentIndent: bm.indent,
      startOnNewline: !key2 || key2.type === "block-scalar"
    });
    offset = valueProps.end;
    if (valueProps.found) {
      if (implicitKey) {
        if (value?.type === "block-map" && !valueProps.hasNewline)
          onError(offset, "BLOCK_AS_IMPLICIT_KEY", "Nested mappings are not allowed in compact mappings");
        if (ctx.options.strict && keyProps.start < valueProps.found.offset - 1024)
          onError(keyNode.range, "KEY_OVER_1024_CHARS", "The : indicator must be at most 1024 chars after the start of an implicit block mapping key");
      }
      const valueNode = value ? composeNode2(ctx, value, valueProps, onError) : composeEmptyNode2(ctx, offset, sep, null, valueProps, onError);
      if (ctx.schema.compat)
        flowIndentCheck(bm.indent, value, onError);
      offset = valueNode.range[2];
      const pair = new Pair(keyNode, valueNode);
      if (ctx.options.keepSourceTokens)
        pair.srcToken = collItem;
      map3.items.push(pair);
    } else {
      if (implicitKey)
        onError(keyNode.range, "MISSING_CHAR", "Implicit map keys need to be followed by map values");
      if (valueProps.comment) {
        if (keyNode.comment)
          keyNode.comment += "\n" + valueProps.comment;
        else
          keyNode.comment = valueProps.comment;
      }
      const pair = new Pair(keyNode);
      if (ctx.options.keepSourceTokens)
        pair.srcToken = collItem;
      map3.items.push(pair);
    }
  }
  if (commentEnd && commentEnd < offset)
    onError(commentEnd, "IMPOSSIBLE", "Map comment with trailing content");
  map3.range = [bm.offset, offset, commentEnd ?? offset];
  return map3;
}
var startColMsg;
var init_resolve_block_map = __esm({
  "node_modules/yaml/browser/dist/compose/resolve-block-map.js"() {
    init_Pair();
    init_YAMLMap();
    init_resolve_props();
    init_util_contains_newline();
    init_util_flow_indent_check();
    init_util_map_includes();
    startColMsg = "All mapping items must start at the same column";
  }
});

// node_modules/yaml/browser/dist/compose/resolve-block-seq.js
function resolveBlockSeq({ composeNode: composeNode2, composeEmptyNode: composeEmptyNode2 }, ctx, bs, onError, tag) {
  const NodeClass = tag?.nodeClass ?? YAMLSeq;
  const seq2 = new NodeClass(ctx.schema);
  if (ctx.atRoot)
    ctx.atRoot = false;
  if (ctx.atKey)
    ctx.atKey = false;
  let offset = bs.offset;
  let commentEnd = null;
  for (const { start, value } of bs.items) {
    const props = resolveProps(start, {
      indicator: "seq-item-ind",
      next: value,
      offset,
      onError,
      parentIndent: bs.indent,
      startOnNewline: true
    });
    if (!props.found) {
      if (props.anchor || props.tag || value) {
        if (value && value.type === "block-seq")
          onError(props.end, "BAD_INDENT", "All sequence items must start at the same column");
        else
          onError(offset, "MISSING_CHAR", "Sequence item without - indicator");
      } else {
        commentEnd = props.end;
        if (props.comment)
          seq2.comment = props.comment;
        continue;
      }
    }
    const node2 = value ? composeNode2(ctx, value, props, onError) : composeEmptyNode2(ctx, props.end, start, null, props, onError);
    if (ctx.schema.compat)
      flowIndentCheck(bs.indent, value, onError);
    offset = node2.range[2];
    seq2.items.push(node2);
  }
  seq2.range = [bs.offset, offset, commentEnd ?? offset];
  return seq2;
}
var init_resolve_block_seq = __esm({
  "node_modules/yaml/browser/dist/compose/resolve-block-seq.js"() {
    init_YAMLSeq();
    init_resolve_props();
    init_util_flow_indent_check();
  }
});

// node_modules/yaml/browser/dist/compose/resolve-end.js
function resolveEnd(end, offset, reqSpace, onError) {
  let comment2 = "";
  if (end) {
    let hasSpace = false;
    let sep = "";
    for (const token2 of end) {
      const { source, type } = token2;
      switch (type) {
        case "space":
          hasSpace = true;
          break;
        case "comment": {
          if (reqSpace && !hasSpace)
            onError(token2, "MISSING_CHAR", "Comments must be separated from other tokens by white space characters");
          const cb = source.substring(1) || " ";
          if (!comment2)
            comment2 = cb;
          else
            comment2 += sep + cb;
          sep = "";
          break;
        }
        case "newline":
          if (comment2)
            sep += source;
          hasSpace = true;
          break;
        default:
          onError(token2, "UNEXPECTED_TOKEN", `Unexpected ${type} at node end`);
      }
      offset += source.length;
    }
  }
  return { comment: comment2, offset };
}
var init_resolve_end = __esm({
  "node_modules/yaml/browser/dist/compose/resolve-end.js"() {
  }
});

// node_modules/yaml/browser/dist/compose/resolve-flow-collection.js
function resolveFlowCollection({ composeNode: composeNode2, composeEmptyNode: composeEmptyNode2 }, ctx, fc, onError, tag) {
  const isMap2 = fc.start.source === "{";
  const fcName = isMap2 ? "flow map" : "flow sequence";
  const NodeClass = tag?.nodeClass ?? (isMap2 ? YAMLMap : YAMLSeq);
  const coll = new NodeClass(ctx.schema);
  coll.flow = true;
  const atRoot = ctx.atRoot;
  if (atRoot)
    ctx.atRoot = false;
  if (ctx.atKey)
    ctx.atKey = false;
  let offset = fc.offset + fc.start.source.length;
  for (let i2 = 0; i2 < fc.items.length; ++i2) {
    const collItem = fc.items[i2];
    const { start, key: key2, sep, value } = collItem;
    const props = resolveProps(start, {
      flow: fcName,
      indicator: "explicit-key-ind",
      next: key2 ?? sep?.[0],
      offset,
      onError,
      parentIndent: fc.indent,
      startOnNewline: false
    });
    if (!props.found) {
      if (!props.anchor && !props.tag && !sep && !value) {
        if (i2 === 0 && props.comma)
          onError(props.comma, "UNEXPECTED_TOKEN", `Unexpected , in ${fcName}`);
        else if (i2 < fc.items.length - 1)
          onError(props.start, "UNEXPECTED_TOKEN", `Unexpected empty item in ${fcName}`);
        if (props.comment) {
          if (coll.comment)
            coll.comment += "\n" + props.comment;
          else
            coll.comment = props.comment;
        }
        offset = props.end;
        continue;
      }
      if (!isMap2 && ctx.options.strict && containsNewline(key2))
        onError(
          key2,
          // checked by containsNewline()
          "MULTILINE_IMPLICIT_KEY",
          "Implicit keys of flow sequence pairs need to be on a single line"
        );
    }
    if (i2 === 0) {
      if (props.comma)
        onError(props.comma, "UNEXPECTED_TOKEN", `Unexpected , in ${fcName}`);
    } else {
      if (!props.comma)
        onError(props.start, "MISSING_CHAR", `Missing , between ${fcName} items`);
      if (props.comment) {
        let prevItemComment = "";
        loop: for (const st2 of start) {
          switch (st2.type) {
            case "comma":
            case "space":
              break;
            case "comment":
              prevItemComment = st2.source.substring(1);
              break loop;
            default:
              break loop;
          }
        }
        if (prevItemComment) {
          let prev2 = coll.items[coll.items.length - 1];
          if (isPair(prev2))
            prev2 = prev2.value ?? prev2.key;
          if (prev2.comment)
            prev2.comment += "\n" + prevItemComment;
          else
            prev2.comment = prevItemComment;
          props.comment = props.comment.substring(prevItemComment.length + 1);
        }
      }
    }
    if (!isMap2 && !sep && !props.found) {
      const valueNode = value ? composeNode2(ctx, value, props, onError) : composeEmptyNode2(ctx, props.end, sep, null, props, onError);
      coll.items.push(valueNode);
      offset = valueNode.range[2];
      if (isBlock(value))
        onError(valueNode.range, "BLOCK_IN_FLOW", blockMsg);
    } else {
      ctx.atKey = true;
      const keyStart = props.end;
      const keyNode = key2 ? composeNode2(ctx, key2, props, onError) : composeEmptyNode2(ctx, keyStart, start, null, props, onError);
      if (isBlock(key2))
        onError(keyNode.range, "BLOCK_IN_FLOW", blockMsg);
      ctx.atKey = false;
      const valueProps = resolveProps(sep ?? [], {
        flow: fcName,
        indicator: "map-value-ind",
        next: value,
        offset: keyNode.range[2],
        onError,
        parentIndent: fc.indent,
        startOnNewline: false
      });
      if (valueProps.found) {
        if (!isMap2 && !props.found && ctx.options.strict) {
          if (sep)
            for (const st2 of sep) {
              if (st2 === valueProps.found)
                break;
              if (st2.type === "newline") {
                onError(st2, "MULTILINE_IMPLICIT_KEY", "Implicit keys of flow sequence pairs need to be on a single line");
                break;
              }
            }
          if (props.start < valueProps.found.offset - 1024)
            onError(valueProps.found, "KEY_OVER_1024_CHARS", "The : indicator must be at most 1024 chars after the start of an implicit flow sequence key");
        }
      } else if (value) {
        if ("source" in value && value.source && value.source[0] === ":")
          onError(value, "MISSING_CHAR", `Missing space after : in ${fcName}`);
        else
          onError(valueProps.start, "MISSING_CHAR", `Missing , or : between ${fcName} items`);
      }
      const valueNode = value ? composeNode2(ctx, value, valueProps, onError) : valueProps.found ? composeEmptyNode2(ctx, valueProps.end, sep, null, valueProps, onError) : null;
      if (valueNode) {
        if (isBlock(value))
          onError(valueNode.range, "BLOCK_IN_FLOW", blockMsg);
      } else if (valueProps.comment) {
        if (keyNode.comment)
          keyNode.comment += "\n" + valueProps.comment;
        else
          keyNode.comment = valueProps.comment;
      }
      const pair = new Pair(keyNode, valueNode);
      if (ctx.options.keepSourceTokens)
        pair.srcToken = collItem;
      if (isMap2) {
        const map3 = coll;
        if (mapIncludes(ctx, map3.items, keyNode))
          onError(keyStart, "DUPLICATE_KEY", "Map keys must be unique");
        map3.items.push(pair);
      } else {
        const map3 = new YAMLMap(ctx.schema);
        map3.flow = true;
        map3.items.push(pair);
        const endRange = (valueNode ?? keyNode).range;
        map3.range = [keyNode.range[0], endRange[1], endRange[2]];
        coll.items.push(map3);
      }
      offset = valueNode ? valueNode.range[2] : valueProps.end;
    }
  }
  const expectedEnd = isMap2 ? "}" : "]";
  const [ce2, ...ee2] = fc.end;
  let cePos = offset;
  if (ce2 && ce2.source === expectedEnd)
    cePos = ce2.offset + ce2.source.length;
  else {
    const name = fcName[0].toUpperCase() + fcName.substring(1);
    const msg = atRoot ? `${name} must end with a ${expectedEnd}` : `${name} in block collection must be sufficiently indented and end with a ${expectedEnd}`;
    onError(offset, atRoot ? "MISSING_CHAR" : "BAD_INDENT", msg);
    if (ce2 && ce2.source.length !== 1)
      ee2.unshift(ce2);
  }
  if (ee2.length > 0) {
    const end = resolveEnd(ee2, cePos, ctx.options.strict, onError);
    if (end.comment) {
      if (coll.comment)
        coll.comment += "\n" + end.comment;
      else
        coll.comment = end.comment;
    }
    coll.range = [fc.offset, cePos, end.offset];
  } else {
    coll.range = [fc.offset, cePos, cePos];
  }
  return coll;
}
var blockMsg, isBlock;
var init_resolve_flow_collection = __esm({
  "node_modules/yaml/browser/dist/compose/resolve-flow-collection.js"() {
    init_identity();
    init_Pair();
    init_YAMLMap();
    init_YAMLSeq();
    init_resolve_end();
    init_resolve_props();
    init_util_contains_newline();
    init_util_map_includes();
    blockMsg = "Block collections are not allowed within flow collections";
    isBlock = (token2) => token2 && (token2.type === "block-map" || token2.type === "block-seq");
  }
});

// node_modules/yaml/browser/dist/compose/compose-collection.js
function resolveCollection(CN2, ctx, token2, onError, tagName, tag) {
  const coll = token2.type === "block-map" ? resolveBlockMap(CN2, ctx, token2, onError, tag) : token2.type === "block-seq" ? resolveBlockSeq(CN2, ctx, token2, onError, tag) : resolveFlowCollection(CN2, ctx, token2, onError, tag);
  const Coll = coll.constructor;
  if (tagName === "!" || tagName === Coll.tagName) {
    coll.tag = Coll.tagName;
    return coll;
  }
  if (tagName)
    coll.tag = tagName;
  return coll;
}
function composeCollection(CN2, ctx, token2, props, onError) {
  const tagToken = props.tag;
  const tagName = !tagToken ? null : ctx.directives.tagName(tagToken.source, (msg) => onError(tagToken, "TAG_RESOLVE_FAILED", msg));
  if (token2.type === "block-seq") {
    const { anchor, newlineAfterProp: nl } = props;
    const lastProp = anchor && tagToken ? anchor.offset > tagToken.offset ? anchor : tagToken : anchor ?? tagToken;
    if (lastProp && (!nl || nl.offset < lastProp.offset)) {
      const message = "Missing newline after block sequence props";
      onError(lastProp, "MISSING_CHAR", message);
    }
  }
  const expType = token2.type === "block-map" ? "map" : token2.type === "block-seq" ? "seq" : token2.start.source === "{" ? "map" : "seq";
  if (!tagToken || !tagName || tagName === "!" || tagName === YAMLMap.tagName && expType === "map" || tagName === YAMLSeq.tagName && expType === "seq") {
    return resolveCollection(CN2, ctx, token2, onError, tagName);
  }
  let tag = ctx.schema.tags.find((t) => t.tag === tagName && t.collection === expType);
  if (!tag) {
    const kt = ctx.schema.knownTags[tagName];
    if (kt && kt.collection === expType) {
      ctx.schema.tags.push(Object.assign({}, kt, { default: false }));
      tag = kt;
    } else {
      if (kt) {
        onError(tagToken, "BAD_COLLECTION_TYPE", `${kt.tag} used for ${expType} collection, but expects ${kt.collection ?? "scalar"}`, true);
      } else {
        onError(tagToken, "TAG_RESOLVE_FAILED", `Unresolved tag: ${tagName}`, true);
      }
      return resolveCollection(CN2, ctx, token2, onError, tagName);
    }
  }
  const coll = resolveCollection(CN2, ctx, token2, onError, tagName, tag);
  const res = tag.resolve?.(coll, (msg) => onError(tagToken, "TAG_RESOLVE_FAILED", msg), ctx.options) ?? coll;
  const node2 = isNode(res) ? res : new Scalar(res);
  node2.range = coll.range;
  node2.tag = tagName;
  if (tag?.format)
    node2.format = tag.format;
  return node2;
}
var init_compose_collection = __esm({
  "node_modules/yaml/browser/dist/compose/compose-collection.js"() {
    init_identity();
    init_Scalar();
    init_YAMLMap();
    init_YAMLSeq();
    init_resolve_block_map();
    init_resolve_block_seq();
    init_resolve_flow_collection();
  }
});

// node_modules/yaml/browser/dist/compose/resolve-block-scalar.js
function resolveBlockScalar(ctx, scalar, onError) {
  const start = scalar.offset;
  const header = parseBlockScalarHeader(scalar, ctx.options.strict, onError);
  if (!header)
    return { value: "", type: null, comment: "", range: [start, start, start] };
  const type = header.mode === ">" ? Scalar.BLOCK_FOLDED : Scalar.BLOCK_LITERAL;
  const lines = scalar.source ? splitLines(scalar.source) : [];
  let chompStart = lines.length;
  for (let i2 = lines.length - 1; i2 >= 0; --i2) {
    const content = lines[i2][1];
    if (content === "" || content === "\r")
      chompStart = i2;
    else
      break;
  }
  if (chompStart === 0) {
    const value2 = header.chomp === "+" && lines.length > 0 ? "\n".repeat(Math.max(1, lines.length - 1)) : "";
    let end2 = start + header.length;
    if (scalar.source)
      end2 += scalar.source.length;
    return { value: value2, type, comment: header.comment, range: [start, end2, end2] };
  }
  let trimIndent = scalar.indent + header.indent;
  let offset = scalar.offset + header.length;
  let contentStart = 0;
  for (let i2 = 0; i2 < chompStart; ++i2) {
    const [indent, content] = lines[i2];
    if (content === "" || content === "\r") {
      if (header.indent === 0 && indent.length > trimIndent)
        trimIndent = indent.length;
    } else {
      if (indent.length < trimIndent) {
        const message = "Block scalars with more-indented leading empty lines must use an explicit indentation indicator";
        onError(offset + indent.length, "MISSING_CHAR", message);
      }
      if (header.indent === 0)
        trimIndent = indent.length;
      contentStart = i2;
      if (trimIndent === 0 && !ctx.atRoot) {
        const message = "Block scalar values in collections must be indented";
        onError(offset, "BAD_INDENT", message);
      }
      break;
    }
    offset += indent.length + content.length + 1;
  }
  for (let i2 = lines.length - 1; i2 >= chompStart; --i2) {
    if (lines[i2][0].length > trimIndent)
      chompStart = i2 + 1;
  }
  let value = "";
  let sep = "";
  let prevMoreIndented = false;
  for (let i2 = 0; i2 < contentStart; ++i2)
    value += lines[i2][0].slice(trimIndent) + "\n";
  for (let i2 = contentStart; i2 < chompStart; ++i2) {
    let [indent, content] = lines[i2];
    offset += indent.length + content.length + 1;
    const crlf = content[content.length - 1] === "\r";
    if (crlf)
      content = content.slice(0, -1);
    if (content && indent.length < trimIndent) {
      const src = header.indent ? "explicit indentation indicator" : "first line";
      const message = `Block scalar lines must not be less indented than their ${src}`;
      onError(offset - content.length - (crlf ? 2 : 1), "BAD_INDENT", message);
      indent = "";
    }
    if (type === Scalar.BLOCK_LITERAL) {
      value += sep + indent.slice(trimIndent) + content;
      sep = "\n";
    } else if (indent.length > trimIndent || content[0] === "	") {
      if (sep === " ")
        sep = "\n";
      else if (!prevMoreIndented && sep === "\n")
        sep = "\n\n";
      value += sep + indent.slice(trimIndent) + content;
      sep = "\n";
      prevMoreIndented = true;
    } else if (content === "") {
      if (sep === "\n")
        value += "\n";
      else
        sep = "\n";
    } else {
      value += sep + content;
      sep = " ";
      prevMoreIndented = false;
    }
  }
  switch (header.chomp) {
    case "-":
      break;
    case "+":
      for (let i2 = chompStart; i2 < lines.length; ++i2)
        value += "\n" + lines[i2][0].slice(trimIndent);
      if (value[value.length - 1] !== "\n")
        value += "\n";
      break;
    default:
      value += "\n";
  }
  const end = start + header.length + scalar.source.length;
  return { value, type, comment: header.comment, range: [start, end, end] };
}
function parseBlockScalarHeader({ offset, props }, strict, onError) {
  if (props[0].type !== "block-scalar-header") {
    onError(props[0], "IMPOSSIBLE", "Block scalar header not found");
    return null;
  }
  const { source } = props[0];
  const mode = source[0];
  let indent = 0;
  let chomp = "";
  let error = -1;
  for (let i2 = 1; i2 < source.length; ++i2) {
    const ch = source[i2];
    if (!chomp && (ch === "-" || ch === "+"))
      chomp = ch;
    else {
      const n = Number(ch);
      if (!indent && n)
        indent = n;
      else if (error === -1)
        error = offset + i2;
    }
  }
  if (error !== -1)
    onError(error, "UNEXPECTED_TOKEN", `Block scalar header includes extra characters: ${source}`);
  let hasSpace = false;
  let comment2 = "";
  let length2 = source.length;
  for (let i2 = 1; i2 < props.length; ++i2) {
    const token2 = props[i2];
    switch (token2.type) {
      case "space":
        hasSpace = true;
      // fallthrough
      case "newline":
        length2 += token2.source.length;
        break;
      case "comment":
        if (strict && !hasSpace) {
          const message = "Comments must be separated from other tokens by white space characters";
          onError(token2, "MISSING_CHAR", message);
        }
        length2 += token2.source.length;
        comment2 = token2.source.substring(1);
        break;
      case "error":
        onError(token2, "UNEXPECTED_TOKEN", token2.message);
        length2 += token2.source.length;
        break;
      /* istanbul ignore next should not happen */
      default: {
        const message = `Unexpected token in block scalar header: ${token2.type}`;
        onError(token2, "UNEXPECTED_TOKEN", message);
        const ts = token2.source;
        if (ts && typeof ts === "string")
          length2 += ts.length;
      }
    }
  }
  return { mode, indent, chomp, comment: comment2, length: length2 };
}
function splitLines(source) {
  const split = source.split(/\n( *)/);
  const first = split[0];
  const m2 = first.match(/^( *)/);
  const line0 = m2?.[1] ? [m2[1], first.slice(m2[1].length)] : ["", first];
  const lines = [line0];
  for (let i2 = 1; i2 < split.length; i2 += 2)
    lines.push([split[i2], split[i2 + 1]]);
  return lines;
}
var init_resolve_block_scalar = __esm({
  "node_modules/yaml/browser/dist/compose/resolve-block-scalar.js"() {
    init_Scalar();
  }
});

// node_modules/yaml/browser/dist/compose/resolve-flow-scalar.js
function resolveFlowScalar(scalar, strict, onError) {
  const { offset, type, source, end } = scalar;
  let _type;
  let value;
  const _onError = (rel, code, msg) => onError(offset + rel, code, msg);
  switch (type) {
    case "scalar":
      _type = Scalar.PLAIN;
      value = plainValue(source, _onError);
      break;
    case "single-quoted-scalar":
      _type = Scalar.QUOTE_SINGLE;
      value = singleQuotedValue(source, _onError);
      break;
    case "double-quoted-scalar":
      _type = Scalar.QUOTE_DOUBLE;
      value = doubleQuotedValue(source, _onError);
      break;
    /* istanbul ignore next should not happen */
    default:
      onError(scalar, "UNEXPECTED_TOKEN", `Expected a flow scalar value, but found: ${type}`);
      return {
        value: "",
        type: null,
        comment: "",
        range: [offset, offset + source.length, offset + source.length]
      };
  }
  const valueEnd = offset + source.length;
  const re2 = resolveEnd(end, valueEnd, strict, onError);
  return {
    value,
    type: _type,
    comment: re2.comment,
    range: [offset, valueEnd, re2.offset]
  };
}
function plainValue(source, onError) {
  let badChar = "";
  switch (source[0]) {
    /* istanbul ignore next should not happen */
    case "	":
      badChar = "a tab character";
      break;
    case ",":
      badChar = "flow indicator character ,";
      break;
    case "%":
      badChar = "directive indicator character %";
      break;
    case "|":
    case ">": {
      badChar = `block scalar indicator ${source[0]}`;
      break;
    }
    case "@":
    case "`": {
      badChar = `reserved character ${source[0]}`;
      break;
    }
  }
  if (badChar)
    onError(0, "BAD_SCALAR_START", `Plain value cannot start with ${badChar}`);
  return foldLines(source);
}
function singleQuotedValue(source, onError) {
  if (source[source.length - 1] !== "'" || source.length === 1)
    onError(source.length, "MISSING_CHAR", "Missing closing 'quote");
  return foldLines(source.slice(1, -1)).replace(/''/g, "'");
}
function foldLines(source) {
  let first, line2;
  try {
    first = new RegExp("(.*?)(?<![ 	])[ 	]*\r?\n", "sy");
    line2 = new RegExp("[ 	]*(.*?)(?:(?<![ 	])[ 	]*)?\r?\n", "sy");
  } catch {
    first = /(.*?)[ \t]*\r?\n/sy;
    line2 = /[ \t]*(.*?)[ \t]*\r?\n/sy;
  }
  let match2 = first.exec(source);
  if (!match2)
    return source;
  let res = match2[1];
  let sep = " ";
  let pos = first.lastIndex;
  line2.lastIndex = pos;
  while (match2 = line2.exec(source)) {
    if (match2[1] === "") {
      if (sep === "\n")
        res += sep;
      else
        sep = "\n";
    } else {
      res += sep + match2[1];
      sep = " ";
    }
    pos = line2.lastIndex;
  }
  const last2 = /[ \t]*(.*)/sy;
  last2.lastIndex = pos;
  match2 = last2.exec(source);
  return res + sep + (match2?.[1] ?? "");
}
function doubleQuotedValue(source, onError) {
  let res = "";
  for (let i2 = 1; i2 < source.length - 1; ++i2) {
    const ch = source[i2];
    if (ch === "\r" && source[i2 + 1] === "\n")
      continue;
    if (ch === "\n") {
      const { fold, offset } = foldNewline(source, i2);
      res += fold;
      i2 = offset;
    } else if (ch === "\\") {
      let next2 = source[++i2];
      const cc = escapeCodes[next2];
      if (cc)
        res += cc;
      else if (next2 === "\n") {
        next2 = source[i2 + 1];
        while (next2 === " " || next2 === "	")
          next2 = source[++i2 + 1];
      } else if (next2 === "\r" && source[i2 + 1] === "\n") {
        next2 = source[++i2 + 1];
        while (next2 === " " || next2 === "	")
          next2 = source[++i2 + 1];
      } else if (next2 === "x" || next2 === "u" || next2 === "U") {
        const length2 = { x: 2, u: 4, U: 8 }[next2];
        res += parseCharCode(source, i2 + 1, length2, onError);
        i2 += length2;
      } else {
        const raw = source.substr(i2 - 1, 2);
        onError(i2 - 1, "BAD_DQ_ESCAPE", `Invalid escape sequence ${raw}`);
        res += raw;
      }
    } else if (ch === " " || ch === "	") {
      const wsStart = i2;
      let next2 = source[i2 + 1];
      while (next2 === " " || next2 === "	")
        next2 = source[++i2 + 1];
      if (next2 !== "\n" && !(next2 === "\r" && source[i2 + 2] === "\n"))
        res += i2 > wsStart ? source.slice(wsStart, i2 + 1) : ch;
    } else {
      res += ch;
    }
  }
  if (source[source.length - 1] !== '"' || source.length === 1)
    onError(source.length, "MISSING_CHAR", 'Missing closing "quote');
  return res;
}
function foldNewline(source, offset) {
  let fold = "";
  let ch = source[offset + 1];
  while (ch === " " || ch === "	" || ch === "\n" || ch === "\r") {
    if (ch === "\r" && source[offset + 2] !== "\n")
      break;
    if (ch === "\n")
      fold += "\n";
    offset += 1;
    ch = source[offset + 1];
  }
  if (!fold)
    fold = " ";
  return { fold, offset };
}
function parseCharCode(source, offset, length2, onError) {
  const cc = source.substr(offset, length2);
  const ok = cc.length === length2 && /^[0-9a-fA-F]+$/.test(cc);
  const code = ok ? parseInt(cc, 16) : NaN;
  if (isNaN(code)) {
    const raw = source.substr(offset - 2, length2 + 2);
    onError(offset - 2, "BAD_DQ_ESCAPE", `Invalid escape sequence ${raw}`);
    return raw;
  }
  return String.fromCodePoint(code);
}
var escapeCodes;
var init_resolve_flow_scalar = __esm({
  "node_modules/yaml/browser/dist/compose/resolve-flow-scalar.js"() {
    init_Scalar();
    init_resolve_end();
    escapeCodes = {
      "0": "\0",
      // null character
      a: "\x07",
      // bell character
      b: "\b",
      // backspace
      e: "\x1B",
      // escape character
      f: "\f",
      // form feed
      n: "\n",
      // line feed
      r: "\r",
      // carriage return
      t: "	",
      // horizontal tab
      v: "\v",
      // vertical tab
      N: "",
      // Unicode next line
      _: " ",
      // Unicode non-breaking space
      L: "\u2028",
      // Unicode line separator
      P: "\u2029",
      // Unicode paragraph separator
      " ": " ",
      '"': '"',
      "/": "/",
      "\\": "\\",
      "	": "	"
    };
  }
});

// node_modules/yaml/browser/dist/compose/compose-scalar.js
function composeScalar(ctx, token2, tagToken, onError) {
  const { value, type, comment: comment2, range } = token2.type === "block-scalar" ? resolveBlockScalar(ctx, token2, onError) : resolveFlowScalar(token2, ctx.options.strict, onError);
  const tagName = tagToken ? ctx.directives.tagName(tagToken.source, (msg) => onError(tagToken, "TAG_RESOLVE_FAILED", msg)) : null;
  let tag;
  if (ctx.options.stringKeys && ctx.atKey) {
    tag = ctx.schema[SCALAR];
  } else if (tagName)
    tag = findScalarTagByName(ctx.schema, value, tagName, tagToken, onError);
  else if (token2.type === "scalar")
    tag = findScalarTagByTest(ctx, value, token2, onError);
  else
    tag = ctx.schema[SCALAR];
  let scalar;
  try {
    const res = tag.resolve(value, (msg) => onError(tagToken ?? token2, "TAG_RESOLVE_FAILED", msg), ctx.options);
    scalar = isScalar(res) ? res : new Scalar(res);
  } catch (error) {
    const msg = error instanceof Error ? error.message : String(error);
    onError(tagToken ?? token2, "TAG_RESOLVE_FAILED", msg);
    scalar = new Scalar(value);
  }
  scalar.range = range;
  scalar.source = value;
  if (type)
    scalar.type = type;
  if (tagName)
    scalar.tag = tagName;
  if (tag.format)
    scalar.format = tag.format;
  if (comment2)
    scalar.comment = comment2;
  return scalar;
}
function findScalarTagByName(schema4, value, tagName, tagToken, onError) {
  if (tagName === "!")
    return schema4[SCALAR];
  const matchWithTest = [];
  for (const tag of schema4.tags) {
    if (!tag.collection && tag.tag === tagName) {
      if (tag.default && tag.test)
        matchWithTest.push(tag);
      else
        return tag;
    }
  }
  for (const tag of matchWithTest)
    if (tag.test?.test(value))
      return tag;
  const kt = schema4.knownTags[tagName];
  if (kt && !kt.collection) {
    schema4.tags.push(Object.assign({}, kt, { default: false, test: void 0 }));
    return kt;
  }
  onError(tagToken, "TAG_RESOLVE_FAILED", `Unresolved tag: ${tagName}`, tagName !== "tag:yaml.org,2002:str");
  return schema4[SCALAR];
}
function findScalarTagByTest({ atKey, directives, schema: schema4 }, value, token2, onError) {
  const tag = schema4.tags.find((tag2) => (tag2.default === true || atKey && tag2.default === "key") && tag2.test?.test(value)) || schema4[SCALAR];
  if (schema4.compat) {
    const compat = schema4.compat.find((tag2) => tag2.default && tag2.test?.test(value)) ?? schema4[SCALAR];
    if (tag.tag !== compat.tag) {
      const ts = directives.tagString(tag.tag);
      const cs = directives.tagString(compat.tag);
      const msg = `Value may be parsed as either ${ts} or ${cs}`;
      onError(token2, "TAG_RESOLVE_FAILED", msg, true);
    }
  }
  return tag;
}
var init_compose_scalar = __esm({
  "node_modules/yaml/browser/dist/compose/compose-scalar.js"() {
    init_identity();
    init_Scalar();
    init_resolve_block_scalar();
    init_resolve_flow_scalar();
  }
});

// node_modules/yaml/browser/dist/compose/util-empty-scalar-position.js
function emptyScalarPosition(offset, before, pos) {
  if (before) {
    pos ?? (pos = before.length);
    for (let i2 = pos - 1; i2 >= 0; --i2) {
      let st2 = before[i2];
      switch (st2.type) {
        case "space":
        case "comment":
        case "newline":
          offset -= st2.source.length;
          continue;
      }
      st2 = before[++i2];
      while (st2?.type === "space") {
        offset += st2.source.length;
        st2 = before[++i2];
      }
      break;
    }
  }
  return offset;
}
var init_util_empty_scalar_position = __esm({
  "node_modules/yaml/browser/dist/compose/util-empty-scalar-position.js"() {
  }
});

// node_modules/yaml/browser/dist/compose/compose-node.js
function composeNode(ctx, token2, props, onError) {
  const atKey = ctx.atKey;
  const { spaceBefore, comment: comment2, anchor, tag } = props;
  let node2;
  let isSrcToken = true;
  switch (token2.type) {
    case "alias":
      node2 = composeAlias(ctx, token2, onError);
      if (anchor || tag)
        onError(token2, "ALIAS_PROPS", "An alias node must not specify any properties");
      break;
    case "scalar":
    case "single-quoted-scalar":
    case "double-quoted-scalar":
    case "block-scalar":
      node2 = composeScalar(ctx, token2, tag, onError);
      if (anchor)
        node2.anchor = anchor.source.substring(1);
      break;
    case "block-map":
    case "block-seq":
    case "flow-collection":
      node2 = composeCollection(CN, ctx, token2, props, onError);
      if (anchor)
        node2.anchor = anchor.source.substring(1);
      break;
    default: {
      const message = token2.type === "error" ? token2.message : `Unsupported token (type: ${token2.type})`;
      onError(token2, "UNEXPECTED_TOKEN", message);
      node2 = composeEmptyNode(ctx, token2.offset, void 0, null, props, onError);
      isSrcToken = false;
    }
  }
  if (anchor && node2.anchor === "")
    onError(anchor, "BAD_ALIAS", "Anchor cannot be an empty string");
  if (atKey && ctx.options.stringKeys && (!isScalar(node2) || typeof node2.value !== "string" || node2.tag && node2.tag !== "tag:yaml.org,2002:str")) {
    const msg = "With stringKeys, all keys must be strings";
    onError(tag ?? token2, "NON_STRING_KEY", msg);
  }
  if (spaceBefore)
    node2.spaceBefore = true;
  if (comment2) {
    if (token2.type === "scalar" && token2.source === "")
      node2.comment = comment2;
    else
      node2.commentBefore = comment2;
  }
  if (ctx.options.keepSourceTokens && isSrcToken)
    node2.srcToken = token2;
  return node2;
}
function composeEmptyNode(ctx, offset, before, pos, { spaceBefore, comment: comment2, anchor, tag, end }, onError) {
  const token2 = {
    type: "scalar",
    offset: emptyScalarPosition(offset, before, pos),
    indent: -1,
    source: ""
  };
  const node2 = composeScalar(ctx, token2, tag, onError);
  if (anchor) {
    node2.anchor = anchor.source.substring(1);
    if (node2.anchor === "")
      onError(anchor, "BAD_ALIAS", "Anchor cannot be an empty string");
  }
  if (spaceBefore)
    node2.spaceBefore = true;
  if (comment2) {
    node2.comment = comment2;
    node2.range[2] = end;
  }
  return node2;
}
function composeAlias({ options }, { offset, source, end }, onError) {
  const alias = new Alias(source.substring(1));
  if (alias.source === "")
    onError(offset, "BAD_ALIAS", "Alias cannot be an empty string");
  if (alias.source.endsWith(":"))
    onError(offset + source.length - 1, "BAD_ALIAS", "Alias ending in : is ambiguous", true);
  const valueEnd = offset + source.length;
  const re2 = resolveEnd(end, valueEnd, options.strict, onError);
  alias.range = [offset, valueEnd, re2.offset];
  if (re2.comment)
    alias.comment = re2.comment;
  return alias;
}
var CN;
var init_compose_node = __esm({
  "node_modules/yaml/browser/dist/compose/compose-node.js"() {
    init_Alias();
    init_identity();
    init_compose_collection();
    init_compose_scalar();
    init_resolve_end();
    init_util_empty_scalar_position();
    CN = { composeNode, composeEmptyNode };
  }
});

// node_modules/yaml/browser/dist/compose/compose-doc.js
function composeDoc(options, directives, { offset, start, value, end }, onError) {
  const opts = Object.assign({ _directives: directives }, options);
  const doc = new Document(void 0, opts);
  const ctx = {
    atKey: false,
    atRoot: true,
    directives: doc.directives,
    options: doc.options,
    schema: doc.schema
  };
  const props = resolveProps(start, {
    indicator: "doc-start",
    next: value ?? end?.[0],
    offset,
    onError,
    parentIndent: 0,
    startOnNewline: true
  });
  if (props.found) {
    doc.directives.docStart = true;
    if (value && (value.type === "block-map" || value.type === "block-seq") && !props.hasNewline)
      onError(props.end, "MISSING_CHAR", "Block collection cannot start on same line with directives-end marker");
  }
  doc.contents = value ? composeNode(ctx, value, props, onError) : composeEmptyNode(ctx, props.end, start, null, props, onError);
  const contentEnd = doc.contents.range[2];
  const re2 = resolveEnd(end, contentEnd, false, onError);
  if (re2.comment)
    doc.comment = re2.comment;
  doc.range = [offset, contentEnd, re2.offset];
  return doc;
}
var init_compose_doc = __esm({
  "node_modules/yaml/browser/dist/compose/compose-doc.js"() {
    init_Document();
    init_compose_node();
    init_resolve_end();
    init_resolve_props();
  }
});

// node_modules/yaml/browser/dist/compose/composer.js
function getErrorPos(src) {
  if (typeof src === "number")
    return [src, src + 1];
  if (Array.isArray(src))
    return src.length === 2 ? src : [src[0], src[1]];
  const { offset, source } = src;
  return [offset, offset + (typeof source === "string" ? source.length : 1)];
}
function parsePrelude(prelude) {
  let comment2 = "";
  let atComment = false;
  let afterEmptyLine = false;
  for (let i2 = 0; i2 < prelude.length; ++i2) {
    const source = prelude[i2];
    switch (source[0]) {
      case "#":
        comment2 += (comment2 === "" ? "" : afterEmptyLine ? "\n\n" : "\n") + (source.substring(1) || " ");
        atComment = true;
        afterEmptyLine = false;
        break;
      case "%":
        if (prelude[i2 + 1]?.[0] !== "#")
          i2 += 1;
        atComment = false;
        break;
      default:
        if (!atComment)
          afterEmptyLine = true;
        atComment = false;
    }
  }
  return { comment: comment2, afterEmptyLine };
}
var Composer;
var init_composer = __esm({
  "node_modules/yaml/browser/dist/compose/composer.js"() {
    init_directives();
    init_Document();
    init_errors();
    init_identity();
    init_compose_doc();
    init_resolve_end();
    Composer = class {
      constructor(options = {}) {
        this.doc = null;
        this.atDirectives = false;
        this.prelude = [];
        this.errors = [];
        this.warnings = [];
        this.onError = (source, code, message, warning) => {
          const pos = getErrorPos(source);
          if (warning)
            this.warnings.push(new YAMLWarning(pos, code, message));
          else
            this.errors.push(new YAMLParseError(pos, code, message));
        };
        this.directives = new Directives({ version: options.version || "1.2" });
        this.options = options;
      }
      decorate(doc, afterDoc) {
        const { comment: comment2, afterEmptyLine } = parsePrelude(this.prelude);
        if (comment2) {
          const dc = doc.contents;
          if (afterDoc) {
            doc.comment = doc.comment ? `${doc.comment}
${comment2}` : comment2;
          } else if (afterEmptyLine || doc.directives.docStart || !dc) {
            doc.commentBefore = comment2;
          } else if (isCollection(dc) && !dc.flow && dc.items.length > 0) {
            let it2 = dc.items[0];
            if (isPair(it2))
              it2 = it2.key;
            const cb = it2.commentBefore;
            it2.commentBefore = cb ? `${comment2}
${cb}` : comment2;
          } else {
            const cb = dc.commentBefore;
            dc.commentBefore = cb ? `${comment2}
${cb}` : comment2;
          }
        }
        if (afterDoc) {
          Array.prototype.push.apply(doc.errors, this.errors);
          Array.prototype.push.apply(doc.warnings, this.warnings);
        } else {
          doc.errors = this.errors;
          doc.warnings = this.warnings;
        }
        this.prelude = [];
        this.errors = [];
        this.warnings = [];
      }
      /**
       * Current stream status information.
       *
       * Mostly useful at the end of input for an empty stream.
       */
      streamInfo() {
        return {
          comment: parsePrelude(this.prelude).comment,
          directives: this.directives,
          errors: this.errors,
          warnings: this.warnings
        };
      }
      /**
       * Compose tokens into documents.
       *
       * @param forceDoc - If the stream contains no document, still emit a final document including any comments and directives that would be applied to a subsequent document.
       * @param endOffset - Should be set if `forceDoc` is also set, to set the document range end and to indicate errors correctly.
       */
      *compose(tokens, forceDoc = false, endOffset = -1) {
        for (const token2 of tokens)
          yield* this.next(token2);
        yield* this.end(forceDoc, endOffset);
      }
      /** Advance the composer by one CST token. */
      *next(token2) {
        switch (token2.type) {
          case "directive":
            this.directives.add(token2.source, (offset, message, warning) => {
              const pos = getErrorPos(token2);
              pos[0] += offset;
              this.onError(pos, "BAD_DIRECTIVE", message, warning);
            });
            this.prelude.push(token2.source);
            this.atDirectives = true;
            break;
          case "document": {
            const doc = composeDoc(this.options, this.directives, token2, this.onError);
            if (this.atDirectives && !doc.directives.docStart)
              this.onError(token2, "MISSING_CHAR", "Missing directives-end/doc-start indicator line");
            this.decorate(doc, false);
            if (this.doc)
              yield this.doc;
            this.doc = doc;
            this.atDirectives = false;
            break;
          }
          case "byte-order-mark":
          case "space":
            break;
          case "comment":
          case "newline":
            this.prelude.push(token2.source);
            break;
          case "error": {
            const msg = token2.source ? `${token2.message}: ${JSON.stringify(token2.source)}` : token2.message;
            const error = new YAMLParseError(getErrorPos(token2), "UNEXPECTED_TOKEN", msg);
            if (this.atDirectives || !this.doc)
              this.errors.push(error);
            else
              this.doc.errors.push(error);
            break;
          }
          case "doc-end": {
            if (!this.doc) {
              const msg = "Unexpected doc-end without preceding document";
              this.errors.push(new YAMLParseError(getErrorPos(token2), "UNEXPECTED_TOKEN", msg));
              break;
            }
            this.doc.directives.docEnd = true;
            const end = resolveEnd(token2.end, token2.offset + token2.source.length, this.doc.options.strict, this.onError);
            this.decorate(this.doc, true);
            if (end.comment) {
              const dc = this.doc.comment;
              this.doc.comment = dc ? `${dc}
${end.comment}` : end.comment;
            }
            this.doc.range[2] = end.offset;
            break;
          }
          default:
            this.errors.push(new YAMLParseError(getErrorPos(token2), "UNEXPECTED_TOKEN", `Unsupported token ${token2.type}`));
        }
      }
      /**
       * Call at end of input to yield any remaining document.
       *
       * @param forceDoc - If the stream contains no document, still emit a final document including any comments and directives that would be applied to a subsequent document.
       * @param endOffset - Should be set if `forceDoc` is also set, to set the document range end and to indicate errors correctly.
       */
      *end(forceDoc = false, endOffset = -1) {
        if (this.doc) {
          this.decorate(this.doc, true);
          yield this.doc;
          this.doc = null;
        } else if (forceDoc) {
          const opts = Object.assign({ _directives: this.directives }, this.options);
          const doc = new Document(void 0, opts);
          if (this.atDirectives)
            this.onError(endOffset, "MISSING_CHAR", "Missing directives-end indicator line");
          doc.range = [0, endOffset, endOffset];
          this.decorate(doc, false);
          yield doc;
        }
      }
    };
  }
});

// node_modules/yaml/browser/dist/parse/cst-scalar.js
function resolveAsScalar(token2, strict = true, onError) {
  if (token2) {
    const _onError = (pos, code, message) => {
      const offset = typeof pos === "number" ? pos : Array.isArray(pos) ? pos[0] : pos.offset;
      if (onError)
        onError(offset, code, message);
      else
        throw new YAMLParseError([offset, offset + 1], code, message);
    };
    switch (token2.type) {
      case "scalar":
      case "single-quoted-scalar":
      case "double-quoted-scalar":
        return resolveFlowScalar(token2, strict, _onError);
      case "block-scalar":
        return resolveBlockScalar({ options: { strict } }, token2, _onError);
    }
  }
  return null;
}
function createScalarToken(value, context) {
  const { implicitKey = false, indent, inFlow = false, offset = -1, type = "PLAIN" } = context;
  const source = stringifyString({ type, value }, {
    implicitKey,
    indent: indent > 0 ? " ".repeat(indent) : "",
    inFlow,
    options: { blockQuote: true, lineWidth: -1 }
  });
  const end = context.end ?? [
    { type: "newline", offset: -1, indent, source: "\n" }
  ];
  switch (source[0]) {
    case "|":
    case ">": {
      const he2 = source.indexOf("\n");
      const head = source.substring(0, he2);
      const body = source.substring(he2 + 1) + "\n";
      const props = [
        { type: "block-scalar-header", offset, indent, source: head }
      ];
      if (!addEndtoBlockProps(props, end))
        props.push({ type: "newline", offset: -1, indent, source: "\n" });
      return { type: "block-scalar", offset, indent, props, source: body };
    }
    case '"':
      return { type: "double-quoted-scalar", offset, indent, source, end };
    case "'":
      return { type: "single-quoted-scalar", offset, indent, source, end };
    default:
      return { type: "scalar", offset, indent, source, end };
  }
}
function setScalarValue(token2, value, context = {}) {
  let { afterKey = false, implicitKey = false, inFlow = false, type } = context;
  let indent = "indent" in token2 ? token2.indent : null;
  if (afterKey && typeof indent === "number")
    indent += 2;
  if (!type)
    switch (token2.type) {
      case "single-quoted-scalar":
        type = "QUOTE_SINGLE";
        break;
      case "double-quoted-scalar":
        type = "QUOTE_DOUBLE";
        break;
      case "block-scalar": {
        const header = token2.props[0];
        if (header.type !== "block-scalar-header")
          throw new Error("Invalid block scalar header");
        type = header.source[0] === ">" ? "BLOCK_FOLDED" : "BLOCK_LITERAL";
        break;
      }
      default:
        type = "PLAIN";
    }
  const source = stringifyString({ type, value }, {
    implicitKey: implicitKey || indent === null,
    indent: indent !== null && indent > 0 ? " ".repeat(indent) : "",
    inFlow,
    options: { blockQuote: true, lineWidth: -1 }
  });
  switch (source[0]) {
    case "|":
    case ">":
      setBlockScalarValue(token2, source);
      break;
    case '"':
      setFlowScalarValue(token2, source, "double-quoted-scalar");
      break;
    case "'":
      setFlowScalarValue(token2, source, "single-quoted-scalar");
      break;
    default:
      setFlowScalarValue(token2, source, "scalar");
  }
}
function setBlockScalarValue(token2, source) {
  const he2 = source.indexOf("\n");
  const head = source.substring(0, he2);
  const body = source.substring(he2 + 1) + "\n";
  if (token2.type === "block-scalar") {
    const header = token2.props[0];
    if (header.type !== "block-scalar-header")
      throw new Error("Invalid block scalar header");
    header.source = head;
    token2.source = body;
  } else {
    const { offset } = token2;
    const indent = "indent" in token2 ? token2.indent : -1;
    const props = [
      { type: "block-scalar-header", offset, indent, source: head }
    ];
    if (!addEndtoBlockProps(props, "end" in token2 ? token2.end : void 0))
      props.push({ type: "newline", offset: -1, indent, source: "\n" });
    for (const key2 of Object.keys(token2))
      if (key2 !== "type" && key2 !== "offset")
        delete token2[key2];
    Object.assign(token2, { type: "block-scalar", indent, props, source: body });
  }
}
function addEndtoBlockProps(props, end) {
  if (end)
    for (const st2 of end)
      switch (st2.type) {
        case "space":
        case "comment":
          props.push(st2);
          break;
        case "newline":
          props.push(st2);
          return true;
      }
  return false;
}
function setFlowScalarValue(token2, source, type) {
  switch (token2.type) {
    case "scalar":
    case "double-quoted-scalar":
    case "single-quoted-scalar":
      token2.type = type;
      token2.source = source;
      break;
    case "block-scalar": {
      const end = token2.props.slice(1);
      let oa = source.length;
      if (token2.props[0].type === "block-scalar-header")
        oa -= token2.props[0].source.length;
      for (const tok of end)
        tok.offset += oa;
      delete token2.props;
      Object.assign(token2, { type, source, end });
      break;
    }
    case "block-map":
    case "block-seq": {
      const offset = token2.offset + source.length;
      const nl = { type: "newline", offset, indent: token2.indent, source: "\n" };
      delete token2.items;
      Object.assign(token2, { type, source, end: [nl] });
      break;
    }
    default: {
      const indent = "indent" in token2 ? token2.indent : -1;
      const end = "end" in token2 && Array.isArray(token2.end) ? token2.end.filter((st2) => st2.type === "space" || st2.type === "comment" || st2.type === "newline") : [];
      for (const key2 of Object.keys(token2))
        if (key2 !== "type" && key2 !== "offset")
          delete token2[key2];
      Object.assign(token2, { type, indent, source, end });
    }
  }
}
var init_cst_scalar = __esm({
  "node_modules/yaml/browser/dist/parse/cst-scalar.js"() {
    init_resolve_block_scalar();
    init_resolve_flow_scalar();
    init_errors();
    init_stringifyString();
  }
});

// node_modules/yaml/browser/dist/parse/cst-stringify.js
function stringifyToken(token2) {
  switch (token2.type) {
    case "block-scalar": {
      let res = "";
      for (const tok of token2.props)
        res += stringifyToken(tok);
      return res + token2.source;
    }
    case "block-map":
    case "block-seq": {
      let res = "";
      for (const item of token2.items)
        res += stringifyItem(item);
      return res;
    }
    case "flow-collection": {
      let res = token2.start.source;
      for (const item of token2.items)
        res += stringifyItem(item);
      for (const st2 of token2.end)
        res += st2.source;
      return res;
    }
    case "document": {
      let res = stringifyItem(token2);
      if (token2.end)
        for (const st2 of token2.end)
          res += st2.source;
      return res;
    }
    default: {
      let res = token2.source;
      if ("end" in token2 && token2.end)
        for (const st2 of token2.end)
          res += st2.source;
      return res;
    }
  }
}
function stringifyItem({ start, key: key2, sep, value }) {
  let res = "";
  for (const st2 of start)
    res += st2.source;
  if (key2)
    res += stringifyToken(key2);
  if (sep)
    for (const st2 of sep)
      res += st2.source;
  if (value)
    res += stringifyToken(value);
  return res;
}
var stringify2;
var init_cst_stringify = __esm({
  "node_modules/yaml/browser/dist/parse/cst-stringify.js"() {
    stringify2 = (cst) => "type" in cst ? stringifyToken(cst) : stringifyItem(cst);
  }
});

// node_modules/yaml/browser/dist/parse/cst-visit.js
function visit2(cst, visitor) {
  if ("type" in cst && cst.type === "document")
    cst = { start: cst.start, value: cst.value };
  _visit(Object.freeze([]), cst, visitor);
}
function _visit(path, item, visitor) {
  let ctrl = visitor(item, path);
  if (typeof ctrl === "symbol")
    return ctrl;
  for (const field of ["key", "value"]) {
    const token2 = item[field];
    if (token2 && "items" in token2) {
      for (let i2 = 0; i2 < token2.items.length; ++i2) {
        const ci = _visit(Object.freeze(path.concat([[field, i2]])), token2.items[i2], visitor);
        if (typeof ci === "number")
          i2 = ci - 1;
        else if (ci === BREAK2)
          return BREAK2;
        else if (ci === REMOVE2) {
          token2.items.splice(i2, 1);
          i2 -= 1;
        }
      }
      if (typeof ctrl === "function" && field === "key")
        ctrl = ctrl(item, path);
    }
  }
  return typeof ctrl === "function" ? ctrl(item, path) : ctrl;
}
var BREAK2, SKIP2, REMOVE2;
var init_cst_visit = __esm({
  "node_modules/yaml/browser/dist/parse/cst-visit.js"() {
    BREAK2 = Symbol("break visit");
    SKIP2 = Symbol("skip children");
    REMOVE2 = Symbol("remove item");
    visit2.BREAK = BREAK2;
    visit2.SKIP = SKIP2;
    visit2.REMOVE = REMOVE2;
    visit2.itemAtPath = (cst, path) => {
      let item = cst;
      for (const [field, index] of path) {
        const tok = item?.[field];
        if (tok && "items" in tok) {
          item = tok.items[index];
        } else
          return void 0;
      }
      return item;
    };
    visit2.parentCollection = (cst, path) => {
      const parent = visit2.itemAtPath(cst, path.slice(0, -1));
      const field = path[path.length - 1][0];
      const coll = parent?.[field];
      if (coll && "items" in coll)
        return coll;
      throw new Error("Parent collection not found");
    };
  }
});

// node_modules/yaml/browser/dist/parse/cst.js
var cst_exports = {};
__export(cst_exports, {
  BOM: () => BOM,
  DOCUMENT: () => DOCUMENT,
  FLOW_END: () => FLOW_END,
  SCALAR: () => SCALAR2,
  createScalarToken: () => createScalarToken,
  isCollection: () => isCollection2,
  isScalar: () => isScalar2,
  prettyToken: () => prettyToken,
  resolveAsScalar: () => resolveAsScalar,
  setScalarValue: () => setScalarValue,
  stringify: () => stringify2,
  tokenType: () => tokenType,
  visit: () => visit2
});
function prettyToken(token2) {
  switch (token2) {
    case BOM:
      return "<BOM>";
    case DOCUMENT:
      return "<DOC>";
    case FLOW_END:
      return "<FLOW_END>";
    case SCALAR2:
      return "<SCALAR>";
    default:
      return JSON.stringify(token2);
  }
}
function tokenType(source) {
  switch (source) {
    case BOM:
      return "byte-order-mark";
    case DOCUMENT:
      return "doc-mode";
    case FLOW_END:
      return "flow-error-end";
    case SCALAR2:
      return "scalar";
    case "---":
      return "doc-start";
    case "...":
      return "doc-end";
    case "":
    case "\n":
    case "\r\n":
      return "newline";
    case "-":
      return "seq-item-ind";
    case "?":
      return "explicit-key-ind";
    case ":":
      return "map-value-ind";
    case "{":
      return "flow-map-start";
    case "}":
      return "flow-map-end";
    case "[":
      return "flow-seq-start";
    case "]":
      return "flow-seq-end";
    case ",":
      return "comma";
  }
  switch (source[0]) {
    case " ":
    case "	":
      return "space";
    case "#":
      return "comment";
    case "%":
      return "directive-line";
    case "*":
      return "alias";
    case "&":
      return "anchor";
    case "!":
      return "tag";
    case "'":
      return "single-quoted-scalar";
    case '"':
      return "double-quoted-scalar";
    case "|":
    case ">":
      return "block-scalar-header";
  }
  return null;
}
var BOM, DOCUMENT, FLOW_END, SCALAR2, isCollection2, isScalar2;
var init_cst = __esm({
  "node_modules/yaml/browser/dist/parse/cst.js"() {
    init_cst_scalar();
    init_cst_stringify();
    init_cst_visit();
    BOM = "\uFEFF";
    DOCUMENT = "";
    FLOW_END = "";
    SCALAR2 = "";
    isCollection2 = (token2) => !!token2 && "items" in token2;
    isScalar2 = (token2) => !!token2 && (token2.type === "scalar" || token2.type === "single-quoted-scalar" || token2.type === "double-quoted-scalar" || token2.type === "block-scalar");
  }
});

// node_modules/yaml/browser/dist/parse/lexer.js
function isEmpty(ch) {
  switch (ch) {
    case void 0:
    case " ":
    case "\n":
    case "\r":
    case "	":
      return true;
    default:
      return false;
  }
}
var hexDigits, tagChars, flowIndicatorChars, invalidAnchorChars, isNotAnchorChar, Lexer;
var init_lexer = __esm({
  "node_modules/yaml/browser/dist/parse/lexer.js"() {
    init_cst();
    hexDigits = new Set("0123456789ABCDEFabcdef");
    tagChars = new Set("0123456789ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz-#;/?:@&=+$_.!~*'()");
    flowIndicatorChars = new Set(",[]{}");
    invalidAnchorChars = new Set(" ,[]{}\n\r	");
    isNotAnchorChar = (ch) => !ch || invalidAnchorChars.has(ch);
    Lexer = class {
      constructor() {
        this.atEnd = false;
        this.blockScalarIndent = -1;
        this.blockScalarKeep = false;
        this.buffer = "";
        this.flowKey = false;
        this.flowLevel = 0;
        this.indentNext = 0;
        this.indentValue = 0;
        this.lineEndPos = null;
        this.next = null;
        this.pos = 0;
      }
      /**
       * Generate YAML tokens from the `source` string. If `incomplete`,
       * a part of the last line may be left as a buffer for the next call.
       *
       * @returns A generator of lexical tokens
       */
      *lex(source, incomplete = false) {
        if (source) {
          if (typeof source !== "string")
            throw TypeError("source is not a string");
          this.buffer = this.buffer ? this.buffer + source : source;
          this.lineEndPos = null;
        }
        this.atEnd = !incomplete;
        let next2 = this.next ?? "stream";
        while (next2 && (incomplete || this.hasChars(1)))
          next2 = yield* this.parseNext(next2);
      }
      atLineEnd() {
        let i2 = this.pos;
        let ch = this.buffer[i2];
        while (ch === " " || ch === "	")
          ch = this.buffer[++i2];
        if (!ch || ch === "#" || ch === "\n")
          return true;
        if (ch === "\r")
          return this.buffer[i2 + 1] === "\n";
        return false;
      }
      charAt(n) {
        return this.buffer[this.pos + n];
      }
      continueScalar(offset) {
        let ch = this.buffer[offset];
        if (this.indentNext > 0) {
          let indent = 0;
          while (ch === " ")
            ch = this.buffer[++indent + offset];
          if (ch === "\r") {
            const next2 = this.buffer[indent + offset + 1];
            if (next2 === "\n" || !next2 && !this.atEnd)
              return offset + indent + 1;
          }
          return ch === "\n" || indent >= this.indentNext || !ch && !this.atEnd ? offset + indent : -1;
        }
        if (ch === "-" || ch === ".") {
          const dt2 = this.buffer.substr(offset, 3);
          if ((dt2 === "---" || dt2 === "...") && isEmpty(this.buffer[offset + 3]))
            return -1;
        }
        return offset;
      }
      getLine() {
        let end = this.lineEndPos;
        if (typeof end !== "number" || end !== -1 && end < this.pos) {
          end = this.buffer.indexOf("\n", this.pos);
          this.lineEndPos = end;
        }
        if (end === -1)
          return this.atEnd ? this.buffer.substring(this.pos) : null;
        if (this.buffer[end - 1] === "\r")
          end -= 1;
        return this.buffer.substring(this.pos, end);
      }
      hasChars(n) {
        return this.pos + n <= this.buffer.length;
      }
      setNext(state) {
        this.buffer = this.buffer.substring(this.pos);
        this.pos = 0;
        this.lineEndPos = null;
        this.next = state;
        return null;
      }
      peek(n) {
        return this.buffer.substr(this.pos, n);
      }
      *parseNext(next2) {
        switch (next2) {
          case "stream":
            return yield* this.parseStream();
          case "line-start":
            return yield* this.parseLineStart();
          case "block-start":
            return yield* this.parseBlockStart();
          case "doc":
            return yield* this.parseDocument();
          case "flow":
            return yield* this.parseFlowCollection();
          case "quoted-scalar":
            return yield* this.parseQuotedScalar();
          case "block-scalar":
            return yield* this.parseBlockScalar();
          case "plain-scalar":
            return yield* this.parsePlainScalar();
        }
      }
      *parseStream() {
        let line2 = this.getLine();
        if (line2 === null)
          return this.setNext("stream");
        if (line2[0] === BOM) {
          yield* this.pushCount(1);
          line2 = line2.substring(1);
        }
        if (line2[0] === "%") {
          let dirEnd = line2.length;
          let cs = line2.indexOf("#");
          while (cs !== -1) {
            const ch = line2[cs - 1];
            if (ch === " " || ch === "	") {
              dirEnd = cs - 1;
              break;
            } else {
              cs = line2.indexOf("#", cs + 1);
            }
          }
          while (true) {
            const ch = line2[dirEnd - 1];
            if (ch === " " || ch === "	")
              dirEnd -= 1;
            else
              break;
          }
          const n = (yield* this.pushCount(dirEnd)) + (yield* this.pushSpaces(true));
          yield* this.pushCount(line2.length - n);
          this.pushNewline();
          return "stream";
        }
        if (this.atLineEnd()) {
          const sp = yield* this.pushSpaces(true);
          yield* this.pushCount(line2.length - sp);
          yield* this.pushNewline();
          return "stream";
        }
        yield DOCUMENT;
        return yield* this.parseLineStart();
      }
      *parseLineStart() {
        const ch = this.charAt(0);
        if (!ch && !this.atEnd)
          return this.setNext("line-start");
        if (ch === "-" || ch === ".") {
          if (!this.atEnd && !this.hasChars(4))
            return this.setNext("line-start");
          const s2 = this.peek(3);
          if ((s2 === "---" || s2 === "...") && isEmpty(this.charAt(3))) {
            yield* this.pushCount(3);
            this.indentValue = 0;
            this.indentNext = 0;
            return s2 === "---" ? "doc" : "stream";
          }
        }
        this.indentValue = yield* this.pushSpaces(false);
        if (this.indentNext > this.indentValue && !isEmpty(this.charAt(1)))
          this.indentNext = this.indentValue;
        return yield* this.parseBlockStart();
      }
      *parseBlockStart() {
        const [ch0, ch1] = this.peek(2);
        if (!ch1 && !this.atEnd)
          return this.setNext("block-start");
        if ((ch0 === "-" || ch0 === "?" || ch0 === ":") && isEmpty(ch1)) {
          const n = (yield* this.pushCount(1)) + (yield* this.pushSpaces(true));
          this.indentNext = this.indentValue + 1;
          this.indentValue += n;
          return yield* this.parseBlockStart();
        }
        return "doc";
      }
      *parseDocument() {
        yield* this.pushSpaces(true);
        const line2 = this.getLine();
        if (line2 === null)
          return this.setNext("doc");
        let n = yield* this.pushIndicators();
        switch (line2[n]) {
          case "#":
            yield* this.pushCount(line2.length - n);
          // fallthrough
          case void 0:
            yield* this.pushNewline();
            return yield* this.parseLineStart();
          case "{":
          case "[":
            yield* this.pushCount(1);
            this.flowKey = false;
            this.flowLevel = 1;
            return "flow";
          case "}":
          case "]":
            yield* this.pushCount(1);
            return "doc";
          case "*":
            yield* this.pushUntil(isNotAnchorChar);
            return "doc";
          case '"':
          case "'":
            return yield* this.parseQuotedScalar();
          case "|":
          case ">":
            n += yield* this.parseBlockScalarHeader();
            n += yield* this.pushSpaces(true);
            yield* this.pushCount(line2.length - n);
            yield* this.pushNewline();
            return yield* this.parseBlockScalar();
          default:
            return yield* this.parsePlainScalar();
        }
      }
      *parseFlowCollection() {
        let nl, sp;
        let indent = -1;
        do {
          nl = yield* this.pushNewline();
          if (nl > 0) {
            sp = yield* this.pushSpaces(false);
            this.indentValue = indent = sp;
          } else {
            sp = 0;
          }
          sp += yield* this.pushSpaces(true);
        } while (nl + sp > 0);
        const line2 = this.getLine();
        if (line2 === null)
          return this.setNext("flow");
        if (indent !== -1 && indent < this.indentNext && line2[0] !== "#" || indent === 0 && (line2.startsWith("---") || line2.startsWith("...")) && isEmpty(line2[3])) {
          const atFlowEndMarker = indent === this.indentNext - 1 && this.flowLevel === 1 && (line2[0] === "]" || line2[0] === "}");
          if (!atFlowEndMarker) {
            this.flowLevel = 0;
            yield FLOW_END;
            return yield* this.parseLineStart();
          }
        }
        let n = 0;
        while (line2[n] === ",") {
          n += yield* this.pushCount(1);
          n += yield* this.pushSpaces(true);
          this.flowKey = false;
        }
        n += yield* this.pushIndicators();
        switch (line2[n]) {
          case void 0:
            return "flow";
          case "#":
            yield* this.pushCount(line2.length - n);
            return "flow";
          case "{":
          case "[":
            yield* this.pushCount(1);
            this.flowKey = false;
            this.flowLevel += 1;
            return "flow";
          case "}":
          case "]":
            yield* this.pushCount(1);
            this.flowKey = true;
            this.flowLevel -= 1;
            return this.flowLevel ? "flow" : "doc";
          case "*":
            yield* this.pushUntil(isNotAnchorChar);
            return "flow";
          case '"':
          case "'":
            this.flowKey = true;
            return yield* this.parseQuotedScalar();
          case ":": {
            const next2 = this.charAt(1);
            if (this.flowKey || isEmpty(next2) || next2 === ",") {
              this.flowKey = false;
              yield* this.pushCount(1);
              yield* this.pushSpaces(true);
              return "flow";
            }
          }
          // fallthrough
          default:
            this.flowKey = false;
            return yield* this.parsePlainScalar();
        }
      }
      *parseQuotedScalar() {
        const quote = this.charAt(0);
        let end = this.buffer.indexOf(quote, this.pos + 1);
        if (quote === "'") {
          while (end !== -1 && this.buffer[end + 1] === "'")
            end = this.buffer.indexOf("'", end + 2);
        } else {
          while (end !== -1) {
            let n = 0;
            while (this.buffer[end - 1 - n] === "\\")
              n += 1;
            if (n % 2 === 0)
              break;
            end = this.buffer.indexOf('"', end + 1);
          }
        }
        const qb = this.buffer.substring(0, end);
        let nl = qb.indexOf("\n", this.pos);
        if (nl !== -1) {
          while (nl !== -1) {
            const cs = this.continueScalar(nl + 1);
            if (cs === -1)
              break;
            nl = qb.indexOf("\n", cs);
          }
          if (nl !== -1) {
            end = nl - (qb[nl - 1] === "\r" ? 2 : 1);
          }
        }
        if (end === -1) {
          if (!this.atEnd)
            return this.setNext("quoted-scalar");
          end = this.buffer.length;
        }
        yield* this.pushToIndex(end + 1, false);
        return this.flowLevel ? "flow" : "doc";
      }
      *parseBlockScalarHeader() {
        this.blockScalarIndent = -1;
        this.blockScalarKeep = false;
        let i2 = this.pos;
        while (true) {
          const ch = this.buffer[++i2];
          if (ch === "+")
            this.blockScalarKeep = true;
          else if (ch > "0" && ch <= "9")
            this.blockScalarIndent = Number(ch) - 1;
          else if (ch !== "-")
            break;
        }
        return yield* this.pushUntil((ch) => isEmpty(ch) || ch === "#");
      }
      *parseBlockScalar() {
        let nl = this.pos - 1;
        let indent = 0;
        let ch;
        loop: for (let i3 = this.pos; ch = this.buffer[i3]; ++i3) {
          switch (ch) {
            case " ":
              indent += 1;
              break;
            case "\n":
              nl = i3;
              indent = 0;
              break;
            case "\r": {
              const next2 = this.buffer[i3 + 1];
              if (!next2 && !this.atEnd)
                return this.setNext("block-scalar");
              if (next2 === "\n")
                break;
            }
            // fallthrough
            default:
              break loop;
          }
        }
        if (!ch && !this.atEnd)
          return this.setNext("block-scalar");
        if (indent >= this.indentNext) {
          if (this.blockScalarIndent === -1)
            this.indentNext = indent;
          else {
            this.indentNext = this.blockScalarIndent + (this.indentNext === 0 ? 1 : this.indentNext);
          }
          do {
            const cs = this.continueScalar(nl + 1);
            if (cs === -1)
              break;
            nl = this.buffer.indexOf("\n", cs);
          } while (nl !== -1);
          if (nl === -1) {
            if (!this.atEnd)
              return this.setNext("block-scalar");
            nl = this.buffer.length;
          }
        }
        let i2 = nl + 1;
        ch = this.buffer[i2];
        while (ch === " ")
          ch = this.buffer[++i2];
        if (ch === "	") {
          while (ch === "	" || ch === " " || ch === "\r" || ch === "\n")
            ch = this.buffer[++i2];
          nl = i2 - 1;
        } else if (!this.blockScalarKeep) {
          do {
            let i3 = nl - 1;
            let ch2 = this.buffer[i3];
            if (ch2 === "\r")
              ch2 = this.buffer[--i3];
            const lastChar = i3;
            while (ch2 === " ")
              ch2 = this.buffer[--i3];
            if (ch2 === "\n" && i3 >= this.pos && i3 + 1 + indent > lastChar)
              nl = i3;
            else
              break;
          } while (true);
        }
        yield SCALAR2;
        yield* this.pushToIndex(nl + 1, true);
        return yield* this.parseLineStart();
      }
      *parsePlainScalar() {
        const inFlow = this.flowLevel > 0;
        let end = this.pos - 1;
        let i2 = this.pos - 1;
        let ch;
        while (ch = this.buffer[++i2]) {
          if (ch === ":") {
            const next2 = this.buffer[i2 + 1];
            if (isEmpty(next2) || inFlow && flowIndicatorChars.has(next2))
              break;
            end = i2;
          } else if (isEmpty(ch)) {
            let next2 = this.buffer[i2 + 1];
            if (ch === "\r") {
              if (next2 === "\n") {
                i2 += 1;
                ch = "\n";
                next2 = this.buffer[i2 + 1];
              } else
                end = i2;
            }
            if (next2 === "#" || inFlow && flowIndicatorChars.has(next2))
              break;
            if (ch === "\n") {
              const cs = this.continueScalar(i2 + 1);
              if (cs === -1)
                break;
              i2 = Math.max(i2, cs - 2);
            }
          } else {
            if (inFlow && flowIndicatorChars.has(ch))
              break;
            end = i2;
          }
        }
        if (!ch && !this.atEnd)
          return this.setNext("plain-scalar");
        yield SCALAR2;
        yield* this.pushToIndex(end + 1, true);
        return inFlow ? "flow" : "doc";
      }
      *pushCount(n) {
        if (n > 0) {
          yield this.buffer.substr(this.pos, n);
          this.pos += n;
          return n;
        }
        return 0;
      }
      *pushToIndex(i2, allowEmpty) {
        const s2 = this.buffer.slice(this.pos, i2);
        if (s2) {
          yield s2;
          this.pos += s2.length;
          return s2.length;
        } else if (allowEmpty)
          yield "";
        return 0;
      }
      *pushIndicators() {
        switch (this.charAt(0)) {
          case "!":
            return (yield* this.pushTag()) + (yield* this.pushSpaces(true)) + (yield* this.pushIndicators());
          case "&":
            return (yield* this.pushUntil(isNotAnchorChar)) + (yield* this.pushSpaces(true)) + (yield* this.pushIndicators());
          case "-":
          // this is an error
          case "?":
          // this is an error outside flow collections
          case ":": {
            const inFlow = this.flowLevel > 0;
            const ch1 = this.charAt(1);
            if (isEmpty(ch1) || inFlow && flowIndicatorChars.has(ch1)) {
              if (!inFlow)
                this.indentNext = this.indentValue + 1;
              else if (this.flowKey)
                this.flowKey = false;
              return (yield* this.pushCount(1)) + (yield* this.pushSpaces(true)) + (yield* this.pushIndicators());
            }
          }
        }
        return 0;
      }
      *pushTag() {
        if (this.charAt(1) === "<") {
          let i2 = this.pos + 2;
          let ch = this.buffer[i2];
          while (!isEmpty(ch) && ch !== ">")
            ch = this.buffer[++i2];
          return yield* this.pushToIndex(ch === ">" ? i2 + 1 : i2, false);
        } else {
          let i2 = this.pos + 1;
          let ch = this.buffer[i2];
          while (ch) {
            if (tagChars.has(ch))
              ch = this.buffer[++i2];
            else if (ch === "%" && hexDigits.has(this.buffer[i2 + 1]) && hexDigits.has(this.buffer[i2 + 2])) {
              ch = this.buffer[i2 += 3];
            } else
              break;
          }
          return yield* this.pushToIndex(i2, false);
        }
      }
      *pushNewline() {
        const ch = this.buffer[this.pos];
        if (ch === "\n")
          return yield* this.pushCount(1);
        else if (ch === "\r" && this.charAt(1) === "\n")
          return yield* this.pushCount(2);
        else
          return 0;
      }
      *pushSpaces(allowTabs) {
        let i2 = this.pos - 1;
        let ch;
        do {
          ch = this.buffer[++i2];
        } while (ch === " " || allowTabs && ch === "	");
        const n = i2 - this.pos;
        if (n > 0) {
          yield this.buffer.substr(this.pos, n);
          this.pos = i2;
        }
        return n;
      }
      *pushUntil(test) {
        let i2 = this.pos;
        let ch = this.buffer[i2];
        while (!test(ch))
          ch = this.buffer[++i2];
        return yield* this.pushToIndex(i2, false);
      }
    };
  }
});

// node_modules/yaml/browser/dist/parse/line-counter.js
var LineCounter;
var init_line_counter = __esm({
  "node_modules/yaml/browser/dist/parse/line-counter.js"() {
    LineCounter = class {
      constructor() {
        this.lineStarts = [];
        this.addNewLine = (offset) => this.lineStarts.push(offset);
        this.linePos = (offset) => {
          let low = 0;
          let high = this.lineStarts.length;
          while (low < high) {
            const mid = low + high >> 1;
            if (this.lineStarts[mid] < offset)
              low = mid + 1;
            else
              high = mid;
          }
          if (this.lineStarts[low] === offset)
            return { line: low + 1, col: 1 };
          if (low === 0)
            return { line: 0, col: offset };
          const start = this.lineStarts[low - 1];
          return { line: low, col: offset - start + 1 };
        };
      }
    };
  }
});

// node_modules/yaml/browser/dist/parse/parser.js
function includesToken(list, type) {
  for (let i2 = 0; i2 < list.length; ++i2)
    if (list[i2].type === type)
      return true;
  return false;
}
function findNonEmptyIndex(list) {
  for (let i2 = 0; i2 < list.length; ++i2) {
    switch (list[i2].type) {
      case "space":
      case "comment":
      case "newline":
        break;
      default:
        return i2;
    }
  }
  return -1;
}
function isFlowToken(token2) {
  switch (token2?.type) {
    case "alias":
    case "scalar":
    case "single-quoted-scalar":
    case "double-quoted-scalar":
    case "flow-collection":
      return true;
    default:
      return false;
  }
}
function getPrevProps(parent) {
  switch (parent.type) {
    case "document":
      return parent.start;
    case "block-map": {
      const it2 = parent.items[parent.items.length - 1];
      return it2.sep ?? it2.start;
    }
    case "block-seq":
      return parent.items[parent.items.length - 1].start;
    /* istanbul ignore next should not happen */
    default:
      return [];
  }
}
function getFirstKeyStartProps(prev2) {
  if (prev2.length === 0)
    return [];
  let i2 = prev2.length;
  loop: while (--i2 >= 0) {
    switch (prev2[i2].type) {
      case "doc-start":
      case "explicit-key-ind":
      case "map-value-ind":
      case "seq-item-ind":
      case "newline":
        break loop;
    }
  }
  while (prev2[++i2]?.type === "space") {
  }
  return prev2.splice(i2, prev2.length);
}
function fixFlowSeqItems(fc) {
  if (fc.start.type === "flow-seq-start") {
    for (const it2 of fc.items) {
      if (it2.sep && !it2.value && !includesToken(it2.start, "explicit-key-ind") && !includesToken(it2.sep, "map-value-ind")) {
        if (it2.key)
          it2.value = it2.key;
        delete it2.key;
        if (isFlowToken(it2.value)) {
          if (it2.value.end)
            Array.prototype.push.apply(it2.value.end, it2.sep);
          else
            it2.value.end = it2.sep;
        } else
          Array.prototype.push.apply(it2.start, it2.sep);
        delete it2.sep;
      }
    }
  }
}
var Parser;
var init_parser = __esm({
  "node_modules/yaml/browser/dist/parse/parser.js"() {
    init_cst();
    init_lexer();
    Parser = class {
      /**
       * @param onNewLine - If defined, called separately with the start position of
       *   each new line (in `parse()`, including the start of input).
       */
      constructor(onNewLine) {
        this.atNewLine = true;
        this.atScalar = false;
        this.indent = 0;
        this.offset = 0;
        this.onKeyLine = false;
        this.stack = [];
        this.source = "";
        this.type = "";
        this.lexer = new Lexer();
        this.onNewLine = onNewLine;
      }
      /**
       * Parse `source` as a YAML stream.
       * If `incomplete`, a part of the last line may be left as a buffer for the next call.
       *
       * Errors are not thrown, but yielded as `{ type: 'error', message }` tokens.
       *
       * @returns A generator of tokens representing each directive, document, and other structure.
       */
      *parse(source, incomplete = false) {
        if (this.onNewLine && this.offset === 0)
          this.onNewLine(0);
        for (const lexeme of this.lexer.lex(source, incomplete))
          yield* this.next(lexeme);
        if (!incomplete)
          yield* this.end();
      }
      /**
       * Advance the parser by the `source` of one lexical token.
       */
      *next(source) {
        this.source = source;
        if (this.atScalar) {
          this.atScalar = false;
          yield* this.step();
          this.offset += source.length;
          return;
        }
        const type = tokenType(source);
        if (!type) {
          const message = `Not a YAML token: ${source}`;
          yield* this.pop({ type: "error", offset: this.offset, message, source });
          this.offset += source.length;
        } else if (type === "scalar") {
          this.atNewLine = false;
          this.atScalar = true;
          this.type = "scalar";
        } else {
          this.type = type;
          yield* this.step();
          switch (type) {
            case "newline":
              this.atNewLine = true;
              this.indent = 0;
              if (this.onNewLine)
                this.onNewLine(this.offset + source.length);
              break;
            case "space":
              if (this.atNewLine && source[0] === " ")
                this.indent += source.length;
              break;
            case "explicit-key-ind":
            case "map-value-ind":
            case "seq-item-ind":
              if (this.atNewLine)
                this.indent += source.length;
              break;
            case "doc-mode":
            case "flow-error-end":
              return;
            default:
              this.atNewLine = false;
          }
          this.offset += source.length;
        }
      }
      /** Call at end of input to push out any remaining constructions */
      *end() {
        while (this.stack.length > 0)
          yield* this.pop();
      }
      get sourceToken() {
        const st2 = {
          type: this.type,
          offset: this.offset,
          indent: this.indent,
          source: this.source
        };
        return st2;
      }
      *step() {
        const top = this.peek(1);
        if (this.type === "doc-end" && (!top || top.type !== "doc-end")) {
          while (this.stack.length > 0)
            yield* this.pop();
          this.stack.push({
            type: "doc-end",
            offset: this.offset,
            source: this.source
          });
          return;
        }
        if (!top)
          return yield* this.stream();
        switch (top.type) {
          case "document":
            return yield* this.document(top);
          case "alias":
          case "scalar":
          case "single-quoted-scalar":
          case "double-quoted-scalar":
            return yield* this.scalar(top);
          case "block-scalar":
            return yield* this.blockScalar(top);
          case "block-map":
            return yield* this.blockMap(top);
          case "block-seq":
            return yield* this.blockSequence(top);
          case "flow-collection":
            return yield* this.flowCollection(top);
          case "doc-end":
            return yield* this.documentEnd(top);
        }
        yield* this.pop();
      }
      peek(n) {
        return this.stack[this.stack.length - n];
      }
      *pop(error) {
        const token2 = error ?? this.stack.pop();
        if (!token2) {
          const message = "Tried to pop an empty stack";
          yield { type: "error", offset: this.offset, source: "", message };
        } else if (this.stack.length === 0) {
          yield token2;
        } else {
          const top = this.peek(1);
          if (token2.type === "block-scalar") {
            token2.indent = "indent" in top ? top.indent : 0;
          } else if (token2.type === "flow-collection" && top.type === "document") {
            token2.indent = 0;
          }
          if (token2.type === "flow-collection")
            fixFlowSeqItems(token2);
          switch (top.type) {
            case "document":
              top.value = token2;
              break;
            case "block-scalar":
              top.props.push(token2);
              break;
            case "block-map": {
              const it2 = top.items[top.items.length - 1];
              if (it2.value) {
                top.items.push({ start: [], key: token2, sep: [] });
                this.onKeyLine = true;
                return;
              } else if (it2.sep) {
                it2.value = token2;
              } else {
                Object.assign(it2, { key: token2, sep: [] });
                this.onKeyLine = !it2.explicitKey;
                return;
              }
              break;
            }
            case "block-seq": {
              const it2 = top.items[top.items.length - 1];
              if (it2.value)
                top.items.push({ start: [], value: token2 });
              else
                it2.value = token2;
              break;
            }
            case "flow-collection": {
              const it2 = top.items[top.items.length - 1];
              if (!it2 || it2.value)
                top.items.push({ start: [], key: token2, sep: [] });
              else if (it2.sep)
                it2.value = token2;
              else
                Object.assign(it2, { key: token2, sep: [] });
              return;
            }
            /* istanbul ignore next should not happen */
            default:
              yield* this.pop();
              yield* this.pop(token2);
          }
          if ((top.type === "document" || top.type === "block-map" || top.type === "block-seq") && (token2.type === "block-map" || token2.type === "block-seq")) {
            const last2 = token2.items[token2.items.length - 1];
            if (last2 && !last2.sep && !last2.value && last2.start.length > 0 && findNonEmptyIndex(last2.start) === -1 && (token2.indent === 0 || last2.start.every((st2) => st2.type !== "comment" || st2.indent < token2.indent))) {
              if (top.type === "document")
                top.end = last2.start;
              else
                top.items.push({ start: last2.start });
              token2.items.splice(-1, 1);
            }
          }
        }
      }
      *stream() {
        switch (this.type) {
          case "directive-line":
            yield { type: "directive", offset: this.offset, source: this.source };
            return;
          case "byte-order-mark":
          case "space":
          case "comment":
          case "newline":
            yield this.sourceToken;
            return;
          case "doc-mode":
          case "doc-start": {
            const doc = {
              type: "document",
              offset: this.offset,
              start: []
            };
            if (this.type === "doc-start")
              doc.start.push(this.sourceToken);
            this.stack.push(doc);
            return;
          }
        }
        yield {
          type: "error",
          offset: this.offset,
          message: `Unexpected ${this.type} token in YAML stream`,
          source: this.source
        };
      }
      *document(doc) {
        if (doc.value)
          return yield* this.lineEnd(doc);
        switch (this.type) {
          case "doc-start": {
            if (findNonEmptyIndex(doc.start) !== -1) {
              yield* this.pop();
              yield* this.step();
            } else
              doc.start.push(this.sourceToken);
            return;
          }
          case "anchor":
          case "tag":
          case "space":
          case "comment":
          case "newline":
            doc.start.push(this.sourceToken);
            return;
        }
        const bv = this.startBlockValue(doc);
        if (bv)
          this.stack.push(bv);
        else {
          yield {
            type: "error",
            offset: this.offset,
            message: `Unexpected ${this.type} token in YAML document`,
            source: this.source
          };
        }
      }
      *scalar(scalar) {
        if (this.type === "map-value-ind") {
          const prev2 = getPrevProps(this.peek(2));
          const start = getFirstKeyStartProps(prev2);
          let sep;
          if (scalar.end) {
            sep = scalar.end;
            sep.push(this.sourceToken);
            delete scalar.end;
          } else
            sep = [this.sourceToken];
          const map3 = {
            type: "block-map",
            offset: scalar.offset,
            indent: scalar.indent,
            items: [{ start, key: scalar, sep }]
          };
          this.onKeyLine = true;
          this.stack[this.stack.length - 1] = map3;
        } else
          yield* this.lineEnd(scalar);
      }
      *blockScalar(scalar) {
        switch (this.type) {
          case "space":
          case "comment":
          case "newline":
            scalar.props.push(this.sourceToken);
            return;
          case "scalar":
            scalar.source = this.source;
            this.atNewLine = true;
            this.indent = 0;
            if (this.onNewLine) {
              let nl = this.source.indexOf("\n") + 1;
              while (nl !== 0) {
                this.onNewLine(this.offset + nl);
                nl = this.source.indexOf("\n", nl) + 1;
              }
            }
            yield* this.pop();
            break;
          /* istanbul ignore next should not happen */
          default:
            yield* this.pop();
            yield* this.step();
        }
      }
      *blockMap(map3) {
        const it2 = map3.items[map3.items.length - 1];
        switch (this.type) {
          case "newline":
            this.onKeyLine = false;
            if (it2.value) {
              const end = "end" in it2.value ? it2.value.end : void 0;
              const last2 = Array.isArray(end) ? end[end.length - 1] : void 0;
              if (last2?.type === "comment")
                end?.push(this.sourceToken);
              else
                map3.items.push({ start: [this.sourceToken] });
            } else if (it2.sep) {
              it2.sep.push(this.sourceToken);
            } else {
              it2.start.push(this.sourceToken);
            }
            return;
          case "space":
          case "comment":
            if (it2.value) {
              map3.items.push({ start: [this.sourceToken] });
            } else if (it2.sep) {
              it2.sep.push(this.sourceToken);
            } else {
              if (this.atIndentedComment(it2.start, map3.indent)) {
                const prev2 = map3.items[map3.items.length - 2];
                const end = prev2?.value?.end;
                if (Array.isArray(end)) {
                  Array.prototype.push.apply(end, it2.start);
                  end.push(this.sourceToken);
                  map3.items.pop();
                  return;
                }
              }
              it2.start.push(this.sourceToken);
            }
            return;
        }
        if (this.indent >= map3.indent) {
          const atMapIndent = !this.onKeyLine && this.indent === map3.indent;
          const atNextItem = atMapIndent && (it2.sep || it2.explicitKey) && this.type !== "seq-item-ind";
          let start = [];
          if (atNextItem && it2.sep && !it2.value) {
            const nl = [];
            for (let i2 = 0; i2 < it2.sep.length; ++i2) {
              const st2 = it2.sep[i2];
              switch (st2.type) {
                case "newline":
                  nl.push(i2);
                  break;
                case "space":
                  break;
                case "comment":
                  if (st2.indent > map3.indent)
                    nl.length = 0;
                  break;
                default:
                  nl.length = 0;
              }
            }
            if (nl.length >= 2)
              start = it2.sep.splice(nl[1]);
          }
          switch (this.type) {
            case "anchor":
            case "tag":
              if (atNextItem || it2.value) {
                start.push(this.sourceToken);
                map3.items.push({ start });
                this.onKeyLine = true;
              } else if (it2.sep) {
                it2.sep.push(this.sourceToken);
              } else {
                it2.start.push(this.sourceToken);
              }
              return;
            case "explicit-key-ind":
              if (!it2.sep && !it2.explicitKey) {
                it2.start.push(this.sourceToken);
                it2.explicitKey = true;
              } else if (atNextItem || it2.value) {
                start.push(this.sourceToken);
                map3.items.push({ start, explicitKey: true });
              } else {
                this.stack.push({
                  type: "block-map",
                  offset: this.offset,
                  indent: this.indent,
                  items: [{ start: [this.sourceToken], explicitKey: true }]
                });
              }
              this.onKeyLine = true;
              return;
            case "map-value-ind":
              if (it2.explicitKey) {
                if (!it2.sep) {
                  if (includesToken(it2.start, "newline")) {
                    Object.assign(it2, { key: null, sep: [this.sourceToken] });
                  } else {
                    const start2 = getFirstKeyStartProps(it2.start);
                    this.stack.push({
                      type: "block-map",
                      offset: this.offset,
                      indent: this.indent,
                      items: [{ start: start2, key: null, sep: [this.sourceToken] }]
                    });
                  }
                } else if (it2.value) {
                  map3.items.push({ start: [], key: null, sep: [this.sourceToken] });
                } else if (includesToken(it2.sep, "map-value-ind")) {
                  this.stack.push({
                    type: "block-map",
                    offset: this.offset,
                    indent: this.indent,
                    items: [{ start, key: null, sep: [this.sourceToken] }]
                  });
                } else if (isFlowToken(it2.key) && !includesToken(it2.sep, "newline")) {
                  const start2 = getFirstKeyStartProps(it2.start);
                  const key2 = it2.key;
                  const sep = it2.sep;
                  sep.push(this.sourceToken);
                  delete it2.key;
                  delete it2.sep;
                  this.stack.push({
                    type: "block-map",
                    offset: this.offset,
                    indent: this.indent,
                    items: [{ start: start2, key: key2, sep }]
                  });
                } else if (start.length > 0) {
                  it2.sep = it2.sep.concat(start, this.sourceToken);
                } else {
                  it2.sep.push(this.sourceToken);
                }
              } else {
                if (!it2.sep) {
                  Object.assign(it2, { key: null, sep: [this.sourceToken] });
                } else if (it2.value || atNextItem) {
                  map3.items.push({ start, key: null, sep: [this.sourceToken] });
                } else if (includesToken(it2.sep, "map-value-ind")) {
                  this.stack.push({
                    type: "block-map",
                    offset: this.offset,
                    indent: this.indent,
                    items: [{ start: [], key: null, sep: [this.sourceToken] }]
                  });
                } else {
                  it2.sep.push(this.sourceToken);
                }
              }
              this.onKeyLine = true;
              return;
            case "alias":
            case "scalar":
            case "single-quoted-scalar":
            case "double-quoted-scalar": {
              const fs = this.flowScalar(this.type);
              if (atNextItem || it2.value) {
                map3.items.push({ start, key: fs, sep: [] });
                this.onKeyLine = true;
              } else if (it2.sep) {
                this.stack.push(fs);
              } else {
                Object.assign(it2, { key: fs, sep: [] });
                this.onKeyLine = true;
              }
              return;
            }
            default: {
              const bv = this.startBlockValue(map3);
              if (bv) {
                if (bv.type === "block-seq") {
                  if (!it2.explicitKey && it2.sep && !includesToken(it2.sep, "newline")) {
                    yield* this.pop({
                      type: "error",
                      offset: this.offset,
                      message: "Unexpected block-seq-ind on same line with key",
                      source: this.source
                    });
                    return;
                  }
                } else if (atMapIndent) {
                  map3.items.push({ start });
                }
                this.stack.push(bv);
                return;
              }
            }
          }
        }
        yield* this.pop();
        yield* this.step();
      }
      *blockSequence(seq2) {
        const it2 = seq2.items[seq2.items.length - 1];
        switch (this.type) {
          case "newline":
            if (it2.value) {
              const end = "end" in it2.value ? it2.value.end : void 0;
              const last2 = Array.isArray(end) ? end[end.length - 1] : void 0;
              if (last2?.type === "comment")
                end?.push(this.sourceToken);
              else
                seq2.items.push({ start: [this.sourceToken] });
            } else
              it2.start.push(this.sourceToken);
            return;
          case "space":
          case "comment":
            if (it2.value)
              seq2.items.push({ start: [this.sourceToken] });
            else {
              if (this.atIndentedComment(it2.start, seq2.indent)) {
                const prev2 = seq2.items[seq2.items.length - 2];
                const end = prev2?.value?.end;
                if (Array.isArray(end)) {
                  Array.prototype.push.apply(end, it2.start);
                  end.push(this.sourceToken);
                  seq2.items.pop();
                  return;
                }
              }
              it2.start.push(this.sourceToken);
            }
            return;
          case "anchor":
          case "tag":
            if (it2.value || this.indent <= seq2.indent)
              break;
            it2.start.push(this.sourceToken);
            return;
          case "seq-item-ind":
            if (this.indent !== seq2.indent)
              break;
            if (it2.value || includesToken(it2.start, "seq-item-ind"))
              seq2.items.push({ start: [this.sourceToken] });
            else
              it2.start.push(this.sourceToken);
            return;
        }
        if (this.indent > seq2.indent) {
          const bv = this.startBlockValue(seq2);
          if (bv) {
            this.stack.push(bv);
            return;
          }
        }
        yield* this.pop();
        yield* this.step();
      }
      *flowCollection(fc) {
        const it2 = fc.items[fc.items.length - 1];
        if (this.type === "flow-error-end") {
          let top;
          do {
            yield* this.pop();
            top = this.peek(1);
          } while (top && top.type === "flow-collection");
        } else if (fc.end.length === 0) {
          switch (this.type) {
            case "comma":
            case "explicit-key-ind":
              if (!it2 || it2.sep)
                fc.items.push({ start: [this.sourceToken] });
              else
                it2.start.push(this.sourceToken);
              return;
            case "map-value-ind":
              if (!it2 || it2.value)
                fc.items.push({ start: [], key: null, sep: [this.sourceToken] });
              else if (it2.sep)
                it2.sep.push(this.sourceToken);
              else
                Object.assign(it2, { key: null, sep: [this.sourceToken] });
              return;
            case "space":
            case "comment":
            case "newline":
            case "anchor":
            case "tag":
              if (!it2 || it2.value)
                fc.items.push({ start: [this.sourceToken] });
              else if (it2.sep)
                it2.sep.push(this.sourceToken);
              else
                it2.start.push(this.sourceToken);
              return;
            case "alias":
            case "scalar":
            case "single-quoted-scalar":
            case "double-quoted-scalar": {
              const fs = this.flowScalar(this.type);
              if (!it2 || it2.value)
                fc.items.push({ start: [], key: fs, sep: [] });
              else if (it2.sep)
                this.stack.push(fs);
              else
                Object.assign(it2, { key: fs, sep: [] });
              return;
            }
            case "flow-map-end":
            case "flow-seq-end":
              fc.end.push(this.sourceToken);
              return;
          }
          const bv = this.startBlockValue(fc);
          if (bv)
            this.stack.push(bv);
          else {
            yield* this.pop();
            yield* this.step();
          }
        } else {
          const parent = this.peek(2);
          if (parent.type === "block-map" && (this.type === "map-value-ind" && parent.indent === fc.indent || this.type === "newline" && !parent.items[parent.items.length - 1].sep)) {
            yield* this.pop();
            yield* this.step();
          } else if (this.type === "map-value-ind" && parent.type !== "flow-collection") {
            const prev2 = getPrevProps(parent);
            const start = getFirstKeyStartProps(prev2);
            fixFlowSeqItems(fc);
            const sep = fc.end.splice(1, fc.end.length);
            sep.push(this.sourceToken);
            const map3 = {
              type: "block-map",
              offset: fc.offset,
              indent: fc.indent,
              items: [{ start, key: fc, sep }]
            };
            this.onKeyLine = true;
            this.stack[this.stack.length - 1] = map3;
          } else {
            yield* this.lineEnd(fc);
          }
        }
      }
      flowScalar(type) {
        if (this.onNewLine) {
          let nl = this.source.indexOf("\n") + 1;
          while (nl !== 0) {
            this.onNewLine(this.offset + nl);
            nl = this.source.indexOf("\n", nl) + 1;
          }
        }
        return {
          type,
          offset: this.offset,
          indent: this.indent,
          source: this.source
        };
      }
      startBlockValue(parent) {
        switch (this.type) {
          case "alias":
          case "scalar":
          case "single-quoted-scalar":
          case "double-quoted-scalar":
            return this.flowScalar(this.type);
          case "block-scalar-header":
            return {
              type: "block-scalar",
              offset: this.offset,
              indent: this.indent,
              props: [this.sourceToken],
              source: ""
            };
          case "flow-map-start":
          case "flow-seq-start":
            return {
              type: "flow-collection",
              offset: this.offset,
              indent: this.indent,
              start: this.sourceToken,
              items: [],
              end: []
            };
          case "seq-item-ind":
            return {
              type: "block-seq",
              offset: this.offset,
              indent: this.indent,
              items: [{ start: [this.sourceToken] }]
            };
          case "explicit-key-ind": {
            this.onKeyLine = true;
            const prev2 = getPrevProps(parent);
            const start = getFirstKeyStartProps(prev2);
            start.push(this.sourceToken);
            return {
              type: "block-map",
              offset: this.offset,
              indent: this.indent,
              items: [{ start, explicitKey: true }]
            };
          }
          case "map-value-ind": {
            this.onKeyLine = true;
            const prev2 = getPrevProps(parent);
            const start = getFirstKeyStartProps(prev2);
            return {
              type: "block-map",
              offset: this.offset,
              indent: this.indent,
              items: [{ start, key: null, sep: [this.sourceToken] }]
            };
          }
        }
        return null;
      }
      atIndentedComment(start, indent) {
        if (this.type !== "comment")
          return false;
        if (this.indent <= indent)
          return false;
        return start.every((st2) => st2.type === "newline" || st2.type === "space");
      }
      *documentEnd(docEnd) {
        if (this.type !== "doc-mode") {
          if (docEnd.end)
            docEnd.end.push(this.sourceToken);
          else
            docEnd.end = [this.sourceToken];
          if (this.type === "newline")
            yield* this.pop();
        }
      }
      *lineEnd(token2) {
        switch (this.type) {
          case "comma":
          case "doc-start":
          case "doc-end":
          case "flow-seq-end":
          case "flow-map-end":
          case "map-value-ind":
            yield* this.pop();
            yield* this.step();
            break;
          case "newline":
            this.onKeyLine = false;
          // fallthrough
          case "space":
          case "comment":
          default:
            if (token2.end)
              token2.end.push(this.sourceToken);
            else
              token2.end = [this.sourceToken];
            if (this.type === "newline")
              yield* this.pop();
        }
      }
    };
  }
});

// node_modules/yaml/browser/dist/public-api.js
function parseOptions(options) {
  const prettyErrors = options.prettyErrors !== false;
  const lineCounter = options.lineCounter || prettyErrors && new LineCounter() || null;
  return { lineCounter, prettyErrors };
}
function parseAllDocuments(source, options = {}) {
  const { lineCounter, prettyErrors } = parseOptions(options);
  const parser = new Parser(lineCounter?.addNewLine);
  const composer = new Composer(options);
  const docs = Array.from(composer.compose(parser.parse(source)));
  if (prettyErrors && lineCounter)
    for (const doc of docs) {
      doc.errors.forEach(prettifyError(source, lineCounter));
      doc.warnings.forEach(prettifyError(source, lineCounter));
    }
  if (docs.length > 0)
    return docs;
  return Object.assign([], { empty: true }, composer.streamInfo());
}
function parseDocument(source, options = {}) {
  const { lineCounter, prettyErrors } = parseOptions(options);
  const parser = new Parser(lineCounter?.addNewLine);
  const composer = new Composer(options);
  let doc = null;
  for (const _doc of composer.compose(parser.parse(source), true, source.length)) {
    if (!doc)
      doc = _doc;
    else if (doc.options.logLevel !== "silent") {
      doc.errors.push(new YAMLParseError(_doc.range.slice(0, 2), "MULTIPLE_DOCS", "Source contains multiple documents; please use YAML.parseAllDocuments()"));
      break;
    }
  }
  if (prettyErrors && lineCounter) {
    doc.errors.forEach(prettifyError(source, lineCounter));
    doc.warnings.forEach(prettifyError(source, lineCounter));
  }
  return doc;
}
function parse(src, reviver, options) {
  let _reviver = void 0;
  if (typeof reviver === "function") {
    _reviver = reviver;
  } else if (options === void 0 && reviver && typeof reviver === "object") {
    options = reviver;
  }
  const doc = parseDocument(src, options);
  if (!doc)
    return null;
  doc.warnings.forEach((warning) => warn(doc.options.logLevel, warning));
  if (doc.errors.length > 0) {
    if (doc.options.logLevel !== "silent")
      throw doc.errors[0];
    else
      doc.errors = [];
  }
  return doc.toJS(Object.assign({ reviver: _reviver }, options));
}
function stringify3(value, replacer, options) {
  let _replacer = null;
  if (typeof replacer === "function" || Array.isArray(replacer)) {
    _replacer = replacer;
  } else if (options === void 0 && replacer) {
    options = replacer;
  }
  if (typeof options === "string")
    options = options.length;
  if (typeof options === "number") {
    const indent = Math.round(options);
    options = indent < 1 ? void 0 : indent > 8 ? { indent: 8 } : { indent };
  }
  if (value === void 0) {
    const { keepUndefined } = options ?? replacer ?? {};
    if (!keepUndefined)
      return void 0;
  }
  if (isDocument(value) && !_replacer)
    return value.toString(options);
  return new Document(value, _replacer, options).toString(options);
}
var init_public_api = __esm({
  "node_modules/yaml/browser/dist/public-api.js"() {
    init_composer();
    init_Document();
    init_errors();
    init_log();
    init_identity();
    init_line_counter();
    init_parser();
  }
});

// node_modules/yaml/browser/dist/index.js
var dist_exports = {};
__export(dist_exports, {
  Alias: () => Alias,
  CST: () => cst_exports,
  Composer: () => Composer,
  Document: () => Document,
  Lexer: () => Lexer,
  LineCounter: () => LineCounter,
  Pair: () => Pair,
  Parser: () => Parser,
  Scalar: () => Scalar,
  Schema: () => Schema,
  YAMLError: () => YAMLError,
  YAMLMap: () => YAMLMap,
  YAMLParseError: () => YAMLParseError,
  YAMLSeq: () => YAMLSeq,
  YAMLWarning: () => YAMLWarning,
  isAlias: () => isAlias,
  isCollection: () => isCollection,
  isDocument: () => isDocument,
  isMap: () => isMap,
  isNode: () => isNode,
  isPair: () => isPair,
  isScalar: () => isScalar,
  isSeq: () => isSeq,
  parse: () => parse,
  parseAllDocuments: () => parseAllDocuments,
  parseDocument: () => parseDocument,
  stringify: () => stringify3,
  visit: () => visit,
  visitAsync: () => visitAsync
});
var init_dist = __esm({
  "node_modules/yaml/browser/dist/index.js"() {
    init_composer();
    init_Document();
    init_Schema();
    init_errors();
    init_Alias();
    init_identity();
    init_Pair();
    init_Scalar();
    init_YAMLMap();
    init_YAMLSeq();
    init_cst();
    init_lexer();
    init_line_counter();
    init_parser();
    init_public_api();
    init_visit();
  }
});

// node_modules/yaml/browser/index.js
var browser_exports = {};
__export(browser_exports, {
  Alias: () => Alias,
  CST: () => cst_exports,
  Composer: () => Composer,
  Document: () => Document,
  Lexer: () => Lexer,
  LineCounter: () => LineCounter,
  Pair: () => Pair,
  Parser: () => Parser,
  Scalar: () => Scalar,
  Schema: () => Schema,
  YAMLError: () => YAMLError,
  YAMLMap: () => YAMLMap,
  YAMLParseError: () => YAMLParseError,
  YAMLSeq: () => YAMLSeq,
  YAMLWarning: () => YAMLWarning,
  default: () => browser_default,
  isAlias: () => isAlias,
  isCollection: () => isCollection,
  isDocument: () => isDocument,
  isMap: () => isMap,
  isNode: () => isNode,
  isPair: () => isPair,
  isScalar: () => isScalar,
  isSeq: () => isSeq,
  parse: () => parse,
  parseAllDocuments: () => parseAllDocuments,
  parseDocument: () => parseDocument,
  stringify: () => stringify3,
  visit: () => visit,
  visitAsync: () => visitAsync
});
var browser_default;
var init_browser = __esm({
  "node_modules/yaml/browser/index.js"() {
    init_dist();
    init_dist();
    browser_default = dist_exports;
  }
});

// node_modules/@babel/runtime/helpers/esm/extends.js
function _extends() {
  return _extends = Object.assign ? Object.assign.bind() : function(n) {
    for (var e = 1; e < arguments.length; e++) {
      var t = arguments[e];
      for (var r2 in t) ({}).hasOwnProperty.call(t, r2) && (n[r2] = t[r2]);
    }
    return n;
  }, _extends.apply(null, arguments);
}
var init_extends = __esm({
  "node_modules/@babel/runtime/helpers/esm/extends.js"() {
  }
});

// node_modules/@babel/runtime/helpers/esm/assertThisInitialized.js
function _assertThisInitialized(e) {
  if (void 0 === e) throw new ReferenceError("this hasn't been initialised - super() hasn't been called");
  return e;
}
var init_assertThisInitialized = __esm({
  "node_modules/@babel/runtime/helpers/esm/assertThisInitialized.js"() {
  }
});

// node_modules/@babel/runtime/helpers/esm/setPrototypeOf.js
function _setPrototypeOf(t, e) {
  return _setPrototypeOf = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function(t2, e2) {
    return t2.__proto__ = e2, t2;
  }, _setPrototypeOf(t, e);
}
var init_setPrototypeOf = __esm({
  "node_modules/@babel/runtime/helpers/esm/setPrototypeOf.js"() {
  }
});

// node_modules/@babel/runtime/helpers/esm/inheritsLoose.js
function _inheritsLoose(t, o2) {
  t.prototype = Object.create(o2.prototype), t.prototype.constructor = t, _setPrototypeOf(t, o2);
}
var init_inheritsLoose = __esm({
  "node_modules/@babel/runtime/helpers/esm/inheritsLoose.js"() {
    init_setPrototypeOf();
  }
});

// node_modules/@babel/runtime/helpers/esm/getPrototypeOf.js
function _getPrototypeOf(t) {
  return _getPrototypeOf = Object.setPrototypeOf ? Object.getPrototypeOf.bind() : function(t2) {
    return t2.__proto__ || Object.getPrototypeOf(t2);
  }, _getPrototypeOf(t);
}
var init_getPrototypeOf = __esm({
  "node_modules/@babel/runtime/helpers/esm/getPrototypeOf.js"() {
  }
});

// node_modules/@babel/runtime/helpers/esm/isNativeFunction.js
function _isNativeFunction(t) {
  try {
    return -1 !== Function.toString.call(t).indexOf("[native code]");
  } catch (n) {
    return "function" == typeof t;
  }
}
var init_isNativeFunction = __esm({
  "node_modules/@babel/runtime/helpers/esm/isNativeFunction.js"() {
  }
});

// node_modules/@babel/runtime/helpers/esm/isNativeReflectConstruct.js
function _isNativeReflectConstruct() {
  try {
    var t = !Boolean.prototype.valueOf.call(Reflect.construct(Boolean, [], function() {
    }));
  } catch (t2) {
  }
  return (_isNativeReflectConstruct = function _isNativeReflectConstruct2() {
    return !!t;
  })();
}
var init_isNativeReflectConstruct = __esm({
  "node_modules/@babel/runtime/helpers/esm/isNativeReflectConstruct.js"() {
  }
});

// node_modules/@babel/runtime/helpers/esm/construct.js
function _construct(t, e, r2) {
  if (_isNativeReflectConstruct()) return Reflect.construct.apply(null, arguments);
  var o2 = [null];
  o2.push.apply(o2, e);
  var p2 = new (t.bind.apply(t, o2))();
  return r2 && _setPrototypeOf(p2, r2.prototype), p2;
}
var init_construct = __esm({
  "node_modules/@babel/runtime/helpers/esm/construct.js"() {
    init_isNativeReflectConstruct();
    init_setPrototypeOf();
  }
});

// node_modules/@babel/runtime/helpers/esm/wrapNativeSuper.js
function _wrapNativeSuper(t) {
  var r2 = "function" == typeof Map ? /* @__PURE__ */ new Map() : void 0;
  return _wrapNativeSuper = function _wrapNativeSuper2(t2) {
    if (null === t2 || !_isNativeFunction(t2)) return t2;
    if ("function" != typeof t2) throw new TypeError("Super expression must either be null or a function");
    if (void 0 !== r2) {
      if (r2.has(t2)) return r2.get(t2);
      r2.set(t2, Wrapper);
    }
    function Wrapper() {
      return _construct(t2, arguments, _getPrototypeOf(this).constructor);
    }
    return Wrapper.prototype = Object.create(t2.prototype, {
      constructor: {
        value: Wrapper,
        enumerable: false,
        writable: true,
        configurable: true
      }
    }), _setPrototypeOf(Wrapper, t2);
  }, _wrapNativeSuper(t);
}
var init_wrapNativeSuper = __esm({
  "node_modules/@babel/runtime/helpers/esm/wrapNativeSuper.js"() {
    init_getPrototypeOf();
    init_setPrototypeOf();
    init_isNativeFunction();
    init_construct();
  }
});

// node_modules/@babel/runtime/helpers/esm/taggedTemplateLiteralLoose.js
function _taggedTemplateLiteralLoose(e, t) {
  return t || (t = e.slice(0)), e.raw = t, e;
}
var init_taggedTemplateLiteralLoose = __esm({
  "node_modules/@babel/runtime/helpers/esm/taggedTemplateLiteralLoose.js"() {
  }
});

// node_modules/polished/dist/polished.esm.js
var polished_esm_exports = {};
__export(polished_esm_exports, {
  adjustHue: () => curriedAdjustHue$1,
  animation: () => animation,
  backgroundImages: () => backgroundImages,
  backgrounds: () => backgrounds,
  between: () => between,
  border: () => border,
  borderColor: () => borderColor,
  borderRadius: () => borderRadius,
  borderStyle: () => borderStyle,
  borderWidth: () => borderWidth,
  buttons: () => buttons,
  clearFix: () => clearFix,
  complement: () => complement,
  cover: () => cover,
  cssVar: () => cssVar,
  darken: () => curriedDarken$1,
  desaturate: () => curriedDesaturate$1,
  directionalProperty: () => directionalProperty,
  easeIn: () => easeIn,
  easeInOut: () => easeInOut,
  easeOut: () => easeOut,
  ellipsis: () => ellipsis,
  em: () => em$1,
  fluidRange: () => fluidRange,
  fontFace: () => fontFace,
  getContrast: () => getContrast,
  getLuminance: () => getLuminance,
  getValueAndUnit: () => getValueAndUnit,
  grayscale: () => grayscale,
  hiDPI: () => hiDPI,
  hideText: () => hideText,
  hideVisually: () => hideVisually,
  hsl: () => hsl,
  hslToColorString: () => hslToColorString,
  hsla: () => hsla,
  important: () => important,
  invert: () => invert,
  lighten: () => curriedLighten$1,
  linearGradient: () => linearGradient,
  margin: () => margin,
  math: () => math,
  meetsContrastGuidelines: () => meetsContrastGuidelines,
  mix: () => mix$1,
  modularScale: () => modularScale,
  normalize: () => normalize,
  opacify: () => curriedOpacify$1,
  padding: () => padding,
  parseToHsl: () => parseToHsl,
  parseToRgb: () => parseToRgb,
  position: () => position,
  radialGradient: () => radialGradient,
  readableColor: () => readableColor,
  rem: () => rem$1,
  remToPx: () => remToPx,
  retinaImage: () => retinaImage,
  rgb: () => rgb,
  rgbToColorString: () => rgbToColorString,
  rgba: () => rgba,
  saturate: () => curriedSaturate$1,
  setHue: () => curriedSetHue$1,
  setLightness: () => curriedSetLightness$1,
  setSaturation: () => curriedSetSaturation$1,
  shade: () => curriedShade$1,
  size: () => size,
  stripUnit: () => stripUnit,
  textInputs: () => textInputs,
  timingFunctions: () => timingFunctions,
  tint: () => curriedTint$1,
  toColorString: () => toColorString,
  transitions: () => transitions,
  transparentize: () => curriedTransparentize$1,
  triangle: () => triangle,
  wordWrap: () => wordWrap
});
function last() {
  var _ref;
  return _ref = arguments.length - 1, _ref < 0 || arguments.length <= _ref ? void 0 : arguments[_ref];
}
function negation(a2) {
  return -a2;
}
function addition(a2, b2) {
  return a2 + b2;
}
function subtraction(a2, b2) {
  return a2 - b2;
}
function multiplication(a2, b2) {
  return a2 * b2;
}
function division(a2, b2) {
  return a2 / b2;
}
function max() {
  return Math.max.apply(Math, arguments);
}
function min() {
  return Math.min.apply(Math, arguments);
}
function comma() {
  return Array.of.apply(Array, arguments);
}
function format() {
  for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
    args[_key] = arguments[_key];
  }
  var a2 = args[0];
  var b2 = [];
  var c2;
  for (c2 = 1; c2 < args.length; c2 += 1) {
    b2.push(args[c2]);
  }
  b2.forEach(function(d) {
    a2 = a2.replace(/%[a-z]/, d);
  });
  return a2;
}
function mergeSymbolMaps(additionalSymbols) {
  var symbolMap = {};
  symbolMap.symbols = additionalSymbols ? _extends({}, defaultSymbolMap.symbols, additionalSymbols.symbols) : _extends({}, defaultSymbolMap.symbols);
  return symbolMap;
}
function exec(operators, values2) {
  var _ref;
  var op = operators.pop();
  values2.push(op.f.apply(op, (_ref = []).concat.apply(_ref, values2.splice(-op.argCount))));
  return op.precedence;
}
function calculate(expression, additionalSymbols) {
  var symbolMap = mergeSymbolMaps(additionalSymbols);
  var match2;
  var operators = [symbolMap.symbols["("].prefix];
  var values2 = [];
  var pattern = new RegExp(
    // Pattern for numbers
    "\\d+(?:\\.\\d+)?|" + // ...and patterns for individual operators/function names
    Object.keys(symbolMap.symbols).map(function(key2) {
      return symbolMap.symbols[key2];
    }).sort(function(a2, b2) {
      return b2.symbol.length - a2.symbol.length;
    }).map(function(val) {
      return val.regSymbol;
    }).join("|") + "|(\\S)",
    "g"
  );
  pattern.lastIndex = 0;
  var afterValue = false;
  do {
    match2 = pattern.exec(expression);
    var _ref2 = match2 || [")", void 0], token2 = _ref2[0], bad = _ref2[1];
    var notNumber = symbolMap.symbols[token2];
    var notNewValue = notNumber && !notNumber.prefix && !notNumber.func;
    var notAfterValue = !notNumber || !notNumber.postfix && !notNumber.infix;
    if (bad || (afterValue ? notAfterValue : notNewValue)) {
      throw new PolishedError(37, match2 ? match2.index : expression.length, expression);
    }
    if (afterValue) {
      var curr = notNumber.postfix || notNumber.infix;
      do {
        var prev2 = operators[operators.length - 1];
        if ((curr.precedence - prev2.precedence || prev2.rightToLeft) > 0) break;
      } while (exec(operators, values2));
      afterValue = curr.notation === "postfix";
      if (curr.symbol !== ")") {
        operators.push(curr);
        if (afterValue) exec(operators, values2);
      }
    } else if (notNumber) {
      operators.push(notNumber.prefix || notNumber.func);
      if (notNumber.func) {
        match2 = pattern.exec(expression);
        if (!match2 || match2[0] !== "(") {
          throw new PolishedError(38, match2 ? match2.index : expression.length, expression);
        }
      }
    } else {
      values2.push(+token2);
      afterValue = true;
    }
  } while (match2 && operators.length);
  if (operators.length) {
    throw new PolishedError(39, match2 ? match2.index : expression.length, expression);
  } else if (match2) {
    throw new PolishedError(40, match2 ? match2.index : expression.length, expression);
  } else {
    return values2.pop();
  }
}
function reverseString(str) {
  return str.split("").reverse().join("");
}
function math(formula, additionalSymbols) {
  var reversedFormula = reverseString(formula);
  var formulaMatch = reversedFormula.match(unitRegExp);
  if (formulaMatch && !formulaMatch.every(function(unit) {
    return unit === formulaMatch[0];
  })) {
    throw new PolishedError(41);
  }
  var cleanFormula = reverseString(reversedFormula.replace(unitRegExp, ""));
  return "" + calculate(cleanFormula, additionalSymbols) + (formulaMatch ? reverseString(formulaMatch[0]) : "");
}
function cssVar(cssVariable, defaultValue) {
  if (!cssVariable || !cssVariable.match(cssVariableRegex)) {
    throw new PolishedError(73);
  }
  var variableValue;
  if (typeof document !== "undefined" && document.documentElement !== null) {
    variableValue = getComputedStyle(document.documentElement).getPropertyValue(cssVariable);
  }
  if (variableValue) {
    return variableValue.trim();
  } else if (defaultValue) {
    return defaultValue;
  }
  throw new PolishedError(74);
}
function capitalizeString(string2) {
  return string2.charAt(0).toUpperCase() + string2.slice(1);
}
function generateProperty(property, position3) {
  if (!property) return position3.toLowerCase();
  var splitProperty = property.split("-");
  if (splitProperty.length > 1) {
    splitProperty.splice(1, 0, position3);
    return splitProperty.reduce(function(acc, val) {
      return "" + acc + capitalizeString(val);
    });
  }
  var joinedProperty = property.replace(/([a-z])([A-Z])/g, "$1" + position3 + "$2");
  return property === joinedProperty ? "" + property + position3 : joinedProperty;
}
function generateStyles(property, valuesWithDefaults) {
  var styles = {};
  for (var i2 = 0; i2 < valuesWithDefaults.length; i2 += 1) {
    if (valuesWithDefaults[i2] || valuesWithDefaults[i2] === 0) {
      styles[generateProperty(property, positionMap$1[i2])] = valuesWithDefaults[i2];
    }
  }
  return styles;
}
function directionalProperty(property) {
  for (var _len = arguments.length, values2 = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
    values2[_key - 1] = arguments[_key];
  }
  var firstValue = values2[0], _values$ = values2[1], secondValue = _values$ === void 0 ? firstValue : _values$, _values$2 = values2[2], thirdValue = _values$2 === void 0 ? firstValue : _values$2, _values$3 = values2[3], fourthValue = _values$3 === void 0 ? secondValue : _values$3;
  var valuesWithDefaults = [firstValue, secondValue, thirdValue, fourthValue];
  return generateStyles(property, valuesWithDefaults);
}
function endsWith(string2, suffix) {
  return string2.substr(-suffix.length) === suffix;
}
function stripUnit(value) {
  if (typeof value !== "string") return value;
  var matchedValue = value.match(cssRegex$1);
  return matchedValue ? parseFloat(value) : value;
}
function getValueAndUnit(value) {
  if (typeof value !== "string") return [value, ""];
  var matchedValue = value.match(cssRegex);
  if (matchedValue) return [parseFloat(value), matchedValue[2]];
  return [value, void 0];
}
function important(styleBlock, rules) {
  if (typeof styleBlock !== "object" || styleBlock === null) {
    throw new PolishedError(75, typeof styleBlock);
  }
  var newStyleBlock = {};
  Object.keys(styleBlock).forEach(function(key2) {
    if (typeof styleBlock[key2] === "object" && styleBlock[key2] !== null) {
      newStyleBlock[key2] = important(styleBlock[key2], rules);
    } else if (!rules || rules && (rules === key2 || rules.indexOf(key2) >= 0)) {
      newStyleBlock[key2] = styleBlock[key2] + " !important";
    } else {
      newStyleBlock[key2] = styleBlock[key2];
    }
  });
  return newStyleBlock;
}
function getRatio(ratioName) {
  return ratioNames[ratioName];
}
function modularScale(steps, base, ratio) {
  if (base === void 0) {
    base = "1em";
  }
  if (ratio === void 0) {
    ratio = 1.333;
  }
  if (typeof steps !== "number") {
    throw new PolishedError(42);
  }
  if (typeof ratio === "string" && !ratioNames[ratio]) {
    throw new PolishedError(43);
  }
  var _ref = typeof base === "string" ? getValueAndUnit(base) : [base, ""], realBase = _ref[0], unit = _ref[1];
  var realRatio = typeof ratio === "string" ? getRatio(ratio) : ratio;
  if (typeof realBase === "string") {
    throw new PolishedError(44, base);
  }
  return "" + realBase * Math.pow(realRatio, steps) + (unit || "");
}
function convertBase(base) {
  var deconstructedValue = getValueAndUnit(base);
  if (deconstructedValue[1] === "px") {
    return parseFloat(base);
  }
  if (deconstructedValue[1] === "%") {
    return parseFloat(base) / 100 * defaultFontSize;
  }
  throw new PolishedError(78, deconstructedValue[1]);
}
function getBaseFromDoc() {
  if (typeof document !== "undefined" && document.documentElement !== null) {
    var rootFontSize = getComputedStyle(document.documentElement).fontSize;
    return rootFontSize ? convertBase(rootFontSize) : defaultFontSize;
  }
  return defaultFontSize;
}
function remToPx(value, base) {
  var deconstructedValue = getValueAndUnit(value);
  if (deconstructedValue[1] !== "rem" && deconstructedValue[1] !== "") {
    throw new PolishedError(77, deconstructedValue[1]);
  }
  var newBase = base ? convertBase(base) : getBaseFromDoc();
  return deconstructedValue[0] * newBase + "px";
}
function easeIn(functionName) {
  return functionsMap$3[functionName.toLowerCase().trim()];
}
function easeInOut(functionName) {
  return functionsMap$2[functionName.toLowerCase().trim()];
}
function easeOut(functionName) {
  return functionsMap$1[functionName.toLowerCase().trim()];
}
function between(fromSize, toSize, minScreen, maxScreen) {
  if (minScreen === void 0) {
    minScreen = "320px";
  }
  if (maxScreen === void 0) {
    maxScreen = "1200px";
  }
  var _getValueAndUnit = getValueAndUnit(fromSize), unitlessFromSize = _getValueAndUnit[0], fromSizeUnit = _getValueAndUnit[1];
  var _getValueAndUnit2 = getValueAndUnit(toSize), unitlessToSize = _getValueAndUnit2[0], toSizeUnit = _getValueAndUnit2[1];
  var _getValueAndUnit3 = getValueAndUnit(minScreen), unitlessMinScreen = _getValueAndUnit3[0], minScreenUnit = _getValueAndUnit3[1];
  var _getValueAndUnit4 = getValueAndUnit(maxScreen), unitlessMaxScreen = _getValueAndUnit4[0], maxScreenUnit = _getValueAndUnit4[1];
  if (typeof unitlessMinScreen !== "number" || typeof unitlessMaxScreen !== "number" || !minScreenUnit || !maxScreenUnit || minScreenUnit !== maxScreenUnit) {
    throw new PolishedError(47);
  }
  if (typeof unitlessFromSize !== "number" || typeof unitlessToSize !== "number" || fromSizeUnit !== toSizeUnit) {
    throw new PolishedError(48);
  }
  if (fromSizeUnit !== minScreenUnit || toSizeUnit !== maxScreenUnit) {
    throw new PolishedError(76);
  }
  var slope = (unitlessFromSize - unitlessToSize) / (unitlessMinScreen - unitlessMaxScreen);
  var base = unitlessToSize - slope * unitlessMaxScreen;
  return "calc(" + base.toFixed(2) + (fromSizeUnit || "") + " + " + (100 * slope).toFixed(2) + "vw)";
}
function clearFix(parent) {
  var _ref;
  if (parent === void 0) {
    parent = "&";
  }
  var pseudoSelector = parent + "::after";
  return _ref = {}, _ref[pseudoSelector] = {
    clear: "both",
    content: '""',
    display: "table"
  }, _ref;
}
function cover(offset) {
  if (offset === void 0) {
    offset = 0;
  }
  return {
    position: "absolute",
    top: offset,
    right: offset,
    bottom: offset,
    left: offset
  };
}
function ellipsis(width, lines) {
  if (lines === void 0) {
    lines = 1;
  }
  var styles = {
    display: "inline-block",
    maxWidth: width || "100%",
    overflow: "hidden",
    textOverflow: "ellipsis",
    whiteSpace: "nowrap",
    wordWrap: "normal"
  };
  return lines > 1 ? _extends({}, styles, {
    WebkitBoxOrient: "vertical",
    WebkitLineClamp: lines,
    display: "-webkit-box",
    whiteSpace: "normal"
  }) : styles;
}
function _createForOfIteratorHelperLoose(o2, allowArrayLike) {
  var it2 = typeof Symbol !== "undefined" && o2[Symbol.iterator] || o2["@@iterator"];
  if (it2) return (it2 = it2.call(o2)).next.bind(it2);
  if (Array.isArray(o2) || (it2 = _unsupportedIterableToArray(o2)) || allowArrayLike && o2 && typeof o2.length === "number") {
    if (it2) o2 = it2;
    var i2 = 0;
    return function() {
      if (i2 >= o2.length) return { done: true };
      return { done: false, value: o2[i2++] };
    };
  }
  throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
}
function _unsupportedIterableToArray(o2, minLen) {
  if (!o2) return;
  if (typeof o2 === "string") return _arrayLikeToArray(o2, minLen);
  var n = Object.prototype.toString.call(o2).slice(8, -1);
  if (n === "Object" && o2.constructor) n = o2.constructor.name;
  if (n === "Map" || n === "Set") return Array.from(o2);
  if (n === "Arguments" || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n)) return _arrayLikeToArray(o2, minLen);
}
function _arrayLikeToArray(arr, len) {
  if (len == null || len > arr.length) len = arr.length;
  for (var i2 = 0, arr2 = new Array(len); i2 < len; i2++) arr2[i2] = arr[i2];
  return arr2;
}
function fluidRange(cssProp, minScreen, maxScreen) {
  if (minScreen === void 0) {
    minScreen = "320px";
  }
  if (maxScreen === void 0) {
    maxScreen = "1200px";
  }
  if (!Array.isArray(cssProp) && typeof cssProp !== "object" || cssProp === null) {
    throw new PolishedError(49);
  }
  if (Array.isArray(cssProp)) {
    var mediaQueries = {};
    var fallbacks = {};
    for (var _iterator = _createForOfIteratorHelperLoose(cssProp), _step; !(_step = _iterator()).done; ) {
      var _extends22, _extends32;
      var obj = _step.value;
      if (!obj.prop || !obj.fromSize || !obj.toSize) {
        throw new PolishedError(50);
      }
      fallbacks[obj.prop] = obj.fromSize;
      mediaQueries["@media (min-width: " + minScreen + ")"] = _extends({}, mediaQueries["@media (min-width: " + minScreen + ")"], (_extends22 = {}, _extends22[obj.prop] = between(obj.fromSize, obj.toSize, minScreen, maxScreen), _extends22));
      mediaQueries["@media (min-width: " + maxScreen + ")"] = _extends({}, mediaQueries["@media (min-width: " + maxScreen + ")"], (_extends32 = {}, _extends32[obj.prop] = obj.toSize, _extends32));
    }
    return _extends({}, fallbacks, mediaQueries);
  } else {
    var _ref, _ref2, _ref3;
    if (!cssProp.prop || !cssProp.fromSize || !cssProp.toSize) {
      throw new PolishedError(51);
    }
    return _ref3 = {}, _ref3[cssProp.prop] = cssProp.fromSize, _ref3["@media (min-width: " + minScreen + ")"] = (_ref = {}, _ref[cssProp.prop] = between(cssProp.fromSize, cssProp.toSize, minScreen, maxScreen), _ref), _ref3["@media (min-width: " + maxScreen + ")"] = (_ref2 = {}, _ref2[cssProp.prop] = cssProp.toSize, _ref2), _ref3;
  }
}
function generateFormatHint(format2, formatHint) {
  if (!formatHint) return "";
  return ' format("' + formatHintMap[format2] + '")';
}
function isDataURI(fontFilePath) {
  return !!fontFilePath.replace(/\s+/g, " ").match(dataURIRegex);
}
function generateFileReferences(fontFilePath, fileFormats, formatHint) {
  if (isDataURI(fontFilePath)) {
    return 'url("' + fontFilePath + '")' + generateFormatHint(fileFormats[0], formatHint);
  }
  var fileFontReferences = fileFormats.map(function(format2) {
    return 'url("' + fontFilePath + "." + format2 + '")' + generateFormatHint(format2, formatHint);
  });
  return fileFontReferences.join(", ");
}
function generateLocalReferences(localFonts) {
  var localFontReferences = localFonts.map(function(font) {
    return 'local("' + font + '")';
  });
  return localFontReferences.join(", ");
}
function generateSources(fontFilePath, localFonts, fileFormats, formatHint) {
  var fontReferences = [];
  if (localFonts) fontReferences.push(generateLocalReferences(localFonts));
  if (fontFilePath) {
    fontReferences.push(generateFileReferences(fontFilePath, fileFormats, formatHint));
  }
  return fontReferences.join(", ");
}
function fontFace(_ref) {
  var fontFamily = _ref.fontFamily, fontFilePath = _ref.fontFilePath, fontStretch = _ref.fontStretch, fontStyle = _ref.fontStyle, fontVariant = _ref.fontVariant, fontWeight = _ref.fontWeight, _ref$fileFormats = _ref.fileFormats, fileFormats = _ref$fileFormats === void 0 ? ["eot", "woff2", "woff", "ttf", "svg"] : _ref$fileFormats, _ref$formatHint = _ref.formatHint, formatHint = _ref$formatHint === void 0 ? false : _ref$formatHint, _ref$localFonts = _ref.localFonts, localFonts = _ref$localFonts === void 0 ? [fontFamily] : _ref$localFonts, unicodeRange = _ref.unicodeRange, fontDisplay = _ref.fontDisplay, fontVariationSettings = _ref.fontVariationSettings, fontFeatureSettings = _ref.fontFeatureSettings;
  if (!fontFamily) throw new PolishedError(55);
  if (!fontFilePath && !localFonts) {
    throw new PolishedError(52);
  }
  if (localFonts && !Array.isArray(localFonts)) {
    throw new PolishedError(53);
  }
  if (!Array.isArray(fileFormats)) {
    throw new PolishedError(54);
  }
  var fontFaceDeclaration = {
    "@font-face": {
      fontFamily,
      src: generateSources(fontFilePath, localFonts, fileFormats, formatHint),
      unicodeRange,
      fontStretch,
      fontStyle,
      fontVariant,
      fontWeight,
      fontDisplay,
      fontVariationSettings,
      fontFeatureSettings
    }
  };
  return JSON.parse(JSON.stringify(fontFaceDeclaration));
}
function hideText() {
  return {
    textIndent: "101%",
    overflow: "hidden",
    whiteSpace: "nowrap"
  };
}
function hideVisually() {
  return {
    border: "0",
    clip: "rect(0 0 0 0)",
    height: "1px",
    margin: "-1px",
    overflow: "hidden",
    padding: "0",
    position: "absolute",
    whiteSpace: "nowrap",
    width: "1px"
  };
}
function hiDPI(ratio) {
  if (ratio === void 0) {
    ratio = 1.3;
  }
  return "\n    @media only screen and (-webkit-min-device-pixel-ratio: " + ratio + "),\n    only screen and (min--moz-device-pixel-ratio: " + ratio + "),\n    only screen and (-o-min-device-pixel-ratio: " + ratio + "/1),\n    only screen and (min-resolution: " + Math.round(ratio * 96) + "dpi),\n    only screen and (min-resolution: " + ratio + "dppx)\n  ";
}
function constructGradientValue(literals) {
  var template2 = "";
  for (var _len = arguments.length, substitutions = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
    substitutions[_key - 1] = arguments[_key];
  }
  for (var i2 = 0; i2 < literals.length; i2 += 1) {
    template2 += literals[i2];
    if (i2 === substitutions.length - 1 && substitutions[i2]) {
      var definedValues = substitutions.filter(function(substitute) {
        return !!substitute;
      });
      if (definedValues.length > 1) {
        template2 = template2.slice(0, -1);
        template2 += ", " + substitutions[i2];
      } else if (definedValues.length === 1) {
        template2 += "" + substitutions[i2];
      }
    } else if (substitutions[i2]) {
      template2 += substitutions[i2] + " ";
    }
  }
  return template2.trim();
}
function linearGradient(_ref) {
  var colorStops = _ref.colorStops, fallback = _ref.fallback, _ref$toDirection = _ref.toDirection, toDirection = _ref$toDirection === void 0 ? "" : _ref$toDirection;
  if (!colorStops || colorStops.length < 2) {
    throw new PolishedError(56);
  }
  return {
    backgroundColor: fallback || colorStops[0].replace(/,\s+/g, ",").split(" ")[0].replace(/,(?=\S)/g, ", "),
    backgroundImage: constructGradientValue(_templateObject$1 || (_templateObject$1 = _taggedTemplateLiteralLoose(["linear-gradient(", "", ")"])), toDirection, colorStops.join(", ").replace(/,(?=\S)/g, ", "))
  };
}
function normalize() {
  var _ref;
  return [(_ref = {
    html: {
      lineHeight: "1.15",
      textSizeAdjust: "100%"
    },
    body: {
      margin: "0"
    },
    main: {
      display: "block"
    },
    h1: {
      fontSize: "2em",
      margin: "0.67em 0"
    },
    hr: {
      boxSizing: "content-box",
      height: "0",
      overflow: "visible"
    },
    pre: {
      fontFamily: "monospace, monospace",
      fontSize: "1em"
    },
    a: {
      backgroundColor: "transparent"
    },
    "abbr[title]": {
      borderBottom: "none",
      textDecoration: "underline"
    }
  }, _ref["b,\n    strong"] = {
    fontWeight: "bolder"
  }, _ref["code,\n    kbd,\n    samp"] = {
    fontFamily: "monospace, monospace",
    fontSize: "1em"
  }, _ref.small = {
    fontSize: "80%"
  }, _ref["sub,\n    sup"] = {
    fontSize: "75%",
    lineHeight: "0",
    position: "relative",
    verticalAlign: "baseline"
  }, _ref.sub = {
    bottom: "-0.25em"
  }, _ref.sup = {
    top: "-0.5em"
  }, _ref.img = {
    borderStyle: "none"
  }, _ref["button,\n    input,\n    optgroup,\n    select,\n    textarea"] = {
    fontFamily: "inherit",
    fontSize: "100%",
    lineHeight: "1.15",
    margin: "0"
  }, _ref["button,\n    input"] = {
    overflow: "visible"
  }, _ref["button,\n    select"] = {
    textTransform: "none"
  }, _ref['button,\n    html [type="button"],\n    [type="reset"],\n    [type="submit"]'] = {
    WebkitAppearance: "button"
  }, _ref['button::-moz-focus-inner,\n    [type="button"]::-moz-focus-inner,\n    [type="reset"]::-moz-focus-inner,\n    [type="submit"]::-moz-focus-inner'] = {
    borderStyle: "none",
    padding: "0"
  }, _ref['button:-moz-focusring,\n    [type="button"]:-moz-focusring,\n    [type="reset"]:-moz-focusring,\n    [type="submit"]:-moz-focusring'] = {
    outline: "1px dotted ButtonText"
  }, _ref.fieldset = {
    padding: "0.35em 0.625em 0.75em"
  }, _ref.legend = {
    boxSizing: "border-box",
    color: "inherit",
    display: "table",
    maxWidth: "100%",
    padding: "0",
    whiteSpace: "normal"
  }, _ref.progress = {
    verticalAlign: "baseline"
  }, _ref.textarea = {
    overflow: "auto"
  }, _ref['[type="checkbox"],\n    [type="radio"]'] = {
    boxSizing: "border-box",
    padding: "0"
  }, _ref['[type="number"]::-webkit-inner-spin-button,\n    [type="number"]::-webkit-outer-spin-button'] = {
    height: "auto"
  }, _ref['[type="search"]'] = {
    WebkitAppearance: "textfield",
    outlineOffset: "-2px"
  }, _ref['[type="search"]::-webkit-search-decoration'] = {
    WebkitAppearance: "none"
  }, _ref["::-webkit-file-upload-button"] = {
    WebkitAppearance: "button",
    font: "inherit"
  }, _ref.details = {
    display: "block"
  }, _ref.summary = {
    display: "list-item"
  }, _ref.template = {
    display: "none"
  }, _ref["[hidden]"] = {
    display: "none"
  }, _ref), {
    "abbr[title]": {
      textDecoration: "underline dotted"
    }
  }];
}
function radialGradient(_ref) {
  var colorStops = _ref.colorStops, _ref$extent = _ref.extent, extent = _ref$extent === void 0 ? "" : _ref$extent, fallback = _ref.fallback, _ref$position = _ref.position, position3 = _ref$position === void 0 ? "" : _ref$position, _ref$shape = _ref.shape, shape = _ref$shape === void 0 ? "" : _ref$shape;
  if (!colorStops || colorStops.length < 2) {
    throw new PolishedError(57);
  }
  return {
    backgroundColor: fallback || colorStops[0].split(" ")[0],
    backgroundImage: constructGradientValue(_templateObject || (_templateObject = _taggedTemplateLiteralLoose(["radial-gradient(", "", "", "", ")"])), position3, shape, extent, colorStops.join(", "))
  };
}
function retinaImage(filename, backgroundSize, extension, retinaFilename, retinaSuffix) {
  var _ref;
  if (extension === void 0) {
    extension = "png";
  }
  if (retinaSuffix === void 0) {
    retinaSuffix = "_2x";
  }
  if (!filename) {
    throw new PolishedError(58);
  }
  var ext = extension.replace(/^\./, "");
  var rFilename = retinaFilename ? retinaFilename + "." + ext : "" + filename + retinaSuffix + "." + ext;
  return _ref = {
    backgroundImage: "url(" + filename + "." + ext + ")"
  }, _ref[hiDPI()] = _extends({
    backgroundImage: "url(" + rFilename + ")"
  }, backgroundSize ? {
    backgroundSize
  } : {}), _ref;
}
function getTimingFunction(functionName) {
  return functionsMap[functionName];
}
function timingFunctions(timingFunction) {
  return getTimingFunction(timingFunction);
}
function triangle(_ref) {
  var pointingDirection = _ref.pointingDirection, height = _ref.height, width = _ref.width, foregroundColor = _ref.foregroundColor, _ref$backgroundColor = _ref.backgroundColor, backgroundColor = _ref$backgroundColor === void 0 ? "transparent" : _ref$backgroundColor;
  var widthAndUnit = getValueAndUnit(width);
  var heightAndUnit = getValueAndUnit(height);
  if (isNaN(heightAndUnit[0]) || isNaN(widthAndUnit[0])) {
    throw new PolishedError(60);
  }
  return _extends({
    width: "0",
    height: "0",
    borderColor: backgroundColor
  }, getBorderColor(pointingDirection, foregroundColor), {
    borderStyle: "solid",
    borderWidth: getBorderWidth(pointingDirection, heightAndUnit, widthAndUnit)
  });
}
function wordWrap(wrap) {
  if (wrap === void 0) {
    wrap = "break-word";
  }
  var wordBreak = wrap === "break-word" ? "break-all" : wrap;
  return {
    overflowWrap: wrap,
    wordWrap: wrap,
    wordBreak
  };
}
function colorToInt(color) {
  return Math.round(color * 255);
}
function convertToInt(red, green, blue) {
  return colorToInt(red) + "," + colorToInt(green) + "," + colorToInt(blue);
}
function hslToRgb(hue, saturation, lightness, convert) {
  if (convert === void 0) {
    convert = convertToInt;
  }
  if (saturation === 0) {
    return convert(lightness, lightness, lightness);
  }
  var huePrime = (hue % 360 + 360) % 360 / 60;
  var chroma = (1 - Math.abs(2 * lightness - 1)) * saturation;
  var secondComponent = chroma * (1 - Math.abs(huePrime % 2 - 1));
  var red = 0;
  var green = 0;
  var blue = 0;
  if (huePrime >= 0 && huePrime < 1) {
    red = chroma;
    green = secondComponent;
  } else if (huePrime >= 1 && huePrime < 2) {
    red = secondComponent;
    green = chroma;
  } else if (huePrime >= 2 && huePrime < 3) {
    green = chroma;
    blue = secondComponent;
  } else if (huePrime >= 3 && huePrime < 4) {
    green = secondComponent;
    blue = chroma;
  } else if (huePrime >= 4 && huePrime < 5) {
    red = secondComponent;
    blue = chroma;
  } else if (huePrime >= 5 && huePrime < 6) {
    red = chroma;
    blue = secondComponent;
  }
  var lightnessModification = lightness - chroma / 2;
  var finalRed = red + lightnessModification;
  var finalGreen = green + lightnessModification;
  var finalBlue = blue + lightnessModification;
  return convert(finalRed, finalGreen, finalBlue);
}
function nameToHex(color) {
  if (typeof color !== "string") return color;
  var normalizedColorName = color.toLowerCase();
  return namedColorMap[normalizedColorName] ? "#" + namedColorMap[normalizedColorName] : color;
}
function parseToRgb(color) {
  if (typeof color !== "string") {
    throw new PolishedError(3);
  }
  var normalizedColor = nameToHex(color);
  if (normalizedColor.match(hexRegex)) {
    return {
      red: parseInt("" + normalizedColor[1] + normalizedColor[2], 16),
      green: parseInt("" + normalizedColor[3] + normalizedColor[4], 16),
      blue: parseInt("" + normalizedColor[5] + normalizedColor[6], 16)
    };
  }
  if (normalizedColor.match(hexRgbaRegex)) {
    var alpha = parseFloat((parseInt("" + normalizedColor[7] + normalizedColor[8], 16) / 255).toFixed(2));
    return {
      red: parseInt("" + normalizedColor[1] + normalizedColor[2], 16),
      green: parseInt("" + normalizedColor[3] + normalizedColor[4], 16),
      blue: parseInt("" + normalizedColor[5] + normalizedColor[6], 16),
      alpha
    };
  }
  if (normalizedColor.match(reducedHexRegex)) {
    return {
      red: parseInt("" + normalizedColor[1] + normalizedColor[1], 16),
      green: parseInt("" + normalizedColor[2] + normalizedColor[2], 16),
      blue: parseInt("" + normalizedColor[3] + normalizedColor[3], 16)
    };
  }
  if (normalizedColor.match(reducedRgbaHexRegex)) {
    var _alpha = parseFloat((parseInt("" + normalizedColor[4] + normalizedColor[4], 16) / 255).toFixed(2));
    return {
      red: parseInt("" + normalizedColor[1] + normalizedColor[1], 16),
      green: parseInt("" + normalizedColor[2] + normalizedColor[2], 16),
      blue: parseInt("" + normalizedColor[3] + normalizedColor[3], 16),
      alpha: _alpha
    };
  }
  var rgbMatched = rgbRegex.exec(normalizedColor);
  if (rgbMatched) {
    return {
      red: parseInt("" + rgbMatched[1], 10),
      green: parseInt("" + rgbMatched[2], 10),
      blue: parseInt("" + rgbMatched[3], 10)
    };
  }
  var rgbaMatched = rgbaRegex.exec(normalizedColor.substring(0, 50));
  if (rgbaMatched) {
    return {
      red: parseInt("" + rgbaMatched[1], 10),
      green: parseInt("" + rgbaMatched[2], 10),
      blue: parseInt("" + rgbaMatched[3], 10),
      alpha: parseFloat("" + rgbaMatched[4]) > 1 ? parseFloat("" + rgbaMatched[4]) / 100 : parseFloat("" + rgbaMatched[4])
    };
  }
  var hslMatched = hslRegex.exec(normalizedColor);
  if (hslMatched) {
    var hue = parseInt("" + hslMatched[1], 10);
    var saturation = parseInt("" + hslMatched[2], 10) / 100;
    var lightness = parseInt("" + hslMatched[3], 10) / 100;
    var rgbColorString = "rgb(" + hslToRgb(hue, saturation, lightness) + ")";
    var hslRgbMatched = rgbRegex.exec(rgbColorString);
    if (!hslRgbMatched) {
      throw new PolishedError(4, normalizedColor, rgbColorString);
    }
    return {
      red: parseInt("" + hslRgbMatched[1], 10),
      green: parseInt("" + hslRgbMatched[2], 10),
      blue: parseInt("" + hslRgbMatched[3], 10)
    };
  }
  var hslaMatched = hslaRegex.exec(normalizedColor.substring(0, 50));
  if (hslaMatched) {
    var _hue = parseInt("" + hslaMatched[1], 10);
    var _saturation = parseInt("" + hslaMatched[2], 10) / 100;
    var _lightness = parseInt("" + hslaMatched[3], 10) / 100;
    var _rgbColorString = "rgb(" + hslToRgb(_hue, _saturation, _lightness) + ")";
    var _hslRgbMatched = rgbRegex.exec(_rgbColorString);
    if (!_hslRgbMatched) {
      throw new PolishedError(4, normalizedColor, _rgbColorString);
    }
    return {
      red: parseInt("" + _hslRgbMatched[1], 10),
      green: parseInt("" + _hslRgbMatched[2], 10),
      blue: parseInt("" + _hslRgbMatched[3], 10),
      alpha: parseFloat("" + hslaMatched[4]) > 1 ? parseFloat("" + hslaMatched[4]) / 100 : parseFloat("" + hslaMatched[4])
    };
  }
  throw new PolishedError(5);
}
function rgbToHsl(color) {
  var red = color.red / 255;
  var green = color.green / 255;
  var blue = color.blue / 255;
  var max2 = Math.max(red, green, blue);
  var min2 = Math.min(red, green, blue);
  var lightness = (max2 + min2) / 2;
  if (max2 === min2) {
    if (color.alpha !== void 0) {
      return {
        hue: 0,
        saturation: 0,
        lightness,
        alpha: color.alpha
      };
    } else {
      return {
        hue: 0,
        saturation: 0,
        lightness
      };
    }
  }
  var hue;
  var delta = max2 - min2;
  var saturation = lightness > 0.5 ? delta / (2 - max2 - min2) : delta / (max2 + min2);
  switch (max2) {
    case red:
      hue = (green - blue) / delta + (green < blue ? 6 : 0);
      break;
    case green:
      hue = (blue - red) / delta + 2;
      break;
    default:
      hue = (red - green) / delta + 4;
      break;
  }
  hue *= 60;
  if (color.alpha !== void 0) {
    return {
      hue,
      saturation,
      lightness,
      alpha: color.alpha
    };
  }
  return {
    hue,
    saturation,
    lightness
  };
}
function parseToHsl(color) {
  return rgbToHsl(parseToRgb(color));
}
function numberToHex(value) {
  var hex = value.toString(16);
  return hex.length === 1 ? "0" + hex : hex;
}
function colorToHex(color) {
  return numberToHex(Math.round(color * 255));
}
function convertToHex(red, green, blue) {
  return reduceHexValue$1("#" + colorToHex(red) + colorToHex(green) + colorToHex(blue));
}
function hslToHex(hue, saturation, lightness) {
  return hslToRgb(hue, saturation, lightness, convertToHex);
}
function hsl(value, saturation, lightness) {
  if (typeof value === "number" && typeof saturation === "number" && typeof lightness === "number") {
    return hslToHex(value, saturation, lightness);
  } else if (typeof value === "object" && saturation === void 0 && lightness === void 0) {
    return hslToHex(value.hue, value.saturation, value.lightness);
  }
  throw new PolishedError(1);
}
function hsla(value, saturation, lightness, alpha) {
  if (typeof value === "number" && typeof saturation === "number" && typeof lightness === "number" && typeof alpha === "number") {
    return alpha >= 1 ? hslToHex(value, saturation, lightness) : "rgba(" + hslToRgb(value, saturation, lightness) + "," + alpha + ")";
  } else if (typeof value === "object" && saturation === void 0 && lightness === void 0 && alpha === void 0) {
    return value.alpha >= 1 ? hslToHex(value.hue, value.saturation, value.lightness) : "rgba(" + hslToRgb(value.hue, value.saturation, value.lightness) + "," + value.alpha + ")";
  }
  throw new PolishedError(2);
}
function rgb(value, green, blue) {
  if (typeof value === "number" && typeof green === "number" && typeof blue === "number") {
    return reduceHexValue$1("#" + numberToHex(value) + numberToHex(green) + numberToHex(blue));
  } else if (typeof value === "object" && green === void 0 && blue === void 0) {
    return reduceHexValue$1("#" + numberToHex(value.red) + numberToHex(value.green) + numberToHex(value.blue));
  }
  throw new PolishedError(6);
}
function rgba(firstValue, secondValue, thirdValue, fourthValue) {
  if (typeof firstValue === "string" && typeof secondValue === "number") {
    var rgbValue = parseToRgb(firstValue);
    return "rgba(" + rgbValue.red + "," + rgbValue.green + "," + rgbValue.blue + "," + secondValue + ")";
  } else if (typeof firstValue === "number" && typeof secondValue === "number" && typeof thirdValue === "number" && typeof fourthValue === "number") {
    return fourthValue >= 1 ? rgb(firstValue, secondValue, thirdValue) : "rgba(" + firstValue + "," + secondValue + "," + thirdValue + "," + fourthValue + ")";
  } else if (typeof firstValue === "object" && secondValue === void 0 && thirdValue === void 0 && fourthValue === void 0) {
    return firstValue.alpha >= 1 ? rgb(firstValue.red, firstValue.green, firstValue.blue) : "rgba(" + firstValue.red + "," + firstValue.green + "," + firstValue.blue + "," + firstValue.alpha + ")";
  }
  throw new PolishedError(7);
}
function toColorString(color) {
  if (typeof color !== "object") throw new PolishedError(8);
  if (isRgba(color)) return rgba(color);
  if (isRgb(color)) return rgb(color);
  if (isHsla(color)) return hsla(color);
  if (isHsl(color)) return hsl(color);
  throw new PolishedError(8);
}
function curried(f2, length2, acc) {
  return function fn() {
    var combined = acc.concat(Array.prototype.slice.call(arguments));
    return combined.length >= length2 ? f2.apply(this, combined) : curried(f2, length2, combined);
  };
}
function curry(f2) {
  return curried(f2, f2.length, []);
}
function adjustHue(degree, color) {
  if (color === "transparent") return color;
  var hslColor = parseToHsl(color);
  return toColorString(_extends({}, hslColor, {
    hue: hslColor.hue + parseFloat(degree)
  }));
}
function complement(color) {
  if (color === "transparent") return color;
  var hslColor = parseToHsl(color);
  return toColorString(_extends({}, hslColor, {
    hue: (hslColor.hue + 180) % 360
  }));
}
function guard(lowerBoundary, upperBoundary, value) {
  return Math.max(lowerBoundary, Math.min(upperBoundary, value));
}
function darken(amount, color) {
  if (color === "transparent") return color;
  var hslColor = parseToHsl(color);
  return toColorString(_extends({}, hslColor, {
    lightness: guard(0, 1, hslColor.lightness - parseFloat(amount))
  }));
}
function desaturate(amount, color) {
  if (color === "transparent") return color;
  var hslColor = parseToHsl(color);
  return toColorString(_extends({}, hslColor, {
    saturation: guard(0, 1, hslColor.saturation - parseFloat(amount))
  }));
}
function getLuminance(color) {
  if (color === "transparent") return 0;
  var rgbColor = parseToRgb(color);
  var _Object$keys$map = Object.keys(rgbColor).map(function(key2) {
    var channel = rgbColor[key2] / 255;
    return channel <= 0.03928 ? channel / 12.92 : Math.pow((channel + 0.055) / 1.055, 2.4);
  }), r2 = _Object$keys$map[0], g2 = _Object$keys$map[1], b2 = _Object$keys$map[2];
  return parseFloat((0.2126 * r2 + 0.7152 * g2 + 0.0722 * b2).toFixed(3));
}
function getContrast(color1, color2) {
  var luminance1 = getLuminance(color1);
  var luminance2 = getLuminance(color2);
  return parseFloat((luminance1 > luminance2 ? (luminance1 + 0.05) / (luminance2 + 0.05) : (luminance2 + 0.05) / (luminance1 + 0.05)).toFixed(2));
}
function grayscale(color) {
  if (color === "transparent") return color;
  return toColorString(_extends({}, parseToHsl(color), {
    saturation: 0
  }));
}
function hslToColorString(color) {
  if (typeof color === "object" && typeof color.hue === "number" && typeof color.saturation === "number" && typeof color.lightness === "number") {
    if (color.alpha && typeof color.alpha === "number") {
      return hsla({
        hue: color.hue,
        saturation: color.saturation,
        lightness: color.lightness,
        alpha: color.alpha
      });
    }
    return hsl({
      hue: color.hue,
      saturation: color.saturation,
      lightness: color.lightness
    });
  }
  throw new PolishedError(45);
}
function invert(color) {
  if (color === "transparent") return color;
  var value = parseToRgb(color);
  return toColorString(_extends({}, value, {
    red: 255 - value.red,
    green: 255 - value.green,
    blue: 255 - value.blue
  }));
}
function lighten(amount, color) {
  if (color === "transparent") return color;
  var hslColor = parseToHsl(color);
  return toColorString(_extends({}, hslColor, {
    lightness: guard(0, 1, hslColor.lightness + parseFloat(amount))
  }));
}
function meetsContrastGuidelines(color1, color2) {
  var contrastRatio = getContrast(color1, color2);
  return {
    AA: contrastRatio >= 4.5,
    AALarge: contrastRatio >= 3,
    AAA: contrastRatio >= 7,
    AAALarge: contrastRatio >= 4.5
  };
}
function mix(weight, color, otherColor) {
  if (color === "transparent") return otherColor;
  if (otherColor === "transparent") return color;
  if (weight === 0) return otherColor;
  var parsedColor1 = parseToRgb(color);
  var color1 = _extends({}, parsedColor1, {
    alpha: typeof parsedColor1.alpha === "number" ? parsedColor1.alpha : 1
  });
  var parsedColor2 = parseToRgb(otherColor);
  var color2 = _extends({}, parsedColor2, {
    alpha: typeof parsedColor2.alpha === "number" ? parsedColor2.alpha : 1
  });
  var alphaDelta = color1.alpha - color2.alpha;
  var x2 = parseFloat(weight) * 2 - 1;
  var y2 = x2 * alphaDelta === -1 ? x2 : x2 + alphaDelta;
  var z2 = 1 + x2 * alphaDelta;
  var weight1 = (y2 / z2 + 1) / 2;
  var weight2 = 1 - weight1;
  var mixedColor = {
    red: Math.floor(color1.red * weight1 + color2.red * weight2),
    green: Math.floor(color1.green * weight1 + color2.green * weight2),
    blue: Math.floor(color1.blue * weight1 + color2.blue * weight2),
    alpha: color1.alpha * parseFloat(weight) + color2.alpha * (1 - parseFloat(weight))
  };
  return rgba(mixedColor);
}
function opacify(amount, color) {
  if (color === "transparent") return color;
  var parsedColor = parseToRgb(color);
  var alpha = typeof parsedColor.alpha === "number" ? parsedColor.alpha : 1;
  var colorWithAlpha = _extends({}, parsedColor, {
    alpha: guard(0, 1, (alpha * 100 + parseFloat(amount) * 100) / 100)
  });
  return rgba(colorWithAlpha);
}
function readableColor(color, returnIfLightColor, returnIfDarkColor, strict) {
  if (returnIfLightColor === void 0) {
    returnIfLightColor = defaultReturnIfLightColor;
  }
  if (returnIfDarkColor === void 0) {
    returnIfDarkColor = defaultReturnIfDarkColor;
  }
  if (strict === void 0) {
    strict = true;
  }
  var isColorLight = getLuminance(color) > 0.179;
  var preferredReturnColor = isColorLight ? returnIfLightColor : returnIfDarkColor;
  if (!strict || getContrast(color, preferredReturnColor) >= 4.5) {
    return preferredReturnColor;
  }
  return isColorLight ? defaultReturnIfLightColor : defaultReturnIfDarkColor;
}
function rgbToColorString(color) {
  if (typeof color === "object" && typeof color.red === "number" && typeof color.green === "number" && typeof color.blue === "number") {
    if (typeof color.alpha === "number") {
      return rgba({
        red: color.red,
        green: color.green,
        blue: color.blue,
        alpha: color.alpha
      });
    }
    return rgb({
      red: color.red,
      green: color.green,
      blue: color.blue
    });
  }
  throw new PolishedError(46);
}
function saturate(amount, color) {
  if (color === "transparent") return color;
  var hslColor = parseToHsl(color);
  return toColorString(_extends({}, hslColor, {
    saturation: guard(0, 1, hslColor.saturation + parseFloat(amount))
  }));
}
function setHue(hue, color) {
  if (color === "transparent") return color;
  return toColorString(_extends({}, parseToHsl(color), {
    hue: parseFloat(hue)
  }));
}
function setLightness(lightness, color) {
  if (color === "transparent") return color;
  return toColorString(_extends({}, parseToHsl(color), {
    lightness: parseFloat(lightness)
  }));
}
function setSaturation(saturation, color) {
  if (color === "transparent") return color;
  return toColorString(_extends({}, parseToHsl(color), {
    saturation: parseFloat(saturation)
  }));
}
function shade(percentage, color) {
  if (color === "transparent") return color;
  return mix$1(parseFloat(percentage), "rgb(0, 0, 0)", color);
}
function tint(percentage, color) {
  if (color === "transparent") return color;
  return mix$1(parseFloat(percentage), "rgb(255, 255, 255)", color);
}
function transparentize(amount, color) {
  if (color === "transparent") return color;
  var parsedColor = parseToRgb(color);
  var alpha = typeof parsedColor.alpha === "number" ? parsedColor.alpha : 1;
  var colorWithAlpha = _extends({}, parsedColor, {
    alpha: guard(0, 1, +(alpha * 100 - parseFloat(amount) * 100).toFixed(2) / 100)
  });
  return rgba(colorWithAlpha);
}
function animation() {
  for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
    args[_key] = arguments[_key];
  }
  var multiMode = Array.isArray(args[0]);
  if (!multiMode && args.length > 8) {
    throw new PolishedError(64);
  }
  var code = args.map(function(arg) {
    if (multiMode && !Array.isArray(arg) || !multiMode && Array.isArray(arg)) {
      throw new PolishedError(65);
    }
    if (Array.isArray(arg) && arg.length > 8) {
      throw new PolishedError(66);
    }
    return Array.isArray(arg) ? arg.join(" ") : arg;
  }).join(", ");
  return {
    animation: code
  };
}
function backgroundImages() {
  for (var _len = arguments.length, properties = new Array(_len), _key = 0; _key < _len; _key++) {
    properties[_key] = arguments[_key];
  }
  return {
    backgroundImage: properties.join(", ")
  };
}
function backgrounds() {
  for (var _len = arguments.length, properties = new Array(_len), _key = 0; _key < _len; _key++) {
    properties[_key] = arguments[_key];
  }
  return {
    background: properties.join(", ")
  };
}
function border(sideKeyword) {
  for (var _len = arguments.length, values2 = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
    values2[_key - 1] = arguments[_key];
  }
  if (typeof sideKeyword === "string" && sideMap.indexOf(sideKeyword) >= 0) {
    var _ref;
    return _ref = {}, _ref["border" + capitalizeString(sideKeyword) + "Width"] = values2[0], _ref["border" + capitalizeString(sideKeyword) + "Style"] = values2[1], _ref["border" + capitalizeString(sideKeyword) + "Color"] = values2[2], _ref;
  } else {
    values2.unshift(sideKeyword);
    return {
      borderWidth: values2[0],
      borderStyle: values2[1],
      borderColor: values2[2]
    };
  }
}
function borderColor() {
  for (var _len = arguments.length, values2 = new Array(_len), _key = 0; _key < _len; _key++) {
    values2[_key] = arguments[_key];
  }
  return directionalProperty.apply(void 0, ["borderColor"].concat(values2));
}
function borderRadius(side, radius) {
  var uppercaseSide = capitalizeString(side);
  if (!radius && radius !== 0) {
    throw new PolishedError(62);
  }
  if (uppercaseSide === "Top" || uppercaseSide === "Bottom") {
    var _ref;
    return _ref = {}, _ref["border" + uppercaseSide + "RightRadius"] = radius, _ref["border" + uppercaseSide + "LeftRadius"] = radius, _ref;
  }
  if (uppercaseSide === "Left" || uppercaseSide === "Right") {
    var _ref2;
    return _ref2 = {}, _ref2["borderTop" + uppercaseSide + "Radius"] = radius, _ref2["borderBottom" + uppercaseSide + "Radius"] = radius, _ref2;
  }
  throw new PolishedError(63);
}
function borderStyle() {
  for (var _len = arguments.length, values2 = new Array(_len), _key = 0; _key < _len; _key++) {
    values2[_key] = arguments[_key];
  }
  return directionalProperty.apply(void 0, ["borderStyle"].concat(values2));
}
function borderWidth() {
  for (var _len = arguments.length, values2 = new Array(_len), _key = 0; _key < _len; _key++) {
    values2[_key] = arguments[_key];
  }
  return directionalProperty.apply(void 0, ["borderWidth"].concat(values2));
}
function generateSelectors(template2, state) {
  var stateSuffix = state ? ":" + state : "";
  return template2(stateSuffix);
}
function statefulSelectors(states, template2, stateMap2) {
  if (!template2) throw new PolishedError(67);
  if (states.length === 0) return generateSelectors(template2, null);
  var selectors = [];
  for (var i2 = 0; i2 < states.length; i2 += 1) {
    if (stateMap2 && stateMap2.indexOf(states[i2]) < 0) {
      throw new PolishedError(68);
    }
    selectors.push(generateSelectors(template2, states[i2]));
  }
  selectors = selectors.join(",");
  return selectors;
}
function template$1(state) {
  return "button" + state + ',\n  input[type="button"]' + state + ',\n  input[type="reset"]' + state + ',\n  input[type="submit"]' + state;
}
function buttons() {
  for (var _len = arguments.length, states = new Array(_len), _key = 0; _key < _len; _key++) {
    states[_key] = arguments[_key];
  }
  return statefulSelectors(states, template$1, stateMap$1);
}
function margin() {
  for (var _len = arguments.length, values2 = new Array(_len), _key = 0; _key < _len; _key++) {
    values2[_key] = arguments[_key];
  }
  return directionalProperty.apply(void 0, ["margin"].concat(values2));
}
function padding() {
  for (var _len = arguments.length, values2 = new Array(_len), _key = 0; _key < _len; _key++) {
    values2[_key] = arguments[_key];
  }
  return directionalProperty.apply(void 0, ["padding"].concat(values2));
}
function position(firstValue) {
  for (var _len = arguments.length, values2 = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
    values2[_key - 1] = arguments[_key];
  }
  if (positionMap.indexOf(firstValue) >= 0 && firstValue) {
    return _extends({}, directionalProperty.apply(void 0, [""].concat(values2)), {
      position: firstValue
    });
  } else {
    return directionalProperty.apply(void 0, ["", firstValue].concat(values2));
  }
}
function size(height, width) {
  if (width === void 0) {
    width = height;
  }
  return {
    height,
    width
  };
}
function template(state) {
  return 'input[type="color"]' + state + ',\n    input[type="date"]' + state + ',\n    input[type="datetime"]' + state + ',\n    input[type="datetime-local"]' + state + ',\n    input[type="email"]' + state + ',\n    input[type="month"]' + state + ',\n    input[type="number"]' + state + ',\n    input[type="password"]' + state + ',\n    input[type="search"]' + state + ',\n    input[type="tel"]' + state + ',\n    input[type="text"]' + state + ',\n    input[type="time"]' + state + ',\n    input[type="url"]' + state + ',\n    input[type="week"]' + state + ",\n    input:not([type])" + state + ",\n    textarea" + state;
}
function textInputs() {
  for (var _len = arguments.length, states = new Array(_len), _key = 0; _key < _len; _key++) {
    states[_key] = arguments[_key];
  }
  return statefulSelectors(states, template, stateMap);
}
function transitions() {
  for (var _len = arguments.length, properties = new Array(_len), _key = 0; _key < _len; _key++) {
    properties[_key] = arguments[_key];
  }
  if (Array.isArray(properties[0]) && properties.length === 2) {
    var value = properties[1];
    if (typeof value !== "string") {
      throw new PolishedError(61);
    }
    var transitionsString = properties[0].map(function(property) {
      return property + " " + value;
    }).join(", ");
    return {
      transition: transitionsString
    };
  } else {
    return {
      transition: properties.join(", ")
    };
  }
}
var defaultSymbols, defaultSymbolMap, ERRORS, PolishedError, unitRegExp, cssVariableRegex, positionMap$1, cssRegex$1, pxtoFactory, pixelsto, em, em$1, cssRegex, ratioNames, rem, rem$1, defaultFontSize, functionsMap$3, functionsMap$2, functionsMap$1, dataURIRegex, formatHintMap, _templateObject$1, _templateObject, functionsMap, getBorderWidth, getBorderColor, namedColorMap, hexRegex, hexRgbaRegex, reducedHexRegex, reducedRgbaHexRegex, rgbRegex, rgbaRegex, hslRegex, hslaRegex, reduceHexValue, reduceHexValue$1, isRgb, isRgba, isHsl, isHsla, curriedAdjustHue, curriedAdjustHue$1, curriedDarken, curriedDarken$1, curriedDesaturate, curriedDesaturate$1, curriedLighten, curriedLighten$1, curriedMix, mix$1, curriedOpacify, curriedOpacify$1, defaultReturnIfLightColor, defaultReturnIfDarkColor, curriedSaturate, curriedSaturate$1, curriedSetHue, curriedSetHue$1, curriedSetLightness, curriedSetLightness$1, curriedSetSaturation, curriedSetSaturation$1, curriedShade, curriedShade$1, curriedTint, curriedTint$1, curriedTransparentize, curriedTransparentize$1, sideMap, stateMap$1, positionMap, stateMap;
var init_polished_esm = __esm({
  "node_modules/polished/dist/polished.esm.js"() {
    init_extends();
    init_assertThisInitialized();
    init_inheritsLoose();
    init_wrapNativeSuper();
    init_taggedTemplateLiteralLoose();
    defaultSymbols = {
      symbols: {
        "*": {
          infix: {
            symbol: "*",
            f: multiplication,
            notation: "infix",
            precedence: 4,
            rightToLeft: 0,
            argCount: 2
          },
          symbol: "*",
          regSymbol: "\\*"
        },
        "/": {
          infix: {
            symbol: "/",
            f: division,
            notation: "infix",
            precedence: 4,
            rightToLeft: 0,
            argCount: 2
          },
          symbol: "/",
          regSymbol: "/"
        },
        "+": {
          infix: {
            symbol: "+",
            f: addition,
            notation: "infix",
            precedence: 2,
            rightToLeft: 0,
            argCount: 2
          },
          prefix: {
            symbol: "+",
            f: last,
            notation: "prefix",
            precedence: 3,
            rightToLeft: 0,
            argCount: 1
          },
          symbol: "+",
          regSymbol: "\\+"
        },
        "-": {
          infix: {
            symbol: "-",
            f: subtraction,
            notation: "infix",
            precedence: 2,
            rightToLeft: 0,
            argCount: 2
          },
          prefix: {
            symbol: "-",
            f: negation,
            notation: "prefix",
            precedence: 3,
            rightToLeft: 0,
            argCount: 1
          },
          symbol: "-",
          regSymbol: "-"
        },
        ",": {
          infix: {
            symbol: ",",
            f: comma,
            notation: "infix",
            precedence: 1,
            rightToLeft: 0,
            argCount: 2
          },
          symbol: ",",
          regSymbol: ","
        },
        "(": {
          prefix: {
            symbol: "(",
            f: last,
            notation: "prefix",
            precedence: 0,
            rightToLeft: 0,
            argCount: 1
          },
          symbol: "(",
          regSymbol: "\\("
        },
        ")": {
          postfix: {
            symbol: ")",
            f: void 0,
            notation: "postfix",
            precedence: 0,
            rightToLeft: 0,
            argCount: 1
          },
          symbol: ")",
          regSymbol: "\\)"
        },
        min: {
          func: {
            symbol: "min",
            f: min,
            notation: "func",
            precedence: 0,
            rightToLeft: 0,
            argCount: 1
          },
          symbol: "min",
          regSymbol: "min\\b"
        },
        max: {
          func: {
            symbol: "max",
            f: max,
            notation: "func",
            precedence: 0,
            rightToLeft: 0,
            argCount: 1
          },
          symbol: "max",
          regSymbol: "max\\b"
        }
      }
    };
    defaultSymbolMap = defaultSymbols;
    ERRORS = {
      "1": "Passed invalid arguments to hsl, please pass multiple numbers e.g. hsl(360, 0.75, 0.4) or an object e.g. rgb({ hue: 255, saturation: 0.4, lightness: 0.75 }).\n\n",
      "2": "Passed invalid arguments to hsla, please pass multiple numbers e.g. hsla(360, 0.75, 0.4, 0.7) or an object e.g. rgb({ hue: 255, saturation: 0.4, lightness: 0.75, alpha: 0.7 }).\n\n",
      "3": "Passed an incorrect argument to a color function, please pass a string representation of a color.\n\n",
      "4": "Couldn't generate valid rgb string from %s, it returned %s.\n\n",
      "5": "Couldn't parse the color string. Please provide the color as a string in hex, rgb, rgba, hsl or hsla notation.\n\n",
      "6": "Passed invalid arguments to rgb, please pass multiple numbers e.g. rgb(255, 205, 100) or an object e.g. rgb({ red: 255, green: 205, blue: 100 }).\n\n",
      "7": "Passed invalid arguments to rgba, please pass multiple numbers e.g. rgb(255, 205, 100, 0.75) or an object e.g. rgb({ red: 255, green: 205, blue: 100, alpha: 0.75 }).\n\n",
      "8": "Passed invalid argument to toColorString, please pass a RgbColor, RgbaColor, HslColor or HslaColor object.\n\n",
      "9": "Please provide a number of steps to the modularScale helper.\n\n",
      "10": "Please pass a number or one of the predefined scales to the modularScale helper as the ratio.\n\n",
      "11": 'Invalid value passed as base to modularScale, expected number or em string but got "%s"\n\n',
      "12": 'Expected a string ending in "px" or a number passed as the first argument to %s(), got "%s" instead.\n\n',
      "13": 'Expected a string ending in "px" or a number passed as the second argument to %s(), got "%s" instead.\n\n',
      "14": 'Passed invalid pixel value ("%s") to %s(), please pass a value like "12px" or 12.\n\n',
      "15": 'Passed invalid base value ("%s") to %s(), please pass a value like "12px" or 12.\n\n',
      "16": "You must provide a template to this method.\n\n",
      "17": "You passed an unsupported selector state to this method.\n\n",
      "18": "minScreen and maxScreen must be provided as stringified numbers with the same units.\n\n",
      "19": "fromSize and toSize must be provided as stringified numbers with the same units.\n\n",
      "20": "expects either an array of objects or a single object with the properties prop, fromSize, and toSize.\n\n",
      "21": "expects the objects in the first argument array to have the properties `prop`, `fromSize`, and `toSize`.\n\n",
      "22": "expects the first argument object to have the properties `prop`, `fromSize`, and `toSize`.\n\n",
      "23": "fontFace expects a name of a font-family.\n\n",
      "24": "fontFace expects either the path to the font file(s) or a name of a local copy.\n\n",
      "25": "fontFace expects localFonts to be an array.\n\n",
      "26": "fontFace expects fileFormats to be an array.\n\n",
      "27": "radialGradient requries at least 2 color-stops to properly render.\n\n",
      "28": "Please supply a filename to retinaImage() as the first argument.\n\n",
      "29": "Passed invalid argument to triangle, please pass correct pointingDirection e.g. 'right'.\n\n",
      "30": "Passed an invalid value to `height` or `width`. Please provide a pixel based unit.\n\n",
      "31": "The animation shorthand only takes 8 arguments. See the specification for more information: http://mdn.io/animation\n\n",
      "32": "To pass multiple animations please supply them in arrays, e.g. animation(['rotate', '2s'], ['move', '1s'])\nTo pass a single animation please supply them in simple values, e.g. animation('rotate', '2s')\n\n",
      "33": "The animation shorthand arrays can only have 8 elements. See the specification for more information: http://mdn.io/animation\n\n",
      "34": "borderRadius expects a radius value as a string or number as the second argument.\n\n",
      "35": 'borderRadius expects one of "top", "bottom", "left" or "right" as the first argument.\n\n',
      "36": "Property must be a string value.\n\n",
      "37": "Syntax Error at %s.\n\n",
      "38": "Formula contains a function that needs parentheses at %s.\n\n",
      "39": "Formula is missing closing parenthesis at %s.\n\n",
      "40": "Formula has too many closing parentheses at %s.\n\n",
      "41": "All values in a formula must have the same unit or be unitless.\n\n",
      "42": "Please provide a number of steps to the modularScale helper.\n\n",
      "43": "Please pass a number or one of the predefined scales to the modularScale helper as the ratio.\n\n",
      "44": "Invalid value passed as base to modularScale, expected number or em/rem string but got %s.\n\n",
      "45": "Passed invalid argument to hslToColorString, please pass a HslColor or HslaColor object.\n\n",
      "46": "Passed invalid argument to rgbToColorString, please pass a RgbColor or RgbaColor object.\n\n",
      "47": "minScreen and maxScreen must be provided as stringified numbers with the same units.\n\n",
      "48": "fromSize and toSize must be provided as stringified numbers with the same units.\n\n",
      "49": "Expects either an array of objects or a single object with the properties prop, fromSize, and toSize.\n\n",
      "50": "Expects the objects in the first argument array to have the properties prop, fromSize, and toSize.\n\n",
      "51": "Expects the first argument object to have the properties prop, fromSize, and toSize.\n\n",
      "52": "fontFace expects either the path to the font file(s) or a name of a local copy.\n\n",
      "53": "fontFace expects localFonts to be an array.\n\n",
      "54": "fontFace expects fileFormats to be an array.\n\n",
      "55": "fontFace expects a name of a font-family.\n\n",
      "56": "linearGradient requries at least 2 color-stops to properly render.\n\n",
      "57": "radialGradient requries at least 2 color-stops to properly render.\n\n",
      "58": "Please supply a filename to retinaImage() as the first argument.\n\n",
      "59": "Passed invalid argument to triangle, please pass correct pointingDirection e.g. 'right'.\n\n",
      "60": "Passed an invalid value to `height` or `width`. Please provide a pixel based unit.\n\n",
      "61": "Property must be a string value.\n\n",
      "62": "borderRadius expects a radius value as a string or number as the second argument.\n\n",
      "63": 'borderRadius expects one of "top", "bottom", "left" or "right" as the first argument.\n\n',
      "64": "The animation shorthand only takes 8 arguments. See the specification for more information: http://mdn.io/animation.\n\n",
      "65": "To pass multiple animations please supply them in arrays, e.g. animation(['rotate', '2s'], ['move', '1s'])\\nTo pass a single animation please supply them in simple values, e.g. animation('rotate', '2s').\n\n",
      "66": "The animation shorthand arrays can only have 8 elements. See the specification for more information: http://mdn.io/animation.\n\n",
      "67": "You must provide a template to this method.\n\n",
      "68": "You passed an unsupported selector state to this method.\n\n",
      "69": 'Expected a string ending in "px" or a number passed as the first argument to %s(), got %s instead.\n\n',
      "70": 'Expected a string ending in "px" or a number passed as the second argument to %s(), got %s instead.\n\n',
      "71": 'Passed invalid pixel value %s to %s(), please pass a value like "12px" or 12.\n\n',
      "72": 'Passed invalid base value %s to %s(), please pass a value like "12px" or 12.\n\n',
      "73": "Please provide a valid CSS variable.\n\n",
      "74": "CSS variable not found and no default was provided.\n\n",
      "75": "important requires a valid style object, got a %s instead.\n\n",
      "76": "fromSize and toSize must be provided as stringified numbers with the same units as minScreen and maxScreen.\n\n",
      "77": 'remToPx expects a value in "rem" but you provided it in "%s".\n\n',
      "78": 'base must be set in "px" or "%" but you set it in "%s".\n'
    };
    PolishedError = (function(_Error) {
      _inheritsLoose(PolishedError2, _Error);
      function PolishedError2(code) {
        var _this;
        if (false) {
          _this = _Error.call(this, "An error occurred. See https://github.com/styled-components/polished/blob/main/src/internalHelpers/errors.md#" + code + " for more information.") || this;
        } else {
          for (var _len2 = arguments.length, args = new Array(_len2 > 1 ? _len2 - 1 : 0), _key2 = 1; _key2 < _len2; _key2++) {
            args[_key2 - 1] = arguments[_key2];
          }
          _this = _Error.call(this, format.apply(void 0, [ERRORS[code]].concat(args))) || this;
        }
        return _assertThisInitialized(_this);
      }
      return PolishedError2;
    })(_wrapNativeSuper(Error));
    unitRegExp = /((?!\w)a|na|hc|mc|dg|me[r]?|xe|ni(?![a-zA-Z])|mm|cp|tp|xp|q(?!s)|hv|xamv|nimv|wv|sm|s(?!\D|$)|ged|darg?|nrut)/g;
    cssVariableRegex = /--[\S]*/g;
    positionMap$1 = ["Top", "Right", "Bottom", "Left"];
    cssRegex$1 = /^([+-]?(?:\d+|\d*\.\d+))([a-z]*|%)$/;
    pxtoFactory = function pxtoFactory2(to) {
      return function(pxval, base) {
        if (base === void 0) {
          base = "16px";
        }
        var newPxval = pxval;
        var newBase = base;
        if (typeof pxval === "string") {
          if (!endsWith(pxval, "px")) {
            throw new PolishedError(69, to, pxval);
          }
          newPxval = stripUnit(pxval);
        }
        if (typeof base === "string") {
          if (!endsWith(base, "px")) {
            throw new PolishedError(70, to, base);
          }
          newBase = stripUnit(base);
        }
        if (typeof newPxval === "string") {
          throw new PolishedError(71, pxval, to);
        }
        if (typeof newBase === "string") {
          throw new PolishedError(72, base, to);
        }
        return "" + newPxval / newBase + to;
      };
    };
    pixelsto = pxtoFactory;
    em = pixelsto("em");
    em$1 = em;
    cssRegex = /^([+-]?(?:\d+|\d*\.\d+))([a-z]*|%)$/;
    ratioNames = {
      minorSecond: 1.067,
      majorSecond: 1.125,
      minorThird: 1.2,
      majorThird: 1.25,
      perfectFourth: 1.333,
      augFourth: 1.414,
      perfectFifth: 1.5,
      minorSixth: 1.6,
      goldenSection: 1.618,
      majorSixth: 1.667,
      minorSeventh: 1.778,
      majorSeventh: 1.875,
      octave: 2,
      majorTenth: 2.5,
      majorEleventh: 2.667,
      majorTwelfth: 3,
      doubleOctave: 4
    };
    rem = pixelsto("rem");
    rem$1 = rem;
    defaultFontSize = 16;
    functionsMap$3 = {
      back: "cubic-bezier(0.600, -0.280, 0.735, 0.045)",
      circ: "cubic-bezier(0.600,  0.040, 0.980, 0.335)",
      cubic: "cubic-bezier(0.550,  0.055, 0.675, 0.190)",
      expo: "cubic-bezier(0.950,  0.050, 0.795, 0.035)",
      quad: "cubic-bezier(0.550,  0.085, 0.680, 0.530)",
      quart: "cubic-bezier(0.895,  0.030, 0.685, 0.220)",
      quint: "cubic-bezier(0.755,  0.050, 0.855, 0.060)",
      sine: "cubic-bezier(0.470,  0.000, 0.745, 0.715)"
    };
    functionsMap$2 = {
      back: "cubic-bezier(0.680, -0.550, 0.265, 1.550)",
      circ: "cubic-bezier(0.785,  0.135, 0.150, 0.860)",
      cubic: "cubic-bezier(0.645,  0.045, 0.355, 1.000)",
      expo: "cubic-bezier(1.000,  0.000, 0.000, 1.000)",
      quad: "cubic-bezier(0.455,  0.030, 0.515, 0.955)",
      quart: "cubic-bezier(0.770,  0.000, 0.175, 1.000)",
      quint: "cubic-bezier(0.860,  0.000, 0.070, 1.000)",
      sine: "cubic-bezier(0.445,  0.050, 0.550, 0.950)"
    };
    functionsMap$1 = {
      back: "cubic-bezier(0.175,  0.885, 0.320, 1.275)",
      cubic: "cubic-bezier(0.215,  0.610, 0.355, 1.000)",
      circ: "cubic-bezier(0.075,  0.820, 0.165, 1.000)",
      expo: "cubic-bezier(0.190,  1.000, 0.220, 1.000)",
      quad: "cubic-bezier(0.250,  0.460, 0.450, 0.940)",
      quart: "cubic-bezier(0.165,  0.840, 0.440, 1.000)",
      quint: "cubic-bezier(0.230,  1.000, 0.320, 1.000)",
      sine: "cubic-bezier(0.390,  0.575, 0.565, 1.000)"
    };
    dataURIRegex = /^\s*data:([a-z]+\/[a-z-]+(;[a-z-]+=[a-z-]+)?)?(;charset=[a-z0-9-]+)?(;base64)?,[a-z0-9!$&',()*+,;=\-._~:@/?%\s]*\s*$/i;
    formatHintMap = {
      woff: "woff",
      woff2: "woff2",
      ttf: "truetype",
      otf: "opentype",
      eot: "embedded-opentype",
      svg: "svg",
      svgz: "svg"
    };
    functionsMap = {
      easeInBack: "cubic-bezier(0.600, -0.280, 0.735, 0.045)",
      easeInCirc: "cubic-bezier(0.600,  0.040, 0.980, 0.335)",
      easeInCubic: "cubic-bezier(0.550,  0.055, 0.675, 0.190)",
      easeInExpo: "cubic-bezier(0.950,  0.050, 0.795, 0.035)",
      easeInQuad: "cubic-bezier(0.550,  0.085, 0.680, 0.530)",
      easeInQuart: "cubic-bezier(0.895,  0.030, 0.685, 0.220)",
      easeInQuint: "cubic-bezier(0.755,  0.050, 0.855, 0.060)",
      easeInSine: "cubic-bezier(0.470,  0.000, 0.745, 0.715)",
      easeOutBack: "cubic-bezier(0.175,  0.885, 0.320, 1.275)",
      easeOutCubic: "cubic-bezier(0.215,  0.610, 0.355, 1.000)",
      easeOutCirc: "cubic-bezier(0.075,  0.820, 0.165, 1.000)",
      easeOutExpo: "cubic-bezier(0.190,  1.000, 0.220, 1.000)",
      easeOutQuad: "cubic-bezier(0.250,  0.460, 0.450, 0.940)",
      easeOutQuart: "cubic-bezier(0.165,  0.840, 0.440, 1.000)",
      easeOutQuint: "cubic-bezier(0.230,  1.000, 0.320, 1.000)",
      easeOutSine: "cubic-bezier(0.390,  0.575, 0.565, 1.000)",
      easeInOutBack: "cubic-bezier(0.680, -0.550, 0.265, 1.550)",
      easeInOutCirc: "cubic-bezier(0.785,  0.135, 0.150, 0.860)",
      easeInOutCubic: "cubic-bezier(0.645,  0.045, 0.355, 1.000)",
      easeInOutExpo: "cubic-bezier(1.000,  0.000, 0.000, 1.000)",
      easeInOutQuad: "cubic-bezier(0.455,  0.030, 0.515, 0.955)",
      easeInOutQuart: "cubic-bezier(0.770,  0.000, 0.175, 1.000)",
      easeInOutQuint: "cubic-bezier(0.860,  0.000, 0.070, 1.000)",
      easeInOutSine: "cubic-bezier(0.445,  0.050, 0.550, 0.950)"
    };
    getBorderWidth = function getBorderWidth2(pointingDirection, height, width) {
      var fullWidth = "" + width[0] + (width[1] || "");
      var halfWidth = "" + width[0] / 2 + (width[1] || "");
      var fullHeight = "" + height[0] + (height[1] || "");
      var halfHeight = "" + height[0] / 2 + (height[1] || "");
      switch (pointingDirection) {
        case "top":
          return "0 " + halfWidth + " " + fullHeight + " " + halfWidth;
        case "topLeft":
          return fullWidth + " " + fullHeight + " 0 0";
        case "left":
          return halfHeight + " " + fullWidth + " " + halfHeight + " 0";
        case "bottomLeft":
          return fullWidth + " 0 0 " + fullHeight;
        case "bottom":
          return fullHeight + " " + halfWidth + " 0 " + halfWidth;
        case "bottomRight":
          return "0 0 " + fullWidth + " " + fullHeight;
        case "right":
          return halfHeight + " 0 " + halfHeight + " " + fullWidth;
        case "topRight":
        default:
          return "0 " + fullWidth + " " + fullHeight + " 0";
      }
    };
    getBorderColor = function getBorderColor2(pointingDirection, foregroundColor) {
      switch (pointingDirection) {
        case "top":
        case "bottomRight":
          return {
            borderBottomColor: foregroundColor
          };
        case "right":
        case "bottomLeft":
          return {
            borderLeftColor: foregroundColor
          };
        case "bottom":
        case "topLeft":
          return {
            borderTopColor: foregroundColor
          };
        case "left":
        case "topRight":
          return {
            borderRightColor: foregroundColor
          };
        default:
          throw new PolishedError(59);
      }
    };
    namedColorMap = {
      aliceblue: "f0f8ff",
      antiquewhite: "faebd7",
      aqua: "00ffff",
      aquamarine: "7fffd4",
      azure: "f0ffff",
      beige: "f5f5dc",
      bisque: "ffe4c4",
      black: "000",
      blanchedalmond: "ffebcd",
      blue: "0000ff",
      blueviolet: "8a2be2",
      brown: "a52a2a",
      burlywood: "deb887",
      cadetblue: "5f9ea0",
      chartreuse: "7fff00",
      chocolate: "d2691e",
      coral: "ff7f50",
      cornflowerblue: "6495ed",
      cornsilk: "fff8dc",
      crimson: "dc143c",
      cyan: "00ffff",
      darkblue: "00008b",
      darkcyan: "008b8b",
      darkgoldenrod: "b8860b",
      darkgray: "a9a9a9",
      darkgreen: "006400",
      darkgrey: "a9a9a9",
      darkkhaki: "bdb76b",
      darkmagenta: "8b008b",
      darkolivegreen: "556b2f",
      darkorange: "ff8c00",
      darkorchid: "9932cc",
      darkred: "8b0000",
      darksalmon: "e9967a",
      darkseagreen: "8fbc8f",
      darkslateblue: "483d8b",
      darkslategray: "2f4f4f",
      darkslategrey: "2f4f4f",
      darkturquoise: "00ced1",
      darkviolet: "9400d3",
      deeppink: "ff1493",
      deepskyblue: "00bfff",
      dimgray: "696969",
      dimgrey: "696969",
      dodgerblue: "1e90ff",
      firebrick: "b22222",
      floralwhite: "fffaf0",
      forestgreen: "228b22",
      fuchsia: "ff00ff",
      gainsboro: "dcdcdc",
      ghostwhite: "f8f8ff",
      gold: "ffd700",
      goldenrod: "daa520",
      gray: "808080",
      green: "008000",
      greenyellow: "adff2f",
      grey: "808080",
      honeydew: "f0fff0",
      hotpink: "ff69b4",
      indianred: "cd5c5c",
      indigo: "4b0082",
      ivory: "fffff0",
      khaki: "f0e68c",
      lavender: "e6e6fa",
      lavenderblush: "fff0f5",
      lawngreen: "7cfc00",
      lemonchiffon: "fffacd",
      lightblue: "add8e6",
      lightcoral: "f08080",
      lightcyan: "e0ffff",
      lightgoldenrodyellow: "fafad2",
      lightgray: "d3d3d3",
      lightgreen: "90ee90",
      lightgrey: "d3d3d3",
      lightpink: "ffb6c1",
      lightsalmon: "ffa07a",
      lightseagreen: "20b2aa",
      lightskyblue: "87cefa",
      lightslategray: "789",
      lightslategrey: "789",
      lightsteelblue: "b0c4de",
      lightyellow: "ffffe0",
      lime: "0f0",
      limegreen: "32cd32",
      linen: "faf0e6",
      magenta: "f0f",
      maroon: "800000",
      mediumaquamarine: "66cdaa",
      mediumblue: "0000cd",
      mediumorchid: "ba55d3",
      mediumpurple: "9370db",
      mediumseagreen: "3cb371",
      mediumslateblue: "7b68ee",
      mediumspringgreen: "00fa9a",
      mediumturquoise: "48d1cc",
      mediumvioletred: "c71585",
      midnightblue: "191970",
      mintcream: "f5fffa",
      mistyrose: "ffe4e1",
      moccasin: "ffe4b5",
      navajowhite: "ffdead",
      navy: "000080",
      oldlace: "fdf5e6",
      olive: "808000",
      olivedrab: "6b8e23",
      orange: "ffa500",
      orangered: "ff4500",
      orchid: "da70d6",
      palegoldenrod: "eee8aa",
      palegreen: "98fb98",
      paleturquoise: "afeeee",
      palevioletred: "db7093",
      papayawhip: "ffefd5",
      peachpuff: "ffdab9",
      peru: "cd853f",
      pink: "ffc0cb",
      plum: "dda0dd",
      powderblue: "b0e0e6",
      purple: "800080",
      rebeccapurple: "639",
      red: "f00",
      rosybrown: "bc8f8f",
      royalblue: "4169e1",
      saddlebrown: "8b4513",
      salmon: "fa8072",
      sandybrown: "f4a460",
      seagreen: "2e8b57",
      seashell: "fff5ee",
      sienna: "a0522d",
      silver: "c0c0c0",
      skyblue: "87ceeb",
      slateblue: "6a5acd",
      slategray: "708090",
      slategrey: "708090",
      snow: "fffafa",
      springgreen: "00ff7f",
      steelblue: "4682b4",
      tan: "d2b48c",
      teal: "008080",
      thistle: "d8bfd8",
      tomato: "ff6347",
      turquoise: "40e0d0",
      violet: "ee82ee",
      wheat: "f5deb3",
      white: "fff",
      whitesmoke: "f5f5f5",
      yellow: "ff0",
      yellowgreen: "9acd32"
    };
    hexRegex = /^#[a-fA-F0-9]{6}$/;
    hexRgbaRegex = /^#[a-fA-F0-9]{8}$/;
    reducedHexRegex = /^#[a-fA-F0-9]{3}$/;
    reducedRgbaHexRegex = /^#[a-fA-F0-9]{4}$/;
    rgbRegex = /^rgb\(\s*(\d{1,3})\s*(?:,)?\s*(\d{1,3})\s*(?:,)?\s*(\d{1,3})\s*\)$/i;
    rgbaRegex = /^rgb(?:a)?\(\s*(\d{1,3})\s*(?:,)?\s*(\d{1,3})\s*(?:,)?\s*(\d{1,3})\s*(?:,|\/)\s*([-+]?\d*[.]?\d+[%]?)\s*\)$/i;
    hslRegex = /^hsl\(\s*(\d{0,3}[.]?[0-9]+(?:deg)?)\s*(?:,)?\s*(\d{1,3}[.]?[0-9]?)%\s*(?:,)?\s*(\d{1,3}[.]?[0-9]?)%\s*\)$/i;
    hslaRegex = /^hsl(?:a)?\(\s*(\d{0,3}[.]?[0-9]+(?:deg)?)\s*(?:,)?\s*(\d{1,3}[.]?[0-9]?)%\s*(?:,)?\s*(\d{1,3}[.]?[0-9]?)%\s*(?:,|\/)\s*([-+]?\d*[.]?\d+[%]?)\s*\)$/i;
    reduceHexValue = function reduceHexValue2(value) {
      if (value.length === 7 && value[1] === value[2] && value[3] === value[4] && value[5] === value[6]) {
        return "#" + value[1] + value[3] + value[5];
      }
      return value;
    };
    reduceHexValue$1 = reduceHexValue;
    isRgb = function isRgb2(color) {
      return typeof color.red === "number" && typeof color.green === "number" && typeof color.blue === "number" && (typeof color.alpha !== "number" || typeof color.alpha === "undefined");
    };
    isRgba = function isRgba2(color) {
      return typeof color.red === "number" && typeof color.green === "number" && typeof color.blue === "number" && typeof color.alpha === "number";
    };
    isHsl = function isHsl2(color) {
      return typeof color.hue === "number" && typeof color.saturation === "number" && typeof color.lightness === "number" && (typeof color.alpha !== "number" || typeof color.alpha === "undefined");
    };
    isHsla = function isHsla2(color) {
      return typeof color.hue === "number" && typeof color.saturation === "number" && typeof color.lightness === "number" && typeof color.alpha === "number";
    };
    curriedAdjustHue = curry(adjustHue);
    curriedAdjustHue$1 = curriedAdjustHue;
    curriedDarken = curry(darken);
    curriedDarken$1 = curriedDarken;
    curriedDesaturate = curry(desaturate);
    curriedDesaturate$1 = curriedDesaturate;
    curriedLighten = curry(lighten);
    curriedLighten$1 = curriedLighten;
    curriedMix = curry(mix);
    mix$1 = curriedMix;
    curriedOpacify = curry(opacify);
    curriedOpacify$1 = curriedOpacify;
    defaultReturnIfLightColor = "#000";
    defaultReturnIfDarkColor = "#fff";
    curriedSaturate = curry(saturate);
    curriedSaturate$1 = curriedSaturate;
    curriedSetHue = curry(setHue);
    curriedSetHue$1 = curriedSetHue;
    curriedSetLightness = curry(setLightness);
    curriedSetLightness$1 = curriedSetLightness;
    curriedSetSaturation = curry(setSaturation);
    curriedSetSaturation$1 = curriedSetSaturation;
    curriedShade = curry(shade);
    curriedShade$1 = curriedShade;
    curriedTint = curry(tint);
    curriedTint$1 = curriedTint;
    curriedTransparentize = curry(transparentize);
    curriedTransparentize$1 = curriedTransparentize;
    sideMap = ["top", "right", "bottom", "left"];
    stateMap$1 = [void 0, null, "active", "focus", "hover"];
    positionMap = ["absolute", "fixed", "relative", "static", "sticky"];
    stateMap = [void 0, null, "active", "focus", "hover"];
  }
});

// node_modules/slugify/slugify.js
var require_slugify = __commonJS({
  "node_modules/slugify/slugify.js"(exports, module) {
    (function(name, root, factory) {
      if (typeof exports === "object") {
        module.exports = factory();
        module.exports["default"] = factory();
      } else if (typeof define === "function" && define.amd) {
        define(factory);
      } else {
        root[name] = factory();
      }
    })("slugify", exports, function() {
      var charMap = JSON.parse(`{"$":"dollar","%":"percent","&":"and","<":"less",">":"greater","|":"or","¢":"cent","£":"pound","¤":"currency","¥":"yen","©":"(c)","ª":"a","®":"(r)","º":"o","À":"A","Á":"A","Â":"A","Ã":"A","Ä":"A","Å":"A","Æ":"AE","Ç":"C","È":"E","É":"E","Ê":"E","Ë":"E","Ì":"I","Í":"I","Î":"I","Ï":"I","Ð":"D","Ñ":"N","Ò":"O","Ó":"O","Ô":"O","Õ":"O","Ö":"O","Ø":"O","Ù":"U","Ú":"U","Û":"U","Ü":"U","Ý":"Y","Þ":"TH","ß":"ss","à":"a","á":"a","â":"a","ã":"a","ä":"a","å":"a","æ":"ae","ç":"c","è":"e","é":"e","ê":"e","ë":"e","ì":"i","í":"i","î":"i","ï":"i","ð":"d","ñ":"n","ò":"o","ó":"o","ô":"o","õ":"o","ö":"o","ø":"o","ù":"u","ú":"u","û":"u","ü":"u","ý":"y","þ":"th","ÿ":"y","Ā":"A","ā":"a","Ă":"A","ă":"a","Ą":"A","ą":"a","Ć":"C","ć":"c","Č":"C","č":"c","Ď":"D","ď":"d","Đ":"DJ","đ":"dj","Ē":"E","ē":"e","Ė":"E","ė":"e","Ę":"e","ę":"e","Ě":"E","ě":"e","Ğ":"G","ğ":"g","Ģ":"G","ģ":"g","Ĩ":"I","ĩ":"i","Ī":"i","ī":"i","Į":"I","į":"i","İ":"I","ı":"i","Ķ":"k","ķ":"k","Ļ":"L","ļ":"l","Ľ":"L","ľ":"l","Ł":"L","ł":"l","Ń":"N","ń":"n","Ņ":"N","ņ":"n","Ň":"N","ň":"n","Ō":"O","ō":"o","Ő":"O","ő":"o","Œ":"OE","œ":"oe","Ŕ":"R","ŕ":"r","Ř":"R","ř":"r","Ś":"S","ś":"s","Ş":"S","ş":"s","Š":"S","š":"s","Ţ":"T","ţ":"t","Ť":"T","ť":"t","Ũ":"U","ũ":"u","Ū":"u","ū":"u","Ů":"U","ů":"u","Ű":"U","ű":"u","Ų":"U","ų":"u","Ŵ":"W","ŵ":"w","Ŷ":"Y","ŷ":"y","Ÿ":"Y","Ź":"Z","ź":"z","Ż":"Z","ż":"z","Ž":"Z","ž":"z","Ə":"E","ƒ":"f","Ơ":"O","ơ":"o","Ư":"U","ư":"u","ǈ":"LJ","ǉ":"lj","ǋ":"NJ","ǌ":"nj","Ș":"S","ș":"s","Ț":"T","ț":"t","ə":"e","˚":"o","Ά":"A","Έ":"E","Ή":"H","Ί":"I","Ό":"O","Ύ":"Y","Ώ":"W","ΐ":"i","Α":"A","Β":"B","Γ":"G","Δ":"D","Ε":"E","Ζ":"Z","Η":"H","Θ":"8","Ι":"I","Κ":"K","Λ":"L","Μ":"M","Ν":"N","Ξ":"3","Ο":"O","Π":"P","Ρ":"R","Σ":"S","Τ":"T","Υ":"Y","Φ":"F","Χ":"X","Ψ":"PS","Ω":"W","Ϊ":"I","Ϋ":"Y","ά":"a","έ":"e","ή":"h","ί":"i","ΰ":"y","α":"a","β":"b","γ":"g","δ":"d","ε":"e","ζ":"z","η":"h","θ":"8","ι":"i","κ":"k","λ":"l","μ":"m","ν":"n","ξ":"3","ο":"o","π":"p","ρ":"r","ς":"s","σ":"s","τ":"t","υ":"y","φ":"f","χ":"x","ψ":"ps","ω":"w","ϊ":"i","ϋ":"y","ό":"o","ύ":"y","ώ":"w","Ё":"Yo","Ђ":"DJ","Є":"Ye","І":"I","Ї":"Yi","Ј":"J","Љ":"LJ","Њ":"NJ","Ћ":"C","Џ":"DZ","А":"A","Б":"B","В":"V","Г":"G","Д":"D","Е":"E","Ж":"Zh","З":"Z","И":"I","Й":"J","К":"K","Л":"L","М":"M","Н":"N","О":"O","П":"P","Р":"R","С":"S","Т":"T","У":"U","Ф":"F","Х":"H","Ц":"C","Ч":"Ch","Ш":"Sh","Щ":"Sh","Ъ":"U","Ы":"Y","Ь":"","Э":"E","Ю":"Yu","Я":"Ya","а":"a","б":"b","в":"v","г":"g","д":"d","е":"e","ж":"zh","з":"z","и":"i","й":"j","к":"k","л":"l","м":"m","н":"n","о":"o","п":"p","р":"r","с":"s","т":"t","у":"u","ф":"f","х":"h","ц":"c","ч":"ch","ш":"sh","щ":"sh","ъ":"u","ы":"y","ь":"","э":"e","ю":"yu","я":"ya","ё":"yo","ђ":"dj","є":"ye","і":"i","ї":"yi","ј":"j","љ":"lj","њ":"nj","ћ":"c","ѝ":"u","џ":"dz","Ґ":"G","ґ":"g","Ғ":"GH","ғ":"gh","Қ":"KH","қ":"kh","Ң":"NG","ң":"ng","Ү":"UE","ү":"ue","Ұ":"U","ұ":"u","Һ":"H","һ":"h","Ә":"AE","ә":"ae","Ө":"OE","ө":"oe","฿":"baht","ა":"a","ბ":"b","გ":"g","დ":"d","ე":"e","ვ":"v","ზ":"z","თ":"t","ი":"i","კ":"k","ლ":"l","მ":"m","ნ":"n","ო":"o","პ":"p","ჟ":"zh","რ":"r","ს":"s","ტ":"t","უ":"u","ფ":"f","ქ":"k","ღ":"gh","ყ":"q","შ":"sh","ჩ":"ch","ც":"ts","ძ":"dz","წ":"ts","ჭ":"ch","ხ":"kh","ჯ":"j","ჰ":"h","Ẁ":"W","ẁ":"w","Ẃ":"W","ẃ":"w","Ẅ":"W","ẅ":"w","ẞ":"SS","Ạ":"A","ạ":"a","Ả":"A","ả":"a","Ấ":"A","ấ":"a","Ầ":"A","ầ":"a","Ẩ":"A","ẩ":"a","Ẫ":"A","ẫ":"a","Ậ":"A","ậ":"a","Ắ":"A","ắ":"a","Ằ":"A","ằ":"a","Ẳ":"A","ẳ":"a","Ẵ":"A","ẵ":"a","Ặ":"A","ặ":"a","Ẹ":"E","ẹ":"e","Ẻ":"E","ẻ":"e","Ẽ":"E","ẽ":"e","Ế":"E","ế":"e","Ề":"E","ề":"e","Ể":"E","ể":"e","Ễ":"E","ễ":"e","Ệ":"E","ệ":"e","Ỉ":"I","ỉ":"i","Ị":"I","ị":"i","Ọ":"O","ọ":"o","Ỏ":"O","ỏ":"o","Ố":"O","ố":"o","Ồ":"O","ồ":"o","Ổ":"O","ổ":"o","Ỗ":"O","ỗ":"o","Ộ":"O","ộ":"o","Ớ":"O","ớ":"o","Ờ":"O","ờ":"o","Ở":"O","ở":"o","Ỡ":"O","ỡ":"o","Ợ":"O","ợ":"o","Ụ":"U","ụ":"u","Ủ":"U","ủ":"u","Ứ":"U","ứ":"u","Ừ":"U","ừ":"u","Ử":"U","ử":"u","Ữ":"U","ữ":"u","Ự":"U","ự":"u","Ỳ":"Y","ỳ":"y","Ỵ":"Y","ỵ":"y","Ỷ":"Y","ỷ":"y","Ỹ":"Y","ỹ":"y","‘":"'","’":"'","“":"\\"","”":"\\"","†":"+","•":"*","…":"...","₠":"ecu","₢":"cruzeiro","₣":"french franc","₤":"lira","₥":"mill","₦":"naira","₧":"peseta","₨":"rupee","₩":"won","₪":"new shequel","₫":"dong","€":"euro","₭":"kip","₮":"tugrik","₯":"drachma","₰":"penny","₱":"peso","₲":"guarani","₳":"austral","₴":"hryvnia","₵":"cedi","₸":"kazakhstani tenge","₹":"indian rupee","₺":"turkish lira","₽":"russian ruble","₿":"bitcoin","℠":"sm","™":"tm","∂":"d","∆":"delta","∑":"sum","∞":"infinity","♥":"love","元":"yuan","円":"yen","﷼":"rial"}`);
      var locales = JSON.parse('{"de":{"Ä":"AE","ä":"ae","Ö":"OE","ö":"oe","Ü":"UE","ü":"ue","%":"prozent","&":"und","|":"oder","∑":"summe","∞":"unendlich","♥":"liebe"},"vi":{"Đ":"D","đ":"d"},"fr":{"%":"pourcent","&":"et","<":"plus petit",">":"plus grand","|":"ou","¢":"centime","£":"livre","¤":"devise","₣":"franc","∑":"somme","∞":"infini","♥":"amour"}}');
      function replace3(string2, options) {
        if (typeof string2 !== "string") {
          throw new Error("slugify: string argument expected");
        }
        options = typeof options === "string" ? { replacement: options } : options || {};
        var locale = locales[options.locale] || {};
        var replacement = options.replacement === void 0 ? "-" : options.replacement;
        var slug = string2.split("").reduce(function(result, ch) {
          return result + (locale[ch] || charMap[ch] || ch).replace(options.remove || /[^\w\s$*_+~.()'"!\-:@]+/g, "");
        }, "").trim().replace(new RegExp("[\\s" + replacement + "]+", "g"), replacement);
        if (options.lower) {
          slug = slug.toLowerCase();
        }
        if (options.strict) {
          slug = slug.replace(new RegExp("[^a-zA-Z0-9" + replacement + "]", "g"), "").replace(new RegExp("[\\s" + replacement + "]+", "g"), replacement);
        }
        return slug;
      }
      replace3.extend = function(customMap) {
        for (var key2 in customMap) {
          charMap[key2] = customMap[key2];
        }
      };
      return replace3;
    });
  }
});

// node_modules/@emotion/memoize/dist/emotion-memoize.esm.js
function memoize(fn) {
  var cache2 = /* @__PURE__ */ Object.create(null);
  return function(arg) {
    if (cache2[arg] === void 0) cache2[arg] = fn(arg);
    return cache2[arg];
  };
}
var init_emotion_memoize_esm = __esm({
  "node_modules/@emotion/memoize/dist/emotion-memoize.esm.js"() {
  }
});

// node_modules/@emotion/is-prop-valid/dist/emotion-is-prop-valid.esm.js
var reactPropsRegex, isPropValid;
var init_emotion_is_prop_valid_esm = __esm({
  "node_modules/@emotion/is-prop-valid/dist/emotion-is-prop-valid.esm.js"() {
    init_emotion_memoize_esm();
    reactPropsRegex = /^((children|dangerouslySetInnerHTML|key|ref|autoFocus|defaultValue|defaultChecked|innerHTML|suppressContentEditableWarning|suppressHydrationWarning|valueLink|abbr|accept|acceptCharset|accessKey|action|allow|allowUserMedia|allowPaymentRequest|allowFullScreen|allowTransparency|alt|async|autoComplete|autoPlay|capture|cellPadding|cellSpacing|challenge|charSet|checked|cite|classID|className|cols|colSpan|content|contentEditable|contextMenu|controls|controlsList|coords|crossOrigin|data|dateTime|decoding|default|defer|dir|disabled|disablePictureInPicture|disableRemotePlayback|download|draggable|encType|enterKeyHint|form|formAction|formEncType|formMethod|formNoValidate|formTarget|frameBorder|headers|height|hidden|high|href|hrefLang|htmlFor|httpEquiv|id|inputMode|integrity|is|keyParams|keyType|kind|label|lang|list|loading|loop|low|marginHeight|marginWidth|max|maxLength|media|mediaGroup|method|min|minLength|multiple|muted|name|nonce|noValidate|open|optimum|pattern|placeholder|playsInline|poster|preload|profile|radioGroup|readOnly|referrerPolicy|rel|required|reversed|role|rows|rowSpan|sandbox|scope|scoped|scrolling|seamless|selected|shape|size|sizes|slot|span|spellCheck|src|srcDoc|srcLang|srcSet|start|step|style|summary|tabIndex|target|title|translate|type|useMap|value|width|wmode|wrap|about|datatype|inlist|prefix|property|resource|typeof|vocab|autoCapitalize|autoCorrect|autoSave|color|incremental|fallback|inert|itemProp|itemScope|itemType|itemID|itemRef|on|option|results|security|unselectable|accentHeight|accumulate|additive|alignmentBaseline|allowReorder|alphabetic|amplitude|arabicForm|ascent|attributeName|attributeType|autoReverse|azimuth|baseFrequency|baselineShift|baseProfile|bbox|begin|bias|by|calcMode|capHeight|clip|clipPathUnits|clipPath|clipRule|colorInterpolation|colorInterpolationFilters|colorProfile|colorRendering|contentScriptType|contentStyleType|cursor|cx|cy|d|decelerate|descent|diffuseConstant|direction|display|divisor|dominantBaseline|dur|dx|dy|edgeMode|elevation|enableBackground|end|exponent|externalResourcesRequired|fill|fillOpacity|fillRule|filter|filterRes|filterUnits|floodColor|floodOpacity|focusable|fontFamily|fontSize|fontSizeAdjust|fontStretch|fontStyle|fontVariant|fontWeight|format|from|fr|fx|fy|g1|g2|glyphName|glyphOrientationHorizontal|glyphOrientationVertical|glyphRef|gradientTransform|gradientUnits|hanging|horizAdvX|horizOriginX|ideographic|imageRendering|in|in2|intercept|k|k1|k2|k3|k4|kernelMatrix|kernelUnitLength|kerning|keyPoints|keySplines|keyTimes|lengthAdjust|letterSpacing|lightingColor|limitingConeAngle|local|markerEnd|markerMid|markerStart|markerHeight|markerUnits|markerWidth|mask|maskContentUnits|maskUnits|mathematical|mode|numOctaves|offset|opacity|operator|order|orient|orientation|origin|overflow|overlinePosition|overlineThickness|panose1|paintOrder|pathLength|patternContentUnits|patternTransform|patternUnits|pointerEvents|points|pointsAtX|pointsAtY|pointsAtZ|preserveAlpha|preserveAspectRatio|primitiveUnits|r|radius|refX|refY|renderingIntent|repeatCount|repeatDur|requiredExtensions|requiredFeatures|restart|result|rotate|rx|ry|scale|seed|shapeRendering|slope|spacing|specularConstant|specularExponent|speed|spreadMethod|startOffset|stdDeviation|stemh|stemv|stitchTiles|stopColor|stopOpacity|strikethroughPosition|strikethroughThickness|string|stroke|strokeDasharray|strokeDashoffset|strokeLinecap|strokeLinejoin|strokeMiterlimit|strokeOpacity|strokeWidth|surfaceScale|systemLanguage|tableValues|targetX|targetY|textAnchor|textDecoration|textRendering|textLength|to|transform|u1|u2|underlinePosition|underlineThickness|unicode|unicodeBidi|unicodeRange|unitsPerEm|vAlphabetic|vHanging|vIdeographic|vMathematical|values|vectorEffect|version|vertAdvY|vertOriginX|vertOriginY|viewBox|viewTarget|visibility|widths|wordSpacing|writingMode|x|xHeight|x1|x2|xChannelSelector|xlinkActuate|xlinkArcrole|xlinkHref|xlinkRole|xlinkShow|xlinkTitle|xlinkType|xmlBase|xmlns|xmlnsXlink|xmlLang|xmlSpace|y|y1|y2|yChannelSelector|z|zoomAndPan|for|class|autofocus)|(([Dd][Aa][Tt][Aa]|[Aa][Rr][Ii][Aa]|x)-.*))$/;
    isPropValid = memoize(
      function(prop) {
        return reactPropsRegex.test(prop) || prop.charCodeAt(0) === 111 && prop.charCodeAt(1) === 110 && prop.charCodeAt(2) < 91;
      }
      /* Z+1 */
    );
  }
});

// node_modules/shallowequal/index.js
var require_shallowequal = __commonJS({
  "node_modules/shallowequal/index.js"(exports, module) {
    module.exports = function shallowEqual2(objA, objB, compare, compareContext) {
      var ret = compare ? compare.call(compareContext, objA, objB) : void 0;
      if (ret !== void 0) {
        return !!ret;
      }
      if (objA === objB) {
        return true;
      }
      if (typeof objA !== "object" || !objA || typeof objB !== "object" || !objB) {
        return false;
      }
      var keysA = Object.keys(objA);
      var keysB = Object.keys(objB);
      if (keysA.length !== keysB.length) {
        return false;
      }
      var bHasOwnProperty = Object.prototype.hasOwnProperty.bind(objB);
      for (var idx = 0; idx < keysA.length; idx++) {
        var key2 = keysA[idx];
        if (!bHasOwnProperty(key2)) {
          return false;
        }
        var valueA = objA[key2];
        var valueB = objB[key2];
        ret = compare ? compare.call(compareContext, valueA, valueB, key2) : void 0;
        if (ret === false || ret === void 0 && valueA !== valueB) {
          return false;
        }
      }
      return true;
    };
  }
});

// node_modules/stylis/src/Enum.js
var MS, MOZ, WEBKIT, COMMENT, RULESET, DECLARATION, IMPORT, KEYFRAMES, LAYER;
var init_Enum = __esm({
  "node_modules/stylis/src/Enum.js"() {
    MS = "-ms-";
    MOZ = "-moz-";
    WEBKIT = "-webkit-";
    COMMENT = "comm";
    RULESET = "rule";
    DECLARATION = "decl";
    IMPORT = "@import";
    KEYFRAMES = "@keyframes";
    LAYER = "@layer";
  }
});

// node_modules/stylis/src/Utility.js
function hash(value, length2) {
  return charat(value, 0) ^ 45 ? (((length2 << 2 ^ charat(value, 0)) << 2 ^ charat(value, 1)) << 2 ^ charat(value, 2)) << 2 ^ charat(value, 3) : 0;
}
function trim(value) {
  return value.trim();
}
function match(value, pattern) {
  return (value = pattern.exec(value)) ? value[0] : value;
}
function replace(value, pattern, replacement) {
  return value.replace(pattern, replacement);
}
function indexof(value, search, position3) {
  return value.indexOf(search, position3);
}
function charat(value, index) {
  return value.charCodeAt(index) | 0;
}
function substr(value, begin, end) {
  return value.slice(begin, end);
}
function strlen(value) {
  return value.length;
}
function sizeof(value) {
  return value.length;
}
function append(value, array2) {
  return array2.push(value), value;
}
function combine(array2, callback) {
  return array2.map(callback).join("");
}
function filter(array2, pattern) {
  return array2.filter(function(value) {
    return !match(value, pattern);
  });
}
var abs, from, assign;
var init_Utility = __esm({
  "node_modules/stylis/src/Utility.js"() {
    abs = Math.abs;
    from = String.fromCharCode;
    assign = Object.assign;
  }
});

// node_modules/stylis/src/Tokenizer.js
function node(value, root, parent, type, props, children, length2, siblings) {
  return { value, root, parent, type, props, children, line, column, length: length2, return: "", siblings };
}
function copy(root, props) {
  return assign(node("", null, null, "", null, null, 0, root.siblings), root, { length: -root.length }, props);
}
function lift(root) {
  while (root.root)
    root = copy(root.root, { children: [root] });
  append(root, root.siblings);
}
function char() {
  return character;
}
function prev() {
  character = position2 > 0 ? charat(characters, --position2) : 0;
  if (column--, character === 10)
    column = 1, line--;
  return character;
}
function next() {
  character = position2 < length ? charat(characters, position2++) : 0;
  if (column++, character === 10)
    column = 1, line++;
  return character;
}
function peek() {
  return charat(characters, position2);
}
function caret() {
  return position2;
}
function slice(begin, end) {
  return substr(characters, begin, end);
}
function token(type) {
  switch (type) {
    // \0 \t \n \r \s whitespace token
    case 0:
    case 9:
    case 10:
    case 13:
    case 32:
      return 5;
    // ! + , / > @ ~ isolate token
    case 33:
    case 43:
    case 44:
    case 47:
    case 62:
    case 64:
    case 126:
    // ; { } breakpoint token
    case 59:
    case 123:
    case 125:
      return 4;
    // : accompanied token
    case 58:
      return 3;
    // " ' ( [ opening delimit token
    case 34:
    case 39:
    case 40:
    case 91:
      return 2;
    // ) ] closing delimit token
    case 41:
    case 93:
      return 1;
  }
  return 0;
}
function alloc(value) {
  return line = column = 1, length = strlen(characters = value), position2 = 0, [];
}
function dealloc(value) {
  return characters = "", value;
}
function delimit(type) {
  return trim(slice(position2 - 1, delimiter(type === 91 ? type + 2 : type === 40 ? type + 1 : type)));
}
function whitespace(type) {
  while (character = peek())
    if (character < 33)
      next();
    else
      break;
  return token(type) > 2 || token(character) > 3 ? "" : " ";
}
function escaping(index, count) {
  while (--count && next())
    if (character < 48 || character > 102 || character > 57 && character < 65 || character > 70 && character < 97)
      break;
  return slice(index, caret() + (count < 6 && peek() == 32 && next() == 32));
}
function delimiter(type) {
  while (next())
    switch (character) {
      // ] ) " '
      case type:
        return position2;
      // " '
      case 34:
      case 39:
        if (type !== 34 && type !== 39)
          delimiter(character);
        break;
      // (
      case 40:
        if (type === 41)
          delimiter(type);
        break;
      // \
      case 92:
        next();
        break;
    }
  return position2;
}
function commenter(type, index) {
  while (next())
    if (type + character === 47 + 10)
      break;
    else if (type + character === 42 + 42 && peek() === 47)
      break;
  return "/*" + slice(index, position2 - 1) + "*" + from(type === 47 ? type : next());
}
function identifier(index) {
  while (!token(peek()))
    next();
  return slice(index, position2);
}
var line, column, length, position2, character, characters;
var init_Tokenizer = __esm({
  "node_modules/stylis/src/Tokenizer.js"() {
    init_Utility();
    line = 1;
    column = 1;
    length = 0;
    position2 = 0;
    character = 0;
    characters = "";
  }
});

// node_modules/stylis/src/Parser.js
function compile(value) {
  return dealloc(parse2("", null, null, null, [""], value = alloc(value), 0, [0], value));
}
function parse2(value, root, parent, rule, rules, rulesets, pseudo, points, declarations) {
  var index = 0;
  var offset = 0;
  var length2 = pseudo;
  var atrule = 0;
  var property = 0;
  var previous = 0;
  var variable = 1;
  var scanning = 1;
  var ampersand = 1;
  var character2 = 0;
  var type = "";
  var props = rules;
  var children = rulesets;
  var reference = rule;
  var characters2 = type;
  while (scanning)
    switch (previous = character2, character2 = next()) {
      // (
      case 40:
        if (previous != 108 && charat(characters2, length2 - 1) == 58) {
          if (indexof(characters2 += replace(delimit(character2), "&", "&\f"), "&\f", abs(index ? points[index - 1] : 0)) != -1)
            ampersand = -1;
          break;
        }
      // " ' [
      case 34:
      case 39:
      case 91:
        characters2 += delimit(character2);
        break;
      // \t \n \r \s
      case 9:
      case 10:
      case 13:
      case 32:
        characters2 += whitespace(previous);
        break;
      // \
      case 92:
        characters2 += escaping(caret() - 1, 7);
        continue;
      // /
      case 47:
        switch (peek()) {
          case 42:
          case 47:
            append(comment(commenter(next(), caret()), root, parent, declarations), declarations);
            break;
          default:
            characters2 += "/";
        }
        break;
      // {
      case 123 * variable:
        points[index++] = strlen(characters2) * ampersand;
      // } ; \0
      case 125 * variable:
      case 59:
      case 0:
        switch (character2) {
          // \0 }
          case 0:
          case 125:
            scanning = 0;
          // ;
          case 59 + offset:
            if (ampersand == -1) characters2 = replace(characters2, /\f/g, "");
            if (property > 0 && strlen(characters2) - length2)
              append(property > 32 ? declaration(characters2 + ";", rule, parent, length2 - 1, declarations) : declaration(replace(characters2, " ", "") + ";", rule, parent, length2 - 2, declarations), declarations);
            break;
          // @ ;
          case 59:
            characters2 += ";";
          // { rule/at-rule
          default:
            append(reference = ruleset(characters2, root, parent, index, offset, rules, points, type, props = [], children = [], length2, rulesets), rulesets);
            if (character2 === 123)
              if (offset === 0)
                parse2(characters2, root, reference, reference, props, rulesets, length2, points, children);
              else
                switch (atrule === 99 && charat(characters2, 3) === 110 ? 100 : atrule) {
                  // d l m s
                  case 100:
                  case 108:
                  case 109:
                  case 115:
                    parse2(value, reference, reference, rule && append(ruleset(value, reference, reference, 0, 0, rules, points, type, rules, props = [], length2, children), children), rules, children, length2, points, rule ? props : children);
                    break;
                  default:
                    parse2(characters2, reference, reference, reference, [""], children, 0, points, children);
                }
        }
        index = offset = property = 0, variable = ampersand = 1, type = characters2 = "", length2 = pseudo;
        break;
      // :
      case 58:
        length2 = 1 + strlen(characters2), property = previous;
      default:
        if (variable < 1) {
          if (character2 == 123)
            --variable;
          else if (character2 == 125 && variable++ == 0 && prev() == 125)
            continue;
        }
        switch (characters2 += from(character2), character2 * variable) {
          // &
          case 38:
            ampersand = offset > 0 ? 1 : (characters2 += "\f", -1);
            break;
          // ,
          case 44:
            points[index++] = (strlen(characters2) - 1) * ampersand, ampersand = 1;
            break;
          // @
          case 64:
            if (peek() === 45)
              characters2 += delimit(next());
            atrule = peek(), offset = length2 = strlen(type = characters2 += identifier(caret())), character2++;
            break;
          // -
          case 45:
            if (previous === 45 && strlen(characters2) == 2)
              variable = 0;
        }
    }
  return rulesets;
}
function ruleset(value, root, parent, index, offset, rules, points, type, props, children, length2, siblings) {
  var post = offset - 1;
  var rule = offset === 0 ? rules : [""];
  var size2 = sizeof(rule);
  for (var i2 = 0, j2 = 0, k2 = 0; i2 < index; ++i2)
    for (var x2 = 0, y2 = substr(value, post + 1, post = abs(j2 = points[i2])), z2 = value; x2 < size2; ++x2)
      if (z2 = trim(j2 > 0 ? rule[x2] + " " + y2 : replace(y2, /&\f/g, rule[x2])))
        props[k2++] = z2;
  return node(value, root, parent, offset === 0 ? RULESET : type, props, children, length2, siblings);
}
function comment(value, root, parent, siblings) {
  return node(value, root, parent, COMMENT, from(char()), substr(value, 2, -2), 0, siblings);
}
function declaration(value, root, parent, length2, siblings) {
  return node(value, root, parent, DECLARATION, substr(value, 0, length2), substr(value, length2 + 1, -1), length2, siblings);
}
var init_Parser = __esm({
  "node_modules/stylis/src/Parser.js"() {
    init_Enum();
    init_Utility();
    init_Tokenizer();
  }
});

// node_modules/stylis/src/Prefixer.js
function prefix(value, length2, children) {
  switch (hash(value, length2)) {
    // color-adjust
    case 5103:
      return WEBKIT + "print-" + value + value;
    // animation, animation-(delay|direction|duration|fill-mode|iteration-count|name|play-state|timing-function)
    case 5737:
    case 4201:
    case 3177:
    case 3433:
    case 1641:
    case 4457:
    case 2921:
    // text-decoration, filter, clip-path, backface-visibility, column, box-decoration-break
    case 5572:
    case 6356:
    case 5844:
    case 3191:
    case 6645:
    case 3005:
    // mask, mask-image, mask-(mode|clip|size), mask-(repeat|origin), mask-position, mask-composite,
    case 6391:
    case 5879:
    case 5623:
    case 6135:
    case 4599:
    case 4855:
    // background-clip, columns, column-(count|fill|gap|rule|rule-color|rule-style|rule-width|span|width)
    case 4215:
    case 6389:
    case 5109:
    case 5365:
    case 5621:
    case 3829:
      return WEBKIT + value + value;
    // tab-size
    case 4789:
      return MOZ + value + value;
    // appearance, user-select, transform, hyphens, text-size-adjust
    case 5349:
    case 4246:
    case 4810:
    case 6968:
    case 2756:
      return WEBKIT + value + MOZ + value + MS + value + value;
    // writing-mode
    case 5936:
      switch (charat(value, length2 + 11)) {
        // vertical-l(r)
        case 114:
          return WEBKIT + value + MS + replace(value, /[svh]\w+-[tblr]{2}/, "tb") + value;
        // vertical-r(l)
        case 108:
          return WEBKIT + value + MS + replace(value, /[svh]\w+-[tblr]{2}/, "tb-rl") + value;
        // horizontal(-)tb
        case 45:
          return WEBKIT + value + MS + replace(value, /[svh]\w+-[tblr]{2}/, "lr") + value;
      }
    // flex, flex-direction, scroll-snap-type, writing-mode
    case 6828:
    case 4268:
    case 2903:
      return WEBKIT + value + MS + value + value;
    // order
    case 6165:
      return WEBKIT + value + MS + "flex-" + value + value;
    // align-items
    case 5187:
      return WEBKIT + value + replace(value, /(\w+).+(:[^]+)/, WEBKIT + "box-$1$2" + MS + "flex-$1$2") + value;
    // align-self
    case 5443:
      return WEBKIT + value + MS + "flex-item-" + replace(value, /flex-|-self/g, "") + (!match(value, /flex-|baseline/) ? MS + "grid-row-" + replace(value, /flex-|-self/g, "") : "") + value;
    // align-content
    case 4675:
      return WEBKIT + value + MS + "flex-line-pack" + replace(value, /align-content|flex-|-self/g, "") + value;
    // flex-shrink
    case 5548:
      return WEBKIT + value + MS + replace(value, "shrink", "negative") + value;
    // flex-basis
    case 5292:
      return WEBKIT + value + MS + replace(value, "basis", "preferred-size") + value;
    // flex-grow
    case 6060:
      return WEBKIT + "box-" + replace(value, "-grow", "") + WEBKIT + value + MS + replace(value, "grow", "positive") + value;
    // transition
    case 4554:
      return WEBKIT + replace(value, /([^-])(transform)/g, "$1" + WEBKIT + "$2") + value;
    // cursor
    case 6187:
      return replace(replace(replace(value, /(zoom-|grab)/, WEBKIT + "$1"), /(image-set)/, WEBKIT + "$1"), value, "") + value;
    // background, background-image
    case 5495:
    case 3959:
      return replace(value, /(image-set\([^]*)/, WEBKIT + "$1$`$1");
    // justify-content
    case 4968:
      return replace(replace(value, /(.+:)(flex-)?(.*)/, WEBKIT + "box-pack:$3" + MS + "flex-pack:$3"), /s.+-b[^;]+/, "justify") + WEBKIT + value + value;
    // justify-self
    case 4200:
      if (!match(value, /flex-|baseline/)) return MS + "grid-column-align" + substr(value, length2) + value;
      break;
    // grid-template-(columns|rows)
    case 2592:
    case 3360:
      return MS + replace(value, "template-", "") + value;
    // grid-(row|column)-start
    case 4384:
    case 3616:
      if (children && children.some(function(element, index) {
        return length2 = index, match(element.props, /grid-\w+-end/);
      })) {
        return ~indexof(value + (children = children[length2].value), "span", 0) ? value : MS + replace(value, "-start", "") + value + MS + "grid-row-span:" + (~indexof(children, "span", 0) ? match(children, /\d+/) : +match(children, /\d+/) - +match(value, /\d+/)) + ";";
      }
      return MS + replace(value, "-start", "") + value;
    // grid-(row|column)-end
    case 4896:
    case 4128:
      return children && children.some(function(element) {
        return match(element.props, /grid-\w+-start/);
      }) ? value : MS + replace(replace(value, "-end", "-span"), "span ", "") + value;
    // (margin|padding)-inline-(start|end)
    case 4095:
    case 3583:
    case 4068:
    case 2532:
      return replace(value, /(.+)-inline(.+)/, WEBKIT + "$1$2") + value;
    // (min|max)?(width|height|inline-size|block-size)
    case 8116:
    case 7059:
    case 5753:
    case 5535:
    case 5445:
    case 5701:
    case 4933:
    case 4677:
    case 5533:
    case 5789:
    case 5021:
    case 4765:
      if (strlen(value) - 1 - length2 > 6)
        switch (charat(value, length2 + 1)) {
          // (m)ax-content, (m)in-content
          case 109:
            if (charat(value, length2 + 4) !== 45)
              break;
          // (f)ill-available, (f)it-content
          case 102:
            return replace(value, /(.+:)(.+)-([^]+)/, "$1" + WEBKIT + "$2-$3$1" + MOZ + (charat(value, length2 + 3) == 108 ? "$3" : "$2-$3")) + value;
          // (s)tretch
          case 115:
            return ~indexof(value, "stretch", 0) ? prefix(replace(value, "stretch", "fill-available"), length2, children) + value : value;
        }
      break;
    // grid-(column|row)
    case 5152:
    case 5920:
      return replace(value, /(.+?):(\d+)(\s*\/\s*(span)?\s*(\d+))?(.*)/, function(_15, a2, b2, c2, d, e, f2) {
        return MS + a2 + ":" + b2 + f2 + (c2 ? MS + a2 + "-span:" + (d ? e : +e - +b2) + f2 : "") + value;
      });
    // position: sticky
    case 4949:
      if (charat(value, length2 + 6) === 121)
        return replace(value, ":", ":" + WEBKIT) + value;
      break;
    // display: (flex|inline-flex|grid|inline-grid)
    case 6444:
      switch (charat(value, charat(value, 14) === 45 ? 18 : 11)) {
        // (inline-)?fle(x)
        case 120:
          return replace(value, /(.+:)([^;\s!]+)(;|(\s+)?!.+)?/, "$1" + WEBKIT + (charat(value, 14) === 45 ? "inline-" : "") + "box$3$1" + WEBKIT + "$2$3$1" + MS + "$2box$3") + value;
        // (inline-)?gri(d)
        case 100:
          return replace(value, ":", ":" + MS) + value;
      }
      break;
    // scroll-margin, scroll-margin-(top|right|bottom|left)
    case 5719:
    case 2647:
    case 2135:
    case 3927:
    case 2391:
      return replace(value, "scroll-", "scroll-snap-") + value;
  }
  return value;
}
var init_Prefixer = __esm({
  "node_modules/stylis/src/Prefixer.js"() {
    init_Enum();
    init_Utility();
  }
});

// node_modules/stylis/src/Serializer.js
function serialize(children, callback) {
  var output = "";
  for (var i2 = 0; i2 < children.length; i2++)
    output += callback(children[i2], i2, children, callback) || "";
  return output;
}
function stringify4(element, index, children, callback) {
  switch (element.type) {
    case LAYER:
      if (element.children.length) break;
    case IMPORT:
    case DECLARATION:
      return element.return = element.return || element.value;
    case COMMENT:
      return "";
    case KEYFRAMES:
      return element.return = element.value + "{" + serialize(element.children, callback) + "}";
    case RULESET:
      if (!strlen(element.value = element.props.join(","))) return "";
  }
  return strlen(children = serialize(element.children, callback)) ? element.return = element.value + "{" + children + "}" : "";
}
var init_Serializer = __esm({
  "node_modules/stylis/src/Serializer.js"() {
    init_Enum();
    init_Utility();
  }
});

// node_modules/stylis/src/Middleware.js
function middleware(collection) {
  var length2 = sizeof(collection);
  return function(element, index, children, callback) {
    var output = "";
    for (var i2 = 0; i2 < length2; i2++)
      output += collection[i2](element, index, children, callback) || "";
    return output;
  };
}
function rulesheet(callback) {
  return function(element) {
    if (!element.root) {
      if (element = element.return)
        callback(element);
    }
  };
}
function prefixer(element, index, children, callback) {
  if (element.length > -1) {
    if (!element.return)
      switch (element.type) {
        case DECLARATION:
          element.return = prefix(element.value, element.length, children);
          return;
        case KEYFRAMES:
          return serialize([copy(element, { value: replace(element.value, "@", "@" + WEBKIT) })], callback);
        case RULESET:
          if (element.length)
            return combine(children = element.props, function(value) {
              switch (match(value, callback = /(::plac\w+|:read-\w+)/)) {
                // :read-(only|write)
                case ":read-only":
                case ":read-write":
                  lift(copy(element, { props: [replace(value, /:(read-\w+)/, ":" + MOZ + "$1")] }));
                  lift(copy(element, { props: [value] }));
                  assign(element, { props: filter(children, callback) });
                  break;
                // :placeholder
                case "::placeholder":
                  lift(copy(element, { props: [replace(value, /:(plac\w+)/, ":" + WEBKIT + "input-$1")] }));
                  lift(copy(element, { props: [replace(value, /:(plac\w+)/, ":" + MOZ + "$1")] }));
                  lift(copy(element, { props: [replace(value, /:(plac\w+)/, MS + "input-$1")] }));
                  lift(copy(element, { props: [value] }));
                  assign(element, { props: filter(children, callback) });
                  break;
              }
              return "";
            });
      }
  }
}
var init_Middleware = __esm({
  "node_modules/stylis/src/Middleware.js"() {
    init_Enum();
    init_Utility();
    init_Tokenizer();
    init_Serializer();
    init_Prefixer();
  }
});

// node_modules/stylis/index.js
var init_stylis = __esm({
  "node_modules/stylis/index.js"() {
    init_Enum();
    init_Utility();
    init_Parser();
    init_Prefixer();
    init_Tokenizer();
    init_Serializer();
    init_Middleware();
  }
});

// node_modules/@emotion/unitless/dist/emotion-unitless.esm.js
var unitlessKeys;
var init_emotion_unitless_esm = __esm({
  "node_modules/@emotion/unitless/dist/emotion-unitless.esm.js"() {
    unitlessKeys = {
      animationIterationCount: 1,
      aspectRatio: 1,
      borderImageOutset: 1,
      borderImageSlice: 1,
      borderImageWidth: 1,
      boxFlex: 1,
      boxFlexGroup: 1,
      boxOrdinalGroup: 1,
      columnCount: 1,
      columns: 1,
      flex: 1,
      flexGrow: 1,
      flexPositive: 1,
      flexShrink: 1,
      flexNegative: 1,
      flexOrder: 1,
      gridRow: 1,
      gridRowEnd: 1,
      gridRowSpan: 1,
      gridRowStart: 1,
      gridColumn: 1,
      gridColumnEnd: 1,
      gridColumnSpan: 1,
      gridColumnStart: 1,
      msGridRow: 1,
      msGridRowSpan: 1,
      msGridColumn: 1,
      msGridColumnSpan: 1,
      fontWeight: 1,
      lineHeight: 1,
      opacity: 1,
      order: 1,
      orphans: 1,
      tabSize: 1,
      widows: 1,
      zIndex: 1,
      zoom: 1,
      WebkitLineClamp: 1,
      // SVG-related properties
      fillOpacity: 1,
      floodOpacity: 1,
      stopOpacity: 1,
      strokeDasharray: 1,
      strokeDashoffset: 1,
      strokeMiterlimit: 1,
      strokeOpacity: 1,
      strokeWidth: 1
    };
  }
});

// node_modules/styled-components/dist/styled-components.browser.esm.js
var styled_components_browser_esm_exports = {};
__export(styled_components_browser_esm_exports, {
  ServerStyleSheet: () => vt,
  StyleSheetConsumer: () => Be,
  StyleSheetContext: () => $e,
  StyleSheetManager: () => Ye,
  ThemeConsumer: () => tt,
  ThemeContext: () => et,
  ThemeProvider: () => ot,
  __PRIVATE__: () => gt,
  createGlobalStyle: () => ft,
  css: () => lt,
  default: () => dt,
  isStyledComponent: () => se,
  keyframes: () => mt,
  styled: () => dt,
  useTheme: () => nt,
  version: () => v,
  withTheme: () => yt
});
function I(e, t, n) {
  return void 0 === n && (n = C), e.theme !== n.theme && e.theme || t || n.theme;
}
function R(e) {
  return e.replace(O, "-").replace(D, "");
}
function x(e) {
  var t, n = "";
  for (t = Math.abs(e); t > k; t = t / k | 0) n = j(t % k) + n;
  return (j(t % k) + n).replace(T, "$1-$2");
}
function $(e) {
  return x(z(e) >>> 0);
}
function B(e) {
  return "string" == typeof e && e || e.displayName || e.name || "Component";
}
function L(e) {
  return "string" == typeof e && e.charAt(0) === e.charAt(0).toLowerCase();
}
function X(e) {
  return ("type" in (t = e) && t.type.$$typeof) === Y ? U : "$$typeof" in e ? J[e.$$typeof] : q;
  var t;
}
function oe(e, t, n) {
  if ("string" != typeof t) {
    if (ne) {
      var o2 = te(t);
      o2 && o2 !== ne && oe(e, o2, n);
    }
    var r2 = K(t);
    Q && (r2 = r2.concat(Q(t)));
    for (var s2 = X(e), i2 = X(t), a2 = 0; a2 < r2.length; ++a2) {
      var c2 = r2[a2];
      if (!(c2 in H || n && n[c2] || i2 && c2 in i2 || s2 && c2 in s2)) {
        var l2 = ee(t, c2);
        try {
          Z(e, c2, l2);
        } catch (e2) {
        }
      }
    }
  }
  return e;
}
function re(e) {
  return "function" == typeof e;
}
function se(e) {
  return "object" == typeof e && "styledComponentId" in e;
}
function ie(e, t) {
  return e && t ? "".concat(e, " ").concat(t) : e || t || "";
}
function ae(e, t) {
  if (0 === e.length) return "";
  for (var n = e[0], o2 = 1; o2 < e.length; o2++) n += t ? t + e[o2] : e[o2];
  return n;
}
function ce(e) {
  return null !== e && "object" == typeof e && e.constructor.name === Object.name && !("props" in e && e.$$typeof);
}
function le(e, t, n) {
  if (void 0 === n && (n = false), !n && !ce(e) && !Array.isArray(e)) return t;
  if (Array.isArray(t)) for (var o2 = 0; o2 < t.length; o2++) e[o2] = le(e[o2], t[o2]);
  else if (ce(t)) for (var o2 in t) e[o2] = le(e[o2], t[o2]);
  return e;
}
function ue(e, t) {
  Object.defineProperty(e, "toString", { value: t });
}
function de() {
  for (var e = [], t = 0; t < arguments.length; t++) e[t] = arguments[t];
  for (var n = e[0], o2 = [], r2 = 1, s2 = e.length; r2 < s2; r2 += 1) o2.push(e[r2]);
  return o2.forEach(function(e2) {
    n = n.replace(/%[a-z]/, e2);
  }), n;
}
function he(t) {
  for (var n = [], o2 = 1; o2 < arguments.length; o2++) n[o2 - 1] = arguments[o2];
  return false ? new Error("An error occurred. See https://github.com/styled-components/styled-components/blob/main/packages/styled-components/src/utils/errors.md#".concat(t, " for more information.").concat(n.length > 0 ? " Args: ".concat(n.join(", ")) : "")) : new Error(de.apply(void 0, __spreadArray([pe[t]], n, false)).trim());
}
function Ce() {
  return "undefined" != typeof __webpack_nonce__ ? __webpack_nonce__ : null;
}
function Ve(e, t) {
  return e.map(function(e2) {
    return "rule" === e2.type && (e2.value = "".concat(t, " ").concat(e2.value), e2.value = e2.value.replaceAll(",", ",".concat(t, " ")), e2.props = e2.props.map(function(e3) {
      return "".concat(t, " ").concat(e3);
    })), Array.isArray(e2.children) && "@keyframes" !== e2.type && (e2.children = Ve(e2.children, t)), e2;
  });
}
function Fe(e) {
  var t, n, o2, r2 = void 0 === e ? C : e, s2 = r2.options, i2 = void 0 === s2 ? C : s2, a2 = r2.plugins, c2 = void 0 === a2 ? _ : a2, l2 = function(e2, o3, r3) {
    return r3.startsWith(n) && r3.endsWith(n) && r3.replaceAll(n, "").length > 0 ? ".".concat(t) : e2;
  }, u2 = c2.slice();
  u2.push(function(e2) {
    e2.type === RULESET && e2.value.includes("&") && (e2.props[0] = e2.props[0].replace(je, n).replace(o2, l2));
  }), i2.prefix && u2.push(prefixer), u2.push(stringify4);
  var p2 = function(e2, r3, s3, a3) {
    void 0 === r3 && (r3 = ""), void 0 === s3 && (s3 = ""), void 0 === a3 && (a3 = "&"), t = a3, n = r3, o2 = new RegExp("\\".concat(n, "\\b"), "g");
    var c3 = e2.replace(xe, ""), l3 = compile(s3 || r3 ? "".concat(s3, " ").concat(r3, " { ").concat(c3, " }") : c3);
    i2.namespace && (l3 = Ve(l3, i2.namespace));
    var p3 = [];
    return serialize(l3, middleware(u2.concat(rulesheet(function(e3) {
      return p3.push(e3);
    })))), p3;
  };
  return p2.hash = c2.length ? c2.reduce(function(e2, t2) {
    return t2.name || he(15), M(e2, t2.name);
  }, F).toString() : "", p2;
}
function Ge() {
  return (0, import_react.useContext)($e);
}
function Ye(e) {
  var t = (0, import_react.useState)(e.stylisPlugins), n = t[0], r2 = t[1], c2 = Ge().styleSheet, l2 = (0, import_react.useMemo)(function() {
    var t2 = c2;
    return e.sheet ? t2 = e.sheet : e.target && (t2 = t2.reconstructWithOptions({ target: e.target }, false)), e.disableCSSOMInjection && (t2 = t2.reconstructWithOptions({ useCSSOMInjection: false })), t2;
  }, [e.disableCSSOMInjection, e.sheet, e.target, c2]), u2 = (0, import_react.useMemo)(function() {
    return Fe({ options: { namespace: e.namespace, prefix: e.enableVendorPrefixes }, plugins: n });
  }, [e.enableVendorPrefixes, e.namespace, n]);
  (0, import_react.useEffect)(function() {
    (0, import_shallowequal.default)(n, e.stylisPlugins) || r2(e.stylisPlugins);
  }, [e.stylisPlugins]);
  var d = (0, import_react.useMemo)(function() {
    return { shouldForwardProp: e.shouldForwardProp, styleSheet: l2, stylis: u2 };
  }, [e.shouldForwardProp, l2, u2]);
  return import_react.default.createElement($e.Provider, { value: d }, import_react.default.createElement(Le.Provider, { value: u2 }, e.children));
}
function He(e) {
  for (var t = "", n = 0; n < e.length; n++) {
    var o2 = e[n];
    if (1 === n && "-" === o2 && "-" === e[0]) return e;
    qe(o2) ? t += "-" + o2.toLowerCase() : t += o2;
  }
  return t.startsWith("ms-") ? "-" + t : t;
}
function Xe(e, t, n, o2) {
  if (Ue(e)) return [];
  if (se(e)) return [".".concat(e.styledComponentId)];
  if (re(e)) {
    if (!re(s2 = e) || s2.prototype && s2.prototype.isReactComponent || !t) return [e];
    var r2 = e(t);
    return "object" != typeof r2 || Array.isArray(r2) || r2 instanceof We || ce(r2) || null === r2 || console.error("".concat(B(e), " is not a styled component and cannot be referred to via component selector. See https://www.styled-components.com/docs/advanced#referring-to-other-components for more details.")), Xe(r2, t, n, o2);
  }
  var s2;
  return e instanceof We ? n ? (e.inject(n, o2), [e.getName(o2)]) : [e] : ce(e) ? Je(e) : Array.isArray(e) ? Array.prototype.concat.apply(_, e.map(function(e2) {
    return Xe(e2, t, n, o2);
  })) : [e.toString()];
}
function Ze(e) {
  for (var t = 0; t < e.length; t += 1) {
    var n = e[t];
    if (re(n) && !se(n)) return false;
  }
  return true;
}
function nt() {
  var e = (0, import_react.useContext)(et);
  if (!e) throw he(18);
  return e;
}
function ot(e) {
  var n = import_react.default.useContext(et), r2 = (0, import_react.useMemo)(function() {
    return (function(e2, n2) {
      if (!e2) throw he(14);
      if (re(e2)) {
        var o2 = e2(n2);
        if (null === o2 || Array.isArray(o2) || "object" != typeof o2) throw he(7);
        return o2;
      }
      if (Array.isArray(e2) || "object" != typeof e2) throw he(8);
      return n2 ? __assign(__assign({}, n2), e2) : e2;
    })(e.theme, n);
  }, [e.theme, n]);
  return e.children ? import_react.default.createElement(et.Provider, { value: r2 }, e.children) : null;
}
function it(e, r2, s2) {
  var i2 = se(e), a2 = e, c2 = !L(e), p2 = r2.attrs, d = void 0 === p2 ? _ : p2, h = r2.componentId, f2 = void 0 === h ? (function(e2, t) {
    var n = "string" != typeof e2 ? "sc" : R(e2);
    rt[n] = (rt[n] || 0) + 1;
    var o2 = "".concat(n, "-").concat($(v + n + rt[n]));
    return t ? "".concat(t, "-").concat(o2) : o2;
  })(r2.displayName, r2.parentComponentId) : h, m2 = r2.displayName, y2 = void 0 === m2 ? (function(e2) {
    return L(e2) ? "styled.".concat(e2) : "Styled(".concat(B(e2), ")");
  })(e) : m2, g2 = r2.displayName && r2.componentId ? "".concat(R(r2.displayName), "-").concat(r2.componentId) : r2.componentId || f2, S2 = i2 && a2.attrs ? a2.attrs.concat(d).filter(Boolean) : d, w2 = r2.shouldForwardProp;
  if (i2 && a2.shouldForwardProp) {
    var b2 = a2.shouldForwardProp;
    if (r2.shouldForwardProp) {
      var E2 = r2.shouldForwardProp;
      w2 = function(e2, t) {
        return b2(e2, t) && E2(e2, t);
      };
    } else w2 = b2;
  }
  var N2 = new Qe(s2, g2, i2 ? a2.componentStyle : void 0);
  function O2(e2, r3) {
    return (function(e3, r4, s3) {
      var i3 = e3.attrs, a3 = e3.componentStyle, c3 = e3.defaultProps, p3 = e3.foldedComponentIds, d2 = e3.styledComponentId, h2 = e3.target, f3 = import_react.default.useContext(et), m3 = Ge(), y3 = e3.shouldForwardProp || m3.shouldForwardProp;
      (0, import_react.useDebugValue)(d2);
      var v2 = I(r4, f3, c3) || C, g3 = (function(e4, n, o2) {
        for (var r5, s4 = __assign(__assign({}, n), { className: void 0, theme: o2 }), i4 = 0; i4 < e4.length; i4 += 1) {
          var a4 = re(r5 = e4[i4]) ? r5(s4) : r5;
          for (var c4 in a4) s4[c4] = "className" === c4 ? ie(s4[c4], a4[c4]) : "style" === c4 ? __assign(__assign({}, s4[c4]), a4[c4]) : a4[c4];
        }
        return n.className && (s4.className = ie(s4.className, n.className)), s4;
      })(i3, r4, v2), S3 = g3.as || h2, w3 = {};
      for (var b3 in g3) void 0 === g3[b3] || "$" === b3[0] || "as" === b3 || "theme" === b3 && g3.theme === v2 || ("forwardedAs" === b3 ? w3.as = g3.forwardedAs : y3 && !y3(b3, S3) || (w3[b3] = g3[b3], y3 || false || isPropValid(b3) || st.has(b3) || !A.has(S3) || (st.add(b3), console.warn('styled-components: it looks like an unknown prop "'.concat(b3, '" is being sent through to the DOM, which will likely trigger a React console error. If you would like automatic filtering of unknown props, you can opt-into that behavior via `<StyleSheetManager shouldForwardProp={...}>` (connect an API like `@emotion/is-prop-valid`) or consider using transient props (`$` prefix for automatic filtering.)')))));
      var E3 = (function(e4, t) {
        var n = Ge(), o2 = e4.generateAndInjectStyles(t, n.styleSheet, n.stylis);
        return (0, import_react.useDebugValue)(o2), o2;
      })(a3, g3);
      e3.warnTooManyClasses && e3.warnTooManyClasses(E3);
      var N3 = ie(p3, d2);
      return E3 && (N3 += " " + E3), g3.className && (N3 += " " + g3.className), w3[L(S3) && !A.has(S3) ? "class" : "className"] = N3, s3 && (w3.ref = s3), (0, import_react.createElement)(S3, w3);
    })(D2, e2, r3);
  }
  O2.displayName = y2;
  var D2 = import_react.default.forwardRef(O2);
  return D2.attrs = S2, D2.componentStyle = N2, D2.displayName = y2, D2.shouldForwardProp = w2, D2.foldedComponentIds = i2 ? ie(a2.foldedComponentIds, a2.styledComponentId) : "", D2.styledComponentId = g2, D2.target = i2 ? a2.target : e, Object.defineProperty(D2, "defaultProps", { get: function() {
    return this._foldedDefaultProps;
  }, set: function(e2) {
    this._foldedDefaultProps = i2 ? (function(e3) {
      for (var t = [], n = 1; n < arguments.length; n++) t[n - 1] = arguments[n];
      for (var o2 = 0, r3 = t; o2 < r3.length; o2++) le(e3, r3[o2], true);
      return e3;
    })({}, a2.defaultProps, e2) : e2;
  } }), P(y2, g2), D2.warnTooManyClasses = /* @__PURE__ */ (function(e2, t) {
    var n = {}, o2 = false;
    return function(r3) {
      if (!o2 && (n[r3] = true, Object.keys(n).length >= 200)) {
        var s3 = t ? ' with the id of "'.concat(t, '"') : "";
        console.warn("Over ".concat(200, " classes were generated for component ").concat(e2).concat(s3, ".\n") + "Consider using the attrs method, together with a style object for frequently changed styles.\nExample:\n  const Component = styled.div.attrs(props => ({\n    style: {\n      background: props.background,\n    },\n  }))`width: 100%;`\n\n  <Component />"), o2 = true, n = {};
      }
    };
  })(y2, g2), ue(D2, function() {
    return ".".concat(D2.styledComponentId);
  }), c2 && oe(D2, e, { attrs: true, componentStyle: true, displayName: true, foldedComponentIds: true, shouldForwardProp: true, styledComponentId: true, target: true }), D2;
}
function at(e, t) {
  for (var n = [e[0]], o2 = 0, r2 = t.length; o2 < r2; o2 += 1) n.push(t[o2], e[o2 + 1]);
  return n;
}
function lt(t) {
  for (var n = [], o2 = 1; o2 < arguments.length; o2++) n[o2 - 1] = arguments[o2];
  if (re(t) || ce(t)) return ct(Xe(at(_, __spreadArray([t], n, true))));
  var r2 = t;
  return 0 === n.length && 1 === r2.length && "string" == typeof r2[0] ? Xe(r2) : ct(Xe(at(r2, n)));
}
function ut(n, o2, r2) {
  if (void 0 === r2 && (r2 = C), !o2) throw he(1, o2);
  var s2 = function(t) {
    for (var s3 = [], i2 = 1; i2 < arguments.length; i2++) s3[i2 - 1] = arguments[i2];
    return n(o2, r2, lt.apply(void 0, __spreadArray([t], s3, false)));
  };
  return s2.attrs = function(e) {
    return ut(n, o2, __assign(__assign({}, r2), { attrs: Array.prototype.concat(r2.attrs, e).filter(Boolean) }));
  }, s2.withConfig = function(e) {
    return ut(n, o2, __assign(__assign({}, r2), e));
  }, s2;
}
function ft(n) {
  for (var r2 = [], s2 = 1; s2 < arguments.length; s2++) r2[s2 - 1] = arguments[s2];
  var i2 = lt.apply(void 0, __spreadArray([n], r2, false)), a2 = "sc-global-".concat($(JSON.stringify(i2))), c2 = new ht(i2, a2);
  P(a2);
  var l2 = function(e) {
    var t = Ge(), n2 = import_react.default.useContext(et), r3 = import_react.default.useRef(t.styleSheet.allocateGSInstance(a2)).current;
    return import_react.default.Children.count(e.children) && console.warn("The global style component ".concat(a2, " was given child JSX. createGlobalStyle does not render children.")), i2.some(function(e2) {
      return "string" == typeof e2 && -1 !== e2.indexOf("@import");
    }) && console.warn("Please do not use @import CSS syntax in createGlobalStyle at this time, as the CSSOM APIs we use in production do not handle it well. Instead, we recommend using a library such as react-helmet to inject a typical <link> meta tag to the stylesheet, or simply embedding it manually in your index.html <head> section for a simpler app."), t.styleSheet.server && u2(r3, e, t.styleSheet, n2, t.stylis), import_react.default.useLayoutEffect(function() {
      if (!t.styleSheet.server) return u2(r3, e, t.styleSheet, n2, t.stylis), function() {
        return c2.removeStyles(r3, t.styleSheet);
      };
    }, [r3, e, t.styleSheet, n2, t.stylis]), null;
  };
  function u2(e, n2, o2, r3, s3) {
    if (c2.isStatic) c2.renderStyles(e, b, o2, s3);
    else {
      var i3 = __assign(__assign({}, n2), { theme: I(n2, r3, l2.defaultProps) });
      c2.renderStyles(e, i3, o2, s3);
    }
  }
  return import_react.default.memo(l2);
}
function mt(t) {
  for (var n = [], o2 = 1; o2 < arguments.length; o2++) n[o2 - 1] = arguments[o2];
  "undefined" != typeof navigator && "ReactNative" === navigator.product && console.warn("`keyframes` cannot be used on ReactNative, only on the web. To do animation in ReactNative please use Animated.");
  var r2 = ae(lt.apply(void 0, __spreadArray([t], n, false))), s2 = $(r2);
  return new We(s2, r2);
}
function yt(e) {
  var n = import_react.default.forwardRef(function(n2, r2) {
    var s2 = I(n2, import_react.default.useContext(et), e.defaultProps);
    return void 0 === s2 && console.warn('[withTheme] You are not using a ThemeProvider nor passing a theme prop or a theme in defaultProps in component class "'.concat(B(e), '"')), import_react.default.createElement(e, __assign({}, n2, { theme: s2, ref: r2 }));
  });
  return n.displayName = "WithTheme(".concat(B(e), ")"), oe(n, e);
}
var import_react, import_shallowequal, f, m, y, v, g, S, w, b, E, N, P, _, C, A, O, D, T, k, j, V, F, M, z, G, Y, W, q, H, U, J, Z, K, Q, ee, te, ne, pe, fe, me, ye, ve, ge, Se, we, be, Ee, Ne, Pe, _e, Ie, Ae, Oe, De, Re, Te, ke, je, xe, Me, ze, $e, Be, Le, We, qe, Ue, Je, Ke, Qe, et, tt, rt, st, ct, pt, dt, ht, vt, gt, St;
var init_styled_components_browser_esm = __esm({
  "node_modules/styled-components/dist/styled-components.browser.esm.js"() {
    init_tslib_es6();
    init_emotion_is_prop_valid_esm();
    import_react = __toESM(require_react());
    import_shallowequal = __toESM(require_shallowequal());
    init_stylis();
    init_emotion_unitless_esm();
    f = "undefined" != typeof process && void 0 !== process.env && (process.env.REACT_APP_SC_ATTR || process.env.SC_ATTR) || "data-styled";
    m = "active";
    y = "data-styled-version";
    v = "6.1.19";
    g = "/*!sc*/\n";
    S = "undefined" != typeof window && "undefined" != typeof document;
    w = Boolean("boolean" == typeof SC_DISABLE_SPEEDY ? SC_DISABLE_SPEEDY : "undefined" != typeof process && void 0 !== process.env && void 0 !== process.env.REACT_APP_SC_DISABLE_SPEEDY && "" !== process.env.REACT_APP_SC_DISABLE_SPEEDY ? "false" !== process.env.REACT_APP_SC_DISABLE_SPEEDY && process.env.REACT_APP_SC_DISABLE_SPEEDY : "undefined" != typeof process && void 0 !== process.env && void 0 !== process.env.SC_DISABLE_SPEEDY && "" !== process.env.SC_DISABLE_SPEEDY ? "false" !== process.env.SC_DISABLE_SPEEDY && process.env.SC_DISABLE_SPEEDY : true);
    b = {};
    E = /invalid hook call/i;
    N = /* @__PURE__ */ new Set();
    P = function(t, n) {
      if (true) {
        var o2 = n ? ' with the id of "'.concat(n, '"') : "", s2 = "The component ".concat(t).concat(o2, " has been created dynamically.\n") + "You may see this warning because you've called styled inside another component.\nTo resolve this only create new StyledComponents outside of any render method and function component.\nSee https://styled-components.com/docs/basics#define-styled-components-outside-of-the-render-method for more info.\n", i2 = console.error;
        try {
          var a2 = true;
          console.error = function(t2) {
            for (var n2 = [], o3 = 1; o3 < arguments.length; o3++) n2[o3 - 1] = arguments[o3];
            E.test(t2) ? (a2 = false, N.delete(s2)) : i2.apply(void 0, __spreadArray([t2], n2, false));
          }, (0, import_react.useRef)(), a2 && !N.has(s2) && (console.warn(s2), N.add(s2));
        } catch (e) {
          E.test(e.message) && N.delete(s2);
        } finally {
          console.error = i2;
        }
      }
    };
    _ = Object.freeze([]);
    C = Object.freeze({});
    A = /* @__PURE__ */ new Set(["a", "abbr", "address", "area", "article", "aside", "audio", "b", "base", "bdi", "bdo", "big", "blockquote", "body", "br", "button", "canvas", "caption", "cite", "code", "col", "colgroup", "data", "datalist", "dd", "del", "details", "dfn", "dialog", "div", "dl", "dt", "em", "embed", "fieldset", "figcaption", "figure", "footer", "form", "h1", "h2", "h3", "h4", "h5", "h6", "header", "hgroup", "hr", "html", "i", "iframe", "img", "input", "ins", "kbd", "keygen", "label", "legend", "li", "link", "main", "map", "mark", "menu", "menuitem", "meta", "meter", "nav", "noscript", "object", "ol", "optgroup", "option", "output", "p", "param", "picture", "pre", "progress", "q", "rp", "rt", "ruby", "s", "samp", "script", "section", "select", "small", "source", "span", "strong", "style", "sub", "summary", "sup", "table", "tbody", "td", "textarea", "tfoot", "th", "thead", "time", "tr", "track", "u", "ul", "use", "var", "video", "wbr", "circle", "clipPath", "defs", "ellipse", "foreignObject", "g", "image", "line", "linearGradient", "marker", "mask", "path", "pattern", "polygon", "polyline", "radialGradient", "rect", "stop", "svg", "text", "tspan"]);
    O = /[!"#$%&'()*+,./:;<=>?@[\\\]^`{|}~-]+/g;
    D = /(^-|-$)/g;
    T = /(a)(d)/gi;
    k = 52;
    j = function(e) {
      return String.fromCharCode(e + (e > 25 ? 39 : 97));
    };
    F = 5381;
    M = function(e, t) {
      for (var n = t.length; n; ) e = 33 * e ^ t.charCodeAt(--n);
      return e;
    };
    z = function(e) {
      return M(F, e);
    };
    G = "function" == typeof Symbol && Symbol.for;
    Y = G ? Symbol.for("react.memo") : 60115;
    W = G ? Symbol.for("react.forward_ref") : 60112;
    q = { childContextTypes: true, contextType: true, contextTypes: true, defaultProps: true, displayName: true, getDefaultProps: true, getDerivedStateFromError: true, getDerivedStateFromProps: true, mixins: true, propTypes: true, type: true };
    H = { name: true, length: true, prototype: true, caller: true, callee: true, arguments: true, arity: true };
    U = { $$typeof: true, compare: true, defaultProps: true, displayName: true, propTypes: true, type: true };
    J = ((V = {})[W] = { $$typeof: true, render: true, defaultProps: true, displayName: true, propTypes: true }, V[Y] = U, V);
    Z = Object.defineProperty;
    K = Object.getOwnPropertyNames;
    Q = Object.getOwnPropertySymbols;
    ee = Object.getOwnPropertyDescriptor;
    te = Object.getPrototypeOf;
    ne = Object.prototype;
    pe = true ? { 1: "Cannot create styled-component for component: %s.\n\n", 2: "Can't collect styles once you've consumed a `ServerStyleSheet`'s styles! `ServerStyleSheet` is a one off instance for each server-side render cycle.\n\n- Are you trying to reuse it across renders?\n- Are you accidentally calling collectStyles twice?\n\n", 3: "Streaming SSR is only supported in a Node.js environment; Please do not try to call this method in the browser.\n\n", 4: "The `StyleSheetManager` expects a valid target or sheet prop!\n\n- Does this error occur on the client and is your target falsy?\n- Does this error occur on the server and is the sheet falsy?\n\n", 5: "The clone method cannot be used on the client!\n\n- Are you running in a client-like environment on the server?\n- Are you trying to run SSR on the client?\n\n", 6: "Trying to insert a new style tag, but the given Node is unmounted!\n\n- Are you using a custom target that isn't mounted?\n- Does your document not have a valid head element?\n- Have you accidentally removed a style tag manually?\n\n", 7: 'ThemeProvider: Please return an object from your "theme" prop function, e.g.\n\n```js\ntheme={() => ({})}\n```\n\n', 8: 'ThemeProvider: Please make your "theme" prop an object.\n\n', 9: "Missing document `<head>`\n\n", 10: "Cannot find a StyleSheet instance. Usually this happens if there are multiple copies of styled-components loaded at once. Check out this issue for how to troubleshoot and fix the common cases where this situation can happen: https://github.com/styled-components/styled-components/issues/1941#issuecomment-417862021\n\n", 11: "_This error was replaced with a dev-time warning, it will be deleted for v4 final._ [createGlobalStyle] received children which will not be rendered. Please use the component without passing children elements.\n\n", 12: "It seems you are interpolating a keyframe declaration (%s) into an untagged string. This was supported in styled-components v3, but is not longer supported in v4 as keyframes are now injected on-demand. Please wrap your string in the css\\`\\` helper which ensures the styles are injected correctly. See https://www.styled-components.com/docs/api#css\n\n", 13: "%s is not a styled component and cannot be referred to via component selector. See https://www.styled-components.com/docs/advanced#referring-to-other-components for more details.\n\n", 14: 'ThemeProvider: "theme" prop is required.\n\n', 15: "A stylis plugin has been supplied that is not named. We need a name for each plugin to be able to prevent styling collisions between different stylis configurations within the same app. Before you pass your plugin to `<StyleSheetManager stylisPlugins={[]}>`, please make sure each plugin is uniquely-named, e.g.\n\n```js\nObject.defineProperty(importedPlugin, 'name', { value: 'some-unique-name' });\n```\n\n", 16: "Reached the limit of how many styled components may be created at group %s.\nYou may only create up to 1,073,741,824 components. If you're creating components dynamically,\nas for instance in your render method then you may be running into this limitation.\n\n", 17: "CSSStyleSheet could not be found on HTMLStyleElement.\nHas styled-components' style tag been unmounted or altered by another script?\n", 18: "ThemeProvider: Please make sure your useTheme hook is within a `<ThemeProvider>`" } : {};
    fe = (function() {
      function e(e2) {
        this.groupSizes = new Uint32Array(512), this.length = 512, this.tag = e2;
      }
      return e.prototype.indexOfGroup = function(e2) {
        for (var t = 0, n = 0; n < e2; n++) t += this.groupSizes[n];
        return t;
      }, e.prototype.insertRules = function(e2, t) {
        if (e2 >= this.groupSizes.length) {
          for (var n = this.groupSizes, o2 = n.length, r2 = o2; e2 >= r2; ) if ((r2 <<= 1) < 0) throw he(16, "".concat(e2));
          this.groupSizes = new Uint32Array(r2), this.groupSizes.set(n), this.length = r2;
          for (var s2 = o2; s2 < r2; s2++) this.groupSizes[s2] = 0;
        }
        for (var i2 = this.indexOfGroup(e2 + 1), a2 = (s2 = 0, t.length); s2 < a2; s2++) this.tag.insertRule(i2, t[s2]) && (this.groupSizes[e2]++, i2++);
      }, e.prototype.clearGroup = function(e2) {
        if (e2 < this.length) {
          var t = this.groupSizes[e2], n = this.indexOfGroup(e2), o2 = n + t;
          this.groupSizes[e2] = 0;
          for (var r2 = n; r2 < o2; r2++) this.tag.deleteRule(n);
        }
      }, e.prototype.getGroup = function(e2) {
        var t = "";
        if (e2 >= this.length || 0 === this.groupSizes[e2]) return t;
        for (var n = this.groupSizes[e2], o2 = this.indexOfGroup(e2), r2 = o2 + n, s2 = o2; s2 < r2; s2++) t += "".concat(this.tag.getRule(s2)).concat(g);
        return t;
      }, e;
    })();
    me = 1 << 30;
    ye = /* @__PURE__ */ new Map();
    ve = /* @__PURE__ */ new Map();
    ge = 1;
    Se = function(e) {
      if (ye.has(e)) return ye.get(e);
      for (; ve.has(ge); ) ge++;
      var t = ge++;
      if ((0 | t) < 0 || t > me) throw he(16, "".concat(t));
      return ye.set(e, t), ve.set(t, e), t;
    };
    we = function(e, t) {
      ge = t + 1, ye.set(e, t), ve.set(t, e);
    };
    be = "style[".concat(f, "][").concat(y, '="').concat(v, '"]');
    Ee = new RegExp("^".concat(f, '\\.g(\\d+)\\[id="([\\w\\d-]+)"\\].*?"([^"]*)'));
    Ne = function(e, t, n) {
      for (var o2, r2 = n.split(","), s2 = 0, i2 = r2.length; s2 < i2; s2++) (o2 = r2[s2]) && e.registerName(t, o2);
    };
    Pe = function(e, t) {
      for (var n, o2 = (null !== (n = t.textContent) && void 0 !== n ? n : "").split(g), r2 = [], s2 = 0, i2 = o2.length; s2 < i2; s2++) {
        var a2 = o2[s2].trim();
        if (a2) {
          var c2 = a2.match(Ee);
          if (c2) {
            var l2 = 0 | parseInt(c2[1], 10), u2 = c2[2];
            0 !== l2 && (we(u2, l2), Ne(e, u2, c2[3]), e.getTag().insertRules(l2, r2)), r2.length = 0;
          } else r2.push(a2);
        }
      }
    };
    _e = function(e) {
      for (var t = document.querySelectorAll(be), n = 0, o2 = t.length; n < o2; n++) {
        var r2 = t[n];
        r2 && r2.getAttribute(f) !== m && (Pe(e, r2), r2.parentNode && r2.parentNode.removeChild(r2));
      }
    };
    Ie = function(e) {
      var t = document.head, n = e || t, o2 = document.createElement("style"), r2 = (function(e2) {
        var t2 = Array.from(e2.querySelectorAll("style[".concat(f, "]")));
        return t2[t2.length - 1];
      })(n), s2 = void 0 !== r2 ? r2.nextSibling : null;
      o2.setAttribute(f, m), o2.setAttribute(y, v);
      var i2 = Ce();
      return i2 && o2.setAttribute("nonce", i2), n.insertBefore(o2, s2), o2;
    };
    Ae = (function() {
      function e(e2) {
        this.element = Ie(e2), this.element.appendChild(document.createTextNode("")), this.sheet = (function(e3) {
          if (e3.sheet) return e3.sheet;
          for (var t = document.styleSheets, n = 0, o2 = t.length; n < o2; n++) {
            var r2 = t[n];
            if (r2.ownerNode === e3) return r2;
          }
          throw he(17);
        })(this.element), this.length = 0;
      }
      return e.prototype.insertRule = function(e2, t) {
        try {
          return this.sheet.insertRule(t, e2), this.length++, true;
        } catch (e3) {
          return false;
        }
      }, e.prototype.deleteRule = function(e2) {
        this.sheet.deleteRule(e2), this.length--;
      }, e.prototype.getRule = function(e2) {
        var t = this.sheet.cssRules[e2];
        return t && t.cssText ? t.cssText : "";
      }, e;
    })();
    Oe = (function() {
      function e(e2) {
        this.element = Ie(e2), this.nodes = this.element.childNodes, this.length = 0;
      }
      return e.prototype.insertRule = function(e2, t) {
        if (e2 <= this.length && e2 >= 0) {
          var n = document.createTextNode(t);
          return this.element.insertBefore(n, this.nodes[e2] || null), this.length++, true;
        }
        return false;
      }, e.prototype.deleteRule = function(e2) {
        this.element.removeChild(this.nodes[e2]), this.length--;
      }, e.prototype.getRule = function(e2) {
        return e2 < this.length ? this.nodes[e2].textContent : "";
      }, e;
    })();
    De = (function() {
      function e(e2) {
        this.rules = [], this.length = 0;
      }
      return e.prototype.insertRule = function(e2, t) {
        return e2 <= this.length && (this.rules.splice(e2, 0, t), this.length++, true);
      }, e.prototype.deleteRule = function(e2) {
        this.rules.splice(e2, 1), this.length--;
      }, e.prototype.getRule = function(e2) {
        return e2 < this.length ? this.rules[e2] : "";
      }, e;
    })();
    Re = S;
    Te = { isServer: !S, useCSSOMInjection: !w };
    ke = (function() {
      function e(e2, n, o2) {
        void 0 === e2 && (e2 = C), void 0 === n && (n = {});
        var r2 = this;
        this.options = __assign(__assign({}, Te), e2), this.gs = n, this.names = new Map(o2), this.server = !!e2.isServer, !this.server && S && Re && (Re = false, _e(this)), ue(this, function() {
          return (function(e3) {
            for (var t = e3.getTag(), n2 = t.length, o3 = "", r3 = function(n3) {
              var r4 = (function(e4) {
                return ve.get(e4);
              })(n3);
              if (void 0 === r4) return "continue";
              var s3 = e3.names.get(r4), i2 = t.getGroup(n3);
              if (void 0 === s3 || !s3.size || 0 === i2.length) return "continue";
              var a2 = "".concat(f, ".g").concat(n3, '[id="').concat(r4, '"]'), c2 = "";
              void 0 !== s3 && s3.forEach(function(e4) {
                e4.length > 0 && (c2 += "".concat(e4, ","));
              }), o3 += "".concat(i2).concat(a2, '{content:"').concat(c2, '"}').concat(g);
            }, s2 = 0; s2 < n2; s2++) r3(s2);
            return o3;
          })(r2);
        });
      }
      return e.registerId = function(e2) {
        return Se(e2);
      }, e.prototype.rehydrate = function() {
        !this.server && S && _e(this);
      }, e.prototype.reconstructWithOptions = function(n, o2) {
        return void 0 === o2 && (o2 = true), new e(__assign(__assign({}, this.options), n), this.gs, o2 && this.names || void 0);
      }, e.prototype.allocateGSInstance = function(e2) {
        return this.gs[e2] = (this.gs[e2] || 0) + 1;
      }, e.prototype.getTag = function() {
        return this.tag || (this.tag = (e2 = (function(e3) {
          var t = e3.useCSSOMInjection, n = e3.target;
          return e3.isServer ? new De(n) : t ? new Ae(n) : new Oe(n);
        })(this.options), new fe(e2)));
        var e2;
      }, e.prototype.hasNameForId = function(e2, t) {
        return this.names.has(e2) && this.names.get(e2).has(t);
      }, e.prototype.registerName = function(e2, t) {
        if (Se(e2), this.names.has(e2)) this.names.get(e2).add(t);
        else {
          var n = /* @__PURE__ */ new Set();
          n.add(t), this.names.set(e2, n);
        }
      }, e.prototype.insertRules = function(e2, t, n) {
        this.registerName(e2, t), this.getTag().insertRules(Se(e2), n);
      }, e.prototype.clearNames = function(e2) {
        this.names.has(e2) && this.names.get(e2).clear();
      }, e.prototype.clearRules = function(e2) {
        this.getTag().clearGroup(Se(e2)), this.clearNames(e2);
      }, e.prototype.clearTag = function() {
        this.tag = void 0;
      }, e;
    })();
    je = /&/g;
    xe = /^\s*\/\/.*$/gm;
    Me = new ke();
    ze = Fe();
    $e = import_react.default.createContext({ shouldForwardProp: void 0, styleSheet: Me, stylis: ze });
    Be = $e.Consumer;
    Le = import_react.default.createContext(void 0);
    We = (function() {
      function e(e2, t) {
        var n = this;
        this.inject = function(e3, t2) {
          void 0 === t2 && (t2 = ze);
          var o2 = n.name + t2.hash;
          e3.hasNameForId(n.id, o2) || e3.insertRules(n.id, o2, t2(n.rules, o2, "@keyframes"));
        }, this.name = e2, this.id = "sc-keyframes-".concat(e2), this.rules = t, ue(this, function() {
          throw he(12, String(n.name));
        });
      }
      return e.prototype.getName = function(e2) {
        return void 0 === e2 && (e2 = ze), this.name + e2.hash;
      }, e;
    })();
    qe = function(e) {
      return e >= "A" && e <= "Z";
    };
    Ue = function(e) {
      return null == e || false === e || "" === e;
    };
    Je = function(t) {
      var n, o2, r2 = [];
      for (var s2 in t) {
        var i2 = t[s2];
        t.hasOwnProperty(s2) && !Ue(i2) && (Array.isArray(i2) && i2.isCss || re(i2) ? r2.push("".concat(He(s2), ":"), i2, ";") : ce(i2) ? r2.push.apply(r2, __spreadArray(__spreadArray(["".concat(s2, " {")], Je(i2), false), ["}"], false)) : r2.push("".concat(He(s2), ": ").concat((n = s2, null == (o2 = i2) || "boolean" == typeof o2 || "" === o2 ? "" : "number" != typeof o2 || 0 === o2 || n in unitlessKeys || n.startsWith("--") ? String(o2).trim() : "".concat(o2, "px")), ";")));
      }
      return r2;
    };
    Ke = z(v);
    Qe = (function() {
      function e(e2, t, n) {
        this.rules = e2, this.staticRulesId = "", this.isStatic = false, this.componentId = t, this.baseHash = M(Ke, t), this.baseStyle = n, ke.registerId(t);
      }
      return e.prototype.generateAndInjectStyles = function(e2, t, n) {
        var o2 = this.baseStyle ? this.baseStyle.generateAndInjectStyles(e2, t, n) : "";
        if (this.isStatic && !n.hash) if (this.staticRulesId && t.hasNameForId(this.componentId, this.staticRulesId)) o2 = ie(o2, this.staticRulesId);
        else {
          var r2 = ae(Xe(this.rules, e2, t, n)), s2 = x(M(this.baseHash, r2) >>> 0);
          if (!t.hasNameForId(this.componentId, s2)) {
            var i2 = n(r2, ".".concat(s2), void 0, this.componentId);
            t.insertRules(this.componentId, s2, i2);
          }
          o2 = ie(o2, s2), this.staticRulesId = s2;
        }
        else {
          for (var a2 = M(this.baseHash, n.hash), c2 = "", l2 = 0; l2 < this.rules.length; l2++) {
            var u2 = this.rules[l2];
            if ("string" == typeof u2) c2 += u2, a2 = M(a2, u2);
            else if (u2) {
              var p2 = ae(Xe(u2, e2, t, n));
              a2 = M(a2, p2 + l2), c2 += p2;
            }
          }
          if (c2) {
            var d = x(a2 >>> 0);
            t.hasNameForId(this.componentId, d) || t.insertRules(this.componentId, d, n(c2, ".".concat(d), void 0, this.componentId)), o2 = ie(o2, d);
          }
        }
        return o2;
      }, e;
    })();
    et = import_react.default.createContext(void 0);
    tt = et.Consumer;
    rt = {};
    st = /* @__PURE__ */ new Set();
    ct = function(e) {
      return Object.assign(e, { isCss: true });
    };
    pt = function(e) {
      return ut(it, e);
    };
    dt = pt;
    A.forEach(function(e) {
      dt[e] = pt(e);
    });
    ht = (function() {
      function e(e2, t) {
        this.rules = e2, this.componentId = t, this.isStatic = Ze(e2), ke.registerId(this.componentId + 1);
      }
      return e.prototype.createStyles = function(e2, t, n, o2) {
        var r2 = o2(ae(Xe(this.rules, t, n, o2)), ""), s2 = this.componentId + e2;
        n.insertRules(s2, s2, r2);
      }, e.prototype.removeStyles = function(e2, t) {
        t.clearRules(this.componentId + e2);
      }, e.prototype.renderStyles = function(e2, t, n, o2) {
        e2 > 2 && ke.registerId(this.componentId + e2), this.removeStyles(e2, n), this.createStyles(e2, t, n, o2);
      }, e;
    })();
    vt = (function() {
      function e() {
        var e2 = this;
        this._emitSheetCSS = function() {
          var t = e2.instance.toString();
          if (!t) return "";
          var n = Ce(), o2 = ae([n && 'nonce="'.concat(n, '"'), "".concat(f, '="true"'), "".concat(y, '="').concat(v, '"')].filter(Boolean), " ");
          return "<style ".concat(o2, ">").concat(t, "</style>");
        }, this.getStyleTags = function() {
          if (e2.sealed) throw he(2);
          return e2._emitSheetCSS();
        }, this.getStyleElement = function() {
          var n;
          if (e2.sealed) throw he(2);
          var r2 = e2.instance.toString();
          if (!r2) return [];
          var s2 = ((n = {})[f] = "", n[y] = v, n.dangerouslySetInnerHTML = { __html: r2 }, n), i2 = Ce();
          return i2 && (s2.nonce = i2), [import_react.default.createElement("style", __assign({}, s2, { key: "sc-0-0" }))];
        }, this.seal = function() {
          e2.sealed = true;
        }, this.instance = new ke({ isServer: true }), this.sealed = false;
      }
      return e.prototype.collectStyles = function(e2) {
        if (this.sealed) throw he(2);
        return import_react.default.createElement(Ye, { sheet: this.instance }, e2);
      }, e.prototype.interleaveWithNodeStream = function(e2) {
        throw he(3);
      }, e;
    })();
    gt = { StyleSheet: ke, mainSheet: Me };
    "undefined" != typeof navigator && "ReactNative" === navigator.product && console.warn("It looks like you've imported 'styled-components' on React Native.\nPerhaps you're looking to import 'styled-components/native'?\nRead more about this at https://www.styled-components.com/docs/basics#react-native");
    St = "__sc-".concat(f, "__");
    "undefined" != typeof window && (window[St] || (window[St] = 0), 1 === window[St] && console.warn("It looks like there are several instances of 'styled-components' initialized in this application. This may cause dynamic styles to not render properly, errors during the rehydration process, a missing theme prop, and makes your application bigger without good reason.\n\nSee https://s-c.sh/2BAXzed for more info."), window[St] += 1);
  }
});

// node_modules/react-is/cjs/react-is.development.js
var require_react_is_development = __commonJS({
  "node_modules/react-is/cjs/react-is.development.js"(exports) {
    "use strict";
    if (true) {
      (function() {
        "use strict";
        var hasSymbol2 = typeof Symbol === "function" && Symbol.for;
        var REACT_ELEMENT_TYPE = hasSymbol2 ? Symbol.for("react.element") : 60103;
        var REACT_PORTAL_TYPE = hasSymbol2 ? Symbol.for("react.portal") : 60106;
        var REACT_FRAGMENT_TYPE = hasSymbol2 ? Symbol.for("react.fragment") : 60107;
        var REACT_STRICT_MODE_TYPE = hasSymbol2 ? Symbol.for("react.strict_mode") : 60108;
        var REACT_PROFILER_TYPE = hasSymbol2 ? Symbol.for("react.profiler") : 60114;
        var REACT_PROVIDER_TYPE = hasSymbol2 ? Symbol.for("react.provider") : 60109;
        var REACT_CONTEXT_TYPE = hasSymbol2 ? Symbol.for("react.context") : 60110;
        var REACT_ASYNC_MODE_TYPE = hasSymbol2 ? Symbol.for("react.async_mode") : 60111;
        var REACT_CONCURRENT_MODE_TYPE = hasSymbol2 ? Symbol.for("react.concurrent_mode") : 60111;
        var REACT_FORWARD_REF_TYPE = hasSymbol2 ? Symbol.for("react.forward_ref") : 60112;
        var REACT_SUSPENSE_TYPE = hasSymbol2 ? Symbol.for("react.suspense") : 60113;
        var REACT_SUSPENSE_LIST_TYPE = hasSymbol2 ? Symbol.for("react.suspense_list") : 60120;
        var REACT_MEMO_TYPE = hasSymbol2 ? Symbol.for("react.memo") : 60115;
        var REACT_LAZY_TYPE = hasSymbol2 ? Symbol.for("react.lazy") : 60116;
        var REACT_BLOCK_TYPE = hasSymbol2 ? Symbol.for("react.block") : 60121;
        var REACT_FUNDAMENTAL_TYPE = hasSymbol2 ? Symbol.for("react.fundamental") : 60117;
        var REACT_RESPONDER_TYPE = hasSymbol2 ? Symbol.for("react.responder") : 60118;
        var REACT_SCOPE_TYPE = hasSymbol2 ? Symbol.for("react.scope") : 60119;
        function isValidElementType(type) {
          return typeof type === "string" || typeof type === "function" || // Note: its typeof might be other than 'symbol' or 'number' if it's a polyfill.
          type === REACT_FRAGMENT_TYPE || type === REACT_CONCURRENT_MODE_TYPE || type === REACT_PROFILER_TYPE || type === REACT_STRICT_MODE_TYPE || type === REACT_SUSPENSE_TYPE || type === REACT_SUSPENSE_LIST_TYPE || typeof type === "object" && type !== null && (type.$$typeof === REACT_LAZY_TYPE || type.$$typeof === REACT_MEMO_TYPE || type.$$typeof === REACT_PROVIDER_TYPE || type.$$typeof === REACT_CONTEXT_TYPE || type.$$typeof === REACT_FORWARD_REF_TYPE || type.$$typeof === REACT_FUNDAMENTAL_TYPE || type.$$typeof === REACT_RESPONDER_TYPE || type.$$typeof === REACT_SCOPE_TYPE || type.$$typeof === REACT_BLOCK_TYPE);
        }
        function typeOf(object2) {
          if (typeof object2 === "object" && object2 !== null) {
            var $$typeof = object2.$$typeof;
            switch ($$typeof) {
              case REACT_ELEMENT_TYPE:
                var type = object2.type;
                switch (type) {
                  case REACT_ASYNC_MODE_TYPE:
                  case REACT_CONCURRENT_MODE_TYPE:
                  case REACT_FRAGMENT_TYPE:
                  case REACT_PROFILER_TYPE:
                  case REACT_STRICT_MODE_TYPE:
                  case REACT_SUSPENSE_TYPE:
                    return type;
                  default:
                    var $$typeofType = type && type.$$typeof;
                    switch ($$typeofType) {
                      case REACT_CONTEXT_TYPE:
                      case REACT_FORWARD_REF_TYPE:
                      case REACT_LAZY_TYPE:
                      case REACT_MEMO_TYPE:
                      case REACT_PROVIDER_TYPE:
                        return $$typeofType;
                      default:
                        return $$typeof;
                    }
                }
              case REACT_PORTAL_TYPE:
                return $$typeof;
            }
          }
          return void 0;
        }
        var AsyncMode = REACT_ASYNC_MODE_TYPE;
        var ConcurrentMode = REACT_CONCURRENT_MODE_TYPE;
        var ContextConsumer = REACT_CONTEXT_TYPE;
        var ContextProvider = REACT_PROVIDER_TYPE;
        var Element2 = REACT_ELEMENT_TYPE;
        var ForwardRef = REACT_FORWARD_REF_TYPE;
        var Fragment = REACT_FRAGMENT_TYPE;
        var Lazy = REACT_LAZY_TYPE;
        var Memo = REACT_MEMO_TYPE;
        var Portal = REACT_PORTAL_TYPE;
        var Profiler = REACT_PROFILER_TYPE;
        var StrictMode = REACT_STRICT_MODE_TYPE;
        var Suspense = REACT_SUSPENSE_TYPE;
        var hasWarnedAboutDeprecatedIsAsyncMode = false;
        function isAsyncMode(object2) {
          {
            if (!hasWarnedAboutDeprecatedIsAsyncMode) {
              hasWarnedAboutDeprecatedIsAsyncMode = true;
              console["warn"]("The ReactIs.isAsyncMode() alias has been deprecated, and will be removed in React 17+. Update your code to use ReactIs.isConcurrentMode() instead. It has the exact same API.");
            }
          }
          return isConcurrentMode(object2) || typeOf(object2) === REACT_ASYNC_MODE_TYPE;
        }
        function isConcurrentMode(object2) {
          return typeOf(object2) === REACT_CONCURRENT_MODE_TYPE;
        }
        function isContextConsumer(object2) {
          return typeOf(object2) === REACT_CONTEXT_TYPE;
        }
        function isContextProvider(object2) {
          return typeOf(object2) === REACT_PROVIDER_TYPE;
        }
        function isElement(object2) {
          return typeof object2 === "object" && object2 !== null && object2.$$typeof === REACT_ELEMENT_TYPE;
        }
        function isForwardRef(object2) {
          return typeOf(object2) === REACT_FORWARD_REF_TYPE;
        }
        function isFragment(object2) {
          return typeOf(object2) === REACT_FRAGMENT_TYPE;
        }
        function isLazy(object2) {
          return typeOf(object2) === REACT_LAZY_TYPE;
        }
        function isMemo(object2) {
          return typeOf(object2) === REACT_MEMO_TYPE;
        }
        function isPortal(object2) {
          return typeOf(object2) === REACT_PORTAL_TYPE;
        }
        function isProfiler(object2) {
          return typeOf(object2) === REACT_PROFILER_TYPE;
        }
        function isStrictMode(object2) {
          return typeOf(object2) === REACT_STRICT_MODE_TYPE;
        }
        function isSuspense(object2) {
          return typeOf(object2) === REACT_SUSPENSE_TYPE;
        }
        exports.AsyncMode = AsyncMode;
        exports.ConcurrentMode = ConcurrentMode;
        exports.ContextConsumer = ContextConsumer;
        exports.ContextProvider = ContextProvider;
        exports.Element = Element2;
        exports.ForwardRef = ForwardRef;
        exports.Fragment = Fragment;
        exports.Lazy = Lazy;
        exports.Memo = Memo;
        exports.Portal = Portal;
        exports.Profiler = Profiler;
        exports.StrictMode = StrictMode;
        exports.Suspense = Suspense;
        exports.isAsyncMode = isAsyncMode;
        exports.isConcurrentMode = isConcurrentMode;
        exports.isContextConsumer = isContextConsumer;
        exports.isContextProvider = isContextProvider;
        exports.isElement = isElement;
        exports.isForwardRef = isForwardRef;
        exports.isFragment = isFragment;
        exports.isLazy = isLazy;
        exports.isMemo = isMemo;
        exports.isPortal = isPortal;
        exports.isProfiler = isProfiler;
        exports.isStrictMode = isStrictMode;
        exports.isSuspense = isSuspense;
        exports.isValidElementType = isValidElementType;
        exports.typeOf = typeOf;
      })();
    }
  }
});

// node_modules/react-is/index.js
var require_react_is = __commonJS({
  "node_modules/react-is/index.js"(exports, module) {
    "use strict";
    if (false) {
      module.exports = null;
    } else {
      module.exports = require_react_is_development();
    }
  }
});

// node_modules/object-assign/index.js
var require_object_assign = __commonJS({
  "node_modules/object-assign/index.js"(exports, module) {
    "use strict";
    var getOwnPropertySymbols = Object.getOwnPropertySymbols;
    var hasOwnProperty = Object.prototype.hasOwnProperty;
    var propIsEnumerable = Object.prototype.propertyIsEnumerable;
    function toObject(val) {
      if (val === null || val === void 0) {
        throw new TypeError("Object.assign cannot be called with null or undefined");
      }
      return Object(val);
    }
    function shouldUseNative() {
      try {
        if (!Object.assign) {
          return false;
        }
        var test1 = new String("abc");
        test1[5] = "de";
        if (Object.getOwnPropertyNames(test1)[0] === "5") {
          return false;
        }
        var test2 = {};
        for (var i2 = 0; i2 < 10; i2++) {
          test2["_" + String.fromCharCode(i2)] = i2;
        }
        var order2 = Object.getOwnPropertyNames(test2).map(function(n) {
          return test2[n];
        });
        if (order2.join("") !== "0123456789") {
          return false;
        }
        var test3 = {};
        "abcdefghijklmnopqrst".split("").forEach(function(letter) {
          test3[letter] = letter;
        });
        if (Object.keys(Object.assign({}, test3)).join("") !== "abcdefghijklmnopqrst") {
          return false;
        }
        return true;
      } catch (err) {
        return false;
      }
    }
    module.exports = shouldUseNative() ? Object.assign : function(target, source) {
      var from2;
      var to = toObject(target);
      var symbols;
      for (var s2 = 1; s2 < arguments.length; s2++) {
        from2 = Object(arguments[s2]);
        for (var key2 in from2) {
          if (hasOwnProperty.call(from2, key2)) {
            to[key2] = from2[key2];
          }
        }
        if (getOwnPropertySymbols) {
          symbols = getOwnPropertySymbols(from2);
          for (var i2 = 0; i2 < symbols.length; i2++) {
            if (propIsEnumerable.call(from2, symbols[i2])) {
              to[symbols[i2]] = from2[symbols[i2]];
            }
          }
        }
      }
      return to;
    };
  }
});

// node_modules/prop-types/lib/ReactPropTypesSecret.js
var require_ReactPropTypesSecret = __commonJS({
  "node_modules/prop-types/lib/ReactPropTypesSecret.js"(exports, module) {
    "use strict";
    var ReactPropTypesSecret = "SECRET_DO_NOT_PASS_THIS_OR_YOU_WILL_BE_FIRED";
    module.exports = ReactPropTypesSecret;
  }
});

// node_modules/prop-types/lib/has.js
var require_has = __commonJS({
  "node_modules/prop-types/lib/has.js"(exports, module) {
    module.exports = Function.call.bind(Object.prototype.hasOwnProperty);
  }
});

// node_modules/prop-types/checkPropTypes.js
var require_checkPropTypes = __commonJS({
  "node_modules/prop-types/checkPropTypes.js"(exports, module) {
    "use strict";
    var printWarning = function() {
    };
    if (true) {
      ReactPropTypesSecret = require_ReactPropTypesSecret();
      loggedTypeFailures = {};
      has3 = require_has();
      printWarning = function(text) {
        var message = "Warning: " + text;
        if (typeof console !== "undefined") {
          console.error(message);
        }
        try {
          throw new Error(message);
        } catch (x2) {
        }
      };
    }
    var ReactPropTypesSecret;
    var loggedTypeFailures;
    var has3;
    function checkPropTypes3(typeSpecs, values2, location, componentName, getStack) {
      if (true) {
        for (var typeSpecName in typeSpecs) {
          if (has3(typeSpecs, typeSpecName)) {
            var error;
            try {
              if (typeof typeSpecs[typeSpecName] !== "function") {
                var err = Error(
                  (componentName || "React class") + ": " + location + " type `" + typeSpecName + "` is invalid; it must be a function, usually from the `prop-types` package, but received `" + typeof typeSpecs[typeSpecName] + "`.This often happens because of typos such as `PropTypes.function` instead of `PropTypes.func`."
                );
                err.name = "Invariant Violation";
                throw err;
              }
              error = typeSpecs[typeSpecName](values2, typeSpecName, componentName, location, null, ReactPropTypesSecret);
            } catch (ex) {
              error = ex;
            }
            if (error && !(error instanceof Error)) {
              printWarning(
                (componentName || "React class") + ": type specification of " + location + " `" + typeSpecName + "` is invalid; the type checker function must return `null` or an `Error` but returned a " + typeof error + ". You may have forgotten to pass an argument to the type checker creator (arrayOf, instanceOf, objectOf, oneOf, oneOfType, and shape all require an argument)."
              );
            }
            if (error instanceof Error && !(error.message in loggedTypeFailures)) {
              loggedTypeFailures[error.message] = true;
              var stack = getStack ? getStack() : "";
              printWarning(
                "Failed " + location + " type: " + error.message + (stack != null ? stack : "")
              );
            }
          }
        }
      }
    }
    checkPropTypes3.resetWarningCache = function() {
      if (true) {
        loggedTypeFailures = {};
      }
    };
    module.exports = checkPropTypes3;
  }
});

// node_modules/prop-types/factoryWithTypeCheckers.js
var require_factoryWithTypeCheckers = __commonJS({
  "node_modules/prop-types/factoryWithTypeCheckers.js"(exports, module) {
    "use strict";
    var ReactIs = require_react_is();
    var assign3 = require_object_assign();
    var ReactPropTypesSecret = require_ReactPropTypesSecret();
    var has3 = require_has();
    var checkPropTypes3 = require_checkPropTypes();
    var printWarning = function() {
    };
    if (true) {
      printWarning = function(text) {
        var message = "Warning: " + text;
        if (typeof console !== "undefined") {
          console.error(message);
        }
        try {
          throw new Error(message);
        } catch (x2) {
        }
      };
    }
    function emptyFunctionThatReturnsNull() {
      return null;
    }
    module.exports = function(isValidElement, throwOnDirectAccess) {
      var ITERATOR_SYMBOL = typeof Symbol === "function" && Symbol.iterator;
      var FAUX_ITERATOR_SYMBOL = "@@iterator";
      function getIteratorFn(maybeIterable) {
        var iteratorFn = maybeIterable && (ITERATOR_SYMBOL && maybeIterable[ITERATOR_SYMBOL] || maybeIterable[FAUX_ITERATOR_SYMBOL]);
        if (typeof iteratorFn === "function") {
          return iteratorFn;
        }
      }
      var ANONYMOUS = "<<anonymous>>";
      var ReactPropTypes = {
        array: createPrimitiveTypeChecker("array"),
        bigint: createPrimitiveTypeChecker("bigint"),
        bool: createPrimitiveTypeChecker("boolean"),
        func: createPrimitiveTypeChecker("function"),
        number: createPrimitiveTypeChecker("number"),
        object: createPrimitiveTypeChecker("object"),
        string: createPrimitiveTypeChecker("string"),
        symbol: createPrimitiveTypeChecker("symbol"),
        any: createAnyTypeChecker(),
        arrayOf: createArrayOfTypeChecker,
        element: createElementTypeChecker(),
        elementType: createElementTypeTypeChecker(),
        instanceOf: createInstanceTypeChecker,
        node: createNodeChecker(),
        objectOf: createObjectOfTypeChecker,
        oneOf: createEnumTypeChecker,
        oneOfType: createUnionTypeChecker,
        shape: createShapeTypeChecker,
        exact: createStrictShapeTypeChecker
      };
      function is2(x2, y2) {
        if (x2 === y2) {
          return x2 !== 0 || 1 / x2 === 1 / y2;
        } else {
          return x2 !== x2 && y2 !== y2;
        }
      }
      function PropTypeError(message, data) {
        this.message = message;
        this.data = data && typeof data === "object" ? data : {};
        this.stack = "";
      }
      PropTypeError.prototype = Error.prototype;
      function createChainableTypeChecker2(validate) {
        if (true) {
          var manualPropTypeCallCache = {};
          var manualPropTypeWarningCount = 0;
        }
        function checkType(isRequired, props, propName, componentName, location, propFullName, secret) {
          componentName = componentName || ANONYMOUS;
          propFullName = propFullName || propName;
          if (secret !== ReactPropTypesSecret) {
            if (throwOnDirectAccess) {
              var err = new Error(
                "Calling PropTypes validators directly is not supported by the `prop-types` package. Use `PropTypes.checkPropTypes()` to call them. Read more at http://fb.me/use-check-prop-types"
              );
              err.name = "Invariant Violation";
              throw err;
            } else if (typeof console !== "undefined") {
              var cacheKey = componentName + ":" + propName;
              if (!manualPropTypeCallCache[cacheKey] && // Avoid spamming the console because they are often not actionable except for lib authors
              manualPropTypeWarningCount < 3) {
                printWarning(
                  "You are manually calling a React.PropTypes validation function for the `" + propFullName + "` prop on `" + componentName + "`. This is deprecated and will throw in the standalone `prop-types` package. You may be seeing this warning due to a third-party PropTypes library. See https://fb.me/react-warning-dont-call-proptypes for details."
                );
                manualPropTypeCallCache[cacheKey] = true;
                manualPropTypeWarningCount++;
              }
            }
          }
          if (props[propName] == null) {
            if (isRequired) {
              if (props[propName] === null) {
                return new PropTypeError("The " + location + " `" + propFullName + "` is marked as required " + ("in `" + componentName + "`, but its value is `null`."));
              }
              return new PropTypeError("The " + location + " `" + propFullName + "` is marked as required in " + ("`" + componentName + "`, but its value is `undefined`."));
            }
            return null;
          } else {
            return validate(props, propName, componentName, location, propFullName);
          }
        }
        var chainedCheckType = checkType.bind(null, false);
        chainedCheckType.isRequired = checkType.bind(null, true);
        return chainedCheckType;
      }
      function createPrimitiveTypeChecker(expectedType) {
        function validate(props, propName, componentName, location, propFullName, secret) {
          var propValue = props[propName];
          var propType = getPropType2(propValue);
          if (propType !== expectedType) {
            var preciseType = getPreciseType2(propValue);
            return new PropTypeError(
              "Invalid " + location + " `" + propFullName + "` of type " + ("`" + preciseType + "` supplied to `" + componentName + "`, expected ") + ("`" + expectedType + "`."),
              { expectedType }
            );
          }
          return null;
        }
        return createChainableTypeChecker2(validate);
      }
      function createAnyTypeChecker() {
        return createChainableTypeChecker2(emptyFunctionThatReturnsNull);
      }
      function createArrayOfTypeChecker(typeChecker) {
        function validate(props, propName, componentName, location, propFullName) {
          if (typeof typeChecker !== "function") {
            return new PropTypeError("Property `" + propFullName + "` of component `" + componentName + "` has invalid PropType notation inside arrayOf.");
          }
          var propValue = props[propName];
          if (!Array.isArray(propValue)) {
            var propType = getPropType2(propValue);
            return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type " + ("`" + propType + "` supplied to `" + componentName + "`, expected an array."));
          }
          for (var i2 = 0; i2 < propValue.length; i2++) {
            var error = typeChecker(propValue, i2, componentName, location, propFullName + "[" + i2 + "]", ReactPropTypesSecret);
            if (error instanceof Error) {
              return error;
            }
          }
          return null;
        }
        return createChainableTypeChecker2(validate);
      }
      function createElementTypeChecker() {
        function validate(props, propName, componentName, location, propFullName) {
          var propValue = props[propName];
          if (!isValidElement(propValue)) {
            var propType = getPropType2(propValue);
            return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type " + ("`" + propType + "` supplied to `" + componentName + "`, expected a single ReactElement."));
          }
          return null;
        }
        return createChainableTypeChecker2(validate);
      }
      function createElementTypeTypeChecker() {
        function validate(props, propName, componentName, location, propFullName) {
          var propValue = props[propName];
          if (!ReactIs.isValidElementType(propValue)) {
            var propType = getPropType2(propValue);
            return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type " + ("`" + propType + "` supplied to `" + componentName + "`, expected a single ReactElement type."));
          }
          return null;
        }
        return createChainableTypeChecker2(validate);
      }
      function createInstanceTypeChecker(expectedClass) {
        function validate(props, propName, componentName, location, propFullName) {
          if (!(props[propName] instanceof expectedClass)) {
            var expectedClassName = expectedClass.name || ANONYMOUS;
            var actualClassName = getClassName(props[propName]);
            return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type " + ("`" + actualClassName + "` supplied to `" + componentName + "`, expected ") + ("instance of `" + expectedClassName + "`."));
          }
          return null;
        }
        return createChainableTypeChecker2(validate);
      }
      function createEnumTypeChecker(expectedValues) {
        if (!Array.isArray(expectedValues)) {
          if (true) {
            if (arguments.length > 1) {
              printWarning(
                "Invalid arguments supplied to oneOf, expected an array, got " + arguments.length + " arguments. A common mistake is to write oneOf(x, y, z) instead of oneOf([x, y, z])."
              );
            } else {
              printWarning("Invalid argument supplied to oneOf, expected an array.");
            }
          }
          return emptyFunctionThatReturnsNull;
        }
        function validate(props, propName, componentName, location, propFullName) {
          var propValue = props[propName];
          for (var i2 = 0; i2 < expectedValues.length; i2++) {
            if (is2(propValue, expectedValues[i2])) {
              return null;
            }
          }
          var valuesString = JSON.stringify(expectedValues, function replacer(key2, value) {
            var type = getPreciseType2(value);
            if (type === "symbol") {
              return String(value);
            }
            return value;
          });
          return new PropTypeError("Invalid " + location + " `" + propFullName + "` of value `" + String(propValue) + "` " + ("supplied to `" + componentName + "`, expected one of " + valuesString + "."));
        }
        return createChainableTypeChecker2(validate);
      }
      function createObjectOfTypeChecker(typeChecker) {
        function validate(props, propName, componentName, location, propFullName) {
          if (typeof typeChecker !== "function") {
            return new PropTypeError("Property `" + propFullName + "` of component `" + componentName + "` has invalid PropType notation inside objectOf.");
          }
          var propValue = props[propName];
          var propType = getPropType2(propValue);
          if (propType !== "object") {
            return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type " + ("`" + propType + "` supplied to `" + componentName + "`, expected an object."));
          }
          for (var key2 in propValue) {
            if (has3(propValue, key2)) {
              var error = typeChecker(propValue, key2, componentName, location, propFullName + "." + key2, ReactPropTypesSecret);
              if (error instanceof Error) {
                return error;
              }
            }
          }
          return null;
        }
        return createChainableTypeChecker2(validate);
      }
      function createUnionTypeChecker(arrayOfTypeCheckers) {
        if (!Array.isArray(arrayOfTypeCheckers)) {
          true ? printWarning("Invalid argument supplied to oneOfType, expected an instance of array.") : void 0;
          return emptyFunctionThatReturnsNull;
        }
        for (var i2 = 0; i2 < arrayOfTypeCheckers.length; i2++) {
          var checker = arrayOfTypeCheckers[i2];
          if (typeof checker !== "function") {
            printWarning(
              "Invalid argument supplied to oneOfType. Expected an array of check functions, but received " + getPostfixForTypeWarning(checker) + " at index " + i2 + "."
            );
            return emptyFunctionThatReturnsNull;
          }
        }
        function validate(props, propName, componentName, location, propFullName) {
          var expectedTypes = [];
          for (var i3 = 0; i3 < arrayOfTypeCheckers.length; i3++) {
            var checker2 = arrayOfTypeCheckers[i3];
            var checkerResult = checker2(props, propName, componentName, location, propFullName, ReactPropTypesSecret);
            if (checkerResult == null) {
              return null;
            }
            if (checkerResult.data && has3(checkerResult.data, "expectedType")) {
              expectedTypes.push(checkerResult.data.expectedType);
            }
          }
          var expectedTypesMessage = expectedTypes.length > 0 ? ", expected one of type [" + expectedTypes.join(", ") + "]" : "";
          return new PropTypeError("Invalid " + location + " `" + propFullName + "` supplied to " + ("`" + componentName + "`" + expectedTypesMessage + "."));
        }
        return createChainableTypeChecker2(validate);
      }
      function createNodeChecker() {
        function validate(props, propName, componentName, location, propFullName) {
          if (!isNode3(props[propName])) {
            return new PropTypeError("Invalid " + location + " `" + propFullName + "` supplied to " + ("`" + componentName + "`, expected a ReactNode."));
          }
          return null;
        }
        return createChainableTypeChecker2(validate);
      }
      function invalidValidatorError(componentName, location, propFullName, key2, type) {
        return new PropTypeError(
          (componentName || "React class") + ": " + location + " type `" + propFullName + "." + key2 + "` is invalid; it must be a function, usually from the `prop-types` package, but received `" + type + "`."
        );
      }
      function createShapeTypeChecker(shapeTypes) {
        function validate(props, propName, componentName, location, propFullName) {
          var propValue = props[propName];
          var propType = getPropType2(propValue);
          if (propType !== "object") {
            return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type `" + propType + "` " + ("supplied to `" + componentName + "`, expected `object`."));
          }
          for (var key2 in shapeTypes) {
            var checker = shapeTypes[key2];
            if (typeof checker !== "function") {
              return invalidValidatorError(componentName, location, propFullName, key2, getPreciseType2(checker));
            }
            var error = checker(propValue, key2, componentName, location, propFullName + "." + key2, ReactPropTypesSecret);
            if (error) {
              return error;
            }
          }
          return null;
        }
        return createChainableTypeChecker2(validate);
      }
      function createStrictShapeTypeChecker(shapeTypes) {
        function validate(props, propName, componentName, location, propFullName) {
          var propValue = props[propName];
          var propType = getPropType2(propValue);
          if (propType !== "object") {
            return new PropTypeError("Invalid " + location + " `" + propFullName + "` of type `" + propType + "` " + ("supplied to `" + componentName + "`, expected `object`."));
          }
          var allKeys = assign3({}, props[propName], shapeTypes);
          for (var key2 in allKeys) {
            var checker = shapeTypes[key2];
            if (has3(shapeTypes, key2) && typeof checker !== "function") {
              return invalidValidatorError(componentName, location, propFullName, key2, getPreciseType2(checker));
            }
            if (!checker) {
              return new PropTypeError(
                "Invalid " + location + " `" + propFullName + "` key `" + key2 + "` supplied to `" + componentName + "`.\nBad object: " + JSON.stringify(props[propName], null, "  ") + "\nValid keys: " + JSON.stringify(Object.keys(shapeTypes), null, "  ")
              );
            }
            var error = checker(propValue, key2, componentName, location, propFullName + "." + key2, ReactPropTypesSecret);
            if (error) {
              return error;
            }
          }
          return null;
        }
        return createChainableTypeChecker2(validate);
      }
      function isNode3(propValue) {
        switch (typeof propValue) {
          case "number":
          case "string":
          case "undefined":
            return true;
          case "boolean":
            return !propValue;
          case "object":
            if (Array.isArray(propValue)) {
              return propValue.every(isNode3);
            }
            if (propValue === null || isValidElement(propValue)) {
              return true;
            }
            var iteratorFn = getIteratorFn(propValue);
            if (iteratorFn) {
              var iterator = iteratorFn.call(propValue);
              var step;
              if (iteratorFn !== propValue.entries) {
                while (!(step = iterator.next()).done) {
                  if (!isNode3(step.value)) {
                    return false;
                  }
                }
              } else {
                while (!(step = iterator.next()).done) {
                  var entry = step.value;
                  if (entry) {
                    if (!isNode3(entry[1])) {
                      return false;
                    }
                  }
                }
              }
            } else {
              return false;
            }
            return true;
          default:
            return false;
        }
      }
      function isSymbol2(propType, propValue) {
        if (propType === "symbol") {
          return true;
        }
        if (!propValue) {
          return false;
        }
        if (propValue["@@toStringTag"] === "Symbol") {
          return true;
        }
        if (typeof Symbol === "function" && propValue instanceof Symbol) {
          return true;
        }
        return false;
      }
      function getPropType2(propValue) {
        var propType = typeof propValue;
        if (Array.isArray(propValue)) {
          return "array";
        }
        if (propValue instanceof RegExp) {
          return "object";
        }
        if (isSymbol2(propType, propValue)) {
          return "symbol";
        }
        return propType;
      }
      function getPreciseType2(propValue) {
        if (typeof propValue === "undefined" || propValue === null) {
          return "" + propValue;
        }
        var propType = getPropType2(propValue);
        if (propType === "object") {
          if (propValue instanceof Date) {
            return "date";
          } else if (propValue instanceof RegExp) {
            return "regexp";
          }
        }
        return propType;
      }
      function getPostfixForTypeWarning(value) {
        var type = getPreciseType2(value);
        switch (type) {
          case "array":
          case "object":
            return "an " + type;
          case "boolean":
          case "date":
          case "regexp":
            return "a " + type;
          default:
            return type;
        }
      }
      function getClassName(propValue) {
        if (!propValue.constructor || !propValue.constructor.name) {
          return ANONYMOUS;
        }
        return propValue.constructor.name;
      }
      ReactPropTypes.checkPropTypes = checkPropTypes3;
      ReactPropTypes.resetWarningCache = checkPropTypes3.resetWarningCache;
      ReactPropTypes.PropTypes = ReactPropTypes;
      return ReactPropTypes;
    };
  }
});

// node_modules/prop-types/index.js
var require_prop_types = __commonJS({
  "node_modules/prop-types/index.js"(exports, module) {
    if (true) {
      ReactIs = require_react_is();
      throwOnDirectAccess = true;
      module.exports = require_factoryWithTypeCheckers()(ReactIs.isElement, throwOnDirectAccess);
    } else {
      module.exports = null();
    }
    var ReactIs;
    var throwOnDirectAccess;
  }
});

// node_modules/mobx/dist/mobx.esm.js
var mobx_esm_exports = {};
__export(mobx_esm_exports, {
  $mobx: () => $mobx,
  FlowCancellationError: () => FlowCancellationError,
  ObservableMap: () => ObservableMap,
  ObservableSet: () => ObservableSet,
  Reaction: () => Reaction,
  _allowStateChanges: () => allowStateChanges,
  _allowStateChangesInsideComputed: () => runInAction,
  _allowStateReadsEnd: () => allowStateReadsEnd,
  _allowStateReadsStart: () => allowStateReadsStart,
  _autoAction: () => autoAction,
  _endAction: () => _endAction,
  _getAdministration: () => getAdministration,
  _getGlobalState: () => getGlobalState,
  _interceptReads: () => interceptReads,
  _isComputingDerivation: () => isComputingDerivation,
  _resetGlobalState: () => resetGlobalState,
  _startAction: () => _startAction,
  action: () => action,
  autorun: () => autorun,
  comparer: () => comparer,
  computed: () => computed,
  configure: () => configure,
  createAtom: () => createAtom,
  defineProperty: () => apiDefineProperty,
  entries: () => entries,
  extendObservable: () => extendObservable,
  flow: () => flow,
  flowResult: () => flowResult,
  get: () => get,
  getAtom: () => getAtom,
  getDebugName: () => getDebugName,
  getDependencyTree: () => getDependencyTree,
  getObserverTree: () => getObserverTree,
  has: () => has,
  intercept: () => intercept,
  isAction: () => isAction,
  isBoxedObservable: () => isObservableValue,
  isComputed: () => isComputed,
  isComputedProp: () => isComputedProp,
  isFlow: () => isFlow,
  isFlowCancellationError: () => isFlowCancellationError,
  isObservable: () => isObservable,
  isObservableArray: () => isObservableArray,
  isObservableMap: () => isObservableMap,
  isObservableObject: () => isObservableObject,
  isObservableProp: () => isObservableProp,
  isObservableSet: () => isObservableSet,
  keys: () => keys,
  makeAutoObservable: () => makeAutoObservable,
  makeObservable: () => makeObservable,
  observable: () => observable,
  observe: () => observe,
  onBecomeObserved: () => onBecomeObserved,
  onBecomeUnobserved: () => onBecomeUnobserved,
  onReactionError: () => onReactionError,
  override: () => override,
  ownKeys: () => apiOwnKeys,
  reaction: () => reaction,
  remove: () => remove,
  runInAction: () => runInAction,
  set: () => set3,
  spy: () => spy,
  toJS: () => toJS2,
  trace: () => trace,
  transaction: () => transaction,
  untracked: () => untracked,
  values: () => values,
  when: () => when
});
function die(error) {
  for (var _len = arguments.length, args = new Array(_len > 1 ? _len - 1 : 0), _key = 1; _key < _len; _key++) {
    args[_key - 1] = arguments[_key];
  }
  if (true) {
    var e = typeof error === "string" ? error : errors[error];
    if (typeof e === "function") e = e.apply(null, args);
    throw new Error("[MobX] " + e);
  }
  throw new Error(typeof error === "number" ? "[MobX] minified error nr: " + error + (args.length ? " " + args.map(String).join(",") : "") + ". Find the full error at: https://github.com/mobxjs/mobx/blob/main/packages/mobx/src/errors.ts" : "[MobX] " + error);
}
function getGlobal() {
  if (typeof globalThis !== "undefined") {
    return globalThis;
  }
  if (typeof window !== "undefined") {
    return window;
  }
  if (typeof global !== "undefined") {
    return global;
  }
  if (typeof self !== "undefined") {
    return self;
  }
  return mockGlobal;
}
function assertProxies() {
  if (!hasProxy) {
    die(true ? "`Proxy` objects are not available in the current environment. Please configure MobX to enable a fallback implementation.`" : "Proxy not available");
  }
}
function warnAboutProxyRequirement(msg) {
  if (globalState.verifyProxies) {
    die("MobX is currently configured to be able to run in ES5 mode, but in ES5 MobX won't be able to " + msg);
  }
}
function getNextId() {
  return ++globalState.mobxGuid;
}
function once(func) {
  var invoked = false;
  return function() {
    if (invoked) {
      return;
    }
    invoked = true;
    return func.apply(this, arguments);
  };
}
function isFunction(fn) {
  return typeof fn === "function";
}
function isStringish(value) {
  var t = typeof value;
  switch (t) {
    case "string":
    case "symbol":
    case "number":
      return true;
  }
  return false;
}
function isObject(value) {
  return value !== null && typeof value === "object";
}
function isPlainObject(value) {
  if (!isObject(value)) {
    return false;
  }
  var proto = Object.getPrototypeOf(value);
  if (proto == null) {
    return true;
  }
  var protoConstructor = Object.hasOwnProperty.call(proto, "constructor") && proto.constructor;
  return typeof protoConstructor === "function" && protoConstructor.toString() === plainObjectString;
}
function isGenerator(obj) {
  var constructor = obj == null ? void 0 : obj.constructor;
  if (!constructor) {
    return false;
  }
  if ("GeneratorFunction" === constructor.name || "GeneratorFunction" === constructor.displayName) {
    return true;
  }
  return false;
}
function addHiddenProp(object2, propName, value) {
  defineProperty(object2, propName, {
    enumerable: false,
    writable: true,
    configurable: true,
    value
  });
}
function addHiddenFinalProp(object2, propName, value) {
  defineProperty(object2, propName, {
    enumerable: false,
    writable: false,
    configurable: true,
    value
  });
}
function createInstanceofPredicate(name, theClass) {
  var propName = "isMobX" + name;
  theClass.prototype[propName] = true;
  return function(x2) {
    return isObject(x2) && x2[propName] === true;
  };
}
function isES6Map(thing) {
  return thing != null && Object.prototype.toString.call(thing) === "[object Map]";
}
function isPlainES6Map(thing) {
  var mapProto = Object.getPrototypeOf(thing);
  var objectProto = Object.getPrototypeOf(mapProto);
  var nullProto = Object.getPrototypeOf(objectProto);
  return nullProto === null;
}
function isES6Set(thing) {
  return thing != null && Object.prototype.toString.call(thing) === "[object Set]";
}
function getPlainObjectKeys(object2) {
  var keys2 = Object.keys(object2);
  if (!hasGetOwnPropertySymbols) {
    return keys2;
  }
  var symbols = Object.getOwnPropertySymbols(object2);
  if (!symbols.length) {
    return keys2;
  }
  return [].concat(keys2, symbols.filter(function(s2) {
    return objectPrototype.propertyIsEnumerable.call(object2, s2);
  }));
}
function stringifyKey2(key2) {
  if (typeof key2 === "string") {
    return key2;
  }
  if (typeof key2 === "symbol") {
    return key2.toString();
  }
  return new String(key2).toString();
}
function toPrimitive(value) {
  return value === null ? null : typeof value === "object" ? "" + value : value;
}
function hasProp(target, prop) {
  return objectPrototype.hasOwnProperty.call(target, prop);
}
function getFlag(flags, mask) {
  return !!(flags & mask);
}
function setFlag(flags, mask, newValue) {
  if (newValue) {
    flags |= mask;
  } else {
    flags &= ~mask;
  }
  return flags;
}
function _arrayLikeToArray2(r2, a2) {
  (null == a2 || a2 > r2.length) && (a2 = r2.length);
  for (var e = 0, n = Array(a2); e < a2; e++) n[e] = r2[e];
  return n;
}
function _defineProperties(e, r2) {
  for (var t = 0; t < r2.length; t++) {
    var o2 = r2[t];
    o2.enumerable = o2.enumerable || false, o2.configurable = true, "value" in o2 && (o2.writable = true), Object.defineProperty(e, _toPropertyKey(o2.key), o2);
  }
}
function _createClass(e, r2, t) {
  return r2 && _defineProperties(e.prototype, r2), t && _defineProperties(e, t), Object.defineProperty(e, "prototype", {
    writable: false
  }), e;
}
function _createForOfIteratorHelperLoose2(r2, e) {
  var t = "undefined" != typeof Symbol && r2[Symbol.iterator] || r2["@@iterator"];
  if (t) return (t = t.call(r2)).next.bind(t);
  if (Array.isArray(r2) || (t = _unsupportedIterableToArray2(r2)) || e && r2 && "number" == typeof r2.length) {
    t && (r2 = t);
    var o2 = 0;
    return function() {
      return o2 >= r2.length ? {
        done: true
      } : {
        done: false,
        value: r2[o2++]
      };
    };
  }
  throw new TypeError("Invalid attempt to iterate non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
}
function _extends2() {
  return _extends2 = Object.assign ? Object.assign.bind() : function(n) {
    for (var e = 1; e < arguments.length; e++) {
      var t = arguments[e];
      for (var r2 in t) ({}).hasOwnProperty.call(t, r2) && (n[r2] = t[r2]);
    }
    return n;
  }, _extends2.apply(null, arguments);
}
function _inheritsLoose2(t, o2) {
  t.prototype = Object.create(o2.prototype), t.prototype.constructor = t, _setPrototypeOf2(t, o2);
}
function _setPrototypeOf2(t, e) {
  return _setPrototypeOf2 = Object.setPrototypeOf ? Object.setPrototypeOf.bind() : function(t2, e2) {
    return t2.__proto__ = e2, t2;
  }, _setPrototypeOf2(t, e);
}
function _toPrimitive(t, r2) {
  if ("object" != typeof t || !t) return t;
  var e = t[Symbol.toPrimitive];
  if (void 0 !== e) {
    var i2 = e.call(t, r2 || "default");
    if ("object" != typeof i2) return i2;
    throw new TypeError("@@toPrimitive must return a primitive value.");
  }
  return ("string" === r2 ? String : Number)(t);
}
function _toPropertyKey(t) {
  var i2 = _toPrimitive(t, "string");
  return "symbol" == typeof i2 ? i2 : i2 + "";
}
function _unsupportedIterableToArray2(r2, a2) {
  if (r2) {
    if ("string" == typeof r2) return _arrayLikeToArray2(r2, a2);
    var t = {}.toString.call(r2).slice(8, -1);
    return "Object" === t && r2.constructor && (t = r2.constructor.name), "Map" === t || "Set" === t ? Array.from(r2) : "Arguments" === t || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(t) ? _arrayLikeToArray2(r2, a2) : void 0;
  }
}
function createDecoratorAnnotation(annotation) {
  function decorator(target, property) {
    if (is20223Decorator(property)) {
      return annotation.decorate_20223_(target, property);
    } else {
      storeAnnotation(target, property, annotation);
    }
  }
  return Object.assign(decorator, annotation);
}
function storeAnnotation(prototype, key2, annotation) {
  if (!hasProp(prototype, storedAnnotationsSymbol)) {
    addHiddenProp(prototype, storedAnnotationsSymbol, _extends2({}, prototype[storedAnnotationsSymbol]));
  }
  if (isOverride(annotation) && !hasProp(prototype[storedAnnotationsSymbol], key2)) {
    var fieldName = prototype.constructor.name + ".prototype." + key2.toString();
    die("'" + fieldName + "' is decorated with 'override', but no such decorated member was found on prototype.");
  }
  assertNotDecorated(prototype, annotation, key2);
  if (!isOverride(annotation)) {
    prototype[storedAnnotationsSymbol][key2] = annotation;
  }
}
function assertNotDecorated(prototype, annotation, key2) {
  if (!isOverride(annotation) && hasProp(prototype[storedAnnotationsSymbol], key2)) {
    var fieldName = prototype.constructor.name + ".prototype." + key2.toString();
    var currentAnnotationType = prototype[storedAnnotationsSymbol][key2].annotationType_;
    var requestedAnnotationType = annotation.annotationType_;
    die("Cannot apply '@" + requestedAnnotationType + "' to '" + fieldName + "':" + ("\nThe field is already decorated with '@" + currentAnnotationType + "'.") + "\nRe-decorating fields is not allowed.\nUse '@override' decorator for methods overridden by subclass.");
  }
}
function collectStoredAnnotations(target) {
  if (!hasProp(target, storedAnnotationsSymbol)) {
    addHiddenProp(target, storedAnnotationsSymbol, _extends2({}, target[storedAnnotationsSymbol]));
  }
  return target[storedAnnotationsSymbol];
}
function is20223Decorator(context) {
  return typeof context == "object" && typeof context["kind"] == "string";
}
function assert20223DecoratorType(context, types) {
  if (!types.includes(context.kind)) {
    die("The decorator applied to '" + String(context.name) + "' cannot be used on a " + context.kind + " element");
  }
}
function createAtom(name, onBecomeObservedHandler, onBecomeUnobservedHandler) {
  if (onBecomeObservedHandler === void 0) {
    onBecomeObservedHandler = noop;
  }
  if (onBecomeUnobservedHandler === void 0) {
    onBecomeUnobservedHandler = noop;
  }
  var atom = new Atom(name);
  if (onBecomeObservedHandler !== noop) {
    onBecomeObserved(atom, onBecomeObservedHandler);
  }
  if (onBecomeUnobservedHandler !== noop) {
    onBecomeUnobserved(atom, onBecomeUnobservedHandler);
  }
  return atom;
}
function identityComparer(a2, b2) {
  return a2 === b2;
}
function structuralComparer(a2, b2) {
  return deepEqual(a2, b2);
}
function shallowComparer(a2, b2) {
  return deepEqual(a2, b2, 1);
}
function defaultComparer(a2, b2) {
  if (Object.is) {
    return Object.is(a2, b2);
  }
  return a2 === b2 ? a2 !== 0 || 1 / a2 === 1 / b2 : a2 !== a2 && b2 !== b2;
}
function deepEnhancer(v2, _15, name) {
  if (isObservable(v2)) {
    return v2;
  }
  if (Array.isArray(v2)) {
    return observable.array(v2, {
      name
    });
  }
  if (isPlainObject(v2)) {
    return observable.object(v2, void 0, {
      name
    });
  }
  if (isES6Map(v2)) {
    return observable.map(v2, {
      name
    });
  }
  if (isES6Set(v2)) {
    return observable.set(v2, {
      name
    });
  }
  if (typeof v2 === "function" && !isAction(v2) && !isFlow(v2)) {
    if (isGenerator(v2)) {
      return flow(v2);
    } else {
      return autoAction(name, v2);
    }
  }
  return v2;
}
function shallowEnhancer(v2, _15, name) {
  if (v2 === void 0 || v2 === null) {
    return v2;
  }
  if (isObservableObject(v2) || isObservableArray(v2) || isObservableMap(v2) || isObservableSet(v2)) {
    return v2;
  }
  if (Array.isArray(v2)) {
    return observable.array(v2, {
      name,
      deep: false
    });
  }
  if (isPlainObject(v2)) {
    return observable.object(v2, void 0, {
      name,
      deep: false
    });
  }
  if (isES6Map(v2)) {
    return observable.map(v2, {
      name,
      deep: false
    });
  }
  if (isES6Set(v2)) {
    return observable.set(v2, {
      name,
      deep: false
    });
  }
  if (true) {
    die("The shallow modifier / decorator can only used in combination with arrays, objects, maps and sets");
  }
}
function referenceEnhancer(newValue) {
  return newValue;
}
function refStructEnhancer(v2, oldValue) {
  if (isObservable(v2)) {
    die("observable.struct should not be used with observable values");
  }
  if (deepEqual(v2, oldValue)) {
    return oldValue;
  }
  return v2;
}
function isOverride(annotation) {
  return annotation.annotationType_ === OVERRIDE;
}
function make_(adm, key2) {
  if (adm.isPlainObject_) {
    die("Cannot apply '" + this.annotationType_ + "' to '" + adm.name_ + "." + key2.toString() + "':" + ("\n'" + this.annotationType_ + "' cannot be used on plain objects."));
  }
  if (!hasProp(adm.appliedAnnotations_, key2)) {
    die("'" + adm.name_ + "." + key2.toString() + "' is annotated with '" + this.annotationType_ + "', but no such annotated member was found on prototype.");
  }
  return 0;
}
function extend_(adm, key2, descriptor, proxyTrap) {
  die("'" + this.annotationType_ + "' can only be used with 'makeObservable'");
}
function decorate_20223_(desc, context) {
  console.warn("'" + this.annotationType_ + "' cannot be used with decorators - this is a no-op");
}
function createActionAnnotation(name, options) {
  return {
    annotationType_: name,
    options_: options,
    make_: make_$1,
    extend_: extend_$1,
    decorate_20223_: decorate_20223_$1
  };
}
function make_$1(adm, key2, descriptor, source) {
  var _this$options_;
  if ((_this$options_ = this.options_) != null && _this$options_.bound) {
    return this.extend_(adm, key2, descriptor, false) === null ? 0 : 1;
  }
  if (source === adm.target_) {
    return this.extend_(adm, key2, descriptor, false) === null ? 0 : 2;
  }
  if (isAction(descriptor.value)) {
    return 1;
  }
  var actionDescriptor = createActionDescriptor(adm, this, key2, descriptor, false);
  defineProperty(source, key2, actionDescriptor);
  return 2;
}
function extend_$1(adm, key2, descriptor, proxyTrap) {
  var actionDescriptor = createActionDescriptor(adm, this, key2, descriptor);
  return adm.defineProperty_(key2, actionDescriptor, proxyTrap);
}
function decorate_20223_$1(mthd, context) {
  if (true) {
    assert20223DecoratorType(context, ["method", "field"]);
  }
  var kind = context.kind, name = context.name, addInitializer = context.addInitializer;
  var ann = this;
  var _createAction = function _createAction2(m2) {
    var _ann$options_$name, _ann$options_, _ann$options_$autoAct, _ann$options_2;
    return createAction((_ann$options_$name = (_ann$options_ = ann.options_) == null ? void 0 : _ann$options_.name) != null ? _ann$options_$name : name.toString(), m2, (_ann$options_$autoAct = (_ann$options_2 = ann.options_) == null ? void 0 : _ann$options_2.autoAction) != null ? _ann$options_$autoAct : false);
  };
  if (kind == "field") {
    return function(initMthd) {
      var _ann$options_3;
      var mthd2 = initMthd;
      if (!isAction(mthd2)) {
        mthd2 = _createAction(mthd2);
      }
      if ((_ann$options_3 = ann.options_) != null && _ann$options_3.bound) {
        mthd2 = mthd2.bind(this);
        mthd2.isMobxAction = true;
      }
      return mthd2;
    };
  }
  if (kind == "method") {
    var _this$options_2;
    if (!isAction(mthd)) {
      mthd = _createAction(mthd);
    }
    if ((_this$options_2 = this.options_) != null && _this$options_2.bound) {
      addInitializer(function() {
        var self2 = this;
        var bound = self2[name].bind(self2);
        bound.isMobxAction = true;
        self2[name] = bound;
      });
    }
    return mthd;
  }
  die("Cannot apply '" + ann.annotationType_ + "' to '" + String(name) + "' (kind: " + kind + "):" + ("\n'" + ann.annotationType_ + "' can only be used on properties with a function value."));
}
function assertActionDescriptor(adm, _ref, key2, _ref2) {
  var annotationType_ = _ref.annotationType_;
  var value = _ref2.value;
  if (!isFunction(value)) {
    die("Cannot apply '" + annotationType_ + "' to '" + adm.name_ + "." + key2.toString() + "':" + ("\n'" + annotationType_ + "' can only be used on properties with a function value."));
  }
}
function createActionDescriptor(adm, annotation, key2, descriptor, safeDescriptors) {
  var _annotation$options_, _annotation$options_$, _annotation$options_2, _annotation$options_$2, _annotation$options_3, _annotation$options_4, _adm$proxy_2;
  if (safeDescriptors === void 0) {
    safeDescriptors = globalState.safeDescriptors;
  }
  assertActionDescriptor(adm, annotation, key2, descriptor);
  var value = descriptor.value;
  if ((_annotation$options_ = annotation.options_) != null && _annotation$options_.bound) {
    var _adm$proxy_;
    value = value.bind((_adm$proxy_ = adm.proxy_) != null ? _adm$proxy_ : adm.target_);
  }
  return {
    value: createAction(
      (_annotation$options_$ = (_annotation$options_2 = annotation.options_) == null ? void 0 : _annotation$options_2.name) != null ? _annotation$options_$ : key2.toString(),
      value,
      (_annotation$options_$2 = (_annotation$options_3 = annotation.options_) == null ? void 0 : _annotation$options_3.autoAction) != null ? _annotation$options_$2 : false,
      // https://github.com/mobxjs/mobx/discussions/3140
      (_annotation$options_4 = annotation.options_) != null && _annotation$options_4.bound ? (_adm$proxy_2 = adm.proxy_) != null ? _adm$proxy_2 : adm.target_ : void 0
    ),
    // Non-configurable for classes
    // prevents accidental field redefinition in subclass
    configurable: safeDescriptors ? adm.isPlainObject_ : true,
    // https://github.com/mobxjs/mobx/pull/2641#issuecomment-737292058
    enumerable: false,
    // Non-obsevable, therefore non-writable
    // Also prevents rewriting in subclass constructor
    writable: safeDescriptors ? false : true
  };
}
function createFlowAnnotation(name, options) {
  return {
    annotationType_: name,
    options_: options,
    make_: make_$2,
    extend_: extend_$2,
    decorate_20223_: decorate_20223_$2
  };
}
function make_$2(adm, key2, descriptor, source) {
  var _this$options_;
  if (source === adm.target_) {
    return this.extend_(adm, key2, descriptor, false) === null ? 0 : 2;
  }
  if ((_this$options_ = this.options_) != null && _this$options_.bound && (!hasProp(adm.target_, key2) || !isFlow(adm.target_[key2]))) {
    if (this.extend_(adm, key2, descriptor, false) === null) {
      return 0;
    }
  }
  if (isFlow(descriptor.value)) {
    return 1;
  }
  var flowDescriptor = createFlowDescriptor(adm, this, key2, descriptor, false, false);
  defineProperty(source, key2, flowDescriptor);
  return 2;
}
function extend_$2(adm, key2, descriptor, proxyTrap) {
  var _this$options_2;
  var flowDescriptor = createFlowDescriptor(adm, this, key2, descriptor, (_this$options_2 = this.options_) == null ? void 0 : _this$options_2.bound);
  return adm.defineProperty_(key2, flowDescriptor, proxyTrap);
}
function decorate_20223_$2(mthd, context) {
  var _this$options_3;
  if (true) {
    assert20223DecoratorType(context, ["method"]);
  }
  var name = context.name, addInitializer = context.addInitializer;
  if (!isFlow(mthd)) {
    mthd = flow(mthd);
  }
  if ((_this$options_3 = this.options_) != null && _this$options_3.bound) {
    addInitializer(function() {
      var self2 = this;
      var bound = self2[name].bind(self2);
      bound.isMobXFlow = true;
      self2[name] = bound;
    });
  }
  return mthd;
}
function assertFlowDescriptor(adm, _ref, key2, _ref2) {
  var annotationType_ = _ref.annotationType_;
  var value = _ref2.value;
  if (!isFunction(value)) {
    die("Cannot apply '" + annotationType_ + "' to '" + adm.name_ + "." + key2.toString() + "':" + ("\n'" + annotationType_ + "' can only be used on properties with a generator function value."));
  }
}
function createFlowDescriptor(adm, annotation, key2, descriptor, bound, safeDescriptors) {
  if (safeDescriptors === void 0) {
    safeDescriptors = globalState.safeDescriptors;
  }
  assertFlowDescriptor(adm, annotation, key2, descriptor);
  var value = descriptor.value;
  if (!isFlow(value)) {
    value = flow(value);
  }
  if (bound) {
    var _adm$proxy_;
    value = value.bind((_adm$proxy_ = adm.proxy_) != null ? _adm$proxy_ : adm.target_);
    value.isMobXFlow = true;
  }
  return {
    value,
    // Non-configurable for classes
    // prevents accidental field redefinition in subclass
    configurable: safeDescriptors ? adm.isPlainObject_ : true,
    // https://github.com/mobxjs/mobx/pull/2641#issuecomment-737292058
    enumerable: false,
    // Non-obsevable, therefore non-writable
    // Also prevents rewriting in subclass constructor
    writable: safeDescriptors ? false : true
  };
}
function createComputedAnnotation(name, options) {
  return {
    annotationType_: name,
    options_: options,
    make_: make_$3,
    extend_: extend_$3,
    decorate_20223_: decorate_20223_$3
  };
}
function make_$3(adm, key2, descriptor) {
  return this.extend_(adm, key2, descriptor, false) === null ? 0 : 1;
}
function extend_$3(adm, key2, descriptor, proxyTrap) {
  assertComputedDescriptor(adm, this, key2, descriptor);
  return adm.defineComputedProperty_(key2, _extends2({}, this.options_, {
    get: descriptor.get,
    set: descriptor.set
  }), proxyTrap);
}
function decorate_20223_$3(get5, context) {
  if (true) {
    assert20223DecoratorType(context, ["getter"]);
  }
  var ann = this;
  var key2 = context.name, addInitializer = context.addInitializer;
  addInitializer(function() {
    var adm = asObservableObject(this)[$mobx];
    var options = _extends2({}, ann.options_, {
      get: get5,
      context: this
    });
    options.name || (options.name = true ? adm.name_ + "." + key2.toString() : "ObservableObject." + key2.toString());
    adm.values_.set(key2, new ComputedValue(options));
  });
  return function() {
    return this[$mobx].getObservablePropValue_(key2);
  };
}
function assertComputedDescriptor(adm, _ref, key2, _ref2) {
  var annotationType_ = _ref.annotationType_;
  var get5 = _ref2.get;
  if (!get5) {
    die("Cannot apply '" + annotationType_ + "' to '" + adm.name_ + "." + key2.toString() + "':" + ("\n'" + annotationType_ + "' can only be used on getter(+setter) properties."));
  }
}
function createObservableAnnotation(name, options) {
  return {
    annotationType_: name,
    options_: options,
    make_: make_$4,
    extend_: extend_$4,
    decorate_20223_: decorate_20223_$4
  };
}
function make_$4(adm, key2, descriptor) {
  return this.extend_(adm, key2, descriptor, false) === null ? 0 : 1;
}
function extend_$4(adm, key2, descriptor, proxyTrap) {
  var _this$options_$enhanc, _this$options_;
  assertObservableDescriptor(adm, this, key2, descriptor);
  return adm.defineObservableProperty_(key2, descriptor.value, (_this$options_$enhanc = (_this$options_ = this.options_) == null ? void 0 : _this$options_.enhancer) != null ? _this$options_$enhanc : deepEnhancer, proxyTrap);
}
function decorate_20223_$4(desc, context) {
  if (true) {
    if (context.kind === "field") {
      throw die("Please use `@observable accessor " + String(context.name) + "` instead of `@observable " + String(context.name) + "`");
    }
    assert20223DecoratorType(context, ["accessor"]);
  }
  var ann = this;
  var kind = context.kind, name = context.name;
  var initializedObjects = /* @__PURE__ */ new WeakSet();
  function initializeObservable(target, value) {
    var _ann$options_$enhance, _ann$options_;
    var adm = asObservableObject(target)[$mobx];
    var observable2 = new ObservableValue(value, (_ann$options_$enhance = (_ann$options_ = ann.options_) == null ? void 0 : _ann$options_.enhancer) != null ? _ann$options_$enhance : deepEnhancer, true ? adm.name_ + "." + name.toString() : "ObservableObject." + name.toString(), false);
    adm.values_.set(name, observable2);
    initializedObjects.add(target);
  }
  if (kind == "accessor") {
    return {
      get: function get5() {
        if (!initializedObjects.has(this)) {
          initializeObservable(this, desc.get.call(this));
        }
        return this[$mobx].getObservablePropValue_(name);
      },
      set: function set7(value) {
        if (!initializedObjects.has(this)) {
          initializeObservable(this, value);
        }
        return this[$mobx].setObservablePropValue_(name, value);
      },
      init: function init(value) {
        if (!initializedObjects.has(this)) {
          initializeObservable(this, value);
        }
        return value;
      }
    };
  }
  return;
}
function assertObservableDescriptor(adm, _ref, key2, descriptor) {
  var annotationType_ = _ref.annotationType_;
  if (!("value" in descriptor)) {
    die("Cannot apply '" + annotationType_ + "' to '" + adm.name_ + "." + key2.toString() + "':" + ("\n'" + annotationType_ + "' cannot be used on getter/setter properties"));
  }
}
function createAutoAnnotation(options) {
  return {
    annotationType_: AUTO,
    options_: options,
    make_: make_$5,
    extend_: extend_$5,
    decorate_20223_: decorate_20223_$5
  };
}
function make_$5(adm, key2, descriptor, source) {
  var _this$options_3, _this$options_4;
  if (descriptor.get) {
    return computed.make_(adm, key2, descriptor, source);
  }
  if (descriptor.set) {
    var set7 = isAction(descriptor.set) ? descriptor.set : createAction(key2.toString(), descriptor.set);
    if (source === adm.target_) {
      return adm.defineProperty_(key2, {
        configurable: globalState.safeDescriptors ? adm.isPlainObject_ : true,
        set: set7
      }) === null ? 0 : 2;
    }
    defineProperty(source, key2, {
      configurable: true,
      set: set7
    });
    return 2;
  }
  if (source !== adm.target_ && typeof descriptor.value === "function") {
    var _this$options_2;
    if (isGenerator(descriptor.value)) {
      var _this$options_;
      var flowAnnotation2 = (_this$options_ = this.options_) != null && _this$options_.autoBind ? flow.bound : flow;
      return flowAnnotation2.make_(adm, key2, descriptor, source);
    }
    var actionAnnotation2 = (_this$options_2 = this.options_) != null && _this$options_2.autoBind ? autoAction.bound : autoAction;
    return actionAnnotation2.make_(adm, key2, descriptor, source);
  }
  var observableAnnotation2 = ((_this$options_3 = this.options_) == null ? void 0 : _this$options_3.deep) === false ? observable.ref : observable;
  if (typeof descriptor.value === "function" && (_this$options_4 = this.options_) != null && _this$options_4.autoBind) {
    var _adm$proxy_;
    descriptor.value = descriptor.value.bind((_adm$proxy_ = adm.proxy_) != null ? _adm$proxy_ : adm.target_);
  }
  return observableAnnotation2.make_(adm, key2, descriptor, source);
}
function extend_$5(adm, key2, descriptor, proxyTrap) {
  var _this$options_5, _this$options_6;
  if (descriptor.get) {
    return computed.extend_(adm, key2, descriptor, proxyTrap);
  }
  if (descriptor.set) {
    return adm.defineProperty_(key2, {
      configurable: globalState.safeDescriptors ? adm.isPlainObject_ : true,
      set: createAction(key2.toString(), descriptor.set)
    }, proxyTrap);
  }
  if (typeof descriptor.value === "function" && (_this$options_5 = this.options_) != null && _this$options_5.autoBind) {
    var _adm$proxy_2;
    descriptor.value = descriptor.value.bind((_adm$proxy_2 = adm.proxy_) != null ? _adm$proxy_2 : adm.target_);
  }
  var observableAnnotation2 = ((_this$options_6 = this.options_) == null ? void 0 : _this$options_6.deep) === false ? observable.ref : observable;
  return observableAnnotation2.extend_(adm, key2, descriptor, proxyTrap);
}
function decorate_20223_$5(desc, context) {
  die("'" + this.annotationType_ + "' cannot be used as a decorator");
}
function asCreateObservableOptions(thing) {
  return thing || defaultCreateObservableOptions;
}
function getEnhancerFromOptions(options) {
  return options.deep === true ? deepEnhancer : options.deep === false ? referenceEnhancer : getEnhancerFromAnnotation(options.defaultDecorator);
}
function getAnnotationFromOptions(options) {
  var _options$defaultDecor;
  return options ? (_options$defaultDecor = options.defaultDecorator) != null ? _options$defaultDecor : createAutoAnnotation(options) : void 0;
}
function getEnhancerFromAnnotation(annotation) {
  var _annotation$options_$, _annotation$options_;
  return !annotation ? deepEnhancer : (_annotation$options_$ = (_annotation$options_ = annotation.options_) == null ? void 0 : _annotation$options_.enhancer) != null ? _annotation$options_$ : deepEnhancer;
}
function createObservable(v2, arg2, arg3) {
  if (is20223Decorator(arg2)) {
    return observableAnnotation.decorate_20223_(v2, arg2);
  }
  if (isStringish(arg2)) {
    storeAnnotation(v2, arg2, observableAnnotation);
    return;
  }
  if (isObservable(v2)) {
    return v2;
  }
  if (isPlainObject(v2)) {
    return observable.object(v2, arg2, arg3);
  }
  if (Array.isArray(v2)) {
    return observable.array(v2, arg2);
  }
  if (isES6Map(v2)) {
    return observable.map(v2, arg2);
  }
  if (isES6Set(v2)) {
    return observable.set(v2, arg2);
  }
  if (typeof v2 === "object" && v2 !== null) {
    return v2;
  }
  return observable.box(v2, arg2);
}
function createAction(actionName, fn, autoAction2, ref) {
  if (autoAction2 === void 0) {
    autoAction2 = false;
  }
  if (true) {
    if (!isFunction(fn)) {
      die("`action` can only be invoked on functions");
    }
    if (typeof actionName !== "string" || !actionName) {
      die("actions should have valid names, got: '" + actionName + "'");
    }
  }
  function res() {
    return executeAction(actionName, autoAction2, fn, ref || this, arguments);
  }
  res.isMobxAction = true;
  res.toString = function() {
    return fn.toString();
  };
  if (isFunctionNameConfigurable) {
    tmpNameDescriptor.value = actionName;
    defineProperty(res, "name", tmpNameDescriptor);
  }
  return res;
}
function executeAction(actionName, canRunAsDerivation, fn, scope, args) {
  var runInfo = _startAction(actionName, canRunAsDerivation, scope, args);
  try {
    return fn.apply(scope, args);
  } catch (err) {
    runInfo.error_ = err;
    throw err;
  } finally {
    _endAction(runInfo);
  }
}
function _startAction(actionName, canRunAsDerivation, scope, args) {
  var notifySpy_ = isSpyEnabled() && !!actionName;
  var startTime_ = 0;
  if (notifySpy_) {
    startTime_ = Date.now();
    var flattenedArgs = args ? Array.from(args) : EMPTY_ARRAY;
    spyReportStart({
      type: ACTION,
      name: actionName,
      object: scope,
      arguments: flattenedArgs
    });
  }
  var prevDerivation_ = globalState.trackingDerivation;
  var runAsAction = !canRunAsDerivation || !prevDerivation_;
  startBatch();
  var prevAllowStateChanges_ = globalState.allowStateChanges;
  if (runAsAction) {
    untrackedStart();
    prevAllowStateChanges_ = allowStateChangesStart(true);
  }
  var prevAllowStateReads_ = allowStateReadsStart(true);
  var runInfo = {
    runAsAction_: runAsAction,
    prevDerivation_,
    prevAllowStateChanges_,
    prevAllowStateReads_,
    notifySpy_,
    startTime_,
    actionId_: nextActionId++,
    parentActionId_: currentActionId
  };
  currentActionId = runInfo.actionId_;
  return runInfo;
}
function _endAction(runInfo) {
  if (currentActionId !== runInfo.actionId_) {
    die(30);
  }
  currentActionId = runInfo.parentActionId_;
  if (runInfo.error_ !== void 0) {
    globalState.suppressReactionErrors = true;
  }
  allowStateChangesEnd(runInfo.prevAllowStateChanges_);
  allowStateReadsEnd(runInfo.prevAllowStateReads_);
  endBatch();
  if (runInfo.runAsAction_) {
    untrackedEnd(runInfo.prevDerivation_);
  }
  if (runInfo.notifySpy_) {
    spyReportEnd({
      time: Date.now() - runInfo.startTime_
    });
  }
  globalState.suppressReactionErrors = false;
}
function allowStateChanges(allowStateChanges2, func) {
  var prev2 = allowStateChangesStart(allowStateChanges2);
  try {
    return func();
  } finally {
    allowStateChangesEnd(prev2);
  }
}
function allowStateChangesStart(allowStateChanges2) {
  var prev2 = globalState.allowStateChanges;
  globalState.allowStateChanges = allowStateChanges2;
  return prev2;
}
function allowStateChangesEnd(prev2) {
  globalState.allowStateChanges = prev2;
}
function isCaughtException(e) {
  return e instanceof CaughtException;
}
function shouldCompute(derivation) {
  switch (derivation.dependenciesState_) {
    case IDerivationState_.UP_TO_DATE_:
      return false;
    case IDerivationState_.NOT_TRACKING_:
    case IDerivationState_.STALE_:
      return true;
    case IDerivationState_.POSSIBLY_STALE_: {
      var prevAllowStateReads = allowStateReadsStart(true);
      var prevUntracked = untrackedStart();
      var obs = derivation.observing_, l2 = obs.length;
      for (var i2 = 0; i2 < l2; i2++) {
        var obj = obs[i2];
        if (isComputedValue(obj)) {
          if (globalState.disableErrorBoundaries) {
            obj.get();
          } else {
            try {
              obj.get();
            } catch (e) {
              untrackedEnd(prevUntracked);
              allowStateReadsEnd(prevAllowStateReads);
              return true;
            }
          }
          if (derivation.dependenciesState_ === IDerivationState_.STALE_) {
            untrackedEnd(prevUntracked);
            allowStateReadsEnd(prevAllowStateReads);
            return true;
          }
        }
      }
      changeDependenciesStateTo0(derivation);
      untrackedEnd(prevUntracked);
      allowStateReadsEnd(prevAllowStateReads);
      return false;
    }
  }
}
function isComputingDerivation() {
  return globalState.trackingDerivation !== null;
}
function checkIfStateModificationsAreAllowed(atom) {
  if (false) {
    return;
  }
  var hasObservers2 = atom.observers_.size > 0;
  if (!globalState.allowStateChanges && (hasObservers2 || globalState.enforceActions === "always")) {
    console.warn("[MobX] " + (globalState.enforceActions ? "Since strict-mode is enabled, changing (observed) observable values without using an action is not allowed. Tried to modify: " : "Side effects like changing state are not allowed at this point. Are you trying to modify state from, for example, a computed value or the render function of a React component? You can wrap side effects in 'runInAction' (or decorate functions with 'action') if needed. Tried to modify: ") + atom.name_);
  }
}
function checkIfStateReadsAreAllowed(observable2) {
  if (!globalState.allowStateReads && globalState.observableRequiresReaction) {
    console.warn("[mobx] Observable '" + observable2.name_ + "' being read outside a reactive context.");
  }
}
function trackDerivedFunction(derivation, f2, context) {
  var prevAllowStateReads = allowStateReadsStart(true);
  changeDependenciesStateTo0(derivation);
  derivation.newObserving_ = new Array(
    // Reserve constant space for initial dependencies, dynamic space otherwise.
    // See https://github.com/mobxjs/mobx/pull/3833
    derivation.runId_ === 0 ? 100 : derivation.observing_.length
  );
  derivation.unboundDepsCount_ = 0;
  derivation.runId_ = ++globalState.runId;
  var prevTracking = globalState.trackingDerivation;
  globalState.trackingDerivation = derivation;
  globalState.inBatch++;
  var result;
  if (globalState.disableErrorBoundaries === true) {
    result = f2.call(context);
  } else {
    try {
      result = f2.call(context);
    } catch (e) {
      result = new CaughtException(e);
    }
  }
  globalState.inBatch--;
  globalState.trackingDerivation = prevTracking;
  bindDependencies(derivation);
  warnAboutDerivationWithoutDependencies(derivation);
  allowStateReadsEnd(prevAllowStateReads);
  return result;
}
function warnAboutDerivationWithoutDependencies(derivation) {
  if (false) {
    return;
  }
  if (derivation.observing_.length !== 0) {
    return;
  }
  if (typeof derivation.requiresObservable_ === "boolean" ? derivation.requiresObservable_ : globalState.reactionRequiresObservable) {
    console.warn("[mobx] Derivation '" + derivation.name_ + "' is created/updated without reading any observable value.");
  }
}
function bindDependencies(derivation) {
  var prevObserving = derivation.observing_;
  var observing = derivation.observing_ = derivation.newObserving_;
  var lowestNewObservingDerivationState = IDerivationState_.UP_TO_DATE_;
  var i0 = 0, l2 = derivation.unboundDepsCount_;
  for (var i2 = 0; i2 < l2; i2++) {
    var dep = observing[i2];
    if (dep.diffValue === 0) {
      dep.diffValue = 1;
      if (i0 !== i2) {
        observing[i0] = dep;
      }
      i0++;
    }
    if (dep.dependenciesState_ > lowestNewObservingDerivationState) {
      lowestNewObservingDerivationState = dep.dependenciesState_;
    }
  }
  observing.length = i0;
  derivation.newObserving_ = null;
  l2 = prevObserving.length;
  while (l2--) {
    var _dep = prevObserving[l2];
    if (_dep.diffValue === 0) {
      removeObserver(_dep, derivation);
    }
    _dep.diffValue = 0;
  }
  while (i0--) {
    var _dep2 = observing[i0];
    if (_dep2.diffValue === 1) {
      _dep2.diffValue = 0;
      addObserver(_dep2, derivation);
    }
  }
  if (lowestNewObservingDerivationState !== IDerivationState_.UP_TO_DATE_) {
    derivation.dependenciesState_ = lowestNewObservingDerivationState;
    derivation.onBecomeStale_();
  }
}
function clearObserving(derivation) {
  var obs = derivation.observing_;
  derivation.observing_ = [];
  var i2 = obs.length;
  while (i2--) {
    removeObserver(obs[i2], derivation);
  }
  derivation.dependenciesState_ = IDerivationState_.NOT_TRACKING_;
}
function untracked(action2) {
  var prev2 = untrackedStart();
  try {
    return action2();
  } finally {
    untrackedEnd(prev2);
  }
}
function untrackedStart() {
  var prev2 = globalState.trackingDerivation;
  globalState.trackingDerivation = null;
  return prev2;
}
function untrackedEnd(prev2) {
  globalState.trackingDerivation = prev2;
}
function allowStateReadsStart(allowStateReads) {
  var prev2 = globalState.allowStateReads;
  globalState.allowStateReads = allowStateReads;
  return prev2;
}
function allowStateReadsEnd(prev2) {
  globalState.allowStateReads = prev2;
}
function changeDependenciesStateTo0(derivation) {
  if (derivation.dependenciesState_ === IDerivationState_.UP_TO_DATE_) {
    return;
  }
  derivation.dependenciesState_ = IDerivationState_.UP_TO_DATE_;
  var obs = derivation.observing_;
  var i2 = obs.length;
  while (i2--) {
    obs[i2].lowestObserverState_ = IDerivationState_.UP_TO_DATE_;
  }
}
function isolateGlobalState() {
  if (globalState.pendingReactions.length || globalState.inBatch || globalState.isRunningReactions) {
    die(36);
  }
  isolateCalled = true;
  if (canMergeGlobalState) {
    var global2 = getGlobal();
    if (--global2.__mobxInstanceCount === 0) {
      global2.__mobxGlobals = void 0;
    }
    globalState = new MobXGlobals();
  }
}
function getGlobalState() {
  return globalState;
}
function resetGlobalState() {
  var defaultGlobals = new MobXGlobals();
  for (var key2 in defaultGlobals) {
    if (persistentKeys.indexOf(key2) === -1) {
      globalState[key2] = defaultGlobals[key2];
    }
  }
  globalState.allowStateChanges = !globalState.enforceActions;
}
function hasObservers(observable2) {
  return observable2.observers_ && observable2.observers_.size > 0;
}
function getObservers(observable2) {
  return observable2.observers_;
}
function addObserver(observable2, node2) {
  observable2.observers_.add(node2);
  if (observable2.lowestObserverState_ > node2.dependenciesState_) {
    observable2.lowestObserverState_ = node2.dependenciesState_;
  }
}
function removeObserver(observable2, node2) {
  observable2.observers_["delete"](node2);
  if (observable2.observers_.size === 0) {
    queueForUnobservation(observable2);
  }
}
function queueForUnobservation(observable2) {
  if (observable2.isPendingUnobservation === false) {
    observable2.isPendingUnobservation = true;
    globalState.pendingUnobservations.push(observable2);
  }
}
function startBatch() {
  globalState.inBatch++;
}
function endBatch() {
  if (--globalState.inBatch === 0) {
    runReactions();
    var list = globalState.pendingUnobservations;
    for (var i2 = 0; i2 < list.length; i2++) {
      var observable2 = list[i2];
      observable2.isPendingUnobservation = false;
      if (observable2.observers_.size === 0) {
        if (observable2.isBeingObserved) {
          observable2.isBeingObserved = false;
          observable2.onBUO();
        }
        if (observable2 instanceof ComputedValue) {
          observable2.suspend_();
        }
      }
    }
    globalState.pendingUnobservations = [];
  }
}
function reportObserved(observable2) {
  checkIfStateReadsAreAllowed(observable2);
  var derivation = globalState.trackingDerivation;
  if (derivation !== null) {
    if (derivation.runId_ !== observable2.lastAccessedBy_) {
      observable2.lastAccessedBy_ = derivation.runId_;
      derivation.newObserving_[derivation.unboundDepsCount_++] = observable2;
      if (!observable2.isBeingObserved && globalState.trackingContext) {
        observable2.isBeingObserved = true;
        observable2.onBO();
      }
    }
    return observable2.isBeingObserved;
  } else if (observable2.observers_.size === 0 && globalState.inBatch > 0) {
    queueForUnobservation(observable2);
  }
  return false;
}
function propagateChanged(observable2) {
  if (observable2.lowestObserverState_ === IDerivationState_.STALE_) {
    return;
  }
  observable2.lowestObserverState_ = IDerivationState_.STALE_;
  observable2.observers_.forEach(function(d) {
    if (d.dependenciesState_ === IDerivationState_.UP_TO_DATE_) {
      if (d.isTracing_ !== TraceMode.NONE) {
        logTraceInfo(d, observable2);
      }
      d.onBecomeStale_();
    }
    d.dependenciesState_ = IDerivationState_.STALE_;
  });
}
function propagateChangeConfirmed(observable2) {
  if (observable2.lowestObserverState_ === IDerivationState_.STALE_) {
    return;
  }
  observable2.lowestObserverState_ = IDerivationState_.STALE_;
  observable2.observers_.forEach(function(d) {
    if (d.dependenciesState_ === IDerivationState_.POSSIBLY_STALE_) {
      d.dependenciesState_ = IDerivationState_.STALE_;
      if (d.isTracing_ !== TraceMode.NONE) {
        logTraceInfo(d, observable2);
      }
    } else if (d.dependenciesState_ === IDerivationState_.UP_TO_DATE_) {
      observable2.lowestObserverState_ = IDerivationState_.UP_TO_DATE_;
    }
  });
}
function propagateMaybeChanged(observable2) {
  if (observable2.lowestObserverState_ !== IDerivationState_.UP_TO_DATE_) {
    return;
  }
  observable2.lowestObserverState_ = IDerivationState_.POSSIBLY_STALE_;
  observable2.observers_.forEach(function(d) {
    if (d.dependenciesState_ === IDerivationState_.UP_TO_DATE_) {
      d.dependenciesState_ = IDerivationState_.POSSIBLY_STALE_;
      d.onBecomeStale_();
    }
  });
}
function logTraceInfo(derivation, observable2) {
  console.log("[mobx.trace] '" + derivation.name_ + "' is invalidated due to a change in: '" + observable2.name_ + "'");
  if (derivation.isTracing_ === TraceMode.BREAK) {
    var lines = [];
    printDepTree(getDependencyTree(derivation), lines, 1);
    new Function("debugger;\n/*\nTracing '" + derivation.name_ + "'\n\nYou are entering this break point because derivation '" + derivation.name_ + "' is being traced and '" + observable2.name_ + "' is now forcing it to update.\nJust follow the stacktrace you should now see in the devtools to see precisely what piece of your code is causing this update\nThe stackframe you are looking for is at least ~6-8 stack-frames up.\n\n" + (derivation instanceof ComputedValue ? derivation.derivation.toString().replace(/[*]\//g, "/") : "") + "\n\nThe dependencies for this derivation are:\n\n" + lines.join("\n") + "\n*/\n    ")();
  }
}
function printDepTree(tree, lines, depth) {
  if (lines.length >= 1e3) {
    lines.push("(and many more)");
    return;
  }
  lines.push("" + "	".repeat(depth - 1) + tree.name);
  if (tree.dependencies) {
    tree.dependencies.forEach(function(child) {
      return printDepTree(child, lines, depth + 1);
    });
  }
}
function onReactionError(handler) {
  globalState.globalReactionErrorHandlers.push(handler);
  return function() {
    var idx = globalState.globalReactionErrorHandlers.indexOf(handler);
    if (idx >= 0) {
      globalState.globalReactionErrorHandlers.splice(idx, 1);
    }
  };
}
function runReactions() {
  if (globalState.inBatch > 0 || globalState.isRunningReactions) {
    return;
  }
  reactionScheduler(runReactionsHelper);
}
function runReactionsHelper() {
  globalState.isRunningReactions = true;
  var allReactions = globalState.pendingReactions;
  var iterations = 0;
  while (allReactions.length > 0) {
    if (++iterations === MAX_REACTION_ITERATIONS) {
      console.error(true ? "Reaction doesn't converge to a stable state after " + MAX_REACTION_ITERATIONS + " iterations." + (" Probably there is a cycle in the reactive function: " + allReactions[0]) : "[mobx] cycle in reaction: " + allReactions[0]);
      allReactions.splice(0);
    }
    var remainingReactions = allReactions.splice(0);
    for (var i2 = 0, l2 = remainingReactions.length; i2 < l2; i2++) {
      remainingReactions[i2].runReaction_();
    }
  }
  globalState.isRunningReactions = false;
}
function setReactionScheduler(fn) {
  var baseScheduler = reactionScheduler;
  reactionScheduler = function reactionScheduler3(f2) {
    return fn(function() {
      return baseScheduler(f2);
    });
  };
}
function isSpyEnabled() {
  return !!globalState.spyListeners.length;
}
function spyReport(event) {
  if (false) {
    return;
  }
  if (!globalState.spyListeners.length) {
    return;
  }
  var listeners = globalState.spyListeners;
  for (var i2 = 0, l2 = listeners.length; i2 < l2; i2++) {
    listeners[i2](event);
  }
}
function spyReportStart(event) {
  if (false) {
    return;
  }
  var change = _extends2({}, event, {
    spyReportStart: true
  });
  spyReport(change);
}
function spyReportEnd(change) {
  if (false) {
    return;
  }
  if (change) {
    spyReport(_extends2({}, change, {
      type: "report-end",
      spyReportEnd: true
    }));
  } else {
    spyReport(END_EVENT);
  }
}
function spy(listener) {
  if (false) {
    console.warn("[mobx.spy] Is a no-op in production builds");
    return function() {
    };
  } else {
    globalState.spyListeners.push(listener);
    return once(function() {
      globalState.spyListeners = globalState.spyListeners.filter(function(l2) {
        return l2 !== listener;
      });
    });
  }
}
function createActionFactory(autoAction2) {
  var res = function action2(arg1, arg2) {
    if (isFunction(arg1)) {
      return createAction(arg1.name || DEFAULT_ACTION_NAME, arg1, autoAction2);
    }
    if (isFunction(arg2)) {
      return createAction(arg1, arg2, autoAction2);
    }
    if (is20223Decorator(arg2)) {
      return (autoAction2 ? autoActionAnnotation : actionAnnotation).decorate_20223_(arg1, arg2);
    }
    if (isStringish(arg2)) {
      return storeAnnotation(arg1, arg2, autoAction2 ? autoActionAnnotation : actionAnnotation);
    }
    if (isStringish(arg1)) {
      return createDecoratorAnnotation(createActionAnnotation(autoAction2 ? AUTOACTION : ACTION, {
        name: arg1,
        autoAction: autoAction2
      }));
    }
    if (true) {
      die("Invalid arguments for `action`");
    }
  };
  return res;
}
function runInAction(fn) {
  return executeAction(fn.name || DEFAULT_ACTION_NAME, false, fn, this, void 0);
}
function isAction(thing) {
  return isFunction(thing) && thing.isMobxAction === true;
}
function autorun(view, opts) {
  var _opts$name, _opts, _opts2, _opts3;
  if (opts === void 0) {
    opts = EMPTY_OBJECT;
  }
  if (true) {
    if (!isFunction(view)) {
      die("Autorun expects a function as first argument");
    }
    if (isAction(view)) {
      die("Autorun does not accept actions since actions are untrackable");
    }
  }
  var name = (_opts$name = (_opts = opts) == null ? void 0 : _opts.name) != null ? _opts$name : true ? view.name || "Autorun@" + getNextId() : "Autorun";
  var runSync = !opts.scheduler && !opts.delay;
  var reaction2;
  if (runSync) {
    reaction2 = new Reaction(name, function() {
      this.track(reactionRunner);
    }, opts.onError, opts.requiresObservable);
  } else {
    var scheduler = createSchedulerFromOptions(opts);
    var isScheduled = false;
    reaction2 = new Reaction(name, function() {
      if (!isScheduled) {
        isScheduled = true;
        scheduler(function() {
          isScheduled = false;
          if (!reaction2.isDisposed) {
            reaction2.track(reactionRunner);
          }
        });
      }
    }, opts.onError, opts.requiresObservable);
  }
  function reactionRunner() {
    view(reaction2);
  }
  if (!((_opts2 = opts) != null && (_opts2 = _opts2.signal) != null && _opts2.aborted)) {
    reaction2.schedule_();
  }
  return reaction2.getDisposer_((_opts3 = opts) == null ? void 0 : _opts3.signal);
}
function createSchedulerFromOptions(opts) {
  return opts.scheduler ? opts.scheduler : opts.delay ? function(f2) {
    return setTimeout(f2, opts.delay);
  } : run;
}
function reaction(expression, effect, opts) {
  var _opts$name2, _opts4, _opts5;
  if (opts === void 0) {
    opts = EMPTY_OBJECT;
  }
  if (true) {
    if (!isFunction(expression) || !isFunction(effect)) {
      die("First and second argument to reaction should be functions");
    }
    if (!isPlainObject(opts)) {
      die("Third argument of reactions should be an object");
    }
  }
  var name = (_opts$name2 = opts.name) != null ? _opts$name2 : true ? "Reaction@" + getNextId() : "Reaction";
  var effectAction = action(name, opts.onError ? wrapErrorHandler(opts.onError, effect) : effect);
  var runSync = !opts.scheduler && !opts.delay;
  var scheduler = createSchedulerFromOptions(opts);
  var firstTime = true;
  var isScheduled = false;
  var value;
  var equals = opts.compareStructural ? comparer.structural : opts.equals || comparer["default"];
  var r2 = new Reaction(name, function() {
    if (firstTime || runSync) {
      reactionRunner();
    } else if (!isScheduled) {
      isScheduled = true;
      scheduler(reactionRunner);
    }
  }, opts.onError, opts.requiresObservable);
  function reactionRunner() {
    isScheduled = false;
    if (r2.isDisposed) {
      return;
    }
    var changed = false;
    var oldValue = value;
    r2.track(function() {
      var nextValue = allowStateChanges(false, function() {
        return expression(r2);
      });
      changed = firstTime || !equals(value, nextValue);
      value = nextValue;
    });
    if (firstTime && opts.fireImmediately) {
      effectAction(value, oldValue, r2);
    } else if (!firstTime && changed) {
      effectAction(value, oldValue, r2);
    }
    firstTime = false;
  }
  if (!((_opts4 = opts) != null && (_opts4 = _opts4.signal) != null && _opts4.aborted)) {
    r2.schedule_();
  }
  return r2.getDisposer_((_opts5 = opts) == null ? void 0 : _opts5.signal);
}
function wrapErrorHandler(errorHandler, baseFn) {
  return function() {
    try {
      return baseFn.apply(this, arguments);
    } catch (e) {
      errorHandler.call(this, e);
    }
  };
}
function onBecomeObserved(thing, arg2, arg3) {
  return interceptHook(ON_BECOME_OBSERVED, thing, arg2, arg3);
}
function onBecomeUnobserved(thing, arg2, arg3) {
  return interceptHook(ON_BECOME_UNOBSERVED, thing, arg2, arg3);
}
function interceptHook(hook, thing, arg2, arg3) {
  var atom = typeof arg3 === "function" ? getAtom(thing, arg2) : getAtom(thing);
  var cb = isFunction(arg3) ? arg3 : arg2;
  var listenersKey = hook + "L";
  if (atom[listenersKey]) {
    atom[listenersKey].add(cb);
  } else {
    atom[listenersKey] = /* @__PURE__ */ new Set([cb]);
  }
  return function() {
    var hookListeners = atom[listenersKey];
    if (hookListeners) {
      hookListeners["delete"](cb);
      if (hookListeners.size === 0) {
        delete atom[listenersKey];
      }
    }
  };
}
function configure(options) {
  if (options.isolateGlobalState === true) {
    isolateGlobalState();
  }
  var useProxies = options.useProxies, enforceActions = options.enforceActions;
  if (useProxies !== void 0) {
    globalState.useProxies = useProxies === ALWAYS ? true : useProxies === NEVER ? false : typeof Proxy !== "undefined";
  }
  if (useProxies === "ifavailable") {
    globalState.verifyProxies = true;
  }
  if (enforceActions !== void 0) {
    var ea = enforceActions === ALWAYS ? ALWAYS : enforceActions === OBSERVED;
    globalState.enforceActions = ea;
    globalState.allowStateChanges = ea === true || ea === ALWAYS ? false : true;
  }
  ["computedRequiresReaction", "reactionRequiresObservable", "observableRequiresReaction", "disableErrorBoundaries", "safeDescriptors"].forEach(function(key2) {
    if (key2 in options) {
      globalState[key2] = !!options[key2];
    }
  });
  globalState.allowStateReads = !globalState.observableRequiresReaction;
  if (globalState.disableErrorBoundaries === true) {
    console.warn("WARNING: Debug feature only. MobX will NOT recover from errors when `disableErrorBoundaries` is enabled.");
  }
  if (options.reactionScheduler) {
    setReactionScheduler(options.reactionScheduler);
  }
}
function extendObservable(target, properties, annotations, options) {
  if (true) {
    if (arguments.length > 4) {
      die("'extendObservable' expected 2-4 arguments");
    }
    if (typeof target !== "object") {
      die("'extendObservable' expects an object as first argument");
    }
    if (isObservableMap(target)) {
      die("'extendObservable' should not be used on maps, use map.merge instead");
    }
    if (!isPlainObject(properties)) {
      die("'extendObservable' only accepts plain objects as second argument");
    }
    if (isObservable(properties) || isObservable(annotations)) {
      die("Extending an object with another observable (object) is not supported");
    }
  }
  var descriptors = getOwnPropertyDescriptors(properties);
  initObservable(function() {
    var adm = asObservableObject(target, options)[$mobx];
    ownKeys(descriptors).forEach(function(key2) {
      adm.extend_(
        key2,
        descriptors[key2],
        // must pass "undefined" for { key: undefined }
        !annotations ? true : key2 in annotations ? annotations[key2] : true
      );
    });
  });
  return target;
}
function getDependencyTree(thing, property) {
  return nodeToDependencyTree(getAtom(thing, property));
}
function nodeToDependencyTree(node2) {
  var result = {
    name: node2.name_
  };
  if (node2.observing_ && node2.observing_.length > 0) {
    result.dependencies = unique(node2.observing_).map(nodeToDependencyTree);
  }
  return result;
}
function getObserverTree(thing, property) {
  return nodeToObserverTree(getAtom(thing, property));
}
function nodeToObserverTree(node2) {
  var result = {
    name: node2.name_
  };
  if (hasObservers(node2)) {
    result.observers = Array.from(getObservers(node2)).map(nodeToObserverTree);
  }
  return result;
}
function unique(list) {
  return Array.from(new Set(list));
}
function FlowCancellationError() {
  this.message = "FLOW_CANCELLED";
}
function isFlowCancellationError(error) {
  return error instanceof FlowCancellationError;
}
function cancelPromise(promise) {
  if (isFunction(promise.cancel)) {
    promise.cancel();
  }
}
function flowResult(result) {
  return result;
}
function isFlow(fn) {
  return (fn == null ? void 0 : fn.isMobXFlow) === true;
}
function interceptReads(thing, propOrHandler, handler) {
  var target;
  if (isObservableMap(thing) || isObservableArray(thing) || isObservableValue(thing)) {
    target = getAdministration(thing);
  } else if (isObservableObject(thing)) {
    if (!isStringish(propOrHandler)) {
      return die("InterceptReads can only be used with a specific property, not with an object in general");
    }
    target = getAdministration(thing, propOrHandler);
  } else if (true) {
    return die("Expected observable map, object or array as first array");
  }
  if (target.dehancer !== void 0) {
    return die("An intercept reader was already established");
  }
  target.dehancer = typeof propOrHandler === "function" ? propOrHandler : handler;
  return function() {
    target.dehancer = void 0;
  };
}
function intercept(thing, propOrHandler, handler) {
  if (isFunction(handler)) {
    return interceptProperty(thing, propOrHandler, handler);
  } else {
    return interceptInterceptable(thing, propOrHandler);
  }
}
function interceptInterceptable(thing, handler) {
  return getAdministration(thing).intercept_(handler);
}
function interceptProperty(thing, property, handler) {
  return getAdministration(thing, property).intercept_(handler);
}
function _isComputed(value, property) {
  if (property === void 0) {
    return isComputedValue(value);
  }
  if (isObservableObject(value) === false) {
    return false;
  }
  if (!value[$mobx].values_.has(property)) {
    return false;
  }
  var atom = getAtom(value, property);
  return isComputedValue(atom);
}
function isComputed(value) {
  if (arguments.length > 1) {
    return die("isComputed expects only 1 argument. Use isComputedProp to inspect the observability of a property");
  }
  return _isComputed(value);
}
function isComputedProp(value, propName) {
  if (!isStringish(propName)) {
    return die("isComputed expected a property name as second argument");
  }
  return _isComputed(value, propName);
}
function _isObservable(value, property) {
  if (!value) {
    return false;
  }
  if (property !== void 0) {
    if (isObservableMap(value) || isObservableArray(value)) {
      return die("isObservable(object, propertyName) is not supported for arrays and maps. Use map.has or array.length instead.");
    }
    if (isObservableObject(value)) {
      return value[$mobx].values_.has(property);
    }
    return false;
  }
  return isObservableObject(value) || !!value[$mobx] || isAtom(value) || isReaction(value) || isComputedValue(value);
}
function isObservable(value) {
  if (arguments.length !== 1) {
    die("isObservable expects only 1 argument. Use isObservableProp to inspect the observability of a property");
  }
  return _isObservable(value);
}
function isObservableProp(value, propName) {
  if (!isStringish(propName)) {
    return die("expected a property name as second argument");
  }
  return _isObservable(value, propName);
}
function keys(obj) {
  if (isObservableObject(obj)) {
    return obj[$mobx].keys_();
  }
  if (isObservableMap(obj) || isObservableSet(obj)) {
    return Array.from(obj.keys());
  }
  if (isObservableArray(obj)) {
    return obj.map(function(_15, index) {
      return index;
    });
  }
  die(5);
}
function values(obj) {
  if (isObservableObject(obj)) {
    return keys(obj).map(function(key2) {
      return obj[key2];
    });
  }
  if (isObservableMap(obj)) {
    return keys(obj).map(function(key2) {
      return obj.get(key2);
    });
  }
  if (isObservableSet(obj)) {
    return Array.from(obj.values());
  }
  if (isObservableArray(obj)) {
    return obj.slice();
  }
  die(6);
}
function entries(obj) {
  if (isObservableObject(obj)) {
    return keys(obj).map(function(key2) {
      return [key2, obj[key2]];
    });
  }
  if (isObservableMap(obj)) {
    return keys(obj).map(function(key2) {
      return [key2, obj.get(key2)];
    });
  }
  if (isObservableSet(obj)) {
    return Array.from(obj.entries());
  }
  if (isObservableArray(obj)) {
    return obj.map(function(key2, index) {
      return [index, key2];
    });
  }
  die(7);
}
function set3(obj, key2, value) {
  if (arguments.length === 2 && !isObservableSet(obj)) {
    startBatch();
    var _values = key2;
    try {
      for (var _key in _values) {
        set3(obj, _key, _values[_key]);
      }
    } finally {
      endBatch();
    }
    return;
  }
  if (isObservableObject(obj)) {
    obj[$mobx].set_(key2, value);
  } else if (isObservableMap(obj)) {
    obj.set(key2, value);
  } else if (isObservableSet(obj)) {
    obj.add(key2);
  } else if (isObservableArray(obj)) {
    if (typeof key2 !== "number") {
      key2 = parseInt(key2, 10);
    }
    if (key2 < 0) {
      die("Invalid index: '" + key2 + "'");
    }
    startBatch();
    if (key2 >= obj.length) {
      obj.length = key2 + 1;
    }
    obj[key2] = value;
    endBatch();
  } else {
    die(8);
  }
}
function remove(obj, key2) {
  if (isObservableObject(obj)) {
    obj[$mobx].delete_(key2);
  } else if (isObservableMap(obj)) {
    obj["delete"](key2);
  } else if (isObservableSet(obj)) {
    obj["delete"](key2);
  } else if (isObservableArray(obj)) {
    if (typeof key2 !== "number") {
      key2 = parseInt(key2, 10);
    }
    obj.splice(key2, 1);
  } else {
    die(9);
  }
}
function has(obj, key2) {
  if (isObservableObject(obj)) {
    return obj[$mobx].has_(key2);
  } else if (isObservableMap(obj)) {
    return obj.has(key2);
  } else if (isObservableSet(obj)) {
    return obj.has(key2);
  } else if (isObservableArray(obj)) {
    return key2 >= 0 && key2 < obj.length;
  }
  die(10);
}
function get(obj, key2) {
  if (!has(obj, key2)) {
    return void 0;
  }
  if (isObservableObject(obj)) {
    return obj[$mobx].get_(key2);
  } else if (isObservableMap(obj)) {
    return obj.get(key2);
  } else if (isObservableArray(obj)) {
    return obj[key2];
  }
  die(11);
}
function apiDefineProperty(obj, key2, descriptor) {
  if (isObservableObject(obj)) {
    return obj[$mobx].defineProperty_(key2, descriptor);
  }
  die(39);
}
function apiOwnKeys(obj) {
  if (isObservableObject(obj)) {
    return obj[$mobx].ownKeys_();
  }
  die(38);
}
function observe(thing, propOrCb, cbOrFire, fireImmediately) {
  if (isFunction(cbOrFire)) {
    return observeObservableProperty(thing, propOrCb, cbOrFire, fireImmediately);
  } else {
    return observeObservable(thing, propOrCb, cbOrFire);
  }
}
function observeObservable(thing, listener, fireImmediately) {
  return getAdministration(thing).observe_(listener, fireImmediately);
}
function observeObservableProperty(thing, property, listener, fireImmediately) {
  return getAdministration(thing, property).observe_(listener, fireImmediately);
}
function cache(map3, key2, value) {
  map3.set(key2, value);
  return value;
}
function toJSHelper(source, __alreadySeen) {
  if (source == null || typeof source !== "object" || source instanceof Date || !isObservable(source)) {
    return source;
  }
  if (isObservableValue(source) || isComputedValue(source)) {
    return toJSHelper(source.get(), __alreadySeen);
  }
  if (__alreadySeen.has(source)) {
    return __alreadySeen.get(source);
  }
  if (isObservableArray(source)) {
    var res = cache(__alreadySeen, source, new Array(source.length));
    source.forEach(function(value, idx) {
      res[idx] = toJSHelper(value, __alreadySeen);
    });
    return res;
  }
  if (isObservableSet(source)) {
    var _res = cache(__alreadySeen, source, /* @__PURE__ */ new Set());
    source.forEach(function(value) {
      _res.add(toJSHelper(value, __alreadySeen));
    });
    return _res;
  }
  if (isObservableMap(source)) {
    var _res2 = cache(__alreadySeen, source, /* @__PURE__ */ new Map());
    source.forEach(function(value, key2) {
      _res2.set(key2, toJSHelper(value, __alreadySeen));
    });
    return _res2;
  } else {
    var _res3 = cache(__alreadySeen, source, {});
    apiOwnKeys(source).forEach(function(key2) {
      if (objectPrototype.propertyIsEnumerable.call(source, key2)) {
        _res3[key2] = toJSHelper(source[key2], __alreadySeen);
      }
    });
    return _res3;
  }
}
function toJS2(source, options) {
  if (options) {
    die("toJS no longer supports options");
  }
  return toJSHelper(source, /* @__PURE__ */ new Map());
}
function trace() {
  if (false) {
    return;
  }
  var enterBreakPoint = false;
  for (var _len = arguments.length, args = new Array(_len), _key = 0; _key < _len; _key++) {
    args[_key] = arguments[_key];
  }
  if (typeof args[args.length - 1] === "boolean") {
    enterBreakPoint = args.pop();
  }
  var derivation = getAtomFromArgs(args);
  if (!derivation) {
    return die("'trace(break?)' can only be used inside a tracked computed value or a Reaction. Consider passing in the computed value or reaction explicitly");
  }
  if (derivation.isTracing_ === TraceMode.NONE) {
    console.log("[mobx.trace] '" + derivation.name_ + "' tracing enabled");
  }
  derivation.isTracing_ = enterBreakPoint ? TraceMode.BREAK : TraceMode.LOG;
}
function getAtomFromArgs(args) {
  switch (args.length) {
    case 0:
      return globalState.trackingDerivation;
    case 1:
      return getAtom(args[0]);
    case 2:
      return getAtom(args[0], args[1]);
  }
}
function transaction(action2, thisArg) {
  if (thisArg === void 0) {
    thisArg = void 0;
  }
  startBatch();
  try {
    return action2.apply(thisArg);
  } finally {
    endBatch();
  }
}
function when(predicate, arg1, arg2) {
  if (arguments.length === 1 || arg1 && typeof arg1 === "object") {
    return whenPromise(predicate, arg1);
  }
  return _when(predicate, arg1, arg2 || {});
}
function _when(predicate, effect, opts) {
  var timeoutHandle;
  if (typeof opts.timeout === "number") {
    var error = new Error("WHEN_TIMEOUT");
    timeoutHandle = setTimeout(function() {
      if (!disposer[$mobx].isDisposed) {
        disposer();
        if (opts.onError) {
          opts.onError(error);
        } else {
          throw error;
        }
      }
    }, opts.timeout);
  }
  opts.name = true ? opts.name || "When@" + getNextId() : "When";
  var effectAction = createAction(true ? opts.name + "-effect" : "When-effect", effect);
  var disposer = autorun(function(r2) {
    var cond = allowStateChanges(false, predicate);
    if (cond) {
      r2.dispose();
      if (timeoutHandle) {
        clearTimeout(timeoutHandle);
      }
      effectAction();
    }
  }, opts);
  return disposer;
}
function whenPromise(predicate, opts) {
  var _opts$signal;
  if (opts && opts.onError) {
    return die("the options 'onError' and 'promise' cannot be combined");
  }
  if (opts != null && (_opts$signal = opts.signal) != null && _opts$signal.aborted) {
    return Object.assign(Promise.reject(new Error("WHEN_ABORTED")), {
      cancel: function cancel2() {
        return null;
      }
    });
  }
  var cancel;
  var abort;
  var res = new Promise(function(resolve, reject) {
    var _opts$signal2;
    var disposer = _when(predicate, resolve, _extends2({}, opts, {
      onError: reject
    }));
    cancel = function cancel2() {
      disposer();
      reject(new Error("WHEN_CANCELLED"));
    };
    abort = function abort2() {
      disposer();
      reject(new Error("WHEN_ABORTED"));
    };
    opts == null || (_opts$signal2 = opts.signal) == null || _opts$signal2.addEventListener == null || _opts$signal2.addEventListener("abort", abort);
  })["finally"](function() {
    var _opts$signal3;
    return opts == null || (_opts$signal3 = opts.signal) == null || _opts$signal3.removeEventListener == null ? void 0 : _opts$signal3.removeEventListener("abort", abort);
  });
  res.cancel = cancel;
  return res;
}
function getAdm(target) {
  return target[$mobx];
}
function asDynamicObservableObject(target, options) {
  var _target$$mobx, _target$$mobx$proxy_;
  assertProxies();
  target = asObservableObject(target, options);
  return (_target$$mobx$proxy_ = (_target$$mobx = target[$mobx]).proxy_) != null ? _target$$mobx$proxy_ : _target$$mobx.proxy_ = new Proxy(target, objectProxyTraps);
}
function hasInterceptors(interceptable) {
  return interceptable.interceptors_ !== void 0 && interceptable.interceptors_.length > 0;
}
function registerInterceptor(interceptable, handler) {
  var interceptors = interceptable.interceptors_ || (interceptable.interceptors_ = []);
  interceptors.push(handler);
  return once(function() {
    var idx = interceptors.indexOf(handler);
    if (idx !== -1) {
      interceptors.splice(idx, 1);
    }
  });
}
function interceptChange(interceptable, change) {
  var prevU = untrackedStart();
  try {
    var interceptors = [].concat(interceptable.interceptors_ || []);
    for (var i2 = 0, l2 = interceptors.length; i2 < l2; i2++) {
      change = interceptors[i2](change);
      if (change && !change.type) {
        die(14);
      }
      if (!change) {
        break;
      }
    }
    return change;
  } finally {
    untrackedEnd(prevU);
  }
}
function hasListeners(listenable) {
  return listenable.changeListeners_ !== void 0 && listenable.changeListeners_.length > 0;
}
function registerListener(listenable, handler) {
  var listeners = listenable.changeListeners_ || (listenable.changeListeners_ = []);
  listeners.push(handler);
  return once(function() {
    var idx = listeners.indexOf(handler);
    if (idx !== -1) {
      listeners.splice(idx, 1);
    }
  });
}
function notifyListeners(listenable, change) {
  var prevU = untrackedStart();
  var listeners = listenable.changeListeners_;
  if (!listeners) {
    return;
  }
  listeners = listeners.slice();
  for (var i2 = 0, l2 = listeners.length; i2 < l2; i2++) {
    listeners[i2](change);
  }
  untrackedEnd(prevU);
}
function makeObservable(target, annotations, options) {
  initObservable(function() {
    var _annotations;
    var adm = asObservableObject(target, options)[$mobx];
    if (annotations && target[storedAnnotationsSymbol]) {
      die("makeObservable second arg must be nullish when using decorators. Mixing @decorator syntax with annotations is not supported.");
    }
    (_annotations = annotations) != null ? _annotations : annotations = collectStoredAnnotations(target);
    ownKeys(annotations).forEach(function(key2) {
      return adm.make_(key2, annotations[key2]);
    });
  });
  return target;
}
function makeAutoObservable(target, overrides, options) {
  if (true) {
    if (!isPlainObject(target) && !isPlainObject(Object.getPrototypeOf(target))) {
      die("'makeAutoObservable' can only be used for classes that don't have a superclass");
    }
    if (isObservableObject(target)) {
      die("makeAutoObservable can only be used on objects not already made observable");
    }
  }
  if (isPlainObject(target)) {
    return extendObservable(target, target, overrides, options);
  }
  initObservable(function() {
    var adm = asObservableObject(target, options)[$mobx];
    if (!target[keysSymbol]) {
      var proto = Object.getPrototypeOf(target);
      var keys2 = new Set([].concat(ownKeys(target), ownKeys(proto)));
      keys2["delete"]("constructor");
      keys2["delete"]($mobx);
      addHiddenProp(proto, keysSymbol, keys2);
    }
    target[keysSymbol].forEach(function(key2) {
      return adm.make_(
        key2,
        // must pass "undefined" for { key: undefined }
        !overrides ? true : key2 in overrides ? overrides[key2] : true
      );
    });
  });
  return target;
}
function createObservableArray(initialValues, enhancer, name, owned) {
  if (name === void 0) {
    name = true ? "ObservableArray@" + getNextId() : "ObservableArray";
  }
  if (owned === void 0) {
    owned = false;
  }
  assertProxies();
  return initObservable(function() {
    var adm = new ObservableArrayAdministration(name, enhancer, owned, false);
    addHiddenFinalProp(adm.values_, $mobx, adm);
    var proxy = new Proxy(adm.values_, arrayTraps);
    adm.proxy_ = proxy;
    if (initialValues && initialValues.length) {
      adm.spliceWithArray_(0, 0, initialValues);
    }
    return proxy;
  });
}
function addArrayExtension(funcName, funcFactory) {
  if (typeof Array.prototype[funcName] === "function") {
    arrayExtensions[funcName] = funcFactory(funcName);
  }
}
function simpleFunc(funcName) {
  return function() {
    var adm = this[$mobx];
    adm.atom_.reportObserved();
    var dehancedValues = adm.dehanceValues_(adm.values_);
    return dehancedValues[funcName].apply(dehancedValues, arguments);
  };
}
function mapLikeFunc(funcName) {
  return function(callback, thisArg) {
    var _this2 = this;
    var adm = this[$mobx];
    adm.atom_.reportObserved();
    var dehancedValues = adm.dehanceValues_(adm.values_);
    return dehancedValues[funcName](function(element, index) {
      return callback.call(thisArg, element, index, _this2);
    });
  };
}
function reduceLikeFunc(funcName) {
  return function() {
    var _this3 = this;
    var adm = this[$mobx];
    adm.atom_.reportObserved();
    var dehancedValues = adm.dehanceValues_(adm.values_);
    var callback = arguments[0];
    arguments[0] = function(accumulator, currentValue, index) {
      return callback(accumulator, currentValue, index, _this3);
    };
    return dehancedValues[funcName].apply(dehancedValues, arguments);
  };
}
function isObservableArray(thing) {
  return isObject(thing) && isObservableArrayAdministration(thing[$mobx]);
}
function makeIterableForMap(iterator) {
  iterator[Symbol.toStringTag] = "MapIterator";
  return makeIterable(iterator);
}
function convertToMap(dataStructure) {
  if (isES6Map(dataStructure) || isObservableMap(dataStructure)) {
    return dataStructure;
  } else if (Array.isArray(dataStructure)) {
    return new Map(dataStructure);
  } else if (isPlainObject(dataStructure)) {
    var map3 = /* @__PURE__ */ new Map();
    for (var key2 in dataStructure) {
      map3.set(key2, dataStructure[key2]);
    }
    return map3;
  } else {
    return die(21, dataStructure);
  }
}
function makeIterableForSet(iterator) {
  iterator[Symbol.toStringTag] = "SetIterator";
  return makeIterable(iterator);
}
function asObservableObject(target, options) {
  var _options$name;
  if (options && isObservableObject(target)) {
    die("Options can't be provided for already observable objects.");
  }
  if (hasProp(target, $mobx)) {
    if (!(getAdministration(target) instanceof ObservableObjectAdministration)) {
      die("Cannot convert '" + getDebugName(target) + "' into observable object:\nThe target is already observable of different type.\nExtending builtins is not supported.");
    }
    return target;
  }
  if (!Object.isExtensible(target)) {
    die("Cannot make the designated object observable; it is not extensible");
  }
  var name = (_options$name = options == null ? void 0 : options.name) != null ? _options$name : true ? (isPlainObject(target) ? "ObservableObject" : target.constructor.name) + "@" + getNextId() : "ObservableObject";
  var adm = new ObservableObjectAdministration(target, /* @__PURE__ */ new Map(), String(name), getAnnotationFromOptions(options));
  addHiddenProp(target, $mobx, adm);
  return target;
}
function getCachedObservablePropDescriptor(key2) {
  return descriptorCache[key2] || (descriptorCache[key2] = {
    get: function get5() {
      return this[$mobx].getObservablePropValue_(key2);
    },
    set: function set7(value) {
      return this[$mobx].setObservablePropValue_(key2, value);
    }
  });
}
function isObservableObject(thing) {
  if (isObject(thing)) {
    return isObservableObjectAdministration(thing[$mobx]);
  }
  return false;
}
function recordAnnotationApplied(adm, annotation, key2) {
  var _adm$target_$storedAn;
  if (true) {
    adm.appliedAnnotations_[key2] = annotation;
  }
  (_adm$target_$storedAn = adm.target_[storedAnnotationsSymbol]) == null || delete _adm$target_$storedAn[key2];
}
function assertAnnotable(adm, annotation, key2) {
  if (!isAnnotation(annotation)) {
    die("Cannot annotate '" + adm.name_ + "." + key2.toString() + "': Invalid annotation.");
  }
  if (!isOverride(annotation) && hasProp(adm.appliedAnnotations_, key2)) {
    var fieldName = adm.name_ + "." + key2.toString();
    var currentAnnotationType = adm.appliedAnnotations_[key2].annotationType_;
    var requestedAnnotationType = annotation.annotationType_;
    die("Cannot apply '" + requestedAnnotationType + "' to '" + fieldName + "':" + ("\nThe field is already annotated with '" + currentAnnotationType + "'.") + "\nRe-annotating fields is not allowed.\nUse 'override' annotation for methods overridden by subclass.");
  }
}
function inherit(ctor, proto) {
  if (Object.setPrototypeOf) {
    Object.setPrototypeOf(ctor.prototype, proto);
  } else if (ctor.prototype.__proto__ !== void 0) {
    ctor.prototype.__proto__ = proto;
  } else {
    ctor.prototype = proto;
  }
}
function createArrayEntryDescriptor(index) {
  return {
    enumerable: false,
    configurable: true,
    get: function get5() {
      return this[$mobx].get_(index);
    },
    set: function set7(value) {
      this[$mobx].set_(index, value);
    }
  };
}
function createArrayBufferItem(index) {
  defineProperty(LegacyObservableArray.prototype, "" + index, createArrayEntryDescriptor(index));
}
function reserveArrayBuffer(max2) {
  if (max2 > OBSERVABLE_ARRAY_BUFFER_SIZE) {
    for (var index = OBSERVABLE_ARRAY_BUFFER_SIZE; index < max2 + 100; index++) {
      createArrayBufferItem(index);
    }
    OBSERVABLE_ARRAY_BUFFER_SIZE = max2;
  }
}
function createLegacyArray(initialValues, enhancer, name) {
  return new LegacyObservableArray(initialValues, enhancer, name);
}
function getAtom(thing, property) {
  if (typeof thing === "object" && thing !== null) {
    if (isObservableArray(thing)) {
      if (property !== void 0) {
        die(23);
      }
      return thing[$mobx].atom_;
    }
    if (isObservableSet(thing)) {
      return thing.atom_;
    }
    if (isObservableMap(thing)) {
      if (property === void 0) {
        return thing.keysAtom_;
      }
      var observable2 = thing.data_.get(property) || thing.hasMap_.get(property);
      if (!observable2) {
        die(25, property, getDebugName(thing));
      }
      return observable2;
    }
    if (isObservableObject(thing)) {
      if (!property) {
        return die(26);
      }
      var _observable = thing[$mobx].values_.get(property);
      if (!_observable) {
        die(27, property, getDebugName(thing));
      }
      return _observable;
    }
    if (isAtom(thing) || isComputedValue(thing) || isReaction(thing)) {
      return thing;
    }
  } else if (isFunction(thing)) {
    if (isReaction(thing[$mobx])) {
      return thing[$mobx];
    }
  }
  die(28);
}
function getAdministration(thing, property) {
  if (!thing) {
    die(29);
  }
  if (property !== void 0) {
    return getAdministration(getAtom(thing, property));
  }
  if (isAtom(thing) || isComputedValue(thing) || isReaction(thing)) {
    return thing;
  }
  if (isObservableMap(thing) || isObservableSet(thing)) {
    return thing;
  }
  if (thing[$mobx]) {
    return thing[$mobx];
  }
  die(24, thing);
}
function getDebugName(thing, property) {
  var named;
  if (property !== void 0) {
    named = getAtom(thing, property);
  } else if (isAction(thing)) {
    return thing.name;
  } else if (isObservableObject(thing) || isObservableMap(thing) || isObservableSet(thing)) {
    named = getAdministration(thing);
  } else {
    named = getAtom(thing);
  }
  return named.name_;
}
function initObservable(cb) {
  var derivation = untrackedStart();
  var allowStateChanges2 = allowStateChangesStart(true);
  startBatch();
  try {
    return cb();
  } finally {
    endBatch();
    allowStateChangesEnd(allowStateChanges2);
    untrackedEnd(derivation);
  }
}
function deepEqual(a2, b2, depth) {
  if (depth === void 0) {
    depth = -1;
  }
  return eq(a2, b2, depth);
}
function eq(a2, b2, depth, aStack, bStack) {
  if (a2 === b2) {
    return a2 !== 0 || 1 / a2 === 1 / b2;
  }
  if (a2 == null || b2 == null) {
    return false;
  }
  if (a2 !== a2) {
    return b2 !== b2;
  }
  var type = typeof a2;
  if (type !== "function" && type !== "object" && typeof b2 != "object") {
    return false;
  }
  var className = toString.call(a2);
  if (className !== toString.call(b2)) {
    return false;
  }
  switch (className) {
    // Strings, numbers, regular expressions, dates, and booleans are compared by value.
    case "[object RegExp]":
    // RegExps are coerced to strings for comparison (Note: '' + /a/i === '/a/i')
    case "[object String]":
      return "" + a2 === "" + b2;
    case "[object Number]":
      if (+a2 !== +a2) {
        return +b2 !== +b2;
      }
      return +a2 === 0 ? 1 / +a2 === 1 / b2 : +a2 === +b2;
    case "[object Date]":
    case "[object Boolean]":
      return +a2 === +b2;
    case "[object Symbol]":
      return typeof Symbol !== "undefined" && Symbol.valueOf.call(a2) === Symbol.valueOf.call(b2);
    case "[object Map]":
    case "[object Set]":
      if (depth >= 0) {
        depth++;
      }
      break;
  }
  a2 = unwrap(a2);
  b2 = unwrap(b2);
  var areArrays = className === "[object Array]";
  if (!areArrays) {
    if (typeof a2 != "object" || typeof b2 != "object") {
      return false;
    }
    var aCtor = a2.constructor, bCtor = b2.constructor;
    if (aCtor !== bCtor && !(isFunction(aCtor) && aCtor instanceof aCtor && isFunction(bCtor) && bCtor instanceof bCtor) && "constructor" in a2 && "constructor" in b2) {
      return false;
    }
  }
  if (depth === 0) {
    return false;
  } else if (depth < 0) {
    depth = -1;
  }
  aStack = aStack || [];
  bStack = bStack || [];
  var length2 = aStack.length;
  while (length2--) {
    if (aStack[length2] === a2) {
      return bStack[length2] === b2;
    }
  }
  aStack.push(a2);
  bStack.push(b2);
  if (areArrays) {
    length2 = a2.length;
    if (length2 !== b2.length) {
      return false;
    }
    while (length2--) {
      if (!eq(a2[length2], b2[length2], depth - 1, aStack, bStack)) {
        return false;
      }
    }
  } else {
    var keys2 = Object.keys(a2);
    var _length = keys2.length;
    if (Object.keys(b2).length !== _length) {
      return false;
    }
    for (var i2 = 0; i2 < _length; i2++) {
      var key2 = keys2[i2];
      if (!(hasProp(b2, key2) && eq(a2[key2], b2[key2], depth - 1, aStack, bStack))) {
        return false;
      }
    }
  }
  aStack.pop();
  bStack.pop();
  return true;
}
function unwrap(a2) {
  if (isObservableArray(a2)) {
    return a2.slice();
  }
  if (isES6Map(a2) || isObservableMap(a2)) {
    return Array.from(a2.entries());
  }
  if (isES6Set(a2) || isObservableSet(a2)) {
    return Array.from(a2.entries());
  }
  return a2;
}
function makeIterable(iterator) {
  iterator[Symbol.iterator] = getSelf;
  return Object.assign(Object.create(maybeIteratorPrototype), iterator);
}
function getSelf() {
  return this;
}
function isAnnotation(thing) {
  return (
    // Can be function
    thing instanceof Object && typeof thing.annotationType_ === "string" && isFunction(thing.make_) && isFunction(thing.extend_)
  );
}
var niceErrors, errors, mockGlobal, assign2, getDescriptor, defineProperty, objectPrototype, EMPTY_ARRAY, EMPTY_OBJECT, hasProxy, plainObjectString, noop, hasGetOwnPropertySymbols, ownKeys, getOwnPropertyDescriptors, storedAnnotationsSymbol, $mobx, Atom, isAtom, comparer, OVERRIDE, override, AUTO, autoAnnotation, OBSERVABLE, OBSERVABLE_REF, OBSERVABLE_SHALLOW, OBSERVABLE_STRUCT, defaultCreateObservableOptions, observableAnnotation, observableRefAnnotation, observableShallowAnnotation, observableStructAnnotation, observableDecoratorAnnotation, observableFactories, observable, COMPUTED, COMPUTED_STRUCT, computedAnnotation, computedStructAnnotation, computed, _getDescriptor$config, _getDescriptor, currentActionId, nextActionId, isFunctionNameConfigurable, tmpNameDescriptor, CREATE, ObservableValue, isObservableValue, ComputedValue, isComputedValue, IDerivationState_, TraceMode, CaughtException, persistentKeys, MobXGlobals, canMergeGlobalState, isolateCalled, globalState, Reaction, MAX_REACTION_ITERATIONS, reactionScheduler, isReaction, END_EVENT, ACTION, ACTION_BOUND, AUTOACTION, AUTOACTION_BOUND, DEFAULT_ACTION_NAME, actionAnnotation, actionBoundAnnotation, autoActionAnnotation, autoActionBoundAnnotation, action, autoAction, run, ON_BECOME_OBSERVED, ON_BECOME_UNOBSERVED, NEVER, ALWAYS, OBSERVED, generatorId, flowAnnotation, flowBoundAnnotation, flow, objectProxyTraps, keysSymbol, SPLICE, UPDATE, MAX_SPLICE_SIZE, arrayTraps, ObservableArrayAdministration, arrayExtensions, isObservableArrayAdministration, ObservableMapMarker, ADD, DELETE, ObservableMap, isObservableMap, ObservableSetMarker, ObservableSet, isObservableSet, descriptorCache, REMOVE3, ObservableObjectAdministration, isObservableObjectAdministration, ENTRY_0, safariPrototypeSetterInheritanceBug, OBSERVABLE_ARRAY_BUFFER_SIZE, StubArray, LegacyObservableArray, toString, _getGlobal$Iterator, maybeIteratorPrototype;
var init_mobx_esm = __esm({
  "node_modules/mobx/dist/mobx.esm.js"() {
    niceErrors = {
      0: "Invalid value for configuration 'enforceActions', expected 'never', 'always' or 'observed'",
      1: function _2(annotationType, key2) {
        return "Cannot apply '" + annotationType + "' to '" + key2.toString() + "': Field not found.";
      },
      /*
      2(prop) {
          return `invalid decorator for '${prop.toString()}'`
      },
      3(prop) {
          return `Cannot decorate '${prop.toString()}': action can only be used on properties with a function value.`
      },
      4(prop) {
          return `Cannot decorate '${prop.toString()}': computed can only be used on getter properties.`
      },
      */
      5: "'keys()' can only be used on observable objects, arrays, sets and maps",
      6: "'values()' can only be used on observable objects, arrays, sets and maps",
      7: "'entries()' can only be used on observable objects, arrays and maps",
      8: "'set()' can only be used on observable objects, arrays and maps",
      9: "'remove()' can only be used on observable objects, arrays and maps",
      10: "'has()' can only be used on observable objects, arrays and maps",
      11: "'get()' can only be used on observable objects, arrays and maps",
      12: "Invalid annotation",
      13: "Dynamic observable objects cannot be frozen. If you're passing observables to 3rd party component/function that calls Object.freeze, pass copy instead: toJS(observable)",
      14: "Intercept handlers should return nothing or a change object",
      15: "Observable arrays cannot be frozen. If you're passing observables to 3rd party component/function that calls Object.freeze, pass copy instead: toJS(observable)",
      16: "Modification exception: the internal structure of an observable array was changed.",
      17: function _3(index, length2) {
        return "[mobx.array] Index out of bounds, " + index + " is larger than " + length2;
      },
      18: "mobx.map requires Map polyfill for the current browser. Check babel-polyfill or core-js/es6/map.js",
      19: function _4(other) {
        return "Cannot initialize from classes that inherit from Map: " + other.constructor.name;
      },
      20: function _5(other) {
        return "Cannot initialize map from " + other;
      },
      21: function _6(dataStructure) {
        return "Cannot convert to map from '" + dataStructure + "'";
      },
      22: "mobx.set requires Set polyfill for the current browser. Check babel-polyfill or core-js/es6/set.js",
      23: "It is not possible to get index atoms from arrays",
      24: function _7(thing) {
        return "Cannot obtain administration from " + thing;
      },
      25: function _8(property, name) {
        return "the entry '" + property + "' does not exist in the observable map '" + name + "'";
      },
      26: "please specify a property",
      27: function _9(property, name) {
        return "no observable property '" + property.toString() + "' found on the observable object '" + name + "'";
      },
      28: function _10(thing) {
        return "Cannot obtain atom from " + thing;
      },
      29: "Expecting some object",
      30: "invalid action stack. did you forget to finish an action?",
      31: "missing option for computed: get",
      32: function _11(name, derivation) {
        return "Cycle detected in computation " + name + ": " + derivation;
      },
      33: function _12(name) {
        return "The setter of computed value '" + name + "' is trying to update itself. Did you intend to update an _observable_ value, instead of the computed property?";
      },
      34: function _13(name) {
        return "[ComputedValue '" + name + "'] It is not possible to assign a new value to a computed value.";
      },
      35: "There are multiple, different versions of MobX active. Make sure MobX is loaded only once or use `configure({ isolateGlobalState: true })`",
      36: "isolateGlobalState should be called before MobX is running any reactions",
      37: function _14(method) {
        return "[mobx] `observableArray." + method + "()` mutates the array in-place, which is not allowed inside a derivation. Use `array.slice()." + method + "()` instead";
      },
      38: "'ownKeys()' can only be used on observable objects",
      39: "'defineProperty()' can only be used on observable objects"
    };
    errors = true ? niceErrors : {};
    mockGlobal = {};
    assign2 = Object.assign;
    getDescriptor = Object.getOwnPropertyDescriptor;
    defineProperty = Object.defineProperty;
    objectPrototype = Object.prototype;
    EMPTY_ARRAY = [];
    Object.freeze(EMPTY_ARRAY);
    EMPTY_OBJECT = {};
    Object.freeze(EMPTY_OBJECT);
    hasProxy = typeof Proxy !== "undefined";
    plainObjectString = Object.toString();
    noop = function noop2() {
    };
    hasGetOwnPropertySymbols = typeof Object.getOwnPropertySymbols !== "undefined";
    ownKeys = typeof Reflect !== "undefined" && Reflect.ownKeys ? Reflect.ownKeys : hasGetOwnPropertySymbols ? function(obj) {
      return Object.getOwnPropertyNames(obj).concat(Object.getOwnPropertySymbols(obj));
    } : (
      /* istanbul ignore next */
      Object.getOwnPropertyNames
    );
    getOwnPropertyDescriptors = Object.getOwnPropertyDescriptors || function getOwnPropertyDescriptors2(target) {
      var res = {};
      ownKeys(target).forEach(function(key2) {
        res[key2] = getDescriptor(target, key2);
      });
      return res;
    };
    storedAnnotationsSymbol = Symbol("mobx-stored-annotations");
    $mobx = Symbol("mobx administration");
    Atom = (function() {
      function Atom2(name_) {
        if (name_ === void 0) {
          name_ = true ? "Atom@" + getNextId() : "Atom";
        }
        this.name_ = void 0;
        this.flags_ = 0;
        this.observers_ = /* @__PURE__ */ new Set();
        this.lastAccessedBy_ = 0;
        this.lowestObserverState_ = IDerivationState_.NOT_TRACKING_;
        this.onBOL = void 0;
        this.onBUOL = void 0;
        this.name_ = name_;
      }
      var _proto = Atom2.prototype;
      _proto.onBO = function onBO() {
        if (this.onBOL) {
          this.onBOL.forEach(function(listener) {
            return listener();
          });
        }
      };
      _proto.onBUO = function onBUO() {
        if (this.onBUOL) {
          this.onBUOL.forEach(function(listener) {
            return listener();
          });
        }
      };
      _proto.reportObserved = function reportObserved$1() {
        return reportObserved(this);
      };
      _proto.reportChanged = function reportChanged() {
        startBatch();
        propagateChanged(this);
        endBatch();
      };
      _proto.toString = function toString2() {
        return this.name_;
      };
      return _createClass(Atom2, [{
        key: "isBeingObserved",
        get: function get5() {
          return getFlag(this.flags_, Atom2.isBeingObservedMask_);
        },
        set: function set7(newValue) {
          this.flags_ = setFlag(this.flags_, Atom2.isBeingObservedMask_, newValue);
        }
      }, {
        key: "isPendingUnobservation",
        get: function get5() {
          return getFlag(this.flags_, Atom2.isPendingUnobservationMask_);
        },
        set: function set7(newValue) {
          this.flags_ = setFlag(this.flags_, Atom2.isPendingUnobservationMask_, newValue);
        }
      }, {
        key: "diffValue",
        get: function get5() {
          return getFlag(this.flags_, Atom2.diffValueMask_) ? 1 : 0;
        },
        set: function set7(newValue) {
          this.flags_ = setFlag(this.flags_, Atom2.diffValueMask_, newValue === 1 ? true : false);
        }
      }]);
    })();
    Atom.isBeingObservedMask_ = 1;
    Atom.isPendingUnobservationMask_ = 2;
    Atom.diffValueMask_ = 4;
    isAtom = createInstanceofPredicate("Atom", Atom);
    comparer = {
      identity: identityComparer,
      structural: structuralComparer,
      "default": defaultComparer,
      shallow: shallowComparer
    };
    OVERRIDE = "override";
    override = createDecoratorAnnotation({
      annotationType_: OVERRIDE,
      make_,
      extend_,
      decorate_20223_
    });
    AUTO = "true";
    autoAnnotation = createAutoAnnotation();
    OBSERVABLE = "observable";
    OBSERVABLE_REF = "observable.ref";
    OBSERVABLE_SHALLOW = "observable.shallow";
    OBSERVABLE_STRUCT = "observable.struct";
    defaultCreateObservableOptions = {
      deep: true,
      name: void 0,
      defaultDecorator: void 0,
      proxy: true
    };
    Object.freeze(defaultCreateObservableOptions);
    observableAnnotation = createObservableAnnotation(OBSERVABLE);
    observableRefAnnotation = createObservableAnnotation(OBSERVABLE_REF, {
      enhancer: referenceEnhancer
    });
    observableShallowAnnotation = createObservableAnnotation(OBSERVABLE_SHALLOW, {
      enhancer: shallowEnhancer
    });
    observableStructAnnotation = createObservableAnnotation(OBSERVABLE_STRUCT, {
      enhancer: refStructEnhancer
    });
    observableDecoratorAnnotation = createDecoratorAnnotation(observableAnnotation);
    assign2(createObservable, observableDecoratorAnnotation);
    observableFactories = {
      box: function box(value, options) {
        var o2 = asCreateObservableOptions(options);
        return new ObservableValue(value, getEnhancerFromOptions(o2), o2.name, true, o2.equals);
      },
      array: function array(initialValues, options) {
        var o2 = asCreateObservableOptions(options);
        return (globalState.useProxies === false || o2.proxy === false ? createLegacyArray : createObservableArray)(initialValues, getEnhancerFromOptions(o2), o2.name);
      },
      map: function map2(initialValues, options) {
        var o2 = asCreateObservableOptions(options);
        return new ObservableMap(initialValues, getEnhancerFromOptions(o2), o2.name);
      },
      set: function set2(initialValues, options) {
        var o2 = asCreateObservableOptions(options);
        return new ObservableSet(initialValues, getEnhancerFromOptions(o2), o2.name);
      },
      object: function object(props, decorators, options) {
        return initObservable(function() {
          return extendObservable(globalState.useProxies === false || (options == null ? void 0 : options.proxy) === false ? asObservableObject({}, options) : asDynamicObservableObject({}, options), props, decorators);
        });
      },
      ref: createDecoratorAnnotation(observableRefAnnotation),
      shallow: createDecoratorAnnotation(observableShallowAnnotation),
      deep: observableDecoratorAnnotation,
      struct: createDecoratorAnnotation(observableStructAnnotation)
    };
    observable = assign2(createObservable, observableFactories);
    COMPUTED = "computed";
    COMPUTED_STRUCT = "computed.struct";
    computedAnnotation = createComputedAnnotation(COMPUTED);
    computedStructAnnotation = createComputedAnnotation(COMPUTED_STRUCT, {
      equals: comparer.structural
    });
    computed = function computed2(arg1, arg2) {
      if (is20223Decorator(arg2)) {
        return computedAnnotation.decorate_20223_(arg1, arg2);
      }
      if (isStringish(arg2)) {
        return storeAnnotation(arg1, arg2, computedAnnotation);
      }
      if (isPlainObject(arg1)) {
        return createDecoratorAnnotation(createComputedAnnotation(COMPUTED, arg1));
      }
      if (true) {
        if (!isFunction(arg1)) {
          die("First argument to `computed` should be an expression.");
        }
        if (isFunction(arg2)) {
          die("A setter as second argument is no longer supported, use `{ set: fn }` option instead");
        }
      }
      var opts = isPlainObject(arg2) ? arg2 : {};
      opts.get = arg1;
      opts.name || (opts.name = arg1.name || "");
      return new ComputedValue(opts);
    };
    Object.assign(computed, computedAnnotation);
    computed.struct = createDecoratorAnnotation(computedStructAnnotation);
    currentActionId = 0;
    nextActionId = 1;
    isFunctionNameConfigurable = (_getDescriptor$config = (_getDescriptor = getDescriptor(function() {
    }, "name")) == null ? void 0 : _getDescriptor.configurable) != null ? _getDescriptor$config : false;
    tmpNameDescriptor = {
      value: "action",
      configurable: true,
      writable: false,
      enumerable: false
    };
    CREATE = "create";
    ObservableValue = (function(_Atom) {
      function ObservableValue2(value, enhancer, name_, notifySpy, equals) {
        var _this;
        if (name_ === void 0) {
          name_ = true ? "ObservableValue@" + getNextId() : "ObservableValue";
        }
        if (notifySpy === void 0) {
          notifySpy = true;
        }
        if (equals === void 0) {
          equals = comparer["default"];
        }
        _this = _Atom.call(this, name_) || this;
        _this.enhancer = void 0;
        _this.name_ = void 0;
        _this.equals = void 0;
        _this.hasUnreportedChange_ = false;
        _this.interceptors_ = void 0;
        _this.changeListeners_ = void 0;
        _this.value_ = void 0;
        _this.dehancer = void 0;
        _this.enhancer = enhancer;
        _this.name_ = name_;
        _this.equals = equals;
        _this.value_ = enhancer(value, void 0, name_);
        if (notifySpy && isSpyEnabled()) {
          var _this$value_;
          spyReport({
            type: CREATE,
            object: _this,
            observableKind: "value",
            debugObjectName: _this.name_,
            newValue: "" + ((_this$value_ = _this.value_) == null ? void 0 : _this$value_.toString())
          });
        }
        return _this;
      }
      _inheritsLoose2(ObservableValue2, _Atom);
      var _proto = ObservableValue2.prototype;
      _proto.dehanceValue = function dehanceValue(value) {
        if (this.dehancer !== void 0) {
          return this.dehancer(value);
        }
        return value;
      };
      _proto.set = function set7(newValue) {
        var oldValue = this.value_;
        newValue = this.prepareNewValue_(newValue);
        if (newValue !== globalState.UNCHANGED) {
          var notifySpy = isSpyEnabled();
          if (notifySpy) {
            spyReportStart({
              type: UPDATE,
              object: this,
              observableKind: "value",
              debugObjectName: this.name_,
              newValue,
              oldValue
            });
          }
          this.setNewValue_(newValue);
          if (notifySpy) {
            spyReportEnd();
          }
        }
      };
      _proto.prepareNewValue_ = function prepareNewValue_(newValue) {
        checkIfStateModificationsAreAllowed(this);
        if (hasInterceptors(this)) {
          var change = interceptChange(this, {
            object: this,
            type: UPDATE,
            newValue
          });
          if (!change) {
            return globalState.UNCHANGED;
          }
          newValue = change.newValue;
        }
        newValue = this.enhancer(newValue, this.value_, this.name_);
        return this.equals(this.value_, newValue) ? globalState.UNCHANGED : newValue;
      };
      _proto.setNewValue_ = function setNewValue_(newValue) {
        var oldValue = this.value_;
        this.value_ = newValue;
        this.reportChanged();
        if (hasListeners(this)) {
          notifyListeners(this, {
            type: UPDATE,
            object: this,
            newValue,
            oldValue
          });
        }
      };
      _proto.get = function get5() {
        this.reportObserved();
        return this.dehanceValue(this.value_);
      };
      _proto.intercept_ = function intercept_(handler) {
        return registerInterceptor(this, handler);
      };
      _proto.observe_ = function observe_(listener, fireImmediately) {
        if (fireImmediately) {
          listener({
            observableKind: "value",
            debugObjectName: this.name_,
            object: this,
            type: UPDATE,
            newValue: this.value_,
            oldValue: void 0
          });
        }
        return registerListener(this, listener);
      };
      _proto.raw = function raw() {
        return this.value_;
      };
      _proto.toJSON = function toJSON2() {
        return this.get();
      };
      _proto.toString = function toString2() {
        return this.name_ + "[" + this.value_ + "]";
      };
      _proto.valueOf = function valueOf() {
        return toPrimitive(this.get());
      };
      _proto[Symbol.toPrimitive] = function() {
        return this.valueOf();
      };
      return ObservableValue2;
    })(Atom);
    isObservableValue = createInstanceofPredicate("ObservableValue", ObservableValue);
    ComputedValue = (function() {
      function ComputedValue2(options) {
        this.dependenciesState_ = IDerivationState_.NOT_TRACKING_;
        this.observing_ = [];
        this.newObserving_ = null;
        this.observers_ = /* @__PURE__ */ new Set();
        this.runId_ = 0;
        this.lastAccessedBy_ = 0;
        this.lowestObserverState_ = IDerivationState_.UP_TO_DATE_;
        this.unboundDepsCount_ = 0;
        this.value_ = new CaughtException(null);
        this.name_ = void 0;
        this.triggeredBy_ = void 0;
        this.flags_ = 0;
        this.derivation = void 0;
        this.setter_ = void 0;
        this.isTracing_ = TraceMode.NONE;
        this.scope_ = void 0;
        this.equals_ = void 0;
        this.requiresReaction_ = void 0;
        this.keepAlive_ = void 0;
        this.onBOL = void 0;
        this.onBUOL = void 0;
        if (!options.get) {
          die(31);
        }
        this.derivation = options.get;
        this.name_ = options.name || (true ? "ComputedValue@" + getNextId() : "ComputedValue");
        if (options.set) {
          this.setter_ = createAction(true ? this.name_ + "-setter" : "ComputedValue-setter", options.set);
        }
        this.equals_ = options.equals || (options.compareStructural || options.struct ? comparer.structural : comparer["default"]);
        this.scope_ = options.context;
        this.requiresReaction_ = options.requiresReaction;
        this.keepAlive_ = !!options.keepAlive;
      }
      var _proto = ComputedValue2.prototype;
      _proto.onBecomeStale_ = function onBecomeStale_() {
        propagateMaybeChanged(this);
      };
      _proto.onBO = function onBO() {
        if (this.onBOL) {
          this.onBOL.forEach(function(listener) {
            return listener();
          });
        }
      };
      _proto.onBUO = function onBUO() {
        if (this.onBUOL) {
          this.onBUOL.forEach(function(listener) {
            return listener();
          });
        }
      };
      _proto.get = function get5() {
        if (this.isComputing) {
          die(32, this.name_, this.derivation);
        }
        if (globalState.inBatch === 0 && // !globalState.trackingDerivatpion &&
        this.observers_.size === 0 && !this.keepAlive_) {
          if (shouldCompute(this)) {
            this.warnAboutUntrackedRead_();
            startBatch();
            this.value_ = this.computeValue_(false);
            endBatch();
          }
        } else {
          reportObserved(this);
          if (shouldCompute(this)) {
            var prevTrackingContext = globalState.trackingContext;
            if (this.keepAlive_ && !prevTrackingContext) {
              globalState.trackingContext = this;
            }
            if (this.trackAndCompute()) {
              propagateChangeConfirmed(this);
            }
            globalState.trackingContext = prevTrackingContext;
          }
        }
        var result = this.value_;
        if (isCaughtException(result)) {
          throw result.cause;
        }
        return result;
      };
      _proto.set = function set7(value) {
        if (this.setter_) {
          if (this.isRunningSetter) {
            die(33, this.name_);
          }
          this.isRunningSetter = true;
          try {
            this.setter_.call(this.scope_, value);
          } finally {
            this.isRunningSetter = false;
          }
        } else {
          die(34, this.name_);
        }
      };
      _proto.trackAndCompute = function trackAndCompute() {
        var oldValue = this.value_;
        var wasSuspended = (
          /* see #1208 */
          this.dependenciesState_ === IDerivationState_.NOT_TRACKING_
        );
        var newValue = this.computeValue_(true);
        var changed = wasSuspended || isCaughtException(oldValue) || isCaughtException(newValue) || !this.equals_(oldValue, newValue);
        if (changed) {
          this.value_ = newValue;
          if (isSpyEnabled()) {
            spyReport({
              observableKind: "computed",
              debugObjectName: this.name_,
              object: this.scope_,
              type: "update",
              oldValue,
              newValue
            });
          }
        }
        return changed;
      };
      _proto.computeValue_ = function computeValue_(track) {
        this.isComputing = true;
        var prev2 = allowStateChangesStart(false);
        var res;
        if (track) {
          res = trackDerivedFunction(this, this.derivation, this.scope_);
        } else {
          if (globalState.disableErrorBoundaries === true) {
            res = this.derivation.call(this.scope_);
          } else {
            try {
              res = this.derivation.call(this.scope_);
            } catch (e) {
              res = new CaughtException(e);
            }
          }
        }
        allowStateChangesEnd(prev2);
        this.isComputing = false;
        return res;
      };
      _proto.suspend_ = function suspend_() {
        if (!this.keepAlive_) {
          clearObserving(this);
          this.value_ = void 0;
          if (this.isTracing_ !== TraceMode.NONE) {
            console.log("[mobx.trace] Computed value '" + this.name_ + "' was suspended and it will recompute on the next access.");
          }
        }
      };
      _proto.observe_ = function observe_(listener, fireImmediately) {
        var _this = this;
        var firstTime = true;
        var prevValue = void 0;
        return autorun(function() {
          var newValue = _this.get();
          if (!firstTime || fireImmediately) {
            var prevU = untrackedStart();
            listener({
              observableKind: "computed",
              debugObjectName: _this.name_,
              type: UPDATE,
              object: _this,
              newValue,
              oldValue: prevValue
            });
            untrackedEnd(prevU);
          }
          firstTime = false;
          prevValue = newValue;
        });
      };
      _proto.warnAboutUntrackedRead_ = function warnAboutUntrackedRead_() {
        if (false) {
          return;
        }
        if (this.isTracing_ !== TraceMode.NONE) {
          console.log("[mobx.trace] Computed value '" + this.name_ + "' is being read outside a reactive context. Doing a full recompute.");
        }
        if (typeof this.requiresReaction_ === "boolean" ? this.requiresReaction_ : globalState.computedRequiresReaction) {
          console.warn("[mobx] Computed value '" + this.name_ + "' is being read outside a reactive context. Doing a full recompute.");
        }
      };
      _proto.toString = function toString2() {
        return this.name_ + "[" + this.derivation.toString() + "]";
      };
      _proto.valueOf = function valueOf() {
        return toPrimitive(this.get());
      };
      _proto[Symbol.toPrimitive] = function() {
        return this.valueOf();
      };
      return _createClass(ComputedValue2, [{
        key: "isComputing",
        get: function get5() {
          return getFlag(this.flags_, ComputedValue2.isComputingMask_);
        },
        set: function set7(newValue) {
          this.flags_ = setFlag(this.flags_, ComputedValue2.isComputingMask_, newValue);
        }
      }, {
        key: "isRunningSetter",
        get: function get5() {
          return getFlag(this.flags_, ComputedValue2.isRunningSetterMask_);
        },
        set: function set7(newValue) {
          this.flags_ = setFlag(this.flags_, ComputedValue2.isRunningSetterMask_, newValue);
        }
      }, {
        key: "isBeingObserved",
        get: function get5() {
          return getFlag(this.flags_, ComputedValue2.isBeingObservedMask_);
        },
        set: function set7(newValue) {
          this.flags_ = setFlag(this.flags_, ComputedValue2.isBeingObservedMask_, newValue);
        }
      }, {
        key: "isPendingUnobservation",
        get: function get5() {
          return getFlag(this.flags_, ComputedValue2.isPendingUnobservationMask_);
        },
        set: function set7(newValue) {
          this.flags_ = setFlag(this.flags_, ComputedValue2.isPendingUnobservationMask_, newValue);
        }
      }, {
        key: "diffValue",
        get: function get5() {
          return getFlag(this.flags_, ComputedValue2.diffValueMask_) ? 1 : 0;
        },
        set: function set7(newValue) {
          this.flags_ = setFlag(this.flags_, ComputedValue2.diffValueMask_, newValue === 1 ? true : false);
        }
      }]);
    })();
    ComputedValue.isComputingMask_ = 1;
    ComputedValue.isRunningSetterMask_ = 2;
    ComputedValue.isBeingObservedMask_ = 4;
    ComputedValue.isPendingUnobservationMask_ = 8;
    ComputedValue.diffValueMask_ = 16;
    isComputedValue = createInstanceofPredicate("ComputedValue", ComputedValue);
    (function(IDerivationState_2) {
      IDerivationState_2[IDerivationState_2["NOT_TRACKING_"] = -1] = "NOT_TRACKING_";
      IDerivationState_2[IDerivationState_2["UP_TO_DATE_"] = 0] = "UP_TO_DATE_";
      IDerivationState_2[IDerivationState_2["POSSIBLY_STALE_"] = 1] = "POSSIBLY_STALE_";
      IDerivationState_2[IDerivationState_2["STALE_"] = 2] = "STALE_";
    })(IDerivationState_ || (IDerivationState_ = {}));
    (function(TraceMode2) {
      TraceMode2[TraceMode2["NONE"] = 0] = "NONE";
      TraceMode2[TraceMode2["LOG"] = 1] = "LOG";
      TraceMode2[TraceMode2["BREAK"] = 2] = "BREAK";
    })(TraceMode || (TraceMode = {}));
    CaughtException = function CaughtException2(cause) {
      this.cause = void 0;
      this.cause = cause;
    };
    persistentKeys = ["mobxGuid", "spyListeners", "enforceActions", "computedRequiresReaction", "reactionRequiresObservable", "observableRequiresReaction", "allowStateReads", "disableErrorBoundaries", "runId", "UNCHANGED", "useProxies"];
    MobXGlobals = function MobXGlobals2() {
      this.version = 6;
      this.UNCHANGED = {};
      this.trackingDerivation = null;
      this.trackingContext = null;
      this.runId = 0;
      this.mobxGuid = 0;
      this.inBatch = 0;
      this.pendingUnobservations = [];
      this.pendingReactions = [];
      this.isRunningReactions = false;
      this.allowStateChanges = false;
      this.allowStateReads = true;
      this.enforceActions = true;
      this.spyListeners = [];
      this.globalReactionErrorHandlers = [];
      this.computedRequiresReaction = false;
      this.reactionRequiresObservable = false;
      this.observableRequiresReaction = false;
      this.disableErrorBoundaries = false;
      this.suppressReactionErrors = false;
      this.useProxies = true;
      this.verifyProxies = false;
      this.safeDescriptors = true;
    };
    canMergeGlobalState = true;
    isolateCalled = false;
    globalState = (function() {
      var global2 = getGlobal();
      if (global2.__mobxInstanceCount > 0 && !global2.__mobxGlobals) {
        canMergeGlobalState = false;
      }
      if (global2.__mobxGlobals && global2.__mobxGlobals.version !== new MobXGlobals().version) {
        canMergeGlobalState = false;
      }
      if (!canMergeGlobalState) {
        setTimeout(function() {
          if (!isolateCalled) {
            die(35);
          }
        }, 1);
        return new MobXGlobals();
      } else if (global2.__mobxGlobals) {
        global2.__mobxInstanceCount += 1;
        if (!global2.__mobxGlobals.UNCHANGED) {
          global2.__mobxGlobals.UNCHANGED = {};
        }
        return global2.__mobxGlobals;
      } else {
        global2.__mobxInstanceCount = 1;
        return global2.__mobxGlobals = new MobXGlobals();
      }
    })();
    Reaction = (function() {
      function Reaction2(name_, onInvalidate_, errorHandler_, requiresObservable_) {
        if (name_ === void 0) {
          name_ = true ? "Reaction@" + getNextId() : "Reaction";
        }
        this.name_ = void 0;
        this.onInvalidate_ = void 0;
        this.errorHandler_ = void 0;
        this.requiresObservable_ = void 0;
        this.observing_ = [];
        this.newObserving_ = [];
        this.dependenciesState_ = IDerivationState_.NOT_TRACKING_;
        this.runId_ = 0;
        this.unboundDepsCount_ = 0;
        this.flags_ = 0;
        this.isTracing_ = TraceMode.NONE;
        this.name_ = name_;
        this.onInvalidate_ = onInvalidate_;
        this.errorHandler_ = errorHandler_;
        this.requiresObservable_ = requiresObservable_;
      }
      var _proto = Reaction2.prototype;
      _proto.onBecomeStale_ = function onBecomeStale_() {
        this.schedule_();
      };
      _proto.schedule_ = function schedule_() {
        if (!this.isScheduled) {
          this.isScheduled = true;
          globalState.pendingReactions.push(this);
          runReactions();
        }
      };
      _proto.runReaction_ = function runReaction_() {
        if (!this.isDisposed) {
          startBatch();
          this.isScheduled = false;
          var prev2 = globalState.trackingContext;
          globalState.trackingContext = this;
          if (shouldCompute(this)) {
            this.isTrackPending = true;
            try {
              this.onInvalidate_();
              if (this.isTrackPending && isSpyEnabled()) {
                spyReport({
                  name: this.name_,
                  type: "scheduled-reaction"
                });
              }
            } catch (e) {
              this.reportExceptionInDerivation_(e);
            }
          }
          globalState.trackingContext = prev2;
          endBatch();
        }
      };
      _proto.track = function track(fn) {
        if (this.isDisposed) {
          return;
        }
        startBatch();
        var notify = isSpyEnabled();
        var startTime;
        if (notify) {
          startTime = Date.now();
          spyReportStart({
            name: this.name_,
            type: "reaction"
          });
        }
        this.isRunning = true;
        var prevReaction = globalState.trackingContext;
        globalState.trackingContext = this;
        var result = trackDerivedFunction(this, fn, void 0);
        globalState.trackingContext = prevReaction;
        this.isRunning = false;
        this.isTrackPending = false;
        if (this.isDisposed) {
          clearObserving(this);
        }
        if (isCaughtException(result)) {
          this.reportExceptionInDerivation_(result.cause);
        }
        if (notify) {
          spyReportEnd({
            time: Date.now() - startTime
          });
        }
        endBatch();
      };
      _proto.reportExceptionInDerivation_ = function reportExceptionInDerivation_(error) {
        var _this = this;
        if (this.errorHandler_) {
          this.errorHandler_(error, this);
          return;
        }
        if (globalState.disableErrorBoundaries) {
          throw error;
        }
        var message = true ? "[mobx] Encountered an uncaught exception that was thrown by a reaction or observer component, in: '" + this + "'" : "[mobx] uncaught error in '" + this + "'";
        if (!globalState.suppressReactionErrors) {
          console.error(message, error);
        } else if (true) {
          console.warn("[mobx] (error in reaction '" + this.name_ + "' suppressed, fix error of causing action below)");
        }
        if (isSpyEnabled()) {
          spyReport({
            type: "error",
            name: this.name_,
            message,
            error: "" + error
          });
        }
        globalState.globalReactionErrorHandlers.forEach(function(f2) {
          return f2(error, _this);
        });
      };
      _proto.dispose = function dispose() {
        if (!this.isDisposed) {
          this.isDisposed = true;
          if (!this.isRunning) {
            startBatch();
            clearObserving(this);
            endBatch();
          }
        }
      };
      _proto.getDisposer_ = function getDisposer_(abortSignal) {
        var _this2 = this;
        var dispose = function dispose2() {
          _this2.dispose();
          abortSignal == null || abortSignal.removeEventListener == null || abortSignal.removeEventListener("abort", dispose2);
        };
        abortSignal == null || abortSignal.addEventListener == null || abortSignal.addEventListener("abort", dispose);
        dispose[$mobx] = this;
        if ("dispose" in Symbol && typeof Symbol.dispose === "symbol") {
          dispose[Symbol.dispose] = dispose;
        }
        return dispose;
      };
      _proto.toString = function toString2() {
        return "Reaction[" + this.name_ + "]";
      };
      _proto.trace = function trace$1(enterBreakPoint) {
        if (enterBreakPoint === void 0) {
          enterBreakPoint = false;
        }
        trace(this, enterBreakPoint);
      };
      return _createClass(Reaction2, [{
        key: "isDisposed",
        get: function get5() {
          return getFlag(this.flags_, Reaction2.isDisposedMask_);
        },
        set: function set7(newValue) {
          this.flags_ = setFlag(this.flags_, Reaction2.isDisposedMask_, newValue);
        }
      }, {
        key: "isScheduled",
        get: function get5() {
          return getFlag(this.flags_, Reaction2.isScheduledMask_);
        },
        set: function set7(newValue) {
          this.flags_ = setFlag(this.flags_, Reaction2.isScheduledMask_, newValue);
        }
      }, {
        key: "isTrackPending",
        get: function get5() {
          return getFlag(this.flags_, Reaction2.isTrackPendingMask_);
        },
        set: function set7(newValue) {
          this.flags_ = setFlag(this.flags_, Reaction2.isTrackPendingMask_, newValue);
        }
      }, {
        key: "isRunning",
        get: function get5() {
          return getFlag(this.flags_, Reaction2.isRunningMask_);
        },
        set: function set7(newValue) {
          this.flags_ = setFlag(this.flags_, Reaction2.isRunningMask_, newValue);
        }
      }, {
        key: "diffValue",
        get: function get5() {
          return getFlag(this.flags_, Reaction2.diffValueMask_) ? 1 : 0;
        },
        set: function set7(newValue) {
          this.flags_ = setFlag(this.flags_, Reaction2.diffValueMask_, newValue === 1 ? true : false);
        }
      }]);
    })();
    Reaction.isDisposedMask_ = 1;
    Reaction.isScheduledMask_ = 2;
    Reaction.isTrackPendingMask_ = 4;
    Reaction.isRunningMask_ = 8;
    Reaction.diffValueMask_ = 16;
    MAX_REACTION_ITERATIONS = 100;
    reactionScheduler = function reactionScheduler2(f2) {
      return f2();
    };
    isReaction = createInstanceofPredicate("Reaction", Reaction);
    END_EVENT = {
      type: "report-end",
      spyReportEnd: true
    };
    ACTION = "action";
    ACTION_BOUND = "action.bound";
    AUTOACTION = "autoAction";
    AUTOACTION_BOUND = "autoAction.bound";
    DEFAULT_ACTION_NAME = "<unnamed action>";
    actionAnnotation = createActionAnnotation(ACTION);
    actionBoundAnnotation = createActionAnnotation(ACTION_BOUND, {
      bound: true
    });
    autoActionAnnotation = createActionAnnotation(AUTOACTION, {
      autoAction: true
    });
    autoActionBoundAnnotation = createActionAnnotation(AUTOACTION_BOUND, {
      autoAction: true,
      bound: true
    });
    action = createActionFactory(false);
    Object.assign(action, actionAnnotation);
    autoAction = createActionFactory(true);
    Object.assign(autoAction, autoActionAnnotation);
    action.bound = createDecoratorAnnotation(actionBoundAnnotation);
    autoAction.bound = createDecoratorAnnotation(autoActionBoundAnnotation);
    run = function run2(f2) {
      return f2();
    };
    ON_BECOME_OBSERVED = "onBO";
    ON_BECOME_UNOBSERVED = "onBUO";
    NEVER = "never";
    ALWAYS = "always";
    OBSERVED = "observed";
    generatorId = 0;
    FlowCancellationError.prototype = Object.create(Error.prototype);
    flowAnnotation = createFlowAnnotation("flow");
    flowBoundAnnotation = createFlowAnnotation("flow.bound", {
      bound: true
    });
    flow = Object.assign(function flow2(arg1, arg2) {
      if (is20223Decorator(arg2)) {
        return flowAnnotation.decorate_20223_(arg1, arg2);
      }
      if (isStringish(arg2)) {
        return storeAnnotation(arg1, arg2, flowAnnotation);
      }
      if (arguments.length !== 1) {
        die("Flow expects single argument with generator function");
      }
      var generator = arg1;
      var name = generator.name || "<unnamed flow>";
      var res = function res2() {
        var ctx = this;
        var args = arguments;
        var runId = ++generatorId;
        var gen = action(name + " - runid: " + runId + " - init", generator).apply(ctx, args);
        var rejector;
        var pendingPromise = void 0;
        var promise = new Promise(function(resolve, reject) {
          var stepId = 0;
          rejector = reject;
          function onFulfilled(res3) {
            pendingPromise = void 0;
            var ret;
            try {
              ret = action(name + " - runid: " + runId + " - yield " + stepId++, gen.next).call(gen, res3);
            } catch (e) {
              return reject(e);
            }
            next2(ret);
          }
          function onRejected(err) {
            pendingPromise = void 0;
            var ret;
            try {
              ret = action(name + " - runid: " + runId + " - yield " + stepId++, gen["throw"]).call(gen, err);
            } catch (e) {
              return reject(e);
            }
            next2(ret);
          }
          function next2(ret) {
            if (isFunction(ret == null ? void 0 : ret.then)) {
              ret.then(next2, reject);
              return;
            }
            if (ret.done) {
              return resolve(ret.value);
            }
            pendingPromise = Promise.resolve(ret.value);
            return pendingPromise.then(onFulfilled, onRejected);
          }
          onFulfilled(void 0);
        });
        promise.cancel = action(name + " - runid: " + runId + " - cancel", function() {
          try {
            if (pendingPromise) {
              cancelPromise(pendingPromise);
            }
            var _res = gen["return"](void 0);
            var yieldedPromise = Promise.resolve(_res.value);
            yieldedPromise.then(noop, noop);
            cancelPromise(yieldedPromise);
            rejector(new FlowCancellationError());
          } catch (e) {
            rejector(e);
          }
        });
        return promise;
      };
      res.isMobXFlow = true;
      return res;
    }, flowAnnotation);
    flow.bound = createDecoratorAnnotation(flowBoundAnnotation);
    objectProxyTraps = {
      has: function has2(target, name) {
        if (globalState.trackingDerivation) {
          warnAboutProxyRequirement("detect new properties using the 'in' operator. Use 'has' from 'mobx' instead.");
        }
        return getAdm(target).has_(name);
      },
      get: function get2(target, name) {
        return getAdm(target).get_(name);
      },
      set: function set4(target, name, value) {
        var _getAdm$set_;
        if (!isStringish(name)) {
          return false;
        }
        if (!getAdm(target).values_.has(name)) {
          warnAboutProxyRequirement("add a new observable property through direct assignment. Use 'set' from 'mobx' instead.");
        }
        return (_getAdm$set_ = getAdm(target).set_(name, value, true)) != null ? _getAdm$set_ : true;
      },
      deleteProperty: function deleteProperty(target, name) {
        var _getAdm$delete_;
        if (true) {
          warnAboutProxyRequirement("delete properties from an observable object. Use 'remove' from 'mobx' instead.");
        }
        if (!isStringish(name)) {
          return false;
        }
        return (_getAdm$delete_ = getAdm(target).delete_(name, true)) != null ? _getAdm$delete_ : true;
      },
      defineProperty: function defineProperty2(target, name, descriptor) {
        var _getAdm$definePropert;
        if (true) {
          warnAboutProxyRequirement("define property on an observable object. Use 'defineProperty' from 'mobx' instead.");
        }
        return (_getAdm$definePropert = getAdm(target).defineProperty_(name, descriptor)) != null ? _getAdm$definePropert : true;
      },
      ownKeys: function ownKeys2(target) {
        if (globalState.trackingDerivation) {
          warnAboutProxyRequirement("iterate keys to detect added / removed properties. Use 'keys' from 'mobx' instead.");
        }
        return getAdm(target).ownKeys_();
      },
      preventExtensions: function preventExtensions(target) {
        die(13);
      }
    };
    keysSymbol = Symbol("mobx-keys");
    SPLICE = "splice";
    UPDATE = "update";
    MAX_SPLICE_SIZE = 1e4;
    arrayTraps = {
      get: function get3(target, name) {
        var adm = target[$mobx];
        if (name === $mobx) {
          return adm;
        }
        if (name === "length") {
          return adm.getArrayLength_();
        }
        if (typeof name === "string" && !isNaN(name)) {
          return adm.get_(parseInt(name));
        }
        if (hasProp(arrayExtensions, name)) {
          return arrayExtensions[name];
        }
        return target[name];
      },
      set: function set5(target, name, value) {
        var adm = target[$mobx];
        if (name === "length") {
          adm.setArrayLength_(value);
        }
        if (typeof name === "symbol" || isNaN(name)) {
          target[name] = value;
        } else {
          adm.set_(parseInt(name), value);
        }
        return true;
      },
      preventExtensions: function preventExtensions2() {
        die(15);
      }
    };
    ObservableArrayAdministration = (function() {
      function ObservableArrayAdministration2(name, enhancer, owned_, legacyMode_) {
        if (name === void 0) {
          name = true ? "ObservableArray@" + getNextId() : "ObservableArray";
        }
        this.owned_ = void 0;
        this.legacyMode_ = void 0;
        this.atom_ = void 0;
        this.values_ = [];
        this.interceptors_ = void 0;
        this.changeListeners_ = void 0;
        this.enhancer_ = void 0;
        this.dehancer = void 0;
        this.proxy_ = void 0;
        this.lastKnownLength_ = 0;
        this.owned_ = owned_;
        this.legacyMode_ = legacyMode_;
        this.atom_ = new Atom(name);
        this.enhancer_ = function(newV, oldV) {
          return enhancer(newV, oldV, true ? name + "[..]" : "ObservableArray[..]");
        };
      }
      var _proto = ObservableArrayAdministration2.prototype;
      _proto.dehanceValue_ = function dehanceValue_(value) {
        if (this.dehancer !== void 0) {
          return this.dehancer(value);
        }
        return value;
      };
      _proto.dehanceValues_ = function dehanceValues_(values2) {
        if (this.dehancer !== void 0 && values2.length > 0) {
          return values2.map(this.dehancer);
        }
        return values2;
      };
      _proto.intercept_ = function intercept_(handler) {
        return registerInterceptor(this, handler);
      };
      _proto.observe_ = function observe_(listener, fireImmediately) {
        if (fireImmediately === void 0) {
          fireImmediately = false;
        }
        if (fireImmediately) {
          listener({
            observableKind: "array",
            object: this.proxy_,
            debugObjectName: this.atom_.name_,
            type: "splice",
            index: 0,
            added: this.values_.slice(),
            addedCount: this.values_.length,
            removed: [],
            removedCount: 0
          });
        }
        return registerListener(this, listener);
      };
      _proto.getArrayLength_ = function getArrayLength_() {
        this.atom_.reportObserved();
        return this.values_.length;
      };
      _proto.setArrayLength_ = function setArrayLength_(newLength) {
        if (typeof newLength !== "number" || isNaN(newLength) || newLength < 0) {
          die("Out of range: " + newLength);
        }
        var currentLength = this.values_.length;
        if (newLength === currentLength) {
          return;
        } else if (newLength > currentLength) {
          var newItems = new Array(newLength - currentLength);
          for (var i2 = 0; i2 < newLength - currentLength; i2++) {
            newItems[i2] = void 0;
          }
          this.spliceWithArray_(currentLength, 0, newItems);
        } else {
          this.spliceWithArray_(newLength, currentLength - newLength);
        }
      };
      _proto.updateArrayLength_ = function updateArrayLength_(oldLength, delta) {
        if (oldLength !== this.lastKnownLength_) {
          die(16);
        }
        this.lastKnownLength_ += delta;
        if (this.legacyMode_ && delta > 0) {
          reserveArrayBuffer(oldLength + delta + 1);
        }
      };
      _proto.spliceWithArray_ = function spliceWithArray_(index, deleteCount, newItems) {
        var _this = this;
        checkIfStateModificationsAreAllowed(this.atom_);
        var length2 = this.values_.length;
        if (index === void 0) {
          index = 0;
        } else if (index > length2) {
          index = length2;
        } else if (index < 0) {
          index = Math.max(0, length2 + index);
        }
        if (arguments.length === 1) {
          deleteCount = length2 - index;
        } else if (deleteCount === void 0 || deleteCount === null) {
          deleteCount = 0;
        } else {
          deleteCount = Math.max(0, Math.min(deleteCount, length2 - index));
        }
        if (newItems === void 0) {
          newItems = EMPTY_ARRAY;
        }
        if (hasInterceptors(this)) {
          var change = interceptChange(this, {
            object: this.proxy_,
            type: SPLICE,
            index,
            removedCount: deleteCount,
            added: newItems
          });
          if (!change) {
            return EMPTY_ARRAY;
          }
          deleteCount = change.removedCount;
          newItems = change.added;
        }
        newItems = newItems.length === 0 ? newItems : newItems.map(function(v2) {
          return _this.enhancer_(v2, void 0);
        });
        if (this.legacyMode_ || true) {
          var lengthDelta = newItems.length - deleteCount;
          this.updateArrayLength_(length2, lengthDelta);
        }
        var res = this.spliceItemsIntoValues_(index, deleteCount, newItems);
        if (deleteCount !== 0 || newItems.length !== 0) {
          this.notifyArraySplice_(index, newItems, res);
        }
        return this.dehanceValues_(res);
      };
      _proto.spliceItemsIntoValues_ = function spliceItemsIntoValues_(index, deleteCount, newItems) {
        if (newItems.length < MAX_SPLICE_SIZE) {
          var _this$values_;
          return (_this$values_ = this.values_).splice.apply(_this$values_, [index, deleteCount].concat(newItems));
        } else {
          var res = this.values_.slice(index, index + deleteCount);
          var oldItems = this.values_.slice(index + deleteCount);
          this.values_.length += newItems.length - deleteCount;
          for (var i2 = 0; i2 < newItems.length; i2++) {
            this.values_[index + i2] = newItems[i2];
          }
          for (var _i = 0; _i < oldItems.length; _i++) {
            this.values_[index + newItems.length + _i] = oldItems[_i];
          }
          return res;
        }
      };
      _proto.notifyArrayChildUpdate_ = function notifyArrayChildUpdate_(index, newValue, oldValue) {
        var notifySpy = !this.owned_ && isSpyEnabled();
        var notify = hasListeners(this);
        var change = notify || notifySpy ? {
          observableKind: "array",
          object: this.proxy_,
          type: UPDATE,
          debugObjectName: this.atom_.name_,
          index,
          newValue,
          oldValue
        } : null;
        if (notifySpy) {
          spyReportStart(change);
        }
        this.atom_.reportChanged();
        if (notify) {
          notifyListeners(this, change);
        }
        if (notifySpy) {
          spyReportEnd();
        }
      };
      _proto.notifyArraySplice_ = function notifyArraySplice_(index, added, removed) {
        var notifySpy = !this.owned_ && isSpyEnabled();
        var notify = hasListeners(this);
        var change = notify || notifySpy ? {
          observableKind: "array",
          object: this.proxy_,
          debugObjectName: this.atom_.name_,
          type: SPLICE,
          index,
          removed,
          added,
          removedCount: removed.length,
          addedCount: added.length
        } : null;
        if (notifySpy) {
          spyReportStart(change);
        }
        this.atom_.reportChanged();
        if (notify) {
          notifyListeners(this, change);
        }
        if (notifySpy) {
          spyReportEnd();
        }
      };
      _proto.get_ = function get_(index) {
        if (this.legacyMode_ && index >= this.values_.length) {
          console.warn(true ? "[mobx.array] Attempt to read an array index (" + index + ") that is out of bounds (" + this.values_.length + "). Please check length first. Out of bound indices will not be tracked by MobX" : "[mobx] Out of bounds read: " + index);
          return void 0;
        }
        this.atom_.reportObserved();
        return this.dehanceValue_(this.values_[index]);
      };
      _proto.set_ = function set_(index, newValue) {
        var values2 = this.values_;
        if (this.legacyMode_ && index > values2.length) {
          die(17, index, values2.length);
        }
        if (index < values2.length) {
          checkIfStateModificationsAreAllowed(this.atom_);
          var oldValue = values2[index];
          if (hasInterceptors(this)) {
            var change = interceptChange(this, {
              type: UPDATE,
              object: this.proxy_,
              // since "this" is the real array we need to pass its proxy
              index,
              newValue
            });
            if (!change) {
              return;
            }
            newValue = change.newValue;
          }
          newValue = this.enhancer_(newValue, oldValue);
          var changed = newValue !== oldValue;
          if (changed) {
            values2[index] = newValue;
            this.notifyArrayChildUpdate_(index, newValue, oldValue);
          }
        } else {
          var newItems = new Array(index + 1 - values2.length);
          for (var i2 = 0; i2 < newItems.length - 1; i2++) {
            newItems[i2] = void 0;
          }
          newItems[newItems.length - 1] = newValue;
          this.spliceWithArray_(values2.length, 0, newItems);
        }
      };
      return ObservableArrayAdministration2;
    })();
    arrayExtensions = {
      clear: function clear() {
        return this.splice(0);
      },
      replace: function replace2(newItems) {
        var adm = this[$mobx];
        return adm.spliceWithArray_(0, adm.values_.length, newItems);
      },
      // Used by JSON.stringify
      toJSON: function toJSON() {
        return this.slice();
      },
      /*
       * functions that do alter the internal structure of the array, (based on lib.es6.d.ts)
       * since these functions alter the inner structure of the array, the have side effects.
       * Because the have side effects, they should not be used in computed function,
       * and for that reason the do not call dependencyState.notifyObserved
       */
      splice: function splice(index, deleteCount) {
        for (var _len = arguments.length, newItems = new Array(_len > 2 ? _len - 2 : 0), _key = 2; _key < _len; _key++) {
          newItems[_key - 2] = arguments[_key];
        }
        var adm = this[$mobx];
        switch (arguments.length) {
          case 0:
            return [];
          case 1:
            return adm.spliceWithArray_(index);
          case 2:
            return adm.spliceWithArray_(index, deleteCount);
        }
        return adm.spliceWithArray_(index, deleteCount, newItems);
      },
      spliceWithArray: function spliceWithArray(index, deleteCount, newItems) {
        return this[$mobx].spliceWithArray_(index, deleteCount, newItems);
      },
      push: function push() {
        var adm = this[$mobx];
        for (var _len2 = arguments.length, items = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
          items[_key2] = arguments[_key2];
        }
        adm.spliceWithArray_(adm.values_.length, 0, items);
        return adm.values_.length;
      },
      pop: function pop() {
        return this.splice(Math.max(this[$mobx].values_.length - 1, 0), 1)[0];
      },
      shift: function shift() {
        return this.splice(0, 1)[0];
      },
      unshift: function unshift() {
        var adm = this[$mobx];
        for (var _len3 = arguments.length, items = new Array(_len3), _key3 = 0; _key3 < _len3; _key3++) {
          items[_key3] = arguments[_key3];
        }
        adm.spliceWithArray_(0, 0, items);
        return adm.values_.length;
      },
      reverse: function reverse() {
        if (globalState.trackingDerivation) {
          die(37, "reverse");
        }
        this.replace(this.slice().reverse());
        return this;
      },
      sort: function sort() {
        if (globalState.trackingDerivation) {
          die(37, "sort");
        }
        var copy2 = this.slice();
        copy2.sort.apply(copy2, arguments);
        this.replace(copy2);
        return this;
      },
      remove: function remove2(value) {
        var adm = this[$mobx];
        var idx = adm.dehanceValues_(adm.values_).indexOf(value);
        if (idx > -1) {
          this.splice(idx, 1);
          return true;
        }
        return false;
      }
    };
    addArrayExtension("at", simpleFunc);
    addArrayExtension("concat", simpleFunc);
    addArrayExtension("flat", simpleFunc);
    addArrayExtension("includes", simpleFunc);
    addArrayExtension("indexOf", simpleFunc);
    addArrayExtension("join", simpleFunc);
    addArrayExtension("lastIndexOf", simpleFunc);
    addArrayExtension("slice", simpleFunc);
    addArrayExtension("toString", simpleFunc);
    addArrayExtension("toLocaleString", simpleFunc);
    addArrayExtension("toSorted", simpleFunc);
    addArrayExtension("toSpliced", simpleFunc);
    addArrayExtension("with", simpleFunc);
    addArrayExtension("every", mapLikeFunc);
    addArrayExtension("filter", mapLikeFunc);
    addArrayExtension("find", mapLikeFunc);
    addArrayExtension("findIndex", mapLikeFunc);
    addArrayExtension("findLast", mapLikeFunc);
    addArrayExtension("findLastIndex", mapLikeFunc);
    addArrayExtension("flatMap", mapLikeFunc);
    addArrayExtension("forEach", mapLikeFunc);
    addArrayExtension("map", mapLikeFunc);
    addArrayExtension("some", mapLikeFunc);
    addArrayExtension("toReversed", mapLikeFunc);
    addArrayExtension("reduce", reduceLikeFunc);
    addArrayExtension("reduceRight", reduceLikeFunc);
    isObservableArrayAdministration = createInstanceofPredicate("ObservableArrayAdministration", ObservableArrayAdministration);
    ObservableMapMarker = {};
    ADD = "add";
    DELETE = "delete";
    ObservableMap = (function() {
      function ObservableMap2(initialData, enhancer_, name_) {
        var _this = this;
        if (enhancer_ === void 0) {
          enhancer_ = deepEnhancer;
        }
        if (name_ === void 0) {
          name_ = true ? "ObservableMap@" + getNextId() : "ObservableMap";
        }
        this.enhancer_ = void 0;
        this.name_ = void 0;
        this[$mobx] = ObservableMapMarker;
        this.data_ = void 0;
        this.hasMap_ = void 0;
        this.keysAtom_ = void 0;
        this.interceptors_ = void 0;
        this.changeListeners_ = void 0;
        this.dehancer = void 0;
        this.enhancer_ = enhancer_;
        this.name_ = name_;
        if (!isFunction(Map)) {
          die(18);
        }
        initObservable(function() {
          _this.keysAtom_ = createAtom(true ? _this.name_ + ".keys()" : "ObservableMap.keys()");
          _this.data_ = /* @__PURE__ */ new Map();
          _this.hasMap_ = /* @__PURE__ */ new Map();
          if (initialData) {
            _this.merge(initialData);
          }
        });
      }
      var _proto = ObservableMap2.prototype;
      _proto.has_ = function has_(key2) {
        return this.data_.has(key2);
      };
      _proto.has = function has3(key2) {
        var _this2 = this;
        if (!globalState.trackingDerivation) {
          return this.has_(key2);
        }
        var entry = this.hasMap_.get(key2);
        if (!entry) {
          var newEntry = entry = new ObservableValue(this.has_(key2), referenceEnhancer, true ? this.name_ + "." + stringifyKey2(key2) + "?" : "ObservableMap.key?", false);
          this.hasMap_.set(key2, newEntry);
          onBecomeUnobserved(newEntry, function() {
            return _this2.hasMap_["delete"](key2);
          });
        }
        return entry.get();
      };
      _proto.set = function set7(key2, value) {
        var hasKey = this.has_(key2);
        if (hasInterceptors(this)) {
          var change = interceptChange(this, {
            type: hasKey ? UPDATE : ADD,
            object: this,
            newValue: value,
            name: key2
          });
          if (!change) {
            return this;
          }
          value = change.newValue;
        }
        if (hasKey) {
          this.updateValue_(key2, value);
        } else {
          this.addValue_(key2, value);
        }
        return this;
      };
      _proto["delete"] = function _delete(key2) {
        var _this3 = this;
        checkIfStateModificationsAreAllowed(this.keysAtom_);
        if (hasInterceptors(this)) {
          var change = interceptChange(this, {
            type: DELETE,
            object: this,
            name: key2
          });
          if (!change) {
            return false;
          }
        }
        if (this.has_(key2)) {
          var notifySpy = isSpyEnabled();
          var notify = hasListeners(this);
          var _change = notify || notifySpy ? {
            observableKind: "map",
            debugObjectName: this.name_,
            type: DELETE,
            object: this,
            oldValue: this.data_.get(key2).value_,
            name: key2
          } : null;
          if (notifySpy) {
            spyReportStart(_change);
          }
          transaction(function() {
            var _this3$hasMap_$get;
            _this3.keysAtom_.reportChanged();
            (_this3$hasMap_$get = _this3.hasMap_.get(key2)) == null || _this3$hasMap_$get.setNewValue_(false);
            var observable2 = _this3.data_.get(key2);
            observable2.setNewValue_(void 0);
            _this3.data_["delete"](key2);
          });
          if (notify) {
            notifyListeners(this, _change);
          }
          if (notifySpy) {
            spyReportEnd();
          }
          return true;
        }
        return false;
      };
      _proto.updateValue_ = function updateValue_(key2, newValue) {
        var observable2 = this.data_.get(key2);
        newValue = observable2.prepareNewValue_(newValue);
        if (newValue !== globalState.UNCHANGED) {
          var notifySpy = isSpyEnabled();
          var notify = hasListeners(this);
          var change = notify || notifySpy ? {
            observableKind: "map",
            debugObjectName: this.name_,
            type: UPDATE,
            object: this,
            oldValue: observable2.value_,
            name: key2,
            newValue
          } : null;
          if (notifySpy) {
            spyReportStart(change);
          }
          observable2.setNewValue_(newValue);
          if (notify) {
            notifyListeners(this, change);
          }
          if (notifySpy) {
            spyReportEnd();
          }
        }
      };
      _proto.addValue_ = function addValue_(key2, newValue) {
        var _this4 = this;
        checkIfStateModificationsAreAllowed(this.keysAtom_);
        transaction(function() {
          var _this4$hasMap_$get;
          var observable2 = new ObservableValue(newValue, _this4.enhancer_, true ? _this4.name_ + "." + stringifyKey2(key2) : "ObservableMap.key", false);
          _this4.data_.set(key2, observable2);
          newValue = observable2.value_;
          (_this4$hasMap_$get = _this4.hasMap_.get(key2)) == null || _this4$hasMap_$get.setNewValue_(true);
          _this4.keysAtom_.reportChanged();
        });
        var notifySpy = isSpyEnabled();
        var notify = hasListeners(this);
        var change = notify || notifySpy ? {
          observableKind: "map",
          debugObjectName: this.name_,
          type: ADD,
          object: this,
          name: key2,
          newValue
        } : null;
        if (notifySpy) {
          spyReportStart(change);
        }
        if (notify) {
          notifyListeners(this, change);
        }
        if (notifySpy) {
          spyReportEnd();
        }
      };
      _proto.get = function get5(key2) {
        if (this.has(key2)) {
          return this.dehanceValue_(this.data_.get(key2).get());
        }
        return this.dehanceValue_(void 0);
      };
      _proto.dehanceValue_ = function dehanceValue_(value) {
        if (this.dehancer !== void 0) {
          return this.dehancer(value);
        }
        return value;
      };
      _proto.keys = function keys2() {
        this.keysAtom_.reportObserved();
        return this.data_.keys();
      };
      _proto.values = function values2() {
        var self2 = this;
        var keys2 = this.keys();
        return makeIterableForMap({
          next: function next2() {
            var _keys$next = keys2.next(), done = _keys$next.done, value = _keys$next.value;
            return {
              done,
              value: done ? void 0 : self2.get(value)
            };
          }
        });
      };
      _proto.entries = function entries2() {
        var self2 = this;
        var keys2 = this.keys();
        return makeIterableForMap({
          next: function next2() {
            var _keys$next2 = keys2.next(), done = _keys$next2.done, value = _keys$next2.value;
            return {
              done,
              value: done ? void 0 : [value, self2.get(value)]
            };
          }
        });
      };
      _proto[Symbol.iterator] = function() {
        return this.entries();
      };
      _proto.forEach = function forEach(callback, thisArg) {
        for (var _iterator = _createForOfIteratorHelperLoose2(this), _step; !(_step = _iterator()).done; ) {
          var _step$value = _step.value, key2 = _step$value[0], value = _step$value[1];
          callback.call(thisArg, value, key2, this);
        }
      };
      _proto.merge = function merge2(other) {
        var _this5 = this;
        if (isObservableMap(other)) {
          other = new Map(other);
        }
        transaction(function() {
          if (isPlainObject(other)) {
            getPlainObjectKeys(other).forEach(function(key2) {
              return _this5.set(key2, other[key2]);
            });
          } else if (Array.isArray(other)) {
            other.forEach(function(_ref) {
              var key2 = _ref[0], value = _ref[1];
              return _this5.set(key2, value);
            });
          } else if (isES6Map(other)) {
            if (!isPlainES6Map(other)) {
              die(19, other);
            }
            other.forEach(function(value, key2) {
              return _this5.set(key2, value);
            });
          } else if (other !== null && other !== void 0) {
            die(20, other);
          }
        });
        return this;
      };
      _proto.clear = function clear2() {
        var _this6 = this;
        transaction(function() {
          untracked(function() {
            for (var _iterator2 = _createForOfIteratorHelperLoose2(_this6.keys()), _step2; !(_step2 = _iterator2()).done; ) {
              var key2 = _step2.value;
              _this6["delete"](key2);
            }
          });
        });
      };
      _proto.replace = function replace3(values2) {
        var _this7 = this;
        transaction(function() {
          var replacementMap = convertToMap(values2);
          var orderedData = /* @__PURE__ */ new Map();
          var keysReportChangedCalled = false;
          for (var _iterator3 = _createForOfIteratorHelperLoose2(_this7.data_.keys()), _step3; !(_step3 = _iterator3()).done; ) {
            var key2 = _step3.value;
            if (!replacementMap.has(key2)) {
              var deleted = _this7["delete"](key2);
              if (deleted) {
                keysReportChangedCalled = true;
              } else {
                var value = _this7.data_.get(key2);
                orderedData.set(key2, value);
              }
            }
          }
          for (var _iterator4 = _createForOfIteratorHelperLoose2(replacementMap.entries()), _step4; !(_step4 = _iterator4()).done; ) {
            var _step4$value = _step4.value, _key = _step4$value[0], _value = _step4$value[1];
            var keyExisted = _this7.data_.has(_key);
            _this7.set(_key, _value);
            if (_this7.data_.has(_key)) {
              var _value2 = _this7.data_.get(_key);
              orderedData.set(_key, _value2);
              if (!keyExisted) {
                keysReportChangedCalled = true;
              }
            }
          }
          if (!keysReportChangedCalled) {
            if (_this7.data_.size !== orderedData.size) {
              _this7.keysAtom_.reportChanged();
            } else {
              var iter1 = _this7.data_.keys();
              var iter2 = orderedData.keys();
              var next1 = iter1.next();
              var next2 = iter2.next();
              while (!next1.done) {
                if (next1.value !== next2.value) {
                  _this7.keysAtom_.reportChanged();
                  break;
                }
                next1 = iter1.next();
                next2 = iter2.next();
              }
            }
          }
          _this7.data_ = orderedData;
        });
        return this;
      };
      _proto.toString = function toString2() {
        return "[object ObservableMap]";
      };
      _proto.toJSON = function toJSON2() {
        return Array.from(this);
      };
      _proto.observe_ = function observe_(listener, fireImmediately) {
        if (fireImmediately === true) {
          die("`observe` doesn't support fireImmediately=true in combination with maps.");
        }
        return registerListener(this, listener);
      };
      _proto.intercept_ = function intercept_(handler) {
        return registerInterceptor(this, handler);
      };
      return _createClass(ObservableMap2, [{
        key: "size",
        get: function get5() {
          this.keysAtom_.reportObserved();
          return this.data_.size;
        }
      }, {
        key: Symbol.toStringTag,
        get: function get5() {
          return "Map";
        }
      }]);
    })();
    isObservableMap = createInstanceofPredicate("ObservableMap", ObservableMap);
    ObservableSetMarker = {};
    ObservableSet = (function() {
      function ObservableSet2(initialData, enhancer, name_) {
        var _this = this;
        if (enhancer === void 0) {
          enhancer = deepEnhancer;
        }
        if (name_ === void 0) {
          name_ = true ? "ObservableSet@" + getNextId() : "ObservableSet";
        }
        this.name_ = void 0;
        this[$mobx] = ObservableSetMarker;
        this.data_ = /* @__PURE__ */ new Set();
        this.atom_ = void 0;
        this.changeListeners_ = void 0;
        this.interceptors_ = void 0;
        this.dehancer = void 0;
        this.enhancer_ = void 0;
        this.name_ = name_;
        if (!isFunction(Set)) {
          die(22);
        }
        this.enhancer_ = function(newV, oldV) {
          return enhancer(newV, oldV, name_);
        };
        initObservable(function() {
          _this.atom_ = createAtom(_this.name_);
          if (initialData) {
            _this.replace(initialData);
          }
        });
      }
      var _proto = ObservableSet2.prototype;
      _proto.dehanceValue_ = function dehanceValue_(value) {
        if (this.dehancer !== void 0) {
          return this.dehancer(value);
        }
        return value;
      };
      _proto.clear = function clear2() {
        var _this2 = this;
        transaction(function() {
          untracked(function() {
            for (var _iterator = _createForOfIteratorHelperLoose2(_this2.data_.values()), _step; !(_step = _iterator()).done; ) {
              var value = _step.value;
              _this2["delete"](value);
            }
          });
        });
      };
      _proto.forEach = function forEach(callbackFn, thisArg) {
        for (var _iterator2 = _createForOfIteratorHelperLoose2(this), _step2; !(_step2 = _iterator2()).done; ) {
          var value = _step2.value;
          callbackFn.call(thisArg, value, value, this);
        }
      };
      _proto.add = function add(value) {
        var _this3 = this;
        checkIfStateModificationsAreAllowed(this.atom_);
        if (hasInterceptors(this)) {
          var change = interceptChange(this, {
            type: ADD,
            object: this,
            newValue: value
          });
          if (!change) {
            return this;
          }
          value = change.newValue;
        }
        if (!this.has(value)) {
          transaction(function() {
            _this3.data_.add(_this3.enhancer_(value, void 0));
            _this3.atom_.reportChanged();
          });
          var notifySpy = isSpyEnabled();
          var notify = hasListeners(this);
          var _change = notify || notifySpy ? {
            observableKind: "set",
            debugObjectName: this.name_,
            type: ADD,
            object: this,
            newValue: value
          } : null;
          if (notifySpy && true) {
            spyReportStart(_change);
          }
          if (notify) {
            notifyListeners(this, _change);
          }
          if (notifySpy && true) {
            spyReportEnd();
          }
        }
        return this;
      };
      _proto["delete"] = function _delete(value) {
        var _this4 = this;
        if (hasInterceptors(this)) {
          var change = interceptChange(this, {
            type: DELETE,
            object: this,
            oldValue: value
          });
          if (!change) {
            return false;
          }
        }
        if (this.has(value)) {
          var notifySpy = isSpyEnabled();
          var notify = hasListeners(this);
          var _change2 = notify || notifySpy ? {
            observableKind: "set",
            debugObjectName: this.name_,
            type: DELETE,
            object: this,
            oldValue: value
          } : null;
          if (notifySpy && true) {
            spyReportStart(_change2);
          }
          transaction(function() {
            _this4.atom_.reportChanged();
            _this4.data_["delete"](value);
          });
          if (notify) {
            notifyListeners(this, _change2);
          }
          if (notifySpy && true) {
            spyReportEnd();
          }
          return true;
        }
        return false;
      };
      _proto.has = function has3(value) {
        this.atom_.reportObserved();
        return this.data_.has(this.dehanceValue_(value));
      };
      _proto.entries = function entries2() {
        var values2 = this.values();
        return makeIterableForSet({
          next: function next2() {
            var _values$next = values2.next(), value = _values$next.value, done = _values$next.done;
            return !done ? {
              value: [value, value],
              done
            } : {
              value: void 0,
              done
            };
          }
        });
      };
      _proto.keys = function keys2() {
        return this.values();
      };
      _proto.values = function values2() {
        this.atom_.reportObserved();
        var self2 = this;
        var values3 = this.data_.values();
        return makeIterableForSet({
          next: function next2() {
            var _values$next2 = values3.next(), value = _values$next2.value, done = _values$next2.done;
            return !done ? {
              value: self2.dehanceValue_(value),
              done
            } : {
              value: void 0,
              done
            };
          }
        });
      };
      _proto.intersection = function intersection(otherSet) {
        if (isES6Set(otherSet) && !isObservableSet(otherSet)) {
          return otherSet.intersection(this);
        } else {
          var dehancedSet = new Set(this);
          return dehancedSet.intersection(otherSet);
        }
      };
      _proto.union = function union(otherSet) {
        if (isES6Set(otherSet) && !isObservableSet(otherSet)) {
          return otherSet.union(this);
        } else {
          var dehancedSet = new Set(this);
          return dehancedSet.union(otherSet);
        }
      };
      _proto.difference = function difference(otherSet) {
        return new Set(this).difference(otherSet);
      };
      _proto.symmetricDifference = function symmetricDifference(otherSet) {
        if (isES6Set(otherSet) && !isObservableSet(otherSet)) {
          return otherSet.symmetricDifference(this);
        } else {
          var dehancedSet = new Set(this);
          return dehancedSet.symmetricDifference(otherSet);
        }
      };
      _proto.isSubsetOf = function isSubsetOf(otherSet) {
        return new Set(this).isSubsetOf(otherSet);
      };
      _proto.isSupersetOf = function isSupersetOf(otherSet) {
        return new Set(this).isSupersetOf(otherSet);
      };
      _proto.isDisjointFrom = function isDisjointFrom(otherSet) {
        if (isES6Set(otherSet) && !isObservableSet(otherSet)) {
          return otherSet.isDisjointFrom(this);
        } else {
          var dehancedSet = new Set(this);
          return dehancedSet.isDisjointFrom(otherSet);
        }
      };
      _proto.replace = function replace3(other) {
        var _this5 = this;
        if (isObservableSet(other)) {
          other = new Set(other);
        }
        transaction(function() {
          if (Array.isArray(other)) {
            _this5.clear();
            other.forEach(function(value) {
              return _this5.add(value);
            });
          } else if (isES6Set(other)) {
            _this5.clear();
            other.forEach(function(value) {
              return _this5.add(value);
            });
          } else if (other !== null && other !== void 0) {
            die("Cannot initialize set from " + other);
          }
        });
        return this;
      };
      _proto.observe_ = function observe_(listener, fireImmediately) {
        if (fireImmediately === true) {
          die("`observe` doesn't support fireImmediately=true in combination with sets.");
        }
        return registerListener(this, listener);
      };
      _proto.intercept_ = function intercept_(handler) {
        return registerInterceptor(this, handler);
      };
      _proto.toJSON = function toJSON2() {
        return Array.from(this);
      };
      _proto.toString = function toString2() {
        return "[object ObservableSet]";
      };
      _proto[Symbol.iterator] = function() {
        return this.values();
      };
      return _createClass(ObservableSet2, [{
        key: "size",
        get: function get5() {
          this.atom_.reportObserved();
          return this.data_.size;
        }
      }, {
        key: Symbol.toStringTag,
        get: function get5() {
          return "Set";
        }
      }]);
    })();
    isObservableSet = createInstanceofPredicate("ObservableSet", ObservableSet);
    descriptorCache = /* @__PURE__ */ Object.create(null);
    REMOVE3 = "remove";
    ObservableObjectAdministration = (function() {
      function ObservableObjectAdministration2(target_, values_, name_, defaultAnnotation_) {
        if (values_ === void 0) {
          values_ = /* @__PURE__ */ new Map();
        }
        if (defaultAnnotation_ === void 0) {
          defaultAnnotation_ = autoAnnotation;
        }
        this.target_ = void 0;
        this.values_ = void 0;
        this.name_ = void 0;
        this.defaultAnnotation_ = void 0;
        this.keysAtom_ = void 0;
        this.changeListeners_ = void 0;
        this.interceptors_ = void 0;
        this.proxy_ = void 0;
        this.isPlainObject_ = void 0;
        this.appliedAnnotations_ = void 0;
        this.pendingKeys_ = void 0;
        this.target_ = target_;
        this.values_ = values_;
        this.name_ = name_;
        this.defaultAnnotation_ = defaultAnnotation_;
        this.keysAtom_ = new Atom(true ? this.name_ + ".keys" : "ObservableObject.keys");
        this.isPlainObject_ = isPlainObject(this.target_);
        if (!isAnnotation(this.defaultAnnotation_)) {
          die("defaultAnnotation must be valid annotation");
        }
        if (true) {
          this.appliedAnnotations_ = {};
        }
      }
      var _proto = ObservableObjectAdministration2.prototype;
      _proto.getObservablePropValue_ = function getObservablePropValue_(key2) {
        return this.values_.get(key2).get();
      };
      _proto.setObservablePropValue_ = function setObservablePropValue_(key2, newValue) {
        var observable2 = this.values_.get(key2);
        if (observable2 instanceof ComputedValue) {
          observable2.set(newValue);
          return true;
        }
        if (hasInterceptors(this)) {
          var change = interceptChange(this, {
            type: UPDATE,
            object: this.proxy_ || this.target_,
            name: key2,
            newValue
          });
          if (!change) {
            return null;
          }
          newValue = change.newValue;
        }
        newValue = observable2.prepareNewValue_(newValue);
        if (newValue !== globalState.UNCHANGED) {
          var notify = hasListeners(this);
          var notifySpy = isSpyEnabled();
          var _change = notify || notifySpy ? {
            type: UPDATE,
            observableKind: "object",
            debugObjectName: this.name_,
            object: this.proxy_ || this.target_,
            oldValue: observable2.value_,
            name: key2,
            newValue
          } : null;
          if (notifySpy) {
            spyReportStart(_change);
          }
          observable2.setNewValue_(newValue);
          if (notify) {
            notifyListeners(this, _change);
          }
          if (notifySpy) {
            spyReportEnd();
          }
        }
        return true;
      };
      _proto.get_ = function get_(key2) {
        if (globalState.trackingDerivation && !hasProp(this.target_, key2)) {
          this.has_(key2);
        }
        return this.target_[key2];
      };
      _proto.set_ = function set_(key2, value, proxyTrap) {
        if (proxyTrap === void 0) {
          proxyTrap = false;
        }
        if (hasProp(this.target_, key2)) {
          if (this.values_.has(key2)) {
            return this.setObservablePropValue_(key2, value);
          } else if (proxyTrap) {
            return Reflect.set(this.target_, key2, value);
          } else {
            this.target_[key2] = value;
            return true;
          }
        } else {
          return this.extend_(key2, {
            value,
            enumerable: true,
            writable: true,
            configurable: true
          }, this.defaultAnnotation_, proxyTrap);
        }
      };
      _proto.has_ = function has_(key2) {
        if (!globalState.trackingDerivation) {
          return key2 in this.target_;
        }
        this.pendingKeys_ || (this.pendingKeys_ = /* @__PURE__ */ new Map());
        var entry = this.pendingKeys_.get(key2);
        if (!entry) {
          entry = new ObservableValue(key2 in this.target_, referenceEnhancer, true ? this.name_ + "." + stringifyKey2(key2) + "?" : "ObservableObject.key?", false);
          this.pendingKeys_.set(key2, entry);
        }
        return entry.get();
      };
      _proto.make_ = function make_2(key2, annotation) {
        if (annotation === true) {
          annotation = this.defaultAnnotation_;
        }
        if (annotation === false) {
          return;
        }
        assertAnnotable(this, annotation, key2);
        if (!(key2 in this.target_)) {
          var _this$target_$storedA;
          if ((_this$target_$storedA = this.target_[storedAnnotationsSymbol]) != null && _this$target_$storedA[key2]) {
            return;
          } else {
            die(1, annotation.annotationType_, this.name_ + "." + key2.toString());
          }
        }
        var source = this.target_;
        while (source && source !== objectPrototype) {
          var descriptor = getDescriptor(source, key2);
          if (descriptor) {
            var outcome = annotation.make_(this, key2, descriptor, source);
            if (outcome === 0) {
              return;
            }
            if (outcome === 1) {
              break;
            }
          }
          source = Object.getPrototypeOf(source);
        }
        recordAnnotationApplied(this, annotation, key2);
      };
      _proto.extend_ = function extend_2(key2, descriptor, annotation, proxyTrap) {
        if (proxyTrap === void 0) {
          proxyTrap = false;
        }
        if (annotation === true) {
          annotation = this.defaultAnnotation_;
        }
        if (annotation === false) {
          return this.defineProperty_(key2, descriptor, proxyTrap);
        }
        assertAnnotable(this, annotation, key2);
        var outcome = annotation.extend_(this, key2, descriptor, proxyTrap);
        if (outcome) {
          recordAnnotationApplied(this, annotation, key2);
        }
        return outcome;
      };
      _proto.defineProperty_ = function defineProperty_(key2, descriptor, proxyTrap) {
        if (proxyTrap === void 0) {
          proxyTrap = false;
        }
        checkIfStateModificationsAreAllowed(this.keysAtom_);
        try {
          startBatch();
          var deleteOutcome = this.delete_(key2);
          if (!deleteOutcome) {
            return deleteOutcome;
          }
          if (hasInterceptors(this)) {
            var change = interceptChange(this, {
              object: this.proxy_ || this.target_,
              name: key2,
              type: ADD,
              newValue: descriptor.value
            });
            if (!change) {
              return null;
            }
            var newValue = change.newValue;
            if (descriptor.value !== newValue) {
              descriptor = _extends2({}, descriptor, {
                value: newValue
              });
            }
          }
          if (proxyTrap) {
            if (!Reflect.defineProperty(this.target_, key2, descriptor)) {
              return false;
            }
          } else {
            defineProperty(this.target_, key2, descriptor);
          }
          this.notifyPropertyAddition_(key2, descriptor.value);
        } finally {
          endBatch();
        }
        return true;
      };
      _proto.defineObservableProperty_ = function defineObservableProperty_(key2, value, enhancer, proxyTrap) {
        if (proxyTrap === void 0) {
          proxyTrap = false;
        }
        checkIfStateModificationsAreAllowed(this.keysAtom_);
        try {
          startBatch();
          var deleteOutcome = this.delete_(key2);
          if (!deleteOutcome) {
            return deleteOutcome;
          }
          if (hasInterceptors(this)) {
            var change = interceptChange(this, {
              object: this.proxy_ || this.target_,
              name: key2,
              type: ADD,
              newValue: value
            });
            if (!change) {
              return null;
            }
            value = change.newValue;
          }
          var cachedDescriptor = getCachedObservablePropDescriptor(key2);
          var descriptor = {
            configurable: globalState.safeDescriptors ? this.isPlainObject_ : true,
            enumerable: true,
            get: cachedDescriptor.get,
            set: cachedDescriptor.set
          };
          if (proxyTrap) {
            if (!Reflect.defineProperty(this.target_, key2, descriptor)) {
              return false;
            }
          } else {
            defineProperty(this.target_, key2, descriptor);
          }
          var observable2 = new ObservableValue(value, enhancer, true ? this.name_ + "." + key2.toString() : "ObservableObject.key", false);
          this.values_.set(key2, observable2);
          this.notifyPropertyAddition_(key2, observable2.value_);
        } finally {
          endBatch();
        }
        return true;
      };
      _proto.defineComputedProperty_ = function defineComputedProperty_(key2, options, proxyTrap) {
        if (proxyTrap === void 0) {
          proxyTrap = false;
        }
        checkIfStateModificationsAreAllowed(this.keysAtom_);
        try {
          startBatch();
          var deleteOutcome = this.delete_(key2);
          if (!deleteOutcome) {
            return deleteOutcome;
          }
          if (hasInterceptors(this)) {
            var change = interceptChange(this, {
              object: this.proxy_ || this.target_,
              name: key2,
              type: ADD,
              newValue: void 0
            });
            if (!change) {
              return null;
            }
          }
          options.name || (options.name = true ? this.name_ + "." + key2.toString() : "ObservableObject.key");
          options.context = this.proxy_ || this.target_;
          var cachedDescriptor = getCachedObservablePropDescriptor(key2);
          var descriptor = {
            configurable: globalState.safeDescriptors ? this.isPlainObject_ : true,
            enumerable: false,
            get: cachedDescriptor.get,
            set: cachedDescriptor.set
          };
          if (proxyTrap) {
            if (!Reflect.defineProperty(this.target_, key2, descriptor)) {
              return false;
            }
          } else {
            defineProperty(this.target_, key2, descriptor);
          }
          this.values_.set(key2, new ComputedValue(options));
          this.notifyPropertyAddition_(key2, void 0);
        } finally {
          endBatch();
        }
        return true;
      };
      _proto.delete_ = function delete_(key2, proxyTrap) {
        if (proxyTrap === void 0) {
          proxyTrap = false;
        }
        checkIfStateModificationsAreAllowed(this.keysAtom_);
        if (!hasProp(this.target_, key2)) {
          return true;
        }
        if (hasInterceptors(this)) {
          var change = interceptChange(this, {
            object: this.proxy_ || this.target_,
            name: key2,
            type: REMOVE3
          });
          if (!change) {
            return null;
          }
        }
        try {
          var _this$pendingKeys_;
          startBatch();
          var notify = hasListeners(this);
          var notifySpy = isSpyEnabled();
          var observable2 = this.values_.get(key2);
          var value = void 0;
          if (!observable2 && (notify || notifySpy)) {
            var _getDescriptor2;
            value = (_getDescriptor2 = getDescriptor(this.target_, key2)) == null ? void 0 : _getDescriptor2.value;
          }
          if (proxyTrap) {
            if (!Reflect.deleteProperty(this.target_, key2)) {
              return false;
            }
          } else {
            delete this.target_[key2];
          }
          if (true) {
            delete this.appliedAnnotations_[key2];
          }
          if (observable2) {
            this.values_["delete"](key2);
            if (observable2 instanceof ObservableValue) {
              value = observable2.value_;
            }
            propagateChanged(observable2);
          }
          this.keysAtom_.reportChanged();
          (_this$pendingKeys_ = this.pendingKeys_) == null || (_this$pendingKeys_ = _this$pendingKeys_.get(key2)) == null || _this$pendingKeys_.set(key2 in this.target_);
          if (notify || notifySpy) {
            var _change2 = {
              type: REMOVE3,
              observableKind: "object",
              object: this.proxy_ || this.target_,
              debugObjectName: this.name_,
              oldValue: value,
              name: key2
            };
            if (notifySpy) {
              spyReportStart(_change2);
            }
            if (notify) {
              notifyListeners(this, _change2);
            }
            if (notifySpy) {
              spyReportEnd();
            }
          }
        } finally {
          endBatch();
        }
        return true;
      };
      _proto.observe_ = function observe_(callback, fireImmediately) {
        if (fireImmediately === true) {
          die("`observe` doesn't support the fire immediately property for observable objects.");
        }
        return registerListener(this, callback);
      };
      _proto.intercept_ = function intercept_(handler) {
        return registerInterceptor(this, handler);
      };
      _proto.notifyPropertyAddition_ = function notifyPropertyAddition_(key2, value) {
        var _this$pendingKeys_2;
        var notify = hasListeners(this);
        var notifySpy = isSpyEnabled();
        if (notify || notifySpy) {
          var change = notify || notifySpy ? {
            type: ADD,
            observableKind: "object",
            debugObjectName: this.name_,
            object: this.proxy_ || this.target_,
            name: key2,
            newValue: value
          } : null;
          if (notifySpy) {
            spyReportStart(change);
          }
          if (notify) {
            notifyListeners(this, change);
          }
          if (notifySpy) {
            spyReportEnd();
          }
        }
        (_this$pendingKeys_2 = this.pendingKeys_) == null || (_this$pendingKeys_2 = _this$pendingKeys_2.get(key2)) == null || _this$pendingKeys_2.set(true);
        this.keysAtom_.reportChanged();
      };
      _proto.ownKeys_ = function ownKeys_() {
        this.keysAtom_.reportObserved();
        return ownKeys(this.target_);
      };
      _proto.keys_ = function keys_() {
        this.keysAtom_.reportObserved();
        return Object.keys(this.target_);
      };
      return ObservableObjectAdministration2;
    })();
    isObservableObjectAdministration = createInstanceofPredicate("ObservableObjectAdministration", ObservableObjectAdministration);
    ENTRY_0 = createArrayEntryDescriptor(0);
    safariPrototypeSetterInheritanceBug = (function() {
      var v2 = false;
      var p2 = {};
      Object.defineProperty(p2, "0", {
        set: function set7() {
          v2 = true;
        }
      });
      Object.create(p2)["0"] = 1;
      return v2 === false;
    })();
    OBSERVABLE_ARRAY_BUFFER_SIZE = 0;
    StubArray = function StubArray2() {
    };
    inherit(StubArray, Array.prototype);
    LegacyObservableArray = (function(_StubArray) {
      function LegacyObservableArray2(initialValues, enhancer, name, owned) {
        var _this;
        if (name === void 0) {
          name = true ? "ObservableArray@" + getNextId() : "ObservableArray";
        }
        if (owned === void 0) {
          owned = false;
        }
        _this = _StubArray.call(this) || this;
        initObservable(function() {
          var adm = new ObservableArrayAdministration(name, enhancer, owned, true);
          adm.proxy_ = _this;
          addHiddenFinalProp(_this, $mobx, adm);
          if (initialValues && initialValues.length) {
            _this.spliceWithArray(0, 0, initialValues);
          }
          if (safariPrototypeSetterInheritanceBug) {
            Object.defineProperty(_this, "0", ENTRY_0);
          }
        });
        return _this;
      }
      _inheritsLoose2(LegacyObservableArray2, _StubArray);
      var _proto = LegacyObservableArray2.prototype;
      _proto.concat = function concat() {
        this[$mobx].atom_.reportObserved();
        for (var _len = arguments.length, arrays = new Array(_len), _key = 0; _key < _len; _key++) {
          arrays[_key] = arguments[_key];
        }
        return Array.prototype.concat.apply(
          this.slice(),
          //@ts-ignore
          arrays.map(function(a2) {
            return isObservableArray(a2) ? a2.slice() : a2;
          })
        );
      };
      _proto[Symbol.iterator] = function() {
        var self2 = this;
        var nextIndex = 0;
        return makeIterable({
          next: function next2() {
            return nextIndex < self2.length ? {
              value: self2[nextIndex++],
              done: false
            } : {
              done: true,
              value: void 0
            };
          }
        });
      };
      return _createClass(LegacyObservableArray2, [{
        key: "length",
        get: function get5() {
          return this[$mobx].getArrayLength_();
        },
        set: function set7(newLength) {
          this[$mobx].setArrayLength_(newLength);
        }
      }, {
        key: Symbol.toStringTag,
        get: function get5() {
          return "Array";
        }
      }]);
    })(StubArray);
    Object.entries(arrayExtensions).forEach(function(_ref) {
      var prop = _ref[0], fn = _ref[1];
      if (prop !== "concat") {
        addHiddenProp(LegacyObservableArray.prototype, prop, fn);
      }
    });
    reserveArrayBuffer(1e3);
    toString = objectPrototype.toString;
    maybeIteratorPrototype = ((_getGlobal$Iterator = getGlobal().Iterator) == null ? void 0 : _getGlobal$Iterator.prototype) || {};
    ["Symbol", "Map", "Set"].forEach(function(m2) {
      var g2 = getGlobal();
      if (typeof g2[m2] === "undefined") {
        die("MobX requires global '" + m2 + "' to be available or polyfilled");
      }
    });
    if (typeof __MOBX_DEVTOOLS_GLOBAL_HOOK__ === "object") {
      __MOBX_DEVTOOLS_GLOBAL_HOOK__.injectMobx({
        spy,
        extras: {
          getDebugName
        },
        $mobx
      });
    }
  }
});

// browser-external:fs
var require_fs = __commonJS({
  "browser-external:fs"(exports, module) {
    module.exports = Object.create(new Proxy({}, {
      get(_15, key2) {
        if (key2 !== "__esModule" && key2 !== "__proto__" && key2 !== "constructor" && key2 !== "splice") {
          console.warn(`Module "fs" has been externalized for browser compatibility. Cannot access "fs.${key2}" in client code. See https://vite.dev/guide/troubleshooting.html#module-externalized-for-browser-compatibility for more details.`);
        }
      }
    }));
  }
});

// node_modules/path-browserify/index.js
var require_path_browserify = __commonJS({
  "node_modules/path-browserify/index.js"(exports, module) {
    "use strict";
    function assertPath(path) {
      if (typeof path !== "string") {
        throw new TypeError("Path must be a string. Received " + JSON.stringify(path));
      }
    }
    function normalizeStringPosix(path, allowAboveRoot) {
      var res = "";
      var lastSegmentLength = 0;
      var lastSlash = -1;
      var dots = 0;
      var code;
      for (var i2 = 0; i2 <= path.length; ++i2) {
        if (i2 < path.length)
          code = path.charCodeAt(i2);
        else if (code === 47)
          break;
        else
          code = 47;
        if (code === 47) {
          if (lastSlash === i2 - 1 || dots === 1) {
          } else if (lastSlash !== i2 - 1 && dots === 2) {
            if (res.length < 2 || lastSegmentLength !== 2 || res.charCodeAt(res.length - 1) !== 46 || res.charCodeAt(res.length - 2) !== 46) {
              if (res.length > 2) {
                var lastSlashIndex = res.lastIndexOf("/");
                if (lastSlashIndex !== res.length - 1) {
                  if (lastSlashIndex === -1) {
                    res = "";
                    lastSegmentLength = 0;
                  } else {
                    res = res.slice(0, lastSlashIndex);
                    lastSegmentLength = res.length - 1 - res.lastIndexOf("/");
                  }
                  lastSlash = i2;
                  dots = 0;
                  continue;
                }
              } else if (res.length === 2 || res.length === 1) {
                res = "";
                lastSegmentLength = 0;
                lastSlash = i2;
                dots = 0;
                continue;
              }
            }
            if (allowAboveRoot) {
              if (res.length > 0)
                res += "/..";
              else
                res = "..";
              lastSegmentLength = 2;
            }
          } else {
            if (res.length > 0)
              res += "/" + path.slice(lastSlash + 1, i2);
            else
              res = path.slice(lastSlash + 1, i2);
            lastSegmentLength = i2 - lastSlash - 1;
          }
          lastSlash = i2;
          dots = 0;
        } else if (code === 46 && dots !== -1) {
          ++dots;
        } else {
          dots = -1;
        }
      }
      return res;
    }
    function _format(sep, pathObject) {
      var dir = pathObject.dir || pathObject.root;
      var base = pathObject.base || (pathObject.name || "") + (pathObject.ext || "");
      if (!dir) {
        return base;
      }
      if (dir === pathObject.root) {
        return dir + base;
      }
      return dir + sep + base;
    }
    var posix = {
      // path.resolve([from ...], to)
      resolve: function resolve() {
        var resolvedPath = "";
        var resolvedAbsolute = false;
        var cwd;
        for (var i2 = arguments.length - 1; i2 >= -1 && !resolvedAbsolute; i2--) {
          var path;
          if (i2 >= 0)
            path = arguments[i2];
          else {
            if (cwd === void 0)
              cwd = process.cwd();
            path = cwd;
          }
          assertPath(path);
          if (path.length === 0) {
            continue;
          }
          resolvedPath = path + "/" + resolvedPath;
          resolvedAbsolute = path.charCodeAt(0) === 47;
        }
        resolvedPath = normalizeStringPosix(resolvedPath, !resolvedAbsolute);
        if (resolvedAbsolute) {
          if (resolvedPath.length > 0)
            return "/" + resolvedPath;
          else
            return "/";
        } else if (resolvedPath.length > 0) {
          return resolvedPath;
        } else {
          return ".";
        }
      },
      normalize: function normalize2(path) {
        assertPath(path);
        if (path.length === 0) return ".";
        var isAbsolute = path.charCodeAt(0) === 47;
        var trailingSeparator = path.charCodeAt(path.length - 1) === 47;
        path = normalizeStringPosix(path, !isAbsolute);
        if (path.length === 0 && !isAbsolute) path = ".";
        if (path.length > 0 && trailingSeparator) path += "/";
        if (isAbsolute) return "/" + path;
        return path;
      },
      isAbsolute: function isAbsolute(path) {
        assertPath(path);
        return path.length > 0 && path.charCodeAt(0) === 47;
      },
      join: function join() {
        if (arguments.length === 0)
          return ".";
        var joined;
        for (var i2 = 0; i2 < arguments.length; ++i2) {
          var arg = arguments[i2];
          assertPath(arg);
          if (arg.length > 0) {
            if (joined === void 0)
              joined = arg;
            else
              joined += "/" + arg;
          }
        }
        if (joined === void 0)
          return ".";
        return posix.normalize(joined);
      },
      relative: function relative(from2, to) {
        assertPath(from2);
        assertPath(to);
        if (from2 === to) return "";
        from2 = posix.resolve(from2);
        to = posix.resolve(to);
        if (from2 === to) return "";
        var fromStart = 1;
        for (; fromStart < from2.length; ++fromStart) {
          if (from2.charCodeAt(fromStart) !== 47)
            break;
        }
        var fromEnd = from2.length;
        var fromLen = fromEnd - fromStart;
        var toStart = 1;
        for (; toStart < to.length; ++toStart) {
          if (to.charCodeAt(toStart) !== 47)
            break;
        }
        var toEnd = to.length;
        var toLen = toEnd - toStart;
        var length2 = fromLen < toLen ? fromLen : toLen;
        var lastCommonSep = -1;
        var i2 = 0;
        for (; i2 <= length2; ++i2) {
          if (i2 === length2) {
            if (toLen > length2) {
              if (to.charCodeAt(toStart + i2) === 47) {
                return to.slice(toStart + i2 + 1);
              } else if (i2 === 0) {
                return to.slice(toStart + i2);
              }
            } else if (fromLen > length2) {
              if (from2.charCodeAt(fromStart + i2) === 47) {
                lastCommonSep = i2;
              } else if (i2 === 0) {
                lastCommonSep = 0;
              }
            }
            break;
          }
          var fromCode = from2.charCodeAt(fromStart + i2);
          var toCode = to.charCodeAt(toStart + i2);
          if (fromCode !== toCode)
            break;
          else if (fromCode === 47)
            lastCommonSep = i2;
        }
        var out = "";
        for (i2 = fromStart + lastCommonSep + 1; i2 <= fromEnd; ++i2) {
          if (i2 === fromEnd || from2.charCodeAt(i2) === 47) {
            if (out.length === 0)
              out += "..";
            else
              out += "/..";
          }
        }
        if (out.length > 0)
          return out + to.slice(toStart + lastCommonSep);
        else {
          toStart += lastCommonSep;
          if (to.charCodeAt(toStart) === 47)
            ++toStart;
          return to.slice(toStart);
        }
      },
      _makeLong: function _makeLong(path) {
        return path;
      },
      dirname: function dirname(path) {
        assertPath(path);
        if (path.length === 0) return ".";
        var code = path.charCodeAt(0);
        var hasRoot = code === 47;
        var end = -1;
        var matchedSlash = true;
        for (var i2 = path.length - 1; i2 >= 1; --i2) {
          code = path.charCodeAt(i2);
          if (code === 47) {
            if (!matchedSlash) {
              end = i2;
              break;
            }
          } else {
            matchedSlash = false;
          }
        }
        if (end === -1) return hasRoot ? "/" : ".";
        if (hasRoot && end === 1) return "//";
        return path.slice(0, end);
      },
      basename: function basename(path, ext) {
        if (ext !== void 0 && typeof ext !== "string") throw new TypeError('"ext" argument must be a string');
        assertPath(path);
        var start = 0;
        var end = -1;
        var matchedSlash = true;
        var i2;
        if (ext !== void 0 && ext.length > 0 && ext.length <= path.length) {
          if (ext.length === path.length && ext === path) return "";
          var extIdx = ext.length - 1;
          var firstNonSlashEnd = -1;
          for (i2 = path.length - 1; i2 >= 0; --i2) {
            var code = path.charCodeAt(i2);
            if (code === 47) {
              if (!matchedSlash) {
                start = i2 + 1;
                break;
              }
            } else {
              if (firstNonSlashEnd === -1) {
                matchedSlash = false;
                firstNonSlashEnd = i2 + 1;
              }
              if (extIdx >= 0) {
                if (code === ext.charCodeAt(extIdx)) {
                  if (--extIdx === -1) {
                    end = i2;
                  }
                } else {
                  extIdx = -1;
                  end = firstNonSlashEnd;
                }
              }
            }
          }
          if (start === end) end = firstNonSlashEnd;
          else if (end === -1) end = path.length;
          return path.slice(start, end);
        } else {
          for (i2 = path.length - 1; i2 >= 0; --i2) {
            if (path.charCodeAt(i2) === 47) {
              if (!matchedSlash) {
                start = i2 + 1;
                break;
              }
            } else if (end === -1) {
              matchedSlash = false;
              end = i2 + 1;
            }
          }
          if (end === -1) return "";
          return path.slice(start, end);
        }
      },
      extname: function extname(path) {
        assertPath(path);
        var startDot = -1;
        var startPart = 0;
        var end = -1;
        var matchedSlash = true;
        var preDotState = 0;
        for (var i2 = path.length - 1; i2 >= 0; --i2) {
          var code = path.charCodeAt(i2);
          if (code === 47) {
            if (!matchedSlash) {
              startPart = i2 + 1;
              break;
            }
            continue;
          }
          if (end === -1) {
            matchedSlash = false;
            end = i2 + 1;
          }
          if (code === 46) {
            if (startDot === -1)
              startDot = i2;
            else if (preDotState !== 1)
              preDotState = 1;
          } else if (startDot !== -1) {
            preDotState = -1;
          }
        }
        if (startDot === -1 || end === -1 || // We saw a non-dot character immediately before the dot
        preDotState === 0 || // The (right-most) trimmed path component is exactly '..'
        preDotState === 1 && startDot === end - 1 && startDot === startPart + 1) {
          return "";
        }
        return path.slice(startDot, end);
      },
      format: function format2(pathObject) {
        if (pathObject === null || typeof pathObject !== "object") {
          throw new TypeError('The "pathObject" argument must be of type Object. Received type ' + typeof pathObject);
        }
        return _format("/", pathObject);
      },
      parse: function parse3(path) {
        assertPath(path);
        var ret = { root: "", dir: "", base: "", ext: "", name: "" };
        if (path.length === 0) return ret;
        var code = path.charCodeAt(0);
        var isAbsolute = code === 47;
        var start;
        if (isAbsolute) {
          ret.root = "/";
          start = 1;
        } else {
          start = 0;
        }
        var startDot = -1;
        var startPart = 0;
        var end = -1;
        var matchedSlash = true;
        var i2 = path.length - 1;
        var preDotState = 0;
        for (; i2 >= start; --i2) {
          code = path.charCodeAt(i2);
          if (code === 47) {
            if (!matchedSlash) {
              startPart = i2 + 1;
              break;
            }
            continue;
          }
          if (end === -1) {
            matchedSlash = false;
            end = i2 + 1;
          }
          if (code === 46) {
            if (startDot === -1) startDot = i2;
            else if (preDotState !== 1) preDotState = 1;
          } else if (startDot !== -1) {
            preDotState = -1;
          }
        }
        if (startDot === -1 || end === -1 || // We saw a non-dot character immediately before the dot
        preDotState === 0 || // The (right-most) trimmed path component is exactly '..'
        preDotState === 1 && startDot === end - 1 && startDot === startPart + 1) {
          if (end !== -1) {
            if (startPart === 0 && isAbsolute) ret.base = ret.name = path.slice(1, end);
            else ret.base = ret.name = path.slice(startPart, end);
          }
        } else {
          if (startPart === 0 && isAbsolute) {
            ret.name = path.slice(1, startDot);
            ret.base = path.slice(1, end);
          } else {
            ret.name = path.slice(startPart, startDot);
            ret.base = path.slice(startPart, end);
          }
          ret.ext = path.slice(startDot, end);
        }
        if (startPart > 0) ret.dir = path.slice(0, startPart - 1);
        else if (isAbsolute) ret.dir = "/";
        return ret;
      },
      sep: "/",
      delimiter: ":",
      win32: null,
      posix: null
    };
    posix.posix = posix;
    module.exports = posix;
  }
});

// node_modules/minimatch/lib/path.js
var require_path = __commonJS({
  "node_modules/minimatch/lib/path.js"(exports, module) {
    var isWindows = typeof process === "object" && process && process.platform === "win32";
    module.exports = isWindows ? { sep: "\\" } : { sep: "/" };
  }
});

// node_modules/balanced-match/index.js
var require_balanced_match = __commonJS({
  "node_modules/balanced-match/index.js"(exports, module) {
    "use strict";
    module.exports = balanced;
    function balanced(a2, b2, str) {
      if (a2 instanceof RegExp) a2 = maybeMatch(a2, str);
      if (b2 instanceof RegExp) b2 = maybeMatch(b2, str);
      var r2 = range(a2, b2, str);
      return r2 && {
        start: r2[0],
        end: r2[1],
        pre: str.slice(0, r2[0]),
        body: str.slice(r2[0] + a2.length, r2[1]),
        post: str.slice(r2[1] + b2.length)
      };
    }
    function maybeMatch(reg, str) {
      var m2 = str.match(reg);
      return m2 ? m2[0] : null;
    }
    balanced.range = range;
    function range(a2, b2, str) {
      var begs, beg, left, right, result;
      var ai = str.indexOf(a2);
      var bi = str.indexOf(b2, ai + 1);
      var i2 = ai;
      if (ai >= 0 && bi > 0) {
        if (a2 === b2) {
          return [ai, bi];
        }
        begs = [];
        left = str.length;
        while (i2 >= 0 && !result) {
          if (i2 == ai) {
            begs.push(i2);
            ai = str.indexOf(a2, i2 + 1);
          } else if (begs.length == 1) {
            result = [begs.pop(), bi];
          } else {
            beg = begs.pop();
            if (beg < left) {
              left = beg;
              right = bi;
            }
            bi = str.indexOf(b2, i2 + 1);
          }
          i2 = ai < bi && ai >= 0 ? ai : bi;
        }
        if (begs.length) {
          result = [left, right];
        }
      }
      return result;
    }
  }
});

// node_modules/brace-expansion/index.js
var require_brace_expansion = __commonJS({
  "node_modules/brace-expansion/index.js"(exports, module) {
    var balanced = require_balanced_match();
    module.exports = expandTop;
    var escSlash = "\0SLASH" + Math.random() + "\0";
    var escOpen = "\0OPEN" + Math.random() + "\0";
    var escClose = "\0CLOSE" + Math.random() + "\0";
    var escComma = "\0COMMA" + Math.random() + "\0";
    var escPeriod = "\0PERIOD" + Math.random() + "\0";
    function numeric(str) {
      return parseInt(str, 10) == str ? parseInt(str, 10) : str.charCodeAt(0);
    }
    function escapeBraces(str) {
      return str.split("\\\\").join(escSlash).split("\\{").join(escOpen).split("\\}").join(escClose).split("\\,").join(escComma).split("\\.").join(escPeriod);
    }
    function unescapeBraces(str) {
      return str.split(escSlash).join("\\").split(escOpen).join("{").split(escClose).join("}").split(escComma).join(",").split(escPeriod).join(".");
    }
    function parseCommaParts(str) {
      if (!str)
        return [""];
      var parts = [];
      var m2 = balanced("{", "}", str);
      if (!m2)
        return str.split(",");
      var pre = m2.pre;
      var body = m2.body;
      var post = m2.post;
      var p2 = pre.split(",");
      p2[p2.length - 1] += "{" + body + "}";
      var postParts = parseCommaParts(post);
      if (post.length) {
        p2[p2.length - 1] += postParts.shift();
        p2.push.apply(p2, postParts);
      }
      parts.push.apply(parts, p2);
      return parts;
    }
    function expandTop(str) {
      if (!str)
        return [];
      if (str.substr(0, 2) === "{}") {
        str = "\\{\\}" + str.substr(2);
      }
      return expand(escapeBraces(str), true).map(unescapeBraces);
    }
    function embrace(str) {
      return "{" + str + "}";
    }
    function isPadded(el) {
      return /^-?0\d/.test(el);
    }
    function lte(i2, y2) {
      return i2 <= y2;
    }
    function gte(i2, y2) {
      return i2 >= y2;
    }
    function expand(str, isTop) {
      var expansions = [];
      var m2 = balanced("{", "}", str);
      if (!m2) return [str];
      var pre = m2.pre;
      var post = m2.post.length ? expand(m2.post, false) : [""];
      if (/\$$/.test(m2.pre)) {
        for (var k2 = 0; k2 < post.length; k2++) {
          var expansion = pre + "{" + m2.body + "}" + post[k2];
          expansions.push(expansion);
        }
      } else {
        var isNumericSequence = /^-?\d+\.\.-?\d+(?:\.\.-?\d+)?$/.test(m2.body);
        var isAlphaSequence = /^[a-zA-Z]\.\.[a-zA-Z](?:\.\.-?\d+)?$/.test(m2.body);
        var isSequence = isNumericSequence || isAlphaSequence;
        var isOptions = m2.body.indexOf(",") >= 0;
        if (!isSequence && !isOptions) {
          if (m2.post.match(/,(?!,).*\}/)) {
            str = m2.pre + "{" + m2.body + escClose + m2.post;
            return expand(str);
          }
          return [str];
        }
        var n;
        if (isSequence) {
          n = m2.body.split(/\.\./);
        } else {
          n = parseCommaParts(m2.body);
          if (n.length === 1) {
            n = expand(n[0], false).map(embrace);
            if (n.length === 1) {
              return post.map(function(p2) {
                return m2.pre + n[0] + p2;
              });
            }
          }
        }
        var N2;
        if (isSequence) {
          var x2 = numeric(n[0]);
          var y2 = numeric(n[1]);
          var width = Math.max(n[0].length, n[1].length);
          var incr = n.length == 3 ? Math.abs(numeric(n[2])) : 1;
          var test = lte;
          var reverse2 = y2 < x2;
          if (reverse2) {
            incr *= -1;
            test = gte;
          }
          var pad2 = n.some(isPadded);
          N2 = [];
          for (var i2 = x2; test(i2, y2); i2 += incr) {
            var c2;
            if (isAlphaSequence) {
              c2 = String.fromCharCode(i2);
              if (c2 === "\\")
                c2 = "";
            } else {
              c2 = String(i2);
              if (pad2) {
                var need = width - c2.length;
                if (need > 0) {
                  var z2 = new Array(need + 1).join("0");
                  if (i2 < 0)
                    c2 = "-" + z2 + c2.slice(1);
                  else
                    c2 = z2 + c2;
                }
              }
            }
            N2.push(c2);
          }
        } else {
          N2 = [];
          for (var j2 = 0; j2 < n.length; j2++) {
            N2.push.apply(N2, expand(n[j2], false));
          }
        }
        for (var j2 = 0; j2 < N2.length; j2++) {
          for (var k2 = 0; k2 < post.length; k2++) {
            var expansion = pre + N2[j2] + post[k2];
            if (!isTop || isSequence || expansion)
              expansions.push(expansion);
          }
        }
      }
      return expansions;
    }
  }
});

// node_modules/minimatch/minimatch.js
var require_minimatch = __commonJS({
  "node_modules/minimatch/minimatch.js"(exports, module) {
    var minimatch = module.exports = (p2, pattern, options = {}) => {
      assertValidPattern(pattern);
      if (!options.nocomment && pattern.charAt(0) === "#") {
        return false;
      }
      return new Minimatch(pattern, options).match(p2);
    };
    module.exports = minimatch;
    var path = require_path();
    minimatch.sep = path.sep;
    var GLOBSTAR = Symbol("globstar **");
    minimatch.GLOBSTAR = GLOBSTAR;
    var expand = require_brace_expansion();
    var plTypes = {
      "!": { open: "(?:(?!(?:", close: "))[^/]*?)" },
      "?": { open: "(?:", close: ")?" },
      "+": { open: "(?:", close: ")+" },
      "*": { open: "(?:", close: ")*" },
      "@": { open: "(?:", close: ")" }
    };
    var qmark = "[^/]";
    var star = qmark + "*?";
    var twoStarDot = "(?:(?!(?:\\/|^)(?:\\.{1,2})($|\\/)).)*?";
    var twoStarNoDot = "(?:(?!(?:\\/|^)\\.).)*?";
    var charSet = (s2) => s2.split("").reduce((set7, c2) => {
      set7[c2] = true;
      return set7;
    }, {});
    var reSpecials = charSet("().*{}+?[]^$\\!");
    var addPatternStartSet = charSet("[.(");
    var slashSplit = /\/+/;
    minimatch.filter = (pattern, options = {}) => (p2, i2, list) => minimatch(p2, pattern, options);
    var ext = (a2, b2 = {}) => {
      const t = {};
      Object.keys(a2).forEach((k2) => t[k2] = a2[k2]);
      Object.keys(b2).forEach((k2) => t[k2] = b2[k2]);
      return t;
    };
    minimatch.defaults = (def) => {
      if (!def || typeof def !== "object" || !Object.keys(def).length) {
        return minimatch;
      }
      const orig = minimatch;
      const m2 = (p2, pattern, options) => orig(p2, pattern, ext(def, options));
      m2.Minimatch = class Minimatch extends orig.Minimatch {
        constructor(pattern, options) {
          super(pattern, ext(def, options));
        }
      };
      m2.Minimatch.defaults = (options) => orig.defaults(ext(def, options)).Minimatch;
      m2.filter = (pattern, options) => orig.filter(pattern, ext(def, options));
      m2.defaults = (options) => orig.defaults(ext(def, options));
      m2.makeRe = (pattern, options) => orig.makeRe(pattern, ext(def, options));
      m2.braceExpand = (pattern, options) => orig.braceExpand(pattern, ext(def, options));
      m2.match = (list, pattern, options) => orig.match(list, pattern, ext(def, options));
      return m2;
    };
    minimatch.braceExpand = (pattern, options) => braceExpand(pattern, options);
    var braceExpand = (pattern, options = {}) => {
      assertValidPattern(pattern);
      if (options.nobrace || !/\{(?:(?!\{).)*\}/.test(pattern)) {
        return [pattern];
      }
      return expand(pattern);
    };
    var MAX_PATTERN_LENGTH = 1024 * 64;
    var assertValidPattern = (pattern) => {
      if (typeof pattern !== "string") {
        throw new TypeError("invalid pattern");
      }
      if (pattern.length > MAX_PATTERN_LENGTH) {
        throw new TypeError("pattern is too long");
      }
    };
    var SUBPARSE = Symbol("subparse");
    minimatch.makeRe = (pattern, options) => new Minimatch(pattern, options || {}).makeRe();
    minimatch.match = (list, pattern, options = {}) => {
      const mm = new Minimatch(pattern, options);
      list = list.filter((f2) => mm.match(f2));
      if (mm.options.nonull && !list.length) {
        list.push(pattern);
      }
      return list;
    };
    var globUnescape = (s2) => s2.replace(/\\(.)/g, "$1");
    var charUnescape = (s2) => s2.replace(/\\([^-\]])/g, "$1");
    var regExpEscape = (s2) => s2.replace(/[-[\]{}()*+?.,\\^$|#\s]/g, "\\$&");
    var braExpEscape = (s2) => s2.replace(/[[\]\\]/g, "\\$&");
    var Minimatch = class {
      constructor(pattern, options) {
        assertValidPattern(pattern);
        if (!options) options = {};
        this.options = options;
        this.set = [];
        this.pattern = pattern;
        this.windowsPathsNoEscape = !!options.windowsPathsNoEscape || options.allowWindowsEscape === false;
        if (this.windowsPathsNoEscape) {
          this.pattern = this.pattern.replace(/\\/g, "/");
        }
        this.regexp = null;
        this.negate = false;
        this.comment = false;
        this.empty = false;
        this.partial = !!options.partial;
        this.make();
      }
      debug() {
      }
      make() {
        const pattern = this.pattern;
        const options = this.options;
        if (!options.nocomment && pattern.charAt(0) === "#") {
          this.comment = true;
          return;
        }
        if (!pattern) {
          this.empty = true;
          return;
        }
        this.parseNegate();
        let set7 = this.globSet = this.braceExpand();
        if (options.debug) this.debug = (...args) => console.error(...args);
        this.debug(this.pattern, set7);
        set7 = this.globParts = set7.map((s2) => s2.split(slashSplit));
        this.debug(this.pattern, set7);
        set7 = set7.map((s2, si, set8) => s2.map(this.parse, this));
        this.debug(this.pattern, set7);
        set7 = set7.filter((s2) => s2.indexOf(false) === -1);
        this.debug(this.pattern, set7);
        this.set = set7;
      }
      parseNegate() {
        if (this.options.nonegate) return;
        const pattern = this.pattern;
        let negate = false;
        let negateOffset = 0;
        for (let i2 = 0; i2 < pattern.length && pattern.charAt(i2) === "!"; i2++) {
          negate = !negate;
          negateOffset++;
        }
        if (negateOffset) this.pattern = pattern.slice(negateOffset);
        this.negate = negate;
      }
      // set partial to true to test if, for example,
      // "/a/b" matches the start of "/*/b/*/d"
      // Partial means, if you run out of file before you run
      // out of pattern, then that's fine, as long as all
      // the parts match.
      matchOne(file, pattern, partial) {
        var options = this.options;
        this.debug(
          "matchOne",
          { "this": this, file, pattern }
        );
        this.debug("matchOne", file.length, pattern.length);
        for (var fi = 0, pi = 0, fl = file.length, pl = pattern.length; fi < fl && pi < pl; fi++, pi++) {
          this.debug("matchOne loop");
          var p2 = pattern[pi];
          var f2 = file[fi];
          this.debug(pattern, p2, f2);
          if (p2 === false) return false;
          if (p2 === GLOBSTAR) {
            this.debug("GLOBSTAR", [pattern, p2, f2]);
            var fr = fi;
            var pr = pi + 1;
            if (pr === pl) {
              this.debug("** at the end");
              for (; fi < fl; fi++) {
                if (file[fi] === "." || file[fi] === ".." || !options.dot && file[fi].charAt(0) === ".") return false;
              }
              return true;
            }
            while (fr < fl) {
              var swallowee = file[fr];
              this.debug("\nglobstar while", file, fr, pattern, pr, swallowee);
              if (this.matchOne(file.slice(fr), pattern.slice(pr), partial)) {
                this.debug("globstar found match!", fr, fl, swallowee);
                return true;
              } else {
                if (swallowee === "." || swallowee === ".." || !options.dot && swallowee.charAt(0) === ".") {
                  this.debug("dot detected!", file, fr, pattern, pr);
                  break;
                }
                this.debug("globstar swallow a segment, and continue");
                fr++;
              }
            }
            if (partial) {
              this.debug("\n>>> no match, partial?", file, fr, pattern, pr);
              if (fr === fl) return true;
            }
            return false;
          }
          var hit;
          if (typeof p2 === "string") {
            hit = f2 === p2;
            this.debug("string match", p2, f2, hit);
          } else {
            hit = f2.match(p2);
            this.debug("pattern match", p2, f2, hit);
          }
          if (!hit) return false;
        }
        if (fi === fl && pi === pl) {
          return true;
        } else if (fi === fl) {
          return partial;
        } else if (pi === pl) {
          return fi === fl - 1 && file[fi] === "";
        }
        throw new Error("wtf?");
      }
      braceExpand() {
        return braceExpand(this.pattern, this.options);
      }
      parse(pattern, isSub) {
        assertValidPattern(pattern);
        const options = this.options;
        if (pattern === "**") {
          if (!options.noglobstar)
            return GLOBSTAR;
          else
            pattern = "*";
        }
        if (pattern === "") return "";
        let re2 = "";
        let hasMagic = false;
        let escaping2 = false;
        const patternListStack = [];
        const negativeLists = [];
        let stateChar;
        let inClass = false;
        let reClassStart = -1;
        let classStart = -1;
        let cs;
        let pl;
        let sp;
        let dotTravAllowed = pattern.charAt(0) === ".";
        let dotFileAllowed = options.dot || dotTravAllowed;
        const patternStart = () => dotTravAllowed ? "" : dotFileAllowed ? "(?!(?:^|\\/)\\.{1,2}(?:$|\\/))" : "(?!\\.)";
        const subPatternStart = (p2) => p2.charAt(0) === "." ? "" : options.dot ? "(?!(?:^|\\/)\\.{1,2}(?:$|\\/))" : "(?!\\.)";
        const clearStateChar = () => {
          if (stateChar) {
            switch (stateChar) {
              case "*":
                re2 += star;
                hasMagic = true;
                break;
              case "?":
                re2 += qmark;
                hasMagic = true;
                break;
              default:
                re2 += "\\" + stateChar;
                break;
            }
            this.debug("clearStateChar %j %j", stateChar, re2);
            stateChar = false;
          }
        };
        for (let i2 = 0, c2; i2 < pattern.length && (c2 = pattern.charAt(i2)); i2++) {
          this.debug("%s	%s %s %j", pattern, i2, re2, c2);
          if (escaping2) {
            if (c2 === "/") {
              return false;
            }
            if (reSpecials[c2]) {
              re2 += "\\";
            }
            re2 += c2;
            escaping2 = false;
            continue;
          }
          switch (c2) {
            /* istanbul ignore next */
            case "/": {
              return false;
            }
            case "\\":
              if (inClass && pattern.charAt(i2 + 1) === "-") {
                re2 += c2;
                continue;
              }
              clearStateChar();
              escaping2 = true;
              continue;
            // the various stateChar values
            // for the "extglob" stuff.
            case "?":
            case "*":
            case "+":
            case "@":
            case "!":
              this.debug("%s	%s %s %j <-- stateChar", pattern, i2, re2, c2);
              if (inClass) {
                this.debug("  in class");
                if (c2 === "!" && i2 === classStart + 1) c2 = "^";
                re2 += c2;
                continue;
              }
              this.debug("call clearStateChar %j", stateChar);
              clearStateChar();
              stateChar = c2;
              if (options.noext) clearStateChar();
              continue;
            case "(": {
              if (inClass) {
                re2 += "(";
                continue;
              }
              if (!stateChar) {
                re2 += "\\(";
                continue;
              }
              const plEntry = {
                type: stateChar,
                start: i2 - 1,
                reStart: re2.length,
                open: plTypes[stateChar].open,
                close: plTypes[stateChar].close
              };
              this.debug(this.pattern, "	", plEntry);
              patternListStack.push(plEntry);
              re2 += plEntry.open;
              if (plEntry.start === 0 && plEntry.type !== "!") {
                dotTravAllowed = true;
                re2 += subPatternStart(pattern.slice(i2 + 1));
              }
              this.debug("plType %j %j", stateChar, re2);
              stateChar = false;
              continue;
            }
            case ")": {
              const plEntry = patternListStack[patternListStack.length - 1];
              if (inClass || !plEntry) {
                re2 += "\\)";
                continue;
              }
              patternListStack.pop();
              clearStateChar();
              hasMagic = true;
              pl = plEntry;
              re2 += pl.close;
              if (pl.type === "!") {
                negativeLists.push(Object.assign(pl, { reEnd: re2.length }));
              }
              continue;
            }
            case "|": {
              const plEntry = patternListStack[patternListStack.length - 1];
              if (inClass || !plEntry) {
                re2 += "\\|";
                continue;
              }
              clearStateChar();
              re2 += "|";
              if (plEntry.start === 0 && plEntry.type !== "!") {
                dotTravAllowed = true;
                re2 += subPatternStart(pattern.slice(i2 + 1));
              }
              continue;
            }
            // these are mostly the same in regexp and glob
            case "[":
              clearStateChar();
              if (inClass) {
                re2 += "\\" + c2;
                continue;
              }
              inClass = true;
              classStart = i2;
              reClassStart = re2.length;
              re2 += c2;
              continue;
            case "]":
              if (i2 === classStart + 1 || !inClass) {
                re2 += "\\" + c2;
                continue;
              }
              cs = pattern.substring(classStart + 1, i2);
              try {
                RegExp("[" + braExpEscape(charUnescape(cs)) + "]");
                re2 += c2;
              } catch (er) {
                re2 = re2.substring(0, reClassStart) + "(?:$.)";
              }
              hasMagic = true;
              inClass = false;
              continue;
            default:
              clearStateChar();
              if (reSpecials[c2] && !(c2 === "^" && inClass)) {
                re2 += "\\";
              }
              re2 += c2;
              break;
          }
        }
        if (inClass) {
          cs = pattern.slice(classStart + 1);
          sp = this.parse(cs, SUBPARSE);
          re2 = re2.substring(0, reClassStart) + "\\[" + sp[0];
          hasMagic = hasMagic || sp[1];
        }
        for (pl = patternListStack.pop(); pl; pl = patternListStack.pop()) {
          let tail;
          tail = re2.slice(pl.reStart + pl.open.length);
          this.debug("setting tail", re2, pl);
          tail = tail.replace(/((?:\\{2}){0,64})(\\?)\|/g, (_15, $1, $2) => {
            if (!$2) {
              $2 = "\\";
            }
            return $1 + $1 + $2 + "|";
          });
          this.debug("tail=%j\n   %s", tail, tail, pl, re2);
          const t = pl.type === "*" ? star : pl.type === "?" ? qmark : "\\" + pl.type;
          hasMagic = true;
          re2 = re2.slice(0, pl.reStart) + t + "\\(" + tail;
        }
        clearStateChar();
        if (escaping2) {
          re2 += "\\\\";
        }
        const addPatternStart = addPatternStartSet[re2.charAt(0)];
        for (let n = negativeLists.length - 1; n > -1; n--) {
          const nl = negativeLists[n];
          const nlBefore = re2.slice(0, nl.reStart);
          const nlFirst = re2.slice(nl.reStart, nl.reEnd - 8);
          let nlAfter = re2.slice(nl.reEnd);
          const nlLast = re2.slice(nl.reEnd - 8, nl.reEnd) + nlAfter;
          const closeParensBefore = nlBefore.split(")").length;
          const openParensBefore = nlBefore.split("(").length - closeParensBefore;
          let cleanAfter = nlAfter;
          for (let i2 = 0; i2 < openParensBefore; i2++) {
            cleanAfter = cleanAfter.replace(/\)[+*?]?/, "");
          }
          nlAfter = cleanAfter;
          const dollar = nlAfter === "" && isSub !== SUBPARSE ? "(?:$|\\/)" : "";
          re2 = nlBefore + nlFirst + nlAfter + dollar + nlLast;
        }
        if (re2 !== "" && hasMagic) {
          re2 = "(?=.)" + re2;
        }
        if (addPatternStart) {
          re2 = patternStart() + re2;
        }
        if (isSub === SUBPARSE) {
          return [re2, hasMagic];
        }
        if (options.nocase && !hasMagic) {
          hasMagic = pattern.toUpperCase() !== pattern.toLowerCase();
        }
        if (!hasMagic) {
          return globUnescape(pattern);
        }
        const flags = options.nocase ? "i" : "";
        try {
          return Object.assign(new RegExp("^" + re2 + "$", flags), {
            _glob: pattern,
            _src: re2
          });
        } catch (er) {
          return new RegExp("$.");
        }
      }
      makeRe() {
        if (this.regexp || this.regexp === false) return this.regexp;
        const set7 = this.set;
        if (!set7.length) {
          this.regexp = false;
          return this.regexp;
        }
        const options = this.options;
        const twoStar = options.noglobstar ? star : options.dot ? twoStarDot : twoStarNoDot;
        const flags = options.nocase ? "i" : "";
        let re2 = set7.map((pattern) => {
          pattern = pattern.map(
            (p2) => typeof p2 === "string" ? regExpEscape(p2) : p2 === GLOBSTAR ? GLOBSTAR : p2._src
          ).reduce((set8, p2) => {
            if (!(set8[set8.length - 1] === GLOBSTAR && p2 === GLOBSTAR)) {
              set8.push(p2);
            }
            return set8;
          }, []);
          pattern.forEach((p2, i2) => {
            if (p2 !== GLOBSTAR || pattern[i2 - 1] === GLOBSTAR) {
              return;
            }
            if (i2 === 0) {
              if (pattern.length > 1) {
                pattern[i2 + 1] = "(?:\\/|" + twoStar + "\\/)?" + pattern[i2 + 1];
              } else {
                pattern[i2] = twoStar;
              }
            } else if (i2 === pattern.length - 1) {
              pattern[i2 - 1] += "(?:\\/|" + twoStar + ")?";
            } else {
              pattern[i2 - 1] += "(?:\\/|\\/" + twoStar + "\\/)" + pattern[i2 + 1];
              pattern[i2 + 1] = GLOBSTAR;
            }
          });
          return pattern.filter((p2) => p2 !== GLOBSTAR).join("/");
        }).join("|");
        re2 = "^(?:" + re2 + ")$";
        if (this.negate) re2 = "^(?!" + re2 + ").*$";
        try {
          this.regexp = new RegExp(re2, flags);
        } catch (ex) {
          this.regexp = false;
        }
        return this.regexp;
      }
      match(f2, partial = this.partial) {
        this.debug("match", f2, this.pattern);
        if (this.comment) return false;
        if (this.empty) return f2 === "";
        if (f2 === "/" && partial) return true;
        const options = this.options;
        if (path.sep !== "/") {
          f2 = f2.split(path.sep).join("/");
        }
        f2 = f2.split(slashSplit);
        this.debug(this.pattern, "split", f2);
        const set7 = this.set;
        this.debug(this.pattern, "set", set7);
        let filename;
        for (let i2 = f2.length - 1; i2 >= 0; i2--) {
          filename = f2[i2];
          if (filename) break;
        }
        for (let i2 = 0; i2 < set7.length; i2++) {
          const pattern = set7[i2];
          let file = f2;
          if (options.matchBase && pattern.length === 1) {
            file = [filename];
          }
          const hit = this.matchOne(file, pattern, partial);
          if (hit) {
            if (options.flipNegate) return true;
            return !this.negate;
          }
        }
        if (options.flipNegate) return false;
        return this.negate;
      }
      static defaults(def) {
        return minimatch.defaults(def).Minimatch;
      }
    };
    minimatch.Minimatch = Minimatch;
  }
});

// node_modules/js-yaml/lib/common.js
var require_common = __commonJS({
  "node_modules/js-yaml/lib/common.js"(exports, module) {
    "use strict";
    function isNothing(subject) {
      return typeof subject === "undefined" || subject === null;
    }
    function isObject2(subject) {
      return typeof subject === "object" && subject !== null;
    }
    function toArray(sequence) {
      if (Array.isArray(sequence)) return sequence;
      else if (isNothing(sequence)) return [];
      return [sequence];
    }
    function extend(target, source) {
      var index, length2, key2, sourceKeys;
      if (source) {
        sourceKeys = Object.keys(source);
        for (index = 0, length2 = sourceKeys.length; index < length2; index += 1) {
          key2 = sourceKeys[index];
          target[key2] = source[key2];
        }
      }
      return target;
    }
    function repeat(string2, count) {
      var result = "", cycle;
      for (cycle = 0; cycle < count; cycle += 1) {
        result += string2;
      }
      return result;
    }
    function isNegativeZero(number) {
      return number === 0 && Number.NEGATIVE_INFINITY === 1 / number;
    }
    module.exports.isNothing = isNothing;
    module.exports.isObject = isObject2;
    module.exports.toArray = toArray;
    module.exports.repeat = repeat;
    module.exports.isNegativeZero = isNegativeZero;
    module.exports.extend = extend;
  }
});

// node_modules/js-yaml/lib/exception.js
var require_exception = __commonJS({
  "node_modules/js-yaml/lib/exception.js"(exports, module) {
    "use strict";
    function formatError(exception, compact) {
      var where = "", message = exception.reason || "(unknown reason)";
      if (!exception.mark) return message;
      if (exception.mark.name) {
        where += 'in "' + exception.mark.name + '" ';
      }
      where += "(" + (exception.mark.line + 1) + ":" + (exception.mark.column + 1) + ")";
      if (!compact && exception.mark.snippet) {
        where += "\n\n" + exception.mark.snippet;
      }
      return message + " " + where;
    }
    function YAMLException(reason, mark) {
      Error.call(this);
      this.name = "YAMLException";
      this.reason = reason;
      this.mark = mark;
      this.message = formatError(this, false);
      if (Error.captureStackTrace) {
        Error.captureStackTrace(this, this.constructor);
      } else {
        this.stack = new Error().stack || "";
      }
    }
    YAMLException.prototype = Object.create(Error.prototype);
    YAMLException.prototype.constructor = YAMLException;
    YAMLException.prototype.toString = function toString2(compact) {
      return this.name + ": " + formatError(this, compact);
    };
    module.exports = YAMLException;
  }
});

// node_modules/js-yaml/lib/snippet.js
var require_snippet = __commonJS({
  "node_modules/js-yaml/lib/snippet.js"(exports, module) {
    "use strict";
    var common = require_common();
    function getLine(buffer, lineStart, lineEnd, position3, maxLineLength) {
      var head = "";
      var tail = "";
      var maxHalfLength = Math.floor(maxLineLength / 2) - 1;
      if (position3 - lineStart > maxHalfLength) {
        head = " ... ";
        lineStart = position3 - maxHalfLength + head.length;
      }
      if (lineEnd - position3 > maxHalfLength) {
        tail = " ...";
        lineEnd = position3 + maxHalfLength - tail.length;
      }
      return {
        str: head + buffer.slice(lineStart, lineEnd).replace(/\t/g, "→") + tail,
        pos: position3 - lineStart + head.length
        // relative position
      };
    }
    function padStart(string2, max2) {
      return common.repeat(" ", max2 - string2.length) + string2;
    }
    function makeSnippet(mark, options) {
      options = Object.create(options || null);
      if (!mark.buffer) return null;
      if (!options.maxLength) options.maxLength = 79;
      if (typeof options.indent !== "number") options.indent = 1;
      if (typeof options.linesBefore !== "number") options.linesBefore = 3;
      if (typeof options.linesAfter !== "number") options.linesAfter = 2;
      var re2 = /\r?\n|\r|\0/g;
      var lineStarts = [0];
      var lineEnds = [];
      var match2;
      var foundLineNo = -1;
      while (match2 = re2.exec(mark.buffer)) {
        lineEnds.push(match2.index);
        lineStarts.push(match2.index + match2[0].length);
        if (mark.position <= match2.index && foundLineNo < 0) {
          foundLineNo = lineStarts.length - 2;
        }
      }
      if (foundLineNo < 0) foundLineNo = lineStarts.length - 1;
      var result = "", i2, line2;
      var lineNoLength = Math.min(mark.line + options.linesAfter, lineEnds.length).toString().length;
      var maxLineLength = options.maxLength - (options.indent + lineNoLength + 3);
      for (i2 = 1; i2 <= options.linesBefore; i2++) {
        if (foundLineNo - i2 < 0) break;
        line2 = getLine(
          mark.buffer,
          lineStarts[foundLineNo - i2],
          lineEnds[foundLineNo - i2],
          mark.position - (lineStarts[foundLineNo] - lineStarts[foundLineNo - i2]),
          maxLineLength
        );
        result = common.repeat(" ", options.indent) + padStart((mark.line - i2 + 1).toString(), lineNoLength) + " | " + line2.str + "\n" + result;
      }
      line2 = getLine(mark.buffer, lineStarts[foundLineNo], lineEnds[foundLineNo], mark.position, maxLineLength);
      result += common.repeat(" ", options.indent) + padStart((mark.line + 1).toString(), lineNoLength) + " | " + line2.str + "\n";
      result += common.repeat("-", options.indent + lineNoLength + 3 + line2.pos) + "^\n";
      for (i2 = 1; i2 <= options.linesAfter; i2++) {
        if (foundLineNo + i2 >= lineEnds.length) break;
        line2 = getLine(
          mark.buffer,
          lineStarts[foundLineNo + i2],
          lineEnds[foundLineNo + i2],
          mark.position - (lineStarts[foundLineNo] - lineStarts[foundLineNo + i2]),
          maxLineLength
        );
        result += common.repeat(" ", options.indent) + padStart((mark.line + i2 + 1).toString(), lineNoLength) + " | " + line2.str + "\n";
      }
      return result.replace(/\n$/, "");
    }
    module.exports = makeSnippet;
  }
});

// node_modules/js-yaml/lib/type.js
var require_type = __commonJS({
  "node_modules/js-yaml/lib/type.js"(exports, module) {
    "use strict";
    var YAMLException = require_exception();
    var TYPE_CONSTRUCTOR_OPTIONS = [
      "kind",
      "multi",
      "resolve",
      "construct",
      "instanceOf",
      "predicate",
      "represent",
      "representName",
      "defaultStyle",
      "styleAliases"
    ];
    var YAML_NODE_KINDS = [
      "scalar",
      "sequence",
      "mapping"
    ];
    function compileStyleAliases(map3) {
      var result = {};
      if (map3 !== null) {
        Object.keys(map3).forEach(function(style) {
          map3[style].forEach(function(alias) {
            result[String(alias)] = style;
          });
        });
      }
      return result;
    }
    function Type(tag, options) {
      options = options || {};
      Object.keys(options).forEach(function(name) {
        if (TYPE_CONSTRUCTOR_OPTIONS.indexOf(name) === -1) {
          throw new YAMLException('Unknown option "' + name + '" is met in definition of "' + tag + '" YAML type.');
        }
      });
      this.options = options;
      this.tag = tag;
      this.kind = options["kind"] || null;
      this.resolve = options["resolve"] || function() {
        return true;
      };
      this.construct = options["construct"] || function(data) {
        return data;
      };
      this.instanceOf = options["instanceOf"] || null;
      this.predicate = options["predicate"] || null;
      this.represent = options["represent"] || null;
      this.representName = options["representName"] || null;
      this.defaultStyle = options["defaultStyle"] || null;
      this.multi = options["multi"] || false;
      this.styleAliases = compileStyleAliases(options["styleAliases"] || null);
      if (YAML_NODE_KINDS.indexOf(this.kind) === -1) {
        throw new YAMLException('Unknown kind "' + this.kind + '" is specified for "' + tag + '" YAML type.');
      }
    }
    module.exports = Type;
  }
});

// node_modules/js-yaml/lib/schema.js
var require_schema = __commonJS({
  "node_modules/js-yaml/lib/schema.js"(exports, module) {
    "use strict";
    var YAMLException = require_exception();
    var Type = require_type();
    function compileList(schema4, name) {
      var result = [];
      schema4[name].forEach(function(currentType) {
        var newIndex = result.length;
        result.forEach(function(previousType, previousIndex) {
          if (previousType.tag === currentType.tag && previousType.kind === currentType.kind && previousType.multi === currentType.multi) {
            newIndex = previousIndex;
          }
        });
        result[newIndex] = currentType;
      });
      return result;
    }
    function compileMap() {
      var result = {
        scalar: {},
        sequence: {},
        mapping: {},
        fallback: {},
        multi: {
          scalar: [],
          sequence: [],
          mapping: [],
          fallback: []
        }
      }, index, length2;
      function collectType(type) {
        if (type.multi) {
          result.multi[type.kind].push(type);
          result.multi["fallback"].push(type);
        } else {
          result[type.kind][type.tag] = result["fallback"][type.tag] = type;
        }
      }
      for (index = 0, length2 = arguments.length; index < length2; index += 1) {
        arguments[index].forEach(collectType);
      }
      return result;
    }
    function Schema2(definition) {
      return this.extend(definition);
    }
    Schema2.prototype.extend = function extend(definition) {
      var implicit = [];
      var explicit = [];
      if (definition instanceof Type) {
        explicit.push(definition);
      } else if (Array.isArray(definition)) {
        explicit = explicit.concat(definition);
      } else if (definition && (Array.isArray(definition.implicit) || Array.isArray(definition.explicit))) {
        if (definition.implicit) implicit = implicit.concat(definition.implicit);
        if (definition.explicit) explicit = explicit.concat(definition.explicit);
      } else {
        throw new YAMLException("Schema.extend argument should be a Type, [ Type ], or a schema definition ({ implicit: [...], explicit: [...] })");
      }
      implicit.forEach(function(type) {
        if (!(type instanceof Type)) {
          throw new YAMLException("Specified list of YAML types (or a single Type object) contains a non-Type object.");
        }
        if (type.loadKind && type.loadKind !== "scalar") {
          throw new YAMLException("There is a non-scalar type in the implicit list of a schema. Implicit resolving of such types is not supported.");
        }
        if (type.multi) {
          throw new YAMLException("There is a multi type in the implicit list of a schema. Multi tags can only be listed as explicit.");
        }
      });
      explicit.forEach(function(type) {
        if (!(type instanceof Type)) {
          throw new YAMLException("Specified list of YAML types (or a single Type object) contains a non-Type object.");
        }
      });
      var result = Object.create(Schema2.prototype);
      result.implicit = (this.implicit || []).concat(implicit);
      result.explicit = (this.explicit || []).concat(explicit);
      result.compiledImplicit = compileList(result, "implicit");
      result.compiledExplicit = compileList(result, "explicit");
      result.compiledTypeMap = compileMap(result.compiledImplicit, result.compiledExplicit);
      return result;
    };
    module.exports = Schema2;
  }
});

// node_modules/js-yaml/lib/type/str.js
var require_str = __commonJS({
  "node_modules/js-yaml/lib/type/str.js"(exports, module) {
    "use strict";
    var Type = require_type();
    module.exports = new Type("tag:yaml.org,2002:str", {
      kind: "scalar",
      construct: function(data) {
        return data !== null ? data : "";
      }
    });
  }
});

// node_modules/js-yaml/lib/type/seq.js
var require_seq = __commonJS({
  "node_modules/js-yaml/lib/type/seq.js"(exports, module) {
    "use strict";
    var Type = require_type();
    module.exports = new Type("tag:yaml.org,2002:seq", {
      kind: "sequence",
      construct: function(data) {
        return data !== null ? data : [];
      }
    });
  }
});

// node_modules/js-yaml/lib/type/map.js
var require_map = __commonJS({
  "node_modules/js-yaml/lib/type/map.js"(exports, module) {
    "use strict";
    var Type = require_type();
    module.exports = new Type("tag:yaml.org,2002:map", {
      kind: "mapping",
      construct: function(data) {
        return data !== null ? data : {};
      }
    });
  }
});

// node_modules/js-yaml/lib/schema/failsafe.js
var require_failsafe = __commonJS({
  "node_modules/js-yaml/lib/schema/failsafe.js"(exports, module) {
    "use strict";
    var Schema2 = require_schema();
    module.exports = new Schema2({
      explicit: [
        require_str(),
        require_seq(),
        require_map()
      ]
    });
  }
});

// node_modules/js-yaml/lib/type/null.js
var require_null = __commonJS({
  "node_modules/js-yaml/lib/type/null.js"(exports, module) {
    "use strict";
    var Type = require_type();
    function resolveYamlNull(data) {
      if (data === null) return true;
      var max2 = data.length;
      return max2 === 1 && data === "~" || max2 === 4 && (data === "null" || data === "Null" || data === "NULL");
    }
    function constructYamlNull() {
      return null;
    }
    function isNull(object2) {
      return object2 === null;
    }
    module.exports = new Type("tag:yaml.org,2002:null", {
      kind: "scalar",
      resolve: resolveYamlNull,
      construct: constructYamlNull,
      predicate: isNull,
      represent: {
        canonical: function() {
          return "~";
        },
        lowercase: function() {
          return "null";
        },
        uppercase: function() {
          return "NULL";
        },
        camelcase: function() {
          return "Null";
        },
        empty: function() {
          return "";
        }
      },
      defaultStyle: "lowercase"
    });
  }
});

// node_modules/js-yaml/lib/type/bool.js
var require_bool = __commonJS({
  "node_modules/js-yaml/lib/type/bool.js"(exports, module) {
    "use strict";
    var Type = require_type();
    function resolveYamlBoolean(data) {
      if (data === null) return false;
      var max2 = data.length;
      return max2 === 4 && (data === "true" || data === "True" || data === "TRUE") || max2 === 5 && (data === "false" || data === "False" || data === "FALSE");
    }
    function constructYamlBoolean(data) {
      return data === "true" || data === "True" || data === "TRUE";
    }
    function isBoolean(object2) {
      return Object.prototype.toString.call(object2) === "[object Boolean]";
    }
    module.exports = new Type("tag:yaml.org,2002:bool", {
      kind: "scalar",
      resolve: resolveYamlBoolean,
      construct: constructYamlBoolean,
      predicate: isBoolean,
      represent: {
        lowercase: function(object2) {
          return object2 ? "true" : "false";
        },
        uppercase: function(object2) {
          return object2 ? "TRUE" : "FALSE";
        },
        camelcase: function(object2) {
          return object2 ? "True" : "False";
        }
      },
      defaultStyle: "lowercase"
    });
  }
});

// node_modules/js-yaml/lib/type/int.js
var require_int = __commonJS({
  "node_modules/js-yaml/lib/type/int.js"(exports, module) {
    "use strict";
    var common = require_common();
    var Type = require_type();
    function isHexCode(c2) {
      return 48 <= c2 && c2 <= 57 || 65 <= c2 && c2 <= 70 || 97 <= c2 && c2 <= 102;
    }
    function isOctCode(c2) {
      return 48 <= c2 && c2 <= 55;
    }
    function isDecCode(c2) {
      return 48 <= c2 && c2 <= 57;
    }
    function resolveYamlInteger(data) {
      if (data === null) return false;
      var max2 = data.length, index = 0, hasDigits = false, ch;
      if (!max2) return false;
      ch = data[index];
      if (ch === "-" || ch === "+") {
        ch = data[++index];
      }
      if (ch === "0") {
        if (index + 1 === max2) return true;
        ch = data[++index];
        if (ch === "b") {
          index++;
          for (; index < max2; index++) {
            ch = data[index];
            if (ch === "_") continue;
            if (ch !== "0" && ch !== "1") return false;
            hasDigits = true;
          }
          return hasDigits && ch !== "_";
        }
        if (ch === "x") {
          index++;
          for (; index < max2; index++) {
            ch = data[index];
            if (ch === "_") continue;
            if (!isHexCode(data.charCodeAt(index))) return false;
            hasDigits = true;
          }
          return hasDigits && ch !== "_";
        }
        if (ch === "o") {
          index++;
          for (; index < max2; index++) {
            ch = data[index];
            if (ch === "_") continue;
            if (!isOctCode(data.charCodeAt(index))) return false;
            hasDigits = true;
          }
          return hasDigits && ch !== "_";
        }
      }
      if (ch === "_") return false;
      for (; index < max2; index++) {
        ch = data[index];
        if (ch === "_") continue;
        if (!isDecCode(data.charCodeAt(index))) {
          return false;
        }
        hasDigits = true;
      }
      if (!hasDigits || ch === "_") return false;
      return true;
    }
    function constructYamlInteger(data) {
      var value = data, sign = 1, ch;
      if (value.indexOf("_") !== -1) {
        value = value.replace(/_/g, "");
      }
      ch = value[0];
      if (ch === "-" || ch === "+") {
        if (ch === "-") sign = -1;
        value = value.slice(1);
        ch = value[0];
      }
      if (value === "0") return 0;
      if (ch === "0") {
        if (value[1] === "b") return sign * parseInt(value.slice(2), 2);
        if (value[1] === "x") return sign * parseInt(value.slice(2), 16);
        if (value[1] === "o") return sign * parseInt(value.slice(2), 8);
      }
      return sign * parseInt(value, 10);
    }
    function isInteger(object2) {
      return Object.prototype.toString.call(object2) === "[object Number]" && (object2 % 1 === 0 && !common.isNegativeZero(object2));
    }
    module.exports = new Type("tag:yaml.org,2002:int", {
      kind: "scalar",
      resolve: resolveYamlInteger,
      construct: constructYamlInteger,
      predicate: isInteger,
      represent: {
        binary: function(obj) {
          return obj >= 0 ? "0b" + obj.toString(2) : "-0b" + obj.toString(2).slice(1);
        },
        octal: function(obj) {
          return obj >= 0 ? "0o" + obj.toString(8) : "-0o" + obj.toString(8).slice(1);
        },
        decimal: function(obj) {
          return obj.toString(10);
        },
        /* eslint-disable max-len */
        hexadecimal: function(obj) {
          return obj >= 0 ? "0x" + obj.toString(16).toUpperCase() : "-0x" + obj.toString(16).toUpperCase().slice(1);
        }
      },
      defaultStyle: "decimal",
      styleAliases: {
        binary: [2, "bin"],
        octal: [8, "oct"],
        decimal: [10, "dec"],
        hexadecimal: [16, "hex"]
      }
    });
  }
});

// node_modules/js-yaml/lib/type/float.js
var require_float = __commonJS({
  "node_modules/js-yaml/lib/type/float.js"(exports, module) {
    "use strict";
    var common = require_common();
    var Type = require_type();
    var YAML_FLOAT_PATTERN = new RegExp(
      // 2.5e4, 2.5 and integers
      "^(?:[-+]?(?:[0-9][0-9_]*)(?:\\.[0-9_]*)?(?:[eE][-+]?[0-9]+)?|\\.[0-9_]+(?:[eE][-+]?[0-9]+)?|[-+]?\\.(?:inf|Inf|INF)|\\.(?:nan|NaN|NAN))$"
    );
    function resolveYamlFloat(data) {
      if (data === null) return false;
      if (!YAML_FLOAT_PATTERN.test(data) || // Quick hack to not allow integers end with `_`
      // Probably should update regexp & check speed
      data[data.length - 1] === "_") {
        return false;
      }
      return true;
    }
    function constructYamlFloat(data) {
      var value, sign;
      value = data.replace(/_/g, "").toLowerCase();
      sign = value[0] === "-" ? -1 : 1;
      if ("+-".indexOf(value[0]) >= 0) {
        value = value.slice(1);
      }
      if (value === ".inf") {
        return sign === 1 ? Number.POSITIVE_INFINITY : Number.NEGATIVE_INFINITY;
      } else if (value === ".nan") {
        return NaN;
      }
      return sign * parseFloat(value, 10);
    }
    var SCIENTIFIC_WITHOUT_DOT = /^[-+]?[0-9]+e/;
    function representYamlFloat(object2, style) {
      var res;
      if (isNaN(object2)) {
        switch (style) {
          case "lowercase":
            return ".nan";
          case "uppercase":
            return ".NAN";
          case "camelcase":
            return ".NaN";
        }
      } else if (Number.POSITIVE_INFINITY === object2) {
        switch (style) {
          case "lowercase":
            return ".inf";
          case "uppercase":
            return ".INF";
          case "camelcase":
            return ".Inf";
        }
      } else if (Number.NEGATIVE_INFINITY === object2) {
        switch (style) {
          case "lowercase":
            return "-.inf";
          case "uppercase":
            return "-.INF";
          case "camelcase":
            return "-.Inf";
        }
      } else if (common.isNegativeZero(object2)) {
        return "-0.0";
      }
      res = object2.toString(10);
      return SCIENTIFIC_WITHOUT_DOT.test(res) ? res.replace("e", ".e") : res;
    }
    function isFloat(object2) {
      return Object.prototype.toString.call(object2) === "[object Number]" && (object2 % 1 !== 0 || common.isNegativeZero(object2));
    }
    module.exports = new Type("tag:yaml.org,2002:float", {
      kind: "scalar",
      resolve: resolveYamlFloat,
      construct: constructYamlFloat,
      predicate: isFloat,
      represent: representYamlFloat,
      defaultStyle: "lowercase"
    });
  }
});

// node_modules/js-yaml/lib/schema/json.js
var require_json = __commonJS({
  "node_modules/js-yaml/lib/schema/json.js"(exports, module) {
    "use strict";
    module.exports = require_failsafe().extend({
      implicit: [
        require_null(),
        require_bool(),
        require_int(),
        require_float()
      ]
    });
  }
});

// node_modules/js-yaml/lib/schema/core.js
var require_core = __commonJS({
  "node_modules/js-yaml/lib/schema/core.js"(exports, module) {
    "use strict";
    module.exports = require_json();
  }
});

// node_modules/js-yaml/lib/type/timestamp.js
var require_timestamp = __commonJS({
  "node_modules/js-yaml/lib/type/timestamp.js"(exports, module) {
    "use strict";
    var Type = require_type();
    var YAML_DATE_REGEXP = new RegExp(
      "^([0-9][0-9][0-9][0-9])-([0-9][0-9])-([0-9][0-9])$"
    );
    var YAML_TIMESTAMP_REGEXP = new RegExp(
      "^([0-9][0-9][0-9][0-9])-([0-9][0-9]?)-([0-9][0-9]?)(?:[Tt]|[ \\t]+)([0-9][0-9]?):([0-9][0-9]):([0-9][0-9])(?:\\.([0-9]*))?(?:[ \\t]*(Z|([-+])([0-9][0-9]?)(?::([0-9][0-9]))?))?$"
    );
    function resolveYamlTimestamp(data) {
      if (data === null) return false;
      if (YAML_DATE_REGEXP.exec(data) !== null) return true;
      if (YAML_TIMESTAMP_REGEXP.exec(data) !== null) return true;
      return false;
    }
    function constructYamlTimestamp(data) {
      var match2, year, month, day, hour, minute, second, fraction = 0, delta = null, tz_hour, tz_minute, date;
      match2 = YAML_DATE_REGEXP.exec(data);
      if (match2 === null) match2 = YAML_TIMESTAMP_REGEXP.exec(data);
      if (match2 === null) throw new Error("Date resolve error");
      year = +match2[1];
      month = +match2[2] - 1;
      day = +match2[3];
      if (!match2[4]) {
        return new Date(Date.UTC(year, month, day));
      }
      hour = +match2[4];
      minute = +match2[5];
      second = +match2[6];
      if (match2[7]) {
        fraction = match2[7].slice(0, 3);
        while (fraction.length < 3) {
          fraction += "0";
        }
        fraction = +fraction;
      }
      if (match2[9]) {
        tz_hour = +match2[10];
        tz_minute = +(match2[11] || 0);
        delta = (tz_hour * 60 + tz_minute) * 6e4;
        if (match2[9] === "-") delta = -delta;
      }
      date = new Date(Date.UTC(year, month, day, hour, minute, second, fraction));
      if (delta) date.setTime(date.getTime() - delta);
      return date;
    }
    function representYamlTimestamp(object2) {
      return object2.toISOString();
    }
    module.exports = new Type("tag:yaml.org,2002:timestamp", {
      kind: "scalar",
      resolve: resolveYamlTimestamp,
      construct: constructYamlTimestamp,
      instanceOf: Date,
      represent: representYamlTimestamp
    });
  }
});

// node_modules/js-yaml/lib/type/merge.js
var require_merge = __commonJS({
  "node_modules/js-yaml/lib/type/merge.js"(exports, module) {
    "use strict";
    var Type = require_type();
    function resolveYamlMerge(data) {
      return data === "<<" || data === null;
    }
    module.exports = new Type("tag:yaml.org,2002:merge", {
      kind: "scalar",
      resolve: resolveYamlMerge
    });
  }
});

// node_modules/js-yaml/lib/type/binary.js
var require_binary = __commonJS({
  "node_modules/js-yaml/lib/type/binary.js"(exports, module) {
    "use strict";
    var Type = require_type();
    var BASE64_MAP = "ABCDEFGHIJKLMNOPQRSTUVWXYZabcdefghijklmnopqrstuvwxyz0123456789+/=\n\r";
    function resolveYamlBinary(data) {
      if (data === null) return false;
      var code, idx, bitlen = 0, max2 = data.length, map3 = BASE64_MAP;
      for (idx = 0; idx < max2; idx++) {
        code = map3.indexOf(data.charAt(idx));
        if (code > 64) continue;
        if (code < 0) return false;
        bitlen += 6;
      }
      return bitlen % 8 === 0;
    }
    function constructYamlBinary(data) {
      var idx, tailbits, input = data.replace(/[\r\n=]/g, ""), max2 = input.length, map3 = BASE64_MAP, bits = 0, result = [];
      for (idx = 0; idx < max2; idx++) {
        if (idx % 4 === 0 && idx) {
          result.push(bits >> 16 & 255);
          result.push(bits >> 8 & 255);
          result.push(bits & 255);
        }
        bits = bits << 6 | map3.indexOf(input.charAt(idx));
      }
      tailbits = max2 % 4 * 6;
      if (tailbits === 0) {
        result.push(bits >> 16 & 255);
        result.push(bits >> 8 & 255);
        result.push(bits & 255);
      } else if (tailbits === 18) {
        result.push(bits >> 10 & 255);
        result.push(bits >> 2 & 255);
      } else if (tailbits === 12) {
        result.push(bits >> 4 & 255);
      }
      return new Uint8Array(result);
    }
    function representYamlBinary(object2) {
      var result = "", bits = 0, idx, tail, max2 = object2.length, map3 = BASE64_MAP;
      for (idx = 0; idx < max2; idx++) {
        if (idx % 3 === 0 && idx) {
          result += map3[bits >> 18 & 63];
          result += map3[bits >> 12 & 63];
          result += map3[bits >> 6 & 63];
          result += map3[bits & 63];
        }
        bits = (bits << 8) + object2[idx];
      }
      tail = max2 % 3;
      if (tail === 0) {
        result += map3[bits >> 18 & 63];
        result += map3[bits >> 12 & 63];
        result += map3[bits >> 6 & 63];
        result += map3[bits & 63];
      } else if (tail === 2) {
        result += map3[bits >> 10 & 63];
        result += map3[bits >> 4 & 63];
        result += map3[bits << 2 & 63];
        result += map3[64];
      } else if (tail === 1) {
        result += map3[bits >> 2 & 63];
        result += map3[bits << 4 & 63];
        result += map3[64];
        result += map3[64];
      }
      return result;
    }
    function isBinary(obj) {
      return Object.prototype.toString.call(obj) === "[object Uint8Array]";
    }
    module.exports = new Type("tag:yaml.org,2002:binary", {
      kind: "scalar",
      resolve: resolveYamlBinary,
      construct: constructYamlBinary,
      predicate: isBinary,
      represent: representYamlBinary
    });
  }
});

// node_modules/js-yaml/lib/type/omap.js
var require_omap = __commonJS({
  "node_modules/js-yaml/lib/type/omap.js"(exports, module) {
    "use strict";
    var Type = require_type();
    var _hasOwnProperty = Object.prototype.hasOwnProperty;
    var _toString = Object.prototype.toString;
    function resolveYamlOmap(data) {
      if (data === null) return true;
      var objectKeys = [], index, length2, pair, pairKey, pairHasKey, object2 = data;
      for (index = 0, length2 = object2.length; index < length2; index += 1) {
        pair = object2[index];
        pairHasKey = false;
        if (_toString.call(pair) !== "[object Object]") return false;
        for (pairKey in pair) {
          if (_hasOwnProperty.call(pair, pairKey)) {
            if (!pairHasKey) pairHasKey = true;
            else return false;
          }
        }
        if (!pairHasKey) return false;
        if (objectKeys.indexOf(pairKey) === -1) objectKeys.push(pairKey);
        else return false;
      }
      return true;
    }
    function constructYamlOmap(data) {
      return data !== null ? data : [];
    }
    module.exports = new Type("tag:yaml.org,2002:omap", {
      kind: "sequence",
      resolve: resolveYamlOmap,
      construct: constructYamlOmap
    });
  }
});

// node_modules/js-yaml/lib/type/pairs.js
var require_pairs = __commonJS({
  "node_modules/js-yaml/lib/type/pairs.js"(exports, module) {
    "use strict";
    var Type = require_type();
    var _toString = Object.prototype.toString;
    function resolveYamlPairs(data) {
      if (data === null) return true;
      var index, length2, pair, keys2, result, object2 = data;
      result = new Array(object2.length);
      for (index = 0, length2 = object2.length; index < length2; index += 1) {
        pair = object2[index];
        if (_toString.call(pair) !== "[object Object]") return false;
        keys2 = Object.keys(pair);
        if (keys2.length !== 1) return false;
        result[index] = [keys2[0], pair[keys2[0]]];
      }
      return true;
    }
    function constructYamlPairs(data) {
      if (data === null) return [];
      var index, length2, pair, keys2, result, object2 = data;
      result = new Array(object2.length);
      for (index = 0, length2 = object2.length; index < length2; index += 1) {
        pair = object2[index];
        keys2 = Object.keys(pair);
        result[index] = [keys2[0], pair[keys2[0]]];
      }
      return result;
    }
    module.exports = new Type("tag:yaml.org,2002:pairs", {
      kind: "sequence",
      resolve: resolveYamlPairs,
      construct: constructYamlPairs
    });
  }
});

// node_modules/js-yaml/lib/type/set.js
var require_set = __commonJS({
  "node_modules/js-yaml/lib/type/set.js"(exports, module) {
    "use strict";
    var Type = require_type();
    var _hasOwnProperty = Object.prototype.hasOwnProperty;
    function resolveYamlSet(data) {
      if (data === null) return true;
      var key2, object2 = data;
      for (key2 in object2) {
        if (_hasOwnProperty.call(object2, key2)) {
          if (object2[key2] !== null) return false;
        }
      }
      return true;
    }
    function constructYamlSet(data) {
      return data !== null ? data : {};
    }
    module.exports = new Type("tag:yaml.org,2002:set", {
      kind: "mapping",
      resolve: resolveYamlSet,
      construct: constructYamlSet
    });
  }
});

// node_modules/js-yaml/lib/schema/default.js
var require_default = __commonJS({
  "node_modules/js-yaml/lib/schema/default.js"(exports, module) {
    "use strict";
    module.exports = require_core().extend({
      implicit: [
        require_timestamp(),
        require_merge()
      ],
      explicit: [
        require_binary(),
        require_omap(),
        require_pairs(),
        require_set()
      ]
    });
  }
});

// node_modules/js-yaml/lib/loader.js
var require_loader = __commonJS({
  "node_modules/js-yaml/lib/loader.js"(exports, module) {
    "use strict";
    var common = require_common();
    var YAMLException = require_exception();
    var makeSnippet = require_snippet();
    var DEFAULT_SCHEMA = require_default();
    var _hasOwnProperty = Object.prototype.hasOwnProperty;
    var CONTEXT_FLOW_IN = 1;
    var CONTEXT_FLOW_OUT = 2;
    var CONTEXT_BLOCK_IN = 3;
    var CONTEXT_BLOCK_OUT = 4;
    var CHOMPING_CLIP = 1;
    var CHOMPING_STRIP = 2;
    var CHOMPING_KEEP = 3;
    var PATTERN_NON_PRINTABLE = /[\x00-\x08\x0B\x0C\x0E-\x1F\x7F-\x84\x86-\x9F\uFFFE\uFFFF]|[\uD800-\uDBFF](?![\uDC00-\uDFFF])|(?:[^\uD800-\uDBFF]|^)[\uDC00-\uDFFF]/;
    var PATTERN_NON_ASCII_LINE_BREAKS = /[\x85\u2028\u2029]/;
    var PATTERN_FLOW_INDICATORS = /[,\[\]\{\}]/;
    var PATTERN_TAG_HANDLE = /^(?:!|!!|![a-z\-]+!)$/i;
    var PATTERN_TAG_URI = /^(?:!|[^,\[\]\{\}])(?:%[0-9a-f]{2}|[0-9a-z\-#;\/\?:@&=\+\$,_\.!~\*'\(\)\[\]])*$/i;
    function _class(obj) {
      return Object.prototype.toString.call(obj);
    }
    function is_EOL(c2) {
      return c2 === 10 || c2 === 13;
    }
    function is_WHITE_SPACE(c2) {
      return c2 === 9 || c2 === 32;
    }
    function is_WS_OR_EOL(c2) {
      return c2 === 9 || c2 === 32 || c2 === 10 || c2 === 13;
    }
    function is_FLOW_INDICATOR(c2) {
      return c2 === 44 || c2 === 91 || c2 === 93 || c2 === 123 || c2 === 125;
    }
    function fromHexCode(c2) {
      var lc;
      if (48 <= c2 && c2 <= 57) {
        return c2 - 48;
      }
      lc = c2 | 32;
      if (97 <= lc && lc <= 102) {
        return lc - 97 + 10;
      }
      return -1;
    }
    function escapedHexLen(c2) {
      if (c2 === 120) {
        return 2;
      }
      if (c2 === 117) {
        return 4;
      }
      if (c2 === 85) {
        return 8;
      }
      return 0;
    }
    function fromDecimalCode(c2) {
      if (48 <= c2 && c2 <= 57) {
        return c2 - 48;
      }
      return -1;
    }
    function simpleEscapeSequence(c2) {
      return c2 === 48 ? "\0" : c2 === 97 ? "\x07" : c2 === 98 ? "\b" : c2 === 116 ? "	" : c2 === 9 ? "	" : c2 === 110 ? "\n" : c2 === 118 ? "\v" : c2 === 102 ? "\f" : c2 === 114 ? "\r" : c2 === 101 ? "\x1B" : c2 === 32 ? " " : c2 === 34 ? '"' : c2 === 47 ? "/" : c2 === 92 ? "\\" : c2 === 78 ? "" : c2 === 95 ? " " : c2 === 76 ? "\u2028" : c2 === 80 ? "\u2029" : "";
    }
    function charFromCodepoint(c2) {
      if (c2 <= 65535) {
        return String.fromCharCode(c2);
      }
      return String.fromCharCode(
        (c2 - 65536 >> 10) + 55296,
        (c2 - 65536 & 1023) + 56320
      );
    }
    var simpleEscapeCheck = new Array(256);
    var simpleEscapeMap = new Array(256);
    for (i2 = 0; i2 < 256; i2++) {
      simpleEscapeCheck[i2] = simpleEscapeSequence(i2) ? 1 : 0;
      simpleEscapeMap[i2] = simpleEscapeSequence(i2);
    }
    var i2;
    function State(input, options) {
      this.input = input;
      this.filename = options["filename"] || null;
      this.schema = options["schema"] || DEFAULT_SCHEMA;
      this.onWarning = options["onWarning"] || null;
      this.legacy = options["legacy"] || false;
      this.json = options["json"] || false;
      this.listener = options["listener"] || null;
      this.implicitTypes = this.schema.compiledImplicit;
      this.typeMap = this.schema.compiledTypeMap;
      this.length = input.length;
      this.position = 0;
      this.line = 0;
      this.lineStart = 0;
      this.lineIndent = 0;
      this.firstTabInLine = -1;
      this.documents = [];
    }
    function generateError(state, message) {
      var mark = {
        name: state.filename,
        buffer: state.input.slice(0, -1),
        // omit trailing \0
        position: state.position,
        line: state.line,
        column: state.position - state.lineStart
      };
      mark.snippet = makeSnippet(mark);
      return new YAMLException(message, mark);
    }
    function throwError(state, message) {
      throw generateError(state, message);
    }
    function throwWarning(state, message) {
      if (state.onWarning) {
        state.onWarning.call(null, generateError(state, message));
      }
    }
    var directiveHandlers = {
      YAML: function handleYamlDirective(state, name, args) {
        var match2, major, minor;
        if (state.version !== null) {
          throwError(state, "duplication of %YAML directive");
        }
        if (args.length !== 1) {
          throwError(state, "YAML directive accepts exactly one argument");
        }
        match2 = /^([0-9]+)\.([0-9]+)$/.exec(args[0]);
        if (match2 === null) {
          throwError(state, "ill-formed argument of the YAML directive");
        }
        major = parseInt(match2[1], 10);
        minor = parseInt(match2[2], 10);
        if (major !== 1) {
          throwError(state, "unacceptable YAML version of the document");
        }
        state.version = args[0];
        state.checkLineBreaks = minor < 2;
        if (minor !== 1 && minor !== 2) {
          throwWarning(state, "unsupported YAML version of the document");
        }
      },
      TAG: function handleTagDirective(state, name, args) {
        var handle, prefix2;
        if (args.length !== 2) {
          throwError(state, "TAG directive accepts exactly two arguments");
        }
        handle = args[0];
        prefix2 = args[1];
        if (!PATTERN_TAG_HANDLE.test(handle)) {
          throwError(state, "ill-formed tag handle (first argument) of the TAG directive");
        }
        if (_hasOwnProperty.call(state.tagMap, handle)) {
          throwError(state, 'there is a previously declared suffix for "' + handle + '" tag handle');
        }
        if (!PATTERN_TAG_URI.test(prefix2)) {
          throwError(state, "ill-formed tag prefix (second argument) of the TAG directive");
        }
        try {
          prefix2 = decodeURIComponent(prefix2);
        } catch (err) {
          throwError(state, "tag prefix is malformed: " + prefix2);
        }
        state.tagMap[handle] = prefix2;
      }
    };
    function captureSegment(state, start, end, checkJson) {
      var _position, _length, _character, _result;
      if (start < end) {
        _result = state.input.slice(start, end);
        if (checkJson) {
          for (_position = 0, _length = _result.length; _position < _length; _position += 1) {
            _character = _result.charCodeAt(_position);
            if (!(_character === 9 || 32 <= _character && _character <= 1114111)) {
              throwError(state, "expected valid JSON character");
            }
          }
        } else if (PATTERN_NON_PRINTABLE.test(_result)) {
          throwError(state, "the stream contains non-printable characters");
        }
        state.result += _result;
      }
    }
    function mergeMappings(state, destination, source, overridableKeys) {
      var sourceKeys, key2, index, quantity;
      if (!common.isObject(source)) {
        throwError(state, "cannot merge mappings; the provided source object is unacceptable");
      }
      sourceKeys = Object.keys(source);
      for (index = 0, quantity = sourceKeys.length; index < quantity; index += 1) {
        key2 = sourceKeys[index];
        if (!_hasOwnProperty.call(destination, key2)) {
          destination[key2] = source[key2];
          overridableKeys[key2] = true;
        }
      }
    }
    function storeMappingPair(state, _result, overridableKeys, keyTag, keyNode, valueNode, startLine, startLineStart, startPos) {
      var index, quantity;
      if (Array.isArray(keyNode)) {
        keyNode = Array.prototype.slice.call(keyNode);
        for (index = 0, quantity = keyNode.length; index < quantity; index += 1) {
          if (Array.isArray(keyNode[index])) {
            throwError(state, "nested arrays are not supported inside keys");
          }
          if (typeof keyNode === "object" && _class(keyNode[index]) === "[object Object]") {
            keyNode[index] = "[object Object]";
          }
        }
      }
      if (typeof keyNode === "object" && _class(keyNode) === "[object Object]") {
        keyNode = "[object Object]";
      }
      keyNode = String(keyNode);
      if (_result === null) {
        _result = {};
      }
      if (keyTag === "tag:yaml.org,2002:merge") {
        if (Array.isArray(valueNode)) {
          for (index = 0, quantity = valueNode.length; index < quantity; index += 1) {
            mergeMappings(state, _result, valueNode[index], overridableKeys);
          }
        } else {
          mergeMappings(state, _result, valueNode, overridableKeys);
        }
      } else {
        if (!state.json && !_hasOwnProperty.call(overridableKeys, keyNode) && _hasOwnProperty.call(_result, keyNode)) {
          state.line = startLine || state.line;
          state.lineStart = startLineStart || state.lineStart;
          state.position = startPos || state.position;
          throwError(state, "duplicated mapping key");
        }
        if (keyNode === "__proto__") {
          Object.defineProperty(_result, keyNode, {
            configurable: true,
            enumerable: true,
            writable: true,
            value: valueNode
          });
        } else {
          _result[keyNode] = valueNode;
        }
        delete overridableKeys[keyNode];
      }
      return _result;
    }
    function readLineBreak(state) {
      var ch;
      ch = state.input.charCodeAt(state.position);
      if (ch === 10) {
        state.position++;
      } else if (ch === 13) {
        state.position++;
        if (state.input.charCodeAt(state.position) === 10) {
          state.position++;
        }
      } else {
        throwError(state, "a line break is expected");
      }
      state.line += 1;
      state.lineStart = state.position;
      state.firstTabInLine = -1;
    }
    function skipSeparationSpace(state, allowComments, checkIndent) {
      var lineBreaks = 0, ch = state.input.charCodeAt(state.position);
      while (ch !== 0) {
        while (is_WHITE_SPACE(ch)) {
          if (ch === 9 && state.firstTabInLine === -1) {
            state.firstTabInLine = state.position;
          }
          ch = state.input.charCodeAt(++state.position);
        }
        if (allowComments && ch === 35) {
          do {
            ch = state.input.charCodeAt(++state.position);
          } while (ch !== 10 && ch !== 13 && ch !== 0);
        }
        if (is_EOL(ch)) {
          readLineBreak(state);
          ch = state.input.charCodeAt(state.position);
          lineBreaks++;
          state.lineIndent = 0;
          while (ch === 32) {
            state.lineIndent++;
            ch = state.input.charCodeAt(++state.position);
          }
        } else {
          break;
        }
      }
      if (checkIndent !== -1 && lineBreaks !== 0 && state.lineIndent < checkIndent) {
        throwWarning(state, "deficient indentation");
      }
      return lineBreaks;
    }
    function testDocumentSeparator(state) {
      var _position = state.position, ch;
      ch = state.input.charCodeAt(_position);
      if ((ch === 45 || ch === 46) && ch === state.input.charCodeAt(_position + 1) && ch === state.input.charCodeAt(_position + 2)) {
        _position += 3;
        ch = state.input.charCodeAt(_position);
        if (ch === 0 || is_WS_OR_EOL(ch)) {
          return true;
        }
      }
      return false;
    }
    function writeFoldedLines(state, count) {
      if (count === 1) {
        state.result += " ";
      } else if (count > 1) {
        state.result += common.repeat("\n", count - 1);
      }
    }
    function readPlainScalar(state, nodeIndent, withinFlowCollection) {
      var preceding, following, captureStart, captureEnd, hasPendingContent, _line, _lineStart, _lineIndent, _kind = state.kind, _result = state.result, ch;
      ch = state.input.charCodeAt(state.position);
      if (is_WS_OR_EOL(ch) || is_FLOW_INDICATOR(ch) || ch === 35 || ch === 38 || ch === 42 || ch === 33 || ch === 124 || ch === 62 || ch === 39 || ch === 34 || ch === 37 || ch === 64 || ch === 96) {
        return false;
      }
      if (ch === 63 || ch === 45) {
        following = state.input.charCodeAt(state.position + 1);
        if (is_WS_OR_EOL(following) || withinFlowCollection && is_FLOW_INDICATOR(following)) {
          return false;
        }
      }
      state.kind = "scalar";
      state.result = "";
      captureStart = captureEnd = state.position;
      hasPendingContent = false;
      while (ch !== 0) {
        if (ch === 58) {
          following = state.input.charCodeAt(state.position + 1);
          if (is_WS_OR_EOL(following) || withinFlowCollection && is_FLOW_INDICATOR(following)) {
            break;
          }
        } else if (ch === 35) {
          preceding = state.input.charCodeAt(state.position - 1);
          if (is_WS_OR_EOL(preceding)) {
            break;
          }
        } else if (state.position === state.lineStart && testDocumentSeparator(state) || withinFlowCollection && is_FLOW_INDICATOR(ch)) {
          break;
        } else if (is_EOL(ch)) {
          _line = state.line;
          _lineStart = state.lineStart;
          _lineIndent = state.lineIndent;
          skipSeparationSpace(state, false, -1);
          if (state.lineIndent >= nodeIndent) {
            hasPendingContent = true;
            ch = state.input.charCodeAt(state.position);
            continue;
          } else {
            state.position = captureEnd;
            state.line = _line;
            state.lineStart = _lineStart;
            state.lineIndent = _lineIndent;
            break;
          }
        }
        if (hasPendingContent) {
          captureSegment(state, captureStart, captureEnd, false);
          writeFoldedLines(state, state.line - _line);
          captureStart = captureEnd = state.position;
          hasPendingContent = false;
        }
        if (!is_WHITE_SPACE(ch)) {
          captureEnd = state.position + 1;
        }
        ch = state.input.charCodeAt(++state.position);
      }
      captureSegment(state, captureStart, captureEnd, false);
      if (state.result) {
        return true;
      }
      state.kind = _kind;
      state.result = _result;
      return false;
    }
    function readSingleQuotedScalar(state, nodeIndent) {
      var ch, captureStart, captureEnd;
      ch = state.input.charCodeAt(state.position);
      if (ch !== 39) {
        return false;
      }
      state.kind = "scalar";
      state.result = "";
      state.position++;
      captureStart = captureEnd = state.position;
      while ((ch = state.input.charCodeAt(state.position)) !== 0) {
        if (ch === 39) {
          captureSegment(state, captureStart, state.position, true);
          ch = state.input.charCodeAt(++state.position);
          if (ch === 39) {
            captureStart = state.position;
            state.position++;
            captureEnd = state.position;
          } else {
            return true;
          }
        } else if (is_EOL(ch)) {
          captureSegment(state, captureStart, captureEnd, true);
          writeFoldedLines(state, skipSeparationSpace(state, false, nodeIndent));
          captureStart = captureEnd = state.position;
        } else if (state.position === state.lineStart && testDocumentSeparator(state)) {
          throwError(state, "unexpected end of the document within a single quoted scalar");
        } else {
          state.position++;
          captureEnd = state.position;
        }
      }
      throwError(state, "unexpected end of the stream within a single quoted scalar");
    }
    function readDoubleQuotedScalar(state, nodeIndent) {
      var captureStart, captureEnd, hexLength, hexResult, tmp, ch;
      ch = state.input.charCodeAt(state.position);
      if (ch !== 34) {
        return false;
      }
      state.kind = "scalar";
      state.result = "";
      state.position++;
      captureStart = captureEnd = state.position;
      while ((ch = state.input.charCodeAt(state.position)) !== 0) {
        if (ch === 34) {
          captureSegment(state, captureStart, state.position, true);
          state.position++;
          return true;
        } else if (ch === 92) {
          captureSegment(state, captureStart, state.position, true);
          ch = state.input.charCodeAt(++state.position);
          if (is_EOL(ch)) {
            skipSeparationSpace(state, false, nodeIndent);
          } else if (ch < 256 && simpleEscapeCheck[ch]) {
            state.result += simpleEscapeMap[ch];
            state.position++;
          } else if ((tmp = escapedHexLen(ch)) > 0) {
            hexLength = tmp;
            hexResult = 0;
            for (; hexLength > 0; hexLength--) {
              ch = state.input.charCodeAt(++state.position);
              if ((tmp = fromHexCode(ch)) >= 0) {
                hexResult = (hexResult << 4) + tmp;
              } else {
                throwError(state, "expected hexadecimal character");
              }
            }
            state.result += charFromCodepoint(hexResult);
            state.position++;
          } else {
            throwError(state, "unknown escape sequence");
          }
          captureStart = captureEnd = state.position;
        } else if (is_EOL(ch)) {
          captureSegment(state, captureStart, captureEnd, true);
          writeFoldedLines(state, skipSeparationSpace(state, false, nodeIndent));
          captureStart = captureEnd = state.position;
        } else if (state.position === state.lineStart && testDocumentSeparator(state)) {
          throwError(state, "unexpected end of the document within a double quoted scalar");
        } else {
          state.position++;
          captureEnd = state.position;
        }
      }
      throwError(state, "unexpected end of the stream within a double quoted scalar");
    }
    function readFlowCollection(state, nodeIndent) {
      var readNext = true, _line, _lineStart, _pos, _tag = state.tag, _result, _anchor = state.anchor, following, terminator, isPair2, isExplicitPair, isMapping, overridableKeys = /* @__PURE__ */ Object.create(null), keyNode, keyTag, valueNode, ch;
      ch = state.input.charCodeAt(state.position);
      if (ch === 91) {
        terminator = 93;
        isMapping = false;
        _result = [];
      } else if (ch === 123) {
        terminator = 125;
        isMapping = true;
        _result = {};
      } else {
        return false;
      }
      if (state.anchor !== null) {
        state.anchorMap[state.anchor] = _result;
      }
      ch = state.input.charCodeAt(++state.position);
      while (ch !== 0) {
        skipSeparationSpace(state, true, nodeIndent);
        ch = state.input.charCodeAt(state.position);
        if (ch === terminator) {
          state.position++;
          state.tag = _tag;
          state.anchor = _anchor;
          state.kind = isMapping ? "mapping" : "sequence";
          state.result = _result;
          return true;
        } else if (!readNext) {
          throwError(state, "missed comma between flow collection entries");
        } else if (ch === 44) {
          throwError(state, "expected the node content, but found ','");
        }
        keyTag = keyNode = valueNode = null;
        isPair2 = isExplicitPair = false;
        if (ch === 63) {
          following = state.input.charCodeAt(state.position + 1);
          if (is_WS_OR_EOL(following)) {
            isPair2 = isExplicitPair = true;
            state.position++;
            skipSeparationSpace(state, true, nodeIndent);
          }
        }
        _line = state.line;
        _lineStart = state.lineStart;
        _pos = state.position;
        composeNode2(state, nodeIndent, CONTEXT_FLOW_IN, false, true);
        keyTag = state.tag;
        keyNode = state.result;
        skipSeparationSpace(state, true, nodeIndent);
        ch = state.input.charCodeAt(state.position);
        if ((isExplicitPair || state.line === _line) && ch === 58) {
          isPair2 = true;
          ch = state.input.charCodeAt(++state.position);
          skipSeparationSpace(state, true, nodeIndent);
          composeNode2(state, nodeIndent, CONTEXT_FLOW_IN, false, true);
          valueNode = state.result;
        }
        if (isMapping) {
          storeMappingPair(state, _result, overridableKeys, keyTag, keyNode, valueNode, _line, _lineStart, _pos);
        } else if (isPair2) {
          _result.push(storeMappingPair(state, null, overridableKeys, keyTag, keyNode, valueNode, _line, _lineStart, _pos));
        } else {
          _result.push(keyNode);
        }
        skipSeparationSpace(state, true, nodeIndent);
        ch = state.input.charCodeAt(state.position);
        if (ch === 44) {
          readNext = true;
          ch = state.input.charCodeAt(++state.position);
        } else {
          readNext = false;
        }
      }
      throwError(state, "unexpected end of the stream within a flow collection");
    }
    function readBlockScalar(state, nodeIndent) {
      var captureStart, folding, chomping = CHOMPING_CLIP, didReadContent = false, detectedIndent = false, textIndent = nodeIndent, emptyLines = 0, atMoreIndented = false, tmp, ch;
      ch = state.input.charCodeAt(state.position);
      if (ch === 124) {
        folding = false;
      } else if (ch === 62) {
        folding = true;
      } else {
        return false;
      }
      state.kind = "scalar";
      state.result = "";
      while (ch !== 0) {
        ch = state.input.charCodeAt(++state.position);
        if (ch === 43 || ch === 45) {
          if (CHOMPING_CLIP === chomping) {
            chomping = ch === 43 ? CHOMPING_KEEP : CHOMPING_STRIP;
          } else {
            throwError(state, "repeat of a chomping mode identifier");
          }
        } else if ((tmp = fromDecimalCode(ch)) >= 0) {
          if (tmp === 0) {
            throwError(state, "bad explicit indentation width of a block scalar; it cannot be less than one");
          } else if (!detectedIndent) {
            textIndent = nodeIndent + tmp - 1;
            detectedIndent = true;
          } else {
            throwError(state, "repeat of an indentation width identifier");
          }
        } else {
          break;
        }
      }
      if (is_WHITE_SPACE(ch)) {
        do {
          ch = state.input.charCodeAt(++state.position);
        } while (is_WHITE_SPACE(ch));
        if (ch === 35) {
          do {
            ch = state.input.charCodeAt(++state.position);
          } while (!is_EOL(ch) && ch !== 0);
        }
      }
      while (ch !== 0) {
        readLineBreak(state);
        state.lineIndent = 0;
        ch = state.input.charCodeAt(state.position);
        while ((!detectedIndent || state.lineIndent < textIndent) && ch === 32) {
          state.lineIndent++;
          ch = state.input.charCodeAt(++state.position);
        }
        if (!detectedIndent && state.lineIndent > textIndent) {
          textIndent = state.lineIndent;
        }
        if (is_EOL(ch)) {
          emptyLines++;
          continue;
        }
        if (state.lineIndent < textIndent) {
          if (chomping === CHOMPING_KEEP) {
            state.result += common.repeat("\n", didReadContent ? 1 + emptyLines : emptyLines);
          } else if (chomping === CHOMPING_CLIP) {
            if (didReadContent) {
              state.result += "\n";
            }
          }
          break;
        }
        if (folding) {
          if (is_WHITE_SPACE(ch)) {
            atMoreIndented = true;
            state.result += common.repeat("\n", didReadContent ? 1 + emptyLines : emptyLines);
          } else if (atMoreIndented) {
            atMoreIndented = false;
            state.result += common.repeat("\n", emptyLines + 1);
          } else if (emptyLines === 0) {
            if (didReadContent) {
              state.result += " ";
            }
          } else {
            state.result += common.repeat("\n", emptyLines);
          }
        } else {
          state.result += common.repeat("\n", didReadContent ? 1 + emptyLines : emptyLines);
        }
        didReadContent = true;
        detectedIndent = true;
        emptyLines = 0;
        captureStart = state.position;
        while (!is_EOL(ch) && ch !== 0) {
          ch = state.input.charCodeAt(++state.position);
        }
        captureSegment(state, captureStart, state.position, false);
      }
      return true;
    }
    function readBlockSequence(state, nodeIndent) {
      var _line, _tag = state.tag, _anchor = state.anchor, _result = [], following, detected = false, ch;
      if (state.firstTabInLine !== -1) return false;
      if (state.anchor !== null) {
        state.anchorMap[state.anchor] = _result;
      }
      ch = state.input.charCodeAt(state.position);
      while (ch !== 0) {
        if (state.firstTabInLine !== -1) {
          state.position = state.firstTabInLine;
          throwError(state, "tab characters must not be used in indentation");
        }
        if (ch !== 45) {
          break;
        }
        following = state.input.charCodeAt(state.position + 1);
        if (!is_WS_OR_EOL(following)) {
          break;
        }
        detected = true;
        state.position++;
        if (skipSeparationSpace(state, true, -1)) {
          if (state.lineIndent <= nodeIndent) {
            _result.push(null);
            ch = state.input.charCodeAt(state.position);
            continue;
          }
        }
        _line = state.line;
        composeNode2(state, nodeIndent, CONTEXT_BLOCK_IN, false, true);
        _result.push(state.result);
        skipSeparationSpace(state, true, -1);
        ch = state.input.charCodeAt(state.position);
        if ((state.line === _line || state.lineIndent > nodeIndent) && ch !== 0) {
          throwError(state, "bad indentation of a sequence entry");
        } else if (state.lineIndent < nodeIndent) {
          break;
        }
      }
      if (detected) {
        state.tag = _tag;
        state.anchor = _anchor;
        state.kind = "sequence";
        state.result = _result;
        return true;
      }
      return false;
    }
    function readBlockMapping(state, nodeIndent, flowIndent) {
      var following, allowCompact, _line, _keyLine, _keyLineStart, _keyPos, _tag = state.tag, _anchor = state.anchor, _result = {}, overridableKeys = /* @__PURE__ */ Object.create(null), keyTag = null, keyNode = null, valueNode = null, atExplicitKey = false, detected = false, ch;
      if (state.firstTabInLine !== -1) return false;
      if (state.anchor !== null) {
        state.anchorMap[state.anchor] = _result;
      }
      ch = state.input.charCodeAt(state.position);
      while (ch !== 0) {
        if (!atExplicitKey && state.firstTabInLine !== -1) {
          state.position = state.firstTabInLine;
          throwError(state, "tab characters must not be used in indentation");
        }
        following = state.input.charCodeAt(state.position + 1);
        _line = state.line;
        if ((ch === 63 || ch === 58) && is_WS_OR_EOL(following)) {
          if (ch === 63) {
            if (atExplicitKey) {
              storeMappingPair(state, _result, overridableKeys, keyTag, keyNode, null, _keyLine, _keyLineStart, _keyPos);
              keyTag = keyNode = valueNode = null;
            }
            detected = true;
            atExplicitKey = true;
            allowCompact = true;
          } else if (atExplicitKey) {
            atExplicitKey = false;
            allowCompact = true;
          } else {
            throwError(state, "incomplete explicit mapping pair; a key node is missed; or followed by a non-tabulated empty line");
          }
          state.position += 1;
          ch = following;
        } else {
          _keyLine = state.line;
          _keyLineStart = state.lineStart;
          _keyPos = state.position;
          if (!composeNode2(state, flowIndent, CONTEXT_FLOW_OUT, false, true)) {
            break;
          }
          if (state.line === _line) {
            ch = state.input.charCodeAt(state.position);
            while (is_WHITE_SPACE(ch)) {
              ch = state.input.charCodeAt(++state.position);
            }
            if (ch === 58) {
              ch = state.input.charCodeAt(++state.position);
              if (!is_WS_OR_EOL(ch)) {
                throwError(state, "a whitespace character is expected after the key-value separator within a block mapping");
              }
              if (atExplicitKey) {
                storeMappingPair(state, _result, overridableKeys, keyTag, keyNode, null, _keyLine, _keyLineStart, _keyPos);
                keyTag = keyNode = valueNode = null;
              }
              detected = true;
              atExplicitKey = false;
              allowCompact = false;
              keyTag = state.tag;
              keyNode = state.result;
            } else if (detected) {
              throwError(state, "can not read an implicit mapping pair; a colon is missed");
            } else {
              state.tag = _tag;
              state.anchor = _anchor;
              return true;
            }
          } else if (detected) {
            throwError(state, "can not read a block mapping entry; a multiline key may not be an implicit key");
          } else {
            state.tag = _tag;
            state.anchor = _anchor;
            return true;
          }
        }
        if (state.line === _line || state.lineIndent > nodeIndent) {
          if (atExplicitKey) {
            _keyLine = state.line;
            _keyLineStart = state.lineStart;
            _keyPos = state.position;
          }
          if (composeNode2(state, nodeIndent, CONTEXT_BLOCK_OUT, true, allowCompact)) {
            if (atExplicitKey) {
              keyNode = state.result;
            } else {
              valueNode = state.result;
            }
          }
          if (!atExplicitKey) {
            storeMappingPair(state, _result, overridableKeys, keyTag, keyNode, valueNode, _keyLine, _keyLineStart, _keyPos);
            keyTag = keyNode = valueNode = null;
          }
          skipSeparationSpace(state, true, -1);
          ch = state.input.charCodeAt(state.position);
        }
        if ((state.line === _line || state.lineIndent > nodeIndent) && ch !== 0) {
          throwError(state, "bad indentation of a mapping entry");
        } else if (state.lineIndent < nodeIndent) {
          break;
        }
      }
      if (atExplicitKey) {
        storeMappingPair(state, _result, overridableKeys, keyTag, keyNode, null, _keyLine, _keyLineStart, _keyPos);
      }
      if (detected) {
        state.tag = _tag;
        state.anchor = _anchor;
        state.kind = "mapping";
        state.result = _result;
      }
      return detected;
    }
    function readTagProperty(state) {
      var _position, isVerbatim = false, isNamed = false, tagHandle, tagName, ch;
      ch = state.input.charCodeAt(state.position);
      if (ch !== 33) return false;
      if (state.tag !== null) {
        throwError(state, "duplication of a tag property");
      }
      ch = state.input.charCodeAt(++state.position);
      if (ch === 60) {
        isVerbatim = true;
        ch = state.input.charCodeAt(++state.position);
      } else if (ch === 33) {
        isNamed = true;
        tagHandle = "!!";
        ch = state.input.charCodeAt(++state.position);
      } else {
        tagHandle = "!";
      }
      _position = state.position;
      if (isVerbatim) {
        do {
          ch = state.input.charCodeAt(++state.position);
        } while (ch !== 0 && ch !== 62);
        if (state.position < state.length) {
          tagName = state.input.slice(_position, state.position);
          ch = state.input.charCodeAt(++state.position);
        } else {
          throwError(state, "unexpected end of the stream within a verbatim tag");
        }
      } else {
        while (ch !== 0 && !is_WS_OR_EOL(ch)) {
          if (ch === 33) {
            if (!isNamed) {
              tagHandle = state.input.slice(_position - 1, state.position + 1);
              if (!PATTERN_TAG_HANDLE.test(tagHandle)) {
                throwError(state, "named tag handle cannot contain such characters");
              }
              isNamed = true;
              _position = state.position + 1;
            } else {
              throwError(state, "tag suffix cannot contain exclamation marks");
            }
          }
          ch = state.input.charCodeAt(++state.position);
        }
        tagName = state.input.slice(_position, state.position);
        if (PATTERN_FLOW_INDICATORS.test(tagName)) {
          throwError(state, "tag suffix cannot contain flow indicator characters");
        }
      }
      if (tagName && !PATTERN_TAG_URI.test(tagName)) {
        throwError(state, "tag name cannot contain such characters: " + tagName);
      }
      try {
        tagName = decodeURIComponent(tagName);
      } catch (err) {
        throwError(state, "tag name is malformed: " + tagName);
      }
      if (isVerbatim) {
        state.tag = tagName;
      } else if (_hasOwnProperty.call(state.tagMap, tagHandle)) {
        state.tag = state.tagMap[tagHandle] + tagName;
      } else if (tagHandle === "!") {
        state.tag = "!" + tagName;
      } else if (tagHandle === "!!") {
        state.tag = "tag:yaml.org,2002:" + tagName;
      } else {
        throwError(state, 'undeclared tag handle "' + tagHandle + '"');
      }
      return true;
    }
    function readAnchorProperty(state) {
      var _position, ch;
      ch = state.input.charCodeAt(state.position);
      if (ch !== 38) return false;
      if (state.anchor !== null) {
        throwError(state, "duplication of an anchor property");
      }
      ch = state.input.charCodeAt(++state.position);
      _position = state.position;
      while (ch !== 0 && !is_WS_OR_EOL(ch) && !is_FLOW_INDICATOR(ch)) {
        ch = state.input.charCodeAt(++state.position);
      }
      if (state.position === _position) {
        throwError(state, "name of an anchor node must contain at least one character");
      }
      state.anchor = state.input.slice(_position, state.position);
      return true;
    }
    function readAlias(state) {
      var _position, alias, ch;
      ch = state.input.charCodeAt(state.position);
      if (ch !== 42) return false;
      ch = state.input.charCodeAt(++state.position);
      _position = state.position;
      while (ch !== 0 && !is_WS_OR_EOL(ch) && !is_FLOW_INDICATOR(ch)) {
        ch = state.input.charCodeAt(++state.position);
      }
      if (state.position === _position) {
        throwError(state, "name of an alias node must contain at least one character");
      }
      alias = state.input.slice(_position, state.position);
      if (!_hasOwnProperty.call(state.anchorMap, alias)) {
        throwError(state, 'unidentified alias "' + alias + '"');
      }
      state.result = state.anchorMap[alias];
      skipSeparationSpace(state, true, -1);
      return true;
    }
    function composeNode2(state, parentIndent, nodeContext, allowToSeek, allowCompact) {
      var allowBlockStyles, allowBlockScalars, allowBlockCollections, indentStatus = 1, atNewLine = false, hasContent = false, typeIndex, typeQuantity, typeList, type, flowIndent, blockIndent;
      if (state.listener !== null) {
        state.listener("open", state);
      }
      state.tag = null;
      state.anchor = null;
      state.kind = null;
      state.result = null;
      allowBlockStyles = allowBlockScalars = allowBlockCollections = CONTEXT_BLOCK_OUT === nodeContext || CONTEXT_BLOCK_IN === nodeContext;
      if (allowToSeek) {
        if (skipSeparationSpace(state, true, -1)) {
          atNewLine = true;
          if (state.lineIndent > parentIndent) {
            indentStatus = 1;
          } else if (state.lineIndent === parentIndent) {
            indentStatus = 0;
          } else if (state.lineIndent < parentIndent) {
            indentStatus = -1;
          }
        }
      }
      if (indentStatus === 1) {
        while (readTagProperty(state) || readAnchorProperty(state)) {
          if (skipSeparationSpace(state, true, -1)) {
            atNewLine = true;
            allowBlockCollections = allowBlockStyles;
            if (state.lineIndent > parentIndent) {
              indentStatus = 1;
            } else if (state.lineIndent === parentIndent) {
              indentStatus = 0;
            } else if (state.lineIndent < parentIndent) {
              indentStatus = -1;
            }
          } else {
            allowBlockCollections = false;
          }
        }
      }
      if (allowBlockCollections) {
        allowBlockCollections = atNewLine || allowCompact;
      }
      if (indentStatus === 1 || CONTEXT_BLOCK_OUT === nodeContext) {
        if (CONTEXT_FLOW_IN === nodeContext || CONTEXT_FLOW_OUT === nodeContext) {
          flowIndent = parentIndent;
        } else {
          flowIndent = parentIndent + 1;
        }
        blockIndent = state.position - state.lineStart;
        if (indentStatus === 1) {
          if (allowBlockCollections && (readBlockSequence(state, blockIndent) || readBlockMapping(state, blockIndent, flowIndent)) || readFlowCollection(state, flowIndent)) {
            hasContent = true;
          } else {
            if (allowBlockScalars && readBlockScalar(state, flowIndent) || readSingleQuotedScalar(state, flowIndent) || readDoubleQuotedScalar(state, flowIndent)) {
              hasContent = true;
            } else if (readAlias(state)) {
              hasContent = true;
              if (state.tag !== null || state.anchor !== null) {
                throwError(state, "alias node should not have any properties");
              }
            } else if (readPlainScalar(state, flowIndent, CONTEXT_FLOW_IN === nodeContext)) {
              hasContent = true;
              if (state.tag === null) {
                state.tag = "?";
              }
            }
            if (state.anchor !== null) {
              state.anchorMap[state.anchor] = state.result;
            }
          }
        } else if (indentStatus === 0) {
          hasContent = allowBlockCollections && readBlockSequence(state, blockIndent);
        }
      }
      if (state.tag === null) {
        if (state.anchor !== null) {
          state.anchorMap[state.anchor] = state.result;
        }
      } else if (state.tag === "?") {
        if (state.result !== null && state.kind !== "scalar") {
          throwError(state, 'unacceptable node kind for !<?> tag; it should be "scalar", not "' + state.kind + '"');
        }
        for (typeIndex = 0, typeQuantity = state.implicitTypes.length; typeIndex < typeQuantity; typeIndex += 1) {
          type = state.implicitTypes[typeIndex];
          if (type.resolve(state.result)) {
            state.result = type.construct(state.result);
            state.tag = type.tag;
            if (state.anchor !== null) {
              state.anchorMap[state.anchor] = state.result;
            }
            break;
          }
        }
      } else if (state.tag !== "!") {
        if (_hasOwnProperty.call(state.typeMap[state.kind || "fallback"], state.tag)) {
          type = state.typeMap[state.kind || "fallback"][state.tag];
        } else {
          type = null;
          typeList = state.typeMap.multi[state.kind || "fallback"];
          for (typeIndex = 0, typeQuantity = typeList.length; typeIndex < typeQuantity; typeIndex += 1) {
            if (state.tag.slice(0, typeList[typeIndex].tag.length) === typeList[typeIndex].tag) {
              type = typeList[typeIndex];
              break;
            }
          }
        }
        if (!type) {
          throwError(state, "unknown tag !<" + state.tag + ">");
        }
        if (state.result !== null && type.kind !== state.kind) {
          throwError(state, "unacceptable node kind for !<" + state.tag + '> tag; it should be "' + type.kind + '", not "' + state.kind + '"');
        }
        if (!type.resolve(state.result, state.tag)) {
          throwError(state, "cannot resolve a node with !<" + state.tag + "> explicit tag");
        } else {
          state.result = type.construct(state.result, state.tag);
          if (state.anchor !== null) {
            state.anchorMap[state.anchor] = state.result;
          }
        }
      }
      if (state.listener !== null) {
        state.listener("close", state);
      }
      return state.tag !== null || state.anchor !== null || hasContent;
    }
    function readDocument(state) {
      var documentStart = state.position, _position, directiveName, directiveArgs, hasDirectives = false, ch;
      state.version = null;
      state.checkLineBreaks = state.legacy;
      state.tagMap = /* @__PURE__ */ Object.create(null);
      state.anchorMap = /* @__PURE__ */ Object.create(null);
      while ((ch = state.input.charCodeAt(state.position)) !== 0) {
        skipSeparationSpace(state, true, -1);
        ch = state.input.charCodeAt(state.position);
        if (state.lineIndent > 0 || ch !== 37) {
          break;
        }
        hasDirectives = true;
        ch = state.input.charCodeAt(++state.position);
        _position = state.position;
        while (ch !== 0 && !is_WS_OR_EOL(ch)) {
          ch = state.input.charCodeAt(++state.position);
        }
        directiveName = state.input.slice(_position, state.position);
        directiveArgs = [];
        if (directiveName.length < 1) {
          throwError(state, "directive name must not be less than one character in length");
        }
        while (ch !== 0) {
          while (is_WHITE_SPACE(ch)) {
            ch = state.input.charCodeAt(++state.position);
          }
          if (ch === 35) {
            do {
              ch = state.input.charCodeAt(++state.position);
            } while (ch !== 0 && !is_EOL(ch));
            break;
          }
          if (is_EOL(ch)) break;
          _position = state.position;
          while (ch !== 0 && !is_WS_OR_EOL(ch)) {
            ch = state.input.charCodeAt(++state.position);
          }
          directiveArgs.push(state.input.slice(_position, state.position));
        }
        if (ch !== 0) readLineBreak(state);
        if (_hasOwnProperty.call(directiveHandlers, directiveName)) {
          directiveHandlers[directiveName](state, directiveName, directiveArgs);
        } else {
          throwWarning(state, 'unknown document directive "' + directiveName + '"');
        }
      }
      skipSeparationSpace(state, true, -1);
      if (state.lineIndent === 0 && state.input.charCodeAt(state.position) === 45 && state.input.charCodeAt(state.position + 1) === 45 && state.input.charCodeAt(state.position + 2) === 45) {
        state.position += 3;
        skipSeparationSpace(state, true, -1);
      } else if (hasDirectives) {
        throwError(state, "directives end mark is expected");
      }
      composeNode2(state, state.lineIndent - 1, CONTEXT_BLOCK_OUT, false, true);
      skipSeparationSpace(state, true, -1);
      if (state.checkLineBreaks && PATTERN_NON_ASCII_LINE_BREAKS.test(state.input.slice(documentStart, state.position))) {
        throwWarning(state, "non-ASCII line breaks are interpreted as content");
      }
      state.documents.push(state.result);
      if (state.position === state.lineStart && testDocumentSeparator(state)) {
        if (state.input.charCodeAt(state.position) === 46) {
          state.position += 3;
          skipSeparationSpace(state, true, -1);
        }
        return;
      }
      if (state.position < state.length - 1) {
        throwError(state, "end of the stream or a document separator is expected");
      } else {
        return;
      }
    }
    function loadDocuments(input, options) {
      input = String(input);
      options = options || {};
      if (input.length !== 0) {
        if (input.charCodeAt(input.length - 1) !== 10 && input.charCodeAt(input.length - 1) !== 13) {
          input += "\n";
        }
        if (input.charCodeAt(0) === 65279) {
          input = input.slice(1);
        }
      }
      var state = new State(input, options);
      var nullpos = input.indexOf("\0");
      if (nullpos !== -1) {
        state.position = nullpos;
        throwError(state, "null byte is not allowed in input");
      }
      state.input += "\0";
      while (state.input.charCodeAt(state.position) === 32) {
        state.lineIndent += 1;
        state.position += 1;
      }
      while (state.position < state.length - 1) {
        readDocument(state);
      }
      return state.documents;
    }
    function loadAll(input, iterator, options) {
      if (iterator !== null && typeof iterator === "object" && typeof options === "undefined") {
        options = iterator;
        iterator = null;
      }
      var documents = loadDocuments(input, options);
      if (typeof iterator !== "function") {
        return documents;
      }
      for (var index = 0, length2 = documents.length; index < length2; index += 1) {
        iterator(documents[index]);
      }
    }
    function load(input, options) {
      var documents = loadDocuments(input, options);
      if (documents.length === 0) {
        return void 0;
      } else if (documents.length === 1) {
        return documents[0];
      }
      throw new YAMLException("expected a single document in the stream, but found more");
    }
    module.exports.loadAll = loadAll;
    module.exports.load = load;
  }
});

// node_modules/js-yaml/lib/dumper.js
var require_dumper = __commonJS({
  "node_modules/js-yaml/lib/dumper.js"(exports, module) {
    "use strict";
    var common = require_common();
    var YAMLException = require_exception();
    var DEFAULT_SCHEMA = require_default();
    var _toString = Object.prototype.toString;
    var _hasOwnProperty = Object.prototype.hasOwnProperty;
    var CHAR_BOM = 65279;
    var CHAR_TAB = 9;
    var CHAR_LINE_FEED = 10;
    var CHAR_CARRIAGE_RETURN = 13;
    var CHAR_SPACE = 32;
    var CHAR_EXCLAMATION = 33;
    var CHAR_DOUBLE_QUOTE = 34;
    var CHAR_SHARP = 35;
    var CHAR_PERCENT = 37;
    var CHAR_AMPERSAND = 38;
    var CHAR_SINGLE_QUOTE = 39;
    var CHAR_ASTERISK = 42;
    var CHAR_COMMA = 44;
    var CHAR_MINUS = 45;
    var CHAR_COLON = 58;
    var CHAR_EQUALS = 61;
    var CHAR_GREATER_THAN = 62;
    var CHAR_QUESTION = 63;
    var CHAR_COMMERCIAL_AT = 64;
    var CHAR_LEFT_SQUARE_BRACKET = 91;
    var CHAR_RIGHT_SQUARE_BRACKET = 93;
    var CHAR_GRAVE_ACCENT = 96;
    var CHAR_LEFT_CURLY_BRACKET = 123;
    var CHAR_VERTICAL_LINE = 124;
    var CHAR_RIGHT_CURLY_BRACKET = 125;
    var ESCAPE_SEQUENCES = {};
    ESCAPE_SEQUENCES[0] = "\\0";
    ESCAPE_SEQUENCES[7] = "\\a";
    ESCAPE_SEQUENCES[8] = "\\b";
    ESCAPE_SEQUENCES[9] = "\\t";
    ESCAPE_SEQUENCES[10] = "\\n";
    ESCAPE_SEQUENCES[11] = "\\v";
    ESCAPE_SEQUENCES[12] = "\\f";
    ESCAPE_SEQUENCES[13] = "\\r";
    ESCAPE_SEQUENCES[27] = "\\e";
    ESCAPE_SEQUENCES[34] = '\\"';
    ESCAPE_SEQUENCES[92] = "\\\\";
    ESCAPE_SEQUENCES[133] = "\\N";
    ESCAPE_SEQUENCES[160] = "\\_";
    ESCAPE_SEQUENCES[8232] = "\\L";
    ESCAPE_SEQUENCES[8233] = "\\P";
    var DEPRECATED_BOOLEANS_SYNTAX = [
      "y",
      "Y",
      "yes",
      "Yes",
      "YES",
      "on",
      "On",
      "ON",
      "n",
      "N",
      "no",
      "No",
      "NO",
      "off",
      "Off",
      "OFF"
    ];
    var DEPRECATED_BASE60_SYNTAX = /^[-+]?[0-9_]+(?::[0-9_]+)+(?:\.[0-9_]*)?$/;
    function compileStyleMap(schema4, map3) {
      var result, keys2, index, length2, tag, style, type;
      if (map3 === null) return {};
      result = {};
      keys2 = Object.keys(map3);
      for (index = 0, length2 = keys2.length; index < length2; index += 1) {
        tag = keys2[index];
        style = String(map3[tag]);
        if (tag.slice(0, 2) === "!!") {
          tag = "tag:yaml.org,2002:" + tag.slice(2);
        }
        type = schema4.compiledTypeMap["fallback"][tag];
        if (type && _hasOwnProperty.call(type.styleAliases, style)) {
          style = type.styleAliases[style];
        }
        result[tag] = style;
      }
      return result;
    }
    function encodeHex(character2) {
      var string2, handle, length2;
      string2 = character2.toString(16).toUpperCase();
      if (character2 <= 255) {
        handle = "x";
        length2 = 2;
      } else if (character2 <= 65535) {
        handle = "u";
        length2 = 4;
      } else if (character2 <= 4294967295) {
        handle = "U";
        length2 = 8;
      } else {
        throw new YAMLException("code point within a string may not be greater than 0xFFFFFFFF");
      }
      return "\\" + handle + common.repeat("0", length2 - string2.length) + string2;
    }
    var QUOTING_TYPE_SINGLE = 1;
    var QUOTING_TYPE_DOUBLE = 2;
    function State(options) {
      this.schema = options["schema"] || DEFAULT_SCHEMA;
      this.indent = Math.max(1, options["indent"] || 2);
      this.noArrayIndent = options["noArrayIndent"] || false;
      this.skipInvalid = options["skipInvalid"] || false;
      this.flowLevel = common.isNothing(options["flowLevel"]) ? -1 : options["flowLevel"];
      this.styleMap = compileStyleMap(this.schema, options["styles"] || null);
      this.sortKeys = options["sortKeys"] || false;
      this.lineWidth = options["lineWidth"] || 80;
      this.noRefs = options["noRefs"] || false;
      this.noCompatMode = options["noCompatMode"] || false;
      this.condenseFlow = options["condenseFlow"] || false;
      this.quotingType = options["quotingType"] === '"' ? QUOTING_TYPE_DOUBLE : QUOTING_TYPE_SINGLE;
      this.forceQuotes = options["forceQuotes"] || false;
      this.replacer = typeof options["replacer"] === "function" ? options["replacer"] : null;
      this.implicitTypes = this.schema.compiledImplicit;
      this.explicitTypes = this.schema.compiledExplicit;
      this.tag = null;
      this.result = "";
      this.duplicates = [];
      this.usedDuplicates = null;
    }
    function indentString(string2, spaces) {
      var ind = common.repeat(" ", spaces), position3 = 0, next2 = -1, result = "", line2, length2 = string2.length;
      while (position3 < length2) {
        next2 = string2.indexOf("\n", position3);
        if (next2 === -1) {
          line2 = string2.slice(position3);
          position3 = length2;
        } else {
          line2 = string2.slice(position3, next2 + 1);
          position3 = next2 + 1;
        }
        if (line2.length && line2 !== "\n") result += ind;
        result += line2;
      }
      return result;
    }
    function generateNextLine(state, level) {
      return "\n" + common.repeat(" ", state.indent * level);
    }
    function testImplicitResolving(state, str) {
      var index, length2, type;
      for (index = 0, length2 = state.implicitTypes.length; index < length2; index += 1) {
        type = state.implicitTypes[index];
        if (type.resolve(str)) {
          return true;
        }
      }
      return false;
    }
    function isWhitespace(c2) {
      return c2 === CHAR_SPACE || c2 === CHAR_TAB;
    }
    function isPrintable(c2) {
      return 32 <= c2 && c2 <= 126 || 161 <= c2 && c2 <= 55295 && c2 !== 8232 && c2 !== 8233 || 57344 <= c2 && c2 <= 65533 && c2 !== CHAR_BOM || 65536 <= c2 && c2 <= 1114111;
    }
    function isNsCharOrWhitespace(c2) {
      return isPrintable(c2) && c2 !== CHAR_BOM && c2 !== CHAR_CARRIAGE_RETURN && c2 !== CHAR_LINE_FEED;
    }
    function isPlainSafe(c2, prev2, inblock) {
      var cIsNsCharOrWhitespace = isNsCharOrWhitespace(c2);
      var cIsNsChar = cIsNsCharOrWhitespace && !isWhitespace(c2);
      return (
        // ns-plain-safe
        (inblock ? (
          // c = flow-in
          cIsNsCharOrWhitespace
        ) : cIsNsCharOrWhitespace && c2 !== CHAR_COMMA && c2 !== CHAR_LEFT_SQUARE_BRACKET && c2 !== CHAR_RIGHT_SQUARE_BRACKET && c2 !== CHAR_LEFT_CURLY_BRACKET && c2 !== CHAR_RIGHT_CURLY_BRACKET) && c2 !== CHAR_SHARP && !(prev2 === CHAR_COLON && !cIsNsChar) || isNsCharOrWhitespace(prev2) && !isWhitespace(prev2) && c2 === CHAR_SHARP || prev2 === CHAR_COLON && cIsNsChar
      );
    }
    function isPlainSafeFirst(c2) {
      return isPrintable(c2) && c2 !== CHAR_BOM && !isWhitespace(c2) && c2 !== CHAR_MINUS && c2 !== CHAR_QUESTION && c2 !== CHAR_COLON && c2 !== CHAR_COMMA && c2 !== CHAR_LEFT_SQUARE_BRACKET && c2 !== CHAR_RIGHT_SQUARE_BRACKET && c2 !== CHAR_LEFT_CURLY_BRACKET && c2 !== CHAR_RIGHT_CURLY_BRACKET && c2 !== CHAR_SHARP && c2 !== CHAR_AMPERSAND && c2 !== CHAR_ASTERISK && c2 !== CHAR_EXCLAMATION && c2 !== CHAR_VERTICAL_LINE && c2 !== CHAR_EQUALS && c2 !== CHAR_GREATER_THAN && c2 !== CHAR_SINGLE_QUOTE && c2 !== CHAR_DOUBLE_QUOTE && c2 !== CHAR_PERCENT && c2 !== CHAR_COMMERCIAL_AT && c2 !== CHAR_GRAVE_ACCENT;
    }
    function isPlainSafeLast(c2) {
      return !isWhitespace(c2) && c2 !== CHAR_COLON;
    }
    function codePointAt(string2, pos) {
      var first = string2.charCodeAt(pos), second;
      if (first >= 55296 && first <= 56319 && pos + 1 < string2.length) {
        second = string2.charCodeAt(pos + 1);
        if (second >= 56320 && second <= 57343) {
          return (first - 55296) * 1024 + second - 56320 + 65536;
        }
      }
      return first;
    }
    function needIndentIndicator(string2) {
      var leadingSpaceRe = /^\n* /;
      return leadingSpaceRe.test(string2);
    }
    var STYLE_PLAIN = 1;
    var STYLE_SINGLE = 2;
    var STYLE_LITERAL = 3;
    var STYLE_FOLDED = 4;
    var STYLE_DOUBLE = 5;
    function chooseScalarStyle(string2, singleLineOnly, indentPerLevel, lineWidth, testAmbiguousType, quotingType, forceQuotes, inblock) {
      var i2;
      var char2 = 0;
      var prevChar = null;
      var hasLineBreak = false;
      var hasFoldableLine = false;
      var shouldTrackWidth = lineWidth !== -1;
      var previousLineBreak = -1;
      var plain = isPlainSafeFirst(codePointAt(string2, 0)) && isPlainSafeLast(codePointAt(string2, string2.length - 1));
      if (singleLineOnly || forceQuotes) {
        for (i2 = 0; i2 < string2.length; char2 >= 65536 ? i2 += 2 : i2++) {
          char2 = codePointAt(string2, i2);
          if (!isPrintable(char2)) {
            return STYLE_DOUBLE;
          }
          plain = plain && isPlainSafe(char2, prevChar, inblock);
          prevChar = char2;
        }
      } else {
        for (i2 = 0; i2 < string2.length; char2 >= 65536 ? i2 += 2 : i2++) {
          char2 = codePointAt(string2, i2);
          if (char2 === CHAR_LINE_FEED) {
            hasLineBreak = true;
            if (shouldTrackWidth) {
              hasFoldableLine = hasFoldableLine || // Foldable line = too long, and not more-indented.
              i2 - previousLineBreak - 1 > lineWidth && string2[previousLineBreak + 1] !== " ";
              previousLineBreak = i2;
            }
          } else if (!isPrintable(char2)) {
            return STYLE_DOUBLE;
          }
          plain = plain && isPlainSafe(char2, prevChar, inblock);
          prevChar = char2;
        }
        hasFoldableLine = hasFoldableLine || shouldTrackWidth && (i2 - previousLineBreak - 1 > lineWidth && string2[previousLineBreak + 1] !== " ");
      }
      if (!hasLineBreak && !hasFoldableLine) {
        if (plain && !forceQuotes && !testAmbiguousType(string2)) {
          return STYLE_PLAIN;
        }
        return quotingType === QUOTING_TYPE_DOUBLE ? STYLE_DOUBLE : STYLE_SINGLE;
      }
      if (indentPerLevel > 9 && needIndentIndicator(string2)) {
        return STYLE_DOUBLE;
      }
      if (!forceQuotes) {
        return hasFoldableLine ? STYLE_FOLDED : STYLE_LITERAL;
      }
      return quotingType === QUOTING_TYPE_DOUBLE ? STYLE_DOUBLE : STYLE_SINGLE;
    }
    function writeScalar(state, string2, level, iskey, inblock) {
      state.dump = (function() {
        if (string2.length === 0) {
          return state.quotingType === QUOTING_TYPE_DOUBLE ? '""' : "''";
        }
        if (!state.noCompatMode) {
          if (DEPRECATED_BOOLEANS_SYNTAX.indexOf(string2) !== -1 || DEPRECATED_BASE60_SYNTAX.test(string2)) {
            return state.quotingType === QUOTING_TYPE_DOUBLE ? '"' + string2 + '"' : "'" + string2 + "'";
          }
        }
        var indent = state.indent * Math.max(1, level);
        var lineWidth = state.lineWidth === -1 ? -1 : Math.max(Math.min(state.lineWidth, 40), state.lineWidth - indent);
        var singleLineOnly = iskey || state.flowLevel > -1 && level >= state.flowLevel;
        function testAmbiguity(string3) {
          return testImplicitResolving(state, string3);
        }
        switch (chooseScalarStyle(
          string2,
          singleLineOnly,
          state.indent,
          lineWidth,
          testAmbiguity,
          state.quotingType,
          state.forceQuotes && !iskey,
          inblock
        )) {
          case STYLE_PLAIN:
            return string2;
          case STYLE_SINGLE:
            return "'" + string2.replace(/'/g, "''") + "'";
          case STYLE_LITERAL:
            return "|" + blockHeader(string2, state.indent) + dropEndingNewline(indentString(string2, indent));
          case STYLE_FOLDED:
            return ">" + blockHeader(string2, state.indent) + dropEndingNewline(indentString(foldString(string2, lineWidth), indent));
          case STYLE_DOUBLE:
            return '"' + escapeString(string2, lineWidth) + '"';
          default:
            throw new YAMLException("impossible error: invalid scalar style");
        }
      })();
    }
    function blockHeader(string2, indentPerLevel) {
      var indentIndicator = needIndentIndicator(string2) ? String(indentPerLevel) : "";
      var clip = string2[string2.length - 1] === "\n";
      var keep = clip && (string2[string2.length - 2] === "\n" || string2 === "\n");
      var chomp = keep ? "+" : clip ? "" : "-";
      return indentIndicator + chomp + "\n";
    }
    function dropEndingNewline(string2) {
      return string2[string2.length - 1] === "\n" ? string2.slice(0, -1) : string2;
    }
    function foldString(string2, width) {
      var lineRe = /(\n+)([^\n]*)/g;
      var result = (function() {
        var nextLF = string2.indexOf("\n");
        nextLF = nextLF !== -1 ? nextLF : string2.length;
        lineRe.lastIndex = nextLF;
        return foldLine(string2.slice(0, nextLF), width);
      })();
      var prevMoreIndented = string2[0] === "\n" || string2[0] === " ";
      var moreIndented;
      var match2;
      while (match2 = lineRe.exec(string2)) {
        var prefix2 = match2[1], line2 = match2[2];
        moreIndented = line2[0] === " ";
        result += prefix2 + (!prevMoreIndented && !moreIndented && line2 !== "" ? "\n" : "") + foldLine(line2, width);
        prevMoreIndented = moreIndented;
      }
      return result;
    }
    function foldLine(line2, width) {
      if (line2 === "" || line2[0] === " ") return line2;
      var breakRe = / [^ ]/g;
      var match2;
      var start = 0, end, curr = 0, next2 = 0;
      var result = "";
      while (match2 = breakRe.exec(line2)) {
        next2 = match2.index;
        if (next2 - start > width) {
          end = curr > start ? curr : next2;
          result += "\n" + line2.slice(start, end);
          start = end + 1;
        }
        curr = next2;
      }
      result += "\n";
      if (line2.length - start > width && curr > start) {
        result += line2.slice(start, curr) + "\n" + line2.slice(curr + 1);
      } else {
        result += line2.slice(start);
      }
      return result.slice(1);
    }
    function escapeString(string2) {
      var result = "";
      var char2 = 0;
      var escapeSeq;
      for (var i2 = 0; i2 < string2.length; char2 >= 65536 ? i2 += 2 : i2++) {
        char2 = codePointAt(string2, i2);
        escapeSeq = ESCAPE_SEQUENCES[char2];
        if (!escapeSeq && isPrintable(char2)) {
          result += string2[i2];
          if (char2 >= 65536) result += string2[i2 + 1];
        } else {
          result += escapeSeq || encodeHex(char2);
        }
      }
      return result;
    }
    function writeFlowSequence(state, level, object2) {
      var _result = "", _tag = state.tag, index, length2, value;
      for (index = 0, length2 = object2.length; index < length2; index += 1) {
        value = object2[index];
        if (state.replacer) {
          value = state.replacer.call(object2, String(index), value);
        }
        if (writeNode(state, level, value, false, false) || typeof value === "undefined" && writeNode(state, level, null, false, false)) {
          if (_result !== "") _result += "," + (!state.condenseFlow ? " " : "");
          _result += state.dump;
        }
      }
      state.tag = _tag;
      state.dump = "[" + _result + "]";
    }
    function writeBlockSequence(state, level, object2, compact) {
      var _result = "", _tag = state.tag, index, length2, value;
      for (index = 0, length2 = object2.length; index < length2; index += 1) {
        value = object2[index];
        if (state.replacer) {
          value = state.replacer.call(object2, String(index), value);
        }
        if (writeNode(state, level + 1, value, true, true, false, true) || typeof value === "undefined" && writeNode(state, level + 1, null, true, true, false, true)) {
          if (!compact || _result !== "") {
            _result += generateNextLine(state, level);
          }
          if (state.dump && CHAR_LINE_FEED === state.dump.charCodeAt(0)) {
            _result += "-";
          } else {
            _result += "- ";
          }
          _result += state.dump;
        }
      }
      state.tag = _tag;
      state.dump = _result || "[]";
    }
    function writeFlowMapping(state, level, object2) {
      var _result = "", _tag = state.tag, objectKeyList = Object.keys(object2), index, length2, objectKey, objectValue, pairBuffer;
      for (index = 0, length2 = objectKeyList.length; index < length2; index += 1) {
        pairBuffer = "";
        if (_result !== "") pairBuffer += ", ";
        if (state.condenseFlow) pairBuffer += '"';
        objectKey = objectKeyList[index];
        objectValue = object2[objectKey];
        if (state.replacer) {
          objectValue = state.replacer.call(object2, objectKey, objectValue);
        }
        if (!writeNode(state, level, objectKey, false, false)) {
          continue;
        }
        if (state.dump.length > 1024) pairBuffer += "? ";
        pairBuffer += state.dump + (state.condenseFlow ? '"' : "") + ":" + (state.condenseFlow ? "" : " ");
        if (!writeNode(state, level, objectValue, false, false)) {
          continue;
        }
        pairBuffer += state.dump;
        _result += pairBuffer;
      }
      state.tag = _tag;
      state.dump = "{" + _result + "}";
    }
    function writeBlockMapping(state, level, object2, compact) {
      var _result = "", _tag = state.tag, objectKeyList = Object.keys(object2), index, length2, objectKey, objectValue, explicitPair, pairBuffer;
      if (state.sortKeys === true) {
        objectKeyList.sort();
      } else if (typeof state.sortKeys === "function") {
        objectKeyList.sort(state.sortKeys);
      } else if (state.sortKeys) {
        throw new YAMLException("sortKeys must be a boolean or a function");
      }
      for (index = 0, length2 = objectKeyList.length; index < length2; index += 1) {
        pairBuffer = "";
        if (!compact || _result !== "") {
          pairBuffer += generateNextLine(state, level);
        }
        objectKey = objectKeyList[index];
        objectValue = object2[objectKey];
        if (state.replacer) {
          objectValue = state.replacer.call(object2, objectKey, objectValue);
        }
        if (!writeNode(state, level + 1, objectKey, true, true, true)) {
          continue;
        }
        explicitPair = state.tag !== null && state.tag !== "?" || state.dump && state.dump.length > 1024;
        if (explicitPair) {
          if (state.dump && CHAR_LINE_FEED === state.dump.charCodeAt(0)) {
            pairBuffer += "?";
          } else {
            pairBuffer += "? ";
          }
        }
        pairBuffer += state.dump;
        if (explicitPair) {
          pairBuffer += generateNextLine(state, level);
        }
        if (!writeNode(state, level + 1, objectValue, true, explicitPair)) {
          continue;
        }
        if (state.dump && CHAR_LINE_FEED === state.dump.charCodeAt(0)) {
          pairBuffer += ":";
        } else {
          pairBuffer += ": ";
        }
        pairBuffer += state.dump;
        _result += pairBuffer;
      }
      state.tag = _tag;
      state.dump = _result || "{}";
    }
    function detectType(state, object2, explicit) {
      var _result, typeList, index, length2, type, style;
      typeList = explicit ? state.explicitTypes : state.implicitTypes;
      for (index = 0, length2 = typeList.length; index < length2; index += 1) {
        type = typeList[index];
        if ((type.instanceOf || type.predicate) && (!type.instanceOf || typeof object2 === "object" && object2 instanceof type.instanceOf) && (!type.predicate || type.predicate(object2))) {
          if (explicit) {
            if (type.multi && type.representName) {
              state.tag = type.representName(object2);
            } else {
              state.tag = type.tag;
            }
          } else {
            state.tag = "?";
          }
          if (type.represent) {
            style = state.styleMap[type.tag] || type.defaultStyle;
            if (_toString.call(type.represent) === "[object Function]") {
              _result = type.represent(object2, style);
            } else if (_hasOwnProperty.call(type.represent, style)) {
              _result = type.represent[style](object2, style);
            } else {
              throw new YAMLException("!<" + type.tag + '> tag resolver accepts not "' + style + '" style');
            }
            state.dump = _result;
          }
          return true;
        }
      }
      return false;
    }
    function writeNode(state, level, object2, block, compact, iskey, isblockseq) {
      state.tag = null;
      state.dump = object2;
      if (!detectType(state, object2, false)) {
        detectType(state, object2, true);
      }
      var type = _toString.call(state.dump);
      var inblock = block;
      var tagStr;
      if (block) {
        block = state.flowLevel < 0 || state.flowLevel > level;
      }
      var objectOrArray = type === "[object Object]" || type === "[object Array]", duplicateIndex, duplicate;
      if (objectOrArray) {
        duplicateIndex = state.duplicates.indexOf(object2);
        duplicate = duplicateIndex !== -1;
      }
      if (state.tag !== null && state.tag !== "?" || duplicate || state.indent !== 2 && level > 0) {
        compact = false;
      }
      if (duplicate && state.usedDuplicates[duplicateIndex]) {
        state.dump = "*ref_" + duplicateIndex;
      } else {
        if (objectOrArray && duplicate && !state.usedDuplicates[duplicateIndex]) {
          state.usedDuplicates[duplicateIndex] = true;
        }
        if (type === "[object Object]") {
          if (block && Object.keys(state.dump).length !== 0) {
            writeBlockMapping(state, level, state.dump, compact);
            if (duplicate) {
              state.dump = "&ref_" + duplicateIndex + state.dump;
            }
          } else {
            writeFlowMapping(state, level, state.dump);
            if (duplicate) {
              state.dump = "&ref_" + duplicateIndex + " " + state.dump;
            }
          }
        } else if (type === "[object Array]") {
          if (block && state.dump.length !== 0) {
            if (state.noArrayIndent && !isblockseq && level > 0) {
              writeBlockSequence(state, level - 1, state.dump, compact);
            } else {
              writeBlockSequence(state, level, state.dump, compact);
            }
            if (duplicate) {
              state.dump = "&ref_" + duplicateIndex + state.dump;
            }
          } else {
            writeFlowSequence(state, level, state.dump);
            if (duplicate) {
              state.dump = "&ref_" + duplicateIndex + " " + state.dump;
            }
          }
        } else if (type === "[object String]") {
          if (state.tag !== "?") {
            writeScalar(state, state.dump, level, iskey, inblock);
          }
        } else if (type === "[object Undefined]") {
          return false;
        } else {
          if (state.skipInvalid) return false;
          throw new YAMLException("unacceptable kind of an object to dump " + type);
        }
        if (state.tag !== null && state.tag !== "?") {
          tagStr = encodeURI(
            state.tag[0] === "!" ? state.tag.slice(1) : state.tag
          ).replace(/!/g, "%21");
          if (state.tag[0] === "!") {
            tagStr = "!" + tagStr;
          } else if (tagStr.slice(0, 18) === "tag:yaml.org,2002:") {
            tagStr = "!!" + tagStr.slice(18);
          } else {
            tagStr = "!<" + tagStr + ">";
          }
          state.dump = tagStr + " " + state.dump;
        }
      }
      return true;
    }
    function getDuplicateReferences(object2, state) {
      var objects = [], duplicatesIndexes = [], index, length2;
      inspectNode(object2, objects, duplicatesIndexes);
      for (index = 0, length2 = duplicatesIndexes.length; index < length2; index += 1) {
        state.duplicates.push(objects[duplicatesIndexes[index]]);
      }
      state.usedDuplicates = new Array(length2);
    }
    function inspectNode(object2, objects, duplicatesIndexes) {
      var objectKeyList, index, length2;
      if (object2 !== null && typeof object2 === "object") {
        index = objects.indexOf(object2);
        if (index !== -1) {
          if (duplicatesIndexes.indexOf(index) === -1) {
            duplicatesIndexes.push(index);
          }
        } else {
          objects.push(object2);
          if (Array.isArray(object2)) {
            for (index = 0, length2 = object2.length; index < length2; index += 1) {
              inspectNode(object2[index], objects, duplicatesIndexes);
            }
          } else {
            objectKeyList = Object.keys(object2);
            for (index = 0, length2 = objectKeyList.length; index < length2; index += 1) {
              inspectNode(object2[objectKeyList[index]], objects, duplicatesIndexes);
            }
          }
        }
      }
    }
    function dump(input, options) {
      options = options || {};
      var state = new State(options);
      if (!state.noRefs) getDuplicateReferences(input, state);
      var value = input;
      if (state.replacer) {
        value = state.replacer.call({ "": value }, "", value);
      }
      if (writeNode(state, 0, value, true, true)) return state.dump + "\n";
      return "";
    }
    module.exports.dump = dump;
  }
});

// node_modules/js-yaml/index.js
var require_js_yaml = __commonJS({
  "node_modules/js-yaml/index.js"(exports, module) {
    "use strict";
    var loader = require_loader();
    var dumper = require_dumper();
    function renamed(from2, to) {
      return function() {
        throw new Error("Function yaml." + from2 + " is removed in js-yaml 4. Use yaml." + to + " instead, which is now safe by default.");
      };
    }
    module.exports.Type = require_type();
    module.exports.Schema = require_schema();
    module.exports.FAILSAFE_SCHEMA = require_failsafe();
    module.exports.JSON_SCHEMA = require_json();
    module.exports.CORE_SCHEMA = require_core();
    module.exports.DEFAULT_SCHEMA = require_default();
    module.exports.load = loader.load;
    module.exports.loadAll = loader.loadAll;
    module.exports.dump = dumper.dump;
    module.exports.YAMLException = require_exception();
    module.exports.types = {
      binary: require_binary(),
      float: require_float(),
      map: require_map(),
      null: require_null(),
      pairs: require_pairs(),
      set: require_set(),
      timestamp: require_timestamp(),
      bool: require_bool(),
      int: require_int(),
      merge: require_merge(),
      omap: require_omap(),
      seq: require_seq(),
      str: require_str()
    };
    module.exports.safeLoad = renamed("safeLoad", "load");
    module.exports.safeLoadAll = renamed("safeLoadAll", "loadAll");
    module.exports.safeDump = renamed("safeDump", "dump");
  }
});

// node_modules/@redocly/openapi-core/lib/js-yaml/index.js
var require_js_yaml2 = __commonJS({
  "node_modules/@redocly/openapi-core/lib/js-yaml/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.stringifyYaml = exports.parseYaml = void 0;
    var js_yaml_1 = require_js_yaml();
    var DEFAULT_SCHEMA_WITHOUT_TIMESTAMP = js_yaml_1.JSON_SCHEMA.extend({
      implicit: [js_yaml_1.types.merge],
      explicit: [js_yaml_1.types.binary, js_yaml_1.types.omap, js_yaml_1.types.pairs, js_yaml_1.types.set]
    });
    var parseYaml = (str, opts) => (0, js_yaml_1.load)(str, { schema: DEFAULT_SCHEMA_WITHOUT_TIMESTAMP, ...opts });
    exports.parseYaml = parseYaml;
    var stringifyYaml = (obj, opts) => (0, js_yaml_1.dump)(obj, opts);
    exports.stringifyYaml = stringifyYaml;
  }
});

// node_modules/@redocly/openapi-core/lib/env.js
var require_env = __commonJS({
  "node_modules/@redocly/openapi-core/lib/env.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.env = exports.isBrowser = void 0;
    exports.isBrowser = typeof window !== "undefined" || typeof process === "undefined" || process?.platform === "browser";
    exports.env = exports.isBrowser ? {} : process.env || {};
  }
});

// browser-external:colorette
var require_colorette = __commonJS({
  "browser-external:colorette"(exports, module) {
    module.exports = Object.create(new Proxy({}, {
      get(_15, key2) {
        if (key2 !== "__esModule" && key2 !== "__proto__" && key2 !== "constructor" && key2 !== "splice") {
          console.warn(`Module "colorette" has been externalized for browser compatibility. Cannot access "colorette.${key2}" in client code. See https://vite.dev/guide/troubleshooting.html#module-externalized-for-browser-compatibility for more details.`);
        }
      }
    }));
  }
});

// node_modules/@redocly/openapi-core/lib/logger.js
var require_logger = __commonJS({
  "node_modules/@redocly/openapi-core/lib/logger.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.logger = exports.colorize = exports.colorOptions = void 0;
    var colorette = require_colorette();
    var env_1 = require_env();
    var utils_1 = require_utils();
    exports.colorOptions = colorette.options;
    exports.colorize = new Proxy(colorette, {
      get(target, prop) {
        if (env_1.isBrowser) {
          return utils_1.identity;
        }
        return target[prop];
      }
    });
    var Logger = class {
      stderr(str) {
        return process.stderr.write(str);
      }
      info(str) {
        return env_1.isBrowser ? console.log(str) : this.stderr(str);
      }
      warn(str) {
        return env_1.isBrowser ? console.warn(str) : this.stderr(exports.colorize.yellow(str));
      }
      error(str) {
        return env_1.isBrowser ? console.error(str) : this.stderr(exports.colorize.red(str));
      }
    };
    exports.logger = new Logger();
  }
});

// browser-external:https-proxy-agent
var require_https_proxy_agent = __commonJS({
  "browser-external:https-proxy-agent"(exports, module) {
    module.exports = Object.create(new Proxy({}, {
      get(_15, key2) {
        if (key2 !== "__esModule" && key2 !== "__proto__" && key2 !== "constructor" && key2 !== "splice") {
          console.warn(`Module "https-proxy-agent" has been externalized for browser compatibility. Cannot access "https-proxy-agent.${key2}" in client code. See https://vite.dev/guide/troubleshooting.html#module-externalized-for-browser-compatibility for more details.`);
        }
      }
    }));
  }
});

// node_modules/pluralize/pluralize.js
var require_pluralize = __commonJS({
  "node_modules/pluralize/pluralize.js"(exports, module) {
    (function(root, pluralize) {
      if (typeof __require === "function" && typeof exports === "object" && typeof module === "object") {
        module.exports = pluralize();
      } else if (typeof define === "function" && define.amd) {
        define(function() {
          return pluralize();
        });
      } else {
        root.pluralize = pluralize();
      }
    })(exports, function() {
      var pluralRules = [];
      var singularRules = [];
      var uncountables = {};
      var irregularPlurals = {};
      var irregularSingles = {};
      function sanitizeRule(rule) {
        if (typeof rule === "string") {
          return new RegExp("^" + rule + "$", "i");
        }
        return rule;
      }
      function restoreCase(word, token2) {
        if (word === token2) return token2;
        if (word === word.toLowerCase()) return token2.toLowerCase();
        if (word === word.toUpperCase()) return token2.toUpperCase();
        if (word[0] === word[0].toUpperCase()) {
          return token2.charAt(0).toUpperCase() + token2.substr(1).toLowerCase();
        }
        return token2.toLowerCase();
      }
      function interpolate(str, args) {
        return str.replace(/\$(\d{1,2})/g, function(match2, index) {
          return args[index] || "";
        });
      }
      function replace3(word, rule) {
        return word.replace(rule[0], function(match2, index) {
          var result = interpolate(rule[1], arguments);
          if (match2 === "") {
            return restoreCase(word[index - 1], result);
          }
          return restoreCase(match2, result);
        });
      }
      function sanitizeWord(token2, word, rules) {
        if (!token2.length || uncountables.hasOwnProperty(token2)) {
          return word;
        }
        var len = rules.length;
        while (len--) {
          var rule = rules[len];
          if (rule[0].test(word)) return replace3(word, rule);
        }
        return word;
      }
      function replaceWord(replaceMap, keepMap, rules) {
        return function(word) {
          var token2 = word.toLowerCase();
          if (keepMap.hasOwnProperty(token2)) {
            return restoreCase(word, token2);
          }
          if (replaceMap.hasOwnProperty(token2)) {
            return restoreCase(word, replaceMap[token2]);
          }
          return sanitizeWord(token2, word, rules);
        };
      }
      function checkWord(replaceMap, keepMap, rules, bool) {
        return function(word) {
          var token2 = word.toLowerCase();
          if (keepMap.hasOwnProperty(token2)) return true;
          if (replaceMap.hasOwnProperty(token2)) return false;
          return sanitizeWord(token2, token2, rules) === token2;
        };
      }
      function pluralize(word, count, inclusive) {
        var pluralized = count === 1 ? pluralize.singular(word) : pluralize.plural(word);
        return (inclusive ? count + " " : "") + pluralized;
      }
      pluralize.plural = replaceWord(
        irregularSingles,
        irregularPlurals,
        pluralRules
      );
      pluralize.isPlural = checkWord(
        irregularSingles,
        irregularPlurals,
        pluralRules
      );
      pluralize.singular = replaceWord(
        irregularPlurals,
        irregularSingles,
        singularRules
      );
      pluralize.isSingular = checkWord(
        irregularPlurals,
        irregularSingles,
        singularRules
      );
      pluralize.addPluralRule = function(rule, replacement) {
        pluralRules.push([sanitizeRule(rule), replacement]);
      };
      pluralize.addSingularRule = function(rule, replacement) {
        singularRules.push([sanitizeRule(rule), replacement]);
      };
      pluralize.addUncountableRule = function(word) {
        if (typeof word === "string") {
          uncountables[word.toLowerCase()] = true;
          return;
        }
        pluralize.addPluralRule(word, "$0");
        pluralize.addSingularRule(word, "$0");
      };
      pluralize.addIrregularRule = function(single, plural) {
        plural = plural.toLowerCase();
        single = single.toLowerCase();
        irregularSingles[single] = plural;
        irregularPlurals[plural] = single;
      };
      [
        // Pronouns.
        ["I", "we"],
        ["me", "us"],
        ["he", "they"],
        ["she", "they"],
        ["them", "them"],
        ["myself", "ourselves"],
        ["yourself", "yourselves"],
        ["itself", "themselves"],
        ["herself", "themselves"],
        ["himself", "themselves"],
        ["themself", "themselves"],
        ["is", "are"],
        ["was", "were"],
        ["has", "have"],
        ["this", "these"],
        ["that", "those"],
        // Words ending in with a consonant and `o`.
        ["echo", "echoes"],
        ["dingo", "dingoes"],
        ["volcano", "volcanoes"],
        ["tornado", "tornadoes"],
        ["torpedo", "torpedoes"],
        // Ends with `us`.
        ["genus", "genera"],
        ["viscus", "viscera"],
        // Ends with `ma`.
        ["stigma", "stigmata"],
        ["stoma", "stomata"],
        ["dogma", "dogmata"],
        ["lemma", "lemmata"],
        ["schema", "schemata"],
        ["anathema", "anathemata"],
        // Other irregular rules.
        ["ox", "oxen"],
        ["axe", "axes"],
        ["die", "dice"],
        ["yes", "yeses"],
        ["foot", "feet"],
        ["eave", "eaves"],
        ["goose", "geese"],
        ["tooth", "teeth"],
        ["quiz", "quizzes"],
        ["human", "humans"],
        ["proof", "proofs"],
        ["carve", "carves"],
        ["valve", "valves"],
        ["looey", "looies"],
        ["thief", "thieves"],
        ["groove", "grooves"],
        ["pickaxe", "pickaxes"],
        ["passerby", "passersby"]
      ].forEach(function(rule) {
        return pluralize.addIrregularRule(rule[0], rule[1]);
      });
      [
        [/s?$/i, "s"],
        [/[^\u0000-\u007F]$/i, "$0"],
        [/([^aeiou]ese)$/i, "$1"],
        [/(ax|test)is$/i, "$1es"],
        [/(alias|[^aou]us|t[lm]as|gas|ris)$/i, "$1es"],
        [/(e[mn]u)s?$/i, "$1s"],
        [/([^l]ias|[aeiou]las|[ejzr]as|[iu]am)$/i, "$1"],
        [/(alumn|syllab|vir|radi|nucle|fung|cact|stimul|termin|bacill|foc|uter|loc|strat)(?:us|i)$/i, "$1i"],
        [/(alumn|alg|vertebr)(?:a|ae)$/i, "$1ae"],
        [/(seraph|cherub)(?:im)?$/i, "$1im"],
        [/(her|at|gr)o$/i, "$1oes"],
        [/(agend|addend|millenni|dat|extrem|bacteri|desiderat|strat|candelabr|errat|ov|symposi|curricul|automat|quor)(?:a|um)$/i, "$1a"],
        [/(apheli|hyperbat|periheli|asyndet|noumen|phenomen|criteri|organ|prolegomen|hedr|automat)(?:a|on)$/i, "$1a"],
        [/sis$/i, "ses"],
        [/(?:(kni|wi|li)fe|(ar|l|ea|eo|oa|hoo)f)$/i, "$1$2ves"],
        [/([^aeiouy]|qu)y$/i, "$1ies"],
        [/([^ch][ieo][ln])ey$/i, "$1ies"],
        [/(x|ch|ss|sh|zz)$/i, "$1es"],
        [/(matr|cod|mur|sil|vert|ind|append)(?:ix|ex)$/i, "$1ices"],
        [/\b((?:tit)?m|l)(?:ice|ouse)$/i, "$1ice"],
        [/(pe)(?:rson|ople)$/i, "$1ople"],
        [/(child)(?:ren)?$/i, "$1ren"],
        [/eaux$/i, "$0"],
        [/m[ae]n$/i, "men"],
        ["thou", "you"]
      ].forEach(function(rule) {
        return pluralize.addPluralRule(rule[0], rule[1]);
      });
      [
        [/s$/i, ""],
        [/(ss)$/i, "$1"],
        [/(wi|kni|(?:after|half|high|low|mid|non|night|[^\w]|^)li)ves$/i, "$1fe"],
        [/(ar|(?:wo|[ae])l|[eo][ao])ves$/i, "$1f"],
        [/ies$/i, "y"],
        [/\b([pl]|zomb|(?:neck|cross)?t|coll|faer|food|gen|goon|group|lass|talk|goal|cut)ies$/i, "$1ie"],
        [/\b(mon|smil)ies$/i, "$1ey"],
        [/\b((?:tit)?m|l)ice$/i, "$1ouse"],
        [/(seraph|cherub)im$/i, "$1"],
        [/(x|ch|ss|sh|zz|tto|go|cho|alias|[^aou]us|t[lm]as|gas|(?:her|at|gr)o|[aeiou]ris)(?:es)?$/i, "$1"],
        [/(analy|diagno|parenthe|progno|synop|the|empha|cri|ne)(?:sis|ses)$/i, "$1sis"],
        [/(movie|twelve|abuse|e[mn]u)s$/i, "$1"],
        [/(test)(?:is|es)$/i, "$1is"],
        [/(alumn|syllab|vir|radi|nucle|fung|cact|stimul|termin|bacill|foc|uter|loc|strat)(?:us|i)$/i, "$1us"],
        [/(agend|addend|millenni|dat|extrem|bacteri|desiderat|strat|candelabr|errat|ov|symposi|curricul|quor)a$/i, "$1um"],
        [/(apheli|hyperbat|periheli|asyndet|noumen|phenomen|criteri|organ|prolegomen|hedr|automat)a$/i, "$1on"],
        [/(alumn|alg|vertebr)ae$/i, "$1a"],
        [/(cod|mur|sil|vert|ind)ices$/i, "$1ex"],
        [/(matr|append)ices$/i, "$1ix"],
        [/(pe)(rson|ople)$/i, "$1rson"],
        [/(child)ren$/i, "$1"],
        [/(eau)x?$/i, "$1"],
        [/men$/i, "man"]
      ].forEach(function(rule) {
        return pluralize.addSingularRule(rule[0], rule[1]);
      });
      [
        // Singular words with no plurals.
        "adulthood",
        "advice",
        "agenda",
        "aid",
        "aircraft",
        "alcohol",
        "ammo",
        "analytics",
        "anime",
        "athletics",
        "audio",
        "bison",
        "blood",
        "bream",
        "buffalo",
        "butter",
        "carp",
        "cash",
        "chassis",
        "chess",
        "clothing",
        "cod",
        "commerce",
        "cooperation",
        "corps",
        "debris",
        "diabetes",
        "digestion",
        "elk",
        "energy",
        "equipment",
        "excretion",
        "expertise",
        "firmware",
        "flounder",
        "fun",
        "gallows",
        "garbage",
        "graffiti",
        "hardware",
        "headquarters",
        "health",
        "herpes",
        "highjinks",
        "homework",
        "housework",
        "information",
        "jeans",
        "justice",
        "kudos",
        "labour",
        "literature",
        "machinery",
        "mackerel",
        "mail",
        "media",
        "mews",
        "moose",
        "music",
        "mud",
        "manga",
        "news",
        "only",
        "personnel",
        "pike",
        "plankton",
        "pliers",
        "police",
        "pollution",
        "premises",
        "rain",
        "research",
        "rice",
        "salmon",
        "scissors",
        "series",
        "sewage",
        "shambles",
        "shrimp",
        "software",
        "species",
        "staff",
        "swine",
        "tennis",
        "traffic",
        "transportation",
        "trout",
        "tuna",
        "wealth",
        "welfare",
        "whiting",
        "wildebeest",
        "wildlife",
        "you",
        /pok[eé]mon$/i,
        // Regexes.
        /[^aeiou]ese$/i,
        // "chinese", "japanese"
        /deer$/i,
        // "deer", "reindeer"
        /fish$/i,
        // "fish", "blowfish", "angelfish"
        /measles$/i,
        /o[iu]s$/i,
        // "carnivorous"
        /pox$/i,
        // "chickpox", "smallpox"
        /sheep$/i
      ].forEach(pluralize.addUncountableRule);
      return pluralize;
    });
  }
});

// node_modules/@redocly/openapi-core/lib/utils.js
var require_utils = __commonJS({
  "node_modules/@redocly/openapi-core/lib/utils.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.assignConfig = exports.stringifyYaml = exports.parseYaml = void 0;
    exports.pushStack = pushStack;
    exports.pluralize = pluralize;
    exports.popStack = popStack;
    exports.loadYaml = loadYaml;
    exports.isDefined = isDefined;
    exports.isPlainObject = isPlainObject2;
    exports.isEmptyObject = isEmptyObject;
    exports.isNotEmptyObject = isNotEmptyObject;
    exports.isEmptyArray = isEmptyArray;
    exports.isNotEmptyArray = isNotEmptyArray;
    exports.readFileFromUrl = readFileFromUrl;
    exports.pickObjectProps = pickObjectProps2;
    exports.omitObjectProps = omitObjectProps2;
    exports.splitCamelCaseIntoWords = splitCamelCaseIntoWords;
    exports.validateMimeType = validateMimeType;
    exports.validateMimeTypeOAS3 = validateMimeTypeOAS3;
    exports.readFileAsStringSync = readFileAsStringSync;
    exports.yamlAndJsonSyncReader = yamlAndJsonSyncReader;
    exports.isPathParameter = isPathParameter;
    exports.slash = slash;
    exports.isString = isString;
    exports.isNotString = isNotString;
    exports.assignOnlyExistingConfig = assignOnlyExistingConfig;
    exports.getMatchingStatusCodeRange = getMatchingStatusCodeRange;
    exports.isCustomRuleId = isCustomRuleId;
    exports.doesYamlFileExist = doesYamlFileExist;
    exports.showWarningForDeprecatedField = showWarningForDeprecatedField;
    exports.showErrorForDeprecatedField = showErrorForDeprecatedField;
    exports.isTruthy = isTruthy;
    exports.identity = identity;
    exports.keysOf = keysOf;
    exports.pickDefined = pickDefined;
    exports.nextTick = nextTick;
    exports.pause = pause;
    exports.getProxyAgent = getProxyAgent;
    exports.dequal = dequal;
    var fs = require_fs();
    var path_1 = require_path_browserify();
    var minimatch = require_minimatch();
    var js_yaml_1 = require_js_yaml2();
    var env_1 = require_env();
    var logger_1 = require_logger();
    var https_proxy_agent_1 = require_https_proxy_agent();
    var pluralizeOne = require_pluralize();
    var js_yaml_2 = require_js_yaml2();
    Object.defineProperty(exports, "parseYaml", { enumerable: true, get: function() {
      return js_yaml_2.parseYaml;
    } });
    Object.defineProperty(exports, "stringifyYaml", { enumerable: true, get: function() {
      return js_yaml_2.stringifyYaml;
    } });
    function pushStack(head, value) {
      return { prev: head, value };
    }
    function pluralize(sentence, count, inclusive) {
      return sentence.split(" ").map((word) => pluralizeOne(word, count, inclusive)).join(" ");
    }
    function popStack(head) {
      return head?.prev ?? null;
    }
    async function loadYaml(filename) {
      const contents = await fs.promises.readFile(filename, "utf-8");
      return (0, js_yaml_1.parseYaml)(contents);
    }
    function isDefined(x2) {
      return x2 !== void 0;
    }
    function isPlainObject2(value) {
      return value !== null && typeof value === "object" && !Array.isArray(value);
    }
    function isEmptyObject(value) {
      return isPlainObject2(value) && Object.keys(value).length === 0;
    }
    function isNotEmptyObject(obj) {
      return isPlainObject2(obj) && !isEmptyObject(obj);
    }
    function isEmptyArray(value) {
      return Array.isArray(value) && value.length === 0;
    }
    function isNotEmptyArray(args) {
      return !!args && Array.isArray(args) && !!args.length;
    }
    async function readFileFromUrl(url, config) {
      const headers = {};
      for (const header of config.headers) {
        if (match2(url, header.matches)) {
          headers[header.name] = header.envVariable !== void 0 ? env_1.env[header.envVariable] || "" : header.value;
        }
      }
      const req = await (config.customFetch || fetch)(url, {
        headers
      });
      if (!req.ok) {
        throw new Error(`Failed to load ${url}: ${req.status} ${req.statusText}`);
      }
      return { body: await req.text(), mimeType: req.headers.get("content-type") };
    }
    function match2(url, pattern) {
      if (!pattern.match(/^https?:\/\//)) {
        url = url.replace(/^https?:\/\//, "");
      }
      return minimatch(url, pattern);
    }
    function pickObjectProps2(object2, keys2) {
      return Object.fromEntries(keys2.filter((key2) => key2 in object2).map((key2) => [key2, object2[key2]]));
    }
    function omitObjectProps2(object2, keys2) {
      return Object.fromEntries(Object.entries(object2).filter(([key2]) => !keys2.includes(key2)));
    }
    function splitCamelCaseIntoWords(str) {
      const camel = str.split(/(?:[-._])|([A-Z][a-z]+)/).filter(isTruthy).map((item) => item.toLocaleLowerCase());
      const caps = str.split(/([A-Z]{2,})/).filter((e) => e && e === e.toUpperCase()).map((item) => item.toLocaleLowerCase());
      return /* @__PURE__ */ new Set([...camel, ...caps]);
    }
    function validateMimeType({ type, value }, { report, location }, allowedValues) {
      const ruleType = type === "consumes" ? "request" : "response";
      if (!allowedValues)
        throw new Error(`Parameter "allowedValues" is not provided for "${ruleType}-mime-type" rule`);
      if (!value[type])
        return;
      for (const mime of value[type]) {
        if (!allowedValues.includes(mime)) {
          report({
            message: `Mime type "${mime}" is not allowed`,
            location: location.child(value[type].indexOf(mime)).key()
          });
        }
      }
    }
    function validateMimeTypeOAS3({ type, value }, { report, location }, allowedValues) {
      const ruleType = type === "consumes" ? "request" : "response";
      if (!allowedValues)
        throw new Error(`Parameter "allowedValues" is not provided for "${ruleType}-mime-type" rule`);
      if (!value.content)
        return;
      for (const mime of Object.keys(value.content)) {
        if (!allowedValues.includes(mime)) {
          report({
            message: `Mime type "${mime}" is not allowed`,
            location: location.child("content").child(mime).key()
          });
        }
      }
    }
    function readFileAsStringSync(filePath) {
      return fs.readFileSync(filePath, "utf-8");
    }
    function yamlAndJsonSyncReader(filePath) {
      const content = fs.readFileSync(filePath, "utf-8");
      return (0, js_yaml_1.parseYaml)(content);
    }
    function isPathParameter(pathSegment) {
      return pathSegment.startsWith("{") && pathSegment.endsWith("}");
    }
    function slash(path) {
      const isExtendedLengthPath = /^\\\\\?\\/.test(path);
      if (isExtendedLengthPath) {
        return path;
      }
      return path.replace(/\\/g, "/");
    }
    function isString(value) {
      return typeof value === "string";
    }
    function isNotString(value) {
      return !isString(value);
    }
    var assignConfig = (target, obj) => {
      if (!obj)
        return;
      for (const k2 of Object.keys(obj)) {
        if (isPlainObject2(target[k2]) && typeof obj[k2] === "string") {
          target[k2].severity = obj[k2];
        } else {
          target[k2] = obj[k2];
        }
      }
    };
    exports.assignConfig = assignConfig;
    function assignOnlyExistingConfig(target, obj) {
      if (!obj)
        return;
      for (const k2 of Object.keys(obj)) {
        if (!target.hasOwnProperty(k2))
          continue;
        if (isPlainObject2(target[k2]) && typeof obj[k2] === "string") {
          target[k2].severity = obj[k2];
        } else {
          target[k2] = obj[k2];
        }
      }
    }
    function getMatchingStatusCodeRange(code) {
      return `${code}`.replace(/^(\d)\d\d$/, (_15, firstDigit) => `${firstDigit}XX`);
    }
    function isCustomRuleId(id) {
      return id.includes("/");
    }
    function doesYamlFileExist(filePath) {
      return ((0, path_1.extname)(filePath) === ".yaml" || (0, path_1.extname)(filePath) === ".yml") && fs?.hasOwnProperty?.("existsSync") && fs.existsSync(filePath);
    }
    function showWarningForDeprecatedField(deprecatedField, updatedField, updatedObject, link) {
      const readMoreText = link ? `Read more about this change: ${link}` : "";
      logger_1.logger.warn(`The '${logger_1.colorize.red(deprecatedField)}' field is deprecated. ${updatedField ? `Use ${logger_1.colorize.green(getUpdatedFieldName(updatedField, updatedObject))} instead. ` : ""}${readMoreText}
`);
    }
    function showErrorForDeprecatedField(deprecatedField, updatedField, updatedObject) {
      throw new Error(`Do not use '${deprecatedField}' field. ${updatedField ? `Use '${getUpdatedFieldName(updatedField, updatedObject)}' instead. ` : ""}
`);
    }
    function isTruthy(value) {
      return !!value;
    }
    function identity(value) {
      return value;
    }
    function keysOf(obj) {
      if (!obj)
        return [];
      return Object.keys(obj);
    }
    function pickDefined(obj) {
      if (!obj)
        return void 0;
      const res = {};
      for (const key2 in obj) {
        if (obj[key2] !== void 0) {
          res[key2] = obj[key2];
        }
      }
      return res;
    }
    function nextTick() {
      return new Promise((resolve) => {
        setTimeout(resolve);
      });
    }
    async function pause(ms) {
      return new Promise((resolve) => setTimeout(resolve, ms));
    }
    function getUpdatedFieldName(updatedField, updatedObject) {
      return `${typeof updatedObject !== "undefined" ? `${updatedObject}.` : ""}${updatedField}`;
    }
    function getProxyAgent() {
      const proxy = process.env.HTTPS_PROXY || process.env.HTTP_PROXY;
      return proxy ? new https_proxy_agent_1.HttpsProxyAgent(proxy) : void 0;
    }
    function dequal(foo, bar) {
      let ctor, len;
      if (foo === bar)
        return true;
      if (foo && bar && (ctor = foo.constructor) === bar.constructor) {
        if (ctor === Date)
          return foo.getTime() === bar.getTime();
        if (ctor === RegExp)
          return foo.toString() === bar.toString();
        if (ctor === Array) {
          if ((len = foo.length) === bar.length) {
            while (len-- && dequal(foo[len], bar[len]))
              ;
          }
          return len === -1;
        }
        if (!ctor || typeof foo === "object") {
          len = 0;
          for (ctor in foo) {
            if (Object.prototype.hasOwnProperty.call(foo, ctor) && ++len && !Object.prototype.hasOwnProperty.call(bar, ctor))
              return false;
            if (!(ctor in bar) || !dequal(foo[ctor], bar[ctor]))
              return false;
          }
          return Object.keys(bar).length === len;
        }
      }
      return foo !== foo && bar !== bar;
    }
  }
});

// node_modules/@redocly/openapi-core/lib/ref-utils.js
var require_ref_utils = __commonJS({
  "node_modules/@redocly/openapi-core/lib/ref-utils.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Location = void 0;
    exports.joinPointer = joinPointer;
    exports.isRef = isRef;
    exports.isExternalValue = isExternalValue;
    exports.unescapePointer = unescapePointer;
    exports.escapePointer = escapePointer;
    exports.parseRef = parseRef;
    exports.parsePointer = parsePointer;
    exports.pointerBaseName = pointerBaseName;
    exports.refBaseName = refBaseName;
    exports.isAbsoluteUrl = isAbsoluteUrl;
    exports.isMappingRef = isMappingRef;
    exports.isAnchor = isAnchor;
    var utils_1 = require_utils();
    function joinPointer(base, key2) {
      if (base === "")
        base = "#/";
      return base[base.length - 1] === "/" ? base + key2 : base + "/" + key2;
    }
    function isRef(node2) {
      return (0, utils_1.isPlainObject)(node2) && typeof node2.$ref === "string";
    }
    function isExternalValue(node2) {
      return (0, utils_1.isPlainObject)(node2) && typeof node2.externalValue === "string";
    }
    var Location = class _Location {
      constructor(source, pointer) {
        this.source = source;
        this.pointer = pointer;
      }
      child(components) {
        return new _Location(this.source, joinPointer(this.pointer, (Array.isArray(components) ? components : [components]).map(escapePointer).join("/")));
      }
      key() {
        return { ...this, reportOnKey: true };
      }
      get absolutePointer() {
        return this.source.absoluteRef + (this.pointer === "#/" ? "" : this.pointer);
      }
    };
    exports.Location = Location;
    function unescapePointer(fragment) {
      return decodeURIComponent(fragment.replace(/~1/g, "/").replace(/~0/g, "~"));
    }
    function escapePointer(fragment) {
      if (typeof fragment === "number")
        return fragment;
      return fragment.replace(/~/g, "~0").replace(/\//g, "~1");
    }
    function parseRef(ref) {
      const [uri, pointer = ""] = ref.split("#/");
      return {
        uri: (uri.endsWith("#") ? uri.slice(0, -1) : uri) || null,
        pointer: parsePointer(pointer)
      };
    }
    function parsePointer(pointer) {
      return pointer.split("/").map(unescapePointer).filter(utils_1.isTruthy);
    }
    function pointerBaseName(pointer) {
      const parts = pointer.split("/");
      return parts[parts.length - 1];
    }
    function refBaseName(ref) {
      const parts = ref.split(/[\/\\]/);
      return parts[parts.length - 1].replace(/\.[^.]+$/, "");
    }
    function isAbsoluteUrl(ref) {
      return ref.startsWith("http://") || ref.startsWith("https://");
    }
    function isMappingRef(mapping) {
      return mapping.startsWith("#") || mapping.startsWith("https://") || mapping.startsWith("http://") || mapping.startsWith("./") || mapping.startsWith("../") || mapping.indexOf("/") > -1;
    }
    function isAnchor(ref) {
      return /^#[A-Za-z][A-Za-z0-9\-_:.]*$/.test(ref);
    }
  }
});

// node_modules/@redocly/openapi-core/lib/types/index.js
var require_types = __commonJS({
  "node_modules/@redocly/openapi-core/lib/types/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.SpecExtension = void 0;
    exports.listOf = listOf;
    exports.mapOf = mapOf;
    exports.normalizeTypes = normalizeTypes;
    exports.isNamedType = isNamedType;
    function listOf(typeName) {
      return {
        name: `${typeName}List`,
        properties: {},
        items: typeName
      };
    }
    function mapOf(typeName) {
      return {
        name: `${typeName}Map`,
        properties: {},
        additionalProperties: () => typeName
      };
    }
    exports.SpecExtension = {
      name: "SpecExtension",
      properties: {},
      // skip validation of additional properties for unknown extensions
      additionalProperties: { resolvable: true }
    };
    function normalizeTypes(types, options = {}) {
      const normalizedTypes = {};
      for (const typeName of Object.keys(types)) {
        normalizedTypes[typeName] = {
          ...types[typeName],
          name: typeName
        };
      }
      for (const type of Object.values(normalizedTypes)) {
        normalizeType(type);
      }
      normalizedTypes["SpecExtension"] = exports.SpecExtension;
      return normalizedTypes;
      function normalizeType(type) {
        if (type.additionalProperties) {
          type.additionalProperties = resolveType(type.additionalProperties);
        }
        if (type.items) {
          type.items = resolveType(type.items);
        }
        if (type.properties) {
          const mappedProps = {};
          for (const [propName, prop] of Object.entries(type.properties)) {
            mappedProps[propName] = resolveType(prop);
            if (options.doNotResolveExamples && prop && prop.isExample) {
              mappedProps[propName] = {
                ...prop,
                resolvable: false
              };
            }
          }
          type.properties = mappedProps;
        }
      }
      function resolveType(type) {
        if (typeof type === "string") {
          if (!normalizedTypes[type]) {
            throw new Error(`Unknown type name found: ${type}`);
          }
          return normalizedTypes[type];
        } else if (typeof type === "function") {
          return (value, key2) => {
            return resolveType(type(value, key2));
          };
        } else if (type && type.name) {
          type = { ...type };
          normalizeType(type);
          return type;
        } else if (type && type.directResolveAs) {
          return {
            ...type,
            directResolveAs: resolveType(type.directResolveAs)
          };
        } else {
          return type;
        }
      }
    }
    function isNamedType(t) {
      return typeof t?.name === "string";
    }
  }
});

// node_modules/@redocly/openapi-core/lib/resolve.js
var require_resolve = __commonJS({
  "node_modules/@redocly/openapi-core/lib/resolve.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.BaseResolver = exports.YamlParseError = exports.ResolveError = exports.Source = void 0;
    exports.makeRefId = makeRefId;
    exports.makeDocumentFromString = makeDocumentFromString;
    exports.resolveDocument = resolveDocument;
    var fs = require_fs();
    var path = require_path_browserify();
    var ref_utils_1 = require_ref_utils();
    var types_1 = require_types();
    var utils_1 = require_utils();
    var Source = class {
      constructor(absoluteRef, body, mimeType) {
        this.absoluteRef = absoluteRef;
        this.body = body;
        this.mimeType = mimeType;
      }
      // pass safeLoad as argument to separate it from browser bundle
      getAst(safeLoad) {
        if (this._ast === void 0) {
          this._ast = safeLoad(this.body, { filename: this.absoluteRef }) ?? void 0;
          if (this._ast && this._ast.kind === 0 && // KIND.scalar = 0
          this._ast.value === "" && this._ast.startPosition !== 1) {
            this._ast.startPosition = 1;
            this._ast.endPosition = 1;
          }
        }
        return this._ast;
      }
      getLines() {
        if (this._lines === void 0) {
          this._lines = this.body.split(/\r\n|[\n\r]/g);
        }
        return this._lines;
      }
    };
    exports.Source = Source;
    var ResolveError = class _ResolveError extends Error {
      constructor(originalError) {
        super(originalError.message);
        this.originalError = originalError;
        Object.setPrototypeOf(this, _ResolveError.prototype);
      }
    };
    exports.ResolveError = ResolveError;
    var jsYamlErrorLineColRegexp = /\((\d+):(\d+)\)$/;
    var YamlParseError = class _YamlParseError extends Error {
      constructor(originalError, source) {
        super(originalError.message.split("\n")[0]);
        this.originalError = originalError;
        this.source = source;
        Object.setPrototypeOf(this, _YamlParseError.prototype);
        const [, line2, col] = this.message.match(jsYamlErrorLineColRegexp) || [];
        this.line = parseInt(line2, 10);
        this.col = parseInt(col, 10);
      }
    };
    exports.YamlParseError = YamlParseError;
    function makeRefId(absoluteRef, pointer) {
      return absoluteRef + "::" + pointer;
    }
    function makeDocumentFromString(sourceString, absoluteRef) {
      const source = new Source(absoluteRef, sourceString);
      try {
        return {
          source,
          parsed: (0, utils_1.parseYaml)(sourceString, { filename: absoluteRef })
        };
      } catch (e) {
        throw new YamlParseError(e, source);
      }
    }
    var BaseResolver = class {
      constructor(config = { http: { headers: [] } }) {
        this.config = config;
        this.cache = /* @__PURE__ */ new Map();
      }
      getFiles() {
        return new Set(Array.from(this.cache.keys()));
      }
      resolveExternalRef(base, ref) {
        if ((0, ref_utils_1.isAbsoluteUrl)(ref)) {
          return ref;
        }
        if (base && (0, ref_utils_1.isAbsoluteUrl)(base)) {
          return new URL(ref, base).href;
        }
        return path.resolve(base ? path.dirname(base) : process.cwd(), ref);
      }
      async loadExternalRef(absoluteRef) {
        try {
          if ((0, ref_utils_1.isAbsoluteUrl)(absoluteRef)) {
            const { body, mimeType } = await (0, utils_1.readFileFromUrl)(absoluteRef, this.config.http);
            return new Source(absoluteRef, body, mimeType);
          } else {
            if (fs.lstatSync(absoluteRef).isDirectory()) {
              throw new Error(`Expected a file but received a folder at ${absoluteRef}.`);
            }
            const content = await fs.promises.readFile(absoluteRef, "utf-8");
            return new Source(absoluteRef, content.replace(/\r\n/g, "\n"));
          }
        } catch (error) {
          error.message = error.message.replace(", lstat", "");
          throw new ResolveError(error);
        }
      }
      parseDocument(source, isRoot = false) {
        const ext = source.absoluteRef.substr(source.absoluteRef.lastIndexOf("."));
        if (![".json", ".json", ".yml", ".yaml"].includes(ext) && !source.mimeType?.match(/(json|yaml|openapi)/) && !isRoot) {
          return { source, parsed: source.body };
        }
        try {
          return {
            source,
            parsed: (0, utils_1.parseYaml)(source.body, { filename: source.absoluteRef })
          };
        } catch (e) {
          throw new YamlParseError(e, source);
        }
      }
      async resolveDocument(base, ref, isRoot = false) {
        const absoluteRef = this.resolveExternalRef(base, ref);
        const cachedDocument = this.cache.get(absoluteRef);
        if (cachedDocument) {
          return cachedDocument;
        }
        const doc = this.loadExternalRef(absoluteRef).then((source) => {
          return this.parseDocument(source, isRoot);
        });
        this.cache.set(absoluteRef, doc);
        return doc;
      }
    };
    exports.BaseResolver = BaseResolver;
    function pushRef(head, node2) {
      return {
        prev: head,
        node: node2
      };
    }
    function hasRef(head, node2) {
      while (head) {
        if (head.node === node2) {
          return true;
        }
        head = head.prev;
      }
      return false;
    }
    var unknownType = { name: "unknown", properties: {} };
    var resolvableScalarType = { name: "scalar", properties: {} };
    async function resolveDocument(opts) {
      const { rootDocument, externalRefResolver, rootType } = opts;
      const resolvedRefMap = /* @__PURE__ */ new Map();
      const seenNodes = /* @__PURE__ */ new Set();
      const resolvePromises = [];
      resolveRefsInParallel(rootDocument.parsed, rootDocument, "#/", rootType);
      let resolved;
      do {
        resolved = await Promise.all(resolvePromises);
      } while (resolvePromises.length !== resolved.length);
      return resolvedRefMap;
      function resolveRefsInParallel(rootNode, rootNodeDocument, rootNodePointer, type) {
        const rootNodeDocAbsoluteRef = rootNodeDocument.source.absoluteRef;
        const anchorRefsMap = /* @__PURE__ */ new Map();
        walk(rootNode, type, rootNodeDocAbsoluteRef + rootNodePointer);
        function walk(node2, type2, nodeAbsoluteRef) {
          if (typeof node2 !== "object" || node2 === null) {
            return;
          }
          const nodeId = `${type2.name}::${nodeAbsoluteRef}`;
          if (seenNodes.has(nodeId)) {
            return;
          }
          seenNodes.add(nodeId);
          const [_15, anchor] = Object.entries(node2).find(([key2]) => key2 === "$anchor") || [];
          if (anchor) {
            anchorRefsMap.set(`#${anchor}`, node2);
          }
          if (Array.isArray(node2)) {
            const itemsType = type2.items;
            if (itemsType === void 0 && type2 !== unknownType && type2 !== types_1.SpecExtension) {
              return;
            }
            const isTypeAFunction = typeof itemsType === "function";
            for (let i2 = 0; i2 < node2.length; i2++) {
              const itemType = isTypeAFunction ? itemsType(node2[i2], (0, ref_utils_1.joinPointer)(nodeAbsoluteRef, i2)) : itemsType;
              if (itemType === void 0 && type2 !== unknownType && type2 !== types_1.SpecExtension) {
                continue;
              }
              walk(node2[i2], (0, types_1.isNamedType)(itemType) ? itemType : unknownType, (0, ref_utils_1.joinPointer)(nodeAbsoluteRef, i2));
            }
            return;
          }
          for (const propName of Object.keys(node2)) {
            let propValue = node2[propName];
            let propType = type2.properties[propName];
            if (propType === void 0)
              propType = type2.additionalProperties;
            if (typeof propType === "function")
              propType = propType(propValue, propName);
            if (propType === void 0)
              propType = unknownType;
            if (type2.extensionsPrefix && propName.startsWith(type2.extensionsPrefix) && propType === unknownType) {
              propType = types_1.SpecExtension;
            }
            if (!(0, types_1.isNamedType)(propType) && propType?.directResolveAs) {
              propType = propType.directResolveAs;
              propValue = { $ref: propValue };
            }
            if (propType && propType.name === void 0 && propType.resolvable !== false) {
              propType = resolvableScalarType;
            }
            if (!(0, types_1.isNamedType)(propType) || typeof propValue !== "object") {
              continue;
            }
            walk(propValue, propType, (0, ref_utils_1.joinPointer)(nodeAbsoluteRef, (0, ref_utils_1.escapePointer)(propName)));
          }
          if ((0, ref_utils_1.isRef)(node2)) {
            const promise = followRef(rootNodeDocument, node2, {
              prev: null,
              node: node2
            }).then((resolvedRef) => {
              if (resolvedRef.resolved) {
                resolveRefsInParallel(resolvedRef.node, resolvedRef.document, resolvedRef.nodePointer, type2);
              }
            });
            resolvePromises.push(promise);
          }
          if ((0, ref_utils_1.isExternalValue)(node2)) {
            const promise = followRef(rootNodeDocument, { $ref: node2.externalValue }, {
              prev: null,
              node: node2
            }).then((resolvedRef) => {
              if (resolvedRef.resolved) {
                resolveRefsInParallel(resolvedRef.node, resolvedRef.document, resolvedRef.nodePointer, type2);
              }
            });
            resolvePromises.push(promise);
          }
        }
        async function followRef(document2, ref, refStack) {
          if (hasRef(refStack.prev, ref)) {
            throw new Error("Self-referencing circular pointer");
          }
          if ((0, ref_utils_1.isAnchor)(ref.$ref)) {
            await (0, utils_1.nextTick)();
            const resolvedRef2 = {
              resolved: true,
              isRemote: false,
              node: anchorRefsMap.get(ref.$ref),
              document: document2,
              nodePointer: ref.$ref
            };
            const refId2 = makeRefId(document2.source.absoluteRef, ref.$ref);
            resolvedRefMap.set(refId2, resolvedRef2);
            return resolvedRef2;
          }
          const { uri, pointer } = (0, ref_utils_1.parseRef)(ref.$ref);
          const isRemote = uri !== null;
          let targetDoc;
          try {
            targetDoc = isRemote ? await externalRefResolver.resolveDocument(document2.source.absoluteRef, uri) : document2;
          } catch (error) {
            const resolvedRef2 = {
              resolved: false,
              isRemote,
              document: void 0,
              error
            };
            const refId2 = makeRefId(document2.source.absoluteRef, ref.$ref);
            resolvedRefMap.set(refId2, resolvedRef2);
            return resolvedRef2;
          }
          let resolvedRef = {
            resolved: true,
            document: targetDoc,
            isRemote,
            node: document2.parsed,
            nodePointer: "#/"
          };
          let target = targetDoc.parsed;
          const segments = pointer;
          for (const segment of segments) {
            if (typeof target !== "object") {
              target = void 0;
              break;
            } else if (target[segment] !== void 0) {
              target = target[segment];
              resolvedRef.nodePointer = (0, ref_utils_1.joinPointer)(resolvedRef.nodePointer, (0, ref_utils_1.escapePointer)(segment));
            } else if ((0, ref_utils_1.isRef)(target)) {
              resolvedRef = await followRef(targetDoc, target, pushRef(refStack, target));
              targetDoc = resolvedRef.document || targetDoc;
              if (typeof resolvedRef.node !== "object") {
                target = void 0;
                break;
              }
              target = resolvedRef.node[segment];
              resolvedRef.nodePointer = (0, ref_utils_1.joinPointer)(resolvedRef.nodePointer, (0, ref_utils_1.escapePointer)(segment));
            } else {
              target = void 0;
              break;
            }
          }
          resolvedRef.node = target;
          resolvedRef.document = targetDoc;
          const refId = makeRefId(document2.source.absoluteRef, ref.$ref);
          if (resolvedRef.document && (0, ref_utils_1.isRef)(target)) {
            resolvedRef = await followRef(resolvedRef.document, target, pushRef(refStack, target));
          }
          resolvedRefMap.set(refId, resolvedRef);
          return { ...resolvedRef };
        }
      }
    }
  }
});

// node_modules/@redocly/openapi-core/lib/visitors.js
var require_visitors = __commonJS({
  "node_modules/@redocly/openapi-core/lib/visitors.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.normalizeVisitors = normalizeVisitors;
    var types_1 = require_types();
    var legacyTypesMap = {
      Root: "DefinitionRoot",
      ServerVariablesMap: "ServerVariableMap",
      Paths: ["PathMap", "PathsMap"],
      CallbacksMap: "CallbackMap",
      MediaTypesMap: "MediaTypeMap",
      ExamplesMap: "ExampleMap",
      EncodingMap: "EncodingsMap",
      HeadersMap: "HeaderMap",
      LinksMap: "LinkMap",
      OAuth2Flows: "SecuritySchemeFlows",
      Responses: "ResponsesMap"
    };
    function normalizeVisitors(visitorsConfig, types) {
      const normalizedVisitors = {};
      normalizedVisitors.any = {
        enter: [],
        leave: []
      };
      for (const typeName of Object.keys(types)) {
        normalizedVisitors[typeName] = {
          enter: [],
          leave: []
        };
      }
      normalizedVisitors.ref = {
        enter: [],
        leave: []
      };
      for (const { ruleId, severity, message, visitor } of visitorsConfig) {
        normalizeVisitorLevel({ ruleId, severity, message }, visitor, null);
      }
      for (const v2 of Object.keys(normalizedVisitors)) {
        normalizedVisitors[v2].enter.sort((a2, b2) => b2.depth - a2.depth);
        normalizedVisitors[v2].leave.sort((a2, b2) => a2.depth - b2.depth);
      }
      return normalizedVisitors;
      function addWeakNodes(ruleConf, from2, to, parentContext, stack = []) {
        if (stack.includes(from2))
          return;
        stack = [...stack, from2];
        const possibleChildren = /* @__PURE__ */ new Set();
        for (const type of Object.values(from2.properties)) {
          if (type === to) {
            addWeakFromStack(ruleConf, stack);
            continue;
          }
          if (typeof type === "object" && type !== null && type.name) {
            possibleChildren.add(type);
          }
        }
        if (from2.additionalProperties && typeof from2.additionalProperties !== "function") {
          if (from2.additionalProperties === to) {
            addWeakFromStack(ruleConf, stack);
          } else if (from2.additionalProperties.name !== void 0) {
            possibleChildren.add(from2.additionalProperties);
          }
        }
        if (from2.items && typeof from2.items !== "function") {
          if (from2.items === to) {
            addWeakFromStack(ruleConf, stack);
          } else if (from2.items.name !== void 0) {
            possibleChildren.add(from2.items);
          }
        }
        if (from2.extensionsPrefix) {
          possibleChildren.add(types_1.SpecExtension);
        }
        for (const fromType of Array.from(possibleChildren.values())) {
          addWeakNodes(ruleConf, fromType, to, parentContext, stack);
        }
        function addWeakFromStack(ruleConf2, stack2) {
          for (const interType of stack2.slice(1)) {
            normalizedVisitors[interType.name] = normalizedVisitors[interType.name] || {
              enter: [],
              leave: []
            };
            normalizedVisitors[interType.name].enter.push({
              ...ruleConf2,
              visit: () => void 0,
              depth: 0,
              context: {
                isSkippedLevel: true,
                seen: /* @__PURE__ */ new Set(),
                parent: parentContext
              }
            });
          }
        }
      }
      function findLegacyVisitorNode(visitor, typeName) {
        if (Array.isArray(typeName)) {
          const name = typeName.find((name2) => visitor[name2]) || void 0;
          return name && visitor[name];
        }
        return visitor[typeName];
      }
      function normalizeVisitorLevel(ruleConf, visitor, parentContext, depth = 0) {
        const visitorKeys = Object.keys(types);
        if (depth === 0) {
          visitorKeys.push("any");
          visitorKeys.push("ref");
        } else {
          if (visitor.any) {
            throw new Error("any() is allowed only on top level");
          }
          if (visitor.ref) {
            throw new Error("ref() is allowed only on top level");
          }
        }
        for (const typeName of visitorKeys) {
          const typeVisitor = visitor[typeName] || findLegacyVisitorNode(visitor, legacyTypesMap[typeName]);
          const normalizedTypeVisitor = normalizedVisitors[typeName];
          if (!typeVisitor)
            continue;
          let visitorEnter;
          let visitorLeave;
          let visitorSkip;
          const isObjectVisitor = typeof typeVisitor === "object";
          if (typeName === "ref" && isObjectVisitor && typeVisitor.skip) {
            throw new Error("ref() visitor does not support skip");
          }
          if (typeof typeVisitor === "function") {
            visitorEnter = typeVisitor;
          } else if (isObjectVisitor) {
            visitorEnter = typeVisitor.enter;
            visitorLeave = typeVisitor.leave;
            visitorSkip = typeVisitor.skip;
          }
          const context = {
            activatedOn: null,
            type: types[typeName],
            parent: parentContext,
            isSkippedLevel: false
          };
          if (typeof typeVisitor === "object") {
            normalizeVisitorLevel(ruleConf, typeVisitor, context, depth + 1);
          }
          if (parentContext) {
            addWeakNodes(ruleConf, parentContext.type, types[typeName], parentContext);
          }
          if (visitorEnter || isObjectVisitor) {
            if (visitorEnter && typeof visitorEnter !== "function") {
              throw new Error("DEV: should be function");
            }
            normalizedTypeVisitor.enter.push({
              ...ruleConf,
              visit: visitorEnter || (() => void 0),
              skip: visitorSkip,
              depth,
              context
            });
          }
          if (visitorLeave) {
            if (typeof visitorLeave !== "function") {
              throw new Error("DEV: should be function");
            }
            normalizedTypeVisitor.leave.push({
              ...ruleConf,
              visit: visitorLeave,
              depth,
              context
            });
          }
        }
      }
    }
  }
});

// node_modules/@redocly/openapi-core/lib/walk.js
var require_walk = __commonJS({
  "node_modules/@redocly/openapi-core/lib/walk.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.walkDocument = walkDocument;
    var ref_utils_1 = require_ref_utils();
    var utils_1 = require_utils();
    var resolve_1 = require_resolve();
    var types_1 = require_types();
    function collectParents(ctx) {
      const parents = {};
      while (ctx.parent) {
        parents[ctx.parent.type.name] = ctx.parent.activatedOn?.value.node;
        ctx = ctx.parent;
      }
      return parents;
    }
    function collectParentsLocations(ctx) {
      const locations = {};
      while (ctx.parent) {
        if (ctx.parent.activatedOn?.value.location) {
          locations[ctx.parent.type.name] = ctx.parent.activatedOn?.value.location;
        }
        ctx = ctx.parent;
      }
      return locations;
    }
    function walkDocument(opts) {
      const { document: document2, rootType, normalizedVisitors, resolvedRefMap, ctx } = opts;
      const seenNodesPerType = {};
      const ignoredNodes = /* @__PURE__ */ new Set();
      walkNode(document2.parsed, rootType, new ref_utils_1.Location(document2.source, "#/"), void 0, "");
      function walkNode(node2, type, location, parent, key2) {
        const resolve = (ref, from2 = currentLocation.source.absoluteRef) => {
          if (!(0, ref_utils_1.isRef)(ref))
            return { location, node: ref };
          const refId = (0, resolve_1.makeRefId)(from2, ref.$ref);
          const resolvedRef = resolvedRefMap.get(refId);
          if (!resolvedRef) {
            return {
              location: void 0,
              node: void 0
            };
          }
          const { resolved, node: node3, document: document3, nodePointer, error: error2 } = resolvedRef;
          const newLocation = resolved ? new ref_utils_1.Location(document3.source, nodePointer) : error2 instanceof resolve_1.YamlParseError ? new ref_utils_1.Location(error2.source, "") : void 0;
          return { location: newLocation, node: node3, error: error2 };
        };
        const rawLocation = location;
        let currentLocation = location;
        const { node: resolvedNode, location: resolvedLocation, error } = resolve(node2);
        const enteredContexts = /* @__PURE__ */ new Set();
        if ((0, ref_utils_1.isRef)(node2)) {
          const refEnterVisitors = normalizedVisitors.ref.enter;
          for (const { visit: visitor, ruleId, severity, message, context } of refEnterVisitors) {
            enteredContexts.add(context);
            const report = reportFn.bind(void 0, ruleId, severity, message);
            visitor(node2, {
              report,
              resolve,
              rawNode: node2,
              rawLocation,
              location,
              type,
              parent,
              key: key2,
              parentLocations: {},
              oasVersion: ctx.oasVersion,
              getVisitorData: getVisitorDataFn.bind(void 0, ruleId)
            }, { node: resolvedNode, location: resolvedLocation, error });
            if (resolvedLocation?.source.absoluteRef && ctx.refTypes) {
              ctx.refTypes.set(resolvedLocation?.source.absoluteRef, type);
            }
          }
        }
        if (resolvedNode !== void 0 && resolvedLocation && type.name !== "scalar") {
          currentLocation = resolvedLocation;
          const isNodeSeen = seenNodesPerType[type.name]?.has?.(resolvedNode);
          let visitedBySome = false;
          const anyEnterVisitors = normalizedVisitors.any.enter;
          const currentEnterVisitors = anyEnterVisitors.concat(normalizedVisitors[type.name]?.enter || []);
          const activatedContexts = [];
          for (const { context, visit: visit3, skip, ruleId, severity, message } of currentEnterVisitors) {
            if (ignoredNodes.has(`${currentLocation.absolutePointer}${currentLocation.pointer}`))
              break;
            if (context.isSkippedLevel) {
              if (context.parent.activatedOn && !context.parent.activatedOn.value.nextLevelTypeActivated && !context.seen.has(node2)) {
                context.seen.add(node2);
                visitedBySome = true;
                activatedContexts.push(context);
              }
            } else {
              if (context.parent && // if nested
              context.parent.activatedOn && context.activatedOn?.value.withParentNode !== context.parent.activatedOn.value.node && // do not enter if visited by parent children (it works thanks because deeper visitors are sorted before)
              context.parent.activatedOn.value.nextLevelTypeActivated?.value !== type || !context.parent && !isNodeSeen) {
                activatedContexts.push(context);
                const activatedOn = {
                  node: resolvedNode,
                  location: resolvedLocation,
                  nextLevelTypeActivated: null,
                  withParentNode: context.parent?.activatedOn?.value.node,
                  skipped: (context.parent?.activatedOn?.value.skipped || skip?.(resolvedNode, key2, {
                    location,
                    rawLocation,
                    resolve,
                    rawNode: node2
                  })) ?? false
                };
                context.activatedOn = (0, utils_1.pushStack)(context.activatedOn, activatedOn);
                let ctx2 = context.parent;
                while (ctx2) {
                  ctx2.activatedOn.value.nextLevelTypeActivated = (0, utils_1.pushStack)(ctx2.activatedOn.value.nextLevelTypeActivated, type);
                  ctx2 = ctx2.parent;
                }
                if (!activatedOn.skipped) {
                  visitedBySome = true;
                  enteredContexts.add(context);
                  visitWithContext(visit3, resolvedNode, node2, context, ruleId, severity, message);
                }
              }
            }
          }
          if (visitedBySome || !isNodeSeen) {
            seenNodesPerType[type.name] = seenNodesPerType[type.name] || /* @__PURE__ */ new Set();
            seenNodesPerType[type.name].add(resolvedNode);
            if (Array.isArray(resolvedNode)) {
              const itemsType = type.items;
              if (itemsType !== void 0) {
                const isTypeAFunction = typeof itemsType === "function";
                for (let i2 = 0; i2 < resolvedNode.length; i2++) {
                  const itemType = isTypeAFunction ? itemsType(resolvedNode[i2], resolvedLocation.child([i2]).absolutePointer) : itemsType;
                  if ((0, types_1.isNamedType)(itemType)) {
                    walkNode(resolvedNode[i2], itemType, resolvedLocation.child([i2]), resolvedNode, i2);
                  }
                }
              }
            } else if (typeof resolvedNode === "object" && resolvedNode !== null) {
              const props = Object.keys(type.properties);
              if (type.additionalProperties) {
                props.push(...Object.keys(resolvedNode).filter((k2) => !props.includes(k2)));
              } else if (type.extensionsPrefix) {
                props.push(...Object.keys(resolvedNode).filter((k2) => k2.startsWith(type.extensionsPrefix)));
              }
              if ((0, ref_utils_1.isRef)(node2)) {
                props.push(...Object.keys(node2).filter((k2) => k2 !== "$ref" && !props.includes(k2)));
              }
              for (const propName of props) {
                let value = resolvedNode[propName];
                let loc = resolvedLocation;
                if (value === void 0) {
                  value = node2[propName];
                  loc = location;
                }
                let propType = type.properties[propName];
                if (propType === void 0)
                  propType = type.additionalProperties;
                if (typeof propType === "function")
                  propType = propType(value, propName);
                if (propType === void 0 && type.extensionsPrefix && propName.startsWith(type.extensionsPrefix)) {
                  propType = types_1.SpecExtension;
                }
                if (!(0, types_1.isNamedType)(propType) && propType?.directResolveAs) {
                  propType = propType.directResolveAs;
                  value = { $ref: value };
                }
                if (propType && propType.name === void 0 && propType.resolvable !== false) {
                  propType = { name: "scalar", properties: {} };
                }
                if (!(0, types_1.isNamedType)(propType) || propType.name === "scalar" && !(0, ref_utils_1.isRef)(value)) {
                  continue;
                }
                walkNode(value, propType, loc.child([propName]), resolvedNode, propName);
              }
            }
          }
          const anyLeaveVisitors = normalizedVisitors.any.leave;
          const currentLeaveVisitors = (normalizedVisitors[type.name]?.leave || []).concat(anyLeaveVisitors);
          for (const context of activatedContexts.reverse()) {
            if (context.isSkippedLevel) {
              context.seen.delete(resolvedNode);
            } else {
              context.activatedOn = (0, utils_1.popStack)(context.activatedOn);
              if (context.parent) {
                let ctx2 = context.parent;
                while (ctx2) {
                  ctx2.activatedOn.value.nextLevelTypeActivated = (0, utils_1.popStack)(ctx2.activatedOn.value.nextLevelTypeActivated);
                  ctx2 = ctx2.parent;
                }
              }
            }
          }
          for (const { context, visit: visit3, ruleId, severity, message } of currentLeaveVisitors) {
            if (!context.isSkippedLevel && enteredContexts.has(context)) {
              visitWithContext(visit3, resolvedNode, node2, context, ruleId, severity, message);
            }
          }
        }
        currentLocation = location;
        if ((0, ref_utils_1.isRef)(node2)) {
          const refLeaveVisitors = normalizedVisitors.ref.leave;
          for (const { visit: visitor, ruleId, severity, context, message } of refLeaveVisitors) {
            if (enteredContexts.has(context)) {
              const report = reportFn.bind(void 0, ruleId, severity, message);
              visitor(node2, {
                report,
                resolve,
                rawNode: node2,
                rawLocation,
                location,
                type,
                parent,
                key: key2,
                parentLocations: {},
                oasVersion: ctx.oasVersion,
                getVisitorData: getVisitorDataFn.bind(void 0, ruleId)
              }, { node: resolvedNode, location: resolvedLocation, error });
            }
          }
        }
        function visitWithContext(visit3, resolvedNode2, node3, context, ruleId, severity, customMessage) {
          const report = reportFn.bind(void 0, ruleId, severity, customMessage);
          visit3(resolvedNode2, {
            report,
            resolve,
            rawNode: node3,
            location: currentLocation,
            rawLocation,
            type,
            parent,
            key: key2,
            parentLocations: collectParentsLocations(context),
            oasVersion: ctx.oasVersion,
            ignoreNextVisitorsOnNode: () => {
              ignoredNodes.add(`${currentLocation.absolutePointer}${currentLocation.pointer}`);
            },
            getVisitorData: getVisitorDataFn.bind(void 0, ruleId)
          }, collectParents(context), context);
        }
        function reportFn(ruleId, severity, customMessage, opts2) {
          const normalizedLocation = opts2.location ? Array.isArray(opts2.location) ? opts2.location : [opts2.location] : [{ ...currentLocation, reportOnKey: false }];
          const location2 = normalizedLocation.map((l2) => ({
            ...currentLocation,
            reportOnKey: false,
            ...l2
          }));
          const ruleSeverity = opts2.forceSeverity || severity;
          if (ruleSeverity !== "off") {
            ctx.problems.push({
              ruleId: opts2.ruleId || ruleId,
              severity: ruleSeverity,
              ...opts2,
              message: customMessage ? customMessage.replace("{{message}}", opts2.message) : opts2.message,
              suggest: opts2.suggest || [],
              location: location2
            });
          }
        }
        function getVisitorDataFn(ruleId) {
          ctx.visitorsData[ruleId] = ctx.visitorsData[ruleId] || {};
          return ctx.visitorsData[ruleId];
        }
      }
    }
  }
});

// node_modules/@redocly/openapi-core/lib/types/oas2.js
var require_oas2 = __commonJS({
  "node_modules/@redocly/openapi-core/lib/types/oas2.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Oas2Types = void 0;
    var _1 = require_types();
    var responseCodeRegexp = /^[0-9][0-9Xx]{2}$/;
    var Root = {
      properties: {
        swagger: { type: "string" },
        info: "Info",
        host: { type: "string" },
        basePath: { type: "string" },
        schemes: { type: "array", items: { type: "string" } },
        consumes: { type: "array", items: { type: "string" } },
        produces: { type: "array", items: { type: "string" } },
        paths: "Paths",
        definitions: "NamedSchemas",
        parameters: "NamedParameters",
        responses: "NamedResponses",
        securityDefinitions: "NamedSecuritySchemes",
        security: "SecurityRequirementList",
        tags: "TagList",
        externalDocs: "ExternalDocs",
        "x-servers": "XServerList",
        "x-tagGroups": "TagGroups",
        "x-ignoredHeaderParameters": { type: "array", items: { type: "string" } }
      },
      required: ["swagger", "paths", "info"],
      extensionsPrefix: "x-"
    };
    var Info = {
      properties: {
        title: { type: "string" },
        description: { type: "string" },
        termsOfService: { type: "string" },
        contact: "Contact",
        license: "License",
        version: { type: "string" },
        "x-logo": "Logo"
      },
      required: ["title", "version"],
      extensionsPrefix: "x-"
    };
    var Logo = {
      properties: {
        url: { type: "string" },
        altText: { type: "string" },
        backgroundColor: { type: "string" },
        href: { type: "string" }
      },
      extensionsPrefix: "x-"
    };
    var Contact = {
      properties: {
        name: { type: "string" },
        url: { type: "string" },
        email: { type: "string" }
      },
      extensionsPrefix: "x-"
    };
    var License = {
      properties: {
        name: { type: "string" },
        url: { type: "string" }
      },
      required: ["name"],
      extensionsPrefix: "x-"
    };
    var Paths = {
      properties: {},
      additionalProperties: (_value, key2) => key2.startsWith("/") ? "PathItem" : void 0
    };
    var PathItem = {
      properties: {
        $ref: { type: "string" },
        // TODO: verify special $ref handling for Path Item
        parameters: "ParameterList",
        get: "Operation",
        put: "Operation",
        post: "Operation",
        delete: "Operation",
        options: "Operation",
        head: "Operation",
        patch: "Operation"
      },
      extensionsPrefix: "x-"
    };
    var Operation = {
      properties: {
        tags: { type: "array", items: { type: "string" } },
        summary: { type: "string" },
        description: { type: "string" },
        externalDocs: "ExternalDocs",
        operationId: { type: "string" },
        consumes: { type: "array", items: { type: "string" } },
        produces: { type: "array", items: { type: "string" } },
        parameters: "ParameterList",
        responses: "Responses",
        schemes: { type: "array", items: { type: "string" } },
        deprecated: { type: "boolean" },
        security: "SecurityRequirementList",
        "x-codeSamples": "XCodeSampleList",
        "x-code-samples": "XCodeSampleList",
        // deprecated
        "x-hideTryItPanel": { type: "boolean" }
      },
      required: ["responses"],
      extensionsPrefix: "x-"
    };
    var XCodeSample = {
      properties: {
        lang: { type: "string" },
        label: { type: "string" },
        source: { type: "string" }
      }
    };
    var XServer = {
      properties: {
        url: { type: "string" },
        description: { type: "string" }
      },
      required: ["url"]
    };
    var ExternalDocs = {
      properties: {
        description: { type: "string" },
        url: { type: "string" }
      },
      required: ["url"],
      extensionsPrefix: "x-"
    };
    var Parameter = {
      properties: {
        name: { type: "string" },
        in: { type: "string", enum: ["query", "header", "path", "formData", "body"] },
        description: { type: "string" },
        required: { type: "boolean" },
        schema: "Schema",
        type: { type: "string", enum: ["string", "number", "integer", "boolean", "array", "file"] },
        format: { type: "string" },
        allowEmptyValue: { type: "boolean" },
        items: "ParameterItems",
        collectionFormat: { type: "string", enum: ["csv", "ssv", "tsv", "pipes", "multi"] },
        default: null,
        maximum: { type: "integer" },
        exclusiveMaximum: { type: "boolean" },
        minimum: { type: "integer" },
        exclusiveMinimum: { type: "boolean" },
        maxLength: { type: "integer" },
        minLength: { type: "integer" },
        pattern: { type: "string" },
        maxItems: { type: "integer" },
        minItems: { type: "integer" },
        uniqueItems: { type: "boolean" },
        enum: { type: "array" },
        multipleOf: { type: "number" },
        "x-example": {},
        // any
        "x-examples": "ExamplesMap"
      },
      required(value) {
        if (!value || !value.in) {
          return ["name", "in"];
        }
        if (value.in === "body") {
          return ["name", "in", "schema"];
        } else {
          if (value.type === "array") {
            return ["name", "in", "type", "items"];
          } else {
            return ["name", "in", "type"];
          }
        }
      },
      extensionsPrefix: "x-"
    };
    var ParameterItems = {
      properties: {
        type: { type: "string", enum: ["string", "number", "integer", "boolean", "array"] },
        format: { type: "string" },
        items: "ParameterItems",
        collectionFormat: { type: "string", enum: ["csv", "ssv", "tsv", "pipes", "multi"] },
        default: null,
        maximum: { type: "integer" },
        exclusiveMaximum: { type: "boolean" },
        minimum: { type: "integer" },
        exclusiveMinimum: { type: "boolean" },
        maxLength: { type: "integer" },
        minLength: { type: "integer" },
        pattern: { type: "string" },
        maxItems: { type: "integer" },
        minItems: { type: "integer" },
        uniqueItems: { type: "boolean" },
        enum: { type: "array" },
        multipleOf: { type: "number" }
      },
      required(value) {
        if (value && value.type === "array") {
          return ["type", "items"];
        } else {
          return ["type"];
        }
      },
      extensionsPrefix: "x-"
    };
    var Responses = {
      properties: {
        default: "Response"
      },
      additionalProperties: (_v, key2) => responseCodeRegexp.test(key2) ? "Response" : void 0
    };
    var Response = {
      properties: {
        description: { type: "string" },
        schema: "Schema",
        headers: (0, _1.mapOf)("Header"),
        examples: "Examples",
        "x-summary": { type: "string" }
      },
      required: ["description"],
      extensionsPrefix: "x-"
    };
    var Examples = {
      properties: {},
      additionalProperties: { isExample: true }
    };
    var Header = {
      properties: {
        description: { type: "string" },
        type: { type: "string", enum: ["string", "number", "integer", "boolean", "array"] },
        format: { type: "string" },
        items: "ParameterItems",
        collectionFormat: { type: "string", enum: ["csv", "ssv", "tsv", "pipes", "multi"] },
        default: null,
        maximum: { type: "integer" },
        exclusiveMaximum: { type: "boolean" },
        minimum: { type: "integer" },
        exclusiveMinimum: { type: "boolean" },
        maxLength: { type: "integer" },
        minLength: { type: "integer" },
        pattern: { type: "string" },
        maxItems: { type: "integer" },
        minItems: { type: "integer" },
        uniqueItems: { type: "boolean" },
        enum: { type: "array" },
        multipleOf: { type: "number" }
      },
      required(value) {
        if (value && value.type === "array") {
          return ["type", "items"];
        } else {
          return ["type"];
        }
      },
      extensionsPrefix: "x-"
    };
    var Tag = {
      properties: {
        name: { type: "string" },
        description: { type: "string" },
        externalDocs: "ExternalDocs",
        "x-traitTag": { type: "boolean" },
        "x-displayName": { type: "string" }
      },
      required: ["name"],
      extensionsPrefix: "x-"
    };
    var TagGroup = {
      properties: {
        name: { type: "string" },
        tags: { type: "array", items: { type: "string" } }
      }
    };
    var Schema2 = {
      properties: {
        format: { type: "string" },
        title: { type: "string" },
        description: { type: "string" },
        default: null,
        multipleOf: { type: "number" },
        maximum: { type: "number" },
        minimum: { type: "number" },
        exclusiveMaximum: { type: "boolean" },
        exclusiveMinimum: { type: "boolean" },
        maxLength: { type: "number" },
        minLength: { type: "number" },
        pattern: { type: "string" },
        maxItems: { type: "number" },
        minItems: { type: "number" },
        uniqueItems: { type: "boolean" },
        maxProperties: { type: "number" },
        minProperties: { type: "number" },
        required: { type: "array", items: { type: "string" } },
        enum: { type: "array" },
        type: {
          type: "string",
          enum: ["object", "array", "string", "number", "integer", "boolean", "null"]
        },
        items: (value) => {
          if (Array.isArray(value)) {
            return (0, _1.listOf)("Schema");
          } else {
            return "Schema";
          }
        },
        allOf: (0, _1.listOf)("Schema"),
        properties: "SchemaProperties",
        additionalProperties: (value) => {
          if (typeof value === "boolean") {
            return { type: "boolean" };
          } else {
            return "Schema";
          }
        },
        discriminator: { type: "string" },
        readOnly: { type: "boolean" },
        xml: "Xml",
        externalDocs: "ExternalDocs",
        example: { isExample: true },
        "x-tags": { type: "array", items: { type: "string" } },
        "x-nullable": { type: "boolean" },
        "x-extendedDiscriminator": { type: "string" },
        "x-additionalPropertiesName": { type: "string" },
        "x-explicitMappingOnly": { type: "boolean" },
        "x-enumDescriptions": "EnumDescriptions"
      },
      extensionsPrefix: "x-"
    };
    var EnumDescriptions = {
      properties: {},
      additionalProperties: { type: "string" }
    };
    var SchemaProperties = {
      properties: {},
      additionalProperties: "Schema"
    };
    var Xml = {
      properties: {
        name: { type: "string" },
        namespace: { type: "string" },
        prefix: { type: "string" },
        attribute: { type: "boolean" },
        wrapped: { type: "boolean" }
      },
      extensionsPrefix: "x-"
    };
    var SecurityScheme = {
      properties: {
        type: { enum: ["basic", "apiKey", "oauth2"] },
        description: { type: "string" },
        name: { type: "string" },
        in: { type: "string", enum: ["query", "header"] },
        flow: { enum: ["implicit", "password", "application", "accessCode"] },
        authorizationUrl: { type: "string" },
        tokenUrl: { type: "string" },
        scopes: { type: "object", additionalProperties: { type: "string" } },
        "x-defaultClientId": { type: "string" }
      },
      required(value) {
        switch (value?.type) {
          case "apiKey":
            return ["type", "name", "in"];
          case "oauth2":
            switch (value?.flow) {
              case "implicit":
                return ["type", "flow", "authorizationUrl", "scopes"];
              case "accessCode":
                return ["type", "flow", "authorizationUrl", "tokenUrl", "scopes"];
              case "application":
              case "password":
                return ["type", "flow", "tokenUrl", "scopes"];
              default:
                return ["type", "flow", "scopes"];
            }
          default:
            return ["type"];
        }
      },
      allowed(value) {
        switch (value?.type) {
          case "basic":
            return ["type", "description"];
          case "apiKey":
            return ["type", "name", "in", "description"];
          case "oauth2":
            switch (value?.flow) {
              case "implicit":
                return ["type", "flow", "authorizationUrl", "description", "scopes"];
              case "accessCode":
                return ["type", "flow", "authorizationUrl", "tokenUrl", "description", "scopes"];
              case "application":
              case "password":
                return ["type", "flow", "tokenUrl", "description", "scopes"];
              default:
                return ["type", "flow", "tokenUrl", "authorizationUrl", "description", "scopes"];
            }
          default:
            return ["type", "description"];
        }
      },
      extensionsPrefix: "x-"
    };
    var SecurityRequirement = {
      properties: {},
      additionalProperties: { type: "array", items: { type: "string" } }
    };
    var Example = {
      properties: {
        value: { isExample: true },
        summary: { type: "string" },
        description: { type: "string" },
        externalValue: { type: "string" }
      },
      extensionsPrefix: "x-"
    };
    exports.Oas2Types = {
      Root,
      Tag,
      TagList: (0, _1.listOf)("Tag"),
      TagGroups: (0, _1.listOf)("TagGroup"),
      TagGroup,
      ExternalDocs,
      Example,
      ExamplesMap: (0, _1.mapOf)("Example"),
      EnumDescriptions,
      SecurityRequirement,
      SecurityRequirementList: (0, _1.listOf)("SecurityRequirement"),
      Info,
      Contact,
      License,
      Logo,
      Paths,
      PathItem,
      Parameter,
      ParameterItems,
      ParameterList: (0, _1.listOf)("Parameter"),
      Operation,
      Examples,
      Header,
      Responses,
      Response,
      Schema: Schema2,
      Xml,
      SchemaProperties,
      NamedSchemas: (0, _1.mapOf)("Schema"),
      NamedResponses: (0, _1.mapOf)("Response"),
      NamedParameters: (0, _1.mapOf)("Parameter"),
      NamedSecuritySchemes: (0, _1.mapOf)("SecurityScheme"),
      SecurityScheme,
      XCodeSample,
      XCodeSampleList: (0, _1.listOf)("XCodeSample"),
      XServerList: (0, _1.listOf)("XServer"),
      XServer
    };
  }
});

// node_modules/@redocly/openapi-core/lib/types/oas3.js
var require_oas3 = __commonJS({
  "node_modules/@redocly/openapi-core/lib/types/oas3.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Oas3Types = exports.Discriminator = exports.DiscriminatorMapping = exports.Xml = exports.ExternalDocs = void 0;
    var _1 = require_types();
    var ref_utils_1 = require_ref_utils();
    var responseCodeRegexp = /^[0-9][0-9Xx]{2}$/;
    var Root = {
      properties: {
        openapi: null,
        info: "Info",
        servers: "ServerList",
        security: "SecurityRequirementList",
        tags: "TagList",
        externalDocs: "ExternalDocs",
        paths: "Paths",
        components: "Components",
        "x-webhooks": "WebhooksMap",
        "x-tagGroups": "TagGroups",
        "x-ignoredHeaderParameters": { type: "array", items: { type: "string" } }
      },
      required: ["openapi", "paths", "info"],
      extensionsPrefix: "x-"
    };
    var Tag = {
      properties: {
        name: { type: "string" },
        description: { type: "string" },
        externalDocs: "ExternalDocs",
        "x-traitTag": { type: "boolean" },
        "x-displayName": { type: "string" }
      },
      required: ["name"],
      extensionsPrefix: "x-"
    };
    var TagGroup = {
      properties: {
        name: { type: "string" },
        tags: { type: "array", items: { type: "string" } }
      },
      extensionsPrefix: "x-"
    };
    exports.ExternalDocs = {
      properties: {
        description: { type: "string" },
        url: { type: "string" }
      },
      required: ["url"],
      extensionsPrefix: "x-"
    };
    var Server = {
      properties: {
        url: { type: "string" },
        description: { type: "string" },
        variables: "ServerVariablesMap"
      },
      required: ["url"],
      extensionsPrefix: "x-"
    };
    var ServerVariable = {
      properties: {
        enum: {
          type: "array",
          items: { type: "string" }
        },
        default: { type: "string" },
        description: { type: "string" }
      },
      required: ["default"],
      extensionsPrefix: "x-"
    };
    var SecurityRequirement = {
      properties: {},
      additionalProperties: { type: "array", items: { type: "string" } }
    };
    var Info = {
      properties: {
        title: { type: "string" },
        version: { type: "string" },
        description: { type: "string" },
        termsOfService: { type: "string" },
        contact: "Contact",
        license: "License",
        "x-logo": "Logo"
      },
      required: ["title", "version"],
      extensionsPrefix: "x-"
    };
    var Logo = {
      properties: {
        url: { type: "string" },
        altText: { type: "string" },
        backgroundColor: { type: "string" },
        href: { type: "string" }
      }
    };
    var Contact = {
      properties: {
        name: { type: "string" },
        url: { type: "string" },
        email: { type: "string" }
      },
      extensionsPrefix: "x-"
    };
    var License = {
      properties: {
        name: { type: "string" },
        url: { type: "string" }
      },
      required: ["name"],
      extensionsPrefix: "x-"
    };
    var Paths = {
      properties: {},
      additionalProperties: (_value, key2) => key2.startsWith("/") ? "PathItem" : void 0
    };
    var WebhooksMap = {
      properties: {},
      additionalProperties: () => "PathItem"
    };
    var PathItem = {
      properties: {
        $ref: { type: "string" },
        // TODO: verify special $ref handling for Path Item
        servers: "ServerList",
        parameters: "ParameterList",
        summary: { type: "string" },
        description: { type: "string" },
        get: "Operation",
        put: "Operation",
        post: "Operation",
        delete: "Operation",
        options: "Operation",
        head: "Operation",
        patch: "Operation",
        trace: "Operation"
      },
      extensionsPrefix: "x-"
    };
    var Parameter = {
      properties: {
        name: { type: "string" },
        in: { enum: ["query", "header", "path", "cookie"] },
        description: { type: "string" },
        required: { type: "boolean" },
        deprecated: { type: "boolean" },
        allowEmptyValue: { type: "boolean" },
        style: {
          enum: ["form", "simple", "label", "matrix", "spaceDelimited", "pipeDelimited", "deepObject"]
        },
        explode: { type: "boolean" },
        allowReserved: { type: "boolean" },
        schema: "Schema",
        example: { isExample: true },
        examples: "ExamplesMap",
        content: "MediaTypesMap"
      },
      required: ["name", "in"],
      requiredOneOf: ["schema", "content"],
      extensionsPrefix: "x-"
    };
    var Operation = {
      properties: {
        tags: {
          type: "array",
          items: { type: "string" }
        },
        summary: { type: "string" },
        description: { type: "string" },
        externalDocs: "ExternalDocs",
        operationId: { type: "string" },
        parameters: "ParameterList",
        security: "SecurityRequirementList",
        servers: "ServerList",
        requestBody: "RequestBody",
        responses: "Responses",
        deprecated: { type: "boolean" },
        callbacks: "CallbacksMap",
        "x-codeSamples": "XCodeSampleList",
        "x-code-samples": "XCodeSampleList",
        // deprecated
        "x-hideTryItPanel": { type: "boolean" }
      },
      required: ["responses"],
      extensionsPrefix: "x-"
    };
    var XCodeSample = {
      properties: {
        lang: { type: "string" },
        label: { type: "string" },
        source: { type: "string" }
      }
    };
    var RequestBody = {
      properties: {
        description: { type: "string" },
        required: { type: "boolean" },
        content: "MediaTypesMap"
      },
      required: ["content"],
      extensionsPrefix: "x-"
    };
    var MediaTypesMap = {
      properties: {},
      additionalProperties: "MediaType"
    };
    var MediaType = {
      properties: {
        schema: "Schema",
        example: { isExample: true },
        examples: "ExamplesMap",
        encoding: "EncodingMap"
      },
      extensionsPrefix: "x-"
    };
    var Example = {
      properties: {
        value: { isExample: true },
        summary: { type: "string" },
        description: { type: "string" },
        externalValue: { type: "string" }
      },
      extensionsPrefix: "x-"
    };
    var Encoding = {
      properties: {
        contentType: { type: "string" },
        headers: "HeadersMap",
        style: {
          enum: ["form", "simple", "label", "matrix", "spaceDelimited", "pipeDelimited", "deepObject"]
        },
        explode: { type: "boolean" },
        allowReserved: { type: "boolean" }
      },
      extensionsPrefix: "x-"
    };
    var EnumDescriptions = {
      properties: {},
      additionalProperties: { type: "string" }
    };
    var Header = {
      properties: {
        description: { type: "string" },
        required: { type: "boolean" },
        deprecated: { type: "boolean" },
        allowEmptyValue: { type: "boolean" },
        style: {
          enum: ["form", "simple", "label", "matrix", "spaceDelimited", "pipeDelimited", "deepObject"]
        },
        explode: { type: "boolean" },
        allowReserved: { type: "boolean" },
        schema: "Schema",
        example: { isExample: true },
        examples: "ExamplesMap",
        content: "MediaTypesMap"
      },
      requiredOneOf: ["schema", "content"],
      extensionsPrefix: "x-"
    };
    var Responses = {
      properties: { default: "Response" },
      additionalProperties: (_v, key2) => responseCodeRegexp.test(key2) ? "Response" : void 0
    };
    var Response = {
      properties: {
        description: { type: "string" },
        headers: "HeadersMap",
        content: "MediaTypesMap",
        links: "LinksMap",
        "x-summary": { type: "string" }
      },
      required: ["description"],
      extensionsPrefix: "x-"
    };
    var Link = {
      properties: {
        operationRef: { type: "string" },
        operationId: { type: "string" },
        parameters: null,
        // TODO: figure out how to describe/validate this
        requestBody: null,
        // TODO: figure out how to describe/validate this
        description: { type: "string" },
        server: "Server"
      },
      extensionsPrefix: "x-"
    };
    var Schema2 = {
      properties: {
        externalDocs: "ExternalDocs",
        discriminator: "Discriminator",
        title: { type: "string" },
        multipleOf: { type: "number", minimum: 0 },
        maximum: { type: "number" },
        minimum: { type: "number" },
        exclusiveMaximum: { type: "boolean" },
        exclusiveMinimum: { type: "boolean" },
        maxLength: { type: "integer", minimum: 0 },
        minLength: { type: "integer", minimum: 0 },
        pattern: { type: "string" },
        maxItems: { type: "integer", minimum: 0 },
        minItems: { type: "integer", minimum: 0 },
        uniqueItems: { type: "boolean" },
        maxProperties: { type: "integer", minimum: 0 },
        minProperties: { type: "integer", minimum: 0 },
        required: { type: "array", items: { type: "string" } },
        enum: { type: "array" },
        type: {
          enum: ["object", "array", "string", "number", "integer", "boolean"]
        },
        allOf: (0, _1.listOf)("Schema"),
        anyOf: (0, _1.listOf)("Schema"),
        oneOf: (0, _1.listOf)("Schema"),
        not: "Schema",
        properties: "SchemaProperties",
        items: (value) => {
          if (Array.isArray(value)) {
            return (0, _1.listOf)("Schema");
          } else {
            return "Schema";
          }
        },
        additionalProperties: (value) => {
          if (typeof value === "boolean") {
            return { type: "boolean" };
          } else {
            return "Schema";
          }
        },
        description: { type: "string" },
        format: { type: "string" },
        default: null,
        nullable: { type: "boolean" },
        readOnly: { type: "boolean" },
        writeOnly: { type: "boolean" },
        xml: "Xml",
        example: { isExample: true },
        deprecated: { type: "boolean" },
        "x-tags": { type: "array", items: { type: "string" } },
        "x-additionalPropertiesName": { type: "string" },
        "x-explicitMappingOnly": { type: "boolean" }
      },
      extensionsPrefix: "x-"
    };
    exports.Xml = {
      properties: {
        name: { type: "string" },
        namespace: { type: "string" },
        prefix: { type: "string" },
        attribute: { type: "boolean" },
        wrapped: { type: "boolean" }
      },
      extensionsPrefix: "x-"
    };
    var SchemaProperties = {
      properties: {},
      additionalProperties: "Schema"
    };
    exports.DiscriminatorMapping = {
      properties: {},
      additionalProperties: (value) => {
        if ((0, ref_utils_1.isMappingRef)(value)) {
          return { type: "string", directResolveAs: "Schema" };
        } else {
          return { type: "string" };
        }
      }
    };
    exports.Discriminator = {
      properties: {
        propertyName: { type: "string" },
        mapping: "DiscriminatorMapping"
      },
      required: ["propertyName"],
      extensionsPrefix: "x-"
    };
    var Components = {
      properties: {
        parameters: "NamedParameters",
        schemas: "NamedSchemas",
        responses: "NamedResponses",
        examples: "NamedExamples",
        requestBodies: "NamedRequestBodies",
        headers: "NamedHeaders",
        securitySchemes: "NamedSecuritySchemes",
        links: "NamedLinks",
        callbacks: "NamedCallbacks"
      },
      extensionsPrefix: "x-"
    };
    var ImplicitFlow = {
      properties: {
        refreshUrl: { type: "string" },
        scopes: { type: "object", additionalProperties: { type: "string" } },
        // TODO: validate scopes
        authorizationUrl: { type: "string" }
      },
      required: ["authorizationUrl", "scopes"],
      extensionsPrefix: "x-"
    };
    var PasswordFlow = {
      properties: {
        refreshUrl: { type: "string" },
        scopes: { type: "object", additionalProperties: { type: "string" } },
        // TODO: validate scopes
        tokenUrl: { type: "string" }
      },
      required: ["tokenUrl", "scopes"],
      extensionsPrefix: "x-"
    };
    var ClientCredentials = {
      properties: {
        refreshUrl: { type: "string" },
        scopes: { type: "object", additionalProperties: { type: "string" } },
        // TODO: validate scopes
        tokenUrl: { type: "string" }
      },
      required: ["tokenUrl", "scopes"],
      extensionsPrefix: "x-"
    };
    var AuthorizationCode = {
      properties: {
        refreshUrl: { type: "string" },
        authorizationUrl: { type: "string" },
        scopes: { type: "object", additionalProperties: { type: "string" } },
        // TODO: validate scopes
        tokenUrl: { type: "string" },
        "x-usePkce": (value) => {
          if (typeof value === "boolean") {
            return { type: "boolean" };
          } else {
            return "XUsePkce";
          }
        }
      },
      required: ["authorizationUrl", "tokenUrl", "scopes"],
      extensionsPrefix: "x-"
    };
    var OAuth2Flows = {
      properties: {
        implicit: "ImplicitFlow",
        password: "PasswordFlow",
        clientCredentials: "ClientCredentials",
        authorizationCode: "AuthorizationCode"
      },
      extensionsPrefix: "x-"
    };
    var SecurityScheme = {
      properties: {
        type: { enum: ["apiKey", "http", "oauth2", "openIdConnect"] },
        description: { type: "string" },
        name: { type: "string" },
        in: { type: "string", enum: ["query", "header", "cookie"] },
        scheme: { type: "string" },
        bearerFormat: { type: "string" },
        flows: "OAuth2Flows",
        openIdConnectUrl: { type: "string" },
        "x-defaultClientId": { type: "string" }
      },
      required(value) {
        switch (value?.type) {
          case "apiKey":
            return ["type", "name", "in"];
          case "http":
            return ["type", "scheme"];
          case "oauth2":
            return ["type", "flows"];
          case "openIdConnect":
            return ["type", "openIdConnectUrl"];
          default:
            return ["type"];
        }
      },
      allowed(value) {
        switch (value?.type) {
          case "apiKey":
            return ["type", "name", "in", "description"];
          case "http":
            return ["type", "scheme", "bearerFormat", "description"];
          case "oauth2":
            return ["type", "flows", "description"];
          case "openIdConnect":
            return ["type", "openIdConnectUrl", "description"];
          default:
            return ["type", "description"];
        }
      },
      extensionsPrefix: "x-"
    };
    var XUsePkce = {
      properties: {
        disableManualConfiguration: { type: "boolean" },
        hideClientSecretInput: { type: "boolean" }
      }
    };
    exports.Oas3Types = {
      Root,
      Tag,
      TagList: (0, _1.listOf)("Tag"),
      TagGroups: (0, _1.listOf)("TagGroup"),
      TagGroup,
      ExternalDocs: exports.ExternalDocs,
      Server,
      ServerList: (0, _1.listOf)("Server"),
      ServerVariable,
      ServerVariablesMap: (0, _1.mapOf)("ServerVariable"),
      SecurityRequirement,
      SecurityRequirementList: (0, _1.listOf)("SecurityRequirement"),
      Info,
      Contact,
      License,
      Paths,
      PathItem,
      Parameter,
      ParameterList: (0, _1.listOf)("Parameter"),
      Operation,
      Callback: (0, _1.mapOf)("PathItem"),
      CallbacksMap: (0, _1.mapOf)("Callback"),
      RequestBody,
      MediaTypesMap,
      MediaType,
      Example,
      ExamplesMap: (0, _1.mapOf)("Example"),
      Encoding,
      EncodingMap: (0, _1.mapOf)("Encoding"),
      EnumDescriptions,
      Header,
      HeadersMap: (0, _1.mapOf)("Header"),
      Responses,
      Response,
      Link,
      Logo,
      Schema: Schema2,
      Xml: exports.Xml,
      SchemaProperties,
      DiscriminatorMapping: exports.DiscriminatorMapping,
      Discriminator: exports.Discriminator,
      Components,
      LinksMap: (0, _1.mapOf)("Link"),
      NamedSchemas: (0, _1.mapOf)("Schema"),
      NamedResponses: (0, _1.mapOf)("Response"),
      NamedParameters: (0, _1.mapOf)("Parameter"),
      NamedExamples: (0, _1.mapOf)("Example"),
      NamedRequestBodies: (0, _1.mapOf)("RequestBody"),
      NamedHeaders: (0, _1.mapOf)("Header"),
      NamedSecuritySchemes: (0, _1.mapOf)("SecurityScheme"),
      NamedLinks: (0, _1.mapOf)("Link"),
      NamedCallbacks: (0, _1.mapOf)("Callback"),
      ImplicitFlow,
      PasswordFlow,
      ClientCredentials,
      AuthorizationCode,
      OAuth2Flows,
      SecurityScheme,
      XCodeSample,
      XCodeSampleList: (0, _1.listOf)("XCodeSample"),
      XUsePkce,
      WebhooksMap
    };
  }
});

// node_modules/@redocly/openapi-core/lib/types/oas3_1.js
var require_oas3_1 = __commonJS({
  "node_modules/@redocly/openapi-core/lib/types/oas3_1.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Oas3_1Types = exports.DependentRequired = exports.SchemaProperties = exports.Schema = void 0;
    var _1 = require_types();
    var oas3_1 = require_oas3();
    var Root = {
      properties: {
        openapi: null,
        info: "Info",
        servers: "ServerList",
        security: "SecurityRequirementList",
        tags: "TagList",
        externalDocs: "ExternalDocs",
        paths: "Paths",
        webhooks: "WebhooksMap",
        components: "Components",
        jsonSchemaDialect: { type: "string" }
      },
      required: ["openapi", "info"],
      requiredOneOf: ["paths", "components", "webhooks"],
      extensionsPrefix: "x-"
    };
    var License = {
      properties: {
        name: { type: "string" },
        url: { type: "string" },
        identifier: { type: "string" }
      },
      required: ["name"],
      extensionsPrefix: "x-"
    };
    var Info = {
      properties: {
        title: { type: "string" },
        version: { type: "string" },
        description: { type: "string" },
        termsOfService: { type: "string" },
        summary: { type: "string" },
        contact: "Contact",
        license: "License",
        "x-logo": "Logo"
      },
      required: ["title", "version"],
      extensionsPrefix: "x-"
    };
    var Components = {
      properties: {
        parameters: "NamedParameters",
        schemas: "NamedSchemas",
        responses: "NamedResponses",
        examples: "NamedExamples",
        requestBodies: "NamedRequestBodies",
        headers: "NamedHeaders",
        securitySchemes: "NamedSecuritySchemes",
        links: "NamedLinks",
        callbacks: "NamedCallbacks",
        pathItems: "NamedPathItems"
      },
      extensionsPrefix: "x-"
    };
    var Operation = {
      properties: {
        tags: {
          type: "array",
          items: { type: "string" }
        },
        summary: { type: "string" },
        description: { type: "string" },
        externalDocs: "ExternalDocs",
        operationId: { type: "string" },
        parameters: "ParameterList",
        security: "SecurityRequirementList",
        servers: "ServerList",
        requestBody: "RequestBody",
        responses: "Responses",
        deprecated: { type: "boolean" },
        callbacks: "CallbacksMap",
        "x-codeSamples": "XCodeSampleList",
        "x-code-samples": "XCodeSampleList",
        // deprecated
        "x-hideTryItPanel": { type: "boolean" }
      },
      extensionsPrefix: "x-"
    };
    exports.Schema = {
      properties: {
        $id: { type: "string" },
        $anchor: { type: "string" },
        id: { type: "string" },
        $schema: { type: "string" },
        definitions: "NamedSchemas",
        $defs: "NamedSchemas",
        $vocabulary: { type: "string" },
        externalDocs: "ExternalDocs",
        discriminator: "Discriminator",
        title: { type: "string" },
        multipleOf: { type: "number", minimum: 0 },
        maximum: { type: "number" },
        minimum: { type: "number" },
        exclusiveMaximum: { type: "number" },
        exclusiveMinimum: { type: "number" },
        maxLength: { type: "integer", minimum: 0 },
        minLength: { type: "integer", minimum: 0 },
        pattern: { type: "string" },
        maxItems: { type: "integer", minimum: 0 },
        minItems: { type: "integer", minimum: 0 },
        uniqueItems: { type: "boolean" },
        maxProperties: { type: "integer", minimum: 0 },
        minProperties: { type: "integer", minimum: 0 },
        required: { type: "array", items: { type: "string" } },
        enum: { type: "array" },
        type: (value) => {
          if (Array.isArray(value)) {
            return {
              type: "array",
              items: { enum: ["object", "array", "string", "number", "integer", "boolean", "null"] }
            };
          } else {
            return {
              enum: ["object", "array", "string", "number", "integer", "boolean", "null"]
            };
          }
        },
        allOf: (0, _1.listOf)("Schema"),
        anyOf: (0, _1.listOf)("Schema"),
        oneOf: (0, _1.listOf)("Schema"),
        not: "Schema",
        if: "Schema",
        then: "Schema",
        else: "Schema",
        dependentSchemas: (0, _1.mapOf)("Schema"),
        dependentRequired: "DependentRequired",
        prefixItems: (0, _1.listOf)("Schema"),
        contains: "Schema",
        minContains: { type: "integer", minimum: 0 },
        maxContains: { type: "integer", minimum: 0 },
        patternProperties: "PatternProperties",
        propertyNames: "Schema",
        unevaluatedItems: (value) => {
          if (typeof value === "boolean") {
            return { type: "boolean" };
          } else {
            return "Schema";
          }
        },
        unevaluatedProperties: (value) => {
          if (typeof value === "boolean") {
            return { type: "boolean" };
          } else {
            return "Schema";
          }
        },
        summary: { type: "string" },
        properties: "SchemaProperties",
        items: (value) => {
          if (typeof value === "boolean") {
            return { type: "boolean" };
          } else {
            return "Schema";
          }
        },
        additionalProperties: (value) => {
          return typeof value === "boolean" ? { type: "boolean" } : "Schema";
        },
        description: { type: "string" },
        format: { type: "string" },
        contentEncoding: { type: "string" },
        contentMediaType: { type: "string" },
        contentSchema: "Schema",
        default: null,
        readOnly: { type: "boolean" },
        writeOnly: { type: "boolean" },
        xml: "Xml",
        examples: { type: "array" },
        example: { isExample: true },
        deprecated: { type: "boolean" },
        const: null,
        $comment: { type: "string" },
        "x-tags": { type: "array", items: { type: "string" } },
        $dynamicAnchor: { type: "string" },
        $dynamicRef: { type: "string" }
      },
      extensionsPrefix: "x-"
    };
    exports.SchemaProperties = {
      properties: {},
      additionalProperties: (value) => {
        if (typeof value === "boolean") {
          return { type: "boolean" };
        } else {
          return "Schema";
        }
      }
    };
    var SecurityScheme = {
      properties: {
        type: { enum: ["apiKey", "http", "oauth2", "openIdConnect", "mutualTLS"] },
        description: { type: "string" },
        name: { type: "string" },
        in: { type: "string", enum: ["query", "header", "cookie"] },
        scheme: { type: "string" },
        bearerFormat: { type: "string" },
        flows: "OAuth2Flows",
        openIdConnectUrl: { type: "string" }
      },
      required(value) {
        switch (value?.type) {
          case "apiKey":
            return ["type", "name", "in"];
          case "http":
            return ["type", "scheme"];
          case "oauth2":
            return ["type", "flows"];
          case "openIdConnect":
            return ["type", "openIdConnectUrl"];
          default:
            return ["type"];
        }
      },
      allowed(value) {
        switch (value?.type) {
          case "apiKey":
            return ["type", "name", "in", "description"];
          case "http":
            return ["type", "scheme", "bearerFormat", "description"];
          case "oauth2":
            switch (value?.flows) {
              case "implicit":
                return ["type", "flows", "authorizationUrl", "refreshUrl", "description", "scopes"];
              case "password":
              case "clientCredentials":
                return ["type", "flows", "tokenUrl", "refreshUrl", "description", "scopes"];
              case "authorizationCode":
                return [
                  "type",
                  "flows",
                  "authorizationUrl",
                  "refreshUrl",
                  "tokenUrl",
                  "description",
                  "scopes"
                ];
              default:
                return [
                  "type",
                  "flows",
                  "authorizationUrl",
                  "refreshUrl",
                  "tokenUrl",
                  "description",
                  "scopes"
                ];
            }
          case "openIdConnect":
            return ["type", "openIdConnectUrl", "description"];
          case "mutualTLS":
            return ["type", "description"];
          default:
            return ["type", "description"];
        }
      },
      extensionsPrefix: "x-"
    };
    exports.DependentRequired = {
      properties: {},
      additionalProperties: { type: "array", items: { type: "string" } }
    };
    exports.Oas3_1Types = {
      ...oas3_1.Oas3Types,
      Info,
      Root,
      Schema: exports.Schema,
      SchemaProperties: exports.SchemaProperties,
      PatternProperties: exports.SchemaProperties,
      License,
      Components,
      NamedPathItems: (0, _1.mapOf)("PathItem"),
      SecurityScheme,
      Operation,
      DependentRequired: exports.DependentRequired
    };
  }
});

// node_modules/@redocly/openapi-core/lib/types/asyncapi2.js
var require_asyncapi2 = __commonJS({
  "node_modules/@redocly/openapi-core/lib/types/asyncapi2.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.AsyncApi2Types = exports.AsyncApi2Bindings = exports.Dependencies = exports.SecuritySchemeFlows = exports.Discriminator = exports.DiscriminatorMapping = exports.SchemaProperties = exports.Schema = exports.MessageExample = exports.CorrelationId = exports.License = exports.Contact = exports.ServerVariable = exports.ServerMap = exports.ExternalDocs = exports.Tag = void 0;
    var _1 = require_types();
    var ref_utils_1 = require_ref_utils();
    var Root = {
      properties: {
        asyncapi: null,
        // TODO: validate semver format and supported version
        info: "Info",
        id: { type: "string" },
        servers: "ServerMap",
        channels: "ChannelMap",
        components: "Components",
        tags: "TagList",
        externalDocs: "ExternalDocs",
        defaultContentType: { type: "string" }
      },
      required: ["asyncapi", "channels", "info"]
    };
    var Channel = {
      properties: {
        description: { type: "string" },
        subscribe: "Operation",
        publish: "Operation",
        parameters: "ParametersMap",
        bindings: "ChannelBindings",
        servers: { type: "array", items: { type: "string" } }
      }
    };
    var ChannelMap = {
      properties: {},
      additionalProperties: "Channel"
    };
    var ChannelBindings = {
      properties: {},
      allowed() {
        return [
          "http",
          "ws",
          "kafka",
          "anypointmq",
          "amqp",
          "amqp1",
          "mqtt",
          "mqtt5",
          "nats",
          "jms",
          "sns",
          "solace",
          "sqs",
          "stomp",
          "redis",
          "mercure",
          "ibmmq",
          "googlepubsub",
          "pulsar"
        ];
      },
      additionalProperties: { type: "object" }
    };
    exports.Tag = {
      properties: {
        name: { type: "string" },
        description: { type: "string" },
        externalDocs: "ExternalDocs"
      },
      required: ["name"]
    };
    exports.ExternalDocs = {
      properties: {
        description: { type: "string" },
        url: { type: "string" }
      },
      required: ["url"]
    };
    var SecurityRequirement = {
      properties: {},
      additionalProperties: { type: "array", items: { type: "string" } }
    };
    var ServerBindings = {
      properties: {},
      allowed() {
        return [
          "http",
          "ws",
          "kafka",
          "anypointmq",
          "amqp",
          "amqp1",
          "mqtt",
          "mqtt5",
          "nats",
          "jms",
          "sns",
          "solace",
          "sqs",
          "stomp",
          "redis",
          "mercure",
          "ibmmq",
          "googlepubsub",
          "pulsar"
        ];
      },
      additionalProperties: { type: "object" }
    };
    var Server = {
      properties: {
        url: { type: "string" },
        protocol: { type: "string" },
        protocolVersion: { type: "string" },
        description: { type: "string" },
        variables: "ServerVariablesMap",
        security: "SecurityRequirementList",
        bindings: "ServerBindings",
        tags: "TagList"
      },
      required: ["url", "protocol"]
    };
    exports.ServerMap = {
      properties: {},
      additionalProperties: (_value, key2) => (
        // eslint-disable-next-line no-useless-escape
        key2.match(/^[A-Za-z0-9_\-]+$/) ? "Server" : void 0
      )
    };
    exports.ServerVariable = {
      properties: {
        enum: {
          type: "array",
          items: { type: "string" }
        },
        default: { type: "string" },
        description: { type: "string" },
        examples: {
          type: "array",
          items: { type: "string" }
        }
      },
      required: []
    };
    var Info = {
      properties: {
        title: { type: "string" },
        version: { type: "string" },
        description: { type: "string" },
        termsOfService: { type: "string" },
        contact: "Contact",
        license: "License"
      },
      required: ["title", "version"]
    };
    exports.Contact = {
      properties: {
        name: { type: "string" },
        url: { type: "string" },
        email: { type: "string" }
      }
    };
    exports.License = {
      properties: {
        name: { type: "string" },
        url: { type: "string" }
      },
      required: ["name"]
    };
    var Parameter = {
      properties: {
        description: { type: "string" },
        schema: "Schema",
        location: { type: "string" }
      }
    };
    exports.CorrelationId = {
      properties: {
        description: { type: "string" },
        location: { type: "string" }
      },
      required: ["location"]
    };
    var Message = {
      properties: {
        messageId: { type: "string" },
        headers: "Schema",
        payload: "Schema",
        // TODO: strictly this does not cover all cases
        correlationId: "CorrelationId",
        schemaFormat: { type: "string" },
        // TODO: support official list of schema formats and custom values
        contentType: { type: "string" },
        name: { type: "string" },
        title: { type: "string" },
        summary: { type: "string" },
        description: { type: "string" },
        tags: "TagList",
        externalDocs: "ExternalDocs",
        bindings: "MessageBindings",
        examples: "MessageExampleList",
        traits: "MessageTraitList"
      },
      additionalProperties: {}
    };
    var MessageBindings = {
      properties: {},
      allowed() {
        return [
          "http",
          "ws",
          "kafka",
          "anypointmq",
          "amqp",
          "amqp1",
          "mqtt",
          "mqtt5",
          "nats",
          "jms",
          "sns",
          "solace",
          "sqs",
          "stomp",
          "redis",
          "mercure",
          "ibmmq",
          "googlepubsub",
          "pulsar"
        ];
      },
      additionalProperties: { type: "object" }
    };
    var OperationBindings = {
      properties: {},
      allowed() {
        return [
          "http",
          "ws",
          "kafka",
          "anypointmq",
          "amqp",
          "amqp1",
          "mqtt",
          "mqtt5",
          "nats",
          "jms",
          "sns",
          "solace",
          "sqs",
          "stomp",
          "redis",
          "mercure",
          "ibmmq",
          "googlepubsub",
          "pulsar"
        ];
      },
      additionalProperties: { type: "object" }
    };
    var OperationTrait = {
      properties: {
        tags: "TagList",
        summary: { type: "string" },
        description: { type: "string" },
        externalDocs: "ExternalDocs",
        operationId: { type: "string" },
        security: "SecurityRequirementList",
        bindings: "OperationBindings"
      },
      required: []
    };
    var MessageTrait = {
      properties: {
        messageId: { type: "string" },
        headers: "Schema",
        correlationId: "CorrelationId",
        schemaFormat: { type: "string" },
        contentType: { type: "string" },
        name: { type: "string" },
        title: { type: "string" },
        summary: { type: "string" },
        description: { type: "string" },
        tags: "TagList",
        externalDocs: "ExternalDocs",
        bindings: "MessageBindings",
        examples: "MessageExampleList"
      },
      additionalProperties: {}
    };
    var Operation = {
      properties: {
        tags: "TagList",
        summary: { type: "string" },
        description: { type: "string" },
        externalDocs: "ExternalDocs",
        operationId: { type: "string" },
        security: "SecurityRequirementList",
        bindings: "OperationBindings",
        traits: "OperationTraitList",
        message: "Message"
      },
      required: []
    };
    exports.MessageExample = {
      properties: {
        payload: { isExample: true },
        summary: { type: "string" },
        name: { type: "string" },
        headers: { type: "object" }
      }
    };
    exports.Schema = {
      properties: {
        $id: { type: "string" },
        $schema: { type: "string" },
        definitions: "NamedSchemas",
        externalDocs: "ExternalDocs",
        discriminator: "Discriminator",
        myArbitraryKeyword: { type: "boolean" },
        title: { type: "string" },
        multipleOf: { type: "number", minimum: 0 },
        maximum: { type: "number" },
        minimum: { type: "number" },
        exclusiveMaximum: { type: "number" },
        exclusiveMinimum: { type: "number" },
        maxLength: { type: "integer", minimum: 0 },
        minLength: { type: "integer", minimum: 0 },
        pattern: { type: "string" },
        maxItems: { type: "integer", minimum: 0 },
        minItems: { type: "integer", minimum: 0 },
        uniqueItems: { type: "boolean" },
        maxProperties: { type: "integer", minimum: 0 },
        minProperties: { type: "integer", minimum: 0 },
        required: { type: "array", items: { type: "string" } },
        enum: { type: "array" },
        type: (value) => {
          return Array.isArray(value) ? {
            type: "array",
            items: { enum: ["object", "array", "string", "number", "integer", "boolean", "null"] }
          } : {
            enum: ["object", "array", "string", "number", "integer", "boolean", "null"]
          };
        },
        allOf: (0, _1.listOf)("Schema"),
        anyOf: (0, _1.listOf)("Schema"),
        oneOf: (0, _1.listOf)("Schema"),
        not: "Schema",
        if: "Schema",
        then: "Schema",
        else: "Schema",
        contains: "Schema",
        patternProperties: { type: "object" },
        propertyNames: "Schema",
        properties: "SchemaProperties",
        items: (value) => {
          return Array.isArray(value) ? (0, _1.listOf)("Schema") : "Schema";
        },
        additionalProperties: (value) => {
          return typeof value === "boolean" ? { type: "boolean" } : "Schema";
        },
        description: { type: "string" },
        format: { type: "string" },
        contentEncoding: { type: "string" },
        contentMediaType: { type: "string" },
        default: null,
        readOnly: { type: "boolean" },
        writeOnly: { type: "boolean" },
        examples: { type: "array" },
        example: { isExample: true },
        deprecated: { type: "boolean" },
        const: null,
        $comment: { type: "string" },
        additionalItems: (value) => {
          return typeof value === "boolean" ? { type: "boolean" } : "Schema";
        },
        dependencies: "Dependencies"
      }
    };
    exports.SchemaProperties = {
      properties: {},
      additionalProperties: (value) => {
        return typeof value === "boolean" ? { type: "boolean" } : "Schema";
      }
    };
    exports.DiscriminatorMapping = {
      properties: {},
      additionalProperties: (value) => {
        if ((0, ref_utils_1.isMappingRef)(value)) {
          return { type: "string", directResolveAs: "Schema" };
        } else {
          return { type: "string" };
        }
      }
    };
    exports.Discriminator = {
      properties: {
        propertyName: { type: "string" },
        mapping: "DiscriminatorMapping"
      },
      required: ["propertyName"]
    };
    var Components = {
      properties: {
        messages: "NamedMessages",
        parameters: "NamedParameters",
        schemas: "NamedSchemas",
        correlationIds: "NamedCorrelationIds",
        messageTraits: "NamedMessageTraits",
        operationTraits: "NamedOperationTraits",
        securitySchemes: "NamedSecuritySchemes",
        servers: "ServerMap",
        serverVariables: "ServerVariablesMap",
        channels: "ChannelMap",
        serverBindings: "ServerBindings",
        channelBindings: "ChannelBindings",
        operationBindings: "OperationBindings",
        messageBindings: "MessageBindings"
      }
    };
    var ImplicitFlow = {
      properties: {
        refreshUrl: { type: "string" },
        scopes: { type: "object", additionalProperties: { type: "string" } },
        // TODO: validate scopes
        authorizationUrl: { type: "string" }
      },
      required: ["authorizationUrl", "scopes"]
    };
    var PasswordFlow = {
      properties: {
        refreshUrl: { type: "string" },
        scopes: { type: "object", additionalProperties: { type: "string" } },
        // TODO: validate scopes
        tokenUrl: { type: "string" }
      },
      required: ["tokenUrl", "scopes"]
    };
    var ClientCredentials = {
      properties: {
        refreshUrl: { type: "string" },
        scopes: { type: "object", additionalProperties: { type: "string" } },
        // TODO: validate scopes
        tokenUrl: { type: "string" }
      },
      required: ["tokenUrl", "scopes"]
    };
    var AuthorizationCode = {
      properties: {
        refreshUrl: { type: "string" },
        authorizationUrl: { type: "string" },
        scopes: { type: "object", additionalProperties: { type: "string" } },
        // TODO: validate scopes
        tokenUrl: { type: "string" }
      },
      required: ["authorizationUrl", "tokenUrl", "scopes"]
    };
    exports.SecuritySchemeFlows = {
      properties: {
        implicit: "ImplicitFlow",
        password: "PasswordFlow",
        clientCredentials: "ClientCredentials",
        authorizationCode: "AuthorizationCode"
      }
    };
    var SecurityScheme = {
      properties: {
        type: {
          enum: [
            "userPassword",
            "apiKey",
            "X509",
            "symmetricEncryption",
            "asymmetricEncryption",
            "httpApiKey",
            "http",
            "oauth2",
            "openIdConnect",
            "plain",
            "scramSha256",
            "scramSha512",
            "gssapi"
          ]
        },
        description: { type: "string" },
        name: { type: "string" },
        in: { type: "string", enum: ["query", "header", "cookie", "user", "password"] },
        scheme: { type: "string" },
        bearerFormat: { type: "string" },
        flows: "SecuritySchemeFlows",
        openIdConnectUrl: { type: "string" }
      },
      required(value) {
        switch (value?.type) {
          case "apiKey":
            return ["type", "in"];
          case "httpApiKey":
            return ["type", "name", "in"];
          case "http":
            return ["type", "scheme"];
          case "oauth2":
            return ["type", "flows"];
          case "openIdConnect":
            return ["type", "openIdConnectUrl"];
          default:
            return ["type"];
        }
      },
      allowed(value) {
        switch (value?.type) {
          case "apiKey":
            return ["type", "in", "description"];
          case "httpApiKey":
            return ["type", "name", "in", "description"];
          case "http":
            return ["type", "scheme", "bearerFormat", "description"];
          case "oauth2":
            return ["type", "flows", "description"];
          case "openIdConnect":
            return ["type", "openIdConnectUrl", "description"];
          default:
            return ["type", "description"];
        }
      },
      extensionsPrefix: "x-"
    };
    exports.Dependencies = {
      properties: {},
      additionalProperties: (value) => {
        return Array.isArray(value) ? { type: "array", items: { type: "string" } } : "Schema";
      }
    };
    var HttpChannelBinding = {
      properties: {}
      // empty object
    };
    ChannelBindings.properties.http = HttpChannelBinding;
    var HttpServerBinding = {
      properties: {}
      // empty object
    };
    ServerBindings.properties.http = HttpServerBinding;
    var HttpMessageBinding = {
      properties: {
        headers: "Schema",
        bindingVersion: { type: "string" }
      }
    };
    MessageBindings.properties.http = HttpMessageBinding;
    var HttpOperationBinding = {
      properties: {
        type: { type: "string" },
        method: {
          type: "string",
          enum: ["GET", "POST", "PUT", "PATCH", "DELETE", "HEAD", "OPTIONS", "CONNECT", "TRACE"]
        },
        headers: "Schema",
        bindingVersion: { type: "string" }
      }
    };
    OperationBindings.properties.http = HttpOperationBinding;
    var WsChannelBinding = {
      properties: {
        method: { type: "string" },
        query: "Schema",
        headers: "Schema",
        bindingVersion: { type: "string" }
      }
    };
    ChannelBindings.properties.ws = WsChannelBinding;
    var WsServerBinding = {
      properties: {}
      // empty object
    };
    ServerBindings.properties.ws = WsServerBinding;
    var WsMessageBinding = {
      properties: {}
      // empty object
    };
    MessageBindings.properties.ws = WsMessageBinding;
    var WsOperationBinding = {
      properties: {}
      // empty object
    };
    OperationBindings.properties.ws = WsOperationBinding;
    var KafkaTopicConfiguration = {
      properties: {
        "cleanup.policy": { type: "array", items: { enum: ["delete", "compact"] } },
        "retention.ms": { type: "integer" },
        "retention.bytes": { type: "integer" },
        "delete.retention.ms": { type: "integer" },
        "max.message.bytes": { type: "integer" }
      }
    };
    var KafkaChannelBinding = {
      properties: {
        topic: { type: "string" },
        partitions: { type: "integer" },
        replicas: { type: "integer" },
        topicConfiguration: "KafkaTopicConfiguration",
        bindingVersion: { type: "string" }
      }
    };
    ChannelBindings.properties.kafka = KafkaChannelBinding;
    var KafkaServerBinding = {
      properties: {}
      // empty object
    };
    ServerBindings.properties.kafka = KafkaServerBinding;
    var KafkaMessageBinding = {
      properties: {
        key: "Schema",
        // TODO: add avro support
        schemaIdLocation: { type: "string" },
        schemaIdPayloadEncoding: { type: "string" },
        schemaLookupStrategy: { type: "string" },
        bindingVersion: { type: "string" }
      }
    };
    MessageBindings.properties.kafka = KafkaMessageBinding;
    var KafkaOperationBinding = {
      properties: {
        groupId: "Schema",
        clientId: "Schema",
        bindingVersion: { type: "string" }
      }
    };
    OperationBindings.properties.kafka = KafkaOperationBinding;
    var AnypointmqChannelBinding = {
      properties: {
        destination: { type: "string" },
        destinationType: { type: "string" },
        bindingVersion: { type: "string" }
      }
    };
    ChannelBindings.properties.anypointmq = AnypointmqChannelBinding;
    var AnypointmqServerBinding = {
      properties: {}
      // empty object
    };
    ServerBindings.properties.anypointmq = AnypointmqServerBinding;
    var AnypointmqMessageBinding = {
      properties: {
        headers: "Schema",
        bindingVersion: { type: "string" }
      }
    };
    MessageBindings.properties.anypointmq = AnypointmqMessageBinding;
    var AnypointmqOperationBinding = {
      properties: {}
      // empty object
    };
    OperationBindings.properties.anypointmq = AnypointmqOperationBinding;
    var AmqpChannelBinding = {
      properties: {}
      // empty object
    };
    ChannelBindings.properties.amqp = AmqpChannelBinding;
    var AmqpServerBinding = {
      properties: {}
      // empty object
    };
    ServerBindings.properties.amqp = AmqpServerBinding;
    var AmqpMessageBinding = {
      properties: {
        contentEncoding: { type: "string" },
        messageType: { type: "string" },
        bindingVersion: { type: "string" }
      }
    };
    MessageBindings.properties.amqp = AmqpMessageBinding;
    var AmqpOperationBinding = {
      // TODO: some fields are subscribe only
      properties: {
        expiration: { type: "integer" },
        userId: { type: "string" },
        cc: { type: "array", items: { type: "string" } },
        priority: { type: "integer" },
        deliveryMode: { type: "integer" },
        // TODO: enum: [1, 2]
        mandatory: { type: "boolean" },
        bcc: { type: "array", items: { type: "string" } },
        replyTo: { type: "string" },
        timestamp: { type: "boolean" },
        ack: { type: "boolean" },
        bindingVersion: { type: "string" }
      }
    };
    OperationBindings.properties.amqp = AmqpOperationBinding;
    var Amqp1ChannelBinding = {
      properties: {}
      // empty object
    };
    ChannelBindings.properties.amqp1 = Amqp1ChannelBinding;
    var Amqp1ServerBinding = {
      properties: {}
      // empty object
    };
    ServerBindings.properties.amqp1 = Amqp1ServerBinding;
    var Amqp1MessageBinding = {
      properties: {}
      // empty object
    };
    MessageBindings.properties.amqp1 = Amqp1MessageBinding;
    var Amqp1OperationBinding = {
      properties: {}
      // empty object
    };
    OperationBindings.properties.amqp1 = Amqp1OperationBinding;
    var MqttChannelBinding = {
      properties: {
        qos: { type: "integer" },
        retain: { type: "boolean" },
        bindingVersion: { type: "string" }
      }
    };
    ChannelBindings.properties.mqtt = MqttChannelBinding;
    var MqttServerBindingLastWill = {
      properties: {
        topic: { type: "string" },
        qos: { type: "integer" },
        message: { type: "string" },
        retain: { type: "boolean" }
      }
    };
    var MqttServerBinding = {
      properties: {
        clientId: { type: "string" },
        cleanSession: { type: "boolean" },
        lastWill: "MqttServerBindingLastWill",
        keepAlive: { type: "integer" },
        bindingVersion: { type: "string" }
      }
    };
    ServerBindings.properties.mqtt = MqttServerBinding;
    var MqttMessageBinding = {
      properties: {
        bindingVersion: { type: "string" }
      }
    };
    MessageBindings.properties.mqtt = MqttMessageBinding;
    var MqttOperationBinding = {
      properties: {
        qos: { type: "integer" },
        retain: { type: "boolean" },
        bindingVersion: { type: "string" }
      }
    };
    OperationBindings.properties.mqtt = MqttOperationBinding;
    var Mqtt5ChannelBinding = {
      properties: {}
      // empty object
    };
    ChannelBindings.properties.mqtt5 = Mqtt5ChannelBinding;
    var Mqtt5ServerBinding = {
      properties: {}
      // empty object
    };
    ServerBindings.properties.mqtt5 = Mqtt5ServerBinding;
    var Mqtt5MessageBinding = {
      properties: {}
      // empty object
    };
    MessageBindings.properties.mqtt5 = Mqtt5MessageBinding;
    var Mqtt5OperationBinding = {
      properties: {}
      // empty object
    };
    OperationBindings.properties.mqtt5 = Mqtt5OperationBinding;
    var NatsChannelBinding = {
      properties: {}
      // empty object
    };
    ChannelBindings.properties.nats = NatsChannelBinding;
    var NatsServerBinding = {
      properties: {}
      // empty object
    };
    ServerBindings.properties.nats = NatsServerBinding;
    var NatsMessageBinding = {
      properties: {}
      // empty object
    };
    MessageBindings.properties.nats = NatsMessageBinding;
    var NatsOperationBinding = {
      properties: {
        queue: { type: "string" },
        bindingVersion: { type: "string" }
      }
    };
    OperationBindings.properties.nats = NatsOperationBinding;
    var JmsChannelBinding = {
      properties: {
        destination: { type: "string" },
        destinationType: { type: "string" },
        bindingVersion: { type: "string" }
      }
    };
    ChannelBindings.properties.jms = JmsChannelBinding;
    var JmsServerBinding = {
      properties: {}
      // empty object
    };
    ServerBindings.properties.jms = JmsServerBinding;
    var JmsMessageBinding = {
      properties: {
        headers: "Schema",
        bindingVersion: { type: "string" }
      }
    };
    MessageBindings.properties.jms = JmsMessageBinding;
    var JmsOperationBinding = {
      properties: {
        headers: "Schema",
        bindingVersion: { type: "string" }
      }
    };
    OperationBindings.properties.jms = JmsOperationBinding;
    var SolaceChannelBinding = {
      properties: {}
      // empty object
    };
    ChannelBindings.properties.solace = SolaceChannelBinding;
    var SolaceServerBinding = {
      properties: {
        bindingVersion: { type: "string" },
        msgVpn: { type: "string" }
      }
    };
    ServerBindings.properties.solace = SolaceServerBinding;
    var SolaceMessageBinding = {
      properties: {}
      // empty object
    };
    MessageBindings.properties.solace = SolaceMessageBinding;
    var SolaceDestination = {
      properties: {
        destinationType: { type: "string", enum: ["queue", "topic"] },
        deliveryMode: { type: "string", enum: ["direct", "persistent"] },
        "queue.name": { type: "string" },
        "queue.topicSubscriptions": { type: "array", items: { type: "string" } },
        "queue.accessType": { type: "string", enum: ["exclusive", "nonexclusive"] },
        "queue.maxMsgSpoolSize": { type: "string" },
        "queue.maxTtl": { type: "string" },
        "topic.topicSubscriptions": { type: "array", items: { type: "string" } }
      }
    };
    var SolaceOperationBinding = {
      properties: {
        bindingVersion: { type: "string" },
        destinations: (0, _1.listOf)("SolaceDestination")
      }
    };
    OperationBindings.properties.solace = SolaceOperationBinding;
    var StompChannelBinding = {
      properties: {}
      // empty object
    };
    ChannelBindings.properties.stomp = StompChannelBinding;
    var StompServerBinding = {
      properties: {}
      // empty object
    };
    ServerBindings.properties.stomp = StompServerBinding;
    var StompMessageBinding = {
      properties: {}
      // empty object
    };
    MessageBindings.properties.stomp = StompMessageBinding;
    var StompOperationBinding = {
      properties: {}
      // empty object
    };
    OperationBindings.properties.stomp = StompOperationBinding;
    var RedisChannelBinding = {
      properties: {}
      // empty object
    };
    ChannelBindings.properties.redis = RedisChannelBinding;
    var RedisServerBinding = {
      properties: {}
      // empty object
    };
    ServerBindings.properties.redis = RedisServerBinding;
    var RedisMessageBinding = {
      properties: {}
      // empty object
    };
    MessageBindings.properties.redis = RedisMessageBinding;
    var RedisOperationBinding = {
      properties: {}
      // empty object
    };
    OperationBindings.properties.redis = RedisOperationBinding;
    var MercureChannelBinding = {
      properties: {}
      // empty object
    };
    ChannelBindings.properties.mercure = MercureChannelBinding;
    var MercureServerBinding = {
      properties: {}
      // empty object
    };
    ServerBindings.properties.mercure = MercureServerBinding;
    var MercureMessageBinding = {
      properties: {}
      // empty object
    };
    MessageBindings.properties.mercure = MercureMessageBinding;
    var MercureOperationBinding = {
      properties: {}
      // empty object
    };
    OperationBindings.properties.mercure = MercureOperationBinding;
    exports.AsyncApi2Bindings = {
      HttpServerBinding,
      HttpChannelBinding,
      HttpMessageBinding,
      HttpOperationBinding,
      WsServerBinding,
      WsChannelBinding,
      WsMessageBinding,
      WsOperationBinding,
      KafkaServerBinding,
      KafkaTopicConfiguration,
      KafkaChannelBinding,
      KafkaMessageBinding,
      KafkaOperationBinding,
      AnypointmqServerBinding,
      AnypointmqChannelBinding,
      AnypointmqMessageBinding,
      AnypointmqOperationBinding,
      AmqpServerBinding,
      AmqpChannelBinding,
      AmqpMessageBinding,
      AmqpOperationBinding,
      Amqp1ServerBinding,
      Amqp1ChannelBinding,
      Amqp1MessageBinding,
      Amqp1OperationBinding,
      MqttServerBindingLastWill,
      MqttServerBinding,
      MqttChannelBinding,
      MqttMessageBinding,
      MqttOperationBinding,
      Mqtt5ServerBinding,
      Mqtt5ChannelBinding,
      Mqtt5MessageBinding,
      Mqtt5OperationBinding,
      NatsServerBinding,
      NatsChannelBinding,
      NatsMessageBinding,
      NatsOperationBinding,
      JmsServerBinding,
      JmsChannelBinding,
      JmsMessageBinding,
      JmsOperationBinding,
      SolaceServerBinding,
      SolaceChannelBinding,
      SolaceMessageBinding,
      SolaceDestination,
      SolaceOperationBinding,
      StompServerBinding,
      StompChannelBinding,
      StompMessageBinding,
      StompOperationBinding,
      RedisServerBinding,
      RedisChannelBinding,
      RedisMessageBinding,
      RedisOperationBinding,
      MercureServerBinding,
      MercureChannelBinding,
      MercureMessageBinding,
      MercureOperationBinding,
      ServerBindings,
      ChannelBindings,
      MessageBindings,
      OperationBindings
    };
    exports.AsyncApi2Types = {
      ...exports.AsyncApi2Bindings,
      Root,
      Tag: exports.Tag,
      TagList: (0, _1.listOf)("Tag"),
      ServerMap: exports.ServerMap,
      ExternalDocs: exports.ExternalDocs,
      Server,
      ServerVariable: exports.ServerVariable,
      ServerVariablesMap: (0, _1.mapOf)("ServerVariable"),
      SecurityRequirement,
      SecurityRequirementList: (0, _1.listOf)("SecurityRequirement"),
      Info,
      Contact: exports.Contact,
      License: exports.License,
      ChannelMap,
      Channel,
      Parameter,
      ParametersMap: (0, _1.mapOf)("Parameter"),
      Operation,
      Schema: exports.Schema,
      MessageExample: exports.MessageExample,
      SchemaProperties: exports.SchemaProperties,
      DiscriminatorMapping: exports.DiscriminatorMapping,
      Discriminator: exports.Discriminator,
      Components,
      NamedSchemas: (0, _1.mapOf)("Schema"),
      NamedMessages: (0, _1.mapOf)("Message"),
      NamedMessageTraits: (0, _1.mapOf)("MessageTrait"),
      NamedOperationTraits: (0, _1.mapOf)("OperationTrait"),
      NamedParameters: (0, _1.mapOf)("Parameter"),
      NamedSecuritySchemes: (0, _1.mapOf)("SecurityScheme"),
      NamedCorrelationIds: (0, _1.mapOf)("CorrelationId"),
      ImplicitFlow,
      PasswordFlow,
      ClientCredentials,
      AuthorizationCode,
      SecuritySchemeFlows: exports.SecuritySchemeFlows,
      SecurityScheme,
      Message,
      MessageBindings,
      OperationBindings,
      OperationTrait,
      OperationTraitList: (0, _1.listOf)("OperationTrait"),
      MessageTrait,
      MessageTraitList: (0, _1.listOf)("MessageTrait"),
      MessageExampleList: (0, _1.listOf)("MessageExample"),
      CorrelationId: exports.CorrelationId,
      Dependencies: exports.Dependencies
    };
  }
});

// node_modules/@redocly/openapi-core/lib/types/asyncapi3.js
var require_asyncapi3 = __commonJS({
  "node_modules/@redocly/openapi-core/lib/types/asyncapi3.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.AsyncApi3Types = void 0;
    var _1 = require_types();
    var asyncapi2_1 = require_asyncapi2();
    var Root = {
      properties: {
        asyncapi: { type: "string", enum: ["3.0.0"] },
        info: "Info",
        id: { type: "string" },
        servers: "ServerMap",
        channels: "NamedChannels",
        components: "Components",
        operations: "NamedOperations",
        defaultContentType: { type: "string" }
      },
      required: ["asyncapi", "info"]
    };
    var Channel = {
      properties: {
        address: { type: "string" },
        messages: "NamedMessages",
        title: { type: "string" },
        summary: { type: "string" },
        description: { type: "string" },
        servers: "ServerList",
        parameters: "ParametersMap",
        bindings: "ChannelBindings",
        tags: "TagList",
        externalDocs: "ExternalDocs"
      }
    };
    var Server = {
      properties: {
        host: { type: "string" },
        pathname: { type: "string" },
        protocol: { type: "string" },
        protocolVersion: { type: "string" },
        description: { type: "string" },
        variables: "ServerVariablesMap",
        security: "SecuritySchemeList",
        bindings: "ServerBindings",
        externalDocs: "ExternalDocs",
        tags: "TagList"
      },
      required: ["host", "protocol"]
    };
    var Info = {
      properties: {
        title: { type: "string" },
        version: { type: "string" },
        description: { type: "string" },
        termsOfService: { type: "string" },
        contact: "Contact",
        license: "License",
        tags: "TagList",
        externalDocs: "ExternalDocs"
      },
      required: ["title", "version"]
    };
    var Parameter = {
      properties: {
        description: { type: "string" },
        enum: { type: "array", items: { type: "string" } },
        default: { type: "string" },
        examples: { type: "array", items: { type: "string" } },
        location: { type: "string" }
      }
    };
    var Message = {
      properties: {
        headers: "Schema",
        payload: (value) => {
          if (!!value && value?.["schemaFormat"]) {
            return {
              properties: {
                schema: "Schema",
                schemaFormat: { type: "string" }
              },
              required: ["schema", "schemaFormat"]
            };
          } else {
            return "Schema";
          }
        },
        correlationId: "CorrelationId",
        contentType: { type: "string" },
        name: { type: "string" },
        title: { type: "string" },
        summary: { type: "string" },
        description: { type: "string" },
        tags: "TagList",
        externalDocs: "ExternalDocs",
        bindings: "MessageBindings",
        examples: "MessageExampleList",
        traits: "MessageTraitList"
      },
      additionalProperties: {}
    };
    var OperationTrait = {
      properties: {
        tags: "TagList",
        title: { type: "string" },
        summary: { type: "string" },
        description: { type: "string" },
        externalDocs: "ExternalDocs",
        security: "SecuritySchemeList",
        bindings: "OperationBindings"
      },
      required: []
    };
    var MessageTrait = {
      properties: {
        headers: (value) => {
          if (typeof value === "function" || typeof value === "object" && !!value) {
            return {
              properties: {
                schema: "Schema",
                schemaFormat: { type: "string" }
              }
            };
          } else {
            return "Schema";
          }
        },
        correlationId: "CorrelationId",
        contentType: { type: "string" },
        name: { type: "string" },
        title: { type: "string" },
        summary: { type: "string" },
        description: { type: "string" },
        tags: "TagList",
        externalDocs: "ExternalDocs",
        bindings: "MessageBindings",
        examples: "MessageExampleList"
      },
      additionalProperties: {}
    };
    var Operation = {
      properties: {
        action: { type: "string", enum: ["send", "receive"] },
        channel: "Channel",
        title: { type: "string" },
        tags: "TagList",
        summary: { type: "string" },
        description: { type: "string" },
        externalDocs: "ExternalDocs",
        operationId: { type: "string" },
        security: "SecuritySchemeList",
        bindings: "OperationBindings",
        traits: "OperationTraitList",
        messages: "MessageList",
        reply: "OperationReply"
      },
      required: ["action", "channel"]
    };
    var OperationReply = {
      properties: {
        channel: "Channel",
        messages: "MessageList",
        address: "OperationReplyAddress"
      }
    };
    var OperationReplyAddress = {
      properties: {
        location: { type: "string" },
        description: { type: "string" }
      },
      required: ["location"]
    };
    var Components = {
      properties: {
        messages: "NamedMessages",
        parameters: "NamedParameters",
        schemas: "NamedSchemas",
        replies: "NamedOperationReplies",
        replyAddresses: "NamedOperationRelyAddresses",
        correlationIds: "NamedCorrelationIds",
        messageTraits: "NamedMessageTraits",
        operationTraits: "NamedOperationTraits",
        tags: "NamedTags",
        externalDocs: "NamedExternalDocs",
        securitySchemes: "NamedSecuritySchemes",
        servers: "ServerMap",
        serverVariables: "ServerVariablesMap",
        channels: "NamedChannels",
        operations: "NamedOperations",
        serverBindings: "ServerBindings",
        channelBindings: "ChannelBindings",
        operationBindings: "OperationBindings",
        messageBindings: "MessageBindings"
      }
    };
    var ImplicitFlow = {
      properties: {
        refreshUrl: { type: "string" },
        availableScopes: { type: "object", additionalProperties: { type: "string" } },
        authorizationUrl: { type: "string" }
      },
      required: ["authorizationUrl", "availableScopes"]
    };
    var PasswordFlow = {
      properties: {
        refreshUrl: { type: "string" },
        availableScopes: { type: "object", additionalProperties: { type: "string" } },
        tokenUrl: { type: "string" }
      },
      required: ["tokenUrl", "availableScopes"]
    };
    var ClientCredentials = {
      properties: {
        refreshUrl: { type: "string" },
        availableScopes: { type: "object", additionalProperties: { type: "string" } },
        tokenUrl: { type: "string" }
      },
      required: ["tokenUrl", "availableScopes"]
    };
    var AuthorizationCode = {
      properties: {
        refreshUrl: { type: "string" },
        authorizationUrl: { type: "string" },
        availableScopes: { type: "object", additionalProperties: { type: "string" } },
        tokenUrl: { type: "string" }
      },
      required: ["authorizationUrl", "tokenUrl", "availableScopes"]
    };
    var SecurityScheme = {
      properties: {
        type: {
          enum: [
            "userPassword",
            "apiKey",
            "X509",
            "symmetricEncryption",
            "asymmetricEncryption",
            "httpApiKey",
            "http",
            "oauth2",
            "openIdConnect",
            "plain",
            "scramSha256",
            "scramSha512",
            "gssapi"
          ]
        },
        description: { type: "string" },
        name: { type: "string" },
        in: { type: "string", enum: ["query", "header", "cookie", "user", "password"] },
        scheme: { type: "string" },
        bearerFormat: { type: "string" },
        flows: "SecuritySchemeFlows",
        openIdConnectUrl: { type: "string" },
        scopes: { type: "array", items: { type: "string" } }
      },
      required(value) {
        switch (value?.type) {
          case "apiKey":
            return ["type", "in"];
          case "httpApiKey":
            return ["type", "name", "in"];
          case "http":
            return ["type", "scheme"];
          case "oauth2":
            return ["type", "flows"];
          case "openIdConnect":
            return ["type", "openIdConnectUrl"];
          default:
            return ["type"];
        }
      },
      allowed(value) {
        switch (value?.type) {
          case "apiKey":
            return ["type", "in", "description"];
          case "httpApiKey":
            return ["type", "name", "in", "description"];
          case "http":
            return ["type", "scheme", "bearerFormat", "description"];
          case "oauth2":
            return ["type", "flows", "description", "scopes"];
          case "openIdConnect":
            return ["type", "openIdConnectUrl", "description", "scopes"];
          default:
            return ["type", "description"];
        }
      },
      extensionsPrefix: "x-"
    };
    exports.AsyncApi3Types = {
      // from asyncapi2
      ...asyncapi2_1.AsyncApi2Bindings,
      CorrelationId: asyncapi2_1.CorrelationId,
      SecuritySchemeFlows: asyncapi2_1.SecuritySchemeFlows,
      ServerVariable: asyncapi2_1.ServerVariable,
      Contact: asyncapi2_1.Contact,
      License: asyncapi2_1.License,
      MessageExample: asyncapi2_1.MessageExample,
      Tag: asyncapi2_1.Tag,
      Dependencies: asyncapi2_1.Dependencies,
      Schema: asyncapi2_1.Schema,
      Discriminator: asyncapi2_1.Discriminator,
      DiscriminatorMapping: asyncapi2_1.DiscriminatorMapping,
      SchemaProperties: asyncapi2_1.SchemaProperties,
      ServerMap: asyncapi2_1.ServerMap,
      ExternalDocs: asyncapi2_1.ExternalDocs,
      Root,
      Channel,
      Parameter,
      Info,
      Server,
      MessageTrait,
      Operation,
      OperationReply,
      OperationReplyAddress,
      Components,
      ImplicitFlow,
      PasswordFlow,
      ClientCredentials,
      AuthorizationCode,
      SecurityScheme,
      Message,
      OperationTrait,
      ServerVariablesMap: (0, _1.mapOf)("ServerVariable"),
      NamedTags: (0, _1.mapOf)("Tag"),
      NamedExternalDocs: (0, _1.mapOf)("ExternalDocs"),
      NamedChannels: (0, _1.mapOf)("Channel"),
      ParametersMap: (0, _1.mapOf)("Parameter"),
      NamedOperations: (0, _1.mapOf)("Operation"),
      NamedOperationReplies: (0, _1.mapOf)("OperationReply"),
      NamedOperationRelyAddresses: (0, _1.mapOf)("OperationReplyAddress"),
      NamedSchemas: (0, _1.mapOf)("Schema"),
      NamedMessages: (0, _1.mapOf)("Message"),
      NamedMessageTraits: (0, _1.mapOf)("MessageTrait"),
      NamedOperationTraits: (0, _1.mapOf)("OperationTrait"),
      NamedParameters: (0, _1.mapOf)("Parameter"),
      NamedSecuritySchemes: (0, _1.mapOf)("SecurityScheme"),
      NamedCorrelationIds: (0, _1.mapOf)("CorrelationId"),
      ServerList: (0, _1.listOf)("Server"),
      SecuritySchemeList: (0, _1.listOf)("SecurityScheme"),
      MessageList: (0, _1.listOf)("Message"),
      OperationTraitList: (0, _1.listOf)("OperationTrait"),
      MessageTraitList: (0, _1.listOf)("MessageTrait"),
      MessageExampleList: (0, _1.listOf)("MessageExample"),
      TagList: (0, _1.listOf)("Tag")
    };
  }
});

// node_modules/@redocly/openapi-core/lib/types/arazzo.js
var require_arazzo = __commonJS({
  "node_modules/@redocly/openapi-core/lib/types/arazzo.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Arazzo1Types = void 0;
    var _1 = require_types();
    var oas3_1_1 = require_oas3_1();
    var oas3_1 = require_oas3();
    var Root = {
      properties: {
        arazzo: { type: "string" },
        info: "Info",
        sourceDescriptions: "SourceDescriptions",
        workflows: "Workflows",
        components: "Components"
      },
      required: ["arazzo", "info", "sourceDescriptions", "workflows"],
      extensionsPrefix: "x-"
    };
    var NamedParameters = {
      properties: {},
      additionalProperties: "Parameter"
    };
    var NamedSuccessActions = {
      properties: {},
      additionalProperties: "SuccessActionObject"
    };
    var NamedFailureActions = {
      properties: {},
      additionalProperties: "FailureActionObject"
    };
    var Components = {
      properties: {
        inputs: "NamedInputs",
        parameters: "NamedParameters",
        successActions: "NamedSuccessActions",
        failureActions: "NamedFailureActions"
      },
      extensionsPrefix: "x-"
    };
    var NamedInputs = (0, _1.mapOf)("Schema");
    var Info = {
      properties: {
        title: { type: "string" },
        description: { type: "string" },
        summary: { type: "string" },
        version: { type: "string" }
      },
      required: ["title", "version"],
      extensionsPrefix: "x-"
    };
    var SourceDescriptions = {
      properties: {},
      items: (value) => {
        if (value?.type === "openapi") {
          return "OpenAPISourceDescription";
        } else {
          return "ArazzoSourceDescription";
        }
      }
    };
    var OpenAPISourceDescription = {
      properties: {
        name: { type: "string" },
        type: { type: "string", enum: ["openapi"] },
        url: { type: "string" },
        "x-serverUrl": { type: "string" }
      },
      required: ["name", "type", "url"],
      extensionsPrefix: "x-"
    };
    var ArazzoSourceDescription = {
      properties: {
        name: { type: "string" },
        type: { type: "string", enum: ["arazzo"] },
        url: { type: "string" }
      },
      required: ["name", "type", "url"],
      extensionsPrefix: "x-"
    };
    var ReusableObject = {
      properties: {
        reference: { type: "string" },
        value: {}
        // any
      },
      required: ["reference"],
      extensionsPrefix: "x-"
    };
    var Parameter = {
      properties: {
        in: { type: "string", enum: ["header", "query", "path", "cookie"] },
        name: { type: "string" },
        value: {}
        // any
      },
      required: ["name", "value"],
      extensionsPrefix: "x-"
    };
    var Parameters = {
      properties: {},
      items: (value) => {
        if (value?.reference) {
          return "ReusableObject";
        } else {
          return "Parameter";
        }
      }
    };
    var Workflow = {
      properties: {
        workflowId: { type: "string" },
        summary: { type: "string" },
        description: { type: "string" },
        parameters: "Parameters",
        dependsOn: { type: "array", items: { type: "string" } },
        inputs: "Schema",
        outputs: "Outputs",
        steps: "Steps",
        successActions: "OnSuccessActionList",
        failureActions: "OnFailureActionList"
      },
      required: ["workflowId", "steps"],
      extensionsPrefix: "x-"
    };
    var Workflows = (0, _1.listOf)("Workflow");
    var Steps = (0, _1.listOf)("Step");
    var Step = {
      properties: {
        stepId: { type: "string" },
        description: { type: "string" },
        operationId: { type: "string" },
        operationPath: { type: "string" },
        workflowId: { type: "string" },
        parameters: "Parameters",
        successCriteria: (0, _1.listOf)("CriterionObject"),
        onSuccess: "OnSuccessActionList",
        onFailure: "OnFailureActionList",
        outputs: "Outputs",
        "x-operation": "ExtendedOperation",
        requestBody: "RequestBody"
      },
      required: ["stepId"],
      requiredOneOf: ["x-operation", "operationId", "operationPath", "workflowId"],
      extensionsPrefix: "x-"
    };
    var Outputs = {
      properties: {},
      additionalProperties: {
        type: "string"
      }
    };
    var RequestBody = {
      properties: {
        contentType: { type: "string" },
        payload: {},
        replacements: (0, _1.listOf)("Replacement")
      },
      required: ["payload"],
      extensionsPrefix: "x-"
    };
    var Replacement = {
      properties: {
        target: { type: "string" },
        value: {}
      },
      required: ["target", "value"],
      extensionsPrefix: "x-"
    };
    var ExtendedOperation = {
      properties: {
        url: { type: "string" },
        method: {
          enum: [
            "get",
            "post",
            "put",
            "delete",
            "patch",
            "head",
            "options",
            "trace",
            "connect",
            "query",
            "GET",
            "POST",
            "PUT",
            "DELETE",
            "PATCH",
            "OPTIONS",
            "HEAD",
            "TRACE",
            "CONNECT",
            "QUERY"
          ]
        }
      },
      required: ["url", "method"]
    };
    var CriterionObject = {
      properties: {
        condition: { type: "string" },
        context: { type: "string" },
        type: (value) => {
          if (!value) {
            return void 0;
          } else if (typeof value === "string") {
            return { enum: ["regex", "jsonpath", "simple", "xpath"] };
          } else if (value?.type === "jsonpath") {
            return "JSONPathCriterion";
          } else {
            return "XPathCriterion";
          }
        }
      },
      required: ["condition"]
    };
    var JSONPathCriterion = {
      properties: {
        type: { type: "string", enum: ["jsonpath"] },
        version: { type: "string", enum: ["draft-goessner-dispatch-jsonpath-00"] }
      }
    };
    var XPathCriterion = {
      properties: {
        type: { type: "string", enum: ["xpath"] },
        version: { type: "string", enum: ["xpath-30", "xpath-20", "xpath-10"] }
      }
    };
    var SuccessActionObject = {
      properties: {
        name: { type: "string" },
        type: { type: "string", enum: ["goto", "end"] },
        stepId: { type: "string" },
        workflowId: { type: "string" },
        criteria: (0, _1.listOf)("CriterionObject")
      },
      required: ["type", "name"]
    };
    var OnSuccessActionList = {
      properties: {},
      items: (value) => {
        if (value?.type && value?.name) {
          return "SuccessActionObject";
        } else {
          return "ReusableObject";
        }
      }
    };
    var FailureActionObject = {
      properties: {
        name: { type: "string" },
        type: { type: "string", enum: ["goto", "retry", "end"] },
        workflowId: { type: "string" },
        stepId: { type: "string" },
        retryAfter: { type: "number" },
        retryLimit: { type: "number" },
        criteria: (0, _1.listOf)("CriterionObject")
      },
      required: ["type", "name"]
    };
    var OnFailureActionList = {
      properties: {},
      items: (value) => {
        if (value?.type && value?.name) {
          return "FailureActionObject";
        } else {
          return "ReusableObject";
        }
      }
    };
    exports.Arazzo1Types = {
      Root,
      Info,
      SourceDescriptions,
      OpenAPISourceDescription,
      ArazzoSourceDescription,
      Parameters,
      Parameter,
      ReusableObject,
      Workflows,
      Workflow,
      Steps,
      Step,
      RequestBody,
      Replacement,
      ExtendedOperation,
      Outputs,
      CriterionObject,
      XPathCriterion,
      JSONPathCriterion,
      SuccessActionObject,
      OnSuccessActionList,
      FailureActionObject,
      OnFailureActionList,
      Schema: oas3_1_1.Schema,
      NamedSchemas: (0, _1.mapOf)("Schema"),
      ExternalDocs: oas3_1.ExternalDocs,
      DiscriminatorMapping: oas3_1.DiscriminatorMapping,
      Discriminator: oas3_1.Discriminator,
      DependentRequired: oas3_1_1.DependentRequired,
      SchemaProperties: oas3_1_1.SchemaProperties,
      PatternProperties: oas3_1_1.SchemaProperties,
      Components,
      NamedInputs,
      NamedParameters,
      NamedSuccessActions,
      NamedFailureActions,
      Xml: oas3_1.Xml
    };
  }
});

// node_modules/@redocly/openapi-core/lib/types/overlay.js
var require_overlay = __commonJS({
  "node_modules/@redocly/openapi-core/lib/types/overlay.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Overlay1Types = void 0;
    var _1 = require_types();
    var Root = {
      properties: {
        overlay: { type: "string" },
        info: "Info",
        extends: { type: "string" },
        actions: "Actions"
      },
      required: ["overlay", "info", "actions"],
      extensionsPrefix: "x-"
    };
    var Info = {
      properties: {
        title: { type: "string" },
        version: { type: "string" }
      },
      required: ["title", "version"],
      extensionsPrefix: "x-"
    };
    var Actions = (0, _1.listOf)("Action");
    var Action = {
      properties: {
        target: { type: "string" },
        description: { type: "string" },
        update: {},
        // any
        remove: { type: "boolean" }
      },
      required: ["target"],
      extensionsPrefix: "x-"
    };
    exports.Overlay1Types = {
      Root,
      Info,
      Actions,
      Action
    };
  }
});

// node_modules/@redocly/openapi-core/lib/typings/arazzo.js
var require_arazzo2 = __commonJS({
  "node_modules/@redocly/openapi-core/lib/typings/arazzo.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ARAZZO_VERSIONS_SUPPORTED_BY_RESPECT = exports.VERSION_PATTERN = void 0;
    exports.VERSION_PATTERN = /^1\.0\.\d+(-.+)?$/;
    exports.ARAZZO_VERSIONS_SUPPORTED_BY_RESPECT = ["1.0.1"];
  }
});

// node_modules/@redocly/openapi-core/lib/oas-types.js
var require_oas_types = __commonJS({
  "node_modules/@redocly/openapi-core/lib/oas-types.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.SpecMajorVersion = exports.SpecVersion = void 0;
    exports.detectSpec = detectSpec;
    exports.getMajorSpecVersion = getMajorSpecVersion;
    exports.getTypes = getTypes;
    var oas2_1 = require_oas2();
    var oas3_1 = require_oas3();
    var oas3_1_1 = require_oas3_1();
    var asyncapi2_1 = require_asyncapi2();
    var asyncapi3_1 = require_asyncapi3();
    var arazzo_1 = require_arazzo();
    var overlay_1 = require_overlay();
    var utils_1 = require_utils();
    var arazzo_2 = require_arazzo2();
    var SpecVersion;
    (function(SpecVersion2) {
      SpecVersion2["OAS2"] = "oas2";
      SpecVersion2["OAS3_0"] = "oas3_0";
      SpecVersion2["OAS3_1"] = "oas3_1";
      SpecVersion2["Async2"] = "async2";
      SpecVersion2["Async3"] = "async3";
      SpecVersion2["Arazzo1"] = "arazzo1";
      SpecVersion2["Overlay1"] = "overlay1";
    })(SpecVersion || (exports.SpecVersion = SpecVersion = {}));
    var SpecMajorVersion;
    (function(SpecMajorVersion2) {
      SpecMajorVersion2["OAS2"] = "oas2";
      SpecMajorVersion2["OAS3"] = "oas3";
      SpecMajorVersion2["Async2"] = "async2";
      SpecMajorVersion2["Async3"] = "async3";
      SpecMajorVersion2["Arazzo1"] = "arazzo1";
      SpecMajorVersion2["Overlay1"] = "overlay1";
    })(SpecMajorVersion || (exports.SpecMajorVersion = SpecMajorVersion = {}));
    var typesMap = {
      [SpecVersion.OAS2]: oas2_1.Oas2Types,
      [SpecVersion.OAS3_0]: oas3_1.Oas3Types,
      [SpecVersion.OAS3_1]: oas3_1_1.Oas3_1Types,
      [SpecVersion.Async2]: asyncapi2_1.AsyncApi2Types,
      [SpecVersion.Async3]: asyncapi3_1.AsyncApi3Types,
      [SpecVersion.Arazzo1]: arazzo_1.Arazzo1Types,
      [SpecVersion.Overlay1]: overlay_1.Overlay1Types
    };
    function detectSpec(root) {
      if (!(0, utils_1.isPlainObject)(root)) {
        throw new Error(`Document must be JSON object, got ${typeof root}`);
      }
      if (root.openapi && typeof root.openapi !== "string") {
        throw new Error(`Invalid OpenAPI version: should be a string but got "${typeof root.openapi}"`);
      }
      if (typeof root.openapi === "string" && root.openapi.startsWith("3.0")) {
        return SpecVersion.OAS3_0;
      }
      if (typeof root.openapi === "string" && root.openapi.startsWith("3.1")) {
        return SpecVersion.OAS3_1;
      }
      if (root.swagger && root.swagger === "2.0") {
        return SpecVersion.OAS2;
      }
      if (root.openapi || root.swagger) {
        throw new Error(`Unsupported OpenAPI version: ${root.openapi || root.swagger}`);
      }
      if (typeof root.asyncapi === "string" && root.asyncapi.startsWith("2.")) {
        return SpecVersion.Async2;
      }
      if (typeof root.asyncapi === "string" && root.asyncapi.startsWith("3.")) {
        return SpecVersion.Async3;
      }
      if (root.asyncapi) {
        throw new Error(`Unsupported AsyncAPI version: ${root.asyncapi}`);
      }
      if (typeof root.arazzo === "string" && arazzo_2.VERSION_PATTERN.test(root.arazzo)) {
        return SpecVersion.Arazzo1;
      }
      if (typeof root.overlay === "string" && arazzo_2.VERSION_PATTERN.test(root.overlay)) {
        return SpecVersion.Overlay1;
      }
      throw new Error(`Unsupported specification`);
    }
    function getMajorSpecVersion(version) {
      if (version === SpecVersion.OAS2) {
        return SpecMajorVersion.OAS2;
      } else if (version === SpecVersion.Async2) {
        return SpecMajorVersion.Async2;
      } else if (version === SpecVersion.Async3) {
        return SpecMajorVersion.Async3;
      } else if (version === SpecVersion.Arazzo1) {
        return SpecMajorVersion.Arazzo1;
      } else if (version === SpecVersion.Overlay1) {
        return SpecMajorVersion.Overlay1;
      } else {
        return SpecMajorVersion.OAS3;
      }
    }
    function getTypes(spec) {
      return typesMap[spec];
    }
  }
});

// node_modules/@redocly/openapi-core/lib/config/rules.js
var require_rules = __commonJS({
  "node_modules/@redocly/openapi-core/lib/config/rules.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.initRules = initRules;
    var utils_1 = require_utils();
    function initRules(rules, config, type, oasVersion) {
      return rules.flatMap((ruleset2) => Object.keys(ruleset2).map((ruleId) => {
        const rule = ruleset2[ruleId];
        const ruleSettings = type === "rules" ? config.getRuleSettings(ruleId, oasVersion) : type === "preprocessors" ? config.getPreprocessorSettings(ruleId, oasVersion) : config.getDecoratorSettings(ruleId, oasVersion);
        if (ruleSettings.severity === "off") {
          return void 0;
        }
        const severity = ruleSettings.severity;
        const message = ruleSettings.message;
        const visitors = rule(ruleSettings);
        if (Array.isArray(visitors)) {
          return visitors.map((visitor) => ({
            severity,
            ruleId,
            message,
            visitor
          }));
        }
        return {
          severity,
          message,
          ruleId,
          visitor: visitors
          // note: actually it is only one visitor object
        };
      })).flatMap((visitor) => visitor).filter(utils_1.isDefined);
    }
  }
});

// node_modules/@redocly/openapi-core/lib/rules/no-unresolved-refs.js
var require_no_unresolved_refs = __commonJS({
  "node_modules/@redocly/openapi-core/lib/rules/no-unresolved-refs.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.NoUnresolvedRefs = void 0;
    exports.reportUnresolvedRef = reportUnresolvedRef;
    var resolve_1 = require_resolve();
    var NoUnresolvedRefs = () => {
      return {
        ref: {
          leave(_15, { report, location }, resolved) {
            if (resolved.node !== void 0)
              return;
            reportUnresolvedRef(resolved, report, location);
          }
        },
        DiscriminatorMapping(mapping, { report, resolve, location }) {
          for (const mappingName of Object.keys(mapping)) {
            const resolved = resolve({ $ref: mapping[mappingName] });
            if (resolved.node !== void 0)
              return;
            reportUnresolvedRef(resolved, report, location.child(mappingName));
          }
        }
      };
    };
    exports.NoUnresolvedRefs = NoUnresolvedRefs;
    function reportUnresolvedRef(resolved, report, location) {
      const error = resolved.error;
      if (error instanceof resolve_1.YamlParseError) {
        report({
          message: "Failed to parse: " + error.message,
          location: {
            source: error.source,
            pointer: void 0,
            start: {
              col: error.col,
              line: error.line
            }
          }
        });
      }
      const message = resolved.error?.message;
      report({
        location,
        message: `Can't resolve $ref${message ? ": " + message : ""}`
      });
    }
  }
});

// node_modules/@redocly/openapi-core/lib/redocly/domains.js
var require_domains = __commonJS({
  "node_modules/@redocly/openapi-core/lib/redocly/domains.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.AVAILABLE_REGIONS = exports.DOMAINS = exports.DEFAULT_REGION = void 0;
    exports.getDomains = getDomains;
    exports.setRedoclyDomain = setRedoclyDomain;
    exports.getRedoclyDomain = getRedoclyDomain;
    exports.isRedoclyRegistryURL = isRedoclyRegistryURL;
    var redoclyDomain = "redocly.com";
    exports.DEFAULT_REGION = "us";
    exports.DOMAINS = getDomains();
    exports.AVAILABLE_REGIONS = Object.keys(exports.DOMAINS);
    function getDomains() {
      const domains = {
        us: "redocly.com",
        eu: "eu.redocly.com"
      };
      const domain = redoclyDomain;
      if (domain?.endsWith(".redocly.host")) {
        domains[domain.split(".")[0]] = domain;
      }
      if (domain === "redoc.online") {
        domains[domain] = domain;
      }
      return domains;
    }
    function setRedoclyDomain(domain) {
      redoclyDomain = domain;
    }
    function getRedoclyDomain() {
      return redoclyDomain;
    }
    function isRedoclyRegistryURL(link) {
      const domain = getRedoclyDomain() || exports.DOMAINS[exports.DEFAULT_REGION];
      const legacyDomain = domain === "redocly.com" ? "redoc.ly" : domain;
      if (!link.startsWith(`https://api.${domain}/registry/`) && !link.startsWith(`https://api.${legacyDomain}/registry/`)) {
        return false;
      }
      return true;
    }
  }
});

// node_modules/@redocly/openapi-core/lib/decorators/oas2/remove-unused-components.js
var require_remove_unused_components = __commonJS({
  "node_modules/@redocly/openapi-core/lib/decorators/oas2/remove-unused-components.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.RemoveUnusedComponents = void 0;
    var utils_1 = require_utils();
    var RemoveUnusedComponents = () => {
      const components = /* @__PURE__ */ new Map();
      function registerComponent(location, componentType, name) {
        components.set(location.absolutePointer, {
          usedIn: components.get(location.absolutePointer)?.usedIn ?? [],
          componentType,
          name
        });
      }
      function removeUnusedComponents(root, removedPaths) {
        const removedLengthStart = removedPaths.length;
        for (const [path, { usedIn, name, componentType }] of components) {
          const used = usedIn.some((location) => !removedPaths.some((removed) => (
            // Check if the current location's absolute pointer starts with the 'removed' path
            // and either its length matches exactly with 'removed' or the character after the 'removed' path is a '/'
            location.absolutePointer.startsWith(removed) && (location.absolutePointer.length === removed.length || location.absolutePointer[removed.length] === "/")
          )));
          if (!used && componentType) {
            removedPaths.push(path);
            delete root[componentType][name];
            components.delete(path);
            if ((0, utils_1.isEmptyObject)(root[componentType])) {
              delete root[componentType];
            }
          }
        }
        return removedPaths.length > removedLengthStart ? removeUnusedComponents(root, removedPaths) : removedPaths.length;
      }
      return {
        ref: {
          leave(ref, { location, type, resolve, key: key2 }) {
            if (["Schema", "Parameter", "Response", "SecurityScheme"].includes(type.name)) {
              const resolvedRef = resolve(ref);
              if (!resolvedRef.location)
                return;
              const [fileLocation, localPointer] = resolvedRef.location.absolutePointer.split("#", 2);
              const componentLevelLocalPointer = localPointer.split("/").slice(0, 3).join("/");
              const pointer = `${fileLocation}#${componentLevelLocalPointer}`;
              const registered = components.get(pointer);
              if (registered) {
                registered.usedIn.push(location);
              } else {
                components.set(pointer, {
                  usedIn: [location],
                  name: key2.toString()
                });
              }
            }
          }
        },
        Root: {
          leave(root, ctx) {
            const data = ctx.getVisitorData();
            data.removedCount = removeUnusedComponents(root, []);
          }
        },
        NamedSchemas: {
          Schema(schema4, { location, key: key2 }) {
            if (!schema4.allOf) {
              registerComponent(location, "definitions", key2.toString());
            }
          }
        },
        NamedParameters: {
          Parameter(_parameter, { location, key: key2 }) {
            registerComponent(location, "parameters", key2.toString());
          }
        },
        NamedResponses: {
          Response(_response, { location, key: key2 }) {
            registerComponent(location, "responses", key2.toString());
          }
        },
        NamedSecuritySchemes: {
          SecurityScheme(_securityScheme, { location, key: key2 }) {
            registerComponent(location, "securityDefinitions", key2.toString());
          }
        }
      };
    };
    exports.RemoveUnusedComponents = RemoveUnusedComponents;
  }
});

// node_modules/@redocly/openapi-core/lib/decorators/oas3/remove-unused-components.js
var require_remove_unused_components2 = __commonJS({
  "node_modules/@redocly/openapi-core/lib/decorators/oas3/remove-unused-components.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.RemoveUnusedComponents = void 0;
    var utils_1 = require_utils();
    var RemoveUnusedComponents = () => {
      const components = /* @__PURE__ */ new Map();
      function registerComponent(location, componentType, name) {
        components.set(location.absolutePointer, {
          usedIn: components.get(location.absolutePointer)?.usedIn ?? [],
          componentType,
          name
        });
      }
      function removeUnusedComponents(root, removedPaths) {
        const removedLengthStart = removedPaths.length;
        for (const [path, { usedIn, name, componentType }] of components) {
          const used = usedIn.some((location) => !removedPaths.some((removed) => location.absolutePointer.startsWith(removed) && (location.absolutePointer.length === removed.length || location.absolutePointer[removed.length] === "/")));
          if (!used && componentType && root.components) {
            removedPaths.push(path);
            const componentChild = root.components[componentType];
            delete componentChild[name];
            components.delete(path);
            if ((0, utils_1.isEmptyObject)(componentChild)) {
              delete root.components[componentType];
            }
          }
        }
        return removedPaths.length > removedLengthStart ? removeUnusedComponents(root, removedPaths) : removedPaths.length;
      }
      return {
        ref: {
          leave(ref, { location, type, resolve, key: key2 }) {
            if (["Schema", "Header", "Parameter", "Response", "Example", "RequestBody"].includes(type.name)) {
              const resolvedRef = resolve(ref);
              if (!resolvedRef.location)
                return;
              const [fileLocation, localPointer] = resolvedRef.location.absolutePointer.split("#", 2);
              const componentLevelLocalPointer = localPointer.split("/").slice(0, 4).join("/");
              const pointer = `${fileLocation}#${componentLevelLocalPointer}`;
              const registered = components.get(pointer);
              if (registered) {
                registered.usedIn.push(location);
              } else {
                components.set(pointer, {
                  usedIn: [location],
                  name: key2.toString()
                });
              }
            }
          }
        },
        Root: {
          leave(root, ctx) {
            const data = ctx.getVisitorData();
            data.removedCount = removeUnusedComponents(root, []);
            if ((0, utils_1.isEmptyObject)(root.components)) {
              delete root.components;
            }
          }
        },
        NamedSchemas: {
          Schema(schema4, { location, key: key2 }) {
            if (!schema4.allOf) {
              registerComponent(location, "schemas", key2.toString());
            }
          }
        },
        NamedParameters: {
          Parameter(_parameter, { location, key: key2 }) {
            registerComponent(location, "parameters", key2.toString());
          }
        },
        NamedResponses: {
          Response(_response, { location, key: key2 }) {
            registerComponent(location, "responses", key2.toString());
          }
        },
        NamedExamples: {
          Example(_example, { location, key: key2 }) {
            registerComponent(location, "examples", key2.toString());
          }
        },
        NamedRequestBodies: {
          RequestBody(_requestBody, { location, key: key2 }) {
            registerComponent(location, "requestBodies", key2.toString());
          }
        },
        NamedHeaders: {
          Header(_header, { location, key: key2 }) {
            registerComponent(location, "headers", key2.toString());
          }
        }
      };
    };
    exports.RemoveUnusedComponents = RemoveUnusedComponents;
  }
});

// node_modules/@redocly/config/lib-esm/feedback-config-schema.js
var reasonsBaseProperties, reasonsSettings, optionalEmailSettings, feedbackConfigSchema;
var init_feedback_config_schema = __esm({
  "node_modules/@redocly/config/lib-esm/feedback-config-schema.js"() {
    reasonsBaseProperties = {
      hide: {
        type: "boolean",
        default: false
      },
      component: {
        type: "string",
        enum: ["radio", "checkbox"],
        default: "checkbox"
      },
      label: { type: "string" },
      items: { type: "array", items: { type: "string" } }
    };
    reasonsSettings = {
      type: "object",
      properties: reasonsBaseProperties,
      additionalProperties: false
    };
    optionalEmailSettings = {
      type: "object",
      properties: {
        hide: {
          type: "boolean",
          default: false
        },
        label: { type: "string" },
        placeholder: { type: "string" }
      },
      additionalProperties: false
    };
    feedbackConfigSchema = {
      type: "object",
      properties: {
        hide: {
          type: "boolean",
          default: false
        },
        type: {
          type: "string",
          enum: ["rating", "sentiment", "comment", "reasons", "mood", "scale"],
          default: "sentiment"
        },
        settings: {
          type: "object",
          properties: {
            label: { type: "string" },
            submitText: { type: "string" },
            buttonText: { type: "string" },
            component: {
              type: "string",
              enum: ["radio", "checkbox"],
              default: "checkbox"
            },
            items: { type: "array", items: { type: "string" }, minItems: 1 },
            leftScaleLabel: { type: "string" },
            rightScaleLabel: { type: "string" },
            reasons: {
              type: "object",
              properties: Object.assign(Object.assign({}, reasonsBaseProperties), { like: reasonsSettings, dislike: reasonsSettings, satisfied: reasonsSettings, neutral: reasonsSettings, dissatisfied: reasonsSettings }),
              additionalProperties: false
            },
            comment: {
              type: "object",
              properties: {
                hide: {
                  type: "boolean",
                  default: false
                },
                label: { type: "string" },
                likeLabel: { type: "string" },
                dislikeLabel: { type: "string" },
                satisfiedLabel: { type: "string" },
                neutralLabel: { type: "string" },
                dissatisfiedLabel: { type: "string" }
              },
              additionalProperties: false
            },
            optionalEmail: optionalEmailSettings
          },
          additionalProperties: false
        }
      },
      additionalProperties: false
    };
  }
});

// node_modules/@redocly/config/lib-esm/graphql-config-schema.js
var typeGroupConfig, menuGroupConfig, menuGroupingConfig, graphqlConfigSchema;
var init_graphql_config_schema = __esm({
  "node_modules/@redocly/config/lib-esm/graphql-config-schema.js"() {
    init_feedback_config_schema();
    typeGroupConfig = {
      type: "object",
      properties: {
        includeByName: {
          type: "array",
          items: {
            type: "string"
          }
        },
        excludeByName: {
          type: "array",
          items: {
            type: "string"
          }
        }
      },
      additionalProperties: false
    };
    menuGroupConfig = {
      type: "object",
      properties: {
        name: {
          type: "string"
        },
        items: typeGroupConfig,
        queries: typeGroupConfig,
        mutations: typeGroupConfig,
        subscriptions: typeGroupConfig,
        types: typeGroupConfig,
        directives: typeGroupConfig
      },
      required: ["name"],
      additionalProperties: false
    };
    menuGroupingConfig = {
      type: "object",
      properties: {
        requireExactGroups: {
          type: "boolean"
        },
        groups: {
          type: "array",
          items: menuGroupConfig
        },
        otherItemsGroupName: {
          type: "string"
        }
      },
      required: ["requireExactGroups", "groups", "otherItemsGroupName"],
      additionalProperties: false
    };
    graphqlConfigSchema = {
      type: "object",
      properties: {
        hidePaginationButtons: {
          type: "boolean"
        },
        menu: {
          type: "object",
          properties: Object.assign({}, menuGroupingConfig.properties),
          additionalProperties: false
        },
        sidebar: {
          type: "object",
          properties: {
            hide: {
              type: "boolean"
            }
          }
        },
        apiLogo: {
          type: "object",
          properties: {
            imageUrl: {
              type: "string"
            },
            href: {
              type: "string"
            },
            altText: {
              type: "string"
            },
            backgroundColor: {
              type: "string"
            }
          }
        },
        jsonSamplesDepth: {
          type: "number"
        },
        samplesMaxInlineArgs: {
          type: "number"
        },
        licenseKey: {
          type: "string"
        },
        fieldExpandLevel: {
          type: "number"
        },
        baseUrlPath: {
          type: "string"
        },
        feedback: feedbackConfigSchema
      },
      additionalProperties: false
    };
  }
});

// node_modules/@redocly/config/lib-esm/redoc-config-schema.js
var codeSamplesConfigSchema, downloadUrlsSchema, redocConfigSchema;
var init_redoc_config_schema = __esm({
  "node_modules/@redocly/config/lib-esm/redoc-config-schema.js"() {
    init_feedback_config_schema();
    codeSamplesConfigSchema = {
      type: "object",
      properties: {
        languages: {
          type: "array",
          items: {
            type: "object",
            properties: {
              lang: {
                type: "string",
                enum: [
                  "curl",
                  "JavaScript",
                  "Node.js",
                  "Python",
                  "Java8+Apache",
                  "Java",
                  "C#",
                  "C#+Newtonsoft",
                  "PHP",
                  "Go",
                  "Ruby",
                  "R",
                  "Payload"
                ]
              },
              label: { type: "string" },
              options: {
                type: "object",
                properties: {
                  indent: { type: "string" },
                  withImports: { type: "boolean" },
                  withComments: { type: "boolean" },
                  binary: { type: "boolean" },
                  credentials: {
                    type: "string",
                    enum: ["omit", "same-origin", "include"]
                  }
                },
                additionalProperties: false
              }
            },
            required: ["lang"],
            additionalProperties: false
          }
        },
        skipOptionalParameters: { type: "boolean" },
        withOAuth2Call: { type: "boolean" }
      },
      required: ["languages"],
      additionalProperties: false
    };
    downloadUrlsSchema = {
      type: "array",
      items: {
        type: "object",
        properties: {
          title: { type: "string" },
          url: { type: "string" }
        },
        required: ["url"],
        additionalProperties: false
      }
    };
    redocConfigSchema = {
      type: "object",
      properties: {
        licenseKey: { type: "string" },
        hideLoading: { type: "boolean" },
        disableRouter: { type: "boolean" },
        hideSidebar: { type: "boolean" },
        feedback: feedbackConfigSchema,
        hideReplay: { type: "boolean" },
        oAuth2RedirectURI: { type: "string", nullable: true },
        corsProxyUrl: { type: "string" },
        sortRequiredPropsFirst: { type: "boolean" },
        sanitize: { type: "boolean" },
        hideDownloadButtons: { type: "boolean" },
        downloadUrls: downloadUrlsSchema,
        onlyRequiredInSamples: { type: "boolean" },
        generatedSamplesMaxDepth: { oneOf: [{ type: "number" }, { type: "string" }] },
        showExtensions: {
          oneOf: [
            { type: "boolean" },
            { type: "string" },
            { type: "array", items: { type: "string" } }
          ]
        },
        hideSchemaTitles: { type: "boolean" },
        jsonSamplesExpandLevel: { oneOf: [{ type: "number" }, { type: "string" }] },
        schemasExpansionLevel: { oneOf: [{ type: "number" }, { type: "string" }] },
        mockServer: {
          type: "object",
          properties: {
            url: { type: "string" },
            position: { type: "string", enum: ["first", "last", "replace", "off"] },
            description: { type: "string" }
          }
        },
        maxDisplayedEnumValues: { type: "number" },
        schemaDefinitionsTagName: { type: "string" },
        layout: { type: "string", enum: ["stacked", "three-panel"] },
        hideInfoMetadata: { type: "boolean" },
        events: { type: "object" },
        skipBundle: { type: "boolean" },
        routingBasePath: { type: "string" },
        codeSamples: codeSamplesConfigSchema,
        ignoreNamedSchemas: {
          oneOf: [{ type: "array", items: { type: "string" } }, { type: "string" }]
        },
        hidePropertiesPrefix: { type: "boolean" },
        excludeFromSearch: { type: "boolean" }
      },
      additionalProperties: false
    };
  }
});

// node_modules/@redocly/config/lib-esm/reference-docs-config-schema.js
function pickObjectProps(object2, keys2) {
  return Object.fromEntries(keys2.filter((key2) => key2 in object2).map((key2) => [key2, object2[key2]]));
}
function omitObjectProps(object2, keys2) {
  return Object.fromEntries(Object.entries(object2).filter(([key2]) => !keys2.includes(key2)));
}
var ConfigLanguage, ConfigLabels, CommonConfigSidebarLinks, ConfigSidebarLinks, CommonThemeColors, CommonColorProps, BorderThemeColors, HttpColors, ResponseColors, SecondaryColors, TextThemeColors, ThemeColors, SizeProps, Sizes, FontConfig, ButtonsConfig, BadgeFontConfig, BadgeSizes, HttpBadgesConfig, LabelControls, Panels, TryItButton, ConfigThemeComponents, Breakpoints, StackedConfig, ThreePanelConfig, Layout, SchemaColorsConfig, ConfigThemeSchema, GroupItemsConfig, Level1Items, SpacingConfig, Sidebar, Heading, CodeConfig, HeadingsConfig, LinksConfig, Typography, TokenProps, CodeBlock, ConfigThemeLogo, Fab, ButtonOverrides, Overrides, RightPanel, Shape, ThemeSpacing, ConfigTheme, GenerateCodeSamples, deprecatedRefDocsSchema;
var init_reference_docs_config_schema = __esm({
  "node_modules/@redocly/config/lib-esm/reference-docs-config-schema.js"() {
    ConfigLanguage = {
      type: "object",
      properties: {
        label: { type: "string" },
        lang: {
          enum: [
            "curl",
            "C#",
            "Go",
            "Java",
            "Java8+Apache",
            "JavaScript",
            "Node.js",
            "PHP",
            "Python",
            "R",
            "Ruby"
          ]
        }
      },
      required: ["lang"]
    };
    ConfigLabels = {
      type: "object",
      properties: {
        enum: { type: "string" },
        enumSingleValue: { type: "string" },
        enumArray: { type: "string" },
        default: { type: "string" },
        deprecated: { type: "string" },
        example: { type: "string" },
        examples: { type: "string" },
        nullable: { type: "string" },
        recursive: { type: "string" },
        arrayOf: { type: "string" },
        webhook: { type: "string" },
        authorizations: { type: "string" },
        tryItAuthBasicUsername: { type: "string" },
        tryItAuthBasicPassword: { type: "string" }
      }
    };
    CommonConfigSidebarLinks = {
      type: "object",
      properties: {
        label: { type: "string" },
        link: { type: "string" },
        target: { type: "string" }
      },
      required: ["label", "link"]
    };
    ConfigSidebarLinks = {
      type: "object",
      properties: {
        beforeInfo: { type: "array", items: CommonConfigSidebarLinks },
        end: { type: "array", items: CommonConfigSidebarLinks }
      }
    };
    CommonThemeColors = {
      type: "object",
      properties: {
        main: { type: "string" },
        light: { type: "string" },
        dark: { type: "string" },
        contrastText: { type: "string" }
      }
    };
    CommonColorProps = {
      type: "object",
      properties: {
        backgroundColor: { type: "string" },
        borderColor: { type: "string" },
        color: { type: "string" },
        tabTextColor: { type: "string" }
      }
    };
    BorderThemeColors = {
      type: "object",
      properties: pickObjectProps(CommonThemeColors.properties, ["light", "dark"])
    };
    HttpColors = {
      type: "object",
      properties: {
        basic: { type: "string" },
        delete: { type: "string" },
        get: { type: "string" },
        head: { type: "string" },
        link: { type: "string" },
        options: { type: "string" },
        patch: { type: "string" },
        post: { type: "string" },
        put: { type: "string" }
      }
    };
    ResponseColors = {
      type: "object",
      properties: {
        error: CommonColorProps,
        info: CommonColorProps,
        redirect: CommonColorProps,
        success: CommonColorProps
      }
    };
    SecondaryColors = {
      type: "object",
      properties: omitObjectProps(CommonThemeColors.properties, ["dark"])
    };
    TextThemeColors = {
      type: "object",
      properties: {
        primary: { type: "string" },
        secondary: { type: "string" },
        light: { type: "string" }
      }
    };
    ThemeColors = {
      type: "object",
      properties: {
        accent: CommonThemeColors,
        border: BorderThemeColors,
        error: CommonThemeColors,
        http: HttpColors,
        primary: CommonThemeColors,
        responses: ResponseColors,
        secondary: SecondaryColors,
        success: CommonThemeColors,
        text: TextThemeColors,
        tonalOffset: { type: "number" },
        warning: CommonThemeColors
      }
    };
    SizeProps = {
      type: "object",
      properties: {
        fontSize: { type: "string" },
        padding: { type: "string" },
        minWidth: { type: "string" }
      }
    };
    Sizes = {
      type: "object",
      properties: {
        small: SizeProps,
        medium: SizeProps,
        large: SizeProps,
        xlarge: SizeProps
      }
    };
    FontConfig = {
      type: "object",
      properties: {
        fontFamily: { type: "string" },
        fontSize: { type: "string" },
        fontWeight: { type: "string" },
        lineHeight: { type: "string" }
      }
    };
    ButtonsConfig = {
      type: "object",
      properties: Object.assign(Object.assign({}, omitObjectProps(FontConfig.properties, ["fontSize", "lineHeight"])), { borderRadius: { type: "string" }, hoverStyle: { type: "string" }, boxShadow: { type: "string" }, hoverBoxShadow: { type: "string" }, sizes: Sizes })
    };
    BadgeFontConfig = {
      type: "object",
      properties: pickObjectProps(FontConfig.properties, ["fontSize", "lineHeight"])
    };
    BadgeSizes = {
      type: "object",
      properties: {
        medium: BadgeFontConfig,
        small: BadgeFontConfig
      }
    };
    HttpBadgesConfig = {
      type: "object",
      properties: Object.assign(Object.assign({}, omitObjectProps(FontConfig.properties, ["fontSize", "lineHeight"])), { borderRadius: { type: "string" }, color: { type: "string" }, sizes: BadgeSizes })
    };
    LabelControls = {
      type: "object",
      properties: {
        top: { type: "string" },
        width: { type: "string" },
        height: { type: "string" }
      }
    };
    Panels = {
      type: "object",
      properties: {
        borderRadius: { type: "string" },
        backgroundColor: { type: "string" }
      }
    };
    TryItButton = {
      type: "object",
      properties: {
        fullWidth: { type: "boolean" }
      }
    };
    ConfigThemeComponents = {
      type: "object",
      properties: {
        buttons: ButtonsConfig,
        httpBadges: HttpBadgesConfig,
        layoutControls: LabelControls,
        panels: Panels,
        tryItButton: TryItButton,
        tryItSendButton: TryItButton
      }
    };
    Breakpoints = {
      type: "object",
      properties: {
        small: { type: "string" },
        medium: { type: "string" },
        large: { type: "string" }
      }
    };
    StackedConfig = {
      type: "object",
      properties: {
        maxWidth: Breakpoints
      }
    };
    ThreePanelConfig = {
      type: "object",
      properties: {
        maxWidth: Breakpoints,
        middlePanelMaxWidth: Breakpoints
      }
    };
    Layout = {
      type: "object",
      properties: {
        showDarkRightPanel: { type: "boolean" },
        stacked: StackedConfig,
        "three-panel": ThreePanelConfig
      }
    };
    SchemaColorsConfig = {
      type: "object",
      properties: {
        backgroundColor: { type: "string" },
        border: { type: "string" }
      }
    };
    ConfigThemeSchema = {
      type: "object",
      properties: {
        breakFieldNames: { type: "boolean" },
        caretColor: { type: "string" },
        caretSize: { type: "string" },
        constraints: SchemaColorsConfig,
        defaultDetailsWidth: { type: "string" },
        examples: SchemaColorsConfig,
        labelsTextSize: { type: "string" },
        linesColor: { type: "string" },
        nestedBackground: { type: "string" },
        nestingSpacing: { type: "string" },
        requireLabelColor: { type: "string" },
        typeNameColor: { type: "string" },
        typeTitleColor: { type: "string" }
      }
    };
    GroupItemsConfig = {
      type: "object",
      properties: {
        subItemsColor: { type: "string" },
        textTransform: { type: "string" },
        fontWeight: { type: "string" }
      }
    };
    Level1Items = {
      type: "object",
      properties: pickObjectProps(GroupItemsConfig.properties, ["textTransform"])
    };
    SpacingConfig = {
      type: "object",
      properties: {
        unit: { type: "number" },
        paddingHorizontal: { type: "string" },
        paddingVertical: { type: "string" },
        offsetTop: { type: "string" },
        offsetLeft: { type: "string" },
        offsetNesting: { type: "string" }
      }
    };
    Sidebar = {
      type: "object",
      properties: Object.assign(Object.assign({}, omitObjectProps(FontConfig.properties, ["fontWeight", "lineHeight"])), { activeBgColor: { type: "string" }, activeTextColor: { type: "string" }, backgroundColor: { type: "string" }, borderRadius: { type: "string" }, breakPath: { type: "boolean" }, caretColor: { type: "string" }, caretSize: { type: "string" }, groupItems: GroupItemsConfig, level1items: Level1Items, rightLineColor: { type: "string" }, separatorLabelColor: { type: "string" }, showAtBreakpoint: { type: "string" }, spacing: SpacingConfig, textColor: { type: "string" }, width: { type: "string" } })
    };
    Heading = {
      type: "object",
      properties: Object.assign(Object.assign({}, FontConfig.properties), { color: { type: "string" }, transform: { type: "string" } })
    };
    CodeConfig = {
      type: "object",
      properties: Object.assign(Object.assign({}, FontConfig.properties), { backgroundColor: { type: "string" }, color: { type: "string" }, wordBreak: {
        type: "string",
        enum: [
          "break-all",
          "break-word",
          "keep-all",
          "normal",
          "revert",
          "unset",
          "inherit",
          "initial"
        ]
      }, wrap: { type: "boolean" } })
    };
    HeadingsConfig = {
      type: "object",
      properties: omitObjectProps(FontConfig.properties, ["fontSize"])
    };
    LinksConfig = {
      type: "object",
      properties: {
        color: { type: "string" },
        hover: { type: "string" },
        textDecoration: { type: "string" },
        hoverTextDecoration: { type: "string" },
        visited: { type: "string" }
      }
    };
    Typography = {
      type: "object",
      properties: Object.assign(Object.assign({ code: CodeConfig, fieldName: FontConfig }, pickObjectProps(FontConfig.properties, ["fontSize", "fontFamily"])), { fontWeightBold: { type: "string" }, fontWeightLight: { type: "string" }, fontWeightRegular: { type: "string" }, heading1: Heading, heading2: Heading, heading3: Heading, headings: HeadingsConfig, lineHeight: { type: "string" }, links: LinksConfig, optimizeSpeed: { type: "boolean" }, rightPanelHeading: Heading, smoothing: {
        type: "string",
        enum: ["auto", "none", "antialiased", "subpixel-antialiased", "grayscale"]
      } })
    };
    TokenProps = {
      type: "object",
      properties: Object.assign({ color: { type: "string" } }, omitObjectProps(FontConfig.properties, ["fontWeight"]))
    };
    CodeBlock = {
      type: "object",
      properties: {
        backgroundColor: { type: "string" },
        borderRadius: { type: "string" },
        tokens: TokenProps
      }
    };
    ConfigThemeLogo = {
      type: "object",
      properties: {
        gutter: { type: "string" },
        maxHeight: { type: "string" },
        maxWidth: { type: "string" }
      }
    };
    Fab = {
      type: "object",
      properties: {
        backgroundColor: { type: "string" },
        color: { type: "string" }
      }
    };
    ButtonOverrides = {
      type: "object",
      properties: {
        custom: { type: "string" }
      }
    };
    Overrides = {
      type: "object",
      properties: {
        DownloadButton: ButtonOverrides,
        NextSectionButton: ButtonOverrides
      }
    };
    RightPanel = {
      type: "object",
      properties: {
        backgroundColor: { type: "string" },
        panelBackgroundColor: { type: "string" },
        panelControlsBackgroundColor: { type: "string" },
        showAtBreakpoint: { type: "string" },
        textColor: { type: "string" },
        width: { type: "string" }
      }
    };
    Shape = {
      type: "object",
      properties: { borderRadius: { type: "string" } }
    };
    ThemeSpacing = {
      type: "object",
      properties: {
        sectionHorizontal: { type: "number" },
        sectionVertical: { type: "number" },
        unit: { type: "number" }
      }
    };
    ConfigTheme = {
      type: "object",
      properties: {
        breakpoints: Breakpoints,
        codeBlock: CodeBlock,
        colors: ThemeColors,
        components: ConfigThemeComponents,
        layout: Layout,
        logo: ConfigThemeLogo,
        fab: Fab,
        overrides: Overrides,
        rightPanel: RightPanel,
        schema: ConfigThemeSchema,
        shape: Shape,
        sidebar: Sidebar,
        spacing: ThemeSpacing,
        typography: Typography,
        links: { properties: { color: { type: "string" } } },
        codeSample: { properties: { backgroundColor: { type: "string" } } }
      }
    };
    GenerateCodeSamples = {
      type: "object",
      properties: {
        skipOptionalParameters: { type: "boolean" },
        languages: { type: "array", items: ConfigLanguage }
      },
      required: ["languages"]
    };
    deprecatedRefDocsSchema = {
      type: "object",
      properties: {
        theme: ConfigTheme,
        ctrlFHijack: { type: "boolean" },
        defaultSampleLanguage: { type: "string" },
        disableDeepLinks: { type: "boolean" },
        disableSearch: { type: "boolean" },
        disableSidebar: { type: "boolean" },
        downloadDefinitionUrl: { type: "string" },
        expandDefaultServerVariables: { type: "boolean" },
        enumSkipQuotes: { type: "boolean" },
        expandDefaultRequest: { type: "boolean" },
        expandDefaultResponse: { type: "boolean" },
        expandResponses: { type: "string" },
        expandSingleSchemaField: { type: "boolean" },
        generateCodeSamples: GenerateCodeSamples,
        generatedPayloadSamplesMaxDepth: { type: "number" },
        hideDownloadButton: { type: "boolean" },
        hideHostname: { type: "boolean" },
        hideInfoSection: { type: "boolean" },
        hideLogo: { type: "boolean" },
        hideRequestPayloadSample: { type: "boolean" },
        hideRightPanel: { type: "boolean" },
        hideSchemaPattern: { type: "boolean" },
        hideSingleRequestSampleTab: { type: "boolean" },
        hideSecuritySection: { type: "boolean" },
        hideTryItPanel: { type: "boolean" },
        hideFab: { type: "boolean" },
        hideOneOfDescription: { type: "boolean" },
        htmlTemplate: { type: "string" },
        jsonSampleExpandLevel: {
          oneOf: [{ type: "number", minimum: 1 }, { type: "string" }]
        },
        labels: ConfigLabels,
        menuToggle: { type: "boolean" },
        nativeScrollbars: { type: "boolean" },
        noAutoAuth: { type: "boolean" },
        onDeepLinkClick: { type: "object" },
        pagination: { enum: ["none", "section", "item"] },
        pathInMiddlePanel: { type: "boolean" },
        payloadSampleIdx: { type: "number", minimum: 0 },
        requestInterceptor: { type: "object" },
        requiredPropsFirst: { type: "boolean" },
        routingStrategy: { type: "string" },
        samplesTabsMaxCount: { type: "number" },
        schemaExpansionLevel: {
          oneOf: [{ type: "number", minimum: 0 }, { type: "string" }]
        },
        minCharacterLengthToInitSearch: { type: "number", minimum: 1 },
        maxResponseHeadersToShowInTryIt: { type: "number", minimum: 0 },
        scrollYOffset: {
          oneOf: [{ type: "number" }, { type: "string" }]
        },
        searchAutoExpand: { type: "boolean" },
        searchFieldLevelBoost: { type: "number", minimum: 0 },
        searchMaxDepth: { type: "number", minimum: 1 },
        searchMode: { type: "string", enum: ["default", "path-only"] },
        searchOperationTitleBoost: { type: "number" },
        searchTagTitleBoost: { type: "number" },
        sendXUserAgentInTryIt: { type: "boolean" },
        showChangeLayoutButton: { type: "boolean" },
        showConsole: { type: "boolean" },
        showNextButton: { type: "boolean" },
        showRightPanelToggle: { type: "boolean" },
        showSecuritySchemeType: { type: "boolean" },
        showWebhookVerb: { type: "boolean" },
        showObjectSchemaExamples: { type: "boolean" },
        disableTryItRequestUrlEncoding: { type: "boolean" },
        sidebarLinks: ConfigSidebarLinks,
        sideNavStyle: { type: "string", enum: ["summary-only", "path-first", "id-only", "path-only"] },
        simpleOneOfTypeLabel: { type: "boolean" },
        sortEnumValuesAlphabetically: { type: "boolean" },
        sortOperationsAlphabetically: { type: "boolean" },
        sortPropsAlphabetically: { type: "boolean" },
        sortTagsAlphabetically: { type: "boolean" },
        suppressWarnings: { type: "boolean" },
        unstable_externalDescription: { type: "boolean" },
        unstable_ignoreMimeParameters: { type: "boolean" },
        untrustedDefinition: { type: "boolean" },
        showAccessMode: { type: "boolean" },
        preserveOriginalExtensionsName: { type: "boolean" },
        markdownHeadingsAnchorLevel: { type: "number" }
      },
      additionalProperties: false
    };
  }
});

// node_modules/@redocly/config/lib-esm/ex-theme-config-schemas.js
var hideConfigSchema, scriptConfigSchema, navItemSchema, navItemsSchema, productConfigSchema, aiSearchConfigSchema, searchFacetsConfigSchema, searchFiltersConfigSchema, searchSuggestedPageSchema, logoConfigSchema, navbarConfigSchema, productsConfigSchema, footerConfigSchema, sidebarConfigSchema, scriptsConfigSchema, linksConfigSchema, searchConfigSchema, colorModeConfigSchema, navigationConfigSchema, codeSnippetConfigSchema, markdownConfigSchema, openapiConfigSchema, adobeAnalyticsConfigSchema, amplitudeAnalyticsConfigSchema, fullstoryAnalyticsConfigSchema, heapAnalyticsConfigSchema, rudderstackAnalyticsConfigSchema, segmentAnalyticsConfigSchema, gtmAnalyticsConfigSchema, productGoogleAnalyticsConfigSchema, googleAnalyticsConfigSchema, analyticsConfigSchema, userMenuConfigSchema, versionPickerConfigSchema, breadcrumbsConfigSchema, catalogFilterSchema, catalogSchema, catalogsConfigSchema, scorecardConfigSchema;
var init_ex_theme_config_schemas = __esm({
  "node_modules/@redocly/config/lib-esm/ex-theme-config-schemas.js"() {
    init_redoc_config_schema();
    init_reference_docs_config_schema();
    hideConfigSchema = {
      type: "object",
      properties: {
        hide: { type: "boolean" }
      },
      additionalProperties: false
    };
    scriptConfigSchema = {
      type: "object",
      properties: {
        src: { type: "string" },
        async: { type: "boolean" },
        crossorigin: { type: "string" },
        defer: { type: "boolean" },
        fetchpriority: { type: "string" },
        integrity: { type: "string" },
        module: { type: "boolean" },
        nomodule: { type: "boolean" },
        nonce: { type: "string" },
        referrerpolicy: { type: "string" },
        type: { type: "string" }
      },
      required: ["src"],
      additionalProperties: true
    };
    navItemSchema = {
      type: "object",
      properties: {
        page: { type: "string" },
        directory: { type: "string" },
        disconnect: { type: "boolean", default: false },
        group: { type: "string" },
        label: { type: "string" },
        href: { type: "string" },
        external: { type: "boolean" },
        labelTranslationKey: { type: "string" },
        groupTranslationKey: { type: "string" },
        icon: {
          oneOf: [
            { type: "string" },
            { type: "object", properties: { srcSet: { type: "string" } }, required: ["srcSet"] }
          ]
        },
        separator: { type: "string" },
        separatorLine: { type: "boolean" },
        linePosition: {
          type: "string",
          enum: ["top", "bottom"],
          default: "top"
        },
        version: { type: "string" },
        menuStyle: { type: "string", enum: ["drilldown"] },
        expanded: { type: "string", const: "always" },
        selectFirstItemOnExpand: { type: "boolean" },
        flatten: { type: "boolean" },
        linkedSidebars: {
          type: "array",
          items: { type: "string" }
        },
        // Allow users to eject the navbar and implement additional levels of nesting
        items: { type: "array", items: { type: "object", additionalProperties: true } },
        rbac: {
          type: "object",
          additionalProperties: { type: "string" }
        }
      }
    };
    navItemsSchema = {
      type: "array",
      items: Object.assign(Object.assign({}, navItemSchema), { properties: Object.assign(Object.assign({}, navItemSchema.properties), { items: { type: "array", items: navItemSchema } }) })
    };
    productConfigSchema = {
      type: "object",
      properties: {
        name: { type: "string" },
        icon: { type: "string" },
        folder: { type: "string" }
      },
      additionalProperties: false,
      required: ["name", "folder"]
    };
    aiSearchConfigSchema = {
      type: "object",
      properties: {
        hide: {
          type: "boolean",
          default: false
        },
        suggestions: {
          default: [],
          type: "array",
          items: {
            type: "string"
          }
        },
        prompt: {
          type: "string"
        }
      },
      additionalProperties: false
    };
    searchFacetsConfigSchema = {
      type: "array",
      items: {
        type: "object",
        required: ["name", "field", "type"],
        properties: {
          name: { type: "string" },
          field: { type: "string" },
          type: {
            type: "string",
            enum: ["multi-select", "select", "tags"]
          }
        },
        additionalProperties: false
      }
    };
    searchFiltersConfigSchema = {
      type: "object",
      properties: Object.assign({ facets: searchFacetsConfigSchema }, hideConfigSchema.properties),
      additionalProperties: false
    };
    searchSuggestedPageSchema = {
      type: "object",
      properties: {
        page: { type: "string" },
        label: { type: "string" },
        labelTranslationKey: { type: "string" }
      },
      required: ["page"]
    };
    logoConfigSchema = {
      type: "object",
      properties: {
        image: { type: "string" },
        srcSet: { type: "string" },
        altText: { type: "string" },
        link: { type: "string" },
        favicon: { type: "string" }
      },
      additionalProperties: false
    };
    navbarConfigSchema = {
      type: "object",
      properties: Object.assign({ items: navItemsSchema }, hideConfigSchema.properties),
      additionalProperties: false
    };
    productsConfigSchema = {
      type: "object",
      additionalProperties: productConfigSchema
    };
    footerConfigSchema = {
      type: "object",
      properties: Object.assign({ items: navItemsSchema, copyrightText: { type: "string" }, logo: hideConfigSchema }, hideConfigSchema.properties),
      additionalProperties: false
    };
    sidebarConfigSchema = {
      type: "object",
      properties: Object.assign({ separatorLine: { type: "boolean" }, linePosition: {
        type: "string",
        enum: ["top", "bottom"],
        default: "bottom"
      } }, hideConfigSchema.properties),
      additionalProperties: false
    };
    scriptsConfigSchema = {
      type: "object",
      properties: {
        head: { type: "array", items: scriptConfigSchema },
        body: { type: "array", items: scriptConfigSchema }
      },
      additionalProperties: false
    };
    linksConfigSchema = {
      type: "array",
      items: {
        type: "object",
        properties: {
          href: { type: "string" },
          as: { type: "string" },
          crossorigin: { type: "string" },
          fetchpriority: { type: "string" },
          hreflang: { type: "string" },
          imagesizes: { type: "string" },
          imagesrcset: { type: "string" },
          integrity: { type: "string" },
          media: { type: "string" },
          prefetch: { type: "string" },
          referrerpolicy: { type: "string" },
          rel: { type: "string" },
          sizes: { type: "string" },
          title: { type: "string" },
          type: { type: "string" }
        },
        required: ["href"],
        additionalProperties: true
      }
    };
    searchConfigSchema = {
      type: "object",
      properties: Object.assign({ engine: {
        type: "string",
        enum: ["flexsearch", "typesense"],
        default: "flexsearch"
      }, ai: aiSearchConfigSchema, filters: searchFiltersConfigSchema, placement: {
        type: "string",
        default: "navbar"
      }, shortcuts: {
        type: "array",
        items: { type: "string" },
        default: ["/"]
      }, suggestedPages: {
        type: "array",
        items: searchSuggestedPageSchema
      } }, hideConfigSchema.properties),
      additionalProperties: false
    };
    colorModeConfigSchema = {
      type: "object",
      properties: Object.assign({ ignoreDetection: { type: "boolean" }, modes: {
        type: "array",
        items: { type: "string" },
        default: ["light", "dark"]
      } }, hideConfigSchema.properties),
      additionalProperties: false
    };
    navigationConfigSchema = {
      type: "object",
      properties: {
        nextButton: {
          type: "object",
          properties: Object.assign({ text: { type: "string", default: "Next page" } }, hideConfigSchema.properties),
          additionalProperties: false,
          default: {}
        },
        previousButton: {
          type: "object",
          properties: Object.assign({ text: { type: "string", default: "Previous page" } }, hideConfigSchema.properties),
          additionalProperties: false,
          default: {}
        }
      },
      additionalProperties: false
    };
    codeSnippetConfigSchema = {
      type: "object",
      properties: {
        elementFormat: { type: "string", default: "icon" },
        copy: {
          type: "object",
          properties: Object.assign({}, hideConfigSchema.properties),
          additionalProperties: false,
          default: { hide: false }
        },
        report: {
          type: "object",
          properties: Object.assign({ tooltipText: { type: "string" }, buttonText: { type: "string" }, label: { type: "string" } }, hideConfigSchema.properties),
          additionalProperties: false,
          default: { hide: false }
        },
        expand: {
          type: "object",
          properties: Object.assign({}, hideConfigSchema.properties),
          additionalProperties: false,
          default: { hide: false }
        },
        collapse: {
          type: "object",
          properties: Object.assign({}, hideConfigSchema.properties),
          additionalProperties: false,
          default: { hide: false }
        }
      },
      additionalProperties: false
    };
    markdownConfigSchema = {
      type: "object",
      properties: {
        frontMatterKeysToResolve: {
          type: "array",
          items: { type: "string" },
          default: ["image", "links"]
        },
        partialsFolders: {
          type: "array",
          items: { type: "string" },
          default: ["_partials"]
        },
        lastUpdatedBlock: {
          type: "object",
          properties: Object.assign({ format: {
            type: "string",
            enum: ["timeago", "iso", "long", "short"],
            default: "timeago"
          }, locale: { type: "string" } }, hideConfigSchema.properties),
          additionalProperties: false,
          default: {}
        },
        toc: {
          type: "object",
          properties: Object.assign({ header: { type: "string", default: "On this page" }, depth: { type: "integer", default: 3, minimum: 1 } }, hideConfigSchema.properties),
          additionalProperties: false,
          default: {}
        },
        editPage: {
          type: "object",
          properties: Object.assign({ baseUrl: { type: "string" } }, hideConfigSchema.properties),
          additionalProperties: false,
          default: {}
        }
      },
      additionalProperties: false,
      default: {}
    };
    openapiConfigSchema = Object.assign(Object.assign({}, redocConfigSchema), { properties: Object.assign(Object.assign({}, redocConfigSchema.properties), deprecatedRefDocsSchema.properties) });
    adobeAnalyticsConfigSchema = {
      type: "object",
      properties: {
        includeInDevelopment: { type: "boolean" },
        scriptUrl: { type: "string" },
        pageViewEventName: { type: "string" }
      },
      additionalProperties: false,
      required: ["scriptUrl"]
    };
    amplitudeAnalyticsConfigSchema = {
      type: "object",
      properties: {
        includeInDevelopment: { type: "boolean" },
        apiKey: { type: "string" },
        head: { type: "boolean" },
        respectDNT: { type: "boolean" },
        exclude: { type: "array", items: { type: "string" } },
        outboundClickEventName: { type: "string" },
        pageViewEventName: { type: "string" },
        amplitudeConfig: { type: "object", additionalProperties: true }
      },
      additionalProperties: false,
      required: ["apiKey"]
    };
    fullstoryAnalyticsConfigSchema = {
      type: "object",
      properties: {
        includeInDevelopment: { type: "boolean" },
        orgId: { type: "string" }
      },
      additionalProperties: false,
      required: ["orgId"]
    };
    heapAnalyticsConfigSchema = {
      type: "object",
      properties: {
        includeInDevelopment: { type: "boolean" },
        appId: { type: "string" }
      },
      additionalProperties: false,
      required: ["appId"]
    };
    rudderstackAnalyticsConfigSchema = {
      type: "object",
      properties: {
        includeInDevelopment: { type: "boolean" },
        writeKey: { type: "string", minLength: 10 },
        trackPage: { type: "boolean" },
        dataPlaneUrl: { type: "string" },
        controlPlaneUrl: { type: "string" },
        sdkUrl: { type: "string" },
        loadOptions: { type: "object", additionalProperties: true }
      },
      additionalProperties: false,
      required: ["writeKey"]
    };
    segmentAnalyticsConfigSchema = {
      type: "object",
      properties: {
        includeInDevelopment: { type: "boolean" },
        writeKey: { type: "string", minLength: 10 },
        trackPage: { type: "boolean" },
        includeTitleInPageCall: { type: "boolean" },
        host: { type: "string" }
      },
      additionalProperties: false,
      required: ["writeKey"]
    };
    gtmAnalyticsConfigSchema = {
      type: "object",
      properties: {
        includeInDevelopment: { type: "boolean" },
        trackingId: { type: "string" },
        gtmAuth: { type: "string" },
        gtmPreview: { type: "string" },
        defaultDataLayer: {},
        dataLayerName: { type: "string" },
        enableWebVitalsTracking: { type: "boolean" },
        selfHostedOrigin: { type: "string" },
        pageViewEventName: { type: "string" }
      },
      additionalProperties: false,
      required: ["trackingId"]
    };
    productGoogleAnalyticsConfigSchema = {
      type: "object",
      properties: {
        includeInDevelopment: { type: "boolean" },
        trackingId: { type: "string" },
        conversionId: { type: "string" },
        floodlightId: { type: "string" },
        optimizeId: { type: "string" },
        exclude: { type: "array", items: { type: "string" } }
      },
      additionalProperties: false,
      required: ["trackingId"]
    };
    googleAnalyticsConfigSchema = {
      type: "object",
      properties: {
        includeInDevelopment: { type: "boolean" },
        trackingId: { type: "string" },
        conversionId: { type: "string" },
        floodlightId: { type: "string" },
        head: { type: "boolean" },
        respectDNT: { type: "boolean" },
        exclude: { type: "array", items: { type: "string" } },
        optimizeId: { type: "string" },
        anonymizeIp: { type: "boolean" },
        cookieExpires: { type: "number" },
        // All enabled tracking configs
        trackers: {
          type: "object",
          additionalProperties: productGoogleAnalyticsConfigSchema
        }
      },
      additionalProperties: false,
      required: ["trackingId"]
    };
    analyticsConfigSchema = {
      type: "object",
      properties: {
        adobe: adobeAnalyticsConfigSchema,
        amplitude: amplitudeAnalyticsConfigSchema,
        fullstory: fullstoryAnalyticsConfigSchema,
        heap: heapAnalyticsConfigSchema,
        rudderstack: rudderstackAnalyticsConfigSchema,
        segment: segmentAnalyticsConfigSchema,
        gtm: gtmAnalyticsConfigSchema,
        ga: googleAnalyticsConfigSchema
      }
    };
    userMenuConfigSchema = {
      type: "object",
      properties: Object.assign({ items: {
        type: "array",
        items: {
          type: "object",
          properties: {
            label: { type: "string" },
            external: { type: "boolean" },
            link: { type: "string" },
            separatorLine: { type: "boolean" }
          },
          additionalProperties: true
        },
        default: []
      }, hideLoginButton: { type: "boolean" } }, hideConfigSchema.properties),
      additionalProperties: false
    };
    versionPickerConfigSchema = {
      type: "object",
      properties: {
        hide: { type: "boolean" },
        showForUnversioned: {
          type: "boolean"
        }
      }
    };
    breadcrumbsConfigSchema = {
      type: "object",
      properties: {
        hide: { type: "boolean" },
        prefixItems: {
          type: "array",
          items: {
            type: "object",
            properties: {
              label: { type: "string" },
              labelTranslationKey: { type: "string" },
              page: { type: "string" }
            },
            additionalProperties: false,
            default: {}
          }
        }
      },
      additionalProperties: false
    };
    catalogFilterSchema = {
      type: "object",
      additionalProperties: false,
      required: ["title", "property"],
      properties: {
        type: {
          type: "string",
          enum: ["select", "checkboxes", "date-range"],
          default: "checkboxes"
        },
        title: { type: "string" },
        titleTranslationKey: { type: "string" },
        property: { type: "string" },
        parentFilter: { type: "string" },
        valuesMapping: { type: "object", additionalProperties: { type: "string" } },
        missingCategoryName: { type: "string" },
        missingCategoryNameTranslationKey: { type: "string" },
        options: { type: "array", items: { type: "string" } }
      }
    };
    catalogSchema = {
      type: "object",
      additionalProperties: true,
      required: ["slug", "items"],
      properties: {
        slug: { type: "string" },
        filters: { type: "array", items: catalogFilterSchema },
        groupByFirstFilter: { type: "boolean" },
        filterValuesCasing: {
          type: "string",
          enum: ["sentence", "original", "lowercase", "uppercase"]
        },
        items: navItemsSchema,
        requiredPermission: { type: "string" },
        separateVersions: { type: "boolean" },
        title: { type: "string" },
        titleTranslationKey: { type: "string" },
        description: { type: "string" },
        descriptionTranslationKey: { type: "string" }
      }
    };
    catalogsConfigSchema = {
      type: "object",
      patternProperties: {
        ".*": catalogSchema
      }
    };
    scorecardConfigSchema = {
      type: "object",
      additionalProperties: true,
      required: [],
      properties: {
        /**
         * @deprecated Should use `reunite.ignoreLint` instead
         */
        ignoreNonCompliant: { type: "boolean", default: false },
        teamMetadataProperty: {
          type: "object",
          properties: {
            property: { type: "string" },
            label: { type: "string" },
            default: { type: "string" }
          }
        },
        levels: {
          type: "array",
          items: {
            type: "object",
            required: ["name"],
            properties: {
              name: { type: "string" },
              color: { type: "string" },
              extends: { type: "array", items: { type: "string" } },
              rules: {
                type: "object",
                additionalProperties: {
                  oneOf: [{ type: "string" }, { type: "object" }]
                }
              }
            },
            additionalProperties: false
          }
        },
        targets: {
          type: "array",
          items: {
            type: "object",
            required: ["where"],
            properties: {
              minimumLevel: { type: "string" },
              rules: { type: "object", additionalProperties: true },
              where: {
                type: "object",
                required: ["metadata"],
                properties: {
                  metadata: { type: "object", additionalProperties: { type: "string" } }
                },
                additionalProperties: false
              }
            },
            additionalProperties: false
          }
        },
        ignore: {
          type: "array",
          items: { type: "string" }
        }
      }
    };
  }
});

// node_modules/@redocly/config/lib-esm/default-theme-config-schema.js
var themeConfigSchema, closedThemeConfigSchema;
var init_default_theme_config_schema = __esm({
  "node_modules/@redocly/config/lib-esm/default-theme-config-schema.js"() {
    init_graphql_config_schema();
    init_feedback_config_schema();
    init_ex_theme_config_schemas();
    themeConfigSchema = {
      type: "object",
      properties: {
        /**
         * @deprecated Should use `plugins` instead
         */
        imports: {
          type: "array",
          items: { type: "string" }
        },
        logo: logoConfigSchema,
        navbar: navbarConfigSchema,
        products: productsConfigSchema,
        footer: footerConfigSchema,
        sidebar: sidebarConfigSchema,
        scripts: scriptsConfigSchema,
        links: linksConfigSchema,
        feedback: feedbackConfigSchema,
        search: searchConfigSchema,
        colorMode: colorModeConfigSchema,
        navigation: navigationConfigSchema,
        codeSnippet: codeSnippetConfigSchema,
        markdown: markdownConfigSchema,
        openapi: openapiConfigSchema,
        graphql: graphqlConfigSchema,
        analytics: analyticsConfigSchema,
        userMenu: userMenuConfigSchema,
        versionPicker: versionPickerConfigSchema,
        breadcrumbs: breadcrumbsConfigSchema,
        catalog: catalogsConfigSchema,
        scorecard: scorecardConfigSchema
      },
      additionalProperties: true
    };
    closedThemeConfigSchema = Object.assign(Object.assign({}, themeConfigSchema), { additionalProperties: false });
  }
});

// node_modules/@redocly/config/lib-esm/constants.js
var DEFAULT_TEAM_CLAIM_NAME, AuthProviderType, ApigeeDevOnboardingIntegrationAuthType, REDOCLY_TEAMS_RBAC, REDOCLY_ROUTE_RBAC, LayoutVariant;
var init_constants = __esm({
  "node_modules/@redocly/config/lib-esm/constants.js"() {
    DEFAULT_TEAM_CLAIM_NAME = "https://redocly.com/sso/teams";
    (function(AuthProviderType2) {
      AuthProviderType2["OIDC"] = "OIDC";
      AuthProviderType2["SAML2"] = "SAML2";
    })(AuthProviderType || (AuthProviderType = {}));
    (function(ApigeeDevOnboardingIntegrationAuthType2) {
      ApigeeDevOnboardingIntegrationAuthType2["SERVICE_ACCOUNT"] = "SERVICE_ACCOUNT";
      ApigeeDevOnboardingIntegrationAuthType2["OAUTH2"] = "OAUTH2";
    })(ApigeeDevOnboardingIntegrationAuthType || (ApigeeDevOnboardingIntegrationAuthType = {}));
    REDOCLY_TEAMS_RBAC = "redocly::teams-rbac";
    REDOCLY_ROUTE_RBAC = "redocly::route-rbac";
    (function(LayoutVariant2) {
      LayoutVariant2["STACKED"] = "stacked";
      LayoutVariant2["THREE_PANEL"] = "three-panel";
    })(LayoutVariant || (LayoutVariant = {}));
  }
});

// node_modules/@redocly/config/lib-esm/remove-property-recursively.js
function removePropertyRecursively(object2, propToRemove) {
  return Object.fromEntries(Object.entries(object2).map(([key2, value]) => {
    if (key2 === propToRemove) {
      return void 0;
    }
    if (typeof value !== "object" || !value) {
      return [key2, value];
    }
    if (Array.isArray(value)) {
      return [
        key2,
        value.map((arrayItem) => typeof arrayItem === "object" ? removePropertyRecursively(arrayItem, propToRemove) : arrayItem)
      ];
    }
    return [key2, removePropertyRecursively(value, propToRemove)];
  }).filter(Boolean));
}
var init_remove_property_recursively = __esm({
  "node_modules/@redocly/config/lib-esm/remove-property-recursively.js"() {
  }
});

// node_modules/@redocly/config/lib-esm/reunite-config-schema.js
var jobInputsSchema, jobServersSchema, severitySchema, jobSeveritySchema, jobTriggerSchema, reuniteConfigSchema;
var init_reunite_config_schema = __esm({
  "node_modules/@redocly/config/lib-esm/reunite-config-schema.js"() {
    jobInputsSchema = {
      type: "object",
      additionalProperties: { type: "string" }
    };
    jobServersSchema = {
      type: "object",
      additionalProperties: false,
      patternProperties: {
        "^[a-zA-Z0-9_-]+$": {
          type: "string",
          pattern: "^https?://[^\\s/$.?#].[^\\s]*$"
        }
      }
    };
    severitySchema = {
      type: "string",
      enum: ["error", "warn", "off"]
    };
    jobSeveritySchema = {
      type: "object",
      additionalProperties: false,
      properties: {
        schemaCheck: severitySchema,
        statusCodeCheck: severitySchema,
        contentTypeCheck: severitySchema,
        successCriteriaCheck: severitySchema
      }
    };
    jobTriggerSchema = {
      type: "object",
      additionalProperties: false,
      properties: {
        event: {
          type: "string",
          enum: ["schedule"]
        },
        interval: { type: "string", pattern: "^[1-9]\\d*[mhdw]$" }
      },
      required: ["event"]
    };
    reuniteConfigSchema = {
      type: "object",
      properties: {
        ignoreLint: {
          oneOf: [
            { type: "boolean", default: false },
            {
              type: "object",
              additionalProperties: { type: "boolean" }
            }
          ]
        },
        ignoreLinkChecker: { type: "boolean" },
        ignoreMarkdocErrors: { type: "boolean" },
        jobs: {
          type: "array",
          items: {
            type: "object",
            properties: {
              path: {
                type: "string",
                pattern: "^(?!\\.\\./)(/[a-zA-Z0-9_\\-\\./]+|./[a-zA-Z0-9_\\-\\./]+|[a-zA-Z0-9_\\-\\./]+)$"
              },
              agent: {
                type: "string",
                enum: ["respect"]
              },
              trigger: jobTriggerSchema,
              inputs: jobInputsSchema,
              servers: jobServersSchema,
              severity: jobSeveritySchema
            },
            required: ["path", "trigger", "agent"],
            additionalProperties: false
          }
        }
      },
      additionalProperties: false
    };
  }
});

// node_modules/@redocly/config/lib-esm/root-config-schema.js
var oidcIssuerMetadataSchema, oidcProviderConfigSchema, saml2ProviderConfigSchema, authProviderConfigSchema, ssoDirectConfigSchema, ssoConfigSchema, redirectConfigSchema, redirectsConfigSchema, rulesSchema, apiConfigSchema, metadataConfigSchema, metadataGlobsConfigSchema, seoConfigSchema, apiFunctionsConfigSchema, rbacScopeItemsSchema, rbacConfigSchema, graviteeAdapterAuthStaticSchema, graviteeAdapterAuthIdpSchema, graviteeAdapterConfigSchema, apigeeAdapterAuthOauth2Schema, apigeeAdapterAuthServiceAccountSchema, apigeeXAdapterConfigSchema, apigeeEdgeAdapterConfigSchema, devOnboardingAdapterConfigSchema, devOnboardingConfigSchema, l10nConfigSchema, responseHeaderSchema, redoclyConfigSchema, environmentSchema, rootRedoclyConfigSchema;
var init_root_config_schema = __esm({
  "node_modules/@redocly/config/lib-esm/root-config-schema.js"() {
    init_constants();
    init_default_theme_config_schema();
    init_feedback_config_schema();
    init_graphql_config_schema();
    init_remove_property_recursively();
    init_reunite_config_schema();
    init_ex_theme_config_schemas();
    oidcIssuerMetadataSchema = {
      type: "object",
      properties: {
        end_session_endpoint: { type: "string" },
        token_endpoint: { type: "string" },
        authorization_endpoint: { type: "string" },
        jwks_uri: { type: "string" }
      },
      required: ["token_endpoint", "authorization_endpoint"],
      additionalProperties: true
    };
    oidcProviderConfigSchema = {
      type: "object",
      properties: {
        type: { type: "string", const: AuthProviderType.OIDC },
        title: { type: "string" },
        pkce: { type: "boolean", default: false },
        configurationUrl: { type: "string", minLength: 1 },
        configuration: oidcIssuerMetadataSchema,
        clientId: { type: "string", minLength: 1 },
        clientSecret: { type: "string", minLength: 0 },
        teamsClaimName: { type: "string" },
        teamsClaimMap: { type: "object", additionalProperties: { type: "string" } },
        defaultTeams: { type: "array", items: { type: "string" } },
        scopes: { type: "array", items: { type: "string" } },
        tokenExpirationTime: { type: "number" },
        authorizationRequestCustomParams: { type: "object", additionalProperties: { type: "string" } },
        tokenRequestCustomParams: { type: "object", additionalProperties: { type: "string" } },
        audience: { type: "array", items: { type: "string" } }
      },
      required: ["type", "clientId"],
      oneOf: [{ required: ["configurationUrl"] }, { required: ["configuration"] }],
      additionalProperties: false
    };
    saml2ProviderConfigSchema = {
      type: "object",
      properties: {
        type: { type: "string", const: AuthProviderType.SAML2 },
        title: { type: "string" },
        issuerId: { type: "string" },
        entityId: { type: "string" },
        ssoUrl: { type: "string" },
        x509PublicCert: { type: "string" },
        teamsAttributeName: { type: "string", default: DEFAULT_TEAM_CLAIM_NAME },
        teamsAttributeMap: { type: "object", additionalProperties: { type: "string" } },
        defaultTeams: { type: "array", items: { type: "string" } }
      },
      additionalProperties: false,
      required: ["type", "issuerId", "ssoUrl", "x509PublicCert"]
    };
    authProviderConfigSchema = {
      oneOf: [oidcProviderConfigSchema, saml2ProviderConfigSchema],
      discriminator: { propertyName: "type" }
    };
    ssoDirectConfigSchema = {
      type: "object",
      additionalProperties: authProviderConfigSchema
    };
    ssoConfigSchema = {
      oneOf: [
        {
          type: "array",
          items: {
            type: "string",
            enum: ["REDOCLY", "CORPORATE", "GUEST"]
          },
          uniqueItems: true
        },
        {
          type: "string",
          enum: ["REDOCLY", "CORPORATE", "GUEST"]
        }
      ]
    };
    redirectConfigSchema = {
      type: "object",
      properties: {
        to: { type: "string" },
        type: { type: "number", default: 301 }
      },
      additionalProperties: false
    };
    redirectsConfigSchema = {
      type: "object",
      additionalProperties: redirectConfigSchema,
      default: {}
    };
    rulesSchema = {
      type: "object",
      additionalProperties: {
        oneOf: [{ type: "string" }, { type: "object" }]
      }
    };
    apiConfigSchema = {
      type: "object",
      properties: {
        root: { type: "string" },
        output: { type: "string", pattern: "(.ya?ml|.json)$" },
        rbac: { type: "object", additionalProperties: true },
        openapi: openapiConfigSchema,
        graphql: graphqlConfigSchema,
        /**
         * @deprecated left for backwards compatibility
         */
        theme: {
          type: "object",
          properties: {
            openapi: openapiConfigSchema,
            graphql: graphqlConfigSchema
          },
          additionalProperties: false
        },
        title: { type: "string" },
        metadata: { type: "object", additionalProperties: true },
        rules: rulesSchema,
        decorators: { type: "object", additionalProperties: true },
        preprocessors: { type: "object", additionalProperties: true }
      },
      required: ["root"]
    };
    metadataConfigSchema = {
      type: "object",
      additionalProperties: true
    };
    metadataGlobsConfigSchema = {
      type: "object",
      additionalProperties: {
        type: "object",
        additionalProperties: true
      }
    };
    seoConfigSchema = {
      type: "object",
      properties: {
        title: { type: "string" },
        description: { type: "string" },
        siteUrl: { type: "string" },
        image: { type: "string" },
        keywords: {
          oneOf: [{ type: "array", items: { type: "string" } }, { type: "string" }]
        },
        lang: { type: "string" },
        jsonLd: { type: "object" },
        meta: {
          type: "array",
          items: {
            type: "object",
            properties: {
              name: { type: "string" },
              content: { type: "string" }
            },
            required: ["name", "content"],
            additionalProperties: false
          }
        }
      },
      additionalProperties: false
    };
    apiFunctionsConfigSchema = {
      type: "object",
      properties: {
        folders: { type: "array", items: { type: "string" } }
      },
      additionalProperties: false
    };
    rbacScopeItemsSchema = {
      type: "object",
      additionalProperties: { type: "string" }
    };
    rbacConfigSchema = {
      type: "object",
      properties: {
        teamNamePatterns: { type: "array", items: { type: "string" } },
        teamFolders: { type: "array", items: { type: "string" } },
        teamFoldersBaseRoles: rbacScopeItemsSchema,
        cms: rbacScopeItemsSchema,
        // deprecated in favor of reunite
        reunite: rbacScopeItemsSchema,
        features: {
          type: "object",
          properties: {
            aiSearch: rbacScopeItemsSchema
          },
          additionalProperties: false
        },
        content: {
          type: "object",
          properties: {
            "**": rbacScopeItemsSchema
          },
          additionalProperties: rbacScopeItemsSchema
        }
      },
      additionalProperties: rbacScopeItemsSchema
    };
    graviteeAdapterAuthStaticSchema = {
      type: "object",
      properties: { static: { type: "string" } },
      additionalProperties: false,
      required: ["static"]
    };
    graviteeAdapterAuthIdpSchema = {
      type: "object",
      properties: { idp: { type: "string" } },
      additionalProperties: false,
      required: ["idp"]
    };
    graviteeAdapterConfigSchema = {
      type: "object",
      properties: {
        type: { type: "string", const: "GRAVITEE" },
        apiBaseUrl: { type: "string" },
        env: { type: "string" },
        allowApiProductsOutsideCatalog: { type: "boolean", default: false },
        stage: { type: "string", default: "non-production" },
        auth: {
          oneOf: [graviteeAdapterAuthStaticSchema, graviteeAdapterAuthIdpSchema]
        }
      },
      additionalProperties: false,
      required: ["type", "apiBaseUrl"]
    };
    apigeeAdapterAuthOauth2Schema = {
      type: "object",
      properties: {
        type: { type: "string", const: ApigeeDevOnboardingIntegrationAuthType.OAUTH2 },
        tokenEndpoint: { type: "string" },
        clientId: { type: "string" },
        clientSecret: { type: "string" }
      },
      additionalProperties: false,
      required: ["type", "tokenEndpoint", "clientId", "clientSecret"]
    };
    apigeeAdapterAuthServiceAccountSchema = {
      type: "object",
      properties: {
        type: { type: "string", const: ApigeeDevOnboardingIntegrationAuthType.SERVICE_ACCOUNT },
        serviceAccountEmail: { type: "string" },
        serviceAccountPrivateKey: { type: "string" }
      },
      additionalProperties: false,
      required: ["type", "serviceAccountEmail", "serviceAccountPrivateKey"]
    };
    apigeeXAdapterConfigSchema = {
      type: "object",
      properties: {
        type: { type: "string", const: "APIGEE_X" },
        apiUrl: { type: "string" },
        stage: { type: "string", default: "non-production" },
        organizationName: { type: "string" },
        ignoreApiProducts: { type: "array", items: { type: "string" } },
        allowApiProductsOutsideCatalog: { type: "boolean", default: false },
        auth: {
          type: "object",
          oneOf: [apigeeAdapterAuthOauth2Schema, apigeeAdapterAuthServiceAccountSchema],
          discriminator: { propertyName: "type" }
        }
      },
      additionalProperties: false,
      required: ["type", "organizationName", "auth"]
    };
    apigeeEdgeAdapterConfigSchema = Object.assign(Object.assign({}, apigeeXAdapterConfigSchema), { properties: Object.assign(Object.assign({}, apigeeXAdapterConfigSchema.properties), { type: { type: "string", const: "APIGEE_EDGE" } }) });
    devOnboardingAdapterConfigSchema = {
      type: "object",
      oneOf: [apigeeXAdapterConfigSchema, apigeeEdgeAdapterConfigSchema, graviteeAdapterConfigSchema],
      discriminator: { propertyName: "type" }
    };
    devOnboardingConfigSchema = {
      type: "object",
      required: ["adapters"],
      additionalProperties: false,
      properties: {
        adapters: {
          type: "array",
          items: devOnboardingAdapterConfigSchema
        }
      }
    };
    l10nConfigSchema = {
      type: "object",
      properties: {
        defaultLocale: {
          type: "string"
        },
        locales: {
          type: "array",
          items: {
            type: "object",
            properties: {
              code: {
                type: "string"
              },
              name: {
                type: "string"
              }
            },
            required: ["code"]
          }
        }
      },
      additionalProperties: false,
      required: ["defaultLocale"]
    };
    responseHeaderSchema = {
      type: "object",
      properties: {
        name: { type: "string" },
        value: { type: "string" }
      },
      additionalProperties: false,
      required: ["name", "value"]
    };
    redoclyConfigSchema = {
      type: "object",
      properties: {
        /**
         * @deprecated Should use `plugins` instead
         */
        imports: {
          type: "array",
          items: { type: "string" }
        },
        licenseKey: { type: "string" },
        redirects: redirectsConfigSchema,
        seo: seoConfigSchema,
        rbac: rbacConfigSchema,
        apiFunctions: apiFunctionsConfigSchema,
        requiresLogin: { type: "boolean" },
        responseHeaders: {
          type: "object",
          additionalProperties: {
            type: "array",
            items: responseHeaderSchema
          }
        },
        mockServer: {
          type: "object",
          properties: {
            off: { type: "boolean", default: false },
            position: { type: "string", enum: ["first", "last", "replace", "off"], default: "first" },
            strictExamples: { type: "boolean", default: false },
            errorIfForcedExampleNotFound: { type: "boolean", default: false },
            description: { type: "string" }
          }
        },
        apis: {
          type: "object",
          additionalProperties: apiConfigSchema
        },
        rules: rulesSchema,
        decorators: { type: "object", additionalProperties: true },
        preprocessors: { type: "object", additionalProperties: true },
        ssoDirect: ssoDirectConfigSchema,
        sso: ssoConfigSchema,
        residency: { type: "string" },
        developerOnboarding: devOnboardingConfigSchema,
        removeAttribution: { type: "boolean" },
        i18n: l10nConfigSchema,
        // deprecated
        l10n: l10nConfigSchema,
        metadata: metadataConfigSchema,
        metadataGlobs: metadataGlobsConfigSchema,
        ignore: {
          type: "array",
          items: {
            type: "string"
          }
        },
        /**
         * @deprecated properties moved to the root of the config
         */
        theme: themeConfigSchema,
        reunite: reuniteConfigSchema,
        // Ex theme properties
        logo: logoConfigSchema,
        navbar: navbarConfigSchema,
        products: productsConfigSchema,
        footer: footerConfigSchema,
        sidebar: sidebarConfigSchema,
        scripts: scriptsConfigSchema,
        links: linksConfigSchema,
        feedback: feedbackConfigSchema,
        search: searchConfigSchema,
        colorMode: colorModeConfigSchema,
        navigation: navigationConfigSchema,
        codeSnippet: codeSnippetConfigSchema,
        markdown: markdownConfigSchema,
        openapi: openapiConfigSchema,
        graphql: graphqlConfigSchema,
        analytics: analyticsConfigSchema,
        userMenu: userMenuConfigSchema,
        versionPicker: versionPickerConfigSchema,
        breadcrumbs: breadcrumbsConfigSchema,
        catalog: catalogsConfigSchema,
        scorecard: scorecardConfigSchema
      },
      default: { redirects: {} },
      additionalProperties: true
    };
    environmentSchema = Object.assign(Object.assign({}, removePropertyRecursively(redoclyConfigSchema, "default")), { additionalProperties: false });
    rootRedoclyConfigSchema = Object.assign(Object.assign({ $id: "root-redocly-config" }, redoclyConfigSchema), { properties: Object.assign(Object.assign({ plugins: {
      type: "array",
      items: { type: "string" }
    } }, redoclyConfigSchema.properties), { env: {
      type: "object",
      additionalProperties: environmentSchema
      // TODO: if we want full validation we need to override apis, theme and the root
    } }), default: {}, additionalProperties: false });
  }
});

// node_modules/@redocly/config/lib-esm/product-override-schema.js
var productThemeOverrideSchema, productConfigOverrideSchema;
var init_product_override_schema = __esm({
  "node_modules/@redocly/config/lib-esm/product-override-schema.js"() {
    init_default_theme_config_schema();
    init_ex_theme_config_schemas();
    init_root_config_schema();
    productThemeOverrideSchema = {
      type: "object",
      properties: {
        logo: themeConfigSchema.properties.logo,
        navbar: themeConfigSchema.properties.navbar,
        footer: themeConfigSchema.properties.footer,
        sidebar: themeConfigSchema.properties.sidebar,
        search: themeConfigSchema.properties.search,
        codeSnippet: themeConfigSchema.properties.codeSnippet,
        breadcrumbs: themeConfigSchema.properties.breadcrumbs,
        openapi: themeConfigSchema.properties.openapi,
        feedback: themeConfigSchema.properties.feedback,
        analytics: {
          type: "object",
          properties: {
            ga: productGoogleAnalyticsConfigSchema
          }
        }
      },
      additionalProperties: true,
      default: {}
    };
    productConfigOverrideSchema = {
      $id: "product-config-override",
      type: "object",
      properties: Object.assign(Object.assign({}, productThemeOverrideSchema.properties), {
        apis: {
          type: "object",
          additionalProperties: apiConfigSchema
        },
        /**
         * @deprecated left for backwards compatibility
         */
        theme: productThemeOverrideSchema
      }),
      additionalProperties: false
    };
  }
});

// node_modules/@redocly/config/lib-esm/types/api-functions-types.js
var init_api_functions_types = __esm({
  "node_modules/@redocly/config/lib-esm/types/api-functions-types.js"() {
  }
});

// node_modules/@redocly/config/lib-esm/types/config-types.js
var init_config_types = __esm({
  "node_modules/@redocly/config/lib-esm/types/config-types.js"() {
  }
});

// node_modules/@redocly/config/lib-esm/types/code-walkthrough-types.js
var init_code_walkthrough_types = __esm({
  "node_modules/@redocly/config/lib-esm/types/code-walkthrough-types.js"() {
  }
});

// node_modules/@redocly/config/lib-esm/types/portal-shared-types.js
var init_portal_shared_types = __esm({
  "node_modules/@redocly/config/lib-esm/types/portal-shared-types.js"() {
    init_code_walkthrough_types();
  }
});

// node_modules/@redocly/config/lib-esm/types/index.js
var init_types = __esm({
  "node_modules/@redocly/config/lib-esm/types/index.js"() {
    init_api_functions_types();
    init_config_types();
    init_portal_shared_types();
  }
});

// node_modules/@redocly/config/lib-esm/index.js
var lib_esm_exports = {};
__export(lib_esm_exports, {
  ApigeeDevOnboardingIntegrationAuthType: () => ApigeeDevOnboardingIntegrationAuthType,
  AuthProviderType: () => AuthProviderType,
  DEFAULT_TEAM_CLAIM_NAME: () => DEFAULT_TEAM_CLAIM_NAME,
  LayoutVariant: () => LayoutVariant,
  REDOCLY_ROUTE_RBAC: () => REDOCLY_ROUTE_RBAC,
  REDOCLY_TEAMS_RBAC: () => REDOCLY_TEAMS_RBAC,
  productConfigOverrideSchema: () => productConfigOverrideSchema,
  productThemeOverrideSchema: () => productThemeOverrideSchema,
  rbacConfigSchema: () => rbacConfigSchema,
  redocConfigSchema: () => redocConfigSchema,
  rootRedoclyConfigSchema: () => rootRedoclyConfigSchema
});
var init_lib_esm = __esm({
  "node_modules/@redocly/config/lib-esm/index.js"() {
    init_product_override_schema();
    init_root_config_schema();
    init_redoc_config_schema();
    init_types();
    init_constants();
  }
});

// node_modules/@redocly/ajv/dist/compile/codegen/code.js
var require_code = __commonJS({
  "node_modules/@redocly/ajv/dist/compile/codegen/code.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.nil = exports._Code = exports.Name = exports.IDENTIFIER = exports._CodeOrName = void 0;
    exports._ = _15;
    exports.str = str;
    exports.addCodeArg = addCodeArg;
    exports.strConcat = strConcat;
    exports.stringify = stringify5;
    exports.safeStringify = safeStringify;
    exports.getProperty = getProperty;
    exports.getEsmExportName = getEsmExportName;
    exports.regexpCode = regexpCode;
    var _CodeOrName = class {
    };
    exports._CodeOrName = _CodeOrName;
    exports.IDENTIFIER = /^[a-z$_][a-z$_0-9]*$/i;
    var Name = class extends _CodeOrName {
      constructor(s2) {
        super();
        if (!exports.IDENTIFIER.test(s2))
          throw new Error("CodeGen: name must be a valid identifier");
        this.str = s2;
      }
      toString() {
        return this.str;
      }
      emptyStr() {
        return false;
      }
      get names() {
        return { [this.str]: 1 };
      }
    };
    exports.Name = Name;
    var _Code = class extends _CodeOrName {
      constructor(code) {
        super();
        this._items = typeof code === "string" ? [code] : code;
      }
      toString() {
        return this.str;
      }
      emptyStr() {
        if (this._items.length > 1)
          return false;
        const item = this._items[0];
        return item === "" || item === '""';
      }
      get str() {
        var _a3;
        return (_a3 = this._str) !== null && _a3 !== void 0 ? _a3 : this._str = this._items.reduce((s2, c2) => `${s2}${c2}`, "");
      }
      get names() {
        var _a3;
        return (_a3 = this._names) !== null && _a3 !== void 0 ? _a3 : this._names = this._items.reduce((names, c2) => {
          if (c2 instanceof Name)
            names[c2.str] = (names[c2.str] || 0) + 1;
          return names;
        }, {});
      }
    };
    exports._Code = _Code;
    exports.nil = new _Code("");
    function _15(strs, ...args) {
      const code = [strs[0]];
      let i2 = 0;
      while (i2 < args.length) {
        addCodeArg(code, args[i2]);
        code.push(strs[++i2]);
      }
      return new _Code(code);
    }
    var plus = new _Code("+");
    function str(strs, ...args) {
      const expr = [safeStringify(strs[0])];
      let i2 = 0;
      while (i2 < args.length) {
        expr.push(plus);
        addCodeArg(expr, args[i2]);
        expr.push(plus, safeStringify(strs[++i2]));
      }
      optimize(expr);
      return new _Code(expr);
    }
    function addCodeArg(code, arg) {
      if (arg instanceof _Code)
        code.push(...arg._items);
      else if (arg instanceof Name)
        code.push(arg);
      else
        code.push(interpolate(arg));
    }
    function optimize(expr) {
      let i2 = 1;
      while (i2 < expr.length - 1) {
        if (expr[i2] === plus) {
          const res = mergeExprItems(expr[i2 - 1], expr[i2 + 1]);
          if (res !== void 0) {
            expr.splice(i2 - 1, 3, res);
            continue;
          }
          expr[i2++] = "+";
        }
        i2++;
      }
    }
    function mergeExprItems(a2, b2) {
      if (b2 === '""')
        return a2;
      if (a2 === '""')
        return b2;
      if (typeof a2 == "string") {
        if (b2 instanceof Name || a2[a2.length - 1] !== '"')
          return;
        if (typeof b2 != "string")
          return `${a2.slice(0, -1)}${b2}"`;
        if (b2[0] === '"')
          return a2.slice(0, -1) + b2.slice(1);
        return;
      }
      if (typeof b2 == "string" && b2[0] === '"' && !(a2 instanceof Name))
        return `"${a2}${b2.slice(1)}`;
      return;
    }
    function strConcat(c1, c2) {
      return c2.emptyStr() ? c1 : c1.emptyStr() ? c2 : str`${c1}${c2}`;
    }
    function interpolate(x2) {
      return typeof x2 == "number" || typeof x2 == "boolean" || x2 === null ? x2 : safeStringify(Array.isArray(x2) ? x2.join(",") : x2);
    }
    function stringify5(x2) {
      return new _Code(safeStringify(x2));
    }
    function safeStringify(x2) {
      return JSON.stringify(x2).replace(/\u2028/g, "\\u2028").replace(/\u2029/g, "\\u2029");
    }
    function getProperty(key2) {
      return typeof key2 == "string" && exports.IDENTIFIER.test(key2) ? new _Code(`.${key2}`) : _15`[${key2}]`;
    }
    function getEsmExportName(key2) {
      if (typeof key2 == "string" && exports.IDENTIFIER.test(key2)) {
        return new _Code(`${key2}`);
      }
      throw new Error(`CodeGen: invalid export name: ${key2}, use explicit $id name mapping`);
    }
    function regexpCode(rx) {
      return new _Code(rx.toString());
    }
  }
});

// node_modules/@redocly/ajv/dist/compile/codegen/scope.js
var require_scope = __commonJS({
  "node_modules/@redocly/ajv/dist/compile/codegen/scope.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ValueScope = exports.ValueScopeName = exports.Scope = exports.varKinds = exports.UsedValueState = void 0;
    var code_1 = require_code();
    var ValueError = class extends Error {
      constructor(name) {
        super(`CodeGen: "code" for ${name} not defined`);
        this.value = name.value;
      }
    };
    var UsedValueState;
    (function(UsedValueState2) {
      UsedValueState2[UsedValueState2["Started"] = 0] = "Started";
      UsedValueState2[UsedValueState2["Completed"] = 1] = "Completed";
    })(UsedValueState || (exports.UsedValueState = UsedValueState = {}));
    exports.varKinds = {
      const: new code_1.Name("const"),
      let: new code_1.Name("let"),
      var: new code_1.Name("var")
    };
    var Scope = class {
      constructor({ prefixes, parent } = {}) {
        this._names = {};
        this._prefixes = prefixes;
        this._parent = parent;
      }
      toName(nameOrPrefix) {
        return nameOrPrefix instanceof code_1.Name ? nameOrPrefix : this.name(nameOrPrefix);
      }
      name(prefix2) {
        return new code_1.Name(this._newName(prefix2));
      }
      _newName(prefix2) {
        const ng = this._names[prefix2] || this._nameGroup(prefix2);
        return `${prefix2}${ng.index++}`;
      }
      _nameGroup(prefix2) {
        var _a3, _b2;
        if (((_b2 = (_a3 = this._parent) === null || _a3 === void 0 ? void 0 : _a3._prefixes) === null || _b2 === void 0 ? void 0 : _b2.has(prefix2)) || this._prefixes && !this._prefixes.has(prefix2)) {
          throw new Error(`CodeGen: prefix "${prefix2}" is not allowed in this scope`);
        }
        return this._names[prefix2] = { prefix: prefix2, index: 0 };
      }
    };
    exports.Scope = Scope;
    var ValueScopeName = class extends code_1.Name {
      constructor(prefix2, nameStr) {
        super(nameStr);
        this.prefix = prefix2;
      }
      setValue(value, { property, itemIndex }) {
        this.value = value;
        this.scopePath = (0, code_1._)`.${new code_1.Name(property)}[${itemIndex}]`;
      }
    };
    exports.ValueScopeName = ValueScopeName;
    var line2 = (0, code_1._)`\n`;
    var ValueScope = class extends Scope {
      constructor(opts) {
        super(opts);
        this._values = {};
        this._scope = opts.scope;
        this.opts = { ...opts, _n: opts.lines ? line2 : code_1.nil };
      }
      get() {
        return this._scope;
      }
      name(prefix2) {
        return new ValueScopeName(prefix2, this._newName(prefix2));
      }
      value(nameOrPrefix, value) {
        var _a3;
        if (value.ref === void 0)
          throw new Error("CodeGen: ref must be passed in value");
        const name = this.toName(nameOrPrefix);
        const { prefix: prefix2 } = name;
        const valueKey = (_a3 = value.key) !== null && _a3 !== void 0 ? _a3 : value.ref;
        let vs = this._values[prefix2];
        if (vs) {
          const _name = vs.get(valueKey);
          if (_name)
            return _name;
        } else {
          vs = this._values[prefix2] = /* @__PURE__ */ new Map();
        }
        vs.set(valueKey, name);
        const s2 = this._scope[prefix2] || (this._scope[prefix2] = []);
        const itemIndex = s2.length;
        s2[itemIndex] = value.ref;
        name.setValue(value, { property: prefix2, itemIndex });
        return name;
      }
      getValue(prefix2, keyOrRef) {
        const vs = this._values[prefix2];
        if (!vs)
          return;
        return vs.get(keyOrRef);
      }
      scopeRefs(scopeName, values2 = this._values) {
        return this._reduceValues(values2, (name) => {
          if (name.scopePath === void 0)
            throw new Error(`CodeGen: name "${name}" has no value`);
          return (0, code_1._)`${scopeName}${name.scopePath}`;
        });
      }
      scopeCode(values2 = this._values, usedValues, getCode) {
        return this._reduceValues(values2, (name) => {
          if (name.value === void 0)
            throw new Error(`CodeGen: name "${name}" has no value`);
          return name.value.code;
        }, usedValues, getCode);
      }
      _reduceValues(values2, valueCode, usedValues = {}, getCode) {
        let code = code_1.nil;
        for (const prefix2 in values2) {
          const vs = values2[prefix2];
          if (!vs)
            continue;
          const nameSet = usedValues[prefix2] = usedValues[prefix2] || /* @__PURE__ */ new Map();
          vs.forEach((name) => {
            if (nameSet.has(name))
              return;
            nameSet.set(name, UsedValueState.Started);
            let c2 = valueCode(name);
            if (c2) {
              const def = this.opts.es5 ? exports.varKinds.var : exports.varKinds.const;
              code = (0, code_1._)`${code}${def} ${name} = ${c2};${this.opts._n}`;
            } else if (c2 = getCode === null || getCode === void 0 ? void 0 : getCode(name)) {
              code = (0, code_1._)`${code}${c2}${this.opts._n}`;
            } else {
              throw new ValueError(name);
            }
            nameSet.set(name, UsedValueState.Completed);
          });
        }
        return code;
      }
    };
    exports.ValueScope = ValueScope;
  }
});

// node_modules/@redocly/ajv/dist/compile/codegen/index.js
var require_codegen = __commonJS({
  "node_modules/@redocly/ajv/dist/compile/codegen/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.CodeGen = exports.operators = exports.varKinds = exports.ValueScopeName = exports.ValueScope = exports.Scope = exports.Name = exports.regexpCode = exports.stringify = exports.getProperty = exports.nil = exports.strConcat = exports.str = exports._ = void 0;
    exports.not = not;
    exports.and = and;
    exports.or = or;
    var code_1 = require_code();
    var scope_1 = require_scope();
    var code_2 = require_code();
    Object.defineProperty(exports, "_", { enumerable: true, get: function() {
      return code_2._;
    } });
    Object.defineProperty(exports, "str", { enumerable: true, get: function() {
      return code_2.str;
    } });
    Object.defineProperty(exports, "strConcat", { enumerable: true, get: function() {
      return code_2.strConcat;
    } });
    Object.defineProperty(exports, "nil", { enumerable: true, get: function() {
      return code_2.nil;
    } });
    Object.defineProperty(exports, "getProperty", { enumerable: true, get: function() {
      return code_2.getProperty;
    } });
    Object.defineProperty(exports, "stringify", { enumerable: true, get: function() {
      return code_2.stringify;
    } });
    Object.defineProperty(exports, "regexpCode", { enumerable: true, get: function() {
      return code_2.regexpCode;
    } });
    Object.defineProperty(exports, "Name", { enumerable: true, get: function() {
      return code_2.Name;
    } });
    var scope_2 = require_scope();
    Object.defineProperty(exports, "Scope", { enumerable: true, get: function() {
      return scope_2.Scope;
    } });
    Object.defineProperty(exports, "ValueScope", { enumerable: true, get: function() {
      return scope_2.ValueScope;
    } });
    Object.defineProperty(exports, "ValueScopeName", { enumerable: true, get: function() {
      return scope_2.ValueScopeName;
    } });
    Object.defineProperty(exports, "varKinds", { enumerable: true, get: function() {
      return scope_2.varKinds;
    } });
    exports.operators = {
      GT: new code_1._Code(">"),
      GTE: new code_1._Code(">="),
      LT: new code_1._Code("<"),
      LTE: new code_1._Code("<="),
      EQ: new code_1._Code("==="),
      NEQ: new code_1._Code("!=="),
      NOT: new code_1._Code("!"),
      OR: new code_1._Code("||"),
      AND: new code_1._Code("&&"),
      ADD: new code_1._Code("+")
    };
    var Node2 = class {
      optimizeNodes() {
        return this;
      }
      optimizeNames(_names, _constants) {
        return this;
      }
    };
    var Def = class extends Node2 {
      constructor(varKind, name, rhs) {
        super();
        this.varKind = varKind;
        this.name = name;
        this.rhs = rhs;
      }
      render({ es5, _n }) {
        const varKind = es5 ? scope_1.varKinds.var : this.varKind;
        const rhs = this.rhs === void 0 ? "" : ` = ${this.rhs}`;
        return `${varKind} ${this.name}${rhs};` + _n;
      }
      optimizeNames(names, constants) {
        if (!names[this.name.str])
          return;
        if (this.rhs)
          this.rhs = optimizeExpr(this.rhs, names, constants);
        return this;
      }
      get names() {
        return this.rhs instanceof code_1._CodeOrName ? this.rhs.names : {};
      }
    };
    var Assign = class extends Node2 {
      constructor(lhs, rhs, sideEffects) {
        super();
        this.lhs = lhs;
        this.rhs = rhs;
        this.sideEffects = sideEffects;
      }
      render({ _n }) {
        return `${this.lhs} = ${this.rhs};` + _n;
      }
      optimizeNames(names, constants) {
        if (this.lhs instanceof code_1.Name && !names[this.lhs.str] && !this.sideEffects)
          return;
        this.rhs = optimizeExpr(this.rhs, names, constants);
        return this;
      }
      get names() {
        const names = this.lhs instanceof code_1.Name ? {} : { ...this.lhs.names };
        return addExprNames(names, this.rhs);
      }
    };
    var AssignOp = class extends Assign {
      constructor(lhs, op, rhs, sideEffects) {
        super(lhs, rhs, sideEffects);
        this.op = op;
      }
      render({ _n }) {
        return `${this.lhs} ${this.op}= ${this.rhs};` + _n;
      }
    };
    var Label = class extends Node2 {
      constructor(label) {
        super();
        this.label = label;
        this.names = {};
      }
      render({ _n }) {
        return `${this.label}:` + _n;
      }
    };
    var Break = class extends Node2 {
      constructor(label) {
        super();
        this.label = label;
        this.names = {};
      }
      render({ _n }) {
        const label = this.label ? ` ${this.label}` : "";
        return `break${label};` + _n;
      }
    };
    var Throw = class extends Node2 {
      constructor(error) {
        super();
        this.error = error;
      }
      render({ _n }) {
        return `throw ${this.error};` + _n;
      }
      get names() {
        return this.error.names;
      }
    };
    var AnyCode = class extends Node2 {
      constructor(code) {
        super();
        this.code = code;
      }
      render({ _n }) {
        return `${this.code};` + _n;
      }
      optimizeNodes() {
        return `${this.code}` ? this : void 0;
      }
      optimizeNames(names, constants) {
        this.code = optimizeExpr(this.code, names, constants);
        return this;
      }
      get names() {
        return this.code instanceof code_1._CodeOrName ? this.code.names : {};
      }
    };
    var ParentNode = class extends Node2 {
      constructor(nodes = []) {
        super();
        this.nodes = nodes;
      }
      render(opts) {
        return this.nodes.reduce((code, n) => code + n.render(opts), "");
      }
      optimizeNodes() {
        const { nodes } = this;
        let i2 = nodes.length;
        while (i2--) {
          const n = nodes[i2].optimizeNodes();
          if (Array.isArray(n))
            nodes.splice(i2, 1, ...n);
          else if (n)
            nodes[i2] = n;
          else
            nodes.splice(i2, 1);
        }
        return nodes.length > 0 ? this : void 0;
      }
      optimizeNames(names, constants) {
        const { nodes } = this;
        let i2 = nodes.length;
        while (i2--) {
          const n = nodes[i2];
          if (n.optimizeNames(names, constants))
            continue;
          subtractNames(names, n.names);
          nodes.splice(i2, 1);
        }
        return nodes.length > 0 ? this : void 0;
      }
      get names() {
        return this.nodes.reduce((names, n) => addNames(names, n.names), {});
      }
    };
    var BlockNode = class extends ParentNode {
      render(opts) {
        return "{" + opts._n + super.render(opts) + "}" + opts._n;
      }
    };
    var Root = class extends ParentNode {
    };
    var Else = class extends BlockNode {
    };
    Else.kind = "else";
    var If = class _If extends BlockNode {
      constructor(condition, nodes) {
        super(nodes);
        this.condition = condition;
      }
      render(opts) {
        let code = `if(${this.condition})` + super.render(opts);
        if (this.else)
          code += "else " + this.else.render(opts);
        return code;
      }
      optimizeNodes() {
        super.optimizeNodes();
        const cond = this.condition;
        if (cond === true)
          return this.nodes;
        let e = this.else;
        if (e) {
          const ns = e.optimizeNodes();
          e = this.else = Array.isArray(ns) ? new Else(ns) : ns;
        }
        if (e) {
          if (cond === false)
            return e instanceof _If ? e : e.nodes;
          if (this.nodes.length)
            return this;
          return new _If(not(cond), e instanceof _If ? [e] : e.nodes);
        }
        if (cond === false || !this.nodes.length)
          return void 0;
        return this;
      }
      optimizeNames(names, constants) {
        var _a3;
        this.else = (_a3 = this.else) === null || _a3 === void 0 ? void 0 : _a3.optimizeNames(names, constants);
        if (!(super.optimizeNames(names, constants) || this.else))
          return;
        this.condition = optimizeExpr(this.condition, names, constants);
        return this;
      }
      get names() {
        const names = super.names;
        addExprNames(names, this.condition);
        if (this.else)
          addNames(names, this.else.names);
        return names;
      }
    };
    If.kind = "if";
    var For = class extends BlockNode {
    };
    For.kind = "for";
    var ForLoop = class extends For {
      constructor(iteration) {
        super();
        this.iteration = iteration;
      }
      render(opts) {
        return `for(${this.iteration})` + super.render(opts);
      }
      optimizeNames(names, constants) {
        if (!super.optimizeNames(names, constants))
          return;
        this.iteration = optimizeExpr(this.iteration, names, constants);
        return this;
      }
      get names() {
        return addNames(super.names, this.iteration.names);
      }
    };
    var ForRange = class extends For {
      constructor(varKind, name, from2, to) {
        super();
        this.varKind = varKind;
        this.name = name;
        this.from = from2;
        this.to = to;
      }
      render(opts) {
        const varKind = opts.es5 ? scope_1.varKinds.var : this.varKind;
        const { name, from: from2, to } = this;
        return `for(${varKind} ${name}=${from2}; ${name}<${to}; ${name}++)` + super.render(opts);
      }
      get names() {
        const names = addExprNames(super.names, this.from);
        return addExprNames(names, this.to);
      }
    };
    var ForIter = class extends For {
      constructor(loop, varKind, name, iterable) {
        super();
        this.loop = loop;
        this.varKind = varKind;
        this.name = name;
        this.iterable = iterable;
      }
      render(opts) {
        return `for(${this.varKind} ${this.name} ${this.loop} ${this.iterable})` + super.render(opts);
      }
      optimizeNames(names, constants) {
        if (!super.optimizeNames(names, constants))
          return;
        this.iterable = optimizeExpr(this.iterable, names, constants);
        return this;
      }
      get names() {
        return addNames(super.names, this.iterable.names);
      }
    };
    var Func = class extends BlockNode {
      constructor(name, args, async) {
        super();
        this.name = name;
        this.args = args;
        this.async = async;
      }
      render(opts) {
        const _async = this.async ? "async " : "";
        return `${_async}function ${this.name}(${this.args})` + super.render(opts);
      }
    };
    Func.kind = "func";
    var Return = class extends ParentNode {
      render(opts) {
        return "return " + super.render(opts);
      }
    };
    Return.kind = "return";
    var Try = class extends BlockNode {
      render(opts) {
        let code = "try" + super.render(opts);
        if (this.catch)
          code += this.catch.render(opts);
        if (this.finally)
          code += this.finally.render(opts);
        return code;
      }
      optimizeNodes() {
        var _a3, _b2;
        super.optimizeNodes();
        (_a3 = this.catch) === null || _a3 === void 0 ? void 0 : _a3.optimizeNodes();
        (_b2 = this.finally) === null || _b2 === void 0 ? void 0 : _b2.optimizeNodes();
        return this;
      }
      optimizeNames(names, constants) {
        var _a3, _b2;
        super.optimizeNames(names, constants);
        (_a3 = this.catch) === null || _a3 === void 0 ? void 0 : _a3.optimizeNames(names, constants);
        (_b2 = this.finally) === null || _b2 === void 0 ? void 0 : _b2.optimizeNames(names, constants);
        return this;
      }
      get names() {
        const names = super.names;
        if (this.catch)
          addNames(names, this.catch.names);
        if (this.finally)
          addNames(names, this.finally.names);
        return names;
      }
    };
    var Catch = class extends BlockNode {
      constructor(error) {
        super();
        this.error = error;
      }
      render(opts) {
        return `catch(${this.error})` + super.render(opts);
      }
    };
    Catch.kind = "catch";
    var Finally = class extends BlockNode {
      render(opts) {
        return "finally" + super.render(opts);
      }
    };
    Finally.kind = "finally";
    var CodeGen = class {
      constructor(extScope, opts = {}) {
        this._values = {};
        this._blockStarts = [];
        this._constants = {};
        this.opts = { ...opts, _n: opts.lines ? "\n" : "" };
        this._extScope = extScope;
        this._scope = new scope_1.Scope({ parent: extScope });
        this._nodes = [new Root()];
      }
      toString() {
        return this._root.render(this.opts);
      }
      // returns unique name in the internal scope
      name(prefix2) {
        return this._scope.name(prefix2);
      }
      // reserves unique name in the external scope
      scopeName(prefix2) {
        return this._extScope.name(prefix2);
      }
      // reserves unique name in the external scope and assigns value to it
      scopeValue(prefixOrName, value) {
        const name = this._extScope.value(prefixOrName, value);
        const vs = this._values[name.prefix] || (this._values[name.prefix] = /* @__PURE__ */ new Set());
        vs.add(name);
        return name;
      }
      getScopeValue(prefix2, keyOrRef) {
        return this._extScope.getValue(prefix2, keyOrRef);
      }
      // return code that assigns values in the external scope to the names that are used internally
      // (same names that were returned by gen.scopeName or gen.scopeValue)
      scopeRefs(scopeName) {
        return this._extScope.scopeRefs(scopeName, this._values);
      }
      scopeCode() {
        return this._extScope.scopeCode(this._values);
      }
      _def(varKind, nameOrPrefix, rhs, constant) {
        const name = this._scope.toName(nameOrPrefix);
        if (rhs !== void 0 && constant)
          this._constants[name.str] = rhs;
        this._leafNode(new Def(varKind, name, rhs));
        return name;
      }
      // `const` declaration (`var` in es5 mode)
      const(nameOrPrefix, rhs, _constant) {
        return this._def(scope_1.varKinds.const, nameOrPrefix, rhs, _constant);
      }
      // `let` declaration with optional assignment (`var` in es5 mode)
      let(nameOrPrefix, rhs, _constant) {
        return this._def(scope_1.varKinds.let, nameOrPrefix, rhs, _constant);
      }
      // `var` declaration with optional assignment
      var(nameOrPrefix, rhs, _constant) {
        return this._def(scope_1.varKinds.var, nameOrPrefix, rhs, _constant);
      }
      // assignment code
      assign(lhs, rhs, sideEffects) {
        return this._leafNode(new Assign(lhs, rhs, sideEffects));
      }
      // `+=` code
      add(lhs, rhs) {
        return this._leafNode(new AssignOp(lhs, exports.operators.ADD, rhs));
      }
      // appends passed SafeExpr to code or executes Block
      code(c2) {
        if (typeof c2 == "function")
          c2();
        else if (c2 !== code_1.nil)
          this._leafNode(new AnyCode(c2));
        return this;
      }
      // returns code for object literal for the passed argument list of key-value pairs
      object(...keyValues) {
        const code = ["{"];
        for (const [key2, value] of keyValues) {
          if (code.length > 1)
            code.push(",");
          code.push(key2);
          if (key2 !== value || this.opts.es5) {
            code.push(":");
            (0, code_1.addCodeArg)(code, value);
          }
        }
        code.push("}");
        return new code_1._Code(code);
      }
      // `if` clause (or statement if `thenBody` and, optionally, `elseBody` are passed)
      if(condition, thenBody, elseBody) {
        this._blockNode(new If(condition));
        if (thenBody && elseBody) {
          this.code(thenBody).else().code(elseBody).endIf();
        } else if (thenBody) {
          this.code(thenBody).endIf();
        } else if (elseBody) {
          throw new Error('CodeGen: "else" body without "then" body');
        }
        return this;
      }
      // `else if` clause - invalid without `if` or after `else` clauses
      elseIf(condition) {
        return this._elseNode(new If(condition));
      }
      // `else` clause - only valid after `if` or `else if` clauses
      else() {
        return this._elseNode(new Else());
      }
      // end `if` statement (needed if gen.if was used only with condition)
      endIf() {
        return this._endBlockNode(If, Else);
      }
      _for(node2, forBody) {
        this._blockNode(node2);
        if (forBody)
          this.code(forBody).endFor();
        return this;
      }
      // a generic `for` clause (or statement if `forBody` is passed)
      for(iteration, forBody) {
        return this._for(new ForLoop(iteration), forBody);
      }
      // `for` statement for a range of values
      forRange(nameOrPrefix, from2, to, forBody, varKind = this.opts.es5 ? scope_1.varKinds.var : scope_1.varKinds.let) {
        const name = this._scope.toName(nameOrPrefix);
        return this._for(new ForRange(varKind, name, from2, to), () => forBody(name));
      }
      // `for-of` statement (in es5 mode replace with a normal for loop)
      forOf(nameOrPrefix, iterable, forBody, varKind = scope_1.varKinds.const) {
        const name = this._scope.toName(nameOrPrefix);
        if (this.opts.es5) {
          const arr = iterable instanceof code_1.Name ? iterable : this.var("_arr", iterable);
          return this.forRange("_i", 0, (0, code_1._)`${arr}.length`, (i2) => {
            this.var(name, (0, code_1._)`${arr}[${i2}]`);
            forBody(name);
          });
        }
        return this._for(new ForIter("of", varKind, name, iterable), () => forBody(name));
      }
      // `for-in` statement.
      // With option `ownProperties` replaced with a `for-of` loop for object keys
      forIn(nameOrPrefix, obj, forBody, varKind = this.opts.es5 ? scope_1.varKinds.var : scope_1.varKinds.const) {
        if (this.opts.ownProperties) {
          return this.forOf(nameOrPrefix, (0, code_1._)`Object.keys(${obj})`, forBody);
        }
        const name = this._scope.toName(nameOrPrefix);
        return this._for(new ForIter("in", varKind, name, obj), () => forBody(name));
      }
      // end `for` loop
      endFor() {
        return this._endBlockNode(For);
      }
      // `label` statement
      label(label) {
        return this._leafNode(new Label(label));
      }
      // `break` statement
      break(label) {
        return this._leafNode(new Break(label));
      }
      // `return` statement
      return(value) {
        const node2 = new Return();
        this._blockNode(node2);
        this.code(value);
        if (node2.nodes.length !== 1)
          throw new Error('CodeGen: "return" should have one node');
        return this._endBlockNode(Return);
      }
      // `try` statement
      try(tryBody, catchCode, finallyCode) {
        if (!catchCode && !finallyCode)
          throw new Error('CodeGen: "try" without "catch" and "finally"');
        const node2 = new Try();
        this._blockNode(node2);
        this.code(tryBody);
        if (catchCode) {
          const error = this.name("e");
          this._currNode = node2.catch = new Catch(error);
          catchCode(error);
        }
        if (finallyCode) {
          this._currNode = node2.finally = new Finally();
          this.code(finallyCode);
        }
        return this._endBlockNode(Catch, Finally);
      }
      // `throw` statement
      throw(error) {
        return this._leafNode(new Throw(error));
      }
      // start self-balancing block
      block(body, nodeCount) {
        this._blockStarts.push(this._nodes.length);
        if (body)
          this.code(body).endBlock(nodeCount);
        return this;
      }
      // end the current self-balancing block
      endBlock(nodeCount) {
        const len = this._blockStarts.pop();
        if (len === void 0)
          throw new Error("CodeGen: not in self-balancing block");
        const toClose = this._nodes.length - len;
        if (toClose < 0 || nodeCount !== void 0 && toClose !== nodeCount) {
          throw new Error(`CodeGen: wrong number of nodes: ${toClose} vs ${nodeCount} expected`);
        }
        this._nodes.length = len;
        return this;
      }
      // `function` heading (or definition if funcBody is passed)
      func(name, args = code_1.nil, async, funcBody) {
        this._blockNode(new Func(name, args, async));
        if (funcBody)
          this.code(funcBody).endFunc();
        return this;
      }
      // end function definition
      endFunc() {
        return this._endBlockNode(Func);
      }
      optimize(n = 1) {
        while (n-- > 0) {
          this._root.optimizeNodes();
          this._root.optimizeNames(this._root.names, this._constants);
        }
      }
      _leafNode(node2) {
        this._currNode.nodes.push(node2);
        return this;
      }
      _blockNode(node2) {
        this._currNode.nodes.push(node2);
        this._nodes.push(node2);
      }
      _endBlockNode(N1, N2) {
        const n = this._currNode;
        if (n instanceof N1 || N2 && n instanceof N2) {
          this._nodes.pop();
          return this;
        }
        throw new Error(`CodeGen: not in block "${N2 ? `${N1.kind}/${N2.kind}` : N1.kind}"`);
      }
      _elseNode(node2) {
        const n = this._currNode;
        if (!(n instanceof If)) {
          throw new Error('CodeGen: "else" without "if"');
        }
        this._currNode = n.else = node2;
        return this;
      }
      get _root() {
        return this._nodes[0];
      }
      get _currNode() {
        const ns = this._nodes;
        return ns[ns.length - 1];
      }
      set _currNode(node2) {
        const ns = this._nodes;
        ns[ns.length - 1] = node2;
      }
    };
    exports.CodeGen = CodeGen;
    function addNames(names, from2) {
      for (const n in from2)
        names[n] = (names[n] || 0) + (from2[n] || 0);
      return names;
    }
    function addExprNames(names, from2) {
      return from2 instanceof code_1._CodeOrName ? addNames(names, from2.names) : names;
    }
    function optimizeExpr(expr, names, constants) {
      if (expr instanceof code_1.Name)
        return replaceName(expr);
      if (!canOptimize(expr))
        return expr;
      return new code_1._Code(expr._items.reduce((items, c2) => {
        if (c2 instanceof code_1.Name)
          c2 = replaceName(c2);
        if (c2 instanceof code_1._Code)
          items.push(...c2._items);
        else
          items.push(c2);
        return items;
      }, []));
      function replaceName(n) {
        const c2 = constants[n.str];
        if (c2 === void 0 || names[n.str] !== 1)
          return n;
        delete names[n.str];
        return c2;
      }
      function canOptimize(e) {
        return e instanceof code_1._Code && e._items.some((c2) => c2 instanceof code_1.Name && names[c2.str] === 1 && constants[c2.str] !== void 0);
      }
    }
    function subtractNames(names, from2) {
      for (const n in from2)
        names[n] = (names[n] || 0) - (from2[n] || 0);
    }
    function not(x2) {
      return typeof x2 == "boolean" || typeof x2 == "number" || x2 === null ? !x2 : (0, code_1._)`!${par(x2)}`;
    }
    var andCode = mappend(exports.operators.AND);
    function and(...args) {
      return args.reduce(andCode);
    }
    var orCode = mappend(exports.operators.OR);
    function or(...args) {
      return args.reduce(orCode);
    }
    function mappend(op) {
      return (x2, y2) => x2 === code_1.nil ? y2 : y2 === code_1.nil ? x2 : (0, code_1._)`${par(x2)} ${op} ${par(y2)}`;
    }
    function par(x2) {
      return x2 instanceof code_1.Name ? x2 : (0, code_1._)`(${x2})`;
    }
  }
});

// node_modules/@redocly/ajv/dist/compile/util.js
var require_util = __commonJS({
  "node_modules/@redocly/ajv/dist/compile/util.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Type = exports.mergeEvaluated = void 0;
    exports.toHash = toHash;
    exports.alwaysValidSchema = alwaysValidSchema;
    exports.checkUnknownRules = checkUnknownRules;
    exports.schemaHasRules = schemaHasRules;
    exports.schemaHasRulesButRef = schemaHasRulesButRef;
    exports.schemaRefOrVal = schemaRefOrVal;
    exports.unescapeFragment = unescapeFragment;
    exports.escapeFragment = escapeFragment;
    exports.escapeJsonPointer = escapeJsonPointer;
    exports.unescapeJsonPointer = unescapeJsonPointer;
    exports.eachItem = eachItem;
    exports.evaluatedPropsToName = evaluatedPropsToName;
    exports.setEvaluated = setEvaluated;
    exports.useFunc = useFunc;
    exports.getErrorPath = getErrorPath;
    exports.checkStrictMode = checkStrictMode;
    var codegen_1 = require_codegen();
    var code_1 = require_code();
    function toHash(arr) {
      const hash2 = {};
      for (const item of arr)
        hash2[item] = true;
      return hash2;
    }
    function alwaysValidSchema(it2, schema4) {
      if (typeof schema4 == "boolean")
        return schema4;
      if (Object.keys(schema4).length === 0)
        return true;
      checkUnknownRules(it2, schema4);
      return !schemaHasRules(schema4, it2.self.RULES.all);
    }
    function checkUnknownRules(it2, schema4 = it2.schema) {
      const { opts, self: self2 } = it2;
      if (!opts.strictSchema)
        return;
      if (typeof schema4 === "boolean")
        return;
      const rules = self2.RULES.keywords;
      for (const key2 in schema4) {
        if (!rules[key2])
          checkStrictMode(it2, `unknown keyword: "${key2}"`);
      }
    }
    function schemaHasRules(schema4, rules) {
      if (typeof schema4 == "boolean")
        return !schema4;
      for (const key2 in schema4)
        if (rules[key2])
          return true;
      return false;
    }
    function schemaHasRulesButRef(schema4, RULES) {
      if (typeof schema4 == "boolean")
        return !schema4;
      for (const key2 in schema4)
        if (key2 !== "$ref" && RULES.all[key2])
          return true;
      return false;
    }
    function schemaRefOrVal({ topSchemaRef, schemaPath }, schema4, keyword, $data) {
      if (!$data) {
        if (typeof schema4 == "number" || typeof schema4 == "boolean")
          return schema4;
        if (typeof schema4 == "string")
          return (0, codegen_1._)`${schema4}`;
      }
      return (0, codegen_1._)`${topSchemaRef}${schemaPath}${(0, codegen_1.getProperty)(keyword)}`;
    }
    function unescapeFragment(str) {
      return unescapeJsonPointer(decodeURIComponent(str));
    }
    function escapeFragment(str) {
      return encodeURIComponent(escapeJsonPointer(str));
    }
    function escapeJsonPointer(str) {
      if (typeof str == "number")
        return `${str}`;
      return str.replace(/~/g, "~0").replace(/\//g, "~1");
    }
    function unescapeJsonPointer(str) {
      return str.replace(/~1/g, "/").replace(/~0/g, "~");
    }
    function eachItem(xs, f2) {
      if (Array.isArray(xs)) {
        for (const x2 of xs)
          f2(x2);
      } else {
        f2(xs);
      }
    }
    function makeMergeEvaluated({ mergeNames, mergeToName, mergeValues, resultToName }) {
      return (gen, from2, to, toName) => {
        const res = to === void 0 ? from2 : to instanceof codegen_1.Name ? (from2 instanceof codegen_1.Name ? mergeNames(gen, from2, to) : mergeToName(gen, from2, to), to) : from2 instanceof codegen_1.Name ? (mergeToName(gen, to, from2), from2) : mergeValues(from2, to);
        return toName === codegen_1.Name && !(res instanceof codegen_1.Name) ? resultToName(gen, res) : res;
      };
    }
    exports.mergeEvaluated = {
      props: makeMergeEvaluated({
        mergeNames: (gen, from2, to) => gen.if((0, codegen_1._)`${to} !== true && ${from2} !== undefined`, () => {
          gen.if((0, codegen_1._)`${from2} === true`, () => gen.assign(to, true), () => gen.assign(to, (0, codegen_1._)`${to} || {}`).code((0, codegen_1._)`Object.assign(${to}, ${from2})`));
        }),
        mergeToName: (gen, from2, to) => gen.if((0, codegen_1._)`${to} !== true`, () => {
          if (from2 === true) {
            gen.assign(to, true);
          } else {
            gen.assign(to, (0, codegen_1._)`${to} || {}`);
            setEvaluated(gen, to, from2);
          }
        }),
        mergeValues: (from2, to) => from2 === true ? true : { ...from2, ...to },
        resultToName: evaluatedPropsToName
      }),
      items: makeMergeEvaluated({
        mergeNames: (gen, from2, to) => gen.if((0, codegen_1._)`${to} !== true && ${from2} !== undefined`, () => gen.assign(to, (0, codegen_1._)`${from2} === true ? true : ${to} > ${from2} ? ${to} : ${from2}`)),
        mergeToName: (gen, from2, to) => gen.if((0, codegen_1._)`${to} !== true`, () => gen.assign(to, from2 === true ? true : (0, codegen_1._)`${to} > ${from2} ? ${to} : ${from2}`)),
        mergeValues: (from2, to) => from2 === true ? true : Math.max(from2, to),
        resultToName: (gen, items) => gen.var("items", items)
      })
    };
    function evaluatedPropsToName(gen, ps) {
      if (ps === true)
        return gen.var("props", true);
      const props = gen.var("props", (0, codegen_1._)`{}`);
      if (ps !== void 0)
        setEvaluated(gen, props, ps);
      return props;
    }
    function setEvaluated(gen, props, ps) {
      Object.keys(ps).forEach((p2) => gen.assign((0, codegen_1._)`${props}${(0, codegen_1.getProperty)(p2)}`, true));
    }
    var snippets = {};
    function useFunc(gen, f2) {
      return gen.scopeValue("func", {
        ref: f2,
        code: snippets[f2.code] || (snippets[f2.code] = new code_1._Code(f2.code))
      });
    }
    var Type;
    (function(Type2) {
      Type2[Type2["Num"] = 0] = "Num";
      Type2[Type2["Str"] = 1] = "Str";
    })(Type || (exports.Type = Type = {}));
    function getErrorPath(dataProp, dataPropType, jsPropertySyntax) {
      if (dataProp instanceof codegen_1.Name) {
        const isNumber = dataPropType === Type.Num;
        return jsPropertySyntax ? isNumber ? (0, codegen_1._)`"[" + ${dataProp} + "]"` : (0, codegen_1._)`"['" + ${dataProp} + "']"` : isNumber ? (0, codegen_1._)`"/" + ${dataProp}` : (0, codegen_1._)`"/" + ${dataProp}.replace(/~/g, "~0").replace(/\\//g, "~1")`;
      }
      return jsPropertySyntax ? (0, codegen_1.getProperty)(dataProp).toString() : "/" + escapeJsonPointer(dataProp);
    }
    function checkStrictMode(it2, msg, mode = it2.opts.strictSchema) {
      if (!mode)
        return;
      msg = `strict mode: ${msg}`;
      if (mode === true)
        throw new Error(msg);
      it2.self.logger.warn(msg);
    }
  }
});

// node_modules/@redocly/ajv/dist/compile/names.js
var require_names = __commonJS({
  "node_modules/@redocly/ajv/dist/compile/names.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var codegen_1 = require_codegen();
    var names = {
      // validation function arguments
      data: new codegen_1.Name("data"),
      // data passed to validation function
      // args passed from referencing schema
      valCxt: new codegen_1.Name("valCxt"),
      // validation/data context - should not be used directly, it is destructured to the names below
      instancePath: new codegen_1.Name("instancePath"),
      parentData: new codegen_1.Name("parentData"),
      parentDataProperty: new codegen_1.Name("parentDataProperty"),
      rootData: new codegen_1.Name("rootData"),
      // root data - same as the data passed to the first/top validation function
      dynamicAnchors: new codegen_1.Name("dynamicAnchors"),
      // used to support recursiveRef and dynamicRef
      isAllOfVariant: new codegen_1.Name("isAllOfVariant"),
      // used to check in runtime if the current function (ref) is called from allOf
      // function scoped variables
      vErrors: new codegen_1.Name("vErrors"),
      // null or array of validation errors
      errors: new codegen_1.Name("errors"),
      // counter of validation errors
      this: new codegen_1.Name("this"),
      // "globals"
      self: new codegen_1.Name("self"),
      scope: new codegen_1.Name("scope"),
      // JTD serialize/parse name for JSON string and position
      json: new codegen_1.Name("json"),
      jsonPos: new codegen_1.Name("jsonPos"),
      jsonLen: new codegen_1.Name("jsonLen"),
      jsonPart: new codegen_1.Name("jsonPart")
    };
    exports.default = names;
  }
});

// node_modules/@redocly/ajv/dist/compile/errors.js
var require_errors = __commonJS({
  "node_modules/@redocly/ajv/dist/compile/errors.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.keyword$DataError = exports.keywordError = void 0;
    exports.reportError = reportError;
    exports.reportExtraError = reportExtraError;
    exports.resetErrorsCount = resetErrorsCount;
    exports.extendErrors = extendErrors;
    var codegen_1 = require_codegen();
    var util_1 = require_util();
    var names_1 = require_names();
    exports.keywordError = {
      message: ({ keyword }) => (0, codegen_1.str)`must pass "${keyword}" keyword validation`
    };
    exports.keyword$DataError = {
      message: ({ keyword, schemaType }) => schemaType ? (0, codegen_1.str)`"${keyword}" keyword must be ${schemaType} ($data)` : (0, codegen_1.str)`"${keyword}" keyword is invalid ($data)`
    };
    function reportError(cxt, error = exports.keywordError, errorPaths, overrideAllErrors) {
      const { it: it2 } = cxt;
      const { gen, compositeRule, allErrors } = it2;
      const errObj = errorObjectCode(cxt, error, errorPaths);
      if (overrideAllErrors !== null && overrideAllErrors !== void 0 ? overrideAllErrors : compositeRule || allErrors) {
        addError(gen, errObj);
      } else {
        returnErrors(it2, (0, codegen_1._)`[${errObj}]`);
      }
    }
    function reportExtraError(cxt, error = exports.keywordError, errorPaths) {
      const { it: it2 } = cxt;
      const { gen, compositeRule, allErrors } = it2;
      const errObj = errorObjectCode(cxt, error, errorPaths);
      addError(gen, errObj);
      if (!(compositeRule || allErrors)) {
        returnErrors(it2, names_1.default.vErrors);
      }
    }
    function resetErrorsCount(gen, errsCount) {
      gen.assign(names_1.default.errors, errsCount);
      gen.if((0, codegen_1._)`${names_1.default.vErrors} !== null`, () => gen.if(errsCount, () => gen.assign((0, codegen_1._)`${names_1.default.vErrors}.length`, errsCount), () => gen.assign(names_1.default.vErrors, null)));
    }
    function extendErrors({ gen, keyword, schemaValue, data, errsCount, it: it2 }) {
      if (errsCount === void 0)
        throw new Error("ajv implementation error");
      const err = gen.name("err");
      gen.forRange("i", errsCount, names_1.default.errors, (i2) => {
        gen.const(err, (0, codegen_1._)`${names_1.default.vErrors}[${i2}]`);
        gen.if((0, codegen_1._)`${err}.instancePath === undefined`, () => gen.assign((0, codegen_1._)`${err}.instancePath`, (0, codegen_1.strConcat)(names_1.default.instancePath, it2.errorPath)));
        gen.assign((0, codegen_1._)`${err}.schemaPath`, (0, codegen_1.str)`${it2.errSchemaPath}/${keyword}`);
        if (it2.opts.verbose) {
          gen.assign((0, codegen_1._)`${err}.schema`, schemaValue);
          gen.assign((0, codegen_1._)`${err}.data`, data);
        }
      });
    }
    function addError(gen, errObj) {
      const err = gen.const("err", errObj);
      gen.if((0, codegen_1._)`${names_1.default.vErrors} === null`, () => gen.assign(names_1.default.vErrors, (0, codegen_1._)`[${err}]`), (0, codegen_1._)`${names_1.default.vErrors}.push(${err})`);
      gen.code((0, codegen_1._)`${names_1.default.errors}++`);
    }
    function returnErrors(it2, errs) {
      const { gen, validateName, schemaEnv } = it2;
      if (schemaEnv.$async) {
        gen.throw((0, codegen_1._)`new ${it2.ValidationError}(${errs})`);
      } else {
        gen.assign((0, codegen_1._)`${validateName}.errors`, errs);
        gen.return(false);
      }
    }
    var E2 = {
      keyword: new codegen_1.Name("keyword"),
      schemaPath: new codegen_1.Name("schemaPath"),
      // also used in JTD errors
      params: new codegen_1.Name("params"),
      propertyName: new codegen_1.Name("propertyName"),
      message: new codegen_1.Name("message"),
      schema: new codegen_1.Name("schema"),
      parentSchema: new codegen_1.Name("parentSchema")
    };
    function errorObjectCode(cxt, error, errorPaths) {
      const { createErrors } = cxt.it;
      if (createErrors === false)
        return (0, codegen_1._)`{}`;
      return errorObject(cxt, error, errorPaths);
    }
    function errorObject(cxt, error, errorPaths = {}) {
      const { gen, it: it2 } = cxt;
      const keyValues = [
        errorInstancePath(it2, errorPaths),
        errorSchemaPath(cxt, errorPaths)
      ];
      extraErrorProps(cxt, error, keyValues);
      return gen.object(...keyValues);
    }
    function errorInstancePath({ errorPath }, { instancePath }) {
      const instPath = instancePath ? (0, codegen_1.str)`${errorPath}${(0, util_1.getErrorPath)(instancePath, util_1.Type.Str)}` : errorPath;
      return [names_1.default.instancePath, (0, codegen_1.strConcat)(names_1.default.instancePath, instPath)];
    }
    function errorSchemaPath({ keyword, it: { errSchemaPath } }, { schemaPath, parentSchema }) {
      let schPath = parentSchema ? errSchemaPath : (0, codegen_1.str)`${errSchemaPath}/${keyword}`;
      if (schemaPath) {
        schPath = (0, codegen_1.str)`${schPath}${(0, util_1.getErrorPath)(schemaPath, util_1.Type.Str)}`;
      }
      return [E2.schemaPath, schPath];
    }
    function extraErrorProps(cxt, { params, message }, keyValues) {
      const { keyword, data, schemaValue, it: it2 } = cxt;
      const { opts, propertyName, topSchemaRef, schemaPath } = it2;
      keyValues.push([E2.keyword, keyword], [E2.params, typeof params == "function" ? params(cxt) : params || (0, codegen_1._)`{}`]);
      if (opts.messages) {
        keyValues.push([E2.message, typeof message == "function" ? message(cxt) : message]);
      }
      if (opts.verbose) {
        keyValues.push([E2.schema, schemaValue], [E2.parentSchema, (0, codegen_1._)`${topSchemaRef}${schemaPath}`], [names_1.default.data, data]);
      }
      if (propertyName)
        keyValues.push([E2.propertyName, propertyName]);
    }
  }
});

// node_modules/@redocly/ajv/dist/compile/validate/boolSchema.js
var require_boolSchema = __commonJS({
  "node_modules/@redocly/ajv/dist/compile/validate/boolSchema.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.topBoolOrEmptySchema = topBoolOrEmptySchema;
    exports.boolOrEmptySchema = boolOrEmptySchema;
    var errors_1 = require_errors();
    var codegen_1 = require_codegen();
    var names_1 = require_names();
    var boolError = {
      message: "boolean schema is false"
    };
    function topBoolOrEmptySchema(it2) {
      const { gen, schema: schema4, validateName } = it2;
      if (schema4 === false) {
        falseSchemaError(it2, false);
      } else if (typeof schema4 == "object" && schema4.$async === true) {
        gen.return(names_1.default.data);
      } else {
        gen.assign((0, codegen_1._)`${validateName}.errors`, null);
        gen.return(true);
      }
    }
    function boolOrEmptySchema(it2, valid) {
      const { gen, schema: schema4 } = it2;
      if (schema4 === false) {
        gen.var(valid, false);
        falseSchemaError(it2);
      } else {
        gen.var(valid, true);
      }
    }
    function falseSchemaError(it2, overrideAllErrors) {
      const { gen, data } = it2;
      const cxt = {
        gen,
        keyword: "false schema",
        data,
        schema: false,
        schemaCode: false,
        schemaValue: false,
        params: {},
        it: it2
      };
      (0, errors_1.reportError)(cxt, boolError, void 0, overrideAllErrors);
    }
  }
});

// node_modules/@redocly/ajv/dist/compile/rules.js
var require_rules2 = __commonJS({
  "node_modules/@redocly/ajv/dist/compile/rules.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.isJSONType = isJSONType;
    exports.getRules = getRules;
    var _jsonTypes = ["string", "number", "integer", "boolean", "null", "object", "array"];
    var jsonTypes = new Set(_jsonTypes);
    function isJSONType(x2) {
      return typeof x2 == "string" && jsonTypes.has(x2);
    }
    function getRules() {
      const groups = {
        number: { type: "number", rules: [] },
        string: { type: "string", rules: [] },
        array: { type: "array", rules: [] },
        object: { type: "object", rules: [] }
      };
      return {
        types: { ...groups, integer: true, boolean: true, null: true },
        rules: [{ rules: [] }, groups.number, groups.string, groups.array, groups.object],
        post: { rules: [] },
        all: {},
        keywords: {}
      };
    }
  }
});

// node_modules/@redocly/ajv/dist/compile/validate/applicability.js
var require_applicability = __commonJS({
  "node_modules/@redocly/ajv/dist/compile/validate/applicability.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.schemaHasRulesForType = schemaHasRulesForType;
    exports.shouldUseGroup = shouldUseGroup;
    exports.shouldUseRule = shouldUseRule;
    function schemaHasRulesForType({ schema: schema4, self: self2 }, type) {
      const group = self2.RULES.types[type];
      return group && group !== true && shouldUseGroup(schema4, group);
    }
    function shouldUseGroup(schema4, group) {
      return group.rules.some((rule) => shouldUseRule(schema4, rule));
    }
    function shouldUseRule(schema4, rule) {
      var _a3;
      return schema4[rule.keyword] !== void 0 || ((_a3 = rule.definition.implements) === null || _a3 === void 0 ? void 0 : _a3.some((kwd) => schema4[kwd] !== void 0));
    }
  }
});

// node_modules/@redocly/ajv/dist/compile/validate/dataType.js
var require_dataType = __commonJS({
  "node_modules/@redocly/ajv/dist/compile/validate/dataType.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.DataType = void 0;
    exports.getSchemaTypes = getSchemaTypes;
    exports.getJSONTypes = getJSONTypes;
    exports.coerceAndCheckDataType = coerceAndCheckDataType;
    exports.checkDataType = checkDataType;
    exports.checkDataTypes = checkDataTypes;
    exports.reportTypeError = reportTypeError;
    var rules_1 = require_rules2();
    var applicability_1 = require_applicability();
    var errors_1 = require_errors();
    var codegen_1 = require_codegen();
    var util_1 = require_util();
    var DataType;
    (function(DataType2) {
      DataType2[DataType2["Correct"] = 0] = "Correct";
      DataType2[DataType2["Wrong"] = 1] = "Wrong";
    })(DataType || (exports.DataType = DataType = {}));
    function getSchemaTypes(schema4) {
      const types = getJSONTypes(schema4.type);
      const hasNull = types.includes("null");
      if (hasNull) {
        if (schema4.nullable === false)
          throw new Error("type: null contradicts nullable: false");
      } else {
        if (!types.length && schema4.nullable !== void 0) {
          throw new Error('"nullable" cannot be used without "type"');
        }
        if (schema4.nullable === true)
          types.push("null");
      }
      return types;
    }
    function getJSONTypes(ts) {
      const types = Array.isArray(ts) ? ts : ts ? [ts] : [];
      if (types.every(rules_1.isJSONType))
        return types;
      throw new Error("type must be JSONType or JSONType[]: " + types.join(","));
    }
    function coerceAndCheckDataType(it2, types) {
      const { gen, data, opts } = it2;
      const coerceTo = coerceToTypes(types, opts.coerceTypes);
      const checkTypes = types.length > 0 && !(coerceTo.length === 0 && types.length === 1 && (0, applicability_1.schemaHasRulesForType)(it2, types[0]));
      if (checkTypes) {
        const wrongType = checkDataTypes(types, data, opts.strictNumbers, DataType.Wrong);
        gen.if(wrongType, () => {
          if (coerceTo.length)
            coerceData(it2, types, coerceTo);
          else
            reportTypeError(it2);
        });
      }
      return checkTypes;
    }
    var COERCIBLE = /* @__PURE__ */ new Set(["string", "number", "integer", "boolean", "null"]);
    function coerceToTypes(types, coerceTypes) {
      return coerceTypes ? types.filter((t) => COERCIBLE.has(t) || coerceTypes === "array" && t === "array") : [];
    }
    function coerceData(it2, types, coerceTo) {
      const { gen, data, opts } = it2;
      const dataType = gen.let("dataType", (0, codegen_1._)`typeof ${data}`);
      const coerced = gen.let("coerced", (0, codegen_1._)`undefined`);
      if (opts.coerceTypes === "array") {
        gen.if((0, codegen_1._)`${dataType} == 'object' && Array.isArray(${data}) && ${data}.length == 1`, () => gen.assign(data, (0, codegen_1._)`${data}[0]`).assign(dataType, (0, codegen_1._)`typeof ${data}`).if(checkDataTypes(types, data, opts.strictNumbers), () => gen.assign(coerced, data)));
      }
      gen.if((0, codegen_1._)`${coerced} !== undefined`);
      for (const t of coerceTo) {
        if (COERCIBLE.has(t) || t === "array" && opts.coerceTypes === "array") {
          coerceSpecificType(t);
        }
      }
      gen.else();
      reportTypeError(it2);
      gen.endIf();
      gen.if((0, codegen_1._)`${coerced} !== undefined`, () => {
        gen.assign(data, coerced);
        assignParentData(it2, coerced);
      });
      function coerceSpecificType(t) {
        switch (t) {
          case "string":
            gen.elseIf((0, codegen_1._)`${dataType} == "number" || ${dataType} == "boolean"`).assign(coerced, (0, codegen_1._)`"" + ${data}`).elseIf((0, codegen_1._)`${data} === null`).assign(coerced, (0, codegen_1._)`""`);
            return;
          case "number":
            gen.elseIf((0, codegen_1._)`${dataType} == "boolean" || ${data} === null
              || (${dataType} == "string" && ${data} && ${data} == +${data})`).assign(coerced, (0, codegen_1._)`+${data}`);
            return;
          case "integer":
            gen.elseIf((0, codegen_1._)`${dataType} === "boolean" || ${data} === null
              || (${dataType} === "string" && ${data} && ${data} == +${data} && !(${data} % 1))`).assign(coerced, (0, codegen_1._)`+${data}`);
            return;
          case "boolean":
            gen.elseIf((0, codegen_1._)`${data} === "false" || ${data} === 0 || ${data} === null`).assign(coerced, false).elseIf((0, codegen_1._)`${data} === "true" || ${data} === 1`).assign(coerced, true);
            return;
          case "null":
            gen.elseIf((0, codegen_1._)`${data} === "" || ${data} === 0 || ${data} === false`);
            gen.assign(coerced, null);
            return;
          case "array":
            gen.elseIf((0, codegen_1._)`${dataType} === "string" || ${dataType} === "number"
              || ${dataType} === "boolean" || ${data} === null`).assign(coerced, (0, codegen_1._)`[${data}]`);
        }
      }
    }
    function assignParentData({ gen, parentData, parentDataProperty }, expr) {
      gen.if((0, codegen_1._)`${parentData} !== undefined`, () => gen.assign((0, codegen_1._)`${parentData}[${parentDataProperty}]`, expr));
    }
    function checkDataType(dataType, data, strictNums, correct = DataType.Correct) {
      const EQ = correct === DataType.Correct ? codegen_1.operators.EQ : codegen_1.operators.NEQ;
      let cond;
      switch (dataType) {
        case "null":
          return (0, codegen_1._)`${data} ${EQ} null`;
        case "array":
          cond = (0, codegen_1._)`Array.isArray(${data})`;
          break;
        case "object":
          cond = (0, codegen_1._)`${data} && typeof ${data} == "object" && !Array.isArray(${data})`;
          break;
        case "integer":
          cond = numCond((0, codegen_1._)`!(${data} % 1) && !isNaN(${data})`);
          break;
        case "number":
          cond = numCond();
          break;
        default:
          return (0, codegen_1._)`typeof ${data} ${EQ} ${dataType}`;
      }
      return correct === DataType.Correct ? cond : (0, codegen_1.not)(cond);
      function numCond(_cond = codegen_1.nil) {
        return (0, codegen_1.and)((0, codegen_1._)`typeof ${data} == "number"`, _cond, strictNums ? (0, codegen_1._)`isFinite(${data})` : codegen_1.nil);
      }
    }
    function checkDataTypes(dataTypes, data, strictNums, correct) {
      if (dataTypes.length === 1) {
        return checkDataType(dataTypes[0], data, strictNums, correct);
      }
      let cond;
      const types = (0, util_1.toHash)(dataTypes);
      if (types.array && types.object) {
        const notObj = (0, codegen_1._)`typeof ${data} != "object"`;
        cond = types.null ? notObj : (0, codegen_1._)`!${data} || ${notObj}`;
        delete types.null;
        delete types.array;
        delete types.object;
      } else {
        cond = codegen_1.nil;
      }
      if (types.number)
        delete types.integer;
      for (const t in types)
        cond = (0, codegen_1.and)(cond, checkDataType(t, data, strictNums, correct));
      return cond;
    }
    var typeError = {
      message: ({ schema: schema4 }) => `must be ${schema4}`,
      params: ({ schema: schema4, schemaValue }) => typeof schema4 == "string" ? (0, codegen_1._)`{type: ${schema4}}` : (0, codegen_1._)`{type: ${schemaValue}}`
    };
    function reportTypeError(it2) {
      const cxt = getTypeErrorContext(it2);
      (0, errors_1.reportError)(cxt, typeError);
    }
    function getTypeErrorContext(it2) {
      const { gen, data, schema: schema4 } = it2;
      const schemaCode = (0, util_1.schemaRefOrVal)(it2, schema4, "type");
      return {
        gen,
        keyword: "type",
        data,
        schema: schema4.type,
        schemaCode,
        schemaValue: schemaCode,
        parentSchema: schema4,
        params: {},
        it: it2
      };
    }
  }
});

// node_modules/@redocly/ajv/dist/compile/validate/defaults.js
var require_defaults = __commonJS({
  "node_modules/@redocly/ajv/dist/compile/validate/defaults.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.assignDefaults = assignDefaults;
    var codegen_1 = require_codegen();
    var util_1 = require_util();
    function assignDefaults(it2, ty) {
      const { properties, items } = it2.schema;
      if (ty === "object" && properties) {
        for (const key2 in properties) {
          assignDefault(it2, key2, properties[key2].default);
        }
      } else if (ty === "array" && Array.isArray(items)) {
        items.forEach((sch, i2) => assignDefault(it2, i2, sch.default));
      }
    }
    function assignDefault(it2, prop, defaultValue) {
      const { gen, compositeRule, data, opts } = it2;
      if (defaultValue === void 0)
        return;
      const childData = (0, codegen_1._)`${data}${(0, codegen_1.getProperty)(prop)}`;
      if (compositeRule) {
        (0, util_1.checkStrictMode)(it2, `default is ignored for: ${childData}`);
        return;
      }
      let condition = (0, codegen_1._)`${childData} === undefined`;
      if (opts.useDefaults === "empty") {
        condition = (0, codegen_1._)`${condition} || ${childData} === null || ${childData} === ""`;
      }
      gen.if(condition, (0, codegen_1._)`${childData} = ${(0, codegen_1.stringify)(defaultValue)}`);
    }
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/code.js
var require_code2 = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/code.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.checkReportMissingProp = checkReportMissingProp;
    exports.checkMissingProp = checkMissingProp;
    exports.reportMissingProp = reportMissingProp;
    exports.hasPropFunc = hasPropFunc;
    exports.isOwnProperty = isOwnProperty;
    exports.propertyInData = propertyInData;
    exports.noPropertyInData = noPropertyInData;
    exports.allSchemaProperties = allSchemaProperties;
    exports.schemaProperties = schemaProperties;
    exports.callValidateCode = callValidateCode;
    exports.usePattern = usePattern;
    exports.validateArray = validateArray;
    exports.validateUnion = validateUnion;
    var codegen_1 = require_codegen();
    var util_1 = require_util();
    var names_1 = require_names();
    var util_2 = require_util();
    function checkReportMissingProp(cxt, prop) {
      const { gen, data, it: it2 } = cxt;
      gen.if(noPropertyInData(gen, data, prop, it2.opts.ownProperties), () => {
        cxt.setParams({ missingProperty: (0, codegen_1._)`${prop}` }, true);
        cxt.error();
      });
    }
    function checkMissingProp({ gen, data, it: { opts } }, properties, missing) {
      return (0, codegen_1.or)(...properties.map((prop) => (0, codegen_1.and)(noPropertyInData(gen, data, prop, opts.ownProperties), (0, codegen_1._)`${missing} = ${prop}`)));
    }
    function reportMissingProp(cxt, missing) {
      cxt.setParams({ missingProperty: missing }, true);
      cxt.error();
    }
    function hasPropFunc(gen) {
      return gen.scopeValue("func", {
        // eslint-disable-next-line @typescript-eslint/unbound-method
        ref: Object.prototype.hasOwnProperty,
        code: (0, codegen_1._)`Object.prototype.hasOwnProperty`
      });
    }
    function isOwnProperty(gen, data, property) {
      return (0, codegen_1._)`${hasPropFunc(gen)}.call(${data}, ${property})`;
    }
    function propertyInData(gen, data, property, ownProperties) {
      const cond = (0, codegen_1._)`${data}${(0, codegen_1.getProperty)(property)} !== undefined`;
      return ownProperties ? (0, codegen_1._)`${cond} && ${isOwnProperty(gen, data, property)}` : cond;
    }
    function noPropertyInData(gen, data, property, ownProperties) {
      const cond = (0, codegen_1._)`${data}${(0, codegen_1.getProperty)(property)} === undefined`;
      return ownProperties ? (0, codegen_1.or)(cond, (0, codegen_1.not)(isOwnProperty(gen, data, property))) : cond;
    }
    function allSchemaProperties(schemaMap) {
      return schemaMap ? Object.keys(schemaMap).filter((p2) => p2 !== "__proto__") : [];
    }
    function schemaProperties(it2, schemaMap) {
      return allSchemaProperties(schemaMap).filter((p2) => !(0, util_1.alwaysValidSchema)(it2, schemaMap[p2]));
    }
    function callValidateCode({ schemaCode, data, it: { gen, topSchemaRef, schemaPath, errorPath }, it: it2 }, func, context, passSchema) {
      const dataAndSchema = passSchema ? (0, codegen_1._)`${schemaCode}, ${data}, ${topSchemaRef}${schemaPath}` : data;
      const valCxt = [
        [names_1.default.instancePath, (0, codegen_1.strConcat)(names_1.default.instancePath, errorPath)],
        [names_1.default.parentData, it2.parentData],
        [names_1.default.parentDataProperty, it2.parentDataProperty],
        [names_1.default.rootData, names_1.default.rootData],
        [names_1.default.isAllOfVariant, it2.isAllOfVariant ? 1 : 0]
      ];
      if (it2.opts.dynamicRef)
        valCxt.push([names_1.default.dynamicAnchors, names_1.default.dynamicAnchors]);
      const args = (0, codegen_1._)`${dataAndSchema}, ${gen.object(...valCxt)}`;
      return context !== codegen_1.nil ? (0, codegen_1._)`${func}.call(${context}, ${args})` : (0, codegen_1._)`${func}(${args})`;
    }
    var newRegExp = (0, codegen_1._)`new RegExp`;
    function usePattern({ gen, it: { opts } }, pattern) {
      const u2 = opts.unicodeRegExp ? "u" : "";
      const { regExp } = opts.code;
      const rx = regExp(pattern, u2);
      return gen.scopeValue("pattern", {
        key: rx.toString(),
        ref: rx,
        code: (0, codegen_1._)`${regExp.code === "new RegExp" ? newRegExp : (0, util_2.useFunc)(gen, regExp)}(${pattern}, ${u2})`
      });
    }
    function validateArray(cxt) {
      const { gen, data, keyword, it: it2 } = cxt;
      const valid = gen.name("valid");
      if (it2.allErrors) {
        const validArr = gen.let("valid", true);
        validateItems(() => gen.assign(validArr, false));
        return validArr;
      }
      gen.var(valid, true);
      validateItems(() => gen.break());
      return valid;
      function validateItems(notValid) {
        const len = gen.const("len", (0, codegen_1._)`${data}.length`);
        gen.forRange("i", 0, len, (i2) => {
          cxt.subschema({
            keyword,
            dataProp: i2,
            dataPropType: util_1.Type.Num
          }, valid);
          gen.if((0, codegen_1.not)(valid), notValid);
        });
      }
    }
    function validateUnion(cxt) {
      const { gen, schema: schema4, keyword, parentSchema, it: it2 } = cxt;
      if (!Array.isArray(schema4))
        throw new Error("ajv implementation error");
      if (it2.opts.discriminator && parentSchema.discriminator)
        return;
      const alwaysValid = schema4.some((sch) => (0, util_1.alwaysValidSchema)(it2, sch));
      if (alwaysValid && !it2.opts.unevaluated)
        return;
      const valid = gen.let("valid", false);
      const schValid = gen.name("_valid");
      gen.block(() => schema4.forEach((_sch, i2) => {
        const schCxt = cxt.subschema({
          keyword,
          schemaProp: i2,
          compositeRule: true
        }, schValid);
        gen.assign(valid, (0, codegen_1._)`${valid} || ${schValid}`);
        const merged = cxt.mergeValidEvaluated(schCxt, schValid);
        if (!merged)
          gen.if((0, codegen_1.not)(valid));
      }));
      cxt.result(valid, () => cxt.reset(), () => cxt.error(true));
    }
  }
});

// node_modules/@redocly/ajv/dist/compile/validate/keyword.js
var require_keyword = __commonJS({
  "node_modules/@redocly/ajv/dist/compile/validate/keyword.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.macroKeywordCode = macroKeywordCode;
    exports.funcKeywordCode = funcKeywordCode;
    exports.validSchemaType = validSchemaType;
    exports.validateKeywordUsage = validateKeywordUsage;
    var codegen_1 = require_codegen();
    var names_1 = require_names();
    var code_1 = require_code2();
    var errors_1 = require_errors();
    function macroKeywordCode(cxt, def) {
      const { gen, keyword, schema: schema4, parentSchema, it: it2 } = cxt;
      const macroSchema = def.macro.call(it2.self, schema4, parentSchema, it2);
      const schemaRef = useKeyword(gen, keyword, macroSchema);
      if (it2.opts.validateSchema !== false)
        it2.self.validateSchema(macroSchema, true);
      const valid = gen.name("valid");
      cxt.subschema({
        schema: macroSchema,
        schemaPath: codegen_1.nil,
        errSchemaPath: `${it2.errSchemaPath}/${keyword}`,
        topSchemaRef: schemaRef,
        compositeRule: true
      }, valid);
      cxt.pass(valid, () => cxt.error(true));
    }
    function funcKeywordCode(cxt, def) {
      var _a3;
      const { gen, keyword, schema: schema4, parentSchema, $data, it: it2 } = cxt;
      checkAsyncKeyword(it2, def);
      const validate = !$data && def.compile ? def.compile.call(it2.self, schema4, parentSchema, it2) : def.validate;
      const validateRef = useKeyword(gen, keyword, validate);
      const valid = gen.let("valid");
      cxt.block$data(valid, validateKeyword);
      cxt.ok((_a3 = def.valid) !== null && _a3 !== void 0 ? _a3 : valid);
      function validateKeyword() {
        if (def.errors === false) {
          assignValid();
          if (def.modifying)
            modifyData(cxt);
          reportErrs(() => cxt.error());
        } else {
          const ruleErrs = def.async ? validateAsync() : validateSync();
          if (def.modifying)
            modifyData(cxt);
          reportErrs(() => addErrs(cxt, ruleErrs));
        }
      }
      function validateAsync() {
        const ruleErrs = gen.let("ruleErrs", null);
        gen.try(() => assignValid((0, codegen_1._)`await `), (e) => gen.assign(valid, false).if((0, codegen_1._)`${e} instanceof ${it2.ValidationError}`, () => gen.assign(ruleErrs, (0, codegen_1._)`${e}.errors`), () => gen.throw(e)));
        return ruleErrs;
      }
      function validateSync() {
        const validateErrs = (0, codegen_1._)`${validateRef}.errors`;
        gen.assign(validateErrs, null);
        assignValid(codegen_1.nil);
        return validateErrs;
      }
      function assignValid(_await = def.async ? (0, codegen_1._)`await ` : codegen_1.nil) {
        const passCxt = it2.opts.passContext ? names_1.default.this : names_1.default.self;
        const passSchema = !("compile" in def && !$data || def.schema === false);
        gen.assign(valid, (0, codegen_1._)`${_await}${(0, code_1.callValidateCode)(cxt, validateRef, passCxt, passSchema)}`, def.modifying);
      }
      function reportErrs(errors2) {
        var _a4;
        gen.if((0, codegen_1.not)((_a4 = def.valid) !== null && _a4 !== void 0 ? _a4 : valid), errors2);
      }
    }
    function modifyData(cxt) {
      const { gen, data, it: it2 } = cxt;
      gen.if(it2.parentData, () => gen.assign(data, (0, codegen_1._)`${it2.parentData}[${it2.parentDataProperty}]`));
    }
    function addErrs(cxt, errs) {
      const { gen } = cxt;
      gen.if((0, codegen_1._)`Array.isArray(${errs})`, () => {
        gen.assign(names_1.default.vErrors, (0, codegen_1._)`${names_1.default.vErrors} === null ? ${errs} : ${names_1.default.vErrors}.concat(${errs})`).assign(names_1.default.errors, (0, codegen_1._)`${names_1.default.vErrors}.length`);
        (0, errors_1.extendErrors)(cxt);
      }, () => cxt.error());
    }
    function checkAsyncKeyword({ schemaEnv }, def) {
      if (def.async && !schemaEnv.$async)
        throw new Error("async keyword in sync schema");
    }
    function useKeyword(gen, keyword, result) {
      if (result === void 0)
        throw new Error(`keyword "${keyword}" failed to compile`);
      return gen.scopeValue("keyword", typeof result == "function" ? { ref: result } : { ref: result, code: (0, codegen_1.stringify)(result) });
    }
    function validSchemaType(schema4, schemaType, allowUndefined = false) {
      return !schemaType.length || schemaType.some((st2) => st2 === "array" ? Array.isArray(schema4) : st2 === "object" ? schema4 && typeof schema4 == "object" && !Array.isArray(schema4) : typeof schema4 == st2 || allowUndefined && typeof schema4 == "undefined");
    }
    function validateKeywordUsage({ schema: schema4, opts, self: self2, errSchemaPath }, def, keyword) {
      if (Array.isArray(def.keyword) ? !def.keyword.includes(keyword) : def.keyword !== keyword) {
        throw new Error("ajv implementation error");
      }
      const deps = def.dependencies;
      if (deps === null || deps === void 0 ? void 0 : deps.some((kwd) => !Object.prototype.hasOwnProperty.call(schema4, kwd))) {
        throw new Error(`parent schema must have dependencies of ${keyword}: ${deps.join(",")}`);
      }
      if (def.validateSchema) {
        const valid = def.validateSchema(schema4[keyword]);
        if (!valid) {
          const msg = `keyword "${keyword}" value is invalid at path "${errSchemaPath}": ` + self2.errorsText(def.validateSchema.errors);
          if (opts.validateSchema === "log")
            self2.logger.error(msg);
          else
            throw new Error(msg);
        }
      }
    }
  }
});

// node_modules/@redocly/ajv/dist/compile/validate/subschema.js
var require_subschema = __commonJS({
  "node_modules/@redocly/ajv/dist/compile/validate/subschema.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.getSubschema = getSubschema;
    exports.extendSubschemaData = extendSubschemaData;
    exports.extendSubschemaMode = extendSubschemaMode;
    var codegen_1 = require_codegen();
    var util_1 = require_util();
    function getSubschema(it2, { keyword, schemaProp, schema: schema4, schemaPath, errSchemaPath, topSchemaRef }) {
      if (keyword !== void 0 && schema4 !== void 0) {
        throw new Error('both "keyword" and "schema" passed, only one allowed');
      }
      if (keyword !== void 0) {
        const sch = it2.schema[keyword];
        return schemaProp === void 0 ? {
          schema: sch,
          schemaPath: (0, codegen_1._)`${it2.schemaPath}${(0, codegen_1.getProperty)(keyword)}`,
          errSchemaPath: `${it2.errSchemaPath}/${keyword}`
        } : {
          schema: sch[schemaProp],
          schemaPath: (0, codegen_1._)`${it2.schemaPath}${(0, codegen_1.getProperty)(keyword)}${(0, codegen_1.getProperty)(schemaProp)}`,
          errSchemaPath: `${it2.errSchemaPath}/${keyword}/${(0, util_1.escapeFragment)(schemaProp)}`
        };
      }
      if (schema4 !== void 0) {
        if (schemaPath === void 0 || errSchemaPath === void 0 || topSchemaRef === void 0) {
          throw new Error('"schemaPath", "errSchemaPath" and "topSchemaRef" are required with "schema"');
        }
        return {
          schema: schema4,
          schemaPath,
          topSchemaRef,
          errSchemaPath
        };
      }
      throw new Error('either "keyword" or "schema" must be passed');
    }
    function extendSubschemaData(subschema, it2, { dataProp, dataPropType: dpType, data, dataTypes, propertyName }) {
      if (data !== void 0 && dataProp !== void 0) {
        throw new Error('both "data" and "dataProp" passed, only one allowed');
      }
      const { gen } = it2;
      if (dataProp !== void 0) {
        const { errorPath, dataPathArr, opts } = it2;
        const nextData = gen.let("data", (0, codegen_1._)`${it2.data}${(0, codegen_1.getProperty)(dataProp)}`, true);
        dataContextProps(nextData);
        subschema.errorPath = (0, codegen_1.str)`${errorPath}${(0, util_1.getErrorPath)(dataProp, dpType, opts.jsPropertySyntax)}`;
        subschema.parentDataProperty = (0, codegen_1._)`${dataProp}`;
        subschema.dataPathArr = [...dataPathArr, subschema.parentDataProperty];
      }
      if (data !== void 0) {
        const nextData = data instanceof codegen_1.Name ? data : gen.let("data", data, true);
        dataContextProps(nextData);
        if (propertyName !== void 0)
          subschema.propertyName = propertyName;
      }
      if (dataTypes)
        subschema.dataTypes = dataTypes;
      function dataContextProps(_nextData) {
        subschema.data = _nextData;
        subschema.dataLevel = it2.dataLevel + 1;
        subschema.dataTypes = [];
        it2.definedProperties = /* @__PURE__ */ new Set();
        subschema.parentData = it2.data;
        subschema.dataNames = [...it2.dataNames, _nextData];
      }
    }
    function extendSubschemaMode(subschema, { jtdDiscriminator, jtdMetadata, compositeRule, createErrors, allErrors }) {
      if (compositeRule !== void 0)
        subschema.compositeRule = compositeRule;
      if (createErrors !== void 0)
        subschema.createErrors = createErrors;
      if (allErrors !== void 0)
        subschema.allErrors = allErrors;
      subschema.jtdDiscriminator = jtdDiscriminator;
      subschema.jtdMetadata = jtdMetadata;
    }
  }
});

// node_modules/fast-deep-equal/index.js
var require_fast_deep_equal = __commonJS({
  "node_modules/fast-deep-equal/index.js"(exports, module) {
    "use strict";
    module.exports = function equal(a2, b2) {
      if (a2 === b2) return true;
      if (a2 && b2 && typeof a2 == "object" && typeof b2 == "object") {
        if (a2.constructor !== b2.constructor) return false;
        var length2, i2, keys2;
        if (Array.isArray(a2)) {
          length2 = a2.length;
          if (length2 != b2.length) return false;
          for (i2 = length2; i2-- !== 0; )
            if (!equal(a2[i2], b2[i2])) return false;
          return true;
        }
        if (a2.constructor === RegExp) return a2.source === b2.source && a2.flags === b2.flags;
        if (a2.valueOf !== Object.prototype.valueOf) return a2.valueOf() === b2.valueOf();
        if (a2.toString !== Object.prototype.toString) return a2.toString() === b2.toString();
        keys2 = Object.keys(a2);
        length2 = keys2.length;
        if (length2 !== Object.keys(b2).length) return false;
        for (i2 = length2; i2-- !== 0; )
          if (!Object.prototype.hasOwnProperty.call(b2, keys2[i2])) return false;
        for (i2 = length2; i2-- !== 0; ) {
          var key2 = keys2[i2];
          if (!equal(a2[key2], b2[key2])) return false;
        }
        return true;
      }
      return a2 !== a2 && b2 !== b2;
    };
  }
});

// node_modules/json-schema-traverse/index.js
var require_json_schema_traverse = __commonJS({
  "node_modules/json-schema-traverse/index.js"(exports, module) {
    "use strict";
    var traverse2 = module.exports = function(schema4, opts, cb) {
      if (typeof opts == "function") {
        cb = opts;
        opts = {};
      }
      cb = opts.cb || cb;
      var pre = typeof cb == "function" ? cb : cb.pre || function() {
      };
      var post = cb.post || function() {
      };
      _traverse(opts, pre, post, schema4, "", schema4);
    };
    traverse2.keywords = {
      additionalItems: true,
      items: true,
      contains: true,
      additionalProperties: true,
      propertyNames: true,
      not: true,
      if: true,
      then: true,
      else: true
    };
    traverse2.arrayKeywords = {
      items: true,
      allOf: true,
      anyOf: true,
      oneOf: true
    };
    traverse2.propsKeywords = {
      $defs: true,
      definitions: true,
      properties: true,
      patternProperties: true,
      dependencies: true
    };
    traverse2.skipKeywords = {
      default: true,
      enum: true,
      const: true,
      required: true,
      maximum: true,
      minimum: true,
      exclusiveMaximum: true,
      exclusiveMinimum: true,
      multipleOf: true,
      maxLength: true,
      minLength: true,
      pattern: true,
      format: true,
      maxItems: true,
      minItems: true,
      uniqueItems: true,
      maxProperties: true,
      minProperties: true
    };
    function _traverse(opts, pre, post, schema4, jsonPtr, rootSchema, parentJsonPtr, parentKeyword, parentSchema, keyIndex) {
      if (schema4 && typeof schema4 == "object" && !Array.isArray(schema4)) {
        pre(schema4, jsonPtr, rootSchema, parentJsonPtr, parentKeyword, parentSchema, keyIndex);
        for (var key2 in schema4) {
          var sch = schema4[key2];
          if (Array.isArray(sch)) {
            if (key2 in traverse2.arrayKeywords) {
              for (var i2 = 0; i2 < sch.length; i2++)
                _traverse(opts, pre, post, sch[i2], jsonPtr + "/" + key2 + "/" + i2, rootSchema, jsonPtr, key2, schema4, i2);
            }
          } else if (key2 in traverse2.propsKeywords) {
            if (sch && typeof sch == "object") {
              for (var prop in sch)
                _traverse(opts, pre, post, sch[prop], jsonPtr + "/" + key2 + "/" + escapeJsonPtr(prop), rootSchema, jsonPtr, key2, schema4, prop);
            }
          } else if (key2 in traverse2.keywords || opts.allKeys && !(key2 in traverse2.skipKeywords)) {
            _traverse(opts, pre, post, sch, jsonPtr + "/" + key2, rootSchema, jsonPtr, key2, schema4);
          }
        }
        post(schema4, jsonPtr, rootSchema, parentJsonPtr, parentKeyword, parentSchema, keyIndex);
      }
    }
    function escapeJsonPtr(str) {
      return str.replace(/~/g, "~0").replace(/\//g, "~1");
    }
  }
});

// node_modules/@redocly/ajv/dist/compile/resolve.js
var require_resolve2 = __commonJS({
  "node_modules/@redocly/ajv/dist/compile/resolve.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.inlineRef = inlineRef;
    exports.getFullPath = getFullPath;
    exports._getFullPath = _getFullPath;
    exports.normalizeId = normalizeId;
    exports.resolveUrl = resolveUrl;
    exports.getSchemaRefs = getSchemaRefs;
    var util_1 = require_util();
    var equal = require_fast_deep_equal();
    var traverse2 = require_json_schema_traverse();
    var SIMPLE_INLINED = /* @__PURE__ */ new Set([
      "type",
      "format",
      "pattern",
      "maxLength",
      "minLength",
      "maxProperties",
      "minProperties",
      "maxItems",
      "minItems",
      "maximum",
      "minimum",
      "uniqueItems",
      "multipleOf",
      "required",
      "enum",
      "const"
    ]);
    function inlineRef(schema4, limit = true) {
      if (typeof schema4 == "boolean")
        return true;
      if (limit === true)
        return !hasRef(schema4);
      if (!limit)
        return false;
      return countKeys(schema4) <= limit;
    }
    var REF_KEYWORDS = /* @__PURE__ */ new Set([
      "$ref",
      "$recursiveRef",
      "$recursiveAnchor",
      "$dynamicRef",
      "$dynamicAnchor"
    ]);
    function hasRef(schema4) {
      for (const key2 in schema4) {
        if (REF_KEYWORDS.has(key2))
          return true;
        const sch = schema4[key2];
        if (Array.isArray(sch) && sch.some(hasRef))
          return true;
        if (typeof sch == "object" && hasRef(sch))
          return true;
      }
      return false;
    }
    function countKeys(schema4) {
      let count = 0;
      for (const key2 in schema4) {
        if (key2 === "$ref")
          return Infinity;
        count++;
        if (SIMPLE_INLINED.has(key2))
          continue;
        if (typeof schema4[key2] == "object") {
          (0, util_1.eachItem)(schema4[key2], (sch) => count += countKeys(sch));
        }
        if (count === Infinity)
          return Infinity;
      }
      return count;
    }
    function getFullPath(resolver, id = "", normalize2) {
      if (normalize2 !== false)
        id = normalizeId(id);
      const p2 = resolver.parse(id);
      return _getFullPath(resolver, p2);
    }
    function _getFullPath(resolver, p2) {
      const serialized = resolver.serialize(p2);
      return serialized.split("#")[0] + "#";
    }
    var TRAILING_SLASH_HASH = /#\/?$/;
    function normalizeId(id) {
      return id ? id.replace(TRAILING_SLASH_HASH, "") : "";
    }
    function resolveUrl(resolver, baseId, id) {
      id = normalizeId(id);
      return resolver.resolve(baseId, id);
    }
    var ANCHOR = /^[a-z_][-a-z0-9._]*$/i;
    function getSchemaRefs(schema4, baseId) {
      if (typeof schema4 == "boolean")
        return {};
      const { schemaId, uriResolver } = this.opts;
      const schId = normalizeId(schema4[schemaId] || baseId);
      const baseIds = { "": schId };
      const pathPrefix = getFullPath(uriResolver, schId, false);
      const localRefs = {};
      const schemaRefs = /* @__PURE__ */ new Set();
      traverse2(schema4, { allKeys: true }, (sch, jsonPtr, _15, parentJsonPtr) => {
        if (parentJsonPtr === void 0)
          return;
        const fullPath = pathPrefix + jsonPtr;
        let baseId2 = baseIds[parentJsonPtr];
        if (typeof sch[schemaId] == "string")
          baseId2 = addRef.call(this, sch[schemaId]);
        addAnchor.call(this, sch.$anchor);
        addAnchor.call(this, sch.$dynamicAnchor);
        baseIds[jsonPtr] = baseId2;
        function addRef(ref) {
          const _resolve = this.opts.uriResolver.resolve;
          ref = normalizeId(baseId2 ? _resolve(baseId2, ref) : ref);
          if (schemaRefs.has(ref))
            throw ambiguos(ref);
          schemaRefs.add(ref);
          let schOrRef = this.refs[ref];
          if (typeof schOrRef == "string")
            schOrRef = this.refs[schOrRef];
          if (typeof schOrRef == "object") {
            checkAmbiguosRef(sch, schOrRef.schema, ref);
          } else if (ref !== normalizeId(fullPath)) {
            if (ref[0] === "#") {
              checkAmbiguosRef(sch, localRefs[ref], ref);
              localRefs[ref] = sch;
            } else {
              this.refs[ref] = fullPath;
            }
          }
          return ref;
        }
        function addAnchor(anchor) {
          if (typeof anchor == "string") {
            if (!ANCHOR.test(anchor))
              throw new Error(`invalid anchor "${anchor}"`);
            addRef.call(this, `#${anchor}`);
          }
        }
      });
      return localRefs;
      function checkAmbiguosRef(sch1, sch2, ref) {
        if (sch2 !== void 0 && !equal(sch1, sch2))
          throw ambiguos(ref);
      }
      function ambiguos(ref) {
        return new Error(`reference "${ref}" resolves to more than one schema`);
      }
    }
  }
});

// node_modules/@redocly/ajv/dist/compile/validate/index.js
var require_validate = __commonJS({
  "node_modules/@redocly/ajv/dist/compile/validate/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.KeywordCxt = void 0;
    exports.validateFunctionCode = validateFunctionCode;
    exports.getData = getData;
    var boolSchema_1 = require_boolSchema();
    var dataType_1 = require_dataType();
    var applicability_1 = require_applicability();
    var dataType_2 = require_dataType();
    var defaults_1 = require_defaults();
    var keyword_1 = require_keyword();
    var subschema_1 = require_subschema();
    var codegen_1 = require_codegen();
    var names_1 = require_names();
    var resolve_1 = require_resolve2();
    var util_1 = require_util();
    var errors_1 = require_errors();
    function validateFunctionCode(it2) {
      if (isSchemaObj(it2)) {
        checkKeywords(it2);
        if (schemaCxtHasRules(it2)) {
          topSchemaObjCode(it2);
          return;
        }
      }
      validateFunction(it2, () => (0, boolSchema_1.topBoolOrEmptySchema)(it2));
    }
    function validateFunction({ gen, validateName, schema: schema4, schemaEnv, opts }, body) {
      if (opts.code.es5) {
        gen.func(validateName, (0, codegen_1._)`${names_1.default.data}, ${names_1.default.valCxt}`, schemaEnv.$async, () => {
          gen.code((0, codegen_1._)`"use strict"; ${funcSourceUrl(schema4, opts)}`);
          destructureValCxtES5(gen, opts);
          gen.code(body);
        });
      } else {
        gen.func(validateName, (0, codegen_1._)`${names_1.default.data}, ${destructureValCxt(opts)}`, schemaEnv.$async, () => gen.code(funcSourceUrl(schema4, opts)).code(body));
      }
    }
    function destructureValCxt(opts) {
      return (0, codegen_1._)`{${names_1.default.instancePath}="", ${names_1.default.parentData}, ${names_1.default.parentDataProperty}, ${names_1.default.rootData}=${names_1.default.data}${opts.dynamicRef ? (0, codegen_1._)`, ${names_1.default.dynamicAnchors}={}` : codegen_1.nil}, ${names_1.default.isAllOfVariant} = 0}={}`;
    }
    function destructureValCxtES5(gen, opts) {
      gen.if(names_1.default.valCxt, () => {
        gen.var(names_1.default.instancePath, (0, codegen_1._)`${names_1.default.valCxt}.${names_1.default.instancePath}`);
        gen.var(names_1.default.parentData, (0, codegen_1._)`${names_1.default.valCxt}.${names_1.default.parentData}`);
        gen.var(names_1.default.parentDataProperty, (0, codegen_1._)`${names_1.default.valCxt}.${names_1.default.parentDataProperty}`);
        gen.var(names_1.default.rootData, (0, codegen_1._)`${names_1.default.valCxt}.${names_1.default.rootData}`);
        gen.var(names_1.default.isAllOfVariant, (0, codegen_1._)`${names_1.default.valCxt}.${names_1.default.isAllOfVariant}`);
        if (opts.dynamicRef)
          gen.var(names_1.default.dynamicAnchors, (0, codegen_1._)`${names_1.default.valCxt}.${names_1.default.dynamicAnchors}`);
      }, () => {
        gen.var(names_1.default.instancePath, (0, codegen_1._)`""`);
        gen.var(names_1.default.parentData, (0, codegen_1._)`undefined`);
        gen.var(names_1.default.parentDataProperty, (0, codegen_1._)`undefined`);
        gen.var(names_1.default.rootData, names_1.default.data);
        gen.var(names_1.default.isAllOfVariant, (0, codegen_1._)`0`);
        if (opts.dynamicRef)
          gen.var(names_1.default.dynamicAnchors, (0, codegen_1._)`{}`);
      });
    }
    function topSchemaObjCode(it2) {
      const { schema: schema4, opts, gen } = it2;
      validateFunction(it2, () => {
        if (opts.$comment && schema4.$comment)
          commentKeyword(it2);
        checkNoDefault(it2);
        gen.let(names_1.default.vErrors, null);
        gen.let(names_1.default.errors, 0);
        if (opts.unevaluated)
          resetEvaluated(it2);
        typeAndKeywords(it2);
        returnResults(it2);
      });
      return;
    }
    function resetEvaluated(it2) {
      const { gen, validateName } = it2;
      it2.evaluated = gen.const("evaluated", (0, codegen_1._)`${validateName}.evaluated`);
      gen.if((0, codegen_1._)`${it2.evaluated}.dynamicProps`, () => gen.assign((0, codegen_1._)`${it2.evaluated}.props`, (0, codegen_1._)`undefined`));
      gen.if((0, codegen_1._)`${it2.evaluated}.dynamicItems`, () => gen.assign((0, codegen_1._)`${it2.evaluated}.items`, (0, codegen_1._)`undefined`));
    }
    function funcSourceUrl(schema4, opts) {
      const schId = typeof schema4 == "object" && schema4[opts.schemaId];
      return schId && (opts.code.source || opts.code.process) ? (0, codegen_1._)`/*# sourceURL=${schId} */` : codegen_1.nil;
    }
    function subschemaCode(it2, valid) {
      if (isSchemaObj(it2)) {
        checkKeywords(it2);
        if (schemaCxtHasRules(it2)) {
          subSchemaObjCode(it2, valid);
          return;
        }
      }
      (0, boolSchema_1.boolOrEmptySchema)(it2, valid);
    }
    function schemaCxtHasRules({ schema: schema4, self: self2 }) {
      if (typeof schema4 == "boolean")
        return !schema4;
      for (const key2 in schema4)
        if (self2.RULES.all[key2])
          return true;
      return false;
    }
    function isSchemaObj(it2) {
      return typeof it2.schema != "boolean";
    }
    function subSchemaObjCode(it2, valid) {
      const { schema: schema4, gen, opts } = it2;
      if (opts.$comment && schema4.$comment)
        commentKeyword(it2);
      updateContext(it2);
      checkAsyncSchema(it2);
      const errsCount = gen.const("_errs", names_1.default.errors);
      typeAndKeywords(it2, errsCount);
      gen.var(valid, (0, codegen_1._)`${errsCount} === ${names_1.default.errors}`);
    }
    function checkKeywords(it2) {
      (0, util_1.checkUnknownRules)(it2);
      checkRefsAndKeywords(it2);
    }
    function typeAndKeywords(it2, errsCount) {
      if (it2.opts.jtd)
        return schemaKeywords(it2, [], false, errsCount);
      const types = (0, dataType_1.getSchemaTypes)(it2.schema);
      const checkedTypes = (0, dataType_1.coerceAndCheckDataType)(it2, types);
      schemaKeywords(it2, types, !checkedTypes, errsCount);
    }
    function checkRefsAndKeywords(it2) {
      const { schema: schema4, errSchemaPath, opts, self: self2 } = it2;
      if (schema4.$ref && opts.ignoreKeywordsWithRef && (0, util_1.schemaHasRulesButRef)(schema4, self2.RULES)) {
        self2.logger.warn(`$ref: keywords ignored in schema at path "${errSchemaPath}"`);
      }
    }
    function checkNoDefault(it2) {
      const { schema: schema4, opts } = it2;
      if (schema4.default !== void 0 && opts.useDefaults && opts.strictSchema) {
        (0, util_1.checkStrictMode)(it2, "default is ignored in the schema root");
      }
    }
    function updateContext(it2) {
      const schId = it2.schema[it2.opts.schemaId];
      if (schId)
        it2.baseId = (0, resolve_1.resolveUrl)(it2.opts.uriResolver, it2.baseId, schId);
    }
    function checkAsyncSchema(it2) {
      if (it2.schema.$async && !it2.schemaEnv.$async)
        throw new Error("async schema in sync schema");
    }
    function commentKeyword({ gen, schemaEnv, schema: schema4, errSchemaPath, opts }) {
      const msg = schema4.$comment;
      if (opts.$comment === true) {
        gen.code((0, codegen_1._)`${names_1.default.self}.logger.log(${msg})`);
      } else if (typeof opts.$comment == "function") {
        const schemaPath = (0, codegen_1.str)`${errSchemaPath}/$comment`;
        const rootName = gen.scopeValue("root", { ref: schemaEnv.root });
        gen.code((0, codegen_1._)`${names_1.default.self}.opts.$comment(${msg}, ${schemaPath}, ${rootName}.schema)`);
      }
    }
    function returnResults(it2) {
      const { gen, schemaEnv, validateName, ValidationError, opts } = it2;
      if (schemaEnv.$async) {
        gen.if((0, codegen_1._)`${names_1.default.errors} === 0`, () => gen.return(names_1.default.data), () => gen.throw((0, codegen_1._)`new ${ValidationError}(${names_1.default.vErrors})`));
      } else {
        gen.assign((0, codegen_1._)`${validateName}.errors`, names_1.default.vErrors);
        if (opts.unevaluated)
          assignEvaluated(it2);
        gen.return((0, codegen_1._)`${names_1.default.errors} === 0`);
      }
    }
    function assignEvaluated({ gen, evaluated, props, items }) {
      if (props instanceof codegen_1.Name)
        gen.assign((0, codegen_1._)`${evaluated}.props`, props);
      if (items instanceof codegen_1.Name)
        gen.assign((0, codegen_1._)`${evaluated}.items`, items);
    }
    function schemaKeywords(it2, types, typeErrors, errsCount) {
      const { gen, schema: schema4, data, allErrors, opts, self: self2 } = it2;
      const { RULES } = self2;
      if (schema4.$ref && (opts.ignoreKeywordsWithRef || !(0, util_1.schemaHasRulesButRef)(schema4, RULES))) {
        gen.block(() => keywordCode(it2, "$ref", RULES.all.$ref.definition));
        return;
      }
      if (!opts.jtd)
        checkStrictTypes(it2, types);
      gen.block(() => {
        for (const group of RULES.rules)
          groupKeywords(group);
        groupKeywords(RULES.post);
      });
      function groupKeywords(group) {
        if (!(0, applicability_1.shouldUseGroup)(schema4, group))
          return;
        if (group.type) {
          gen.if((0, dataType_2.checkDataType)(group.type, data, opts.strictNumbers));
          iterateKeywords(it2, group);
          if (types.length === 1 && types[0] === group.type && typeErrors) {
            gen.else();
            (0, dataType_2.reportTypeError)(it2);
          }
          gen.endIf();
        } else {
          iterateKeywords(it2, group);
        }
        if (!allErrors)
          gen.if((0, codegen_1._)`${names_1.default.errors} === ${errsCount || 0}`);
      }
    }
    function iterateKeywords(it2, group) {
      const { gen, schema: schema4, opts: { useDefaults } } = it2;
      if (useDefaults)
        (0, defaults_1.assignDefaults)(it2, group.type);
      gen.block(() => {
        for (const rule of group.rules) {
          if ((0, applicability_1.shouldUseRule)(schema4, rule) || shouldForceUnevaluatedProperties(schema4, rule)) {
            keywordCode(it2, rule.keyword, rule.definition, group.type);
          }
        }
      });
      function shouldForceUnevaluatedProperties(schema5, rule) {
        return !!(rule.keyword === "unevaluatedProperties" && (schema5.properties || schema5.patternProperties) && !it2.isAllOfVariant && it2.opts.defaultUnevaluatedProperties === false);
      }
    }
    function checkStrictTypes(it2, types) {
      if (it2.schemaEnv.meta || !it2.opts.strictTypes)
        return;
      checkContextTypes(it2, types);
      if (!it2.opts.allowUnionTypes)
        checkMultipleTypes(it2, types);
      checkKeywordTypes(it2, it2.dataTypes);
    }
    function checkContextTypes(it2, types) {
      if (!types.length)
        return;
      if (!it2.dataTypes.length) {
        it2.dataTypes = types;
        return;
      }
      types.forEach((t) => {
        if (!includesType(it2.dataTypes, t)) {
          strictTypesError(it2, `type "${t}" not allowed by context "${it2.dataTypes.join(",")}"`);
        }
      });
      it2.dataTypes = it2.dataTypes.filter((t) => includesType(types, t));
    }
    function checkMultipleTypes(it2, ts) {
      if (ts.length > 1 && !(ts.length === 2 && ts.includes("null"))) {
        strictTypesError(it2, "use allowUnionTypes to allow union type keyword");
      }
    }
    function checkKeywordTypes(it2, ts) {
      const rules = it2.self.RULES.all;
      for (const keyword in rules) {
        const rule = rules[keyword];
        if (typeof rule == "object" && (0, applicability_1.shouldUseRule)(it2.schema, rule)) {
          const { type } = rule.definition;
          if (type.length && !type.some((t) => hasApplicableType(ts, t))) {
            strictTypesError(it2, `missing type "${type.join(",")}" for keyword "${keyword}"`);
          }
        }
      }
    }
    function hasApplicableType(schTs, kwdT) {
      return schTs.includes(kwdT) || kwdT === "number" && schTs.includes("integer");
    }
    function includesType(ts, t) {
      return ts.includes(t) || t === "integer" && ts.includes("number");
    }
    function strictTypesError(it2, msg) {
      const schemaPath = it2.schemaEnv.baseId + it2.errSchemaPath;
      msg += ` at "${schemaPath}" (strictTypes)`;
      (0, util_1.checkStrictMode)(it2, msg, it2.opts.strictTypes);
    }
    var KeywordCxt = class {
      constructor(it2, def, keyword) {
        (0, keyword_1.validateKeywordUsage)(it2, def, keyword);
        this.gen = it2.gen;
        this.allErrors = it2.allErrors;
        this.keyword = keyword;
        this.data = it2.data;
        this.schema = it2.schema[keyword];
        this.$data = def.$data && it2.opts.$data && this.schema && this.schema.$data;
        this.schemaValue = (0, util_1.schemaRefOrVal)(it2, this.schema, keyword, this.$data);
        this.schemaType = def.schemaType;
        this.parentSchema = it2.schema;
        this.params = {};
        this.it = it2;
        this.def = def;
        if (this.$data) {
          this.schemaCode = it2.gen.const("vSchema", getData(this.$data, it2));
        } else {
          this.schemaCode = this.schemaValue;
          if (!(0, keyword_1.validSchemaType)(this.schema, def.schemaType, def.allowUndefined)) {
            throw new Error(`${keyword} value must be ${JSON.stringify(def.schemaType)}`);
          }
        }
        if ("code" in def ? def.trackErrors : def.errors !== false) {
          this.errsCount = it2.gen.const("_errs", names_1.default.errors);
        }
      }
      result(condition, successAction, failAction) {
        this.failResult((0, codegen_1.not)(condition), successAction, failAction);
      }
      failResult(condition, successAction, failAction) {
        this.gen.if(condition);
        if (failAction)
          failAction();
        else
          this.error();
        if (successAction) {
          this.gen.else();
          successAction();
          if (this.allErrors)
            this.gen.endIf();
        } else {
          if (this.allErrors)
            this.gen.endIf();
          else
            this.gen.else();
        }
      }
      pass(condition, failAction) {
        this.failResult((0, codegen_1.not)(condition), void 0, failAction);
      }
      fail(condition) {
        if (condition === void 0) {
          this.error();
          if (!this.allErrors)
            this.gen.if(false);
          return;
        }
        this.gen.if(condition);
        this.error();
        if (this.allErrors)
          this.gen.endIf();
        else
          this.gen.else();
      }
      fail$data(condition) {
        if (!this.$data)
          return this.fail(condition);
        const { schemaCode } = this;
        this.fail((0, codegen_1._)`${schemaCode} !== undefined && (${(0, codegen_1.or)(this.invalid$data(), condition)})`);
      }
      error(append2, errorParams, errorPaths) {
        if (errorParams) {
          this.setParams(errorParams);
          this._error(append2, errorPaths);
          this.setParams({});
          return;
        }
        this._error(append2, errorPaths);
      }
      _error(append2, errorPaths) {
        ;
        (append2 ? errors_1.reportExtraError : errors_1.reportError)(this, this.def.error, errorPaths);
      }
      $dataError() {
        (0, errors_1.reportError)(this, this.def.$dataError || errors_1.keyword$DataError);
      }
      reset() {
        if (this.errsCount === void 0)
          throw new Error('add "trackErrors" to keyword definition');
        (0, errors_1.resetErrorsCount)(this.gen, this.errsCount);
      }
      ok(cond) {
        if (!this.allErrors)
          this.gen.if(cond);
      }
      setParams(obj, assign3) {
        if (assign3)
          Object.assign(this.params, obj);
        else
          this.params = obj;
      }
      block$data(valid, codeBlock, $dataValid = codegen_1.nil) {
        this.gen.block(() => {
          this.check$data(valid, $dataValid);
          codeBlock();
        });
      }
      check$data(valid = codegen_1.nil, $dataValid = codegen_1.nil) {
        if (!this.$data)
          return;
        const { gen, schemaCode, schemaType, def } = this;
        gen.if((0, codegen_1.or)((0, codegen_1._)`${schemaCode} === undefined`, $dataValid));
        if (valid !== codegen_1.nil)
          gen.assign(valid, true);
        if (schemaType.length || def.validateSchema) {
          gen.elseIf(this.invalid$data());
          this.$dataError();
          if (valid !== codegen_1.nil)
            gen.assign(valid, false);
        }
        gen.else();
      }
      invalid$data() {
        const { gen, schemaCode, schemaType, def, it: it2 } = this;
        return (0, codegen_1.or)(wrong$DataType(), invalid$DataSchema());
        function wrong$DataType() {
          if (schemaType.length) {
            if (!(schemaCode instanceof codegen_1.Name))
              throw new Error("ajv implementation error");
            const st2 = Array.isArray(schemaType) ? schemaType : [schemaType];
            return (0, codegen_1._)`${(0, dataType_2.checkDataTypes)(st2, schemaCode, it2.opts.strictNumbers, dataType_2.DataType.Wrong)}`;
          }
          return codegen_1.nil;
        }
        function invalid$DataSchema() {
          if (def.validateSchema) {
            const validateSchemaRef = gen.scopeValue("validate$data", { ref: def.validateSchema });
            return (0, codegen_1._)`!${validateSchemaRef}(${schemaCode})`;
          }
          return codegen_1.nil;
        }
      }
      subschema(appl, valid, isAllOfVariant) {
        const subschema = (0, subschema_1.getSubschema)(this.it, appl);
        (0, subschema_1.extendSubschemaData)(subschema, this.it, appl);
        (0, subschema_1.extendSubschemaMode)(subschema, appl);
        const nextContext = {
          ...this.it,
          ...subschema,
          items: void 0,
          props: void 0,
          isAllOfVariant
        };
        subschemaCode(nextContext, valid);
        return nextContext;
      }
      mergeEvaluated(schemaCxt, toName) {
        const { it: it2, gen } = this;
        if (!it2.opts.unevaluated)
          return;
        if (it2.props !== true && schemaCxt.props !== void 0) {
          it2.props = util_1.mergeEvaluated.props(gen, schemaCxt.props, it2.props, toName);
        }
        if (it2.items !== true && schemaCxt.items !== void 0) {
          it2.items = util_1.mergeEvaluated.items(gen, schemaCxt.items, it2.items, toName);
        }
      }
      mergeValidEvaluated(schemaCxt, valid) {
        const { it: it2, gen } = this;
        if (it2.opts.unevaluated && (it2.props !== true || it2.items !== true)) {
          gen.if(valid, () => this.mergeEvaluated(schemaCxt, codegen_1.Name));
          return true;
        }
      }
    };
    exports.KeywordCxt = KeywordCxt;
    function keywordCode(it2, keyword, def, ruleType) {
      const cxt = new KeywordCxt(it2, def, keyword);
      if ("code" in def) {
        def.code(cxt, ruleType);
      } else if (cxt.$data && def.validate) {
        (0, keyword_1.funcKeywordCode)(cxt, def);
      } else if ("macro" in def) {
        (0, keyword_1.macroKeywordCode)(cxt, def);
      } else if (def.compile || def.validate) {
        (0, keyword_1.funcKeywordCode)(cxt, def);
      }
    }
    var JSON_POINTER = /^\/(?:[^~]|~0|~1)*$/;
    var RELATIVE_JSON_POINTER = /^([0-9]+)(#|\/(?:[^~]|~0|~1)*)?$/;
    function getData($data, { dataLevel, dataNames, dataPathArr }) {
      let jsonPointer;
      let data;
      if ($data === "")
        return names_1.default.rootData;
      if ($data[0] === "/") {
        if (!JSON_POINTER.test($data))
          throw new Error(`Invalid JSON-pointer: ${$data}`);
        jsonPointer = $data;
        data = names_1.default.rootData;
      } else {
        const matches2 = RELATIVE_JSON_POINTER.exec($data);
        if (!matches2)
          throw new Error(`Invalid JSON-pointer: ${$data}`);
        const up = +matches2[1];
        jsonPointer = matches2[2];
        if (jsonPointer === "#") {
          if (up >= dataLevel)
            throw new Error(errorMsg("property/index", up));
          return dataPathArr[dataLevel - up];
        }
        if (up > dataLevel)
          throw new Error(errorMsg("data", up));
        data = dataNames[dataLevel - up];
        if (!jsonPointer)
          return data;
      }
      let expr = data;
      const segments = jsonPointer.split("/");
      for (const segment of segments) {
        if (segment) {
          data = (0, codegen_1._)`${data}${(0, codegen_1.getProperty)((0, util_1.unescapeJsonPointer)(segment))}`;
          expr = (0, codegen_1._)`${expr} && ${data}`;
        }
      }
      return expr;
      function errorMsg(pointerType, up) {
        return `Cannot access ${pointerType} ${up} levels up, current level is ${dataLevel}`;
      }
    }
  }
});

// node_modules/@redocly/ajv/dist/runtime/validation_error.js
var require_validation_error = __commonJS({
  "node_modules/@redocly/ajv/dist/runtime/validation_error.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var ValidationError = class extends Error {
      constructor(errors2) {
        super("validation failed");
        this.errors = errors2;
        this.ajv = this.validation = true;
      }
    };
    exports.default = ValidationError;
  }
});

// node_modules/@redocly/ajv/dist/compile/ref_error.js
var require_ref_error = __commonJS({
  "node_modules/@redocly/ajv/dist/compile/ref_error.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var resolve_1 = require_resolve2();
    var MissingRefError = class extends Error {
      constructor(resolver, baseId, ref, msg) {
        super(msg || `can't resolve reference ${ref} from id ${baseId}`);
        this.missingRef = (0, resolve_1.resolveUrl)(resolver, baseId, ref);
        this.missingSchema = (0, resolve_1.normalizeId)((0, resolve_1.getFullPath)(resolver, this.missingRef));
      }
    };
    exports.default = MissingRefError;
  }
});

// node_modules/@redocly/ajv/dist/compile/index.js
var require_compile = __commonJS({
  "node_modules/@redocly/ajv/dist/compile/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.SchemaEnv = void 0;
    exports.compileSchema = compileSchema;
    exports.resolveRef = resolveRef;
    exports.getCompilingSchema = getCompilingSchema;
    exports.resolveSchema = resolveSchema;
    var codegen_1 = require_codegen();
    var validation_error_1 = require_validation_error();
    var names_1 = require_names();
    var resolve_1 = require_resolve2();
    var util_1 = require_util();
    var validate_1 = require_validate();
    var SchemaEnv = class {
      constructor(env2) {
        var _a3;
        this.refs = {};
        this.dynamicAnchors = {};
        let schema4;
        if (typeof env2.schema == "object")
          schema4 = env2.schema;
        this.schema = env2.schema;
        this.schemaId = env2.schemaId;
        this.root = env2.root || this;
        this.baseId = (_a3 = env2.baseId) !== null && _a3 !== void 0 ? _a3 : (0, resolve_1.normalizeId)(schema4 === null || schema4 === void 0 ? void 0 : schema4[env2.schemaId || "$id"]);
        this.schemaPath = env2.schemaPath;
        this.localRefs = env2.localRefs;
        this.meta = env2.meta;
        this.$async = schema4 === null || schema4 === void 0 ? void 0 : schema4.$async;
        this.refs = {};
      }
    };
    exports.SchemaEnv = SchemaEnv;
    function compileSchema(sch) {
      const _sch = getCompilingSchema.call(this, sch);
      if (_sch)
        return _sch;
      const rootId = (0, resolve_1.getFullPath)(this.opts.uriResolver, sch.root.baseId);
      const { es5, lines } = this.opts.code;
      const { ownProperties } = this.opts;
      const gen = new codegen_1.CodeGen(this.scope, { es5, lines, ownProperties });
      let _ValidationError;
      if (sch.$async) {
        _ValidationError = gen.scopeValue("Error", {
          ref: validation_error_1.default,
          code: (0, codegen_1._)`require("ajv/dist/runtime/validation_error").default`
        });
      }
      const validateName = gen.scopeName("validate");
      sch.validateName = validateName;
      const schemaCxt = {
        gen,
        allErrors: this.opts.allErrors,
        data: names_1.default.data,
        parentData: names_1.default.parentData,
        parentDataProperty: names_1.default.parentDataProperty,
        dataNames: [names_1.default.data],
        dataPathArr: [codegen_1.nil],
        // TODO can its length be used as dataLevel if nil is removed?
        dataLevel: 0,
        dataTypes: [],
        definedProperties: /* @__PURE__ */ new Set(),
        topSchemaRef: gen.scopeValue("schema", this.opts.code.source === true ? { ref: sch.schema, code: (0, codegen_1.stringify)(sch.schema) } : { ref: sch.schema }),
        validateName,
        ValidationError: _ValidationError,
        schema: sch.schema,
        schemaEnv: sch,
        rootId,
        baseId: sch.baseId || rootId,
        schemaPath: codegen_1.nil,
        errSchemaPath: sch.schemaPath || (this.opts.jtd ? "" : "#"),
        errorPath: (0, codegen_1._)`""`,
        opts: this.opts,
        self: this
      };
      let sourceCode;
      try {
        this._compilations.add(sch);
        (0, validate_1.validateFunctionCode)(schemaCxt);
        gen.optimize(this.opts.code.optimize);
        const validateCode = gen.toString();
        sourceCode = `const visitedNodesForRef = new WeakMap(); ${gen.scopeRefs(names_1.default.scope)}return ${validateCode}`;
        if (this.opts.code.process)
          sourceCode = this.opts.code.process(sourceCode, sch);
        const makeValidate = new Function(`${names_1.default.self}`, `${names_1.default.scope}`, sourceCode);
        const validate = makeValidate(this, this.scope.get());
        this.scope.value(validateName, { ref: validate });
        validate.errors = null;
        validate.schema = sch.schema;
        validate.schemaEnv = sch;
        if (sch.$async)
          validate.$async = true;
        if (this.opts.code.source === true) {
          validate.source = { validateName, validateCode, scopeValues: gen._values };
        }
        if (this.opts.unevaluated) {
          const { props, items } = schemaCxt;
          validate.evaluated = {
            props: props instanceof codegen_1.Name ? void 0 : props,
            items: items instanceof codegen_1.Name ? void 0 : items,
            dynamicProps: props instanceof codegen_1.Name,
            dynamicItems: items instanceof codegen_1.Name
          };
          if (validate.source)
            validate.source.evaluated = (0, codegen_1.stringify)(validate.evaluated);
        }
        sch.validate = validate;
        return sch;
      } catch (e) {
        delete sch.validate;
        delete sch.validateName;
        if (sourceCode)
          this.logger.error("Error compiling schema, function code:", sourceCode);
        throw e;
      } finally {
        this._compilations.delete(sch);
      }
    }
    function resolveRef(root, baseId, origRef) {
      var _a3;
      const ref = (0, resolve_1.resolveUrl)(this.opts.uriResolver, baseId, origRef);
      const schOrFunc = root.refs[ref];
      if (schOrFunc)
        return schOrFunc;
      let _sch = resolve.call(this, root, ref);
      if (_sch === void 0) {
        const schema4 = (_a3 = root.localRefs) === null || _a3 === void 0 ? void 0 : _a3[ref];
        const { schemaId } = this.opts;
        if (schema4)
          _sch = new SchemaEnv({ schema: schema4, schemaId, root, baseId });
      }
      if (_sch === void 0 && this.opts.loadSchemaSync) {
        const remoteSchema = this.opts.loadSchemaSync(baseId, origRef, ref);
        if (remoteSchema && !(this.refs[ref] || this.schemas[ref])) {
          this.addSchema(remoteSchema, ref, void 0);
          _sch = resolve.call(this, root, ref);
        }
      }
      if (_sch === void 0)
        return;
      return root.refs[ref] = inlineOrCompile.call(this, _sch);
    }
    function inlineOrCompile(sch) {
      if ((0, resolve_1.inlineRef)(sch.schema, this.opts.inlineRefs))
        return sch.schema;
      return sch.validate ? sch : compileSchema.call(this, sch);
    }
    function getCompilingSchema(schEnv) {
      for (const sch of this._compilations) {
        if (sameSchemaEnv(sch, schEnv))
          return sch;
      }
    }
    function sameSchemaEnv(s1, s2) {
      return s1.schema === s2.schema && s1.root === s2.root && s1.baseId === s2.baseId;
    }
    function resolve(root, ref) {
      let sch;
      while (typeof (sch = this.refs[ref]) == "string")
        ref = sch;
      return sch || this.schemas[ref] || resolveSchema.call(this, root, ref);
    }
    function resolveSchema(root, ref) {
      const p2 = this.opts.uriResolver.parse(ref);
      const refPath = (0, resolve_1._getFullPath)(this.opts.uriResolver, p2);
      let baseId = (0, resolve_1.getFullPath)(this.opts.uriResolver, root.baseId, void 0);
      if (Object.keys(root.schema).length > 0 && refPath === baseId) {
        return getJsonPointer.call(this, p2, root);
      }
      const id = (0, resolve_1.normalizeId)(refPath);
      const schOrRef = this.refs[id] || this.schemas[id];
      if (typeof schOrRef == "string") {
        const sch = resolveSchema.call(this, root, schOrRef);
        if (typeof (sch === null || sch === void 0 ? void 0 : sch.schema) !== "object")
          return;
        return getJsonPointer.call(this, p2, sch);
      }
      if (typeof (schOrRef === null || schOrRef === void 0 ? void 0 : schOrRef.schema) !== "object")
        return;
      if (!schOrRef.validate)
        compileSchema.call(this, schOrRef);
      if (id === (0, resolve_1.normalizeId)(ref)) {
        const { schema: schema4 } = schOrRef;
        const { schemaId } = this.opts;
        const schId = schema4[schemaId];
        if (schId)
          baseId = (0, resolve_1.resolveUrl)(this.opts.uriResolver, baseId, schId);
        return new SchemaEnv({ schema: schema4, schemaId, root, baseId });
      }
      return getJsonPointer.call(this, p2, schOrRef);
    }
    var PREVENT_SCOPE_CHANGE = /* @__PURE__ */ new Set([
      "properties",
      "patternProperties",
      "enum",
      "dependencies",
      "definitions"
    ]);
    function getJsonPointer(parsedRef, { baseId, schema: schema4, root }) {
      var _a3;
      if (((_a3 = parsedRef.fragment) === null || _a3 === void 0 ? void 0 : _a3[0]) !== "/")
        return;
      for (const part of parsedRef.fragment.slice(1).split("/")) {
        if (typeof schema4 === "boolean")
          return;
        const partSchema = schema4[(0, util_1.unescapeFragment)(part)];
        if (partSchema === void 0)
          return;
        schema4 = partSchema;
        const schId = typeof schema4 === "object" && schema4[this.opts.schemaId];
        if (!PREVENT_SCOPE_CHANGE.has(part) && schId) {
          baseId = (0, resolve_1.resolveUrl)(this.opts.uriResolver, baseId, schId);
        }
      }
      let env2;
      if (typeof schema4 != "boolean" && schema4.$ref && !(0, util_1.schemaHasRulesButRef)(schema4, this.RULES)) {
        const $ref = (0, resolve_1.resolveUrl)(this.opts.uriResolver, baseId, schema4.$ref);
        env2 = resolveSchema.call(this, root, $ref);
      }
      const { schemaId } = this.opts;
      env2 = env2 || new SchemaEnv({ schema: schema4, schemaId, root, baseId });
      if (env2.schema !== env2.root.schema)
        return env2;
      return void 0;
    }
  }
});

// node_modules/@redocly/ajv/dist/refs/data.json
var require_data = __commonJS({
  "node_modules/@redocly/ajv/dist/refs/data.json"(exports, module) {
    module.exports = {
      $id: "https://raw.githubusercontent.com/ajv-validator/ajv/master/lib/refs/data.json#",
      description: "Meta-schema for $data reference (JSON AnySchema extension proposal)",
      type: "object",
      required: ["$data"],
      properties: {
        $data: {
          type: "string",
          anyOf: [{ format: "relative-json-pointer" }, { format: "json-pointer" }]
        }
      },
      additionalProperties: false
    };
  }
});

// node_modules/uri-js-replace/dist/Serialize.js
var require_Serialize = __commonJS({
  "node_modules/uri-js-replace/dist/Serialize.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.serialize = void 0;
    function serialize2(components) {
      let buildResult = buildStartUrl(components);
      let urlBuilder;
      try {
        urlBuilder = new URL(buildResult.startUrl);
      } catch (error) {
        if (error.message) {
          console.error(error.message + " " + buildResult.startUrl);
        }
        return "";
      }
      if (components.scheme !== void 0 && !buildResult.temporarySchemeAndHostUsed && !buildResult.temporarySchemeUsed) {
        urlBuilder.protocol = components.scheme.toLowerCase();
      } else {
        urlBuilder.protocol = "";
      }
      if (components.host !== void 0 && !buildResult.temporarySchemeAndHostUsed && !buildResult.temporaryHostUsed) {
        urlBuilder.host = components.host;
      } else {
        urlBuilder.host = "";
      }
      if (components.port) {
        urlBuilder.port = String(components.port);
      }
      if (components.path) {
        urlBuilder.pathname = components.path;
      }
      if (components.userinfo) {
        let parts = components.userinfo.split(":");
        if (parts[0]) {
          urlBuilder.username = parts[0];
        }
        if (parts[1]) {
          urlBuilder.password = parts[1];
        }
      }
      if (components.query) {
        urlBuilder.search = components.query;
      }
      if (components.fragment) {
        urlBuilder.hash = components.fragment;
      }
      let result = urlBuilder.toString();
      if (!components.path && result.endsWith("/")) {
        result = result.slice(0, -1);
      }
      if (buildResult.temporarySchemeAndHostUsed) {
        result = result.replace(temporarySchemeAndHost, "");
        if (result.startsWith("/")) {
          result = result.slice(1);
        }
      }
      if (buildResult.temporaryHostUsed) {
        result = result.replace(temporaryHost, "");
      }
      if (buildResult.temporarySchemeUsed) {
        result = result.replace(temporaryScheme, "");
      }
      return result;
    }
    exports.serialize = serialize2;
    var temporaryScheme = "https:";
    var temporaryHost = "_remove_me_host_";
    var temporarySchemeAndHost = temporaryScheme + "//" + temporaryHost;
    function buildStartUrl(components) {
      let result = {
        startUrl: "",
        temporaryHostUsed: false,
        temporarySchemeUsed: false,
        temporarySchemeAndHostUsed: false
      };
      if (components.scheme && components.host) {
        result.startUrl = components.scheme + "://" + components.host;
        return result;
      }
      if (components.host) {
        result.temporarySchemeUsed = true;
        result.startUrl = temporaryScheme + components.host;
        return result;
      }
      if (components.scheme) {
        if (components.path) {
          result.startUrl = components.scheme + ":" + components.path;
          return result;
        }
        result.temporaryHostUsed = true;
        result.startUrl = components.scheme + ":" + temporaryHost;
        return result;
      }
      result.temporarySchemeAndHostUsed = true;
      result.startUrl = temporarySchemeAndHost;
      return result;
    }
  }
});

// node_modules/uri-js-replace/dist/Parse.js
var require_Parse = __commonJS({
  "node_modules/uri-js-replace/dist/Parse.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.parse = void 0;
    function parse3(uriString) {
      let result = {
        path: "",
        fragment: void 0,
        host: void 0,
        port: void 0,
        query: void 0,
        reference: void 0,
        scheme: void 0,
        userinfo: void 0
      };
      if (uriString.includes("#")) {
        result.fragment = "";
      }
      let { parsed, addedDefaultScheme, addedTemporaryHost, error } = recognizeUrl(uriString);
      if (error || parsed === void 0) {
        result.error = error;
        return result;
      }
      if (typeof parsed.protocol !== void 0 && parsed.protocol !== "" && !addedDefaultScheme) {
        result.scheme = String(parsed.protocol).replace(":", "");
      }
      if (typeof parsed.username !== void 0 && parsed.username !== "") {
        let userinfo = parsed.username;
        if (parsed.password) {
          userinfo += ":" + parsed.password;
        }
        result.userinfo = userinfo;
      }
      if (typeof parsed.hostname !== void 0 && parsed.hostname !== "" && !addedTemporaryHost) {
        result.host = parsed.hostname;
        if (result.host.startsWith("[")) {
          result.host = result.host.substring(1);
          result.host = result.host.slice(0, -1);
        }
      }
      if (typeof parsed.port !== void 0 && parsed.port !== "") {
        result.port = Number(parsed.port);
      }
      if (typeof parsed.pathname !== void 0 && parsed.pathname !== "/") {
        result.path = parsed.pathname;
        if (addedTemporaryHost && result.path.startsWith("/")) {
          result.path = result.path.substring(1);
        }
      }
      if (typeof parsed.search !== void 0 && parsed.search !== "") {
        result.query = parsed.search.replace("?", "");
      }
      if (typeof parsed.hash !== void 0 && parsed.hash !== "") {
        result.fragment = parsed.hash.replace("#", "");
      }
      if (result.scheme === void 0 && result.userinfo === void 0 && result.host === void 0 && result.port === void 0 && !result.path && result.query === void 0) {
        result.reference = "same-document";
      } else if (result.scheme === void 0) {
        result.reference = "relative";
      } else if (result.fragment === void 0) {
        result.reference = "absolute";
      } else {
        result.reference = "uri";
      }
      return result;
    }
    exports.parse = parse3;
    var temporaryHost = "_remove_me_host/";
    function recognizeUrl(uriString) {
      let result = {
        parsed: void 0,
        addedDefaultScheme: false,
        addedTemporaryHost: false,
        error: void 0
      };
      let firstError;
      try {
        result.parsed = new URL(uriString);
        return result;
      } catch (error) {
        firstError = error;
      }
      if (uriString.startsWith("//")) {
        try {
          result.parsed = new URL("https:" + uriString);
          result.addedDefaultScheme = true;
          return result;
        } catch (otherError) {
          result.error = firstError.message;
          return result;
        }
      }
      try {
        result.parsed = new URL("https://" + temporaryHost + uriString);
        result.addedDefaultScheme = true;
        result.addedTemporaryHost = true;
        return result;
      } catch (otherError) {
      }
      result.error = firstError.message;
      return result;
    }
  }
});

// node_modules/uri-js-replace/dist/Resolve.js
var require_Resolve = __commonJS({
  "node_modules/uri-js-replace/dist/Resolve.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.resolveComponents = exports.resolve = void 0;
    var Parse_1 = require_Parse();
    var Serialize_1 = require_Serialize();
    function resolve(baseURI, relativeURI, options) {
      const schemeLessOptions = Object.assign({ scheme: "null" }, options);
      const resolved = resolveComponents((0, Parse_1.parse)(baseURI), (0, Parse_1.parse)(relativeURI), schemeLessOptions, true);
      return (0, Serialize_1.serialize)(resolved);
    }
    exports.resolve = resolve;
    function resolveComponents(base, relative, options, skipNormalization) {
      const target = {};
      if (!skipNormalization) {
        base = (0, Parse_1.parse)((0, Serialize_1.serialize)(base));
        relative = (0, Parse_1.parse)((0, Serialize_1.serialize)(relative));
      }
      options = options || {};
      if (!options.tolerant && relative.scheme) {
        target.scheme = relative.scheme;
        target.userinfo = relative.userinfo;
        target.host = relative.host;
        target.port = relative.port;
        target.path = removeDotSegments(relative.path || "");
        target.query = relative.query;
      } else {
        if (relative.userinfo !== void 0 || relative.host !== void 0 || relative.port !== void 0) {
          target.userinfo = relative.userinfo;
          target.host = relative.host;
          target.port = relative.port;
          target.path = removeDotSegments(relative.path || "");
          target.query = relative.query;
        } else {
          if (!relative.path) {
            target.path = base.path;
            if (relative.query !== void 0) {
              target.query = relative.query;
            } else {
              target.query = base.query;
            }
          } else {
            if (relative.path.charAt(0) === "/") {
              target.path = removeDotSegments(relative.path);
            } else {
              if ((base.userinfo !== void 0 || base.host !== void 0 || base.port !== void 0) && !base.path) {
                target.path = "/" + relative.path;
              } else if (!base.path) {
                target.path = relative.path;
              } else {
                target.path = base.path.slice(0, base.path.lastIndexOf("/") + 1) + relative.path;
              }
              target.path = removeDotSegments(target.path);
            }
            target.query = relative.query;
          }
          target.userinfo = base.userinfo;
          target.host = base.host;
          target.port = base.port;
        }
        target.scheme = base.scheme;
      }
      target.fragment = relative.fragment;
      return target;
    }
    exports.resolveComponents = resolveComponents;
    function removeDotSegments(input) {
      const RDS1 = /^\.\.?\//u;
      const RDS2 = /^\/\.(?:\/|$)/u;
      const RDS3 = /^\/\.\.(?:\/|$)/u;
      const RDS5 = /^\/?(?:.|\n)*?(?=\/|$)/u;
      const output = [];
      while (input.length) {
        if (input.match(RDS1)) {
          input = input.replace(RDS1, "");
        } else if (input.match(RDS2)) {
          input = input.replace(RDS2, "/");
        } else if (input.match(RDS3)) {
          input = input.replace(RDS3, "/");
          output.pop();
        } else if (input === "." || input === "..") {
          input = "";
        } else {
          const im = input.match(RDS5);
          if (im) {
            const s2 = im[0];
            input = input.slice(s2.length);
            output.push(s2);
          } else {
            throw new Error("Unexpected dot segment condition");
          }
        }
      }
      return output.join("");
    }
  }
});

// node_modules/uri-js-replace/dist/index.js
var require_dist = __commonJS({
  "node_modules/uri-js-replace/dist/index.js"(exports) {
    "use strict";
    var __createBinding = exports && exports.__createBinding || (Object.create ? (function(o2, m2, k2, k22) {
      if (k22 === void 0) k22 = k2;
      var desc = Object.getOwnPropertyDescriptor(m2, k2);
      if (!desc || ("get" in desc ? !m2.__esModule : desc.writable || desc.configurable)) {
        desc = { enumerable: true, get: function() {
          return m2[k2];
        } };
      }
      Object.defineProperty(o2, k22, desc);
    }) : (function(o2, m2, k2, k22) {
      if (k22 === void 0) k22 = k2;
      o2[k22] = m2[k2];
    }));
    var __exportStar = exports && exports.__exportStar || function(m2, exports2) {
      for (var p2 in m2) if (p2 !== "default" && !Object.prototype.hasOwnProperty.call(exports2, p2)) __createBinding(exports2, m2, p2);
    };
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.normalize = exports.equal = void 0;
    var Serialize_1 = require_Serialize();
    var Parse_1 = require_Parse();
    __exportStar(require_Resolve(), exports);
    __exportStar(require_Serialize(), exports);
    __exportStar(require_Parse(), exports);
    function equal(uriA, uriB) {
      let processedA;
      let processedB;
      if (typeof uriA === "string") {
        processedA = (0, Serialize_1.serialize)((0, Parse_1.parse)(uriA));
      } else {
        processedA = (0, Serialize_1.serialize)(uriA);
      }
      if (typeof uriB === "string") {
        processedB = (0, Serialize_1.serialize)((0, Parse_1.parse)(uriB));
      } else {
        processedB = (0, Serialize_1.serialize)(uriB);
      }
      return processedA.toLowerCase() === processedB.toLowerCase();
    }
    exports.equal = equal;
    function normalize2(uri) {
      if (typeof uri === "string") {
        return (0, Serialize_1.serialize)((0, Parse_1.parse)(uri));
      } else {
        return (0, Parse_1.parse)((0, Serialize_1.serialize)(uri));
      }
    }
    exports.normalize = normalize2;
  }
});

// node_modules/@redocly/ajv/dist/runtime/uri.js
var require_uri = __commonJS({
  "node_modules/@redocly/ajv/dist/runtime/uri.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var uri = require_dist();
    uri.code = 'require("ajv/dist/runtime/uri").default';
    exports.default = uri;
  }
});

// node_modules/@redocly/ajv/dist/core.js
var require_core2 = __commonJS({
  "node_modules/@redocly/ajv/dist/core.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.CodeGen = exports.Name = exports.nil = exports.stringify = exports.str = exports._ = exports.KeywordCxt = void 0;
    var validate_1 = require_validate();
    Object.defineProperty(exports, "KeywordCxt", { enumerable: true, get: function() {
      return validate_1.KeywordCxt;
    } });
    var codegen_1 = require_codegen();
    Object.defineProperty(exports, "_", { enumerable: true, get: function() {
      return codegen_1._;
    } });
    Object.defineProperty(exports, "str", { enumerable: true, get: function() {
      return codegen_1.str;
    } });
    Object.defineProperty(exports, "stringify", { enumerable: true, get: function() {
      return codegen_1.stringify;
    } });
    Object.defineProperty(exports, "nil", { enumerable: true, get: function() {
      return codegen_1.nil;
    } });
    Object.defineProperty(exports, "Name", { enumerable: true, get: function() {
      return codegen_1.Name;
    } });
    Object.defineProperty(exports, "CodeGen", { enumerable: true, get: function() {
      return codegen_1.CodeGen;
    } });
    var validation_error_1 = require_validation_error();
    var ref_error_1 = require_ref_error();
    var rules_1 = require_rules2();
    var compile_1 = require_compile();
    var codegen_2 = require_codegen();
    var resolve_1 = require_resolve2();
    var dataType_1 = require_dataType();
    var util_1 = require_util();
    var $dataRefSchema = require_data();
    var uri_1 = require_uri();
    var defaultRegExp = (str, flags) => new RegExp(str, flags);
    defaultRegExp.code = "new RegExp";
    var META_IGNORE_OPTIONS = [
      "removeAdditional",
      "useDefaults",
      "coerceTypes",
      "defaultUnevaluatedProperties",
      "defaultAdditionalProperties"
    ];
    var EXT_SCOPE_NAMES = /* @__PURE__ */ new Set([
      "validate",
      "serialize",
      "parse",
      "wrapper",
      "root",
      "schema",
      "keyword",
      "pattern",
      "formats",
      "validate$data",
      "func",
      "obj",
      "Error"
    ]);
    var removedOptions = {
      errorDataPath: "",
      format: "`validateFormats: false` can be used instead.",
      nullable: '"nullable" keyword is supported by default.',
      jsonPointers: "Deprecated jsPropertySyntax can be used instead.",
      extendRefs: "Deprecated ignoreKeywordsWithRef can be used instead.",
      missingRefs: "Pass empty schema with $id that should be ignored to ajv.addSchema.",
      processCode: "Use option `code: {process: (code, schemaEnv: object) => string}`",
      sourceCode: "Use option `code: {source: true}`",
      strictDefaults: "It is default now, see option `strict`.",
      strictKeywords: "It is default now, see option `strict`.",
      uniqueItems: '"uniqueItems" keyword is always validated.',
      unknownFormats: "Disable strict mode or pass `true` to `ajv.addFormat` (or `formats` option).",
      cache: "Map is used as cache, schema object as key.",
      serialize: "Map is used as cache, schema object as key.",
      ajvErrors: "It is default now."
    };
    var deprecatedOptions = {
      ignoreKeywordsWithRef: "",
      jsPropertySyntax: "",
      unicode: '"minLength"/"maxLength" account for unicode characters by default.'
    };
    var MAX_EXPRESSION = 200;
    function requiredOptions(o2) {
      var _a3, _b2, _c, _d, _e2, _f, _g, _h, _j, _k, _l, _m, _o, _p, _q, _r, _s, _t, _u, _v, _w, _x, _y, _z, _0;
      const s2 = o2.strict;
      const _optz = (_a3 = o2.code) === null || _a3 === void 0 ? void 0 : _a3.optimize;
      const optimize = _optz === true || _optz === void 0 ? 1 : _optz || 0;
      const regExp = (_c = (_b2 = o2.code) === null || _b2 === void 0 ? void 0 : _b2.regExp) !== null && _c !== void 0 ? _c : defaultRegExp;
      const uriResolver = (_d = o2.uriResolver) !== null && _d !== void 0 ? _d : uri_1.default;
      return {
        strictSchema: (_f = (_e2 = o2.strictSchema) !== null && _e2 !== void 0 ? _e2 : s2) !== null && _f !== void 0 ? _f : true,
        strictNumbers: (_h = (_g = o2.strictNumbers) !== null && _g !== void 0 ? _g : s2) !== null && _h !== void 0 ? _h : true,
        strictTypes: (_k = (_j = o2.strictTypes) !== null && _j !== void 0 ? _j : s2) !== null && _k !== void 0 ? _k : "log",
        strictTuples: (_m = (_l = o2.strictTuples) !== null && _l !== void 0 ? _l : s2) !== null && _m !== void 0 ? _m : "log",
        strictRequired: (_p = (_o = o2.strictRequired) !== null && _o !== void 0 ? _o : s2) !== null && _p !== void 0 ? _p : false,
        code: o2.code ? { ...o2.code, optimize, regExp } : { optimize, regExp },
        loopRequired: (_q = o2.loopRequired) !== null && _q !== void 0 ? _q : MAX_EXPRESSION,
        loopEnum: (_r = o2.loopEnum) !== null && _r !== void 0 ? _r : MAX_EXPRESSION,
        meta: (_s = o2.meta) !== null && _s !== void 0 ? _s : true,
        messages: (_t = o2.messages) !== null && _t !== void 0 ? _t : true,
        inlineRefs: (_u = o2.inlineRefs) !== null && _u !== void 0 ? _u : true,
        schemaId: (_v = o2.schemaId) !== null && _v !== void 0 ? _v : "$id",
        addUsedSchema: (_w = o2.addUsedSchema) !== null && _w !== void 0 ? _w : true,
        validateSchema: (_x = o2.validateSchema) !== null && _x !== void 0 ? _x : true,
        validateFormats: (_y = o2.validateFormats) !== null && _y !== void 0 ? _y : true,
        unicodeRegExp: (_z = o2.unicodeRegExp) !== null && _z !== void 0 ? _z : true,
        int32range: (_0 = o2.int32range) !== null && _0 !== void 0 ? _0 : true,
        uriResolver
      };
    }
    var Ajv = class {
      constructor(opts = {}) {
        this.schemas = {};
        this.refs = {};
        this.formats = {};
        this._compilations = /* @__PURE__ */ new Set();
        this._loading = {};
        this._cache = /* @__PURE__ */ new Map();
        opts = this.opts = { ...opts, ...requiredOptions(opts) };
        const { es5, lines } = this.opts.code;
        this.scope = new codegen_2.ValueScope({ scope: {}, prefixes: EXT_SCOPE_NAMES, es5, lines });
        this.logger = getLogger(opts.logger);
        const formatOpt = opts.validateFormats;
        opts.validateFormats = false;
        this.RULES = (0, rules_1.getRules)();
        checkOptions.call(this, removedOptions, opts, "NOT SUPPORTED");
        checkOptions.call(this, deprecatedOptions, opts, "DEPRECATED", "warn");
        this._metaOpts = getMetaSchemaOptions.call(this);
        if (opts.formats)
          addInitialFormats.call(this);
        this._addVocabularies();
        this._addDefaultMetaSchema();
        if (opts.keywords)
          addInitialKeywords.call(this, opts.keywords);
        if (typeof opts.meta == "object")
          this.addMetaSchema(opts.meta);
        addInitialSchemas.call(this);
        opts.validateFormats = formatOpt;
      }
      _addVocabularies() {
        this.addKeyword("$async");
      }
      _addDefaultMetaSchema() {
        const { $data, meta, schemaId } = this.opts;
        let _dataRefSchema = $dataRefSchema;
        if (schemaId === "id") {
          _dataRefSchema = { ...$dataRefSchema };
          _dataRefSchema.id = _dataRefSchema.$id;
          delete _dataRefSchema.$id;
        }
        if (meta && $data)
          this.addMetaSchema(_dataRefSchema, _dataRefSchema[schemaId], false);
      }
      defaultMeta() {
        const { meta, schemaId } = this.opts;
        return this.opts.defaultMeta = typeof meta == "object" ? meta[schemaId] || meta : void 0;
      }
      validate(schemaKeyRef, data) {
        let v2;
        if (typeof schemaKeyRef == "string") {
          v2 = this.getSchema(schemaKeyRef);
          if (!v2)
            throw new Error(`no schema with key or ref "${schemaKeyRef}"`);
        } else {
          v2 = this.compile(schemaKeyRef);
        }
        const valid = v2(data);
        if (!("$async" in v2))
          this.errors = v2.errors;
        return valid;
      }
      compile(schema4, _meta) {
        const sch = this._addSchema(schema4, _meta);
        return sch.validate || this._compileSchemaEnv(sch);
      }
      compileAsync(schema4, meta) {
        if (typeof this.opts.loadSchema != "function") {
          throw new Error("options.loadSchema should be a function");
        }
        const { loadSchema } = this.opts;
        return runCompileAsync.call(this, schema4, meta);
        async function runCompileAsync(_schema, _meta) {
          await loadMetaSchema.call(this, _schema.$schema);
          const sch = this._addSchema(_schema, _meta);
          return sch.validate || _compileAsync.call(this, sch);
        }
        async function loadMetaSchema($ref) {
          if ($ref && !this.getSchema($ref)) {
            await runCompileAsync.call(this, { $ref }, true);
          }
        }
        async function _compileAsync(sch) {
          try {
            return this._compileSchemaEnv(sch);
          } catch (e) {
            if (!(e instanceof ref_error_1.default))
              throw e;
            checkLoaded.call(this, e);
            await loadMissingSchema.call(this, e.missingSchema);
            return _compileAsync.call(this, sch);
          }
        }
        function checkLoaded({ missingSchema: ref, missingRef }) {
          if (this.refs[ref]) {
            throw new Error(`AnySchema ${ref} is loaded but ${missingRef} cannot be resolved`);
          }
        }
        async function loadMissingSchema(ref) {
          const _schema = await _loadSchema.call(this, ref);
          if (!this.refs[ref])
            await loadMetaSchema.call(this, _schema.$schema);
          if (!this.refs[ref])
            this.addSchema(_schema, ref, meta);
        }
        async function _loadSchema(ref) {
          const p2 = this._loading[ref];
          if (p2)
            return p2;
          try {
            return await (this._loading[ref] = loadSchema(ref));
          } finally {
            delete this._loading[ref];
          }
        }
      }
      // Adds schema to the instance
      addSchema(schema4, key2, _meta, _validateSchema = this.opts.validateSchema) {
        if (Array.isArray(schema4)) {
          for (const sch of schema4)
            this.addSchema(sch, void 0, _meta, _validateSchema);
          return this;
        }
        let id;
        if (typeof schema4 === "object") {
          const { schemaId } = this.opts;
          id = schema4[schemaId];
          if (id !== void 0 && typeof id != "string") {
            throw new Error(`schema ${schemaId} must be string`);
          }
        }
        key2 = (0, resolve_1.normalizeId)(key2 || id);
        this._checkUnique(key2);
        this.schemas[key2] = this._addSchema(schema4, _meta, key2, _validateSchema, true);
        return this;
      }
      // Add schema that will be used to validate other schemas
      // options in META_IGNORE_OPTIONS are alway set to false
      addMetaSchema(schema4, key2, _validateSchema = this.opts.validateSchema) {
        this.addSchema(schema4, key2, true, _validateSchema);
        return this;
      }
      //  Validate schema against its meta-schema
      validateSchema(schema4, throwOrLogError) {
        if (typeof schema4 == "boolean")
          return true;
        let $schema;
        $schema = schema4.$schema;
        if ($schema !== void 0 && typeof $schema != "string") {
          throw new Error("$schema must be a string");
        }
        $schema = $schema || this.opts.defaultMeta || this.defaultMeta();
        if (!$schema) {
          this.logger.warn("meta-schema not available");
          this.errors = null;
          return true;
        }
        const valid = this.validate($schema, schema4);
        if (!valid && throwOrLogError) {
          const message = "schema is invalid: " + this.errorsText();
          if (this.opts.validateSchema === "log")
            this.logger.error(message);
          else
            throw new Error(message);
        }
        return valid;
      }
      // Get compiled schema by `key` or `ref`.
      // (`key` that was passed to `addSchema` or full schema reference - `schema.$id` or resolved id)
      getSchema(keyRef) {
        let sch;
        while (typeof (sch = getSchEnv.call(this, keyRef)) == "string")
          keyRef = sch;
        if (sch === void 0) {
          const { schemaId } = this.opts;
          const root = new compile_1.SchemaEnv({ schema: {}, schemaId });
          sch = compile_1.resolveSchema.call(this, root, keyRef);
          if (!sch)
            return;
          this.refs[keyRef] = sch;
        }
        return sch.validate || this._compileSchemaEnv(sch);
      }
      // Remove cached schema(s).
      // If no parameter is passed all schemas but meta-schemas are removed.
      // If RegExp is passed all schemas with key/id matching pattern but meta-schemas are removed.
      // Even if schema is referenced by other schemas it still can be removed as other schemas have local references.
      removeSchema(schemaKeyRef) {
        if (schemaKeyRef instanceof RegExp) {
          this._removeAllSchemas(this.schemas, schemaKeyRef);
          this._removeAllSchemas(this.refs, schemaKeyRef);
          return this;
        }
        switch (typeof schemaKeyRef) {
          case "undefined":
            this._removeAllSchemas(this.schemas);
            this._removeAllSchemas(this.refs);
            this._cache.clear();
            return this;
          case "string": {
            const sch = getSchEnv.call(this, schemaKeyRef);
            if (typeof sch == "object")
              this._cache.delete(sch.schema);
            delete this.schemas[schemaKeyRef];
            delete this.refs[schemaKeyRef];
            return this;
          }
          case "object": {
            const cacheKey = schemaKeyRef;
            this._cache.delete(cacheKey);
            let id = schemaKeyRef[this.opts.schemaId];
            if (id) {
              id = (0, resolve_1.normalizeId)(id);
              delete this.schemas[id];
              delete this.refs[id];
            }
            return this;
          }
          default:
            throw new Error("ajv.removeSchema: invalid parameter");
        }
      }
      // add "vocabulary" - a collection of keywords
      addVocabulary(definitions) {
        for (const def of definitions)
          this.addKeyword(def);
        return this;
      }
      addKeyword(kwdOrDef, def) {
        let keyword;
        if (typeof kwdOrDef == "string") {
          keyword = kwdOrDef;
          if (typeof def == "object") {
            this.logger.warn("these parameters are deprecated, see docs for addKeyword");
            def.keyword = keyword;
          }
        } else if (typeof kwdOrDef == "object" && def === void 0) {
          def = kwdOrDef;
          keyword = def.keyword;
          if (Array.isArray(keyword) && !keyword.length) {
            throw new Error("addKeywords: keyword must be string or non-empty array");
          }
        } else {
          throw new Error("invalid addKeywords parameters");
        }
        checkKeyword.call(this, keyword, def);
        if (!def) {
          (0, util_1.eachItem)(keyword, (kwd) => addRule.call(this, kwd));
          return this;
        }
        keywordMetaschema.call(this, def);
        const definition = {
          ...def,
          type: (0, dataType_1.getJSONTypes)(def.type),
          schemaType: (0, dataType_1.getJSONTypes)(def.schemaType)
        };
        (0, util_1.eachItem)(keyword, definition.type.length === 0 ? (k2) => addRule.call(this, k2, definition) : (k2) => definition.type.forEach((t) => addRule.call(this, k2, definition, t)));
        return this;
      }
      getKeyword(keyword) {
        const rule = this.RULES.all[keyword];
        return typeof rule == "object" ? rule.definition : !!rule;
      }
      // Remove keyword
      removeKeyword(keyword) {
        const { RULES } = this;
        delete RULES.keywords[keyword];
        delete RULES.all[keyword];
        for (const group of RULES.rules) {
          const i2 = group.rules.findIndex((rule) => rule.keyword === keyword);
          if (i2 >= 0)
            group.rules.splice(i2, 1);
        }
        return this;
      }
      // Add format
      addFormat(name, format2) {
        if (typeof format2 == "string")
          format2 = new RegExp(format2);
        this.formats[name] = format2;
        return this;
      }
      errorsText(errors2 = this.errors, { separator = ", ", dataVar = "data" } = {}) {
        if (!errors2 || errors2.length === 0)
          return "No errors";
        return errors2.map((e) => `${dataVar}${e.instancePath} ${e.message}`).reduce((text, msg) => text + separator + msg);
      }
      $dataMetaSchema(metaSchema, keywordsJsonPointers) {
        const rules = this.RULES.all;
        metaSchema = JSON.parse(JSON.stringify(metaSchema));
        for (const jsonPointer of keywordsJsonPointers) {
          const segments = jsonPointer.split("/").slice(1);
          let keywords = metaSchema;
          for (const seg of segments)
            keywords = keywords[seg];
          for (const key2 in rules) {
            const rule = rules[key2];
            if (typeof rule != "object")
              continue;
            const { $data } = rule.definition;
            const schema4 = keywords[key2];
            if ($data && schema4)
              keywords[key2] = schemaOrData(schema4);
          }
        }
        return metaSchema;
      }
      _removeAllSchemas(schemas2, regex) {
        for (const keyRef in schemas2) {
          const sch = schemas2[keyRef];
          if (!regex || regex.test(keyRef)) {
            if (typeof sch == "string") {
              delete schemas2[keyRef];
            } else if (sch && !sch.meta) {
              this._cache.delete(sch.schema);
              delete schemas2[keyRef];
            }
          }
        }
      }
      _addSchema(schema4, meta, baseId, validateSchema = this.opts.validateSchema, addSchema = this.opts.addUsedSchema) {
        let id;
        const { schemaId } = this.opts;
        if (typeof schema4 == "object") {
          id = schema4[schemaId];
        } else {
          if (this.opts.jtd)
            throw new Error("schema must be object");
          else if (typeof schema4 != "boolean")
            throw new Error("schema must be object or boolean");
        }
        let sch = this._cache.get(schema4);
        if (sch !== void 0)
          return sch;
        baseId = (0, resolve_1.normalizeId)(id || baseId);
        const localRefs = resolve_1.getSchemaRefs.call(this, schema4, baseId);
        sch = new compile_1.SchemaEnv({ schema: schema4, schemaId, meta, baseId, localRefs });
        this._cache.set(sch.schema, sch);
        if (addSchema && !baseId.startsWith("#")) {
          if (baseId)
            this._checkUnique(baseId);
          this.refs[baseId] = sch;
        }
        if (validateSchema)
          this.validateSchema(schema4, true);
        return sch;
      }
      _checkUnique(id) {
        if (this.schemas[id] || this.refs[id]) {
          throw new Error(`schema with key or id "${id}" already exists`);
        }
      }
      _compileSchemaEnv(sch) {
        if (sch.meta)
          this._compileMetaSchema(sch);
        else
          compile_1.compileSchema.call(this, sch);
        if (!sch.validate)
          throw new Error("ajv implementation error");
        return sch.validate;
      }
      _compileMetaSchema(sch) {
        const currentOpts = this.opts;
        this.opts = this._metaOpts;
        try {
          compile_1.compileSchema.call(this, sch);
        } finally {
          this.opts = currentOpts;
        }
      }
    };
    Ajv.ValidationError = validation_error_1.default;
    Ajv.MissingRefError = ref_error_1.default;
    exports.default = Ajv;
    function checkOptions(checkOpts, options, msg, log = "error") {
      for (const key2 in checkOpts) {
        const opt = key2;
        if (opt in options)
          this.logger[log](`${msg}: option ${key2}. ${checkOpts[opt]}`);
      }
    }
    function getSchEnv(keyRef) {
      keyRef = (0, resolve_1.normalizeId)(keyRef);
      return this.schemas[keyRef] || this.refs[keyRef];
    }
    function addInitialSchemas() {
      const optsSchemas = this.opts.schemas;
      if (!optsSchemas)
        return;
      if (Array.isArray(optsSchemas))
        this.addSchema(optsSchemas);
      else
        for (const key2 in optsSchemas)
          this.addSchema(optsSchemas[key2], key2);
    }
    function addInitialFormats() {
      for (const name in this.opts.formats) {
        const format2 = this.opts.formats[name];
        if (format2)
          this.addFormat(name, format2);
      }
    }
    function addInitialKeywords(defs) {
      if (Array.isArray(defs)) {
        this.addVocabulary(defs);
        return;
      }
      this.logger.warn("keywords option as map is deprecated, pass array");
      for (const keyword in defs) {
        const def = defs[keyword];
        if (!def.keyword)
          def.keyword = keyword;
        this.addKeyword(def);
      }
    }
    function getMetaSchemaOptions() {
      const metaOpts = { ...this.opts };
      for (const opt of META_IGNORE_OPTIONS)
        delete metaOpts[opt];
      return metaOpts;
    }
    var noLogs = { log() {
    }, warn() {
    }, error() {
    } };
    function getLogger(logger) {
      if (logger === false)
        return noLogs;
      if (logger === void 0)
        return console;
      if (logger.log && logger.warn && logger.error)
        return logger;
      throw new Error("logger must implement log, warn and error methods");
    }
    var KEYWORD_NAME = /^[a-z_$][a-z0-9_$:-]*$/i;
    function checkKeyword(keyword, def) {
      const { RULES } = this;
      (0, util_1.eachItem)(keyword, (kwd) => {
        if (RULES.keywords[kwd])
          throw new Error(`Keyword ${kwd} is already defined`);
        if (!KEYWORD_NAME.test(kwd))
          throw new Error(`Keyword ${kwd} has invalid name`);
      });
      if (!def)
        return;
      if (def.$data && !("code" in def || "validate" in def)) {
        throw new Error('$data keyword must have "code" or "validate" function');
      }
    }
    function addRule(keyword, definition, dataType) {
      var _a3;
      const post = definition === null || definition === void 0 ? void 0 : definition.post;
      if (dataType && post)
        throw new Error('keyword with "post" flag cannot have "type"');
      const { RULES } = this;
      let ruleGroup = post ? RULES.post : RULES.rules.find(({ type: t }) => t === dataType);
      if (!ruleGroup) {
        ruleGroup = { type: dataType, rules: [] };
        RULES.rules.push(ruleGroup);
      }
      RULES.keywords[keyword] = true;
      if (!definition)
        return;
      const rule = {
        keyword,
        definition: {
          ...definition,
          type: (0, dataType_1.getJSONTypes)(definition.type),
          schemaType: (0, dataType_1.getJSONTypes)(definition.schemaType)
        }
      };
      if (definition.before)
        addBeforeRule.call(this, ruleGroup, rule, definition.before);
      else
        ruleGroup.rules.push(rule);
      RULES.all[keyword] = rule;
      (_a3 = definition.implements) === null || _a3 === void 0 ? void 0 : _a3.forEach((kwd) => this.addKeyword(kwd));
    }
    function addBeforeRule(ruleGroup, rule, before) {
      const i2 = ruleGroup.rules.findIndex((_rule) => _rule.keyword === before);
      if (i2 >= 0) {
        ruleGroup.rules.splice(i2, 0, rule);
      } else {
        ruleGroup.rules.push(rule);
        this.logger.warn(`rule ${before} is not defined`);
      }
    }
    function keywordMetaschema(def) {
      let { metaSchema } = def;
      if (metaSchema === void 0)
        return;
      if (def.$data && this.opts.$data)
        metaSchema = schemaOrData(metaSchema);
      def.validateSchema = this.compile(metaSchema, true);
    }
    var $dataRef = {
      $ref: "https://raw.githubusercontent.com/ajv-validator/ajv/master/lib/refs/data.json#"
    };
    function schemaOrData(schema4) {
      return { anyOf: [schema4, $dataRef] };
    }
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/core/id.js
var require_id = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/core/id.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var def = {
      keyword: "id",
      code() {
        throw new Error('NOT SUPPORTED: keyword "id", use "$id" for schema ID');
      }
    };
    exports.default = def;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/core/ref.js
var require_ref = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/core/ref.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.getValidate = getValidate;
    exports.callRef = callRef;
    var ref_error_1 = require_ref_error();
    var code_1 = require_code2();
    var codegen_1 = require_codegen();
    var names_1 = require_names();
    var compile_1 = require_compile();
    var util_1 = require_util();
    var def = {
      keyword: "$ref",
      schemaType: "string",
      code(cxt) {
        const { gen, schema: $ref, it: it2 } = cxt;
        const { baseId, schemaEnv: env2, validateName, opts, self: self2 } = it2;
        const { root } = env2;
        if (($ref === "#" || $ref === "#/") && baseId === root.baseId)
          return callRootRef();
        const schOrEnv = compile_1.resolveRef.call(self2, root, baseId, $ref);
        if (schOrEnv === void 0)
          throw new ref_error_1.default(it2.opts.uriResolver, baseId, $ref);
        if (schOrEnv instanceof compile_1.SchemaEnv)
          return callValidate(schOrEnv);
        return inlineRefSchema(schOrEnv);
        function callRootRef() {
          if (env2 === root)
            return callRef(cxt, validateName, env2, env2.$async);
          const rootName = gen.scopeValue("root", { ref: root });
          return callRef(cxt, (0, codegen_1._)`${rootName}.validate`, root, root.$async);
        }
        function callValidate(sch) {
          const v2 = getValidate(cxt, sch);
          callRef(cxt, v2, sch, sch.$async);
        }
        function inlineRefSchema(sch) {
          const schName = gen.scopeValue("schema", opts.code.source === true ? { ref: sch, code: (0, codegen_1.stringify)(sch) } : { ref: sch });
          const valid = gen.name("valid");
          const schCxt = cxt.subschema(
            {
              schema: sch,
              dataTypes: [],
              schemaPath: codegen_1.nil,
              topSchemaRef: schName,
              errSchemaPath: $ref
            },
            valid,
            it2.isAllOfVariant
            // pass isAllOfVariant into inlined $ref
          );
          cxt.mergeEvaluated(schCxt);
          cxt.ok(valid);
        }
      }
    };
    function getValidate(cxt, sch) {
      const { gen } = cxt;
      return sch.validate ? gen.scopeValue("validate", { ref: sch.validate }) : (0, codegen_1._)`${gen.scopeValue("wrapper", { ref: sch })}.validate`;
    }
    function callRef(cxt, v2, sch, $async) {
      const { gen, it: it2 } = cxt;
      const { allErrors, schemaEnv: env2, opts } = it2;
      const passCxt = opts.passContext ? names_1.default.this : codegen_1.nil;
      if ($async)
        callAsyncRef();
      else
        callSyncRef();
      function callAsyncRef() {
        if (!env2.$async)
          throw new Error("async schema referenced by sync schema");
        const valid = gen.let("valid");
        gen.try(() => {
          gen.code((0, codegen_1._)`await ${(0, code_1.callValidateCode)(cxt, v2, passCxt)}`);
          addEvaluatedFrom(v2);
          if (!allErrors)
            gen.assign(valid, true);
        }, (e) => {
          gen.if((0, codegen_1._)`!(${e} instanceof ${it2.ValidationError})`, () => gen.throw(e));
          addErrorsFrom(e);
          if (!allErrors)
            gen.assign(valid, false);
        });
        cxt.ok(valid);
      }
      function callSyncRef() {
        const visitedNodes = gen.name("visitedNodes");
        gen.code((0, codegen_1._)`const ${visitedNodes} = (typeof visitedNodesForRef !== 'undefined') && visitedNodesForRef.get(${v2}) || new Set()`);
        gen.if((0, codegen_1._)`!${visitedNodes}.has(${cxt.data})`, () => {
          gen.code((0, codegen_1._)`if (typeof visitedNodesForRef !== 'undefined') visitedNodesForRef.set(${v2}, ${visitedNodes})`);
          gen.code((0, codegen_1._)`const dataNode = ${cxt.data}`);
          gen.code((0, codegen_1._)`if (typeof dataNode === "object" && dataNode !== null) ${visitedNodes}.add(dataNode)`);
          const res = cxt.result((0, code_1.callValidateCode)(cxt, v2, passCxt), () => addEvaluatedFrom(v2), () => addErrorsFrom(v2));
          gen.code((0, codegen_1._)`${visitedNodes}.delete(dataNode)`);
          return res;
        });
      }
      function addErrorsFrom(source) {
        const errs = (0, codegen_1._)`${source}.errors`;
        gen.assign(names_1.default.vErrors, (0, codegen_1._)`${names_1.default.vErrors} === null ? ${errs} : ${names_1.default.vErrors}.concat(${errs})`);
        gen.assign(names_1.default.errors, (0, codegen_1._)`${names_1.default.vErrors}.length`);
      }
      function addEvaluatedFrom(source) {
        var _a3;
        if (!it2.opts.unevaluated)
          return;
        const schEvaluated = (_a3 = sch === null || sch === void 0 ? void 0 : sch.validate) === null || _a3 === void 0 ? void 0 : _a3.evaluated;
        if (it2.props !== true) {
          if (schEvaluated && !schEvaluated.dynamicProps) {
            if (schEvaluated.props !== void 0) {
              it2.props = util_1.mergeEvaluated.props(gen, schEvaluated.props, it2.props);
            }
          } else {
            const props = gen.var("props", (0, codegen_1._)`${source}.evaluated.props`);
            it2.props = util_1.mergeEvaluated.props(gen, props, it2.props, codegen_1.Name);
          }
        }
        if (it2.items !== true) {
          if (schEvaluated && !schEvaluated.dynamicItems) {
            if (schEvaluated.items !== void 0) {
              it2.items = util_1.mergeEvaluated.items(gen, schEvaluated.items, it2.items);
            }
          } else {
            const items = gen.var("items", (0, codegen_1._)`${source}.evaluated.items`);
            it2.items = util_1.mergeEvaluated.items(gen, items, it2.items, codegen_1.Name);
          }
        }
      }
    }
    exports.default = def;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/core/index.js
var require_core3 = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/core/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var id_1 = require_id();
    var ref_1 = require_ref();
    var core = [
      "$schema",
      "$id",
      "$defs",
      "$vocabulary",
      { keyword: "$comment" },
      "definitions",
      id_1.default,
      ref_1.default
    ];
    exports.default = core;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/validation/limitNumber.js
var require_limitNumber = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/validation/limitNumber.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var codegen_1 = require_codegen();
    var ops = codegen_1.operators;
    var KWDs = {
      maximum: { okStr: "<=", ok: ops.LTE, fail: ops.GT },
      minimum: { okStr: ">=", ok: ops.GTE, fail: ops.LT },
      exclusiveMaximum: { okStr: "<", ok: ops.LT, fail: ops.GTE },
      exclusiveMinimum: { okStr: ">", ok: ops.GT, fail: ops.LTE }
    };
    var error = {
      message: ({ keyword, schemaCode }) => (0, codegen_1.str)`must be ${KWDs[keyword].okStr} ${schemaCode}`,
      params: ({ keyword, schemaCode }) => (0, codegen_1._)`{comparison: ${KWDs[keyword].okStr}, limit: ${schemaCode}}`
    };
    var def = {
      keyword: Object.keys(KWDs),
      type: "number",
      schemaType: "number",
      $data: true,
      error,
      code(cxt) {
        const { keyword, data, schemaCode } = cxt;
        cxt.fail$data((0, codegen_1._)`${data} ${KWDs[keyword].fail} ${schemaCode} || isNaN(${data})`);
      }
    };
    exports.default = def;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/validation/multipleOf.js
var require_multipleOf = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/validation/multipleOf.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var codegen_1 = require_codegen();
    var error = {
      message: ({ schemaCode }) => (0, codegen_1.str)`must be multiple of ${schemaCode}`,
      params: ({ schemaCode }) => (0, codegen_1._)`{multipleOf: ${schemaCode}}`
    };
    var def = {
      keyword: "multipleOf",
      type: "number",
      schemaType: "number",
      $data: true,
      error,
      code(cxt) {
        const { gen, data, schemaCode, it: it2 } = cxt;
        const prec = it2.opts.multipleOfPrecision;
        const res = gen.let("res");
        const invalid = prec ? (0, codegen_1._)`Math.abs(Math.round(${res}) - ${res}) > 1e-${prec}` : (0, codegen_1._)`${res} !== parseInt(${res})`;
        cxt.fail$data((0, codegen_1._)`(${schemaCode} === 0 || (${res} = ${data}/${schemaCode}, ${invalid}))`);
      }
    };
    exports.default = def;
  }
});

// node_modules/@redocly/ajv/dist/runtime/ucs2length.js
var require_ucs2length = __commonJS({
  "node_modules/@redocly/ajv/dist/runtime/ucs2length.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.default = ucs2length;
    function ucs2length(str) {
      const len = str.length;
      let length2 = 0;
      let pos = 0;
      let value;
      while (pos < len) {
        length2++;
        value = str.charCodeAt(pos++);
        if (value >= 55296 && value <= 56319 && pos < len) {
          value = str.charCodeAt(pos);
          if ((value & 64512) === 56320)
            pos++;
        }
      }
      return length2;
    }
    ucs2length.code = 'require("ajv/dist/runtime/ucs2length").default';
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/validation/limitLength.js
var require_limitLength = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/validation/limitLength.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var codegen_1 = require_codegen();
    var util_1 = require_util();
    var ucs2length_1 = require_ucs2length();
    var error = {
      message({ keyword, schemaCode }) {
        const comp = keyword === "maxLength" ? "more" : "fewer";
        return (0, codegen_1.str)`must NOT have ${comp} than ${schemaCode} characters`;
      },
      params: ({ schemaCode }) => (0, codegen_1._)`{limit: ${schemaCode}}`
    };
    var def = {
      keyword: ["maxLength", "minLength"],
      type: "string",
      schemaType: "number",
      $data: true,
      error,
      code(cxt) {
        const { keyword, data, schemaCode, it: it2 } = cxt;
        const op = keyword === "maxLength" ? codegen_1.operators.GT : codegen_1.operators.LT;
        const len = it2.opts.unicode === false ? (0, codegen_1._)`${data}.length` : (0, codegen_1._)`${(0, util_1.useFunc)(cxt.gen, ucs2length_1.default)}(${data})`;
        cxt.fail$data((0, codegen_1._)`${len} ${op} ${schemaCode}`);
      }
    };
    exports.default = def;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/validation/pattern.js
var require_pattern = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/validation/pattern.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var code_1 = require_code2();
    var codegen_1 = require_codegen();
    var error = {
      message: ({ schemaCode }) => (0, codegen_1.str)`must match pattern "${schemaCode}"`,
      params: ({ schemaCode }) => (0, codegen_1._)`{pattern: ${schemaCode}}`
    };
    var def = {
      keyword: "pattern",
      type: "string",
      schemaType: "string",
      $data: true,
      error,
      code(cxt) {
        const { data, $data, schema: schema4, schemaCode, it: it2 } = cxt;
        const u2 = it2.opts.unicodeRegExp ? "u" : "";
        const regExp = $data ? (0, codegen_1._)`(new RegExp(${schemaCode}, ${u2}))` : (0, code_1.usePattern)(cxt, schema4);
        cxt.fail$data((0, codegen_1._)`!${regExp}.test(${data})`);
      }
    };
    exports.default = def;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/validation/limitProperties.js
var require_limitProperties = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/validation/limitProperties.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var codegen_1 = require_codegen();
    var error = {
      message({ keyword, schemaCode }) {
        const comp = keyword === "maxProperties" ? "more" : "fewer";
        return (0, codegen_1.str)`must NOT have ${comp} than ${schemaCode} properties`;
      },
      params: ({ schemaCode }) => (0, codegen_1._)`{limit: ${schemaCode}}`
    };
    var def = {
      keyword: ["maxProperties", "minProperties"],
      type: "object",
      schemaType: "number",
      $data: true,
      error,
      code(cxt) {
        const { keyword, data, schemaCode } = cxt;
        const op = keyword === "maxProperties" ? codegen_1.operators.GT : codegen_1.operators.LT;
        cxt.fail$data((0, codegen_1._)`Object.keys(${data}).length ${op} ${schemaCode}`);
      }
    };
    exports.default = def;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/validation/required.js
var require_required = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/validation/required.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var code_1 = require_code2();
    var codegen_1 = require_codegen();
    var util_1 = require_util();
    var error = {
      message: ({ params: { missingProperty } }) => (0, codegen_1.str)`must have required property '${missingProperty}'`,
      params: ({ params: { missingProperty } }) => (0, codegen_1._)`{missingProperty: ${missingProperty}}`
    };
    var def = {
      keyword: "required",
      type: "object",
      schemaType: "array",
      $data: true,
      error,
      code(cxt) {
        const { gen, schema: schema4, schemaCode, data, $data, it: it2 } = cxt;
        const { opts } = it2;
        if (!$data && schema4.length === 0)
          return;
        const useLoop = schema4.length >= opts.loopRequired;
        if (it2.allErrors)
          allErrorsMode();
        else
          exitOnErrorMode();
        if (opts.strictRequired) {
          const props = cxt.parentSchema.properties;
          const { definedProperties } = cxt.it;
          for (const requiredKey of schema4) {
            if ((props === null || props === void 0 ? void 0 : props[requiredKey]) === void 0 && !definedProperties.has(requiredKey)) {
              const schemaPath = it2.schemaEnv.baseId + it2.errSchemaPath;
              const msg = `required property "${requiredKey}" is not defined at "${schemaPath}" (strictRequired)`;
              (0, util_1.checkStrictMode)(it2, msg, it2.opts.strictRequired);
            }
          }
        }
        function allErrorsMode() {
          if (useLoop || $data) {
            cxt.block$data(codegen_1.nil, loopAllRequired);
          } else {
            for (const prop of schema4) {
              (0, code_1.checkReportMissingProp)(cxt, prop);
            }
          }
        }
        function exitOnErrorMode() {
          const missing = gen.let("missing");
          if (useLoop || $data) {
            const valid = gen.let("valid", true);
            cxt.block$data(valid, () => loopUntilMissing(missing, valid));
            cxt.ok(valid);
          } else {
            gen.if((0, code_1.checkMissingProp)(cxt, schema4, missing));
            (0, code_1.reportMissingProp)(cxt, missing);
            gen.else();
          }
        }
        function loopAllRequired() {
          gen.forOf("prop", schemaCode, (prop) => {
            cxt.setParams({ missingProperty: prop });
            gen.if((0, code_1.noPropertyInData)(gen, data, prop, opts.ownProperties), () => cxt.error());
          });
        }
        function loopUntilMissing(missing, valid) {
          cxt.setParams({ missingProperty: missing });
          gen.forOf(missing, schemaCode, () => {
            gen.assign(valid, (0, code_1.propertyInData)(gen, data, missing, opts.ownProperties));
            gen.if((0, codegen_1.not)(valid), () => {
              cxt.error();
              gen.break();
            });
          }, codegen_1.nil);
        }
      }
    };
    exports.default = def;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/validation/limitItems.js
var require_limitItems = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/validation/limitItems.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var codegen_1 = require_codegen();
    var error = {
      message({ keyword, schemaCode }) {
        const comp = keyword === "maxItems" ? "more" : "fewer";
        return (0, codegen_1.str)`must NOT have ${comp} than ${schemaCode} items`;
      },
      params: ({ schemaCode }) => (0, codegen_1._)`{limit: ${schemaCode}}`
    };
    var def = {
      keyword: ["maxItems", "minItems"],
      type: "array",
      schemaType: "number",
      $data: true,
      error,
      code(cxt) {
        const { keyword, data, schemaCode } = cxt;
        const op = keyword === "maxItems" ? codegen_1.operators.GT : codegen_1.operators.LT;
        cxt.fail$data((0, codegen_1._)`${data}.length ${op} ${schemaCode}`);
      }
    };
    exports.default = def;
  }
});

// node_modules/@redocly/ajv/dist/runtime/equal.js
var require_equal = __commonJS({
  "node_modules/@redocly/ajv/dist/runtime/equal.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var equal = require_fast_deep_equal();
    equal.code = 'require("ajv/dist/runtime/equal").default';
    exports.default = equal;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/validation/uniqueItems.js
var require_uniqueItems = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/validation/uniqueItems.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var dataType_1 = require_dataType();
    var codegen_1 = require_codegen();
    var util_1 = require_util();
    var equal_1 = require_equal();
    var error = {
      message: ({ params: { i: i2, j: j2 } }) => (0, codegen_1.str)`must NOT have duplicate items (items ## ${j2} and ${i2} are identical)`,
      params: ({ params: { i: i2, j: j2 } }) => (0, codegen_1._)`{i: ${i2}, j: ${j2}}`
    };
    var def = {
      keyword: "uniqueItems",
      type: "array",
      schemaType: "boolean",
      $data: true,
      error,
      code(cxt) {
        const { gen, data, $data, schema: schema4, parentSchema, schemaCode, it: it2 } = cxt;
        if (!$data && !schema4)
          return;
        const valid = gen.let("valid");
        const itemTypes = parentSchema.items ? (0, dataType_1.getSchemaTypes)(parentSchema.items) : [];
        cxt.block$data(valid, validateUniqueItems, (0, codegen_1._)`${schemaCode} === false`);
        cxt.ok(valid);
        function validateUniqueItems() {
          const i2 = gen.let("i", (0, codegen_1._)`${data}.length`);
          const j2 = gen.let("j");
          cxt.setParams({ i: i2, j: j2 });
          gen.assign(valid, true);
          gen.if((0, codegen_1._)`${i2} > 1`, () => (canOptimize() ? loopN : loopN2)(i2, j2));
        }
        function canOptimize() {
          return itemTypes.length > 0 && !itemTypes.some((t) => t === "object" || t === "array");
        }
        function loopN(i2, j2) {
          const item = gen.name("item");
          const wrongType = (0, dataType_1.checkDataTypes)(itemTypes, item, it2.opts.strictNumbers, dataType_1.DataType.Wrong);
          const indices = gen.const("indices", (0, codegen_1._)`{}`);
          gen.for((0, codegen_1._)`;${i2}--;`, () => {
            gen.let(item, (0, codegen_1._)`${data}[${i2}]`);
            gen.if(wrongType, (0, codegen_1._)`continue`);
            if (itemTypes.length > 1)
              gen.if((0, codegen_1._)`typeof ${item} == "string"`, (0, codegen_1._)`${item} += "_"`);
            gen.if((0, codegen_1._)`typeof ${indices}[${item}] == "number"`, () => {
              gen.assign(j2, (0, codegen_1._)`${indices}[${item}]`);
              cxt.error();
              gen.assign(valid, false).break();
            }).code((0, codegen_1._)`${indices}[${item}] = ${i2}`);
          });
        }
        function loopN2(i2, j2) {
          const eql = (0, util_1.useFunc)(gen, equal_1.default);
          const outer = gen.name("outer");
          gen.label(outer).for((0, codegen_1._)`;${i2}--;`, () => gen.for((0, codegen_1._)`${j2} = ${i2}; ${j2}--;`, () => gen.if((0, codegen_1._)`${eql}(${data}[${i2}], ${data}[${j2}])`, () => {
            cxt.error();
            gen.assign(valid, false).break(outer);
          })));
        }
      }
    };
    exports.default = def;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/validation/const.js
var require_const = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/validation/const.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var codegen_1 = require_codegen();
    var util_1 = require_util();
    var equal_1 = require_equal();
    var error = {
      message: "must be equal to constant",
      params: ({ schemaCode }) => (0, codegen_1._)`{allowedValue: ${schemaCode}}`
    };
    var def = {
      keyword: "const",
      $data: true,
      error,
      code(cxt) {
        const { gen, data, $data, schemaCode, schema: schema4 } = cxt;
        if ($data || schema4 && typeof schema4 == "object") {
          cxt.fail$data((0, codegen_1._)`!${(0, util_1.useFunc)(gen, equal_1.default)}(${data}, ${schemaCode})`);
        } else {
          cxt.fail((0, codegen_1._)`${schema4} !== ${data}`);
        }
      }
    };
    exports.default = def;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/validation/enum.js
var require_enum = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/validation/enum.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var codegen_1 = require_codegen();
    var util_1 = require_util();
    var equal_1 = require_equal();
    var error = {
      message: "must be equal to one of the allowed values",
      params: ({ schemaCode }) => (0, codegen_1._)`{allowedValues: ${schemaCode}}`
    };
    var def = {
      keyword: "enum",
      schemaType: "array",
      $data: true,
      error,
      code(cxt) {
        const { gen, data, $data, schema: schema4, schemaCode, it: it2 } = cxt;
        if (!$data && schema4.length === 0)
          throw new Error("enum must have non-empty array");
        const useLoop = schema4.length >= it2.opts.loopEnum;
        let eql;
        const getEql = () => eql !== null && eql !== void 0 ? eql : eql = (0, util_1.useFunc)(gen, equal_1.default);
        let valid;
        if (useLoop || $data) {
          valid = gen.let("valid");
          cxt.block$data(valid, loopEnum);
        } else {
          if (!Array.isArray(schema4))
            throw new Error("ajv implementation error");
          const vSchema = gen.const("vSchema", schemaCode);
          valid = (0, codegen_1.or)(...schema4.map((_x, i2) => equalCode(vSchema, i2)));
        }
        cxt.pass(valid);
        function loopEnum() {
          gen.assign(valid, false);
          gen.forOf("v", schemaCode, (v2) => gen.if((0, codegen_1._)`${getEql()}(${data}, ${v2})`, () => gen.assign(valid, true).break()));
        }
        function equalCode(vSchema, i2) {
          const sch = schema4[i2];
          return typeof sch === "object" && sch !== null ? (0, codegen_1._)`${getEql()}(${data}, ${vSchema}[${i2}])` : (0, codegen_1._)`${data} === ${sch}`;
        }
      }
    };
    exports.default = def;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/validation/index.js
var require_validation = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/validation/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var limitNumber_1 = require_limitNumber();
    var multipleOf_1 = require_multipleOf();
    var limitLength_1 = require_limitLength();
    var pattern_1 = require_pattern();
    var limitProperties_1 = require_limitProperties();
    var required_1 = require_required();
    var limitItems_1 = require_limitItems();
    var uniqueItems_1 = require_uniqueItems();
    var const_1 = require_const();
    var enum_1 = require_enum();
    var validation = [
      // number
      limitNumber_1.default,
      multipleOf_1.default,
      // string
      limitLength_1.default,
      pattern_1.default,
      // object
      limitProperties_1.default,
      required_1.default,
      // array
      limitItems_1.default,
      uniqueItems_1.default,
      // any
      { keyword: "type", schemaType: ["string", "array"] },
      { keyword: "nullable", schemaType: "boolean" },
      const_1.default,
      enum_1.default
    ];
    exports.default = validation;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/applicator/additionalItems.js
var require_additionalItems = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/applicator/additionalItems.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.validateAdditionalItems = validateAdditionalItems;
    var codegen_1 = require_codegen();
    var util_1 = require_util();
    var error = {
      message: ({ params: { len } }) => (0, codegen_1.str)`must NOT have more than ${len} items`,
      params: ({ params: { len } }) => (0, codegen_1._)`{limit: ${len}}`
    };
    var def = {
      keyword: "additionalItems",
      type: "array",
      schemaType: ["boolean", "object"],
      before: "uniqueItems",
      error,
      code(cxt) {
        const { parentSchema, it: it2 } = cxt;
        const { items } = parentSchema;
        if (!Array.isArray(items)) {
          (0, util_1.checkStrictMode)(it2, '"additionalItems" is ignored when "items" is not an array of schemas');
          return;
        }
        validateAdditionalItems(cxt, items);
      }
    };
    function validateAdditionalItems(cxt, items) {
      const { gen, schema: schema4, data, keyword, it: it2 } = cxt;
      it2.items = true;
      const len = gen.const("len", (0, codegen_1._)`${data}.length`);
      if (schema4 === false) {
        cxt.setParams({ len: items.length });
        cxt.pass((0, codegen_1._)`${len} <= ${items.length}`);
      } else if (typeof schema4 == "object" && !(0, util_1.alwaysValidSchema)(it2, schema4)) {
        const valid = gen.var("valid", (0, codegen_1._)`${len} <= ${items.length}`);
        gen.if((0, codegen_1.not)(valid), () => validateItems(valid));
        cxt.ok(valid);
      }
      function validateItems(valid) {
        gen.forRange("i", items.length, len, (i2) => {
          cxt.subschema({ keyword, dataProp: i2, dataPropType: util_1.Type.Num }, valid);
          if (!it2.allErrors)
            gen.if((0, codegen_1.not)(valid), () => gen.break());
        });
      }
    }
    exports.default = def;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/applicator/items.js
var require_items = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/applicator/items.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.validateTuple = validateTuple;
    var codegen_1 = require_codegen();
    var util_1 = require_util();
    var code_1 = require_code2();
    var def = {
      keyword: "items",
      type: "array",
      schemaType: ["object", "array", "boolean"],
      before: "uniqueItems",
      code(cxt) {
        const { schema: schema4, it: it2 } = cxt;
        if (Array.isArray(schema4))
          return validateTuple(cxt, "additionalItems", schema4);
        it2.items = true;
        if ((0, util_1.alwaysValidSchema)(it2, schema4))
          return;
        cxt.ok((0, code_1.validateArray)(cxt));
      }
    };
    function validateTuple(cxt, extraItems, schArr = cxt.schema) {
      const { gen, parentSchema, data, keyword, it: it2 } = cxt;
      checkStrictTuple(parentSchema);
      if (it2.opts.unevaluated && schArr.length && it2.items !== true) {
        it2.items = util_1.mergeEvaluated.items(gen, schArr.length, it2.items);
      }
      const valid = gen.name("valid");
      const len = gen.const("len", (0, codegen_1._)`${data}.length`);
      schArr.forEach((sch, i2) => {
        if ((0, util_1.alwaysValidSchema)(it2, sch))
          return;
        gen.if((0, codegen_1._)`${len} > ${i2}`, () => cxt.subschema({
          keyword,
          schemaProp: i2,
          dataProp: i2
        }, valid));
        cxt.ok(valid);
      });
      function checkStrictTuple(sch) {
        const { opts, errSchemaPath } = it2;
        const l2 = schArr.length;
        const fullTuple = l2 === sch.minItems && (l2 === sch.maxItems || sch[extraItems] === false);
        if (opts.strictTuples && !fullTuple) {
          const msg = `"${keyword}" is ${l2}-tuple, but minItems or maxItems/${extraItems} are not specified or different at path "${errSchemaPath}"`;
          (0, util_1.checkStrictMode)(it2, msg, opts.strictTuples);
        }
      }
    }
    exports.default = def;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/applicator/prefixItems.js
var require_prefixItems = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/applicator/prefixItems.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var items_1 = require_items();
    var def = {
      keyword: "prefixItems",
      type: "array",
      schemaType: ["array"],
      before: "uniqueItems",
      code: (cxt) => (0, items_1.validateTuple)(cxt, "items")
    };
    exports.default = def;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/applicator/items2020.js
var require_items2020 = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/applicator/items2020.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var codegen_1 = require_codegen();
    var util_1 = require_util();
    var code_1 = require_code2();
    var additionalItems_1 = require_additionalItems();
    var error = {
      message: ({ params: { len } }) => (0, codegen_1.str)`must NOT have more than ${len} items`,
      params: ({ params: { len } }) => (0, codegen_1._)`{limit: ${len}}`
    };
    var def = {
      keyword: "items",
      type: "array",
      schemaType: ["object", "boolean"],
      before: "uniqueItems",
      error,
      code(cxt) {
        const { schema: schema4, parentSchema, it: it2 } = cxt;
        const { prefixItems } = parentSchema;
        it2.items = true;
        if ((0, util_1.alwaysValidSchema)(it2, schema4))
          return;
        if (prefixItems)
          (0, additionalItems_1.validateAdditionalItems)(cxt, prefixItems);
        else
          cxt.ok((0, code_1.validateArray)(cxt));
      }
    };
    exports.default = def;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/applicator/contains.js
var require_contains = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/applicator/contains.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var codegen_1 = require_codegen();
    var util_1 = require_util();
    var error = {
      message: ({ params: { min: min2, max: max2 } }) => max2 === void 0 ? (0, codegen_1.str)`must contain at least ${min2} valid item(s)` : (0, codegen_1.str)`must contain at least ${min2} and no more than ${max2} valid item(s)`,
      params: ({ params: { min: min2, max: max2 } }) => max2 === void 0 ? (0, codegen_1._)`{minContains: ${min2}}` : (0, codegen_1._)`{minContains: ${min2}, maxContains: ${max2}}`
    };
    var def = {
      keyword: "contains",
      type: "array",
      schemaType: ["object", "boolean"],
      before: "uniqueItems",
      trackErrors: true,
      error,
      code(cxt) {
        const { gen, schema: schema4, parentSchema, data, it: it2 } = cxt;
        let min2;
        let max2;
        const { minContains, maxContains } = parentSchema;
        if (it2.opts.next) {
          min2 = minContains === void 0 ? 1 : minContains;
          max2 = maxContains;
        } else {
          min2 = 1;
        }
        const len = gen.const("len", (0, codegen_1._)`${data}.length`);
        cxt.setParams({ min: min2, max: max2 });
        if (max2 === void 0 && min2 === 0) {
          (0, util_1.checkStrictMode)(it2, `"minContains" == 0 without "maxContains": "contains" keyword ignored`);
          return;
        }
        if (max2 !== void 0 && min2 > max2) {
          (0, util_1.checkStrictMode)(it2, `"minContains" > "maxContains" is always invalid`);
          cxt.fail();
          return;
        }
        if ((0, util_1.alwaysValidSchema)(it2, schema4)) {
          let cond = (0, codegen_1._)`${len} >= ${min2}`;
          if (max2 !== void 0)
            cond = (0, codegen_1._)`${cond} && ${len} <= ${max2}`;
          cxt.pass(cond);
          return;
        }
        it2.items = true;
        const valid = gen.name("valid");
        if (max2 === void 0 && min2 === 1) {
          validateItems(valid, () => gen.if(valid, () => gen.break()));
        } else if (min2 === 0) {
          gen.let(valid, true);
          if (max2 !== void 0)
            gen.if((0, codegen_1._)`${data}.length > 0`, validateItemsWithCount);
        } else {
          gen.let(valid, false);
          validateItemsWithCount();
        }
        cxt.result(valid, () => cxt.reset());
        function validateItemsWithCount() {
          const schValid = gen.name("_valid");
          const count = gen.let("count", 0);
          validateItems(schValid, () => gen.if(schValid, () => checkLimits(count)));
        }
        function validateItems(_valid, block) {
          gen.forRange("i", 0, len, (i2) => {
            cxt.subschema({
              keyword: "contains",
              dataProp: i2,
              dataPropType: util_1.Type.Num,
              compositeRule: true
            }, _valid);
            block();
          });
        }
        function checkLimits(count) {
          gen.code((0, codegen_1._)`${count}++`);
          if (max2 === void 0) {
            gen.if((0, codegen_1._)`${count} >= ${min2}`, () => gen.assign(valid, true).break());
          } else {
            gen.if((0, codegen_1._)`${count} > ${max2}`, () => gen.assign(valid, false).break());
            if (min2 === 1)
              gen.assign(valid, true);
            else
              gen.if((0, codegen_1._)`${count} >= ${min2}`, () => gen.assign(valid, true));
          }
        }
      }
    };
    exports.default = def;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/applicator/dependencies.js
var require_dependencies = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/applicator/dependencies.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.error = void 0;
    exports.validatePropertyDeps = validatePropertyDeps;
    exports.validateSchemaDeps = validateSchemaDeps;
    var codegen_1 = require_codegen();
    var util_1 = require_util();
    var code_1 = require_code2();
    exports.error = {
      message: ({ params: { property, depsCount, deps } }) => {
        const property_ies = depsCount === 1 ? "property" : "properties";
        return (0, codegen_1.str)`must have ${property_ies} ${deps} when property ${property} is present`;
      },
      params: ({ params: { property, depsCount, deps, missingProperty } }) => (0, codegen_1._)`{property: ${property},
    missingProperty: ${missingProperty},
    depsCount: ${depsCount},
    deps: ${deps}}`
      // TODO change to reference
    };
    var def = {
      keyword: "dependencies",
      type: "object",
      schemaType: "object",
      error: exports.error,
      code(cxt) {
        const [propDeps, schDeps] = splitDependencies(cxt);
        validatePropertyDeps(cxt, propDeps);
        validateSchemaDeps(cxt, schDeps);
      }
    };
    function splitDependencies({ schema: schema4 }) {
      const propertyDeps = {};
      const schemaDeps = {};
      for (const key2 in schema4) {
        if (key2 === "__proto__")
          continue;
        const deps = Array.isArray(schema4[key2]) ? propertyDeps : schemaDeps;
        deps[key2] = schema4[key2];
      }
      return [propertyDeps, schemaDeps];
    }
    function validatePropertyDeps(cxt, propertyDeps = cxt.schema) {
      const { gen, data, it: it2 } = cxt;
      if (Object.keys(propertyDeps).length === 0)
        return;
      const missing = gen.let("missing");
      for (const prop in propertyDeps) {
        const deps = propertyDeps[prop];
        if (deps.length === 0)
          continue;
        const hasProperty = (0, code_1.propertyInData)(gen, data, prop, it2.opts.ownProperties);
        cxt.setParams({
          property: prop,
          depsCount: deps.length,
          deps: deps.join(", ")
        });
        if (it2.allErrors) {
          gen.if(hasProperty, () => {
            for (const depProp of deps) {
              (0, code_1.checkReportMissingProp)(cxt, depProp);
            }
          });
        } else {
          gen.if((0, codegen_1._)`${hasProperty} && (${(0, code_1.checkMissingProp)(cxt, deps, missing)})`);
          (0, code_1.reportMissingProp)(cxt, missing);
          gen.else();
        }
      }
    }
    function validateSchemaDeps(cxt, schemaDeps = cxt.schema) {
      const { gen, data, keyword, it: it2 } = cxt;
      const valid = gen.name("valid");
      for (const prop in schemaDeps) {
        if ((0, util_1.alwaysValidSchema)(it2, schemaDeps[prop]))
          continue;
        gen.if(
          (0, code_1.propertyInData)(gen, data, prop, it2.opts.ownProperties),
          () => {
            const schCxt = cxt.subschema({ keyword, schemaProp: prop }, valid);
            cxt.mergeValidEvaluated(schCxt, valid);
          },
          () => gen.var(valid, true)
          // TODO var
        );
        cxt.ok(valid);
      }
    }
    exports.default = def;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/applicator/propertyNames.js
var require_propertyNames = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/applicator/propertyNames.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var codegen_1 = require_codegen();
    var util_1 = require_util();
    var error = {
      message: "property name must be valid",
      params: ({ params }) => (0, codegen_1._)`{propertyName: ${params.propertyName}}`
    };
    var def = {
      keyword: "propertyNames",
      type: "object",
      schemaType: ["object", "boolean"],
      error,
      code(cxt) {
        const { gen, schema: schema4, data, it: it2 } = cxt;
        if ((0, util_1.alwaysValidSchema)(it2, schema4))
          return;
        const valid = gen.name("valid");
        gen.forIn("key", data, (key2) => {
          cxt.setParams({ propertyName: key2 });
          cxt.subschema({
            keyword: "propertyNames",
            data: key2,
            dataTypes: ["string"],
            propertyName: key2,
            compositeRule: true
          }, valid);
          gen.if((0, codegen_1.not)(valid), () => {
            cxt.error(true);
            if (!it2.allErrors)
              gen.break();
          });
        });
        cxt.ok(valid);
      }
    };
    exports.default = def;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/applicator/additionalProperties.js
var require_additionalProperties = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/applicator/additionalProperties.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var code_1 = require_code2();
    var codegen_1 = require_codegen();
    var names_1 = require_names();
    var util_1 = require_util();
    var error = {
      message: "must NOT have additional properties",
      params: ({ params }) => (0, codegen_1._)`{additionalProperty: ${params.additionalProperty}}`
    };
    var def = {
      keyword: "additionalProperties",
      type: ["object"],
      schemaType: ["boolean", "object"],
      allowUndefined: true,
      trackErrors: true,
      error,
      code(cxt) {
        const { gen, parentSchema, data, errsCount, it: it2 } = cxt;
        const { schema: schema4 = it2.opts.defaultAdditionalProperties } = cxt;
        if (!errsCount)
          throw new Error("ajv implementation error");
        const { allErrors, opts } = it2;
        it2.props = true;
        if (opts.removeAdditional !== "all" && (0, util_1.alwaysValidSchema)(it2, schema4))
          return;
        const props = (0, code_1.allSchemaProperties)(parentSchema.properties);
        const patProps = (0, code_1.allSchemaProperties)(parentSchema.patternProperties);
        checkAdditionalProperties();
        cxt.ok((0, codegen_1._)`${errsCount} === ${names_1.default.errors}`);
        function checkAdditionalProperties() {
          gen.forIn("key", data, (key2) => {
            if (!props.length && !patProps.length)
              additionalPropertyCode(key2);
            else
              gen.if(isAdditional(key2), () => additionalPropertyCode(key2));
          });
        }
        function isAdditional(key2) {
          let definedProp;
          if (props.length > 8) {
            const propsSchema = (0, util_1.schemaRefOrVal)(it2, parentSchema.properties, "properties");
            definedProp = (0, code_1.isOwnProperty)(gen, propsSchema, key2);
          } else if (props.length) {
            definedProp = (0, codegen_1.or)(...props.map((p2) => (0, codegen_1._)`${key2} === ${p2}`));
          } else {
            definedProp = codegen_1.nil;
          }
          if (patProps.length) {
            definedProp = (0, codegen_1.or)(definedProp, ...patProps.map((p2) => (0, codegen_1._)`${(0, code_1.usePattern)(cxt, p2)}.test(${key2})`));
          }
          return (0, codegen_1.not)(definedProp);
        }
        function deleteAdditional(key2) {
          gen.code((0, codegen_1._)`delete ${data}[${key2}]`);
        }
        function additionalPropertyCode(key2) {
          if (opts.removeAdditional === "all" || opts.removeAdditional && schema4 === false) {
            deleteAdditional(key2);
            return;
          }
          if (schema4 === false) {
            cxt.setParams({ additionalProperty: key2 });
            cxt.error();
            if (!allErrors)
              gen.break();
            return;
          }
          if (typeof schema4 == "object" && !(0, util_1.alwaysValidSchema)(it2, schema4)) {
            const valid = gen.name("valid");
            if (opts.removeAdditional === "failing") {
              applyAdditionalSchema(key2, valid, false);
              gen.if((0, codegen_1.not)(valid), () => {
                cxt.reset();
                deleteAdditional(key2);
              });
            } else {
              applyAdditionalSchema(key2, valid);
              if (!allErrors)
                gen.if((0, codegen_1.not)(valid), () => gen.break());
            }
          }
        }
        function applyAdditionalSchema(key2, valid, errors2) {
          const subschema = {
            keyword: "additionalProperties",
            dataProp: key2,
            dataPropType: util_1.Type.Str
          };
          if (errors2 === false) {
            Object.assign(subschema, {
              compositeRule: true,
              createErrors: false,
              allErrors: false
            });
          }
          cxt.subschema(subschema, valid);
        }
      }
    };
    exports.default = def;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/applicator/properties.js
var require_properties = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/applicator/properties.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var validate_1 = require_validate();
    var code_1 = require_code2();
    var util_1 = require_util();
    var additionalProperties_1 = require_additionalProperties();
    var def = {
      keyword: "properties",
      type: "object",
      schemaType: "object",
      code(cxt) {
        const { gen, schema: schema4, parentSchema, data, it: it2 } = cxt;
        if (it2.opts.removeAdditional === "all" && parentSchema.additionalProperties === void 0 || it2.opts.defaultAdditionalProperties === false) {
          additionalProperties_1.default.code(new validate_1.KeywordCxt(it2, additionalProperties_1.default, "additionalProperties"));
        }
        const allProps = (0, code_1.allSchemaProperties)(schema4);
        for (const prop of allProps) {
          it2.definedProperties.add(prop);
        }
        if (it2.opts.unevaluated && allProps.length && it2.props !== true) {
          it2.props = util_1.mergeEvaluated.props(gen, (0, util_1.toHash)(allProps), it2.props);
        }
        const properties = allProps.filter((p2) => !(0, util_1.alwaysValidSchema)(it2, schema4[p2]));
        if (properties.length === 0)
          return;
        const valid = gen.name("valid");
        for (const prop of properties) {
          if (hasDefault(prop)) {
            applyPropertySchema(prop);
          } else {
            gen.if((0, code_1.propertyInData)(gen, data, prop, it2.opts.ownProperties));
            applyPropertySchema(prop);
            if (!it2.allErrors)
              gen.else().var(valid, true);
            gen.endIf();
          }
          cxt.it.definedProperties.add(prop);
          cxt.ok(valid);
        }
        function hasDefault(prop) {
          return it2.opts.useDefaults && !it2.compositeRule && schema4[prop].default !== void 0;
        }
        function applyPropertySchema(prop) {
          cxt.subschema({
            keyword: "properties",
            schemaProp: prop,
            dataProp: prop
          }, valid);
        }
      }
    };
    exports.default = def;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/applicator/patternProperties.js
var require_patternProperties = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/applicator/patternProperties.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var code_1 = require_code2();
    var codegen_1 = require_codegen();
    var util_1 = require_util();
    var util_2 = require_util();
    var def = {
      keyword: "patternProperties",
      type: "object",
      schemaType: "object",
      code(cxt) {
        const { gen, schema: schema4, data, parentSchema, it: it2 } = cxt;
        const { opts } = it2;
        const patterns = (0, code_1.allSchemaProperties)(schema4);
        const alwaysValidPatterns = patterns.filter((p2) => (0, util_1.alwaysValidSchema)(it2, schema4[p2]));
        if (patterns.length === 0 || alwaysValidPatterns.length === patterns.length && (!it2.opts.unevaluated || it2.props === true)) {
          return;
        }
        const checkProperties = opts.strictSchema && !opts.allowMatchingProperties && parentSchema.properties;
        const valid = gen.name("valid");
        if (it2.props !== true && !(it2.props instanceof codegen_1.Name)) {
          it2.props = (0, util_2.evaluatedPropsToName)(gen, it2.props);
        }
        const { props } = it2;
        validatePatternProperties();
        function validatePatternProperties() {
          for (const pat of patterns) {
            if (checkProperties)
              checkMatchingProperties(pat);
            if (it2.allErrors) {
              validateProperties(pat);
            } else {
              gen.var(valid, true);
              validateProperties(pat);
              gen.if(valid);
            }
          }
        }
        function checkMatchingProperties(pat) {
          for (const prop in checkProperties) {
            if (new RegExp(pat).test(prop)) {
              (0, util_1.checkStrictMode)(it2, `property ${prop} matches pattern ${pat} (use allowMatchingProperties)`);
            }
          }
        }
        function validateProperties(pat) {
          gen.forIn("key", data, (key2) => {
            gen.if((0, codegen_1._)`${(0, code_1.usePattern)(cxt, pat)}.test(${key2})`, () => {
              const alwaysValid = alwaysValidPatterns.includes(pat);
              if (!alwaysValid) {
                cxt.subschema({
                  keyword: "patternProperties",
                  schemaProp: pat,
                  dataProp: key2,
                  dataPropType: util_2.Type.Str
                }, valid);
              }
              if (it2.opts.unevaluated && props !== true) {
                gen.assign((0, codegen_1._)`${props}[${key2}]`, true);
              } else if (!alwaysValid && !it2.allErrors) {
                gen.if((0, codegen_1.not)(valid), () => gen.break());
              }
            });
          });
        }
      }
    };
    exports.default = def;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/applicator/not.js
var require_not = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/applicator/not.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var util_1 = require_util();
    var def = {
      keyword: "not",
      schemaType: ["object", "boolean"],
      trackErrors: true,
      code(cxt) {
        const { gen, schema: schema4, it: it2 } = cxt;
        if ((0, util_1.alwaysValidSchema)(it2, schema4)) {
          cxt.fail();
          return;
        }
        const valid = gen.name("valid");
        cxt.subschema({
          keyword: "not",
          compositeRule: true,
          createErrors: false,
          allErrors: false
        }, valid);
        cxt.failResult(valid, () => cxt.reset(), () => cxt.error());
      },
      error: { message: "must NOT be valid" }
    };
    exports.default = def;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/applicator/anyOf.js
var require_anyOf = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/applicator/anyOf.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var code_1 = require_code2();
    var def = {
      keyword: "anyOf",
      schemaType: "array",
      trackErrors: true,
      code: code_1.validateUnion,
      error: { message: "must match a schema in anyOf" }
    };
    exports.default = def;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/applicator/oneOf.js
var require_oneOf = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/applicator/oneOf.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var codegen_1 = require_codegen();
    var util_1 = require_util();
    var error = {
      message: "must match exactly one schema in oneOf",
      params: ({ params }) => (0, codegen_1._)`{passingSchemas: ${params.passing}}`
    };
    var def = {
      keyword: "oneOf",
      schemaType: "array",
      trackErrors: true,
      error,
      code(cxt) {
        const { gen, schema: schema4, parentSchema, it: it2 } = cxt;
        if (!Array.isArray(schema4))
          throw new Error("ajv implementation error");
        if (it2.opts.discriminator && parentSchema.discriminator)
          return;
        const schArr = schema4;
        const valid = gen.let("valid", false);
        const passing = gen.let("passing", null);
        const schValid = gen.name("_valid");
        cxt.setParams({ passing });
        gen.block(validateOneOf);
        cxt.result(valid, () => cxt.reset(), () => cxt.error(true));
        function validateOneOf() {
          schArr.forEach((sch, i2) => {
            let schCxt;
            if ((0, util_1.alwaysValidSchema)(it2, sch)) {
              gen.var(schValid, true);
            } else {
              schCxt = cxt.subschema({
                keyword: "oneOf",
                schemaProp: i2,
                compositeRule: true
              }, schValid);
            }
            if (i2 > 0) {
              gen.if((0, codegen_1._)`${schValid} && ${valid}`).assign(valid, false).assign(passing, (0, codegen_1._)`[${passing}, ${i2}]`).else();
            }
            gen.if(schValid, () => {
              gen.assign(valid, true);
              gen.assign(passing, i2);
              if (schCxt)
                cxt.mergeEvaluated(schCxt, codegen_1.Name);
            });
          });
        }
      }
    };
    exports.default = def;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/applicator/allOf.js
var require_allOf = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/applicator/allOf.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var util_1 = require_util();
    var def = {
      keyword: "allOf",
      schemaType: "array",
      code(cxt) {
        const { gen, schema: schema4, it: it2 } = cxt;
        if (!Array.isArray(schema4))
          throw new Error("ajv implementation error");
        const valid = gen.name("valid");
        schema4.forEach((sch, i2) => {
          if ((0, util_1.alwaysValidSchema)(it2, sch))
            return;
          const schCxt = cxt.subschema({ keyword: "allOf", schemaProp: i2 }, valid, true);
          cxt.ok(valid);
          cxt.mergeEvaluated(schCxt);
        });
      }
    };
    exports.default = def;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/applicator/if.js
var require_if = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/applicator/if.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var codegen_1 = require_codegen();
    var util_1 = require_util();
    var error = {
      message: ({ params }) => (0, codegen_1.str)`must match "${params.ifClause}" schema`,
      params: ({ params }) => (0, codegen_1._)`{failingKeyword: ${params.ifClause}}`
    };
    var def = {
      keyword: "if",
      schemaType: ["object", "boolean"],
      trackErrors: true,
      error,
      code(cxt) {
        const { gen, parentSchema, it: it2 } = cxt;
        if (parentSchema.then === void 0 && parentSchema.else === void 0) {
          (0, util_1.checkStrictMode)(it2, '"if" without "then" and "else" is ignored');
        }
        const hasThen = hasSchema(it2, "then");
        const hasElse = hasSchema(it2, "else");
        if (!hasThen && !hasElse)
          return;
        const valid = gen.let("valid", true);
        const schValid = gen.name("_valid");
        validateIf();
        cxt.reset();
        if (hasThen && hasElse) {
          const ifClause = gen.let("ifClause");
          cxt.setParams({ ifClause });
          gen.if(schValid, validateClause("then", ifClause), validateClause("else", ifClause));
        } else if (hasThen) {
          gen.if(schValid, validateClause("then"));
        } else {
          gen.if((0, codegen_1.not)(schValid), validateClause("else"));
        }
        cxt.pass(valid, () => cxt.error(true));
        function validateIf() {
          const schCxt = cxt.subschema({
            keyword: "if",
            compositeRule: true,
            createErrors: false,
            allErrors: false
          }, schValid);
          cxt.mergeEvaluated(schCxt);
        }
        function validateClause(keyword, ifClause) {
          return () => {
            const schCxt = cxt.subschema({ keyword }, schValid);
            gen.assign(valid, schValid);
            cxt.mergeValidEvaluated(schCxt, valid);
            if (ifClause)
              gen.assign(ifClause, (0, codegen_1._)`${keyword}`);
            else
              cxt.setParams({ ifClause: keyword });
          };
        }
      }
    };
    function hasSchema(it2, keyword) {
      const schema4 = it2.schema[keyword];
      return schema4 !== void 0 && !(0, util_1.alwaysValidSchema)(it2, schema4);
    }
    exports.default = def;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/applicator/thenElse.js
var require_thenElse = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/applicator/thenElse.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var util_1 = require_util();
    var def = {
      keyword: ["then", "else"],
      schemaType: ["object", "boolean"],
      code({ keyword, parentSchema, it: it2 }) {
        if (parentSchema.if === void 0)
          (0, util_1.checkStrictMode)(it2, `"${keyword}" without "if" is ignored`);
      }
    };
    exports.default = def;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/applicator/index.js
var require_applicator = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/applicator/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.default = getApplicator;
    var additionalItems_1 = require_additionalItems();
    var prefixItems_1 = require_prefixItems();
    var items_1 = require_items();
    var items2020_1 = require_items2020();
    var contains_1 = require_contains();
    var dependencies_1 = require_dependencies();
    var propertyNames_1 = require_propertyNames();
    var additionalProperties_1 = require_additionalProperties();
    var properties_1 = require_properties();
    var patternProperties_1 = require_patternProperties();
    var not_1 = require_not();
    var anyOf_1 = require_anyOf();
    var oneOf_1 = require_oneOf();
    var allOf_1 = require_allOf();
    var if_1 = require_if();
    var thenElse_1 = require_thenElse();
    function getApplicator(draft2020 = false) {
      const applicator = [
        // any
        not_1.default,
        anyOf_1.default,
        oneOf_1.default,
        allOf_1.default,
        if_1.default,
        thenElse_1.default,
        // object
        propertyNames_1.default,
        additionalProperties_1.default,
        dependencies_1.default,
        properties_1.default,
        patternProperties_1.default
      ];
      if (draft2020)
        applicator.push(prefixItems_1.default, items2020_1.default);
      else
        applicator.push(additionalItems_1.default, items_1.default);
      applicator.push(contains_1.default);
      return applicator;
    }
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/dynamic/dynamicAnchor.js
var require_dynamicAnchor = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/dynamic/dynamicAnchor.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.dynamicAnchor = dynamicAnchor;
    var codegen_1 = require_codegen();
    var names_1 = require_names();
    var compile_1 = require_compile();
    var ref_1 = require_ref();
    var def = {
      keyword: "$dynamicAnchor",
      schemaType: "string",
      code: (cxt) => dynamicAnchor(cxt, cxt.schema)
    };
    function dynamicAnchor(cxt, anchor) {
      const { gen, it: it2 } = cxt;
      it2.schemaEnv.root.dynamicAnchors[anchor] = true;
      const v2 = (0, codegen_1._)`${names_1.default.dynamicAnchors}${(0, codegen_1.getProperty)(anchor)}`;
      const validate = it2.errSchemaPath === "#" ? it2.validateName : _getValidate(cxt);
      gen.if((0, codegen_1._)`!${v2}`, () => gen.assign(v2, validate));
    }
    function _getValidate(cxt) {
      const { schemaEnv, schema: schema4, self: self2 } = cxt.it;
      const { root, baseId, localRefs, meta } = schemaEnv.root;
      const { schemaId } = self2.opts;
      const sch = new compile_1.SchemaEnv({ schema: schema4, schemaId, root, baseId, localRefs, meta });
      compile_1.compileSchema.call(self2, sch);
      return (0, ref_1.getValidate)(cxt, sch);
    }
    exports.default = def;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/dynamic/dynamicRef.js
var require_dynamicRef = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/dynamic/dynamicRef.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.dynamicRef = dynamicRef;
    var codegen_1 = require_codegen();
    var names_1 = require_names();
    var ref_1 = require_ref();
    var def = {
      keyword: "$dynamicRef",
      schemaType: "string",
      code: (cxt) => dynamicRef(cxt, cxt.schema)
    };
    function dynamicRef(cxt, ref) {
      const { gen, keyword, it: it2 } = cxt;
      if (ref[0] !== "#")
        throw new Error(`"${keyword}" only supports hash fragment reference`);
      const anchor = ref.slice(1);
      if (it2.allErrors) {
        _dynamicRef();
      } else {
        const valid = gen.let("valid", false);
        _dynamicRef(valid);
        cxt.ok(valid);
      }
      function _dynamicRef(valid) {
        if (it2.schemaEnv.root.dynamicAnchors[anchor]) {
          const v2 = gen.let("_v", (0, codegen_1._)`${names_1.default.dynamicAnchors}${(0, codegen_1.getProperty)(anchor)}`);
          gen.if(v2, _callRef(v2, valid), _callRef(it2.validateName, valid));
        } else {
          _callRef(it2.validateName, valid)();
        }
      }
      function _callRef(validate, valid) {
        return valid ? () => gen.block(() => {
          (0, ref_1.callRef)(cxt, validate);
          gen.let(valid, true);
        }) : () => (0, ref_1.callRef)(cxt, validate);
      }
    }
    exports.default = def;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/dynamic/recursiveAnchor.js
var require_recursiveAnchor = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/dynamic/recursiveAnchor.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var dynamicAnchor_1 = require_dynamicAnchor();
    var util_1 = require_util();
    var def = {
      keyword: "$recursiveAnchor",
      schemaType: "boolean",
      code(cxt) {
        if (cxt.schema)
          (0, dynamicAnchor_1.dynamicAnchor)(cxt, "");
        else
          (0, util_1.checkStrictMode)(cxt.it, "$recursiveAnchor: false is ignored");
      }
    };
    exports.default = def;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/dynamic/recursiveRef.js
var require_recursiveRef = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/dynamic/recursiveRef.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var dynamicRef_1 = require_dynamicRef();
    var def = {
      keyword: "$recursiveRef",
      schemaType: "string",
      code: (cxt) => (0, dynamicRef_1.dynamicRef)(cxt, cxt.schema)
    };
    exports.default = def;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/dynamic/index.js
var require_dynamic = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/dynamic/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var dynamicAnchor_1 = require_dynamicAnchor();
    var dynamicRef_1 = require_dynamicRef();
    var recursiveAnchor_1 = require_recursiveAnchor();
    var recursiveRef_1 = require_recursiveRef();
    var dynamic = [dynamicAnchor_1.default, dynamicRef_1.default, recursiveAnchor_1.default, recursiveRef_1.default];
    exports.default = dynamic;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/validation/dependentRequired.js
var require_dependentRequired = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/validation/dependentRequired.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var dependencies_1 = require_dependencies();
    var def = {
      keyword: "dependentRequired",
      type: "object",
      schemaType: "object",
      error: dependencies_1.error,
      code: (cxt) => (0, dependencies_1.validatePropertyDeps)(cxt)
    };
    exports.default = def;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/applicator/dependentSchemas.js
var require_dependentSchemas = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/applicator/dependentSchemas.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var dependencies_1 = require_dependencies();
    var def = {
      keyword: "dependentSchemas",
      type: "object",
      schemaType: "object",
      code: (cxt) => (0, dependencies_1.validateSchemaDeps)(cxt)
    };
    exports.default = def;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/validation/limitContains.js
var require_limitContains = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/validation/limitContains.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var util_1 = require_util();
    var def = {
      keyword: ["maxContains", "minContains"],
      type: "array",
      schemaType: "number",
      code({ keyword, parentSchema, it: it2 }) {
        if (parentSchema.contains === void 0) {
          (0, util_1.checkStrictMode)(it2, `"${keyword}" without "contains" is ignored`);
        }
      }
    };
    exports.default = def;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/next.js
var require_next2 = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/next.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var dependentRequired_1 = require_dependentRequired();
    var dependentSchemas_1 = require_dependentSchemas();
    var limitContains_1 = require_limitContains();
    var next2 = [dependentRequired_1.default, dependentSchemas_1.default, limitContains_1.default];
    exports.default = next2;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/unevaluated/unevaluatedProperties.js
var require_unevaluatedProperties = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/unevaluated/unevaluatedProperties.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var codegen_1 = require_codegen();
    var util_1 = require_util();
    var names_1 = require_names();
    var error = {
      message: "must NOT have unevaluated properties",
      params: ({ params }) => (0, codegen_1._)`{unevaluatedProperty: ${params.unevaluatedProperty}}`
    };
    var def = {
      keyword: "unevaluatedProperties",
      type: "object",
      schemaType: ["boolean", "object"],
      allowUndefined: true,
      trackErrors: true,
      error,
      code(cxt) {
        const { gen, schema: schema4 = cxt.it.opts.defaultUnevaluatedProperties, data, errsCount, it: it2 } = cxt;
        const isForced = cxt.schema === void 0 && cxt.it.opts.defaultUnevaluatedProperties === false;
        if (!errsCount)
          throw new Error("ajv implementation error");
        const { allErrors, props } = it2;
        if (props instanceof codegen_1.Name) {
          gen.if((0, codegen_1._)`${props} !== true`, () => gen.forIn("key", data, (key2) => gen.if(unevaluatedDynamic(props, key2), () => unevaluatedPropCode(key2))));
        } else if (props !== true) {
          const staticCheck = () => gen.forIn("key", data, (key2) => props === void 0 ? unevaluatedPropCode(key2) : gen.if(unevaluatedStatic(props, key2), () => unevaluatedPropCode(key2)));
          if (isForced && it2.errorPath.emptyStr()) {
            gen.if((0, codegen_1._)`${names_1.default.isAllOfVariant} === 0`, staticCheck);
          } else {
            staticCheck();
          }
        }
        if (!isForced) {
          it2.props = true;
        }
        cxt.ok((0, codegen_1._)`${errsCount} === ${names_1.default.errors}`);
        function unevaluatedPropCode(key2) {
          if (schema4 === false) {
            cxt.setParams({ unevaluatedProperty: key2 });
            cxt.error();
            if (!allErrors)
              gen.break();
            return;
          }
          if (!(0, util_1.alwaysValidSchema)(it2, schema4)) {
            const valid = gen.name("valid");
            cxt.subschema({
              keyword: "unevaluatedProperties",
              dataProp: key2,
              dataPropType: util_1.Type.Str
            }, valid);
            if (!allErrors)
              gen.if((0, codegen_1.not)(valid), () => gen.break());
          }
        }
        function unevaluatedDynamic(evaluatedProps, key2) {
          return (0, codegen_1._)`!${evaluatedProps} || !${evaluatedProps}[${key2}]`;
        }
        function unevaluatedStatic(evaluatedProps, key2) {
          const ps = [];
          for (const p2 in evaluatedProps) {
            if (evaluatedProps[p2] === true)
              ps.push((0, codegen_1._)`${key2} !== ${p2}`);
          }
          return (0, codegen_1.and)(...ps);
        }
      }
    };
    exports.default = def;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/unevaluated/unevaluatedItems.js
var require_unevaluatedItems = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/unevaluated/unevaluatedItems.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var codegen_1 = require_codegen();
    var util_1 = require_util();
    var error = {
      message: ({ params: { len } }) => (0, codegen_1.str)`must NOT have more than ${len} items`,
      params: ({ params: { len } }) => (0, codegen_1._)`{limit: ${len}}`
    };
    var def = {
      keyword: "unevaluatedItems",
      type: "array",
      schemaType: ["boolean", "object"],
      error,
      code(cxt) {
        const { gen, schema: schema4, data, it: it2 } = cxt;
        const items = it2.items || 0;
        if (items === true)
          return;
        const len = gen.const("len", (0, codegen_1._)`${data}.length`);
        if (schema4 === false) {
          cxt.setParams({ len: items });
          cxt.fail((0, codegen_1._)`${len} > ${items}`);
        } else if (typeof schema4 == "object" && !(0, util_1.alwaysValidSchema)(it2, schema4)) {
          const valid = gen.var("valid", (0, codegen_1._)`${len} <= ${items}`);
          gen.if((0, codegen_1.not)(valid), () => validateItems(valid, items));
          cxt.ok(valid);
        }
        it2.items = true;
        function validateItems(valid, from2) {
          gen.forRange("i", from2, len, (i2) => {
            cxt.subschema({ keyword: "unevaluatedItems", dataProp: i2, dataPropType: util_1.Type.Num }, valid);
            if (!it2.allErrors)
              gen.if((0, codegen_1.not)(valid), () => gen.break());
          });
        }
      }
    };
    exports.default = def;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/unevaluated/index.js
var require_unevaluated = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/unevaluated/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var unevaluatedProperties_1 = require_unevaluatedProperties();
    var unevaluatedItems_1 = require_unevaluatedItems();
    var unevaluated = [unevaluatedProperties_1.default, unevaluatedItems_1.default];
    exports.default = unevaluated;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/format/format.js
var require_format = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/format/format.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var codegen_1 = require_codegen();
    var error = {
      message: ({ schemaCode }) => (0, codegen_1.str)`must match format "${schemaCode}"`,
      params: ({ schemaCode }) => (0, codegen_1._)`{format: ${schemaCode}}`
    };
    var def = {
      keyword: "format",
      type: ["number", "string"],
      schemaType: "string",
      $data: true,
      error,
      code(cxt, ruleType) {
        const { gen, data, $data, schema: schema4, schemaCode, it: it2 } = cxt;
        const { opts, errSchemaPath, schemaEnv, self: self2 } = it2;
        if (!opts.validateFormats)
          return;
        if ($data)
          validate$DataFormat();
        else
          validateFormat();
        function validate$DataFormat() {
          const fmts = gen.scopeValue("formats", {
            ref: self2.formats,
            code: opts.code.formats
          });
          const fDef = gen.const("fDef", (0, codegen_1._)`${fmts}[${schemaCode}]`);
          const fType = gen.let("fType");
          const format2 = gen.let("format");
          gen.if((0, codegen_1._)`typeof ${fDef} == "object" && !(${fDef} instanceof RegExp)`, () => gen.assign(fType, (0, codegen_1._)`${fDef}.type || "string"`).assign(format2, (0, codegen_1._)`${fDef}.validate`), () => gen.assign(fType, (0, codegen_1._)`"string"`).assign(format2, fDef));
          cxt.fail$data((0, codegen_1.or)(unknownFmt(), invalidFmt()));
          function unknownFmt() {
            if (opts.strictSchema === false)
              return codegen_1.nil;
            return (0, codegen_1._)`${schemaCode} && !${format2}`;
          }
          function invalidFmt() {
            const callFormat = schemaEnv.$async ? (0, codegen_1._)`(${fDef}.async ? await ${format2}(${data}) : ${format2}(${data}))` : (0, codegen_1._)`${format2}(${data})`;
            const validData = (0, codegen_1._)`(typeof ${format2} == "function" ? ${callFormat} : ${format2}.test(${data}))`;
            return (0, codegen_1._)`${format2} && ${format2} !== true && ${fType} === ${ruleType} && !${validData}`;
          }
        }
        function validateFormat() {
          const formatDef = self2.formats[schema4];
          if (!formatDef) {
            unknownFormat();
            return;
          }
          if (formatDef === true)
            return;
          const [fmtType, format2, fmtRef] = getFormat(formatDef);
          if (fmtType === ruleType)
            cxt.pass(validCondition());
          function unknownFormat() {
            if (opts.strictSchema === false) {
              self2.logger.warn(unknownMsg());
              return;
            }
            throw new Error(unknownMsg());
            function unknownMsg() {
              return `unknown format "${schema4}" ignored in schema at path "${errSchemaPath}"`;
            }
          }
          function getFormat(fmtDef) {
            const code = fmtDef instanceof RegExp ? (0, codegen_1.regexpCode)(fmtDef) : opts.code.formats ? (0, codegen_1._)`${opts.code.formats}${(0, codegen_1.getProperty)(schema4)}` : void 0;
            const fmt = gen.scopeValue("formats", { key: schema4, ref: fmtDef, code });
            if (typeof fmtDef == "object" && !(fmtDef instanceof RegExp)) {
              return [fmtDef.type || "string", fmtDef.validate, (0, codegen_1._)`${fmt}.validate`];
            }
            return ["string", fmtDef, fmt];
          }
          function validCondition() {
            if (typeof formatDef == "object" && !(formatDef instanceof RegExp) && formatDef.async) {
              if (!schemaEnv.$async)
                throw new Error("async format in sync schema");
              return (0, codegen_1._)`await ${fmtRef}(${data})`;
            }
            return typeof format2 == "function" ? (0, codegen_1._)`${fmtRef}(${data})` : (0, codegen_1._)`${fmtRef}.test(${data})`;
          }
        }
      }
    };
    exports.default = def;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/format/index.js
var require_format2 = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/format/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var format_1 = require_format();
    var format2 = [format_1.default];
    exports.default = format2;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/metadata.js
var require_metadata = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/metadata.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.contentVocabulary = exports.metadataVocabulary = void 0;
    exports.metadataVocabulary = [
      "title",
      "description",
      "default",
      "deprecated",
      "readOnly",
      "writeOnly",
      "examples"
    ];
    exports.contentVocabulary = [
      "contentMediaType",
      "contentEncoding",
      "contentSchema"
    ];
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/draft2020.js
var require_draft2020 = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/draft2020.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var core_1 = require_core3();
    var validation_1 = require_validation();
    var applicator_1 = require_applicator();
    var dynamic_1 = require_dynamic();
    var next_1 = require_next2();
    var unevaluated_1 = require_unevaluated();
    var format_1 = require_format2();
    var metadata_1 = require_metadata();
    var draft2020Vocabularies = [
      dynamic_1.default,
      core_1.default,
      validation_1.default,
      (0, applicator_1.default)(true),
      format_1.default,
      metadata_1.metadataVocabulary,
      metadata_1.contentVocabulary,
      next_1.default,
      unevaluated_1.default
    ];
    exports.default = draft2020Vocabularies;
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/discriminator/types.js
var require_types2 = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/discriminator/types.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.DiscrError = void 0;
    var DiscrError;
    (function(DiscrError2) {
      DiscrError2["Tag"] = "tag";
      DiscrError2["Mapping"] = "mapping";
    })(DiscrError || (exports.DiscrError = DiscrError = {}));
  }
});

// node_modules/@redocly/ajv/dist/vocabularies/discriminator/index.js
var require_discriminator = __commonJS({
  "node_modules/@redocly/ajv/dist/vocabularies/discriminator/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    var codegen_1 = require_codegen();
    var types_1 = require_types2();
    var compile_1 = require_compile();
    var util_1 = require_util();
    var error = {
      message: ({ params: { discrError, tagName } }) => discrError === types_1.DiscrError.Tag ? `tag "${tagName}" must be string` : `value of tag "${tagName}" must be in oneOf or anyOf`,
      params: ({ params: { discrError, tag, tagName } }) => (0, codegen_1._)`{error: ${discrError}, tag: ${tagName}, tagValue: ${tag}}`
    };
    function getDiscriminatorPropertyFromAllOf(sch, tagName) {
      var _a3;
      if (!sch.allOf || !Array.isArray(sch.allOf)) {
        return void 0;
      }
      for (const subschema of sch.allOf) {
        if ((_a3 = subschema === null || subschema === void 0 ? void 0 : subschema.properties) === null || _a3 === void 0 ? void 0 : _a3[tagName]) {
          return subschema.properties[tagName];
        }
      }
      return void 0;
    }
    var def = {
      keyword: "discriminator",
      type: "object",
      schemaType: "object",
      error,
      code(cxt) {
        const { gen, data, schema: schema4, parentSchema, it: it2 } = cxt;
        const keyword = parentSchema.oneOf ? "oneOf" : parentSchema.anyOf ? "anyOf" : void 0;
        if (!it2.opts.discriminator) {
          throw new Error("discriminator: requires discriminator option");
        }
        const tagName = schema4.propertyName;
        if (typeof tagName != "string")
          throw new Error("discriminator: requires propertyName");
        if (!keyword)
          throw new Error("discriminator: requires oneOf or anyOf composite keyword");
        const parentSchemaVariants = parentSchema[keyword];
        const valid = gen.let("valid", false);
        const tag = gen.const("tag", (0, codegen_1._)`${data}${(0, codegen_1.getProperty)(tagName)}`);
        gen.if((0, codegen_1._)`typeof ${tag} == "string"`, () => validateMapping(), () => cxt.error(false, { discrError: types_1.DiscrError.Tag, tag, tagName }));
        cxt.ok(valid);
        function validateMapping() {
          const mapping = getMapping();
          gen.if(false);
          for (const tagValue in mapping) {
            gen.elseIf((0, codegen_1._)`${tag} === ${tagValue}`);
            gen.assign(valid, applyTagSchema(mapping[tagValue]));
          }
          gen.else();
          cxt.error(false, { discrError: types_1.DiscrError.Mapping, tag, tagName });
          gen.endIf();
        }
        function applyTagSchema(schemaProp) {
          const _valid = gen.name("valid");
          const schCxt = cxt.subschema({ keyword, schemaProp }, _valid);
          cxt.mergeEvaluated(schCxt, codegen_1.Name);
          return _valid;
        }
        function getMapping() {
          var _a3;
          const discriminatorMapping = {};
          const topRequired = hasRequired(parentSchema);
          let tagRequired = true;
          for (let i2 = 0; i2 < parentSchemaVariants.length; i2++) {
            let sch = parentSchemaVariants[i2];
            const schRef = sch === null || sch === void 0 ? void 0 : sch.$ref;
            if (schRef && schema4.mapping) {
              const { mapping } = schema4;
              const matchedKeys = Object.keys(mapping).filter((key2) => mapping[key2] === sch.$ref);
              if (matchedKeys.length) {
                for (const key2 of matchedKeys) {
                  addMapping(key2, i2);
                }
                continue;
              }
            }
            if (schRef && !(0, util_1.schemaHasRulesButRef)(sch, it2.self.RULES)) {
              sch = compile_1.resolveRef.call(it2.self, it2.schemaEnv.root, it2.baseId, schRef);
              if (sch instanceof compile_1.SchemaEnv)
                sch = sch.schema;
            }
            let propSch = (_a3 = sch === null || sch === void 0 ? void 0 : sch.properties) === null || _a3 === void 0 ? void 0 : _a3[tagName];
            if (!propSch && (sch === null || sch === void 0 ? void 0 : sch.allOf)) {
              propSch = getDiscriminatorPropertyFromAllOf(sch, tagName);
            }
            if (typeof propSch != "object") {
              throw new Error(`discriminator: ${keyword} subschemas (or referenced schemas) must have "properties/${tagName}" or match mapping`);
            }
            tagRequired = tagRequired && (topRequired || hasRequired(sch));
            addMappings(propSch, i2);
          }
          if (!tagRequired)
            throw new Error(`discriminator: "${tagName}" must be required`);
          return discriminatorMapping;
          function hasRequired(sch) {
            if (Array.isArray(sch.required) && sch.required.includes(tagName)) {
              return true;
            }
            if (sch.allOf && Array.isArray(sch.allOf)) {
              for (const subschema of sch.allOf) {
                const subSch = subschema;
                if (Array.isArray(subSch.required) && subSch.required.includes(tagName)) {
                  return true;
                }
              }
            }
            return false;
          }
          function addMappings(sch, i2) {
            if (sch.const) {
              addMapping(sch.const, i2);
            } else if (sch.enum) {
              for (const tagValue of sch.enum) {
                addMapping(tagValue, i2);
              }
            } else {
              throw new Error(`discriminator: "properties/${tagName}" must have "const" or "enum"`);
            }
          }
          function addMapping(tagValue, i2) {
            if (typeof tagValue != "string" || tagValue in discriminatorMapping) {
              throw new Error(`discriminator: "${tagName}" values must be unique strings`);
            }
            discriminatorMapping[tagValue] = i2;
          }
        }
      }
    };
    exports.default = def;
  }
});

// node_modules/@redocly/ajv/dist/refs/json-schema-2020-12/schema.json
var require_schema2 = __commonJS({
  "node_modules/@redocly/ajv/dist/refs/json-schema-2020-12/schema.json"(exports, module) {
    module.exports = {
      $schema: "https://json-schema.org/draft/2020-12/schema",
      $id: "https://json-schema.org/draft/2020-12/schema",
      $vocabulary: {
        "https://json-schema.org/draft/2020-12/vocab/core": true,
        "https://json-schema.org/draft/2020-12/vocab/applicator": true,
        "https://json-schema.org/draft/2020-12/vocab/unevaluated": true,
        "https://json-schema.org/draft/2020-12/vocab/validation": true,
        "https://json-schema.org/draft/2020-12/vocab/meta-data": true,
        "https://json-schema.org/draft/2020-12/vocab/format-annotation": true,
        "https://json-schema.org/draft/2020-12/vocab/content": true
      },
      $dynamicAnchor: "meta",
      title: "Core and Validation specifications meta-schema",
      allOf: [
        { $ref: "meta/core" },
        { $ref: "meta/applicator" },
        { $ref: "meta/unevaluated" },
        { $ref: "meta/validation" },
        { $ref: "meta/meta-data" },
        { $ref: "meta/format-annotation" },
        { $ref: "meta/content" }
      ],
      type: ["object", "boolean"],
      $comment: "This meta-schema also defines keywords that have appeared in previous drafts in order to prevent incompatible extensions as they remain in common use.",
      properties: {
        definitions: {
          $comment: '"definitions" has been replaced by "$defs".',
          type: "object",
          additionalProperties: { $dynamicRef: "#meta" },
          deprecated: true,
          default: {}
        },
        dependencies: {
          $comment: '"dependencies" has been split and replaced by "dependentSchemas" and "dependentRequired" in order to serve their differing semantics.',
          type: "object",
          additionalProperties: {
            anyOf: [{ $dynamicRef: "#meta" }, { $ref: "meta/validation#/$defs/stringArray" }]
          },
          deprecated: true,
          default: {}
        },
        $recursiveAnchor: {
          $comment: '"$recursiveAnchor" has been replaced by "$dynamicAnchor".',
          $ref: "meta/core#/$defs/anchorString",
          deprecated: true
        },
        $recursiveRef: {
          $comment: '"$recursiveRef" has been replaced by "$dynamicRef".',
          $ref: "meta/core#/$defs/uriReferenceString",
          deprecated: true
        }
      }
    };
  }
});

// node_modules/@redocly/ajv/dist/refs/json-schema-2020-12/meta/applicator.json
var require_applicator2 = __commonJS({
  "node_modules/@redocly/ajv/dist/refs/json-schema-2020-12/meta/applicator.json"(exports, module) {
    module.exports = {
      $schema: "https://json-schema.org/draft/2020-12/schema",
      $id: "https://json-schema.org/draft/2020-12/meta/applicator",
      $vocabulary: {
        "https://json-schema.org/draft/2020-12/vocab/applicator": true
      },
      $dynamicAnchor: "meta",
      title: "Applicator vocabulary meta-schema",
      type: ["object", "boolean"],
      properties: {
        prefixItems: { $ref: "#/$defs/schemaArray" },
        items: { $dynamicRef: "#meta" },
        contains: { $dynamicRef: "#meta" },
        additionalProperties: { $dynamicRef: "#meta" },
        properties: {
          type: "object",
          additionalProperties: { $dynamicRef: "#meta" },
          default: {}
        },
        patternProperties: {
          type: "object",
          additionalProperties: { $dynamicRef: "#meta" },
          propertyNames: { format: "regex" },
          default: {}
        },
        dependentSchemas: {
          type: "object",
          additionalProperties: { $dynamicRef: "#meta" },
          default: {}
        },
        propertyNames: { $dynamicRef: "#meta" },
        if: { $dynamicRef: "#meta" },
        then: { $dynamicRef: "#meta" },
        else: { $dynamicRef: "#meta" },
        allOf: { $ref: "#/$defs/schemaArray" },
        anyOf: { $ref: "#/$defs/schemaArray" },
        oneOf: { $ref: "#/$defs/schemaArray" },
        not: { $dynamicRef: "#meta" }
      },
      $defs: {
        schemaArray: {
          type: "array",
          minItems: 1,
          items: { $dynamicRef: "#meta" }
        }
      }
    };
  }
});

// node_modules/@redocly/ajv/dist/refs/json-schema-2020-12/meta/unevaluated.json
var require_unevaluated2 = __commonJS({
  "node_modules/@redocly/ajv/dist/refs/json-schema-2020-12/meta/unevaluated.json"(exports, module) {
    module.exports = {
      $schema: "https://json-schema.org/draft/2020-12/schema",
      $id: "https://json-schema.org/draft/2020-12/meta/unevaluated",
      $vocabulary: {
        "https://json-schema.org/draft/2020-12/vocab/unevaluated": true
      },
      $dynamicAnchor: "meta",
      title: "Unevaluated applicator vocabulary meta-schema",
      type: ["object", "boolean"],
      properties: {
        unevaluatedItems: { $dynamicRef: "#meta" },
        unevaluatedProperties: { $dynamicRef: "#meta" }
      }
    };
  }
});

// node_modules/@redocly/ajv/dist/refs/json-schema-2020-12/meta/content.json
var require_content = __commonJS({
  "node_modules/@redocly/ajv/dist/refs/json-schema-2020-12/meta/content.json"(exports, module) {
    module.exports = {
      $schema: "https://json-schema.org/draft/2020-12/schema",
      $id: "https://json-schema.org/draft/2020-12/meta/content",
      $vocabulary: {
        "https://json-schema.org/draft/2020-12/vocab/content": true
      },
      $dynamicAnchor: "meta",
      title: "Content vocabulary meta-schema",
      type: ["object", "boolean"],
      properties: {
        contentEncoding: { type: "string" },
        contentMediaType: { type: "string" },
        contentSchema: { $dynamicRef: "#meta" }
      }
    };
  }
});

// node_modules/@redocly/ajv/dist/refs/json-schema-2020-12/meta/core.json
var require_core4 = __commonJS({
  "node_modules/@redocly/ajv/dist/refs/json-schema-2020-12/meta/core.json"(exports, module) {
    module.exports = {
      $schema: "https://json-schema.org/draft/2020-12/schema",
      $id: "https://json-schema.org/draft/2020-12/meta/core",
      $vocabulary: {
        "https://json-schema.org/draft/2020-12/vocab/core": true
      },
      $dynamicAnchor: "meta",
      title: "Core vocabulary meta-schema",
      type: ["object", "boolean"],
      properties: {
        $id: {
          $ref: "#/$defs/uriReferenceString",
          $comment: "Non-empty fragments not allowed.",
          pattern: "^[^#]*#?$"
        },
        $schema: { $ref: "#/$defs/uriString" },
        $ref: { $ref: "#/$defs/uriReferenceString" },
        $anchor: { $ref: "#/$defs/anchorString" },
        $dynamicRef: { $ref: "#/$defs/uriReferenceString" },
        $dynamicAnchor: { $ref: "#/$defs/anchorString" },
        $vocabulary: {
          type: "object",
          propertyNames: { $ref: "#/$defs/uriString" },
          additionalProperties: {
            type: "boolean"
          }
        },
        $comment: {
          type: "string"
        },
        $defs: {
          type: "object",
          additionalProperties: { $dynamicRef: "#meta" }
        }
      },
      $defs: {
        anchorString: {
          type: "string",
          pattern: "^[A-Za-z_][-A-Za-z0-9._]*$"
        },
        uriString: {
          type: "string",
          format: "uri"
        },
        uriReferenceString: {
          type: "string",
          format: "uri-reference"
        }
      }
    };
  }
});

// node_modules/@redocly/ajv/dist/refs/json-schema-2020-12/meta/format-annotation.json
var require_format_annotation = __commonJS({
  "node_modules/@redocly/ajv/dist/refs/json-schema-2020-12/meta/format-annotation.json"(exports, module) {
    module.exports = {
      $schema: "https://json-schema.org/draft/2020-12/schema",
      $id: "https://json-schema.org/draft/2020-12/meta/format-annotation",
      $vocabulary: {
        "https://json-schema.org/draft/2020-12/vocab/format-annotation": true
      },
      $dynamicAnchor: "meta",
      title: "Format vocabulary meta-schema for annotation results",
      type: ["object", "boolean"],
      properties: {
        format: { type: "string" }
      }
    };
  }
});

// node_modules/@redocly/ajv/dist/refs/json-schema-2020-12/meta/meta-data.json
var require_meta_data = __commonJS({
  "node_modules/@redocly/ajv/dist/refs/json-schema-2020-12/meta/meta-data.json"(exports, module) {
    module.exports = {
      $schema: "https://json-schema.org/draft/2020-12/schema",
      $id: "https://json-schema.org/draft/2020-12/meta/meta-data",
      $vocabulary: {
        "https://json-schema.org/draft/2020-12/vocab/meta-data": true
      },
      $dynamicAnchor: "meta",
      title: "Meta-data vocabulary meta-schema",
      type: ["object", "boolean"],
      properties: {
        title: {
          type: "string"
        },
        description: {
          type: "string"
        },
        default: true,
        deprecated: {
          type: "boolean",
          default: false
        },
        readOnly: {
          type: "boolean",
          default: false
        },
        writeOnly: {
          type: "boolean",
          default: false
        },
        examples: {
          type: "array",
          items: true
        }
      }
    };
  }
});

// node_modules/@redocly/ajv/dist/refs/json-schema-2020-12/meta/validation.json
var require_validation2 = __commonJS({
  "node_modules/@redocly/ajv/dist/refs/json-schema-2020-12/meta/validation.json"(exports, module) {
    module.exports = {
      $schema: "https://json-schema.org/draft/2020-12/schema",
      $id: "https://json-schema.org/draft/2020-12/meta/validation",
      $vocabulary: {
        "https://json-schema.org/draft/2020-12/vocab/validation": true
      },
      $dynamicAnchor: "meta",
      title: "Validation vocabulary meta-schema",
      type: ["object", "boolean"],
      properties: {
        type: {
          anyOf: [
            { $ref: "#/$defs/simpleTypes" },
            {
              type: "array",
              items: { $ref: "#/$defs/simpleTypes" },
              minItems: 1,
              uniqueItems: true
            }
          ]
        },
        const: true,
        enum: {
          type: "array",
          items: true
        },
        multipleOf: {
          type: "number",
          exclusiveMinimum: 0
        },
        maximum: {
          type: "number"
        },
        exclusiveMaximum: {
          type: "number"
        },
        minimum: {
          type: "number"
        },
        exclusiveMinimum: {
          type: "number"
        },
        maxLength: { $ref: "#/$defs/nonNegativeInteger" },
        minLength: { $ref: "#/$defs/nonNegativeIntegerDefault0" },
        pattern: {
          type: "string",
          format: "regex"
        },
        maxItems: { $ref: "#/$defs/nonNegativeInteger" },
        minItems: { $ref: "#/$defs/nonNegativeIntegerDefault0" },
        uniqueItems: {
          type: "boolean",
          default: false
        },
        maxContains: { $ref: "#/$defs/nonNegativeInteger" },
        minContains: {
          $ref: "#/$defs/nonNegativeInteger",
          default: 1
        },
        maxProperties: { $ref: "#/$defs/nonNegativeInteger" },
        minProperties: { $ref: "#/$defs/nonNegativeIntegerDefault0" },
        required: { $ref: "#/$defs/stringArray" },
        dependentRequired: {
          type: "object",
          additionalProperties: {
            $ref: "#/$defs/stringArray"
          }
        }
      },
      $defs: {
        nonNegativeInteger: {
          type: "integer",
          minimum: 0
        },
        nonNegativeIntegerDefault0: {
          $ref: "#/$defs/nonNegativeInteger",
          default: 0
        },
        simpleTypes: {
          enum: ["array", "boolean", "integer", "null", "number", "object", "string"]
        },
        stringArray: {
          type: "array",
          items: { type: "string" },
          uniqueItems: true,
          default: []
        }
      }
    };
  }
});

// node_modules/@redocly/ajv/dist/refs/json-schema-2020-12/index.js
var require_json_schema_2020_12 = __commonJS({
  "node_modules/@redocly/ajv/dist/refs/json-schema-2020-12/index.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.default = addMetaSchema2020;
    var metaSchema = require_schema2();
    var applicator = require_applicator2();
    var unevaluated = require_unevaluated2();
    var content = require_content();
    var core = require_core4();
    var format2 = require_format_annotation();
    var metadata = require_meta_data();
    var validation = require_validation2();
    var META_SUPPORT_DATA = ["/properties"];
    function addMetaSchema2020($data) {
      ;
      [
        metaSchema,
        applicator,
        unevaluated,
        content,
        core,
        with$data(this, format2),
        metadata,
        with$data(this, validation)
      ].forEach((sch) => this.addMetaSchema(sch, void 0, false));
      return this;
      function with$data(ajv, sch) {
        return $data ? ajv.$dataMetaSchema(sch, META_SUPPORT_DATA) : sch;
      }
    }
  }
});

// node_modules/@redocly/ajv/dist/2020.js
var require__ = __commonJS({
  "node_modules/@redocly/ajv/dist/2020.js"(exports, module) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.CodeGen = exports.Name = exports.nil = exports.stringify = exports.str = exports._ = exports.KeywordCxt = void 0;
    var core_1 = require_core2();
    var draft2020_1 = require_draft2020();
    var discriminator_1 = require_discriminator();
    var json_schema_2020_12_1 = require_json_schema_2020_12();
    var META_SCHEMA_ID = "https://json-schema.org/draft/2020-12/schema";
    var Ajv2020 = class extends core_1.default {
      constructor(opts = {}) {
        super({
          ...opts,
          dynamicRef: true,
          next: true,
          unevaluated: true
        });
      }
      _addVocabularies() {
        super._addVocabularies();
        draft2020_1.default.forEach((v2) => this.addVocabulary(v2));
        if (this.opts.discriminator)
          this.addKeyword(discriminator_1.default);
      }
      _addDefaultMetaSchema() {
        super._addDefaultMetaSchema();
        const { $data, meta } = this.opts;
        if (!meta)
          return;
        json_schema_2020_12_1.default.call(this, $data);
        this.refs["http://json-schema.org/schema"] = META_SCHEMA_ID;
      }
      defaultMeta() {
        return this.opts.defaultMeta = super.defaultMeta() || (this.getSchema(META_SCHEMA_ID) ? META_SCHEMA_ID : void 0);
      }
    };
    module.exports = exports = Ajv2020;
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.default = Ajv2020;
    var validate_1 = require_validate();
    Object.defineProperty(exports, "KeywordCxt", { enumerable: true, get: function() {
      return validate_1.KeywordCxt;
    } });
    var codegen_1 = require_codegen();
    Object.defineProperty(exports, "_", { enumerable: true, get: function() {
      return codegen_1._;
    } });
    Object.defineProperty(exports, "str", { enumerable: true, get: function() {
      return codegen_1.str;
    } });
    Object.defineProperty(exports, "stringify", { enumerable: true, get: function() {
      return codegen_1.stringify;
    } });
    Object.defineProperty(exports, "nil", { enumerable: true, get: function() {
      return codegen_1.nil;
    } });
    Object.defineProperty(exports, "Name", { enumerable: true, get: function() {
      return codegen_1.Name;
    } });
    Object.defineProperty(exports, "CodeGen", { enumerable: true, get: function() {
      return codegen_1.CodeGen;
    } });
  }
});

// node_modules/@redocly/openapi-core/lib/types/json-schema-adapter.js
var require_json_schema_adapter = __commonJS({
  "node_modules/@redocly/openapi-core/lib/types/json-schema-adapter.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.getNodeTypesFromJSONSchema = getNodeTypesFromJSONSchema;
    var _2020_1 = require__();
    var utils_1 = require_utils();
    var ajv = new _2020_1.default({
      strictSchema: false,
      allowUnionTypes: true,
      useDefaults: true,
      allErrors: true,
      discriminator: true,
      strictTypes: false,
      verbose: true
    });
    function findOneOf(schemaOneOf, oneOfs) {
      if (oneOfs.some((option) => typeof option === "function")) {
        throw new Error("Unexpected oneOf inside oneOf.");
      }
      return (value) => {
        let index = schemaOneOf.findIndex((option) => ajv.validate(option, value));
        if (index === -1) {
          index = 0;
        }
        return oneOfs[index];
      };
    }
    function transformJSONSchemaToNodeType(propertyName, schema4, ctx) {
      if (!schema4 || typeof schema4 === "boolean") {
        throw new Error(`Unexpected schema in ${propertyName}.`);
      }
      if (schema4 instanceof Array) {
        throw new Error(`Unexpected array schema in ${propertyName}. Try using oneOf instead.`);
      }
      if (schema4.type === "null") {
        throw new Error(`Unexpected null schema type in ${propertyName} schema.`);
      }
      if (schema4.type instanceof Array) {
        throw new Error(`Unexpected array schema type in ${propertyName} schema. Try using oneOf instead.`);
      }
      if (schema4.type === "string" || schema4.type === "number" || schema4.type === "integer" || schema4.type === "boolean") {
        const { default: _15, format: _format, ...rest } = schema4;
        return rest;
      }
      if (schema4.type === "object" && !schema4.properties && !schema4.oneOf) {
        if (schema4.additionalProperties === void 0 || schema4.additionalProperties === true) {
          return { type: "object" };
        } else if (schema4.additionalProperties === false) {
          return { type: "object", properties: {} };
        }
      }
      if (schema4.allOf) {
        throw new Error(`Unexpected allOf in ${propertyName}.`);
      }
      if (schema4.anyOf) {
        throw new Error(`Unexpected anyOf in ${propertyName}.`);
      }
      if ((0, utils_1.isPlainObject)(schema4.properties) || (0, utils_1.isPlainObject)(schema4.additionalProperties) || (0, utils_1.isPlainObject)(schema4.items) && ((0, utils_1.isPlainObject)(schema4.items.properties) || (0, utils_1.isPlainObject)(schema4.items.additionalProperties) || schema4.items.oneOf)) {
        return extractNodeToContext(propertyName, schema4, ctx);
      }
      if (schema4.oneOf) {
        if (schema4.discriminator) {
          const discriminatedPropertyName = schema4.discriminator?.propertyName;
          if (!discriminatedPropertyName) {
            throw new Error(`Unexpected discriminator without a propertyName in ${propertyName}.`);
          }
          const oneOfs = schema4.oneOf.map((option, i2) => {
            if (typeof option === "boolean") {
              throw new Error(`Unexpected boolean schema in ${propertyName} at position ${i2} in oneOf.`);
            }
            const discriminatedProperty = option?.properties?.[discriminatedPropertyName];
            if (!discriminatedProperty || typeof discriminatedProperty === "boolean") {
              throw new Error(`Unexpected property '${discriminatedProperty}' schema in ${propertyName} at position ${i2} in oneOf.`);
            }
            const name = discriminatedProperty.const;
            return transformJSONSchemaToNodeType(name, option, ctx);
          });
          return (value, key2) => {
            if ((0, utils_1.isPlainObject)(value)) {
              const discriminatedTypeName = value[discriminatedPropertyName];
              if (typeof discriminatedTypeName === "string" && ctx[discriminatedTypeName]) {
                return discriminatedTypeName;
              }
            }
            return findOneOf(schema4.oneOf, oneOfs)(value, key2);
          };
        } else {
          const oneOfs = schema4.oneOf.map((option, i2) => transformJSONSchemaToNodeType(propertyName + "_" + i2, option, ctx));
          return findOneOf(schema4.oneOf, oneOfs);
        }
      }
      return schema4;
    }
    function extractNodeToContext(propertyName, schema4, ctx) {
      if (!schema4 || typeof schema4 === "boolean") {
        throw new Error(`Unexpected schema in ${propertyName}.`);
      }
      if (schema4 instanceof Array) {
        throw new Error(`Unexpected array schema in ${propertyName}. Try using oneOf instead.`);
      }
      if (schema4.type === "null") {
        throw new Error(`Unexpected null schema type in ${propertyName} schema.`);
      }
      if (schema4.type instanceof Array) {
        throw new Error(`Unexpected array schema type in ${propertyName} schema. Try using oneOf instead.`);
      }
      const properties = {};
      for (const [name, property] of Object.entries(schema4.properties || {})) {
        properties[name] = transformJSONSchemaToNodeType(propertyName + "." + name, property, ctx);
      }
      let additionalProperties;
      if ((0, utils_1.isPlainObject)(schema4.additionalProperties)) {
        additionalProperties = transformJSONSchemaToNodeType(propertyName + "_additionalProperties", schema4.additionalProperties, ctx);
      }
      if (schema4.additionalProperties === true) {
        additionalProperties = {};
      }
      let items;
      if ((0, utils_1.isPlainObject)(schema4.items) && ((0, utils_1.isPlainObject)(schema4.items.properties) || (0, utils_1.isPlainObject)(schema4.items.additionalProperties) || schema4.items.oneOf)) {
        items = transformJSONSchemaToNodeType(propertyName + "_items", schema4.items, ctx);
      }
      let required = schema4.required;
      if (schema4.oneOf && schema4.oneOf.every((option) => !!option.required)) {
        required = (value) => {
          const requiredList = schema4.oneOf.map((option) => [
            ...schema4.required || [],
            ...option.required
          ]);
          let index = requiredList.findIndex((r2) => r2.every((requiredProp) => value[requiredProp] !== void 0));
          if (index === -1) {
            index = 0;
          }
          return requiredList[index];
        };
      }
      ctx[propertyName] = { properties, additionalProperties, items, required };
      return propertyName;
    }
    function getNodeTypesFromJSONSchema(schemaName, entrySchema) {
      const ctx = {};
      transformJSONSchemaToNodeType(schemaName, entrySchema, ctx);
      return ctx;
    }
  }
});

// node_modules/@redocly/openapi-core/lib/types/redocly-yaml.js
var require_redocly_yaml = __commonJS({
  "node_modules/@redocly/openapi-core/lib/types/redocly-yaml.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.NormalizedConfigTypes = exports.ConfigTypes = void 0;
    exports.createConfigTypes = createConfigTypes;
    var config_1 = (init_lib_esm(), __toCommonJS(lib_esm_exports));
    var _1 = require_types();
    var oas_types_1 = require_oas_types();
    var utils_1 = require_utils();
    var json_schema_adapter_1 = require_json_schema_adapter();
    var types_1 = require_types();
    var builtInOAS2Rules = [
      "info-contact",
      "operation-operationId",
      "tag-description",
      "tags-alphabetical",
      "info-license-url",
      "info-license-strict",
      "info-license",
      "no-ambiguous-paths",
      "no-enum-type-mismatch",
      "no-http-verbs-in-paths",
      "no-identical-paths",
      "no-invalid-parameter-examples",
      "no-invalid-schema-examples",
      "no-path-trailing-slash",
      "operation-2xx-response",
      "operation-4xx-response",
      "operation-description",
      "operation-operationId-unique",
      "operation-operationId-url-safe",
      "operation-parameters-unique",
      "operation-singular-tag",
      "operation-summary",
      "operation-tag-defined",
      "parameter-description",
      "path-declaration-must-exist",
      "path-excludes-patterns",
      "path-http-verbs-order",
      "path-not-include-query",
      "path-params-defined",
      "path-parameters-defined",
      "path-segment-plural",
      "paths-kebab-case",
      "required-string-property-missing-min-length",
      "response-contains-header",
      "scalar-property-missing-example",
      "security-defined",
      "spec-strict-refs",
      "no-unresolved-refs",
      "no-required-schema-properties-undefined",
      "no-schema-type-mismatch",
      "boolean-parameter-prefixes",
      "request-mime-type",
      "response-contains-property",
      "response-mime-type"
    ];
    var builtInOAS3Rules = [
      "info-contact",
      "operation-operationId",
      "tag-description",
      "tags-alphabetical",
      "info-license-url",
      "info-license-strict",
      "info-license",
      "no-ambiguous-paths",
      "no-enum-type-mismatch",
      "no-http-verbs-in-paths",
      "no-identical-paths",
      "no-invalid-parameter-examples",
      "no-invalid-schema-examples",
      "no-path-trailing-slash",
      "operation-2xx-response",
      "operation-4xx-response",
      "operation-description",
      "operation-operationId-unique",
      "operation-operationId-url-safe",
      "operation-parameters-unique",
      "operation-singular-tag",
      "operation-summary",
      "operation-tag-defined",
      "parameter-description",
      "path-declaration-must-exist",
      "path-excludes-patterns",
      "path-http-verbs-order",
      "path-not-include-query",
      "path-params-defined",
      "path-parameters-defined",
      "path-segment-plural",
      "paths-kebab-case",
      "required-string-property-missing-min-length",
      "response-contains-header",
      "scalar-property-missing-example",
      "security-defined",
      "spec-strict-refs",
      "no-unresolved-refs",
      "no-required-schema-properties-undefined",
      "no-schema-type-mismatch",
      "boolean-parameter-prefixes",
      "component-name-unique",
      "no-empty-servers",
      "no-example-value-and-externalValue",
      "no-invalid-media-type-examples",
      "no-server-example.com",
      "no-server-trailing-slash",
      "no-server-variables-empty-enum",
      "no-undefined-server-variable",
      "no-unused-components",
      "operation-4xx-problem-details-rfc7807",
      "request-mime-type",
      "response-contains-property",
      "response-mime-type",
      "spec-components-invalid-map-name",
      "array-parameter-serialization"
    ];
    var builtInAsync2Rules = [
      "info-contact",
      "info-license-strict",
      "operation-operationId",
      "tag-description",
      "tags-alphabetical",
      "channels-kebab-case",
      "no-channel-trailing-slash"
    ];
    var builtInAsync3Rules = [
      "info-contact",
      "info-license-strict",
      "operation-operationId",
      "tag-description",
      "tags-alphabetical",
      "channels-kebab-case",
      "no-channel-trailing-slash"
    ];
    var builtInArazzo1Rules = [
      "sourceDescription-type",
      "workflowId-unique",
      "stepId-unique",
      "sourceDescription-name-unique",
      "sourceDescriptions-not-empty",
      "workflow-dependsOn",
      "parameters-unique",
      "step-onSuccess-unique",
      "step-onFailure-unique",
      "respect-supported-versions",
      "requestBody-replacements-unique",
      "no-criteria-xpath",
      "criteria-unique"
    ];
    var builtInOverlay1Rules = ["info-contact"];
    var builtInRules = [
      ...builtInOAS2Rules,
      ...builtInOAS3Rules,
      ...builtInAsync2Rules,
      ...builtInAsync3Rules,
      ...builtInArazzo1Rules,
      ...builtInOverlay1Rules,
      "spec",
      // TODO: depricated in favor of struct
      "struct"
    ];
    var ConfigStyleguide = {
      properties: {
        extends: {
          type: "array",
          items: {
            type: "string"
          }
        },
        rules: "Rules",
        oas2Rules: "Rules",
        oas3_0Rules: "Rules",
        oas3_1Rules: "Rules",
        async2Rules: "Rules",
        arazzo1Rules: "Rules",
        preprocessors: { type: "object" },
        oas2Preprocessors: { type: "object" },
        oas3_0Preprocessors: { type: "object" },
        oas3_1Preprocessors: { type: "object" },
        async2Preprocessors: { type: "object" },
        arazzoPreprocessors: { type: "object" },
        decorators: { type: "object" },
        oas2Decorators: { type: "object" },
        oas3_0Decorators: { type: "object" },
        oas3_1Decorators: { type: "object" },
        async2Decorators: { type: "object" },
        arazzo1Decorators: { type: "object" }
      }
    };
    var createConfigRoot = (nodeTypes) => ({
      ...nodeTypes.rootRedoclyConfigSchema,
      properties: {
        ...nodeTypes.rootRedoclyConfigSchema.properties,
        ...ConfigStyleguide.properties,
        apis: "ConfigApis",
        // Override apis with internal format
        "features.openapi": "ConfigReferenceDocs",
        // deprecated
        "features.mockServer": "ConfigMockServer",
        // deprecated
        organization: { type: "string" },
        region: { enum: ["us", "eu"] },
        telemetry: { enum: ["on", "off"] },
        resolve: {
          properties: {
            http: "ConfigHTTP",
            doNotResolveExamples: { type: "boolean" }
          }
        },
        files: {
          type: "array",
          items: {
            type: "string"
          }
        }
      }
    });
    var ConfigApis = {
      properties: {},
      additionalProperties: "ConfigApisProperties"
    };
    var createConfigApisProperties = (nodeTypes) => ({
      ...nodeTypes["rootRedoclyConfigSchema.apis_additionalProperties"],
      properties: {
        ...nodeTypes["rootRedoclyConfigSchema.apis_additionalProperties"]?.properties,
        labels: {
          type: "array",
          items: {
            type: "string"
          }
        },
        ...ConfigStyleguide.properties,
        "features.openapi": "ConfigReferenceDocs",
        // deprecated
        "features.mockServer": "ConfigMockServer",
        // deprecated
        files: {
          type: "array",
          items: {
            type: "string"
          }
        }
      }
    });
    var ConfigHTTP = {
      properties: {
        headers: {
          type: "array",
          items: {
            type: "string"
          }
        }
      }
    };
    var Rules = {
      properties: {},
      additionalProperties: (value, key2) => {
        if (key2.startsWith("rule/")) {
          if (typeof value === "string") {
            return { enum: ["error", "warn", "off"] };
          } else {
            return "Assert";
          }
        } else if (key2.startsWith("assert/")) {
          if (typeof value === "string") {
            return { enum: ["error", "warn", "off"] };
          } else {
            return "Assert";
          }
        } else if (builtInRules.includes(key2) || (0, utils_1.isCustomRuleId)(key2)) {
          if (typeof value === "string") {
            return { enum: ["error", "warn", "off"] };
          } else {
            return "ObjectRule";
          }
        } else if (key2 === "metadata-schema" || key2 === "custom-fields-schema") {
          return "Schema";
        }
        return;
      }
    };
    var ObjectRule = {
      properties: {
        severity: { enum: ["error", "warn", "off"] }
      },
      additionalProperties: {},
      required: ["severity"]
    };
    var Schema2 = {
      properties: {},
      additionalProperties: {}
    };
    function createAssertionDefinitionSubject(nodeNames) {
      return {
        properties: {
          type: {
            enum: [.../* @__PURE__ */ new Set(["any", ...nodeNames, "SpecExtension"])]
          },
          property: (value) => {
            if (Array.isArray(value)) {
              return { type: "array", items: { type: "string" } };
            } else if (value === null) {
              return null;
            } else {
              return { type: "string" };
            }
          },
          filterInParentKeys: { type: "array", items: { type: "string" } },
          filterOutParentKeys: { type: "array", items: { type: "string" } },
          matchParentKeys: { type: "string" }
        },
        required: ["type"]
      };
    }
    var AssertionDefinitionAssertions = {
      properties: {
        enum: { type: "array", items: { type: "string" } },
        pattern: { type: "string" },
        notPattern: { type: "string" },
        casing: {
          enum: [
            "camelCase",
            "kebab-case",
            "snake_case",
            "PascalCase",
            "MACRO_CASE",
            "COBOL-CASE",
            "flatcase"
          ]
        },
        mutuallyExclusive: { type: "array", items: { type: "string" } },
        mutuallyRequired: { type: "array", items: { type: "string" } },
        required: { type: "array", items: { type: "string" } },
        requireAny: { type: "array", items: { type: "string" } },
        disallowed: { type: "array", items: { type: "string" } },
        defined: { type: "boolean" },
        // undefined: { type: 'boolean' }, // TODO: Remove `undefined` assertion from codebase overall
        nonEmpty: { type: "boolean" },
        minLength: { type: "integer" },
        maxLength: { type: "integer" },
        ref: (value) => typeof value === "string" ? { type: "string" } : { type: "boolean" },
        const: (value) => {
          if (typeof value === "string") {
            return { type: "string" };
          }
          if (typeof value === "number") {
            return { type: "number" };
          }
          if (typeof value === "boolean") {
            return { type: "boolean" };
          } else {
            return;
          }
        }
      },
      additionalProperties: (_value, key2) => {
        if (/^\w+\/\w+$/.test(key2))
          return { type: "object" };
        return;
      }
    };
    var AssertDefinition = {
      properties: {
        subject: "AssertionDefinitionSubject",
        assertions: "AssertionDefinitionAssertions"
      },
      required: ["subject", "assertions"]
    };
    var Assert = {
      properties: {
        subject: "AssertionDefinitionSubject",
        assertions: "AssertionDefinitionAssertions",
        where: (0, _1.listOf)("AssertDefinition"),
        message: { type: "string" },
        suggest: { type: "array", items: { type: "string" } },
        severity: { enum: ["error", "warn", "off"] }
      },
      required: ["subject", "assertions"]
    };
    var ConfigLanguage2 = {
      properties: {
        label: { type: "string" },
        lang: {
          enum: [
            "curl",
            "C#",
            "Go",
            "Java",
            "Java8+Apache",
            "JavaScript",
            "Node.js",
            "PHP",
            "Python",
            "R",
            "Ruby"
          ]
        }
      },
      required: ["lang"]
    };
    var ConfigLabels2 = {
      properties: {
        enum: { type: "string" },
        enumSingleValue: { type: "string" },
        enumArray: { type: "string" },
        default: { type: "string" },
        deprecated: { type: "string" },
        example: { type: "string" },
        examples: { type: "string" },
        nullable: { type: "string" },
        recursive: { type: "string" },
        arrayOf: { type: "string" },
        webhook: { type: "string" },
        authorizations: { type: "string" },
        tryItAuthBasicUsername: { type: "string" },
        tryItAuthBasicPassword: { type: "string" }
      }
    };
    var ConfigSidebarLinks2 = {
      properties: {
        beforeInfo: (0, _1.listOf)("CommonConfigSidebarLinks"),
        end: (0, _1.listOf)("CommonConfigSidebarLinks")
      }
    };
    var CommonConfigSidebarLinks2 = {
      properties: {
        label: { type: "string" },
        link: { type: "string" },
        target: { type: "string" }
      },
      required: ["label", "link"]
    };
    var CommonThemeColors2 = {
      properties: {
        main: { type: "string" },
        light: { type: "string" },
        dark: { type: "string" },
        contrastText: { type: "string" }
      }
    };
    var CommonColorProps2 = {
      properties: {
        backgroundColor: { type: "string" },
        borderColor: { type: "string" },
        color: { type: "string" },
        tabTextColor: { type: "string" }
      }
    };
    var BorderThemeColors2 = {
      properties: (0, utils_1.pickObjectProps)(CommonThemeColors2.properties, ["light", "dark"])
    };
    var HttpColors2 = {
      properties: {
        basic: { type: "string" },
        delete: { type: "string" },
        get: { type: "string" },
        head: { type: "string" },
        link: { type: "string" },
        options: { type: "string" },
        patch: { type: "string" },
        post: { type: "string" },
        put: { type: "string" }
      }
    };
    var ResponseColors2 = {
      properties: {
        error: "CommonColorProps",
        info: "CommonColorProps",
        redirect: "CommonColorProps",
        success: "CommonColorProps"
      }
    };
    var SecondaryColors2 = {
      properties: (0, utils_1.omitObjectProps)(CommonThemeColors2.properties, ["dark"])
    };
    var TextThemeColors2 = {
      properties: {
        primary: { type: "string" },
        secondary: { type: "string" },
        light: { type: "string" }
      }
    };
    var ThemeColors2 = {
      properties: {
        accent: "CommonThemeColors",
        border: "BorderThemeColors",
        error: "CommonThemeColors",
        http: "HttpColors",
        primary: "CommonThemeColors",
        responses: "ResponseColors",
        secondary: "SecondaryColors",
        success: "CommonThemeColors",
        text: "TextThemeColors",
        tonalOffset: { type: "number" },
        warning: "CommonThemeColors"
      }
    };
    var SizeProps2 = {
      properties: {
        fontSize: { type: "string" },
        padding: { type: "string" },
        minWidth: { type: "string" }
      }
    };
    var Sizes2 = {
      properties: {
        small: "SizeProps",
        medium: "SizeProps",
        large: "SizeProps",
        xlarge: "SizeProps"
      }
    };
    var FontConfig2 = {
      properties: {
        fontFamily: { type: "string" },
        fontSize: { type: "string" },
        fontWeight: { type: "string" },
        lineHeight: { type: "string" }
      }
    };
    var ButtonsConfig2 = {
      properties: {
        ...(0, utils_1.omitObjectProps)(FontConfig2.properties, ["fontSize", "lineHeight"]),
        borderRadius: { type: "string" },
        hoverStyle: { type: "string" },
        boxShadow: { type: "string" },
        hoverBoxShadow: { type: "string" },
        sizes: "Sizes"
      }
    };
    var BadgeFontConfig2 = {
      properties: (0, utils_1.pickObjectProps)(FontConfig2.properties, ["fontSize", "lineHeight"])
    };
    var BadgeSizes2 = {
      properties: {
        medium: "BadgeFontConfig",
        small: "BadgeFontConfig"
      }
    };
    var HttpBadgesConfig2 = {
      properties: {
        ...(0, utils_1.omitObjectProps)(FontConfig2.properties, ["fontSize", "lineHeight"]),
        borderRadius: { type: "string" },
        color: { type: "string" },
        sizes: "BadgeSizes"
      }
    };
    var LabelControls2 = {
      properties: {
        top: { type: "string" },
        width: { type: "string" },
        height: { type: "string" }
      }
    };
    var Panels2 = {
      properties: {
        borderRadius: { type: "string" },
        backgroundColor: { type: "string" }
      }
    };
    var TryItButton2 = {
      properties: {
        fullWidth: { type: "boolean" }
      }
    };
    var ConfigThemeComponents2 = {
      properties: {
        buttons: "ButtonsConfig",
        httpBadges: "HttpBadgesConfig",
        layoutControls: "LabelControls",
        panels: "Panels",
        tryItButton: "TryItButton",
        tryItSendButton: "TryItButton"
      }
    };
    var Breakpoints2 = {
      properties: {
        small: { type: "string" },
        medium: { type: "string" },
        large: { type: "string" }
      }
    };
    var StackedConfig2 = {
      properties: {
        maxWidth: "Breakpoints"
      }
    };
    var ThreePanelConfig2 = {
      properties: {
        maxWidth: "Breakpoints",
        middlePanelMaxWidth: "Breakpoints"
      }
    };
    var Layout2 = {
      properties: {
        showDarkRightPanel: { type: "boolean" },
        stacked: "StackedConfig",
        "three-panel": "ThreePanelConfig"
      }
    };
    var SchemaColorsConfig2 = {
      properties: {
        backgroundColor: { type: "string" },
        border: { type: "string" }
      }
    };
    var ConfigThemeSchema2 = {
      properties: {
        breakFieldNames: { type: "boolean" },
        caretColor: { type: "string" },
        caretSize: { type: "string" },
        constraints: "SchemaColorsConfig",
        defaultDetailsWidth: { type: "string" },
        examples: "SchemaColorsConfig",
        labelsTextSize: { type: "string" },
        linesColor: { type: "string" },
        nestedBackground: { type: "string" },
        nestingSpacing: { type: "string" },
        requireLabelColor: { type: "string" },
        typeNameColor: { type: "string" },
        typeTitleColor: { type: "string" }
      }
    };
    var GroupItemsConfig2 = {
      properties: {
        subItemsColor: { type: "string" },
        textTransform: { type: "string" },
        fontWeight: { type: "string" }
      }
    };
    var Level1Items2 = {
      properties: (0, utils_1.pickObjectProps)(GroupItemsConfig2.properties, ["textTransform"])
    };
    var SpacingConfig2 = {
      properties: {
        unit: { type: "number" },
        paddingHorizontal: { type: "string" },
        paddingVertical: { type: "string" },
        offsetTop: { type: "string" },
        offsetLeft: { type: "string" },
        offsetNesting: { type: "string" }
      }
    };
    var Sidebar2 = {
      properties: {
        ...(0, utils_1.omitObjectProps)(FontConfig2.properties, ["fontWeight", "lineHeight"]),
        activeBgColor: { type: "string" },
        activeTextColor: { type: "string" },
        backgroundColor: { type: "string" },
        borderRadius: { type: "string" },
        breakPath: { type: "boolean" },
        caretColor: { type: "string" },
        caretSize: { type: "string" },
        groupItems: "GroupItemsConfig",
        level1items: "Level1Items",
        rightLineColor: { type: "string" },
        separatorLabelColor: { type: "string" },
        showAtBreakpoint: { type: "string" },
        spacing: "SpacingConfig",
        textColor: { type: "string" },
        width: { type: "string" }
      }
    };
    var Heading2 = {
      properties: {
        ...FontConfig2.properties,
        color: { type: "string" },
        transform: { type: "string" }
      }
    };
    var CodeConfig2 = {
      properties: {
        ...FontConfig2.properties,
        backgroundColor: { type: "string" },
        color: { type: "string" },
        wordBreak: {
          enum: [
            "break-all",
            "break-word",
            "keep-all",
            "normal",
            "revert",
            "unset",
            "inherit",
            "initial"
          ]
        },
        wrap: { type: "boolean" }
      }
    };
    var HeadingsConfig2 = {
      properties: (0, utils_1.omitObjectProps)(FontConfig2.properties, ["fontSize"])
    };
    var LinksConfig2 = {
      properties: {
        color: { type: "string" },
        hover: { type: "string" },
        textDecoration: { type: "string" },
        hoverTextDecoration: { type: "string" },
        visited: { type: "string" }
      }
    };
    var Typography2 = {
      properties: {
        code: "CodeConfig",
        fieldName: "FontConfig",
        ...(0, utils_1.pickObjectProps)(FontConfig2.properties, ["fontSize", "fontFamily"]),
        fontWeightBold: { type: "string" },
        fontWeightLight: { type: "string" },
        fontWeightRegular: { type: "string" },
        heading1: "Heading",
        heading2: "Heading",
        heading3: "Heading",
        headings: "HeadingsConfig",
        lineHeight: { type: "string" },
        links: "LinksConfig",
        optimizeSpeed: { type: "boolean" },
        rightPanelHeading: "Heading",
        smoothing: { enum: ["auto", "none", "antialiased", "subpixel-antialiased", "grayscale"] }
      }
    };
    var TokenProps2 = {
      properties: {
        color: { type: "string" },
        ...(0, utils_1.omitObjectProps)(FontConfig2.properties, ["fontWeight"])
      }
    };
    var CodeBlock2 = {
      properties: {
        backgroundColor: { type: "string" },
        borderRadius: { type: "string" },
        tokens: "TokenProps"
      }
    };
    var ConfigThemeLogo2 = {
      properties: {
        gutter: { type: "string" },
        maxHeight: { type: "string" },
        maxWidth: { type: "string" }
      }
    };
    var Fab2 = {
      properties: {
        backgroundColor: { type: "string" },
        color: { type: "string" }
      }
    };
    var ButtonOverrides2 = {
      properties: {
        custom: { type: "string" }
      }
    };
    var Overrides2 = {
      properties: {
        DownloadButton: "ButtonOverrides",
        NextSectionButton: "ButtonOverrides"
      }
    };
    var RightPanel2 = {
      properties: {
        backgroundColor: { type: "string" },
        panelBackgroundColor: { type: "string" },
        panelControlsBackgroundColor: { type: "string" },
        showAtBreakpoint: { type: "string" },
        textColor: { type: "string" },
        width: { type: "string" }
      }
    };
    var Shape2 = {
      properties: { borderRadius: { type: "string" } }
    };
    var ThemeSpacing2 = {
      properties: {
        sectionHorizontal: { type: "number" },
        sectionVertical: { type: "number" },
        unit: { type: "number" }
      }
    };
    var ConfigTheme2 = {
      properties: {
        breakpoints: "Breakpoints",
        codeBlock: "CodeBlock",
        colors: "ThemeColors",
        components: "ConfigThemeComponents",
        layout: "Layout",
        logo: "ConfigThemeLogo",
        fab: "Fab",
        overrides: "Overrides",
        rightPanel: "RightPanel",
        schema: "ConfigThemeSchema",
        shape: "Shape",
        sidebar: "Sidebar",
        spacing: "ThemeSpacing",
        typography: "Typography",
        links: { properties: { color: { type: "string" } } },
        // deprecated
        codeSample: { properties: { backgroundColor: { type: "string" } } }
        // deprecated
      }
    };
    var GenerateCodeSamples2 = {
      properties: {
        skipOptionalParameters: { type: "boolean" },
        languages: (0, _1.listOf)("ConfigLanguage")
      },
      required: ["languages"]
    };
    var ConfigReferenceDocs = {
      properties: {
        theme: "ConfigTheme",
        corsProxyUrl: { type: "string" },
        ctrlFHijack: { type: "boolean" },
        defaultSampleLanguage: { type: "string" },
        disableDeepLinks: { type: "boolean" },
        disableSearch: { type: "boolean" },
        disableSidebar: { type: "boolean" },
        downloadDefinitionUrl: { type: "string" },
        expandDefaultServerVariables: { type: "boolean" },
        enumSkipQuotes: { type: "boolean" },
        expandDefaultRequest: { type: "boolean" },
        expandDefaultResponse: { type: "boolean" },
        expandResponses: { type: "string" },
        expandSingleSchemaField: { type: "boolean" },
        generateCodeSamples: "GenerateCodeSamples",
        generatedPayloadSamplesMaxDepth: { type: "number" },
        hideDownloadButton: { type: "boolean" },
        hideHostname: { type: "boolean" },
        hideInfoSection: { type: "boolean" },
        hideLoading: { type: "boolean" },
        hideLogo: { type: "boolean" },
        hideRequestPayloadSample: { type: "boolean" },
        hideRightPanel: { type: "boolean" },
        hideSchemaPattern: { type: "boolean" },
        hideSchemaTitles: { type: "boolean" },
        hideSingleRequestSampleTab: { type: "boolean" },
        hideSecuritySection: { type: "boolean" },
        hideTryItPanel: { type: "boolean" },
        hideFab: { type: "boolean" },
        hideOneOfDescription: { type: "boolean" },
        htmlTemplate: { type: "string" },
        jsonSampleExpandLevel: (value) => {
          if (typeof value === "number") {
            return { type: "number", minimum: 1 };
          } else {
            return { type: "string" };
          }
        },
        labels: "ConfigLabels",
        layout: { enum: ["stacked", "three-panel"] },
        maxDisplayedEnumValues: { type: "number" },
        menuToggle: { type: "boolean" },
        nativeScrollbars: { type: "boolean" },
        noAutoAuth: { type: "boolean" },
        // deprecated
        oAuth2RedirectURI: { type: "string" },
        onDeepLinkClick: { type: "object" },
        onlyRequiredInSamples: { type: "boolean" },
        pagination: { enum: ["none", "section", "item"] },
        pathInMiddlePanel: { type: "boolean" },
        payloadSampleIdx: { type: "number", minimum: 0 },
        requestInterceptor: { type: "object" },
        requiredPropsFirst: { type: "boolean" },
        routingBasePath: { type: "string" },
        routingStrategy: { type: "string" },
        // deprecated
        samplesTabsMaxCount: { type: "number" },
        schemaExpansionLevel: (value) => {
          if (typeof value === "number") {
            return { type: "number", minimum: 0 };
          } else {
            return { type: "string" };
          }
        },
        schemaDefinitionsTagName: { type: "string" },
        minCharacterLengthToInitSearch: { type: "number", minimum: 1 },
        maxResponseHeadersToShowInTryIt: { type: "number", minimum: 0 },
        scrollYOffset: (value) => {
          if (typeof value === "number") {
            return { type: "number" };
          } else {
            return { type: "string" };
          }
        },
        searchAutoExpand: { type: "boolean" },
        searchFieldLevelBoost: { type: "number", minimum: 0 },
        searchMaxDepth: { type: "number", minimum: 1 },
        searchMode: { enum: ["default", "path-only"] },
        searchOperationTitleBoost: { type: "number" },
        searchTagTitleBoost: { type: "number" },
        sendXUserAgentInTryIt: { type: "boolean" },
        showChangeLayoutButton: { type: "boolean" },
        showConsole: { type: "boolean" },
        // deprecated
        showExtensions: (value) => {
          if (typeof value === "boolean") {
            return { type: "boolean" };
          } else {
            return {
              type: "array",
              items: {
                type: "string"
              }
            };
          }
        },
        showNextButton: { type: "boolean" },
        showRightPanelToggle: { type: "boolean" },
        showSecuritySchemeType: { type: "boolean" },
        showWebhookVerb: { type: "boolean" },
        showObjectSchemaExamples: { type: "boolean" },
        disableTryItRequestUrlEncoding: { type: "boolean" },
        sidebarLinks: "ConfigSidebarLinks",
        sideNavStyle: { enum: ["summary-only", "path-first", "id-only", "path-only"] },
        simpleOneOfTypeLabel: { type: "boolean" },
        sortEnumValuesAlphabetically: { type: "boolean" },
        sortOperationsAlphabetically: { type: "boolean" },
        sortPropsAlphabetically: { type: "boolean" },
        sortTagsAlphabetically: { type: "boolean" },
        suppressWarnings: { type: "boolean" },
        // deprecated
        unstable_externalDescription: { type: "boolean" },
        // deprecated
        unstable_ignoreMimeParameters: { type: "boolean" },
        untrustedDefinition: { type: "boolean" },
        mockServer: {
          properties: {
            url: { type: "string" },
            position: { enum: ["first", "last", "replace", "off"] },
            description: { type: "string" }
          }
        },
        showAccessMode: { type: "boolean" },
        preserveOriginalExtensionsName: { type: "boolean" },
        markdownHeadingsAnchorLevel: { type: "number" }
      },
      additionalProperties: {}
    };
    var ConfigMockServer = {
      properties: {
        strictExamples: { type: "boolean" },
        errorIfForcedExampleNotFound: { type: "boolean" }
      }
    };
    function createConfigTypes(extraSchemas, config) {
      const nodeNames = Object.values(oas_types_1.SpecVersion).flatMap((version) => {
        const types = config?.styleguide ? config.styleguide.extendTypes((0, oas_types_1.getTypes)(version), version) : (0, oas_types_1.getTypes)(version);
        return Object.keys(types);
      });
      const nodeTypes = (0, json_schema_adapter_1.getNodeTypesFromJSONSchema)("rootRedoclyConfigSchema", extraSchemas);
      return {
        ...CoreConfigTypes,
        ConfigRoot: createConfigRoot(nodeTypes),
        // This is the REAL config root type
        ConfigApisProperties: createConfigApisProperties(nodeTypes),
        AssertionDefinitionSubject: createAssertionDefinitionSubject(nodeNames),
        ...nodeTypes
      };
    }
    var CoreConfigTypes = {
      Assert,
      ConfigApis,
      ConfigStyleguide,
      ConfigReferenceDocs,
      ConfigMockServer,
      ConfigHTTP,
      ConfigLanguage: ConfigLanguage2,
      ConfigLabels: ConfigLabels2,
      ConfigSidebarLinks: ConfigSidebarLinks2,
      CommonConfigSidebarLinks: CommonConfigSidebarLinks2,
      ConfigTheme: ConfigTheme2,
      AssertDefinition,
      ThemeColors: ThemeColors2,
      CommonThemeColors: CommonThemeColors2,
      BorderThemeColors: BorderThemeColors2,
      HttpColors: HttpColors2,
      ResponseColors: ResponseColors2,
      SecondaryColors: SecondaryColors2,
      TextThemeColors: TextThemeColors2,
      Sizes: Sizes2,
      ButtonsConfig: ButtonsConfig2,
      CommonColorProps: CommonColorProps2,
      BadgeFontConfig: BadgeFontConfig2,
      BadgeSizes: BadgeSizes2,
      HttpBadgesConfig: HttpBadgesConfig2,
      LabelControls: LabelControls2,
      Panels: Panels2,
      TryItButton: TryItButton2,
      Breakpoints: Breakpoints2,
      StackedConfig: StackedConfig2,
      ThreePanelConfig: ThreePanelConfig2,
      SchemaColorsConfig: SchemaColorsConfig2,
      SizeProps: SizeProps2,
      Level1Items: Level1Items2,
      SpacingConfig: SpacingConfig2,
      FontConfig: FontConfig2,
      CodeConfig: CodeConfig2,
      HeadingsConfig: HeadingsConfig2,
      LinksConfig: LinksConfig2,
      TokenProps: TokenProps2,
      CodeBlock: CodeBlock2,
      ConfigThemeLogo: ConfigThemeLogo2,
      Fab: Fab2,
      ButtonOverrides: ButtonOverrides2,
      Overrides: Overrides2,
      ObjectRule,
      Schema: Schema2,
      RightPanel: RightPanel2,
      Rules,
      Shape: Shape2,
      ThemeSpacing: ThemeSpacing2,
      GenerateCodeSamples: GenerateCodeSamples2,
      GroupItemsConfig: GroupItemsConfig2,
      ConfigThemeComponents: ConfigThemeComponents2,
      Layout: Layout2,
      ConfigThemeSchema: ConfigThemeSchema2,
      Sidebar: Sidebar2,
      Heading: Heading2,
      Typography: Typography2,
      AssertionDefinitionAssertions
    };
    exports.ConfigTypes = createConfigTypes(config_1.rootRedoclyConfigSchema);
    exports.NormalizedConfigTypes = (0, types_1.normalizeTypes)(exports.ConfigTypes);
  }
});

// node_modules/@redocly/openapi-core/lib/bundle.js
var require_bundle = __commonJS({
  "node_modules/@redocly/openapi-core/lib/bundle.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.OasVersion = void 0;
    exports.bundleConfig = bundleConfig;
    exports.bundle = bundle;
    exports.bundleFromString = bundleFromString;
    exports.bundleDocument = bundleDocument;
    exports.mapTypeToComponent = mapTypeToComponent;
    var resolve_1 = require_resolve();
    var visitors_1 = require_visitors();
    var types_1 = require_types();
    var walk_1 = require_walk();
    var oas_types_1 = require_oas_types();
    var ref_utils_1 = require_ref_utils();
    var rules_1 = require_rules();
    var no_unresolved_refs_1 = require_no_unresolved_refs();
    var utils_1 = require_utils();
    var domains_1 = require_domains();
    var remove_unused_components_1 = require_remove_unused_components();
    var remove_unused_components_2 = require_remove_unused_components2();
    var redocly_yaml_1 = require_redocly_yaml();
    var OasVersion;
    (function(OasVersion2) {
      OasVersion2["Version2"] = "oas2";
      OasVersion2["Version3_0"] = "oas3_0";
      OasVersion2["Version3_1"] = "oas3_1";
    })(OasVersion || (exports.OasVersion = OasVersion = {}));
    var bundleVisitor = (0, visitors_1.normalizeVisitors)([
      {
        severity: "error",
        ruleId: "configBundler",
        visitor: {
          ref: {
            leave(node2, ctx, resolved) {
              replaceRef(node2, resolved, ctx);
            }
          }
        }
      }
    ], redocly_yaml_1.NormalizedConfigTypes);
    async function bundleConfig(document2, resolvedRefMap) {
      const ctx = {
        problems: [],
        oasVersion: oas_types_1.SpecVersion.OAS3_0,
        refTypes: /* @__PURE__ */ new Map(),
        visitorsData: {}
      };
      (0, walk_1.walkDocument)({
        document: document2,
        rootType: redocly_yaml_1.NormalizedConfigTypes.ConfigRoot,
        normalizedVisitors: bundleVisitor,
        resolvedRefMap,
        ctx
      });
      return document2.parsed ?? {};
    }
    async function bundle(opts) {
      const { ref, doc, externalRefResolver = new resolve_1.BaseResolver(opts.config.resolve), base = null } = opts;
      if (!(ref || doc)) {
        throw new Error("Document or reference is required.\n");
      }
      const document2 = doc === void 0 ? await externalRefResolver.resolveDocument(base, ref, true) : doc;
      if (document2 instanceof Error) {
        throw document2;
      }
      opts.collectSpecData?.(document2.parsed);
      return bundleDocument({
        document: document2,
        ...opts,
        config: opts.config.styleguide,
        externalRefResolver
      });
    }
    async function bundleFromString(opts) {
      const { source, absoluteRef, externalRefResolver = new resolve_1.BaseResolver(opts.config.resolve) } = opts;
      const document2 = (0, resolve_1.makeDocumentFromString)(source, absoluteRef || "/");
      return bundleDocument({
        document: document2,
        ...opts,
        externalRefResolver,
        config: opts.config.styleguide
      });
    }
    async function bundleDocument(opts) {
      const { document: document2, config, customTypes, externalRefResolver, dereference = false, skipRedoclyRegistryRefs = false, removeUnusedComponents = false, keepUrlRefs = false } = opts;
      const specVersion = (0, oas_types_1.detectSpec)(document2.parsed);
      const specMajorVersion = (0, oas_types_1.getMajorSpecVersion)(specVersion);
      const rules = config.getRulesForSpecVersion(specMajorVersion);
      const types = (0, types_1.normalizeTypes)(config.extendTypes(customTypes ?? (0, oas_types_1.getTypes)(specVersion), specVersion), config);
      const preprocessors = (0, rules_1.initRules)(rules, config, "preprocessors", specVersion);
      const decorators = (0, rules_1.initRules)(rules, config, "decorators", specVersion);
      const ctx = {
        problems: [],
        oasVersion: specVersion,
        refTypes: /* @__PURE__ */ new Map(),
        visitorsData: {}
      };
      if (removeUnusedComponents) {
        decorators.push({
          severity: "error",
          ruleId: "remove-unused-components",
          visitor: specMajorVersion === oas_types_1.SpecMajorVersion.OAS2 ? (0, remove_unused_components_1.RemoveUnusedComponents)({}) : (0, remove_unused_components_2.RemoveUnusedComponents)({})
        });
      }
      let resolvedRefMap = await (0, resolve_1.resolveDocument)({
        rootDocument: document2,
        rootType: types.Root,
        externalRefResolver
      });
      if (preprocessors.length > 0) {
        (0, walk_1.walkDocument)({
          document: document2,
          rootType: types.Root,
          normalizedVisitors: (0, visitors_1.normalizeVisitors)(preprocessors, types),
          resolvedRefMap,
          ctx
        });
        resolvedRefMap = await (0, resolve_1.resolveDocument)({
          rootDocument: document2,
          rootType: types.Root,
          externalRefResolver
        });
      }
      const bundleVisitor2 = (0, visitors_1.normalizeVisitors)([
        {
          severity: "error",
          ruleId: "bundler",
          visitor: makeBundleVisitor(specMajorVersion, dereference, skipRedoclyRegistryRefs, document2, resolvedRefMap, keepUrlRefs)
        },
        ...decorators
      ], types);
      (0, walk_1.walkDocument)({
        document: document2,
        rootType: types.Root,
        normalizedVisitors: bundleVisitor2,
        resolvedRefMap,
        ctx
      });
      return {
        bundle: document2,
        problems: ctx.problems.map((problem) => config.addProblemToIgnore(problem)),
        fileDependencies: externalRefResolver.getFiles(),
        rootType: types.Root,
        refTypes: ctx.refTypes,
        visitorsData: ctx.visitorsData
      };
    }
    function mapTypeToComponent(typeName, version) {
      switch (version) {
        case oas_types_1.SpecMajorVersion.OAS3:
          switch (typeName) {
            case "Schema":
              return "schemas";
            case "Parameter":
              return "parameters";
            case "Response":
              return "responses";
            case "Example":
              return "examples";
            case "RequestBody":
              return "requestBodies";
            case "Header":
              return "headers";
            case "SecuritySchema":
              return "securitySchemes";
            case "Link":
              return "links";
            case "Callback":
              return "callbacks";
            default:
              return null;
          }
        case oas_types_1.SpecMajorVersion.OAS2:
          switch (typeName) {
            case "Schema":
              return "definitions";
            case "Parameter":
              return "parameters";
            case "Response":
              return "responses";
            default:
              return null;
          }
        case oas_types_1.SpecMajorVersion.Async2:
          switch (typeName) {
            case "Schema":
              return "schemas";
            case "Parameter":
              return "parameters";
            default:
              return null;
          }
        case oas_types_1.SpecMajorVersion.Async3:
          switch (typeName) {
            case "Schema":
              return "schemas";
            case "Parameter":
              return "parameters";
            default:
              return null;
          }
        case oas_types_1.SpecMajorVersion.Arazzo1:
          switch (typeName) {
            case "Root.workflows_items.parameters_items":
            case "Root.workflows_items.steps_items.parameters_items":
              return "parameters";
            default:
              return null;
          }
        case oas_types_1.SpecMajorVersion.Overlay1:
          switch (typeName) {
            default:
              return null;
          }
      }
    }
    function replaceRef(ref, resolved, ctx) {
      if (!(0, utils_1.isPlainObject)(resolved.node)) {
        ctx.parent[ctx.key] = resolved.node;
      } else {
        delete ref.$ref;
        const obj = Object.assign({}, resolved.node, ref);
        Object.assign(ref, obj);
      }
    }
    function makeBundleVisitor(version, dereference, skipRedoclyRegistryRefs, rootDocument, resolvedRefMap, keepUrlRefs) {
      let components;
      let rootLocation;
      const visitor = {
        ref: {
          leave(node2, ctx, resolved) {
            if (!resolved.location || resolved.node === void 0) {
              (0, no_unresolved_refs_1.reportUnresolvedRef)(resolved, ctx.report, ctx.location);
              return;
            }
            if (resolved.location.source === rootDocument.source && resolved.location.source === ctx.location.source && ctx.type.name !== "scalar" && !dereference) {
              return;
            }
            if (skipRedoclyRegistryRefs && (0, domains_1.isRedoclyRegistryURL)(node2.$ref)) {
              return;
            }
            if (keepUrlRefs && (0, ref_utils_1.isAbsoluteUrl)(node2.$ref)) {
              return;
            }
            const componentType = mapTypeToComponent(ctx.type.name, version);
            if (!componentType) {
              replaceRef(node2, resolved, ctx);
            } else {
              if (dereference) {
                saveComponent(componentType, resolved, ctx);
                replaceRef(node2, resolved, ctx);
              } else {
                node2.$ref = saveComponent(componentType, resolved, ctx);
                resolveBundledComponent(node2, resolved, ctx);
              }
            }
          }
        },
        Example: {
          leave(node2, ctx) {
            if ((0, ref_utils_1.isExternalValue)(node2) && node2.value === void 0) {
              const resolved = ctx.resolve({ $ref: node2.externalValue });
              if (!resolved.location || resolved.node === void 0) {
                (0, no_unresolved_refs_1.reportUnresolvedRef)(resolved, ctx.report, ctx.location);
                return;
              }
              if (keepUrlRefs && (0, ref_utils_1.isAbsoluteUrl)(node2.externalValue)) {
                return;
              }
              node2.value = ctx.resolve({ $ref: node2.externalValue }).node;
              delete node2.externalValue;
            }
          }
        },
        Root: {
          enter(root, ctx) {
            rootLocation = ctx.location;
            if (version === oas_types_1.SpecMajorVersion.OAS3) {
              components = root.components = root.components || {};
            } else if (version === oas_types_1.SpecMajorVersion.OAS2) {
              components = root;
            } else if (version === oas_types_1.SpecMajorVersion.Async2) {
              components = root.components = root.components || {};
            } else if (version === oas_types_1.SpecMajorVersion.Async3) {
              components = root.components = root.components || {};
            } else if (version === oas_types_1.SpecMajorVersion.Arazzo1) {
              components = root.components = root.components || {};
            }
          }
        }
      };
      if (version === oas_types_1.SpecMajorVersion.OAS3) {
        visitor.DiscriminatorMapping = {
          leave(mapping, ctx) {
            for (const name of Object.keys(mapping)) {
              const $ref = mapping[name];
              const resolved = ctx.resolve({ $ref });
              if (!resolved.location || resolved.node === void 0) {
                (0, no_unresolved_refs_1.reportUnresolvedRef)(resolved, ctx.report, ctx.location.child(name));
                return;
              }
              const componentType = mapTypeToComponent("Schema", version);
              mapping[name] = saveComponent(componentType, resolved, ctx);
            }
          }
        };
      }
      function resolveBundledComponent(node2, resolved, ctx) {
        const newRefId = (0, resolve_1.makeRefId)(ctx.location.source.absoluteRef, node2.$ref);
        resolvedRefMap.set(newRefId, {
          document: rootDocument,
          isRemote: false,
          node: resolved.node,
          nodePointer: node2.$ref,
          resolved: true
        });
      }
      function saveComponent(componentType, target, ctx) {
        components[componentType] = components[componentType] || {};
        const name = getComponentName(target, componentType, ctx);
        components[componentType][name] = target.node;
        if (version === oas_types_1.SpecMajorVersion.OAS3 || version === oas_types_1.SpecMajorVersion.Async2 || version === oas_types_1.SpecMajorVersion.Async3) {
          return `#/components/${componentType}/${name}`;
        } else {
          return `#/${componentType}/${name}`;
        }
      }
      function isEqualOrEqualRef(node2, target, ctx) {
        if ((0, ref_utils_1.isRef)(node2) && ctx.resolve(node2, rootLocation.absolutePointer).location?.absolutePointer === target.location.absolutePointer) {
          return true;
        }
        return (0, utils_1.dequal)(node2, target.node);
      }
      function getComponentName(target, componentType, ctx) {
        const [fileRef, pointer] = [target.location.source.absoluteRef, target.location.pointer];
        const componentsGroup = components[componentType];
        let name = "";
        const refParts = pointer.slice(2).split("/").filter(utils_1.isTruthy);
        while (refParts.length > 0) {
          name = refParts.pop() + (name ? `-${name}` : "");
          if (!componentsGroup || !componentsGroup[name] || isEqualOrEqualRef(componentsGroup[name], target, ctx)) {
            return name;
          }
        }
        name = (0, ref_utils_1.refBaseName)(fileRef) + (name ? `_${name}` : "");
        if (!componentsGroup[name] || isEqualOrEqualRef(componentsGroup[name], target, ctx)) {
          return name;
        }
        const prevName = name;
        let serialId = 2;
        while (componentsGroup[name] && !isEqualOrEqualRef(componentsGroup[name], target, ctx)) {
          name = `${prevName}-${serialId}`;
          serialId++;
        }
        if (!componentsGroup[name]) {
          ctx.report({
            message: `Two schemas are referenced with the same name but different content. Renamed ${prevName} to ${name}.`,
            location: ctx.location,
            forceSeverity: "warn"
          });
        }
        return name;
      }
      return visitor;
    }
  }
});

// node_modules/@redocly/openapi-core/lib/config/utils.js
var require_utils2 = __commonJS({
  "node_modules/@redocly/openapi-core/lib/config/utils.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.ConfigValidationError = void 0;
    exports.parsePresetName = parsePresetName;
    exports.transformApiDefinitionsToApis = transformApiDefinitionsToApis;
    exports.prefixRules = prefixRules;
    exports.mergeExtends = mergeExtends;
    exports.getMergedConfig = getMergedConfig;
    exports.checkForDeprecatedFields = checkForDeprecatedFields;
    exports.transformConfig = transformConfig;
    exports.getResolveConfig = getResolveConfig;
    exports.getUniquePlugins = getUniquePlugins;
    exports.deepCloneMapWithJSON = deepCloneMapWithJSON;
    exports.isDeprecatedPluginFormat = isDeprecatedPluginFormat;
    exports.isCommonJsPlugin = isCommonJsPlugin;
    var utils_1 = require_utils();
    var config_1 = require_config();
    var logger_1 = require_logger();
    function parsePresetName(presetName) {
      if (presetName.indexOf("/") > -1) {
        const [pluginId, configName] = presetName.split("/");
        return { pluginId, configName };
      } else {
        return { pluginId: "", configName: presetName };
      }
    }
    function transformApiDefinitionsToApis(apiDefinitions) {
      if (!apiDefinitions)
        return void 0;
      const apis = {};
      for (const [apiName, apiPath] of Object.entries(apiDefinitions)) {
        apis[apiName] = { root: apiPath };
      }
      return apis;
    }
    function extractFlatConfig({ plugins, extends: _extends4, rules, oas2Rules, oas3_0Rules, oas3_1Rules, async2Rules, async3Rules, arazzo1Rules, overlay1Rules, preprocessors, oas2Preprocessors, oas3_0Preprocessors, oas3_1Preprocessors, async2Preprocessors, async3Preprocessors, arazzo1Preprocessors, overlay1Preprocessors, decorators, oas2Decorators, oas3_0Decorators, oas3_1Decorators, async2Decorators, async3Decorators, arazzo1Decorators, overlay1Decorators, ...rawConfigRest }) {
      const styleguideConfig = {
        plugins,
        extends: _extends4,
        rules,
        oas2Rules,
        oas3_0Rules,
        oas3_1Rules,
        async2Rules,
        async3Rules,
        arazzo1Rules,
        overlay1Rules,
        preprocessors,
        oas2Preprocessors,
        oas3_0Preprocessors,
        oas3_1Preprocessors,
        async2Preprocessors,
        async3Preprocessors,
        arazzo1Preprocessors,
        overlay1Preprocessors,
        decorators,
        oas2Decorators,
        oas3_0Decorators,
        oas3_1Decorators,
        async2Decorators,
        async3Decorators,
        arazzo1Decorators,
        overlay1Decorators,
        doNotResolveExamples: rawConfigRest.resolve?.doNotResolveExamples
      };
      if (rawConfigRest.lint && rawConfigRest.styleguide || Object.values(styleguideConfig).some(utils_1.isDefined) && (rawConfigRest.lint || rawConfigRest.styleguide)) {
        throw new Error(`Do not use 'lint', 'styleguide' and flat syntax together. 
See more about the configuration in the docs: https://redocly.com/docs/cli/configuration/ 
`);
      }
      return {
        styleguideConfig: Object.values(styleguideConfig).some(utils_1.isDefined) ? styleguideConfig : void 0,
        rawConfigRest
      };
    }
    function transformApis(legacyApis) {
      if (!legacyApis)
        return void 0;
      const apis = {};
      for (const [apiName, { lint, ...apiContent }] of Object.entries(legacyApis)) {
        const { styleguideConfig, rawConfigRest } = extractFlatConfig(apiContent);
        apis[apiName] = {
          styleguide: styleguideConfig || lint,
          ...rawConfigRest
        };
      }
      return apis;
    }
    function prefixRules(rules, prefix2) {
      if (!prefix2)
        return rules;
      const res = {};
      for (const name of Object.keys(rules)) {
        res[`${prefix2}/${name}`] = rules[name];
      }
      return res;
    }
    function mergeExtends(rulesConfList) {
      const result = {
        rules: {},
        oas2Rules: {},
        oas3_0Rules: {},
        oas3_1Rules: {},
        async2Rules: {},
        async3Rules: {},
        arazzo1Rules: {},
        overlay1Rules: {},
        preprocessors: {},
        oas2Preprocessors: {},
        oas3_0Preprocessors: {},
        oas3_1Preprocessors: {},
        async2Preprocessors: {},
        async3Preprocessors: {},
        arazzo1Preprocessors: {},
        overlay1Preprocessors: {},
        decorators: {},
        oas2Decorators: {},
        oas3_0Decorators: {},
        oas3_1Decorators: {},
        async2Decorators: {},
        async3Decorators: {},
        arazzo1Decorators: {},
        overlay1Decorators: {},
        plugins: [],
        pluginPaths: [],
        extendPaths: []
      };
      for (const rulesConf of rulesConfList) {
        if (rulesConf.extends) {
          throw new Error(`'extends' is not supported in shared configs yet:
${JSON.stringify(rulesConf, null, 2)}`);
        }
        (0, utils_1.assignConfig)(result.rules, rulesConf.rules);
        (0, utils_1.assignConfig)(result.oas2Rules, rulesConf.oas2Rules);
        (0, utils_1.assignOnlyExistingConfig)(result.oas2Rules, rulesConf.rules);
        (0, utils_1.assignConfig)(result.oas3_0Rules, rulesConf.oas3_0Rules);
        (0, utils_1.assignOnlyExistingConfig)(result.oas3_0Rules, rulesConf.rules);
        (0, utils_1.assignConfig)(result.oas3_1Rules, rulesConf.oas3_1Rules);
        (0, utils_1.assignOnlyExistingConfig)(result.oas3_1Rules, rulesConf.rules);
        (0, utils_1.assignConfig)(result.async2Rules, rulesConf.async2Rules);
        (0, utils_1.assignOnlyExistingConfig)(result.async2Rules, rulesConf.rules);
        (0, utils_1.assignConfig)(result.async3Rules, rulesConf.async3Rules);
        (0, utils_1.assignOnlyExistingConfig)(result.async3Rules, rulesConf.rules);
        (0, utils_1.assignConfig)(result.arazzo1Rules, rulesConf.arazzo1Rules);
        (0, utils_1.assignOnlyExistingConfig)(result.arazzo1Rules, rulesConf.rules);
        (0, utils_1.assignConfig)(result.overlay1Rules, rulesConf.overlay1Rules);
        (0, utils_1.assignOnlyExistingConfig)(result.overlay1Rules, rulesConf.rules);
        (0, utils_1.assignConfig)(result.preprocessors, rulesConf.preprocessors);
        (0, utils_1.assignConfig)(result.oas2Preprocessors, rulesConf.oas2Preprocessors);
        (0, utils_1.assignOnlyExistingConfig)(result.oas2Preprocessors, rulesConf.preprocessors);
        (0, utils_1.assignConfig)(result.oas3_0Preprocessors, rulesConf.oas3_0Preprocessors);
        (0, utils_1.assignOnlyExistingConfig)(result.oas3_0Preprocessors, rulesConf.preprocessors);
        (0, utils_1.assignConfig)(result.oas3_1Preprocessors, rulesConf.oas3_1Preprocessors);
        (0, utils_1.assignOnlyExistingConfig)(result.oas3_1Preprocessors, rulesConf.preprocessors);
        (0, utils_1.assignConfig)(result.async2Preprocessors, rulesConf.async2Preprocessors);
        (0, utils_1.assignOnlyExistingConfig)(result.async2Preprocessors, rulesConf.preprocessors);
        (0, utils_1.assignConfig)(result.async3Preprocessors, rulesConf.async3Preprocessors);
        (0, utils_1.assignOnlyExistingConfig)(result.async3Preprocessors, rulesConf.preprocessors);
        (0, utils_1.assignConfig)(result.arazzo1Preprocessors, rulesConf.arazzo1Preprocessors);
        (0, utils_1.assignOnlyExistingConfig)(result.arazzo1Preprocessors, rulesConf.preprocessors);
        (0, utils_1.assignConfig)(result.overlay1Preprocessors, rulesConf.overlay1Preprocessors);
        (0, utils_1.assignOnlyExistingConfig)(result.overlay1Preprocessors, rulesConf.preprocessors);
        (0, utils_1.assignConfig)(result.decorators, rulesConf.decorators);
        (0, utils_1.assignConfig)(result.oas2Decorators, rulesConf.oas2Decorators);
        (0, utils_1.assignOnlyExistingConfig)(result.oas2Decorators, rulesConf.decorators);
        (0, utils_1.assignConfig)(result.oas3_0Decorators, rulesConf.oas3_0Decorators);
        (0, utils_1.assignOnlyExistingConfig)(result.oas3_0Decorators, rulesConf.decorators);
        (0, utils_1.assignConfig)(result.oas3_1Decorators, rulesConf.oas3_1Decorators);
        (0, utils_1.assignOnlyExistingConfig)(result.oas3_1Decorators, rulesConf.decorators);
        (0, utils_1.assignConfig)(result.async2Decorators, rulesConf.async2Decorators);
        (0, utils_1.assignOnlyExistingConfig)(result.async2Decorators, rulesConf.decorators);
        (0, utils_1.assignConfig)(result.async3Decorators, rulesConf.async3Decorators);
        (0, utils_1.assignOnlyExistingConfig)(result.async3Decorators, rulesConf.decorators);
        (0, utils_1.assignConfig)(result.arazzo1Decorators, rulesConf.arazzo1Decorators);
        (0, utils_1.assignOnlyExistingConfig)(result.arazzo1Decorators, rulesConf.decorators);
        (0, utils_1.assignConfig)(result.overlay1Decorators, rulesConf.overlay1Decorators);
        (0, utils_1.assignOnlyExistingConfig)(result.overlay1Decorators, rulesConf.decorators);
        result.plugins.push(...rulesConf.plugins || []);
        result.pluginPaths.push(...rulesConf.pluginPaths || []);
        result.extendPaths.push(...new Set(rulesConf.extendPaths));
      }
      return result;
    }
    function getMergedConfig(config, apiName) {
      const extendPaths = [
        ...Object.values(config.apis).map((api) => api?.styleguide?.extendPaths),
        config.rawConfig?.styleguide?.extendPaths
      ].flat().filter(utils_1.isTruthy);
      const pluginPaths = [
        ...Object.values(config.apis).map((api) => api?.styleguide?.pluginPaths),
        config.rawConfig?.styleguide?.pluginPaths
      ].flat().filter(utils_1.isTruthy);
      return apiName ? new config_1.Config({
        ...config.rawConfig,
        styleguide: {
          ...config.apis[apiName] ? config.apis[apiName].styleguide : config.rawConfig.styleguide,
          extendPaths,
          pluginPaths
        },
        theme: {
          ...config.rawConfig.theme,
          ...config.apis[apiName]?.theme
        },
        files: [...config.files, ...config.apis?.[apiName]?.files ?? []]
        // TODO: merge everything else here
      }, config.configFile) : config;
    }
    function checkForDeprecatedFields(deprecatedField, updatedField, rawConfig, updatedObject, link) {
      const isDeprecatedFieldInApis = rawConfig.apis && Object.values(rawConfig.apis).some((api) => api[deprecatedField]);
      if (rawConfig[deprecatedField] && updatedField === null) {
        (0, utils_1.showWarningForDeprecatedField)(deprecatedField, void 0, updatedObject, link);
      }
      if (rawConfig[deprecatedField] && updatedField && rawConfig[updatedField]) {
        (0, utils_1.showErrorForDeprecatedField)(deprecatedField, updatedField);
      }
      if (rawConfig[deprecatedField] && updatedObject && rawConfig[updatedObject]) {
        (0, utils_1.showErrorForDeprecatedField)(deprecatedField, updatedField, updatedObject);
      }
      if (rawConfig[deprecatedField] || isDeprecatedFieldInApis) {
        (0, utils_1.showWarningForDeprecatedField)(deprecatedField, updatedField, updatedObject, link);
      }
    }
    function transformConfig(rawConfig) {
      const migratedFields = [
        ["apiDefinitions", "apis", void 0, void 0],
        ["referenceDocs", "openapi", "theme", void 0],
        [
          "lint",
          void 0,
          void 0,
          "https://redocly.com/docs/api-registry/guides/migration-guide-config-file/#changed-properties"
        ],
        [
          "styleguide",
          void 0,
          void 0,
          "https://redocly.com/docs/api-registry/guides/migration-guide-config-file/#changed-properties"
        ],
        ["features.openapi", "openapi", "theme", void 0]
      ];
      for (const [deprecatedField, updatedField, updatedObject, link] of migratedFields) {
        checkForDeprecatedFields(deprecatedField, updatedField, rawConfig, updatedObject, link);
      }
      const { apis, apiDefinitions, referenceDocs, lint, ...rest } = rawConfig;
      const { styleguideConfig, rawConfigRest } = extractFlatConfig(rest);
      const transformedConfig = {
        theme: {
          openapi: {
            ...referenceDocs,
            ...rawConfig["features.openapi"],
            ...rawConfig.theme?.openapi
          },
          mockServer: {
            ...rawConfig["features.mockServer"],
            ...rawConfig.theme?.mockServer
          }
        },
        apis: transformApis(apis) || transformApiDefinitionsToApis(apiDefinitions),
        styleguide: styleguideConfig || lint,
        ...rawConfigRest
      };
      showDeprecationMessages(transformedConfig);
      return transformedConfig;
    }
    function showDeprecationMessages(config) {
      let allRules = { ...config.styleguide?.rules };
      for (const api of Object.values(config.apis || {})) {
        allRules = { ...allRules, ...api?.styleguide?.rules };
      }
      for (const ruleKey of Object.keys(allRules)) {
        if (ruleKey.startsWith("assert/")) {
          logger_1.logger.warn(`
The 'assert/' syntax in ${ruleKey} is deprecated. Update your configuration to use 'rule/' instead. Examples and more information: https://redocly.com/docs/cli/rules/configurable-rules/
`);
        }
      }
    }
    function getResolveConfig(resolve) {
      return {
        http: {
          headers: resolve?.http?.headers ?? [],
          customFetch: void 0
        }
      };
    }
    function getUniquePlugins(plugins) {
      const seen = /* @__PURE__ */ new Set();
      const results = [];
      for (const p2 of plugins) {
        if (!seen.has(p2.id)) {
          results.push(p2);
          seen.add(p2.id);
        } else if (p2.id) {
          logger_1.logger.warn(`Duplicate plugin id "${logger_1.colorize.red(p2.id)}".
`);
        }
      }
      return results;
    }
    var ConfigValidationError = class extends Error {
    };
    exports.ConfigValidationError = ConfigValidationError;
    function deepCloneMapWithJSON(originalMap) {
      return new Map(JSON.parse(JSON.stringify([...originalMap])));
    }
    function isDeprecatedPluginFormat(plugin) {
      return plugin !== void 0 && typeof plugin === "object" && "id" in plugin;
    }
    function isCommonJsPlugin(plugin) {
      return typeof plugin === "function";
    }
  }
});

// node_modules/@redocly/openapi-core/lib/config/config.js
var require_config = __commonJS({
  "node_modules/@redocly/openapi-core/lib/config/config.js"(exports) {
    "use strict";
    Object.defineProperty(exports, "__esModule", { value: true });
    exports.Config = exports.StyleguideConfig = exports.IGNORE_FILE = void 0;
    var fs = require_fs();
    var path = require_path_browserify();
    var js_yaml_1 = require_js_yaml2();
    var utils_1 = require_utils();
    var oas_types_1 = require_oas_types();
    var env_1 = require_env();
    var utils_2 = require_utils2();
    var ref_utils_1 = require_ref_utils();
    exports.IGNORE_FILE = ".redocly.lint-ignore.yaml";
    var IGNORE_BANNER = `# This file instructs Redocly's linter to ignore the rules contained for specific parts of your API.
# See https://redocly.com/docs/cli/ for more information.
`;
    function getIgnoreFilePath(configFile) {
      if (configFile) {
        return (0, utils_1.doesYamlFileExist)(configFile) ? path.join(path.dirname(configFile), exports.IGNORE_FILE) : path.join(configFile, exports.IGNORE_FILE);
      } else {
        return env_1.isBrowser ? void 0 : path.join(process.cwd(), exports.IGNORE_FILE);
      }
    }
    var StyleguideConfig = class {
      constructor(rawConfig, configFile) {
        this.rawConfig = rawConfig;
        this.configFile = configFile;
        this.ignore = {};
        this._usedRules = /* @__PURE__ */ new Set();
        this._usedVersions = /* @__PURE__ */ new Set();
        this.plugins = rawConfig.plugins || [];
        this.doNotResolveExamples = !!rawConfig.doNotResolveExamples;
        this.recommendedFallback = rawConfig.recommendedFallback || false;
        const ruleGroups = [
          "rules",
          "oas2Rules",
          "oas3_0Rules",
          "oas3_1Rules",
          "async2Rules",
          "async3Rules",
          "arazzo1Rules",
          "overlay1Rules"
        ];
        replaceSpecWithStruct(ruleGroups, rawConfig);
        this.rules = {
          [oas_types_1.SpecVersion.OAS2]: { ...rawConfig.rules, ...rawConfig.oas2Rules },
          [oas_types_1.SpecVersion.OAS3_0]: { ...rawConfig.rules, ...rawConfig.oas3_0Rules },
          [oas_types_1.SpecVersion.OAS3_1]: { ...rawConfig.rules, ...rawConfig.oas3_1Rules },
          [oas_types_1.SpecVersion.Async2]: { ...rawConfig.rules, ...rawConfig.async2Rules },
          [oas_types_1.SpecVersion.Async3]: { ...rawConfig.rules, ...rawConfig.async3Rules },
          [oas_types_1.SpecVersion.Arazzo1]: { ...rawConfig.rules, ...rawConfig.arazzo1Rules },
          [oas_types_1.SpecVersion.Overlay1]: { ...rawConfig.rules, ...rawConfig.overlay1Rules }
        };
        this.preprocessors = {
          [oas_types_1.SpecVersion.OAS2]: { ...rawConfig.preprocessors, ...rawConfig.oas2Preprocessors },
          [oas_types_1.SpecVersion.OAS3_0]: { ...rawConfig.preprocessors, ...rawConfig.oas3_0Preprocessors },
          [oas_types_1.SpecVersion.OAS3_1]: { ...rawConfig.preprocessors, ...rawConfig.oas3_1Preprocessors },
          [oas_types_1.SpecVersion.Async2]: { ...rawConfig.preprocessors, ...rawConfig.async2Preprocessors },
          [oas_types_1.SpecVersion.Async3]: { ...rawConfig.preprocessors, ...rawConfig.async3Preprocessors },
          [oas_types_1.SpecVersion.Arazzo1]: { ...rawConfig.arazzo1Preprocessors },
          [oas_types_1.SpecVersion.Overlay1]: { ...rawConfig.preprocessors, ...rawConfig.overlay1Preprocessors }
        };
        this.decorators = {
          [oas_types_1.SpecVersion.OAS2]: { ...rawConfig.decorators, ...rawConfig.oas2Decorators },
          [oas_types_1.SpecVersion.OAS3_0]: { ...rawConfig.decorators, ...rawConfig.oas3_0Decorators },
          [oas_types_1.SpecVersion.OAS3_1]: { ...rawConfig.decorators, ...rawConfig.oas3_1Decorators },
          [oas_types_1.SpecVersion.Async2]: { ...rawConfig.decorators, ...rawConfig.async2Decorators },
          [oas_types_1.SpecVersion.Async3]: { ...rawConfig.decorators, ...rawConfig.async3Decorators },
          [oas_types_1.SpecVersion.Arazzo1]: { ...rawConfig.arazzo1Decorators },
          [oas_types_1.SpecVersion.Overlay1]: { ...rawConfig.decorators, ...rawConfig.overlay1Decorators }
        };
        this.extendPaths = rawConfig.extendPaths || [];
        this.pluginPaths = rawConfig.pluginPaths || [];
        this.resolveIgnore(getIgnoreFilePath(configFile));
      }
      resolveIgnore(ignoreFile) {
        if (!ignoreFile || !(0, utils_1.doesYamlFileExist)(ignoreFile))
          return;
        this.ignore = (0, js_yaml_1.parseYaml)(fs.readFileSync(ignoreFile, "utf-8")) || {};
        replaceSpecWithStruct(Object.keys(this.ignore), this.ignore);
        for (const fileName of Object.keys(this.ignore)) {
          this.ignore[(0, ref_utils_1.isAbsoluteUrl)(fileName) ? fileName : path.resolve(path.dirname(ignoreFile), fileName)] = this.ignore[fileName];
          for (const ruleId of Object.keys(this.ignore[fileName])) {
            this.ignore[fileName][ruleId] = new Set(this.ignore[fileName][ruleId]);
          }
          if (!(0, ref_utils_1.isAbsoluteUrl)(fileName)) {
            delete this.ignore[fileName];
          }
        }
      }
      saveIgnore() {
        const dir = this.configFile ? path.dirname(this.configFile) : process.cwd();
        const ignoreFile = path.join(dir, exports.IGNORE_FILE);
        const mapped = {};
        for (const absFileName of Object.keys(this.ignore)) {
          const mappedDefinitionName = (0, ref_utils_1.isAbsoluteUrl)(absFileName) ? absFileName : (0, utils_1.slash)(path.relative(dir, absFileName));
          const ignoredRules = mapped[mappedDefinitionName] = this.ignore[absFileName];
          for (const ruleId of Object.keys(ignoredRules)) {
            ignoredRules[ruleId] = Array.from(ignoredRules[ruleId]);
          }
        }
        fs.writeFileSync(ignoreFile, IGNORE_BANNER + (0, js_yaml_1.stringifyYaml)(mapped));
      }
      addIgnore(problem) {
        const ignore = this.ignore;
        const loc = problem.location[0];
        if (loc.pointer === void 0)
          return;
        const fileIgnore = ignore[loc.source.absoluteRef] = ignore[loc.source.absoluteRef] || {};
        const ruleIgnore = fileIgnore[problem.ruleId] = fileIgnore[problem.ruleId] || /* @__PURE__ */ new Set();
        ruleIgnore.add(loc.pointer);
      }
      addProblemToIgnore(problem) {
        const loc = problem.location[0];
        if (loc.pointer === void 0)
          return problem;
        const fileIgnore = this.ignore[loc.source.absoluteRef] || {};
        const ruleIgnore = fileIgnore[problem.ruleId];
        const ignored = ruleIgnore && ruleIgnore.has(loc.pointer);
        return ignored ? {
          ...problem,
          ignored
        } : problem;
      }
      extendTypes(types, version) {
        let extendedTypes = types;
        for (const plugin of this.plugins) {
          if (plugin.typeExtension !== void 0) {
            switch (version) {
              case oas_types_1.SpecVersion.OAS3_0:
              case oas_types_1.SpecVersion.OAS3_1:
                if (!plugin.typeExtension.oas3)
                  continue;
                extendedTypes = plugin.typeExtension.oas3(extendedTypes, version);
                break;
              case oas_types_1.SpecVersion.OAS2:
                if (!plugin.typeExtension.oas2)
                  continue;
                extendedTypes = plugin.typeExtension.oas2(extendedTypes, version);
                break;
              case oas_types_1.SpecVersion.Async2:
                if (!plugin.typeExtension.async2)
                  continue;
                extendedTypes = plugin.typeExtension.async2(extendedTypes, version);
                break;
              case oas_types_1.SpecVersion.Async3:
                if (!plugin.typeExtension.async3)
                  continue;
                extendedTypes = plugin.typeExtension.async3(extendedTypes, version);
                break;
              case oas_types_1.SpecVersion.Arazzo1:
                if (!plugin.typeExtension.arazzo1)
                  continue;
                extendedTypes = plugin.typeExtension.arazzo1(extendedTypes, version);
                break;
              case oas_types_1.SpecVersion.Overlay1:
                if (!plugin.typeExtension.overlay1)
                  continue;
                extendedTypes = plugin.typeExtension.overlay1(extendedTypes, version);
                break;
              default:
                throw new Error("Not implemented");
            }
          }
        }
        return extendedTypes;
      }
      getRuleSettings(ruleId, oasVersion) {
        this._usedRules.add(ruleId);
        this._usedVersions.add(oasVersion);
        const settings = this.rules[oasVersion][ruleId] || "off";
        if (typeof settings === "string") {
          return {
            severity: settings
          };
        } else {
          return { severity: "error", ...settings };
        }
      }
      getPreprocessorSettings(ruleId, oasVersion) {
        this._usedRules.add(ruleId);
        this._usedVersions.add(oasVersion);
        const settings = this.preprocessors[oasVersion][ruleId] || "off";
        if (typeof settings === "string") {
          return {
            severity: settings === "on" ? "error" : settings
          };
        } else {
          return { severity: "error", ...settings };
        }
      }
      getDecoratorSettings(ruleId, oasVersion) {
        this._usedRules.add(ruleId);
        this._usedVersions.add(oasVersion);
        const settings = this.decorators[oasVersion][ruleId] || "off";
        if (typeof settings === "string") {
          return {
            severity: settings === "on" ? "error" : settings
          };
        } else {
          return { severity: "error", ...settings };
        }
      }
      getUnusedRules() {
        const rules = [];
        const decorators = [];
        const preprocessors = [];
        for (const usedVersion of Array.from(this._usedVersions)) {
          rules.push(...Object.keys(this.rules[usedVersion]).filter((name) => !this._usedRules.has(name)));
          decorators.push(...Object.keys(this.decorators[usedVersion]).filter((name) => !this._usedRules.has(name)));
          preprocessors.push(...Object.keys(this.preprocessors[usedVersion]).filter((name) => !this._usedRules.has(name)));
        }
        return {
          rules,
          preprocessors,
          decorators
        };
      }
      getRulesForSpecVersion(version) {
        switch (version) {
          case oas_types_1.SpecMajorVersion.OAS3:
            const oas3Rules = [];
            this.plugins.forEach((p2) => p2.preprocessors?.oas3 && oas3Rules.push(p2.preprocessors.oas3));
            this.plugins.forEach((p2) => p2.rules?.oas3 && oas3Rules.push(p2.rules.oas3));
            this.plugins.forEach((p2) => p2.decorators?.oas3 && oas3Rules.push(p2.decorators.oas3));
            return oas3Rules;
          case oas_types_1.SpecMajorVersion.OAS2:
            const oas2Rules = [];
            this.plugins.forEach((p2) => p2.preprocessors?.oas2 && oas2Rules.push(p2.preprocessors.oas2));
            this.plugins.forEach((p2) => p2.rules?.oas2 && oas2Rules.push(p2.rules.oas2));
            this.plugins.forEach((p2) => p2.decorators?.oas2 && oas2Rules.push(p2.decorators.oas2));
            return oas2Rules;
          case oas_types_1.SpecMajorVersion.Async2:
            const asyncApi2Rules = [];
            this.plugins.forEach((p2) => p2.preprocessors?.async2 && asyncApi2Rules.push(p2.preprocessors.async2));
            this.plugins.forEach((p2) => p2.rules?.async2 && asyncApi2Rules.push(p2.rules.async2));
            this.plugins.forEach((p2) => p2.decorators?.async2 && asyncApi2Rules.push(p2.decorators.async2));
            return asyncApi2Rules;
          case oas_types_1.SpecMajorVersion.Async3:
            const asyncApi3Rules = [];
            this.plugins.forEach((p2) => p2.preprocessors?.async3 && asyncApi3Rules.push(p2.preprocessors.async3));
            this.plugins.forEach((p2) => p2.rules?.async3 && asyncApi3Rules.push(p2.rules.async3));
            this.plugins.forEach((p2) => p2.decorators?.async3 && asyncApi3Rules.push(p2.decorators.async3));
            return asyncApi3Rules;
          case oas_types_1.SpecMajorVersion.Arazzo1:
            const arazzo1Rules = [];
            this.plugins.forEach((p2) => p2.preprocessors?.arazzo1 && arazzo1Rules.push(p2.preprocessors.arazzo1));
            this.plugins.forEach((p2) => p2.rules?.arazzo1 && arazzo1Rules.push(p2.rules.arazzo1));
            this.plugins.forEach((p2) => p2.decorators?.arazzo1 && arazzo1Rules.push(p2.decorators.arazzo1));
            return arazzo1Rules;
          case oas_types_1.SpecMajorVersion.Overlay1:
            const overlay1Rules = [];
            this.plugins.forEach((p2) => p2.preprocessors?.overlay1 && overlay1Rules.push(p2.preprocessors.overlay1));
            this.plugins.forEach((p2) => p2.rules?.overlay1 && overlay1Rules.push(p2.rules.overlay1));
            this.plugins.forEach((p2) => p2.decorators?.overlay1 && overlay1Rules.push(p2.decorators.overlay1));
            return overlay1Rules;
        }
      }
      skipRules(rules) {
        for (const ruleId of rules || []) {
          for (const version of Object.values(oas_types_1.SpecVersion)) {
            if (this.rules[version][ruleId]) {
              this.rules[version][ruleId] = "off";
            } else if (Array.isArray(this.rules[version].assertions)) {
              for (const configurableRule of this.rules[version].assertions) {
                if (configurableRule.assertionId === ruleId) {
                  configurableRule.severity = "off";
                }
              }
            }
          }
        }
      }
      skipPreprocessors(preprocessors) {
        for (const preprocessorId of preprocessors || []) {
          for (const version of Object.values(oas_types_1.SpecVersion)) {
            if (this.preprocessors[version][preprocessorId]) {
              this.preprocessors[version][preprocessorId] = "off";
            }
          }
        }
      }
      skipDecorators(decorators) {
        for (const decoratorId of decorators || []) {
          for (const version of Object.values(oas_types_1.SpecVersion)) {
            if (this.decorators[version][decoratorId]) {
              this.decorators[version][decoratorId] = "off";
            }
          }
        }
      }
    };
    exports.StyleguideConfig = StyleguideConfig;
    function replaceSpecWithStruct(ruleGroups, config) {
      for (const ruleGroup of ruleGroups) {
        if (config[ruleGroup] && (0, utils_1.isPlainObject)(config[ruleGroup]) && "spec" in config[ruleGroup]) {
          (0, utils_1.showWarningForDeprecatedField)("spec", "struct");
          config[ruleGroup].struct = config[ruleGroup].spec;
          delete config[ruleGroup].spec;
        }
      }
    }
    var Config = class {
      constructor(rawConfig, configFile) {
        this.rawConfig = rawConfig;
        this.configFile = configFile;
        this.apis = rawConfig.apis || {};
        this.styleguide = new StyleguideConfig(rawConfig.styleguide || {}, configFile);
        this.theme = rawConfig.theme || {};
        this.resolve = (0, utils_2.getResolveConfig)(rawConfig?.resolve);
        this.region = rawConfig.region;
        this.organization = rawConfig.organization;
        this.files = rawConfig.files || [];
        this.telemetry = rawConfig.telemetry;
      }
    };
    exports.Config = Config;
  }
});

// node_modules/decko/dist/decko.js
var require_decko = __commonJS({
  "node_modules/decko/dist/decko.js"(exports) {
    (function(global2, factory) {
      if (typeof define === "function" && define.amd) {
        define(["exports"], factory);
      } else if (typeof exports !== "undefined") {
        factory(exports);
      } else {
        var mod = { exports: {} };
        factory(mod.exports);
        global2.decko = mod.exports;
      }
    })(exports, function(exports2) {
      "use strict";
      exports2.__esModule = true;
      var EMPTY = {};
      var HOP = Object.prototype.hasOwnProperty;
      var fns = { memoize: function memoize3(fn) {
        var opt = arguments.length <= 1 || arguments[1] === void 0 ? EMPTY : arguments[1];
        var cache2 = opt.cache || {};
        return function() {
          for (var _len = arguments.length, a2 = Array(_len), _key = 0; _key < _len; _key++) {
            a2[_key] = arguments[_key];
          }
          var k2 = String(a2[0]);
          if (opt.caseSensitive === false) k2 = k2.toLowerCase();
          return HOP.call(cache2, k2) ? cache2[k2] : cache2[k2] = fn.apply(this, a2);
        };
      }, debounce: function debounce2(fn, opts) {
        if (typeof opts === "function") {
          var p2 = fn;
          fn = opts;
          opts = p2;
        }
        var delay = opts && opts.delay || opts || 0, args = void 0, context = void 0, timer = void 0;
        return function() {
          for (var _len2 = arguments.length, a2 = Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
            a2[_key2] = arguments[_key2];
          }
          args = a2;
          context = this;
          if (!timer) timer = setTimeout(function() {
            fn.apply(context, args);
            args = context = timer = null;
          }, delay);
        };
      }, bind: function bind4(target, key2, _ref) {
        var fn = _ref.value;
        return { configurable: true, get: function get5() {
          var value = fn.bind(this);
          Object.defineProperty(this, key2, { value, configurable: true, writable: true });
          return value;
        } };
      } };
      var memoize2 = multiMethod(fns.memoize), debounce = multiMethod(fns.debounce), bind3 = multiMethod(function(f2, c2) {
        return f2.bind(c2);
      }, function() {
        return fns.bind;
      });
      exports2.memoize = memoize2;
      exports2.debounce = debounce;
      exports2.bind = bind3;
      exports2["default"] = { memoize: memoize2, debounce, bind: bind3 };
      function multiMethod(inner, deco) {
        deco = deco || inner.decorate || decorator(inner);
        var d = deco();
        return function() {
          for (var _len3 = arguments.length, args = Array(_len3), _key3 = 0; _key3 < _len3; _key3++) {
            args[_key3] = arguments[_key3];
          }
          var l2 = args.length;
          return (l2 < 2 ? deco : l2 > 2 ? d : inner).apply(void 0, args);
        };
      }
      function decorator(fn) {
        return function(opt) {
          return typeof opt === "function" ? fn(opt) : function(target, key2, desc) {
            desc.value = fn(desc.value, opt, target, key2, desc);
          };
        };
      }
    });
  }
});

// node_modules/eventemitter3/index.js
var require_eventemitter3 = __commonJS({
  "node_modules/eventemitter3/index.js"(exports, module) {
    "use strict";
    var has3 = Object.prototype.hasOwnProperty;
    var prefix2 = "~";
    function Events() {
    }
    if (Object.create) {
      Events.prototype = /* @__PURE__ */ Object.create(null);
      if (!new Events().__proto__) prefix2 = false;
    }
    function EE(fn, context, once3) {
      this.fn = fn;
      this.context = context;
      this.once = once3 || false;
    }
    function addListener(emitter, event, fn, context, once3) {
      if (typeof fn !== "function") {
        throw new TypeError("The listener must be a function");
      }
      var listener = new EE(fn, context || emitter, once3), evt = prefix2 ? prefix2 + event : event;
      if (!emitter._events[evt]) emitter._events[evt] = listener, emitter._eventsCount++;
      else if (!emitter._events[evt].fn) emitter._events[evt].push(listener);
      else emitter._events[evt] = [emitter._events[evt], listener];
      return emitter;
    }
    function clearEvent(emitter, evt) {
      if (--emitter._eventsCount === 0) emitter._events = new Events();
      else delete emitter._events[evt];
    }
    function EventEmitter() {
      this._events = new Events();
      this._eventsCount = 0;
    }
    EventEmitter.prototype.eventNames = function eventNames() {
      var names = [], events, name;
      if (this._eventsCount === 0) return names;
      for (name in events = this._events) {
        if (has3.call(events, name)) names.push(prefix2 ? name.slice(1) : name);
      }
      if (Object.getOwnPropertySymbols) {
        return names.concat(Object.getOwnPropertySymbols(events));
      }
      return names;
    };
    EventEmitter.prototype.listeners = function listeners(event) {
      var evt = prefix2 ? prefix2 + event : event, handlers2 = this._events[evt];
      if (!handlers2) return [];
      if (handlers2.fn) return [handlers2.fn];
      for (var i2 = 0, l2 = handlers2.length, ee2 = new Array(l2); i2 < l2; i2++) {
        ee2[i2] = handlers2[i2].fn;
      }
      return ee2;
    };
    EventEmitter.prototype.listenerCount = function listenerCount(event) {
      var evt = prefix2 ? prefix2 + event : event, listeners = this._events[evt];
      if (!listeners) return 0;
      if (listeners.fn) return 1;
      return listeners.length;
    };
    EventEmitter.prototype.emit = function emit(event, a1, a2, a3, a4, a5) {
      var evt = prefix2 ? prefix2 + event : event;
      if (!this._events[evt]) return false;
      var listeners = this._events[evt], len = arguments.length, args, i2;
      if (listeners.fn) {
        if (listeners.once) this.removeListener(event, listeners.fn, void 0, true);
        switch (len) {
          case 1:
            return listeners.fn.call(listeners.context), true;
          case 2:
            return listeners.fn.call(listeners.context, a1), true;
          case 3:
            return listeners.fn.call(listeners.context, a1, a2), true;
          case 4:
            return listeners.fn.call(listeners.context, a1, a2, a3), true;
          case 5:
            return listeners.fn.call(listeners.context, a1, a2, a3, a4), true;
          case 6:
            return listeners.fn.call(listeners.context, a1, a2, a3, a4, a5), true;
        }
        for (i2 = 1, args = new Array(len - 1); i2 < len; i2++) {
          args[i2 - 1] = arguments[i2];
        }
        listeners.fn.apply(listeners.context, args);
      } else {
        var length2 = listeners.length, j2;
        for (i2 = 0; i2 < length2; i2++) {
          if (listeners[i2].once) this.removeListener(event, listeners[i2].fn, void 0, true);
          switch (len) {
            case 1:
              listeners[i2].fn.call(listeners[i2].context);
              break;
            case 2:
              listeners[i2].fn.call(listeners[i2].context, a1);
              break;
            case 3:
              listeners[i2].fn.call(listeners[i2].context, a1, a2);
              break;
            case 4:
              listeners[i2].fn.call(listeners[i2].context, a1, a2, a3);
              break;
            default:
              if (!args) for (j2 = 1, args = new Array(len - 1); j2 < len; j2++) {
                args[j2 - 1] = arguments[j2];
              }
              listeners[i2].fn.apply(listeners[i2].context, args);
          }
        }
      }
      return true;
    };
    EventEmitter.prototype.on = function on(event, fn, context) {
      return addListener(this, event, fn, context, false);
    };
    EventEmitter.prototype.once = function once3(event, fn, context) {
      return addListener(this, event, fn, context, true);
    };
    EventEmitter.prototype.removeListener = function removeListener(event, fn, context, once3) {
      var evt = prefix2 ? prefix2 + event : event;
      if (!this._events[evt]) return this;
      if (!fn) {
        clearEvent(this, evt);
        return this;
      }
      var listeners = this._events[evt];
      if (listeners.fn) {
        if (listeners.fn === fn && (!once3 || listeners.once) && (!context || listeners.context === context)) {
          clearEvent(this, evt);
        }
      } else {
        for (var i2 = 0, events = [], length2 = listeners.length; i2 < length2; i2++) {
          if (listeners[i2].fn !== fn || once3 && !listeners[i2].once || context && listeners[i2].context !== context) {
            events.push(listeners[i2]);
          }
        }
        if (events.length) this._events[evt] = events.length === 1 ? events[0] : events;
        else clearEvent(this, evt);
      }
      return this;
    };
    EventEmitter.prototype.removeAllListeners = function removeAllListeners(event) {
      var evt;
      if (event) {
        evt = prefix2 ? prefix2 + event : event;
        if (this._events[evt]) clearEvent(this, evt);
      } else {
        this._events = new Events();
        this._eventsCount = 0;
      }
      return this;
    };
    EventEmitter.prototype.off = EventEmitter.prototype.removeListener;
    EventEmitter.prototype.addListener = EventEmitter.prototype.on;
    EventEmitter.prefixed = prefix2;
    EventEmitter.EventEmitter = EventEmitter;
    if ("undefined" !== typeof module) {
      module.exports = EventEmitter;
    }
  }
});

// node_modules/foreach/index.js
var require_foreach = __commonJS({
  "node_modules/foreach/index.js"(exports, module) {
    var hasOwn = Object.prototype.hasOwnProperty;
    var toString2 = Object.prototype.toString;
    module.exports = function forEach(obj, fn, ctx) {
      if (toString2.call(fn) !== "[object Function]") {
        throw new TypeError("iterator must be a function");
      }
      var l2 = obj.length;
      if (l2 === +l2) {
        for (var i2 = 0; i2 < l2; i2++) {
          fn.call(ctx, obj[i2], i2, obj);
        }
      } else {
        for (var k2 in obj) {
          if (hasOwn.call(obj, k2)) {
            fn.call(ctx, obj[k2], k2, obj);
          }
        }
      }
    };
  }
});

// node_modules/json-pointer/index.js
var require_json_pointer = __commonJS({
  "node_modules/json-pointer/index.js"(exports, module) {
    "use strict";
    var each = require_foreach();
    module.exports = api;
    function api(obj, pointer, value) {
      if (arguments.length === 3) {
        return api.set(obj, pointer, value);
      }
      if (arguments.length === 2) {
        return api.get(obj, pointer);
      }
      var wrapped = api.bind(api, obj);
      for (var name in api) {
        if (api.hasOwnProperty(name)) {
          wrapped[name] = api[name].bind(wrapped, obj);
        }
      }
      return wrapped;
    }
    api.get = function get5(obj, pointer) {
      var refTokens = Array.isArray(pointer) ? pointer : api.parse(pointer);
      for (var i2 = 0; i2 < refTokens.length; ++i2) {
        var tok = refTokens[i2];
        if (!(typeof obj == "object" && tok in obj)) {
          throw new Error("Invalid reference token: " + tok);
        }
        obj = obj[tok];
      }
      return obj;
    };
    api.set = function set7(obj, pointer, value) {
      var refTokens = Array.isArray(pointer) ? pointer : api.parse(pointer), nextTok = refTokens[0];
      if (refTokens.length === 0) {
        throw Error("Can not set the root object");
      }
      for (var i2 = 0; i2 < refTokens.length - 1; ++i2) {
        var tok = refTokens[i2];
        if (typeof tok !== "string" && typeof tok !== "number") {
          tok = String(tok);
        }
        if (tok === "__proto__" || tok === "constructor" || tok === "prototype") {
          continue;
        }
        if (tok === "-" && Array.isArray(obj)) {
          tok = obj.length;
        }
        nextTok = refTokens[i2 + 1];
        if (!(tok in obj)) {
          if (nextTok.match(/^(\d+|-)$/)) {
            obj[tok] = [];
          } else {
            obj[tok] = {};
          }
        }
        obj = obj[tok];
      }
      if (nextTok === "-" && Array.isArray(obj)) {
        nextTok = obj.length;
      }
      obj[nextTok] = value;
      return this;
    };
    api.remove = function(obj, pointer) {
      var refTokens = Array.isArray(pointer) ? pointer : api.parse(pointer);
      var finalToken = refTokens[refTokens.length - 1];
      if (finalToken === void 0) {
        throw new Error('Invalid JSON pointer for remove: "' + pointer + '"');
      }
      var parent = api.get(obj, refTokens.slice(0, -1));
      if (Array.isArray(parent)) {
        var index = +finalToken;
        if (finalToken === "" && isNaN(index)) {
          throw new Error('Invalid array index: "' + finalToken + '"');
        }
        Array.prototype.splice.call(parent, index, 1);
      } else {
        delete parent[finalToken];
      }
    };
    api.dict = function dict(obj, descend) {
      var results = {};
      api.walk(obj, function(value, pointer) {
        results[pointer] = value;
      }, descend);
      return results;
    };
    api.walk = function walk(obj, iterator, descend) {
      var refTokens = [];
      descend = descend || function(value) {
        var type = Object.prototype.toString.call(value);
        return type === "[object Object]" || type === "[object Array]";
      };
      (function next2(cur) {
        each(cur, function(value, key2) {
          refTokens.push(String(key2));
          if (descend(value)) {
            next2(value);
          } else {
            iterator(value, api.compile(refTokens));
          }
          refTokens.pop();
        });
      })(obj);
    };
    api.has = function has3(obj, pointer) {
      try {
        api.get(obj, pointer);
      } catch (e) {
        return false;
      }
      return true;
    };
    api.escape = function escape(str) {
      return str.toString().replace(/~/g, "~0").replace(/\//g, "~1");
    };
    api.unescape = function unescape2(str) {
      return str.replace(/~1/g, "/").replace(/~0/g, "~");
    };
    api.parse = function parse3(pointer) {
      if (pointer === "") {
        return [];
      }
      if (pointer.charAt(0) !== "/") {
        throw new Error("Invalid JSON pointer: " + pointer);
      }
      return pointer.substring(1).split(/\//).map(api.unescape);
    };
    api.compile = function compile2(refTokens) {
      if (refTokens.length === 0) {
        return "";
      }
      return "/" + refTokens.map(api.escape).join("/");
    };
  }
});

// node_modules/url-template/lib/url-template.js
var require_url_template = __commonJS({
  "node_modules/url-template/lib/url-template.js"(exports, module) {
    (function(root, factory) {
      if (typeof exports === "object") {
        module.exports = factory();
      } else if (typeof define === "function" && define.amd) {
        define([], factory);
      } else {
        root.urltemplate = factory();
      }
    })(exports, function() {
      function UrlTemplate() {
      }
      UrlTemplate.prototype.encodeReserved = function(str) {
        return str.split(/(%[0-9A-Fa-f]{2})/g).map(function(part) {
          if (!/%[0-9A-Fa-f]/.test(part)) {
            part = encodeURI(part).replace(/%5B/g, "[").replace(/%5D/g, "]");
          }
          return part;
        }).join("");
      };
      UrlTemplate.prototype.encodeUnreserved = function(str) {
        return encodeURIComponent(str).replace(/[!'()*]/g, function(c2) {
          return "%" + c2.charCodeAt(0).toString(16).toUpperCase();
        });
      };
      UrlTemplate.prototype.encodeValue = function(operator, value, key2) {
        value = operator === "+" || operator === "#" ? this.encodeReserved(value) : this.encodeUnreserved(value);
        if (key2) {
          return this.encodeUnreserved(key2) + "=" + value;
        } else {
          return value;
        }
      };
      UrlTemplate.prototype.isDefined = function(value) {
        return value !== void 0 && value !== null;
      };
      UrlTemplate.prototype.isKeyOperator = function(operator) {
        return operator === ";" || operator === "&" || operator === "?";
      };
      UrlTemplate.prototype.getValues = function(context, operator, key2, modifier) {
        var value = context[key2], result = [];
        if (this.isDefined(value) && value !== "") {
          if (typeof value === "string" || typeof value === "number" || typeof value === "boolean") {
            value = value.toString();
            if (modifier && modifier !== "*") {
              value = value.substring(0, parseInt(modifier, 10));
            }
            result.push(this.encodeValue(operator, value, this.isKeyOperator(operator) ? key2 : null));
          } else {
            if (modifier === "*") {
              if (Array.isArray(value)) {
                value.filter(this.isDefined).forEach(function(value2) {
                  result.push(this.encodeValue(operator, value2, this.isKeyOperator(operator) ? key2 : null));
                }, this);
              } else {
                Object.keys(value).forEach(function(k2) {
                  if (this.isDefined(value[k2])) {
                    result.push(this.encodeValue(operator, value[k2], k2));
                  }
                }, this);
              }
            } else {
              var tmp = [];
              if (Array.isArray(value)) {
                value.filter(this.isDefined).forEach(function(value2) {
                  tmp.push(this.encodeValue(operator, value2));
                }, this);
              } else {
                Object.keys(value).forEach(function(k2) {
                  if (this.isDefined(value[k2])) {
                    tmp.push(this.encodeUnreserved(k2));
                    tmp.push(this.encodeValue(operator, value[k2].toString()));
                  }
                }, this);
              }
              if (this.isKeyOperator(operator)) {
                result.push(this.encodeUnreserved(key2) + "=" + tmp.join(","));
              } else if (tmp.length !== 0) {
                result.push(tmp.join(","));
              }
            }
          }
        } else {
          if (operator === ";") {
            if (this.isDefined(value)) {
              result.push(this.encodeUnreserved(key2));
            }
          } else if (value === "" && (operator === "&" || operator === "?")) {
            result.push(this.encodeUnreserved(key2) + "=");
          } else if (value === "") {
            result.push("");
          }
        }
        return result;
      };
      UrlTemplate.prototype.parse = function(template2) {
        var that = this;
        var operators = ["+", "#", ".", "/", ";", "?", "&"];
        return {
          expand: function(context) {
            return template2.replace(/\{([^\{\}]+)\}|([^\{\}]+)/g, function(_15, expression, literal) {
              if (expression) {
                var operator = null, values2 = [];
                if (operators.indexOf(expression.charAt(0)) !== -1) {
                  operator = expression.charAt(0);
                  expression = expression.substr(1);
                }
                expression.split(/,/g).forEach(function(variable) {
                  var tmp = /([^:\*]*)(?::(\d+)|(\*))?/.exec(variable);
                  values2.push.apply(values2, that.getValues(context, operator, tmp[1], tmp[2] || tmp[3]));
                });
                if (operator && operator !== "+") {
                  var separator = ",";
                  if (operator === "?") {
                    separator = "&";
                  } else if (operator !== "#") {
                    separator = operator;
                  }
                  return (values2.length !== 0 ? operator : "") + values2.join(separator);
                } else {
                  return values2.join(",");
                }
              } else {
                return that.encodeReserved(literal);
              }
            });
          }
        };
      };
      return new UrlTemplate();
    });
  }
});

// node_modules/prismjs/prism.js
var require_prism = __commonJS({
  "node_modules/prismjs/prism.js"(exports, module) {
    var _self = typeof window !== "undefined" ? window : typeof WorkerGlobalScope !== "undefined" && self instanceof WorkerGlobalScope ? self : {};
    var Prism2 = (function(_self2) {
      var lang = /(?:^|\s)lang(?:uage)?-([\w-]+)(?=\s|$)/i;
      var uniqueId = 0;
      var plainTextGrammar = {};
      var _15 = {
        /**
         * By default, Prism will attempt to highlight all code elements (by calling {@link Prism.highlightAll}) on the
         * current page after the page finished loading. This might be a problem if e.g. you wanted to asynchronously load
         * additional languages or plugins yourself.
         *
         * By setting this value to `true`, Prism will not automatically highlight all code elements on the page.
         *
         * You obviously have to change this value before the automatic highlighting started. To do this, you can add an
         * empty Prism object into the global scope before loading the Prism script like this:
         *
         * ```js
         * window.Prism = window.Prism || {};
         * Prism.manual = true;
         * // add a new <script> to load Prism's script
         * ```
         *
         * @default false
         * @type {boolean}
         * @memberof Prism
         * @public
         */
        manual: _self2.Prism && _self2.Prism.manual,
        /**
         * By default, if Prism is in a web worker, it assumes that it is in a worker it created itself, so it uses
         * `addEventListener` to communicate with its parent instance. However, if you're using Prism manually in your
         * own worker, you don't want it to do this.
         *
         * By setting this value to `true`, Prism will not add its own listeners to the worker.
         *
         * You obviously have to change this value before Prism executes. To do this, you can add an
         * empty Prism object into the global scope before loading the Prism script like this:
         *
         * ```js
         * window.Prism = window.Prism || {};
         * Prism.disableWorkerMessageHandler = true;
         * // Load Prism's script
         * ```
         *
         * @default false
         * @type {boolean}
         * @memberof Prism
         * @public
         */
        disableWorkerMessageHandler: _self2.Prism && _self2.Prism.disableWorkerMessageHandler,
        /**
         * A namespace for utility methods.
         *
         * All function in this namespace that are not explicitly marked as _public_ are for __internal use only__ and may
         * change or disappear at any time.
         *
         * @namespace
         * @memberof Prism
         */
        util: {
          encode: function encode(tokens) {
            if (tokens instanceof Token) {
              return new Token(tokens.type, encode(tokens.content), tokens.alias);
            } else if (Array.isArray(tokens)) {
              return tokens.map(encode);
            } else {
              return tokens.replace(/&/g, "&amp;").replace(/</g, "&lt;").replace(/\u00a0/g, " ");
            }
          },
          /**
           * Returns the name of the type of the given value.
           *
           * @param {any} o
           * @returns {string}
           * @example
           * type(null)      === 'Null'
           * type(undefined) === 'Undefined'
           * type(123)       === 'Number'
           * type('foo')     === 'String'
           * type(true)      === 'Boolean'
           * type([1, 2])    === 'Array'
           * type({})        === 'Object'
           * type(String)    === 'Function'
           * type(/abc+/)    === 'RegExp'
           */
          type: function(o2) {
            return Object.prototype.toString.call(o2).slice(8, -1);
          },
          /**
           * Returns a unique number for the given object. Later calls will still return the same number.
           *
           * @param {Object} obj
           * @returns {number}
           */
          objId: function(obj) {
            if (!obj["__id"]) {
              Object.defineProperty(obj, "__id", { value: ++uniqueId });
            }
            return obj["__id"];
          },
          /**
           * Creates a deep clone of the given object.
           *
           * The main intended use of this function is to clone language definitions.
           *
           * @param {T} o
           * @param {Record<number, any>} [visited]
           * @returns {T}
           * @template T
           */
          clone: function deepClone(o2, visited) {
            visited = visited || {};
            var clone;
            var id;
            switch (_15.util.type(o2)) {
              case "Object":
                id = _15.util.objId(o2);
                if (visited[id]) {
                  return visited[id];
                }
                clone = /** @type {Record<string, any>} */
                {};
                visited[id] = clone;
                for (var key2 in o2) {
                  if (o2.hasOwnProperty(key2)) {
                    clone[key2] = deepClone(o2[key2], visited);
                  }
                }
                return (
                  /** @type {any} */
                  clone
                );
              case "Array":
                id = _15.util.objId(o2);
                if (visited[id]) {
                  return visited[id];
                }
                clone = [];
                visited[id] = clone;
                /** @type {Array} */
                /** @type {any} */
                o2.forEach(function(v2, i2) {
                  clone[i2] = deepClone(v2, visited);
                });
                return (
                  /** @type {any} */
                  clone
                );
              default:
                return o2;
            }
          },
          /**
           * Returns the Prism language of the given element set by a `language-xxxx` or `lang-xxxx` class.
           *
           * If no language is set for the element or the element is `null` or `undefined`, `none` will be returned.
           *
           * @param {Element} element
           * @returns {string}
           */
          getLanguage: function(element) {
            while (element) {
              var m2 = lang.exec(element.className);
              if (m2) {
                return m2[1].toLowerCase();
              }
              element = element.parentElement;
            }
            return "none";
          },
          /**
           * Sets the Prism `language-xxxx` class of the given element.
           *
           * @param {Element} element
           * @param {string} language
           * @returns {void}
           */
          setLanguage: function(element, language) {
            element.className = element.className.replace(RegExp(lang, "gi"), "");
            element.classList.add("language-" + language);
          },
          /**
           * Returns the script element that is currently executing.
           *
           * This does __not__ work for line script element.
           *
           * @returns {HTMLScriptElement | null}
           */
          currentScript: function() {
            if (typeof document === "undefined") {
              return null;
            }
            if (document.currentScript && document.currentScript.tagName === "SCRIPT" && 1 < 2) {
              return (
                /** @type {any} */
                document.currentScript
              );
            }
            try {
              throw new Error();
            } catch (err) {
              var src = (/at [^(\r\n]*\((.*):[^:]+:[^:]+\)$/i.exec(err.stack) || [])[1];
              if (src) {
                var scripts = document.getElementsByTagName("script");
                for (var i2 in scripts) {
                  if (scripts[i2].src == src) {
                    return scripts[i2];
                  }
                }
              }
              return null;
            }
          },
          /**
           * Returns whether a given class is active for `element`.
           *
           * The class can be activated if `element` or one of its ancestors has the given class and it can be deactivated
           * if `element` or one of its ancestors has the negated version of the given class. The _negated version_ of the
           * given class is just the given class with a `no-` prefix.
           *
           * Whether the class is active is determined by the closest ancestor of `element` (where `element` itself is
           * closest ancestor) that has the given class or the negated version of it. If neither `element` nor any of its
           * ancestors have the given class or the negated version of it, then the default activation will be returned.
           *
           * In the paradoxical situation where the closest ancestor contains __both__ the given class and the negated
           * version of it, the class is considered active.
           *
           * @param {Element} element
           * @param {string} className
           * @param {boolean} [defaultActivation=false]
           * @returns {boolean}
           */
          isActive: function(element, className, defaultActivation) {
            var no = "no-" + className;
            while (element) {
              var classList = element.classList;
              if (classList.contains(className)) {
                return true;
              }
              if (classList.contains(no)) {
                return false;
              }
              element = element.parentElement;
            }
            return !!defaultActivation;
          }
        },
        /**
         * This namespace contains all currently loaded languages and the some helper functions to create and modify languages.
         *
         * @namespace
         * @memberof Prism
         * @public
         */
        languages: {
          /**
           * The grammar for plain, unformatted text.
           */
          plain: plainTextGrammar,
          plaintext: plainTextGrammar,
          text: plainTextGrammar,
          txt: plainTextGrammar,
          /**
           * Creates a deep copy of the language with the given id and appends the given tokens.
           *
           * If a token in `redef` also appears in the copied language, then the existing token in the copied language
           * will be overwritten at its original position.
           *
           * ## Best practices
           *
           * Since the position of overwriting tokens (token in `redef` that overwrite tokens in the copied language)
           * doesn't matter, they can technically be in any order. However, this can be confusing to others that trying to
           * understand the language definition because, normally, the order of tokens matters in Prism grammars.
           *
           * Therefore, it is encouraged to order overwriting tokens according to the positions of the overwritten tokens.
           * Furthermore, all non-overwriting tokens should be placed after the overwriting ones.
           *
           * @param {string} id The id of the language to extend. This has to be a key in `Prism.languages`.
           * @param {Grammar} redef The new tokens to append.
           * @returns {Grammar} The new language created.
           * @public
           * @example
           * Prism.languages['css-with-colors'] = Prism.languages.extend('css', {
           *     // Prism.languages.css already has a 'comment' token, so this token will overwrite CSS' 'comment' token
           *     // at its original position
           *     'comment': { ... },
           *     // CSS doesn't have a 'color' token, so this token will be appended
           *     'color': /\b(?:red|green|blue)\b/
           * });
           */
          extend: function(id, redef) {
            var lang2 = _15.util.clone(_15.languages[id]);
            for (var key2 in redef) {
              lang2[key2] = redef[key2];
            }
            return lang2;
          },
          /**
           * Inserts tokens _before_ another token in a language definition or any other grammar.
           *
           * ## Usage
           *
           * This helper method makes it easy to modify existing languages. For example, the CSS language definition
           * not only defines CSS highlighting for CSS documents, but also needs to define highlighting for CSS embedded
           * in HTML through `<style>` elements. To do this, it needs to modify `Prism.languages.markup` and add the
           * appropriate tokens. However, `Prism.languages.markup` is a regular JavaScript object literal, so if you do
           * this:
           *
           * ```js
           * Prism.languages.markup.style = {
           *     // token
           * };
           * ```
           *
           * then the `style` token will be added (and processed) at the end. `insertBefore` allows you to insert tokens
           * before existing tokens. For the CSS example above, you would use it like this:
           *
           * ```js
           * Prism.languages.insertBefore('markup', 'cdata', {
           *     'style': {
           *         // token
           *     }
           * });
           * ```
           *
           * ## Special cases
           *
           * If the grammars of `inside` and `insert` have tokens with the same name, the tokens in `inside`'s grammar
           * will be ignored.
           *
           * This behavior can be used to insert tokens after `before`:
           *
           * ```js
           * Prism.languages.insertBefore('markup', 'comment', {
           *     'comment': Prism.languages.markup.comment,
           *     // tokens after 'comment'
           * });
           * ```
           *
           * ## Limitations
           *
           * The main problem `insertBefore` has to solve is iteration order. Since ES2015, the iteration order for object
           * properties is guaranteed to be the insertion order (except for integer keys) but some browsers behave
           * differently when keys are deleted and re-inserted. So `insertBefore` can't be implemented by temporarily
           * deleting properties which is necessary to insert at arbitrary positions.
           *
           * To solve this problem, `insertBefore` doesn't actually insert the given tokens into the target object.
           * Instead, it will create a new object and replace all references to the target object with the new one. This
           * can be done without temporarily deleting properties, so the iteration order is well-defined.
           *
           * However, only references that can be reached from `Prism.languages` or `insert` will be replaced. I.e. if
           * you hold the target object in a variable, then the value of the variable will not change.
           *
           * ```js
           * var oldMarkup = Prism.languages.markup;
           * var newMarkup = Prism.languages.insertBefore('markup', 'comment', { ... });
           *
           * assert(oldMarkup !== Prism.languages.markup);
           * assert(newMarkup === Prism.languages.markup);
           * ```
           *
           * @param {string} inside The property of `root` (e.g. a language id in `Prism.languages`) that contains the
           * object to be modified.
           * @param {string} before The key to insert before.
           * @param {Grammar} insert An object containing the key-value pairs to be inserted.
           * @param {Object<string, any>} [root] The object containing `inside`, i.e. the object that contains the
           * object to be modified.
           *
           * Defaults to `Prism.languages`.
           * @returns {Grammar} The new grammar object.
           * @public
           */
          insertBefore: function(inside, before, insert, root) {
            root = root || /** @type {any} */
            _15.languages;
            var grammar = root[inside];
            var ret = {};
            for (var token2 in grammar) {
              if (grammar.hasOwnProperty(token2)) {
                if (token2 == before) {
                  for (var newToken in insert) {
                    if (insert.hasOwnProperty(newToken)) {
                      ret[newToken] = insert[newToken];
                    }
                  }
                }
                if (!insert.hasOwnProperty(token2)) {
                  ret[token2] = grammar[token2];
                }
              }
            }
            var old = root[inside];
            root[inside] = ret;
            _15.languages.DFS(_15.languages, function(key2, value) {
              if (value === old && key2 != inside) {
                this[key2] = ret;
              }
            });
            return ret;
          },
          // Traverse a language definition with Depth First Search
          DFS: function DFS(o2, callback, type, visited) {
            visited = visited || {};
            var objId = _15.util.objId;
            for (var i2 in o2) {
              if (o2.hasOwnProperty(i2)) {
                callback.call(o2, i2, o2[i2], type || i2);
                var property = o2[i2];
                var propertyType = _15.util.type(property);
                if (propertyType === "Object" && !visited[objId(property)]) {
                  visited[objId(property)] = true;
                  DFS(property, callback, null, visited);
                } else if (propertyType === "Array" && !visited[objId(property)]) {
                  visited[objId(property)] = true;
                  DFS(property, callback, i2, visited);
                }
              }
            }
          }
        },
        plugins: {},
        /**
         * This is the most high-level function in Prism’s API.
         * It fetches all the elements that have a `.language-xxxx` class and then calls {@link Prism.highlightElement} on
         * each one of them.
         *
         * This is equivalent to `Prism.highlightAllUnder(document, async, callback)`.
         *
         * @param {boolean} [async=false] Same as in {@link Prism.highlightAllUnder}.
         * @param {HighlightCallback} [callback] Same as in {@link Prism.highlightAllUnder}.
         * @memberof Prism
         * @public
         */
        highlightAll: function(async, callback) {
          _15.highlightAllUnder(document, async, callback);
        },
        /**
         * Fetches all the descendants of `container` that have a `.language-xxxx` class and then calls
         * {@link Prism.highlightElement} on each one of them.
         *
         * The following hooks will be run:
         * 1. `before-highlightall`
         * 2. `before-all-elements-highlight`
         * 3. All hooks of {@link Prism.highlightElement} for each element.
         *
         * @param {ParentNode} container The root element, whose descendants that have a `.language-xxxx` class will be highlighted.
         * @param {boolean} [async=false] Whether each element is to be highlighted asynchronously using Web Workers.
         * @param {HighlightCallback} [callback] An optional callback to be invoked on each element after its highlighting is done.
         * @memberof Prism
         * @public
         */
        highlightAllUnder: function(container, async, callback) {
          var env2 = {
            callback,
            container,
            selector: 'code[class*="language-"], [class*="language-"] code, code[class*="lang-"], [class*="lang-"] code'
          };
          _15.hooks.run("before-highlightall", env2);
          env2.elements = Array.prototype.slice.apply(env2.container.querySelectorAll(env2.selector));
          _15.hooks.run("before-all-elements-highlight", env2);
          for (var i2 = 0, element; element = env2.elements[i2++]; ) {
            _15.highlightElement(element, async === true, env2.callback);
          }
        },
        /**
         * Highlights the code inside a single element.
         *
         * The following hooks will be run:
         * 1. `before-sanity-check`
         * 2. `before-highlight`
         * 3. All hooks of {@link Prism.highlight}. These hooks will be run by an asynchronous worker if `async` is `true`.
         * 4. `before-insert`
         * 5. `after-highlight`
         * 6. `complete`
         *
         * Some the above hooks will be skipped if the element doesn't contain any text or there is no grammar loaded for
         * the element's language.
         *
         * @param {Element} element The element containing the code.
         * It must have a class of `language-xxxx` to be processed, where `xxxx` is a valid language identifier.
         * @param {boolean} [async=false] Whether the element is to be highlighted asynchronously using Web Workers
         * to improve performance and avoid blocking the UI when highlighting very large chunks of code. This option is
         * [disabled by default](https://prismjs.com/faq.html#why-is-asynchronous-highlighting-disabled-by-default).
         *
         * Note: All language definitions required to highlight the code must be included in the main `prism.js` file for
         * asynchronous highlighting to work. You can build your own bundle on the
         * [Download page](https://prismjs.com/download.html).
         * @param {HighlightCallback} [callback] An optional callback to be invoked after the highlighting is done.
         * Mostly useful when `async` is `true`, since in that case, the highlighting is done asynchronously.
         * @memberof Prism
         * @public
         */
        highlightElement: function(element, async, callback) {
          var language = _15.util.getLanguage(element);
          var grammar = _15.languages[language];
          _15.util.setLanguage(element, language);
          var parent = element.parentElement;
          if (parent && parent.nodeName.toLowerCase() === "pre") {
            _15.util.setLanguage(parent, language);
          }
          var code = element.textContent;
          var env2 = {
            element,
            language,
            grammar,
            code
          };
          function insertHighlightedCode(highlightedCode) {
            env2.highlightedCode = highlightedCode;
            _15.hooks.run("before-insert", env2);
            env2.element.innerHTML = env2.highlightedCode;
            _15.hooks.run("after-highlight", env2);
            _15.hooks.run("complete", env2);
            callback && callback.call(env2.element);
          }
          _15.hooks.run("before-sanity-check", env2);
          parent = env2.element.parentElement;
          if (parent && parent.nodeName.toLowerCase() === "pre" && !parent.hasAttribute("tabindex")) {
            parent.setAttribute("tabindex", "0");
          }
          if (!env2.code) {
            _15.hooks.run("complete", env2);
            callback && callback.call(env2.element);
            return;
          }
          _15.hooks.run("before-highlight", env2);
          if (!env2.grammar) {
            insertHighlightedCode(_15.util.encode(env2.code));
            return;
          }
          if (async && _self2.Worker) {
            var worker = new Worker(_15.filename);
            worker.onmessage = function(evt) {
              insertHighlightedCode(evt.data);
            };
            worker.postMessage(JSON.stringify({
              language: env2.language,
              code: env2.code,
              immediateClose: true
            }));
          } else {
            insertHighlightedCode(_15.highlight(env2.code, env2.grammar, env2.language));
          }
        },
        /**
         * Low-level function, only use if you know what you’re doing. It accepts a string of text as input
         * and the language definitions to use, and returns a string with the HTML produced.
         *
         * The following hooks will be run:
         * 1. `before-tokenize`
         * 2. `after-tokenize`
         * 3. `wrap`: On each {@link Token}.
         *
         * @param {string} text A string with the code to be highlighted.
         * @param {Grammar} grammar An object containing the tokens to use.
         *
         * Usually a language definition like `Prism.languages.markup`.
         * @param {string} language The name of the language definition passed to `grammar`.
         * @returns {string} The highlighted HTML.
         * @memberof Prism
         * @public
         * @example
         * Prism.highlight('var foo = true;', Prism.languages.javascript, 'javascript');
         */
        highlight: function(text, grammar, language) {
          var env2 = {
            code: text,
            grammar,
            language
          };
          _15.hooks.run("before-tokenize", env2);
          if (!env2.grammar) {
            throw new Error('The language "' + env2.language + '" has no grammar.');
          }
          env2.tokens = _15.tokenize(env2.code, env2.grammar);
          _15.hooks.run("after-tokenize", env2);
          return Token.stringify(_15.util.encode(env2.tokens), env2.language);
        },
        /**
         * This is the heart of Prism, and the most low-level function you can use. It accepts a string of text as input
         * and the language definitions to use, and returns an array with the tokenized code.
         *
         * When the language definition includes nested tokens, the function is called recursively on each of these tokens.
         *
         * This method could be useful in other contexts as well, as a very crude parser.
         *
         * @param {string} text A string with the code to be highlighted.
         * @param {Grammar} grammar An object containing the tokens to use.
         *
         * Usually a language definition like `Prism.languages.markup`.
         * @returns {TokenStream} An array of strings and tokens, a token stream.
         * @memberof Prism
         * @public
         * @example
         * let code = `var foo = 0;`;
         * let tokens = Prism.tokenize(code, Prism.languages.javascript);
         * tokens.forEach(token => {
         *     if (token instanceof Prism.Token && token.type === 'number') {
         *         console.log(`Found numeric literal: ${token.content}`);
         *     }
         * });
         */
        tokenize: function(text, grammar) {
          var rest = grammar.rest;
          if (rest) {
            for (var token2 in rest) {
              grammar[token2] = rest[token2];
            }
            delete grammar.rest;
          }
          var tokenList = new LinkedList();
          addAfter(tokenList, tokenList.head, text);
          matchGrammar(text, tokenList, grammar, tokenList.head, 0);
          return toArray(tokenList);
        },
        /**
         * @namespace
         * @memberof Prism
         * @public
         */
        hooks: {
          all: {},
          /**
           * Adds the given callback to the list of callbacks for the given hook.
           *
           * The callback will be invoked when the hook it is registered for is run.
           * Hooks are usually directly run by a highlight function but you can also run hooks yourself.
           *
           * One callback function can be registered to multiple hooks and the same hook multiple times.
           *
           * @param {string} name The name of the hook.
           * @param {HookCallback} callback The callback function which is given environment variables.
           * @public
           */
          add: function(name, callback) {
            var hooks = _15.hooks.all;
            hooks[name] = hooks[name] || [];
            hooks[name].push(callback);
          },
          /**
           * Runs a hook invoking all registered callbacks with the given environment variables.
           *
           * Callbacks will be invoked synchronously and in the order in which they were registered.
           *
           * @param {string} name The name of the hook.
           * @param {Object<string, any>} env The environment variables of the hook passed to all callbacks registered.
           * @public
           */
          run: function(name, env2) {
            var callbacks = _15.hooks.all[name];
            if (!callbacks || !callbacks.length) {
              return;
            }
            for (var i2 = 0, callback; callback = callbacks[i2++]; ) {
              callback(env2);
            }
          }
        },
        Token
      };
      _self2.Prism = _15;
      function Token(type, content, alias, matchedStr) {
        this.type = type;
        this.content = content;
        this.alias = alias;
        this.length = (matchedStr || "").length | 0;
      }
      Token.stringify = function stringify5(o2, language) {
        if (typeof o2 == "string") {
          return o2;
        }
        if (Array.isArray(o2)) {
          var s2 = "";
          o2.forEach(function(e) {
            s2 += stringify5(e, language);
          });
          return s2;
        }
        var env2 = {
          type: o2.type,
          content: stringify5(o2.content, language),
          tag: "span",
          classes: ["token", o2.type],
          attributes: {},
          language
        };
        var aliases = o2.alias;
        if (aliases) {
          if (Array.isArray(aliases)) {
            Array.prototype.push.apply(env2.classes, aliases);
          } else {
            env2.classes.push(aliases);
          }
        }
        _15.hooks.run("wrap", env2);
        var attributes = "";
        for (var name in env2.attributes) {
          attributes += " " + name + '="' + (env2.attributes[name] || "").replace(/"/g, "&quot;") + '"';
        }
        return "<" + env2.tag + ' class="' + env2.classes.join(" ") + '"' + attributes + ">" + env2.content + "</" + env2.tag + ">";
      };
      function matchPattern(pattern, pos, text, lookbehind) {
        pattern.lastIndex = pos;
        var match2 = pattern.exec(text);
        if (match2 && lookbehind && match2[1]) {
          var lookbehindLength = match2[1].length;
          match2.index += lookbehindLength;
          match2[0] = match2[0].slice(lookbehindLength);
        }
        return match2;
      }
      function matchGrammar(text, tokenList, grammar, startNode, startPos, rematch) {
        for (var token2 in grammar) {
          if (!grammar.hasOwnProperty(token2) || !grammar[token2]) {
            continue;
          }
          var patterns = grammar[token2];
          patterns = Array.isArray(patterns) ? patterns : [patterns];
          for (var j2 = 0; j2 < patterns.length; ++j2) {
            if (rematch && rematch.cause == token2 + "," + j2) {
              return;
            }
            var patternObj = patterns[j2];
            var inside = patternObj.inside;
            var lookbehind = !!patternObj.lookbehind;
            var greedy = !!patternObj.greedy;
            var alias = patternObj.alias;
            if (greedy && !patternObj.pattern.global) {
              var flags = patternObj.pattern.toString().match(/[imsuy]*$/)[0];
              patternObj.pattern = RegExp(patternObj.pattern.source, flags + "g");
            }
            var pattern = patternObj.pattern || patternObj;
            for (var currentNode = startNode.next, pos = startPos; currentNode !== tokenList.tail; pos += currentNode.value.length, currentNode = currentNode.next) {
              if (rematch && pos >= rematch.reach) {
                break;
              }
              var str = currentNode.value;
              if (tokenList.length > text.length) {
                return;
              }
              if (str instanceof Token) {
                continue;
              }
              var removeCount = 1;
              var match2;
              if (greedy) {
                match2 = matchPattern(pattern, pos, text, lookbehind);
                if (!match2 || match2.index >= text.length) {
                  break;
                }
                var from2 = match2.index;
                var to = match2.index + match2[0].length;
                var p2 = pos;
                p2 += currentNode.value.length;
                while (from2 >= p2) {
                  currentNode = currentNode.next;
                  p2 += currentNode.value.length;
                }
                p2 -= currentNode.value.length;
                pos = p2;
                if (currentNode.value instanceof Token) {
                  continue;
                }
                for (var k2 = currentNode; k2 !== tokenList.tail && (p2 < to || typeof k2.value === "string"); k2 = k2.next) {
                  removeCount++;
                  p2 += k2.value.length;
                }
                removeCount--;
                str = text.slice(pos, p2);
                match2.index -= pos;
              } else {
                match2 = matchPattern(pattern, 0, str, lookbehind);
                if (!match2) {
                  continue;
                }
              }
              var from2 = match2.index;
              var matchStr = match2[0];
              var before = str.slice(0, from2);
              var after = str.slice(from2 + matchStr.length);
              var reach = pos + str.length;
              if (rematch && reach > rematch.reach) {
                rematch.reach = reach;
              }
              var removeFrom = currentNode.prev;
              if (before) {
                removeFrom = addAfter(tokenList, removeFrom, before);
                pos += before.length;
              }
              removeRange(tokenList, removeFrom, removeCount);
              var wrapped = new Token(token2, inside ? _15.tokenize(matchStr, inside) : matchStr, alias, matchStr);
              currentNode = addAfter(tokenList, removeFrom, wrapped);
              if (after) {
                addAfter(tokenList, currentNode, after);
              }
              if (removeCount > 1) {
                var nestedRematch = {
                  cause: token2 + "," + j2,
                  reach
                };
                matchGrammar(text, tokenList, grammar, currentNode.prev, pos, nestedRematch);
                if (rematch && nestedRematch.reach > rematch.reach) {
                  rematch.reach = nestedRematch.reach;
                }
              }
            }
          }
        }
      }
      function LinkedList() {
        var head = { value: null, prev: null, next: null };
        var tail = { value: null, prev: head, next: null };
        head.next = tail;
        this.head = head;
        this.tail = tail;
        this.length = 0;
      }
      function addAfter(list, node2, value) {
        var next2 = node2.next;
        var newNode = { value, prev: node2, next: next2 };
        node2.next = newNode;
        next2.prev = newNode;
        list.length++;
        return newNode;
      }
      function removeRange(list, node2, count) {
        var next2 = node2.next;
        for (var i2 = 0; i2 < count && next2 !== list.tail; i2++) {
          next2 = next2.next;
        }
        node2.next = next2;
        next2.prev = node2;
        list.length -= i2;
      }
      function toArray(list) {
        var array2 = [];
        var node2 = list.head.next;
        while (node2 !== list.tail) {
          array2.push(node2.value);
          node2 = node2.next;
        }
        return array2;
      }
      if (!_self2.document) {
        if (!_self2.addEventListener) {
          return _15;
        }
        if (!_15.disableWorkerMessageHandler) {
          _self2.addEventListener("message", function(evt) {
            var message = JSON.parse(evt.data);
            var lang2 = message.language;
            var code = message.code;
            var immediateClose = message.immediateClose;
            _self2.postMessage(_15.highlight(code, _15.languages[lang2], lang2));
            if (immediateClose) {
              _self2.close();
            }
          }, false);
        }
        return _15;
      }
      var script = _15.util.currentScript();
      if (script) {
        _15.filename = script.src;
        if (script.hasAttribute("data-manual")) {
          _15.manual = true;
        }
      }
      function highlightAutomaticallyCallback() {
        if (!_15.manual) {
          _15.highlightAll();
        }
      }
      if (!_15.manual) {
        var readyState = document.readyState;
        if (readyState === "loading" || readyState === "interactive" && script && script.defer) {
          document.addEventListener("DOMContentLoaded", highlightAutomaticallyCallback);
        } else {
          if (window.requestAnimationFrame) {
            window.requestAnimationFrame(highlightAutomaticallyCallback);
          } else {
            window.setTimeout(highlightAutomaticallyCallback, 16);
          }
        }
      }
      return _15;
    })(_self);
    if (typeof module !== "undefined" && module.exports) {
      module.exports = Prism2;
    }
    if (typeof global !== "undefined") {
      global.Prism = Prism2;
    }
    Prism2.languages.markup = {
      "comment": {
        pattern: /<!--(?:(?!<!--)[\s\S])*?-->/,
        greedy: true
      },
      "prolog": {
        pattern: /<\?[\s\S]+?\?>/,
        greedy: true
      },
      "doctype": {
        // https://www.w3.org/TR/xml/#NT-doctypedecl
        pattern: /<!DOCTYPE(?:[^>"'[\]]|"[^"]*"|'[^']*')+(?:\[(?:[^<"'\]]|"[^"]*"|'[^']*'|<(?!!--)|<!--(?:[^-]|-(?!->))*-->)*\]\s*)?>/i,
        greedy: true,
        inside: {
          "internal-subset": {
            pattern: /(^[^\[]*\[)[\s\S]+(?=\]>$)/,
            lookbehind: true,
            greedy: true,
            inside: null
            // see below
          },
          "string": {
            pattern: /"[^"]*"|'[^']*'/,
            greedy: true
          },
          "punctuation": /^<!|>$|[[\]]/,
          "doctype-tag": /^DOCTYPE/i,
          "name": /[^\s<>'"]+/
        }
      },
      "cdata": {
        pattern: /<!\[CDATA\[[\s\S]*?\]\]>/i,
        greedy: true
      },
      "tag": {
        pattern: /<\/?(?!\d)[^\s>\/=$<%]+(?:\s(?:\s*[^\s>\/=]+(?:\s*=\s*(?:"[^"]*"|'[^']*'|[^\s'">=]+(?=[\s>]))|(?=[\s/>])))+)?\s*\/?>/,
        greedy: true,
        inside: {
          "tag": {
            pattern: /^<\/?[^\s>\/]+/,
            inside: {
              "punctuation": /^<\/?/,
              "namespace": /^[^\s>\/:]+:/
            }
          },
          "special-attr": [],
          "attr-value": {
            pattern: /=\s*(?:"[^"]*"|'[^']*'|[^\s'">=]+)/,
            inside: {
              "punctuation": [
                {
                  pattern: /^=/,
                  alias: "attr-equals"
                },
                {
                  pattern: /^(\s*)["']|["']$/,
                  lookbehind: true
                }
              ]
            }
          },
          "punctuation": /\/?>/,
          "attr-name": {
            pattern: /[^\s>\/]+/,
            inside: {
              "namespace": /^[^\s>\/:]+:/
            }
          }
        }
      },
      "entity": [
        {
          pattern: /&[\da-z]{1,8};/i,
          alias: "named-entity"
        },
        /&#x?[\da-f]{1,8};/i
      ]
    };
    Prism2.languages.markup["tag"].inside["attr-value"].inside["entity"] = Prism2.languages.markup["entity"];
    Prism2.languages.markup["doctype"].inside["internal-subset"].inside = Prism2.languages.markup;
    Prism2.hooks.add("wrap", function(env2) {
      if (env2.type === "entity") {
        env2.attributes["title"] = env2.content.replace(/&amp;/, "&");
      }
    });
    Object.defineProperty(Prism2.languages.markup.tag, "addInlined", {
      /**
       * Adds an inlined language to markup.
       *
       * An example of an inlined language is CSS with `<style>` tags.
       *
       * @param {string} tagName The name of the tag that contains the inlined language. This name will be treated as
       * case insensitive.
       * @param {string} lang The language key.
       * @example
       * addInlined('style', 'css');
       */
      value: function addInlined(tagName, lang) {
        var includedCdataInside = {};
        includedCdataInside["language-" + lang] = {
          pattern: /(^<!\[CDATA\[)[\s\S]+?(?=\]\]>$)/i,
          lookbehind: true,
          inside: Prism2.languages[lang]
        };
        includedCdataInside["cdata"] = /^<!\[CDATA\[|\]\]>$/i;
        var inside = {
          "included-cdata": {
            pattern: /<!\[CDATA\[[\s\S]*?\]\]>/i,
            inside: includedCdataInside
          }
        };
        inside["language-" + lang] = {
          pattern: /[\s\S]+/,
          inside: Prism2.languages[lang]
        };
        var def = {};
        def[tagName] = {
          pattern: RegExp(/(<__[^>]*>)(?:<!\[CDATA\[(?:[^\]]|\](?!\]>))*\]\]>|(?!<!\[CDATA\[)[\s\S])*?(?=<\/__>)/.source.replace(/__/g, function() {
            return tagName;
          }), "i"),
          lookbehind: true,
          greedy: true,
          inside
        };
        Prism2.languages.insertBefore("markup", "cdata", def);
      }
    });
    Object.defineProperty(Prism2.languages.markup.tag, "addAttribute", {
      /**
       * Adds an pattern to highlight languages embedded in HTML attributes.
       *
       * An example of an inlined language is CSS with `style` attributes.
       *
       * @param {string} attrName The name of the tag that contains the inlined language. This name will be treated as
       * case insensitive.
       * @param {string} lang The language key.
       * @example
       * addAttribute('style', 'css');
       */
      value: function(attrName, lang) {
        Prism2.languages.markup.tag.inside["special-attr"].push({
          pattern: RegExp(
            /(^|["'\s])/.source + "(?:" + attrName + ")" + /\s*=\s*(?:"[^"]*"|'[^']*'|[^\s'">=]+(?=[\s>]))/.source,
            "i"
          ),
          lookbehind: true,
          inside: {
            "attr-name": /^[^\s=]+/,
            "attr-value": {
              pattern: /=[\s\S]+/,
              inside: {
                "value": {
                  pattern: /(^=\s*(["']|(?!["'])))\S[\s\S]*(?=\2$)/,
                  lookbehind: true,
                  alias: [lang, "language-" + lang],
                  inside: Prism2.languages[lang]
                },
                "punctuation": [
                  {
                    pattern: /^=/,
                    alias: "attr-equals"
                  },
                  /"|'/
                ]
              }
            }
          }
        });
      }
    });
    Prism2.languages.html = Prism2.languages.markup;
    Prism2.languages.mathml = Prism2.languages.markup;
    Prism2.languages.svg = Prism2.languages.markup;
    Prism2.languages.xml = Prism2.languages.extend("markup", {});
    Prism2.languages.ssml = Prism2.languages.xml;
    Prism2.languages.atom = Prism2.languages.xml;
    Prism2.languages.rss = Prism2.languages.xml;
    (function(Prism3) {
      var string2 = /(?:"(?:\\(?:\r\n|[\s\S])|[^"\\\r\n])*"|'(?:\\(?:\r\n|[\s\S])|[^'\\\r\n])*')/;
      Prism3.languages.css = {
        "comment": /\/\*[\s\S]*?\*\//,
        "atrule": {
          pattern: RegExp("@[\\w-](?:" + /[^;{\s"']|\s+(?!\s)/.source + "|" + string2.source + ")*?" + /(?:;|(?=\s*\{))/.source),
          inside: {
            "rule": /^@[\w-]+/,
            "selector-function-argument": {
              pattern: /(\bselector\s*\(\s*(?![\s)]))(?:[^()\s]|\s+(?![\s)])|\((?:[^()]|\([^()]*\))*\))+(?=\s*\))/,
              lookbehind: true,
              alias: "selector"
            },
            "keyword": {
              pattern: /(^|[^\w-])(?:and|not|only|or)(?![\w-])/,
              lookbehind: true
            }
            // See rest below
          }
        },
        "url": {
          // https://drafts.csswg.org/css-values-3/#urls
          pattern: RegExp("\\burl\\((?:" + string2.source + "|" + /(?:[^\\\r\n()"']|\\[\s\S])*/.source + ")\\)", "i"),
          greedy: true,
          inside: {
            "function": /^url/i,
            "punctuation": /^\(|\)$/,
            "string": {
              pattern: RegExp("^" + string2.source + "$"),
              alias: "url"
            }
          }
        },
        "selector": {
          pattern: RegExp(`(^|[{}\\s])[^{}\\s](?:[^{};"'\\s]|\\s+(?![\\s{])|` + string2.source + ")*(?=\\s*\\{)"),
          lookbehind: true
        },
        "string": {
          pattern: string2,
          greedy: true
        },
        "property": {
          pattern: /(^|[^-\w\xA0-\uFFFF])(?!\s)[-_a-z\xA0-\uFFFF](?:(?!\s)[-\w\xA0-\uFFFF])*(?=\s*:)/i,
          lookbehind: true
        },
        "important": /!important\b/i,
        "function": {
          pattern: /(^|[^-a-z0-9])[-a-z0-9]+(?=\()/i,
          lookbehind: true
        },
        "punctuation": /[(){};:,]/
      };
      Prism3.languages.css["atrule"].inside.rest = Prism3.languages.css;
      var markup = Prism3.languages.markup;
      if (markup) {
        markup.tag.addInlined("style", "css");
        markup.tag.addAttribute("style", "css");
      }
    })(Prism2);
    Prism2.languages.clike = {
      "comment": [
        {
          pattern: /(^|[^\\])\/\*[\s\S]*?(?:\*\/|$)/,
          lookbehind: true,
          greedy: true
        },
        {
          pattern: /(^|[^\\:])\/\/.*/,
          lookbehind: true,
          greedy: true
        }
      ],
      "string": {
        pattern: /(["'])(?:\\(?:\r\n|[\s\S])|(?!\1)[^\\\r\n])*\1/,
        greedy: true
      },
      "class-name": {
        pattern: /(\b(?:class|extends|implements|instanceof|interface|new|trait)\s+|\bcatch\s+\()[\w.\\]+/i,
        lookbehind: true,
        inside: {
          "punctuation": /[.\\]/
        }
      },
      "keyword": /\b(?:break|catch|continue|do|else|finally|for|function|if|in|instanceof|new|null|return|throw|try|while)\b/,
      "boolean": /\b(?:false|true)\b/,
      "function": /\b\w+(?=\()/,
      "number": /\b0x[\da-f]+\b|(?:\b\d+(?:\.\d*)?|\B\.\d+)(?:e[+-]?\d+)?/i,
      "operator": /[<>]=?|[!=]=?=?|--?|\+\+?|&&?|\|\|?|[?*/~^%]/,
      "punctuation": /[{}[\];(),.:]/
    };
    Prism2.languages.javascript = Prism2.languages.extend("clike", {
      "class-name": [
        Prism2.languages.clike["class-name"],
        {
          pattern: /(^|[^$\w\xA0-\uFFFF])(?!\s)[_$A-Z\xA0-\uFFFF](?:(?!\s)[$\w\xA0-\uFFFF])*(?=\.(?:constructor|prototype))/,
          lookbehind: true
        }
      ],
      "keyword": [
        {
          pattern: /((?:^|\})\s*)catch\b/,
          lookbehind: true
        },
        {
          pattern: /(^|[^.]|\.\.\.\s*)\b(?:as|assert(?=\s*\{)|async(?=\s*(?:function\b|\(|[$\w\xA0-\uFFFF]|$))|await|break|case|class|const|continue|debugger|default|delete|do|else|enum|export|extends|finally(?=\s*(?:\{|$))|for|from(?=\s*(?:['"]|$))|function|(?:get|set)(?=\s*(?:[#\[$\w\xA0-\uFFFF]|$))|if|implements|import|in|instanceof|interface|let|new|null|of|package|private|protected|public|return|static|super|switch|this|throw|try|typeof|undefined|var|void|while|with|yield)\b/,
          lookbehind: true
        }
      ],
      // Allow for all non-ASCII characters (See http://stackoverflow.com/a/2008444)
      "function": /#?(?!\s)[_$a-zA-Z\xA0-\uFFFF](?:(?!\s)[$\w\xA0-\uFFFF])*(?=\s*(?:\.\s*(?:apply|bind|call)\s*)?\()/,
      "number": {
        pattern: RegExp(
          /(^|[^\w$])/.source + "(?:" + // constant
          (/NaN|Infinity/.source + "|" + // binary integer
          /0[bB][01]+(?:_[01]+)*n?/.source + "|" + // octal integer
          /0[oO][0-7]+(?:_[0-7]+)*n?/.source + "|" + // hexadecimal integer
          /0[xX][\dA-Fa-f]+(?:_[\dA-Fa-f]+)*n?/.source + "|" + // decimal bigint
          /\d+(?:_\d+)*n/.source + "|" + // decimal number (integer or float) but no bigint
          /(?:\d+(?:_\d+)*(?:\.(?:\d+(?:_\d+)*)?)?|\.\d+(?:_\d+)*)(?:[Ee][+-]?\d+(?:_\d+)*)?/.source) + ")" + /(?![\w$])/.source
        ),
        lookbehind: true
      },
      "operator": /--|\+\+|\*\*=?|=>|&&=?|\|\|=?|[!=]==|<<=?|>>>?=?|[-+*/%&|^!=<>]=?|\.{3}|\?\?=?|\?\.?|[~:]/
    });
    Prism2.languages.javascript["class-name"][0].pattern = /(\b(?:class|extends|implements|instanceof|interface|new)\s+)[\w.\\]+/;
    Prism2.languages.insertBefore("javascript", "keyword", {
      "regex": {
        pattern: RegExp(
          // lookbehind
          // eslint-disable-next-line regexp/no-dupe-characters-character-class
          /((?:^|[^$\w\xA0-\uFFFF."'\])\s]|\b(?:return|yield))\s*)/.source + // Regex pattern:
          // There are 2 regex patterns here. The RegExp set notation proposal added support for nested character
          // classes if the `v` flag is present. Unfortunately, nested CCs are both context-free and incompatible
          // with the only syntax, so we have to define 2 different regex patterns.
          /\//.source + "(?:" + /(?:\[(?:[^\]\\\r\n]|\\.)*\]|\\.|[^/\\\[\r\n])+\/[dgimyus]{0,7}/.source + "|" + // `v` flag syntax. This supports 3 levels of nested character classes.
          /(?:\[(?:[^[\]\\\r\n]|\\.|\[(?:[^[\]\\\r\n]|\\.|\[(?:[^[\]\\\r\n]|\\.)*\])*\])*\]|\\.|[^/\\\[\r\n])+\/[dgimyus]{0,7}v[dgimyus]{0,7}/.source + ")" + // lookahead
          /(?=(?:\s|\/\*(?:[^*]|\*(?!\/))*\*\/)*(?:$|[\r\n,.;:})\]]|\/\/))/.source
        ),
        lookbehind: true,
        greedy: true,
        inside: {
          "regex-source": {
            pattern: /^(\/)[\s\S]+(?=\/[a-z]*$)/,
            lookbehind: true,
            alias: "language-regex",
            inside: Prism2.languages.regex
          },
          "regex-delimiter": /^\/|\/$/,
          "regex-flags": /^[a-z]+$/
        }
      },
      // This must be declared before keyword because we use "function" inside the look-forward
      "function-variable": {
        pattern: /#?(?!\s)[_$a-zA-Z\xA0-\uFFFF](?:(?!\s)[$\w\xA0-\uFFFF])*(?=\s*[=:]\s*(?:async\s*)?(?:\bfunction\b|(?:\((?:[^()]|\([^()]*\))*\)|(?!\s)[_$a-zA-Z\xA0-\uFFFF](?:(?!\s)[$\w\xA0-\uFFFF])*)\s*=>))/,
        alias: "function"
      },
      "parameter": [
        {
          pattern: /(function(?:\s+(?!\s)[_$a-zA-Z\xA0-\uFFFF](?:(?!\s)[$\w\xA0-\uFFFF])*)?\s*\(\s*)(?!\s)(?:[^()\s]|\s+(?![\s)])|\([^()]*\))+(?=\s*\))/,
          lookbehind: true,
          inside: Prism2.languages.javascript
        },
        {
          pattern: /(^|[^$\w\xA0-\uFFFF])(?!\s)[_$a-z\xA0-\uFFFF](?:(?!\s)[$\w\xA0-\uFFFF])*(?=\s*=>)/i,
          lookbehind: true,
          inside: Prism2.languages.javascript
        },
        {
          pattern: /(\(\s*)(?!\s)(?:[^()\s]|\s+(?![\s)])|\([^()]*\))+(?=\s*\)\s*=>)/,
          lookbehind: true,
          inside: Prism2.languages.javascript
        },
        {
          pattern: /((?:\b|\s|^)(?!(?:as|async|await|break|case|catch|class|const|continue|debugger|default|delete|do|else|enum|export|extends|finally|for|from|function|get|if|implements|import|in|instanceof|interface|let|new|null|of|package|private|protected|public|return|set|static|super|switch|this|throw|try|typeof|undefined|var|void|while|with|yield)(?![$\w\xA0-\uFFFF]))(?:(?!\s)[_$a-zA-Z\xA0-\uFFFF](?:(?!\s)[$\w\xA0-\uFFFF])*\s*)\(\s*|\]\s*\(\s*)(?!\s)(?:[^()\s]|\s+(?![\s)])|\([^()]*\))+(?=\s*\)\s*\{)/,
          lookbehind: true,
          inside: Prism2.languages.javascript
        }
      ],
      "constant": /\b[A-Z](?:[A-Z_]|\dx?)*\b/
    });
    Prism2.languages.insertBefore("javascript", "string", {
      "hashbang": {
        pattern: /^#!.*/,
        greedy: true,
        alias: "comment"
      },
      "template-string": {
        pattern: /`(?:\\[\s\S]|\$\{(?:[^{}]|\{(?:[^{}]|\{[^}]*\})*\})+\}|(?!\$\{)[^\\`])*`/,
        greedy: true,
        inside: {
          "template-punctuation": {
            pattern: /^`|`$/,
            alias: "string"
          },
          "interpolation": {
            pattern: /((?:^|[^\\])(?:\\{2})*)\$\{(?:[^{}]|\{(?:[^{}]|\{[^}]*\})*\})+\}/,
            lookbehind: true,
            inside: {
              "interpolation-punctuation": {
                pattern: /^\$\{|\}$/,
                alias: "punctuation"
              },
              rest: Prism2.languages.javascript
            }
          },
          "string": /[\s\S]+/
        }
      },
      "string-property": {
        pattern: /((?:^|[,{])[ \t]*)(["'])(?:\\(?:\r\n|[\s\S])|(?!\2)[^\\\r\n])*\2(?=\s*:)/m,
        lookbehind: true,
        greedy: true,
        alias: "property"
      }
    });
    Prism2.languages.insertBefore("javascript", "operator", {
      "literal-property": {
        pattern: /((?:^|[,{])[ \t]*)(?!\s)[_$a-zA-Z\xA0-\uFFFF](?:(?!\s)[$\w\xA0-\uFFFF])*(?=\s*:)/m,
        lookbehind: true,
        alias: "property"
      }
    });
    if (Prism2.languages.markup) {
      Prism2.languages.markup.tag.addInlined("script", "javascript");
      Prism2.languages.markup.tag.addAttribute(
        /on(?:abort|blur|change|click|composition(?:end|start|update)|dblclick|error|focus(?:in|out)?|key(?:down|up)|load|mouse(?:down|enter|leave|move|out|over|up)|reset|resize|scroll|select|slotchange|submit|unload|wheel)/.source,
        "javascript"
      );
    }
    Prism2.languages.js = Prism2.languages.javascript;
    (function() {
      if (typeof Prism2 === "undefined" || typeof document === "undefined") {
        return;
      }
      if (!Element.prototype.matches) {
        Element.prototype.matches = Element.prototype.msMatchesSelector || Element.prototype.webkitMatchesSelector;
      }
      var LOADING_MESSAGE = "Loading…";
      var FAILURE_MESSAGE = function(status, message) {
        return "✖ Error " + status + " while fetching file: " + message;
      };
      var FAILURE_EMPTY_MESSAGE = "✖ Error: File does not exist or is empty";
      var EXTENSIONS = {
        "js": "javascript",
        "py": "python",
        "rb": "ruby",
        "ps1": "powershell",
        "psm1": "powershell",
        "sh": "bash",
        "bat": "batch",
        "h": "c",
        "tex": "latex"
      };
      var STATUS_ATTR = "data-src-status";
      var STATUS_LOADING = "loading";
      var STATUS_LOADED = "loaded";
      var STATUS_FAILED = "failed";
      var SELECTOR = "pre[data-src]:not([" + STATUS_ATTR + '="' + STATUS_LOADED + '"]):not([' + STATUS_ATTR + '="' + STATUS_LOADING + '"])';
      function loadFile(src, success, error) {
        var xhr = new XMLHttpRequest();
        xhr.open("GET", src, true);
        xhr.onreadystatechange = function() {
          if (xhr.readyState == 4) {
            if (xhr.status < 400 && xhr.responseText) {
              success(xhr.responseText);
            } else {
              if (xhr.status >= 400) {
                error(FAILURE_MESSAGE(xhr.status, xhr.statusText));
              } else {
                error(FAILURE_EMPTY_MESSAGE);
              }
            }
          }
        };
        xhr.send(null);
      }
      function parseRange(range) {
        var m2 = /^\s*(\d+)\s*(?:(,)\s*(?:(\d+)\s*)?)?$/.exec(range || "");
        if (m2) {
          var start = Number(m2[1]);
          var comma2 = m2[2];
          var end = m2[3];
          if (!comma2) {
            return [start, start];
          }
          if (!end) {
            return [start, void 0];
          }
          return [start, Number(end)];
        }
        return void 0;
      }
      Prism2.hooks.add("before-highlightall", function(env2) {
        env2.selector += ", " + SELECTOR;
      });
      Prism2.hooks.add("before-sanity-check", function(env2) {
        var pre = (
          /** @type {HTMLPreElement} */
          env2.element
        );
        if (pre.matches(SELECTOR)) {
          env2.code = "";
          pre.setAttribute(STATUS_ATTR, STATUS_LOADING);
          var code = pre.appendChild(document.createElement("CODE"));
          code.textContent = LOADING_MESSAGE;
          var src = pre.getAttribute("data-src");
          var language = env2.language;
          if (language === "none") {
            var extension = (/\.(\w+)$/.exec(src) || [, "none"])[1];
            language = EXTENSIONS[extension] || extension;
          }
          Prism2.util.setLanguage(code, language);
          Prism2.util.setLanguage(pre, language);
          var autoloader = Prism2.plugins.autoloader;
          if (autoloader) {
            autoloader.loadLanguages(language);
          }
          loadFile(
            src,
            function(text) {
              pre.setAttribute(STATUS_ATTR, STATUS_LOADED);
              var range = parseRange(pre.getAttribute("data-range"));
              if (range) {
                var lines = text.split(/\r\n?|\n/g);
                var start = range[0];
                var end = range[1] == null ? lines.length : range[1];
                if (start < 0) {
                  start += lines.length;
                }
                start = Math.max(0, Math.min(start - 1, lines.length));
                if (end < 0) {
                  end += lines.length;
                }
                end = Math.max(0, Math.min(end, lines.length));
                text = lines.slice(start, end).join("\n");
                if (!pre.hasAttribute("data-start")) {
                  pre.setAttribute("data-start", String(start + 1));
                }
              }
              code.textContent = text;
              Prism2.highlightElement(code);
            },
            function(error) {
              pre.setAttribute(STATUS_ATTR, STATUS_FAILED);
              code.textContent = error;
            }
          );
        }
      });
      Prism2.plugins.fileHighlight = {
        /**
         * Executes the File Highlight plugin for all matching `pre` elements under the given container.
         *
         * Note: Elements which are already loaded or currently loading will not be touched by this method.
         *
         * @param {ParentNode} [container=document]
         */
        highlight: function highlight(container) {
          var elements = (container || document).querySelectorAll(SELECTOR);
          for (var i2 = 0, element; element = elements[i2++]; ) {
            Prism2.highlightElement(element);
          }
        }
      };
      var logged = false;
      Prism2.fileHighlight = function() {
        if (!logged) {
          console.warn("Prism.fileHighlight is deprecated. Use `Prism.plugins.fileHighlight.highlight` instead.");
          logged = true;
        }
        Prism2.plugins.fileHighlight.highlight.apply(this, arguments);
      };
    })();
  }
});

// node_modules/prismjs/components/prism-bash.js
var require_prism_bash = __commonJS({
  "node_modules/prismjs/components/prism-bash.js"() {
    (function(Prism2) {
      var envVars = "\\b(?:BASH|BASHOPTS|BASH_ALIASES|BASH_ARGC|BASH_ARGV|BASH_CMDS|BASH_COMPLETION_COMPAT_DIR|BASH_LINENO|BASH_REMATCH|BASH_SOURCE|BASH_VERSINFO|BASH_VERSION|COLORTERM|COLUMNS|COMP_WORDBREAKS|DBUS_SESSION_BUS_ADDRESS|DEFAULTS_PATH|DESKTOP_SESSION|DIRSTACK|DISPLAY|EUID|GDMSESSION|GDM_LANG|GNOME_KEYRING_CONTROL|GNOME_KEYRING_PID|GPG_AGENT_INFO|GROUPS|HISTCONTROL|HISTFILE|HISTFILESIZE|HISTSIZE|HOME|HOSTNAME|HOSTTYPE|IFS|INSTANCE|JOB|LANG|LANGUAGE|LC_ADDRESS|LC_ALL|LC_IDENTIFICATION|LC_MEASUREMENT|LC_MONETARY|LC_NAME|LC_NUMERIC|LC_PAPER|LC_TELEPHONE|LC_TIME|LESSCLOSE|LESSOPEN|LINES|LOGNAME|LS_COLORS|MACHTYPE|MAILCHECK|MANDATORY_PATH|NO_AT_BRIDGE|OLDPWD|OPTERR|OPTIND|ORBIT_SOCKETDIR|OSTYPE|PAPERSIZE|PATH|PIPESTATUS|PPID|PS1|PS2|PS3|PS4|PWD|RANDOM|REPLY|SECONDS|SELINUX_INIT|SESSION|SESSIONTYPE|SESSION_MANAGER|SHELL|SHELLOPTS|SHLVL|SSH_AUTH_SOCK|TERM|UID|UPSTART_EVENTS|UPSTART_INSTANCE|UPSTART_JOB|UPSTART_SESSION|USER|WINDOWID|XAUTHORITY|XDG_CONFIG_DIRS|XDG_CURRENT_DESKTOP|XDG_DATA_DIRS|XDG_GREETER_DATA_DIR|XDG_MENU_PREFIX|XDG_RUNTIME_DIR|XDG_SEAT|XDG_SEAT_PATH|XDG_SESSION_DESKTOP|XDG_SESSION_ID|XDG_SESSION_PATH|XDG_SESSION_TYPE|XDG_VTNR|XMODIFIERS)\\b";
      var commandAfterHeredoc = {
        pattern: /(^(["']?)\w+\2)[ \t]+\S.*/,
        lookbehind: true,
        alias: "punctuation",
        // this looks reasonably well in all themes
        inside: null
        // see below
      };
      var insideString = {
        "bash": commandAfterHeredoc,
        "environment": {
          pattern: RegExp("\\$" + envVars),
          alias: "constant"
        },
        "variable": [
          // [0]: Arithmetic Environment
          {
            pattern: /\$?\(\([\s\S]+?\)\)/,
            greedy: true,
            inside: {
              // If there is a $ sign at the beginning highlight $(( and )) as variable
              "variable": [
                {
                  pattern: /(^\$\(\([\s\S]+)\)\)/,
                  lookbehind: true
                },
                /^\$\(\(/
              ],
              "number": /\b0x[\dA-Fa-f]+\b|(?:\b\d+(?:\.\d*)?|\B\.\d+)(?:[Ee]-?\d+)?/,
              // Operators according to https://www.gnu.org/software/bash/manual/bashref.html#Shell-Arithmetic
              "operator": /--|\+\+|\*\*=?|<<=?|>>=?|&&|\|\||[=!+\-*/%<>^&|]=?|[?~:]/,
              // If there is no $ sign at the beginning highlight (( and )) as punctuation
              "punctuation": /\(\(?|\)\)?|,|;/
            }
          },
          // [1]: Command Substitution
          {
            pattern: /\$\((?:\([^)]+\)|[^()])+\)|`[^`]+`/,
            greedy: true,
            inside: {
              "variable": /^\$\(|^`|\)$|`$/
            }
          },
          // [2]: Brace expansion
          {
            pattern: /\$\{[^}]+\}/,
            greedy: true,
            inside: {
              "operator": /:[-=?+]?|[!\/]|##?|%%?|\^\^?|,,?/,
              "punctuation": /[\[\]]/,
              "environment": {
                pattern: RegExp("(\\{)" + envVars),
                lookbehind: true,
                alias: "constant"
              }
            }
          },
          /\$(?:\w+|[#?*!@$])/
        ],
        // Escape sequences from echo and printf's manuals, and escaped quotes.
        "entity": /\\(?:[abceEfnrtv\\"]|O?[0-7]{1,3}|U[0-9a-fA-F]{8}|u[0-9a-fA-F]{4}|x[0-9a-fA-F]{1,2})/
      };
      Prism2.languages.bash = {
        "shebang": {
          pattern: /^#!\s*\/.*/,
          alias: "important"
        },
        "comment": {
          pattern: /(^|[^"{\\$])#.*/,
          lookbehind: true
        },
        "function-name": [
          // a) function foo {
          // b) foo() {
          // c) function foo() {
          // but not “foo {”
          {
            // a) and c)
            pattern: /(\bfunction\s+)[\w-]+(?=(?:\s*\(?:\s*\))?\s*\{)/,
            lookbehind: true,
            alias: "function"
          },
          {
            // b)
            pattern: /\b[\w-]+(?=\s*\(\s*\)\s*\{)/,
            alias: "function"
          }
        ],
        // Highlight variable names as variables in for and select beginnings.
        "for-or-select": {
          pattern: /(\b(?:for|select)\s+)\w+(?=\s+in\s)/,
          alias: "variable",
          lookbehind: true
        },
        // Highlight variable names as variables in the left-hand part
        // of assignments (“=” and “+=”).
        "assign-left": {
          pattern: /(^|[\s;|&]|[<>]\()\w+(?:\.\w+)*(?=\+?=)/,
          inside: {
            "environment": {
              pattern: RegExp("(^|[\\s;|&]|[<>]\\()" + envVars),
              lookbehind: true,
              alias: "constant"
            }
          },
          alias: "variable",
          lookbehind: true
        },
        // Highlight parameter names as variables
        "parameter": {
          pattern: /(^|\s)-{1,2}(?:\w+:[+-]?)?\w+(?:\.\w+)*(?=[=\s]|$)/,
          alias: "variable",
          lookbehind: true
        },
        "string": [
          // Support for Here-documents https://en.wikipedia.org/wiki/Here_document
          {
            pattern: /((?:^|[^<])<<-?\s*)(\w+)\s[\s\S]*?(?:\r?\n|\r)\2/,
            lookbehind: true,
            greedy: true,
            inside: insideString
          },
          // Here-document with quotes around the tag
          // → No expansion (so no “inside”).
          {
            pattern: /((?:^|[^<])<<-?\s*)(["'])(\w+)\2\s[\s\S]*?(?:\r?\n|\r)\3/,
            lookbehind: true,
            greedy: true,
            inside: {
              "bash": commandAfterHeredoc
            }
          },
          // “Normal” string
          {
            // https://www.gnu.org/software/bash/manual/html_node/Double-Quotes.html
            pattern: /(^|[^\\](?:\\\\)*)"(?:\\[\s\S]|\$\([^)]+\)|\$(?!\()|`[^`]+`|[^"\\`$])*"/,
            lookbehind: true,
            greedy: true,
            inside: insideString
          },
          {
            // https://www.gnu.org/software/bash/manual/html_node/Single-Quotes.html
            pattern: /(^|[^$\\])'[^']*'/,
            lookbehind: true,
            greedy: true
          },
          {
            // https://www.gnu.org/software/bash/manual/html_node/ANSI_002dC-Quoting.html
            pattern: /\$'(?:[^'\\]|\\[\s\S])*'/,
            greedy: true,
            inside: {
              "entity": insideString.entity
            }
          }
        ],
        "environment": {
          pattern: RegExp("\\$?" + envVars),
          alias: "constant"
        },
        "variable": insideString.variable,
        "function": {
          pattern: /(^|[\s;|&]|[<>]\()(?:add|apropos|apt|apt-cache|apt-get|aptitude|aspell|automysqlbackup|awk|basename|bash|bc|bconsole|bg|bzip2|cal|cargo|cat|cfdisk|chgrp|chkconfig|chmod|chown|chroot|cksum|clear|cmp|column|comm|composer|cp|cron|crontab|csplit|curl|cut|date|dc|dd|ddrescue|debootstrap|df|diff|diff3|dig|dir|dircolors|dirname|dirs|dmesg|docker|docker-compose|du|egrep|eject|env|ethtool|expand|expect|expr|fdformat|fdisk|fg|fgrep|file|find|fmt|fold|format|free|fsck|ftp|fuser|gawk|git|gparted|grep|groupadd|groupdel|groupmod|groups|grub-mkconfig|gzip|halt|head|hg|history|host|hostname|htop|iconv|id|ifconfig|ifdown|ifup|import|install|ip|java|jobs|join|kill|killall|less|link|ln|locate|logname|logrotate|look|lpc|lpr|lprint|lprintd|lprintq|lprm|ls|lsof|lynx|make|man|mc|mdadm|mkconfig|mkdir|mke2fs|mkfifo|mkfs|mkisofs|mknod|mkswap|mmv|more|most|mount|mtools|mtr|mutt|mv|nano|nc|netstat|nice|nl|node|nohup|notify-send|npm|nslookup|op|open|parted|passwd|paste|pathchk|ping|pkill|pnpm|podman|podman-compose|popd|pr|printcap|printenv|ps|pushd|pv|quota|quotacheck|quotactl|ram|rar|rcp|reboot|remsync|rename|renice|rev|rm|rmdir|rpm|rsync|scp|screen|sdiff|sed|sendmail|seq|service|sftp|sh|shellcheck|shuf|shutdown|sleep|slocate|sort|split|ssh|stat|strace|su|sudo|sum|suspend|swapon|sync|sysctl|tac|tail|tar|tee|time|timeout|top|touch|tr|traceroute|tsort|tty|umount|uname|unexpand|uniq|units|unrar|unshar|unzip|update-grub|uptime|useradd|userdel|usermod|users|uudecode|uuencode|v|vcpkg|vdir|vi|vim|virsh|vmstat|wait|watch|wc|wget|whereis|which|who|whoami|write|xargs|xdg-open|yarn|yes|zenity|zip|zsh|zypper)(?=$|[)\s;|&])/,
          lookbehind: true
        },
        "keyword": {
          pattern: /(^|[\s;|&]|[<>]\()(?:case|do|done|elif|else|esac|fi|for|function|if|in|select|then|until|while)(?=$|[)\s;|&])/,
          lookbehind: true
        },
        // https://www.gnu.org/software/bash/manual/html_node/Shell-Builtin-Commands.html
        "builtin": {
          pattern: /(^|[\s;|&]|[<>]\()(?:\.|:|alias|bind|break|builtin|caller|cd|command|continue|declare|echo|enable|eval|exec|exit|export|getopts|hash|help|let|local|logout|mapfile|printf|pwd|read|readarray|readonly|return|set|shift|shopt|source|test|times|trap|type|typeset|ulimit|umask|unalias|unset)(?=$|[)\s;|&])/,
          lookbehind: true,
          // Alias added to make those easier to distinguish from strings.
          alias: "class-name"
        },
        "boolean": {
          pattern: /(^|[\s;|&]|[<>]\()(?:false|true)(?=$|[)\s;|&])/,
          lookbehind: true
        },
        "file-descriptor": {
          pattern: /\B&\d\b/,
          alias: "important"
        },
        "operator": {
          // Lots of redirections here, but not just that.
          pattern: /\d?<>|>\||\+=|=[=~]?|!=?|<<[<-]?|[&\d]?>>|\d[<>]&?|[<>][&=]?|&[>&]?|\|[&|]?/,
          inside: {
            "file-descriptor": {
              pattern: /^\d/,
              alias: "important"
            }
          }
        },
        "punctuation": /\$?\(\(?|\)\)?|\.\.|[{}[\];\\]/,
        "number": {
          pattern: /(^|\s)(?:[1-9]\d*|0)(?:[.,]\d+)?\b/,
          lookbehind: true
        }
      };
      commandAfterHeredoc.inside = Prism2.languages.bash;
      var toBeCopied = [
        "comment",
        "function-name",
        "for-or-select",
        "assign-left",
        "parameter",
        "string",
        "environment",
        "function",
        "keyword",
        "builtin",
        "boolean",
        "file-descriptor",
        "operator",
        "punctuation",
        "number"
      ];
      var inside = insideString.variable[1].inside;
      for (var i2 = 0; i2 < toBeCopied.length; i2++) {
        inside[toBeCopied[i2]] = Prism2.languages.bash[toBeCopied[i2]];
      }
      Prism2.languages.sh = Prism2.languages.bash;
      Prism2.languages.shell = Prism2.languages.bash;
    })(Prism);
  }
});

// node_modules/prismjs/components/prism-c.js
var require_prism_c = __commonJS({
  "node_modules/prismjs/components/prism-c.js"() {
    Prism.languages.c = Prism.languages.extend("clike", {
      "comment": {
        pattern: /\/\/(?:[^\r\n\\]|\\(?:\r\n?|\n|(?![\r\n])))*|\/\*[\s\S]*?(?:\*\/|$)/,
        greedy: true
      },
      "string": {
        // https://en.cppreference.com/w/c/language/string_literal
        pattern: /"(?:\\(?:\r\n|[\s\S])|[^"\\\r\n])*"/,
        greedy: true
      },
      "class-name": {
        pattern: /(\b(?:enum|struct)\s+(?:__attribute__\s*\(\([\s\S]*?\)\)\s*)?)\w+|\b[a-z]\w*_t\b/,
        lookbehind: true
      },
      "keyword": /\b(?:_Alignas|_Alignof|_Atomic|_Bool|_Complex|_Generic|_Imaginary|_Noreturn|_Static_assert|_Thread_local|__attribute__|asm|auto|break|case|char|const|continue|default|do|double|else|enum|extern|float|for|goto|if|inline|int|long|register|return|short|signed|sizeof|static|struct|switch|typedef|typeof|union|unsigned|void|volatile|while)\b/,
      "function": /\b[a-z_]\w*(?=\s*\()/i,
      "number": /(?:\b0x(?:[\da-f]+(?:\.[\da-f]*)?|\.[\da-f]+)(?:p[+-]?\d+)?|(?:\b\d+(?:\.\d*)?|\B\.\d+)(?:e[+-]?\d+)?)[ful]{0,4}/i,
      "operator": />>=?|<<=?|->|([-+&|:])\1|[?:~]|[-+*/%&|^!=<>]=?/
    });
    Prism.languages.insertBefore("c", "string", {
      "char": {
        // https://en.cppreference.com/w/c/language/character_constant
        pattern: /'(?:\\(?:\r\n|[\s\S])|[^'\\\r\n]){0,32}'/,
        greedy: true
      }
    });
    Prism.languages.insertBefore("c", "string", {
      "macro": {
        // allow for multiline macro definitions
        // spaces after the # character compile fine with gcc
        pattern: /(^[\t ]*)#\s*[a-z](?:[^\r\n\\/]|\/(?!\*)|\/\*(?:[^*]|\*(?!\/))*\*\/|\\(?:\r\n|[\s\S]))*/im,
        lookbehind: true,
        greedy: true,
        alias: "property",
        inside: {
          "string": [
            {
              // highlight the path of the include statement as a string
              pattern: /^(#\s*include\s*)<[^>]+>/,
              lookbehind: true
            },
            Prism.languages.c["string"]
          ],
          "char": Prism.languages.c["char"],
          "comment": Prism.languages.c["comment"],
          "macro-name": [
            {
              pattern: /(^#\s*define\s+)\w+\b(?!\()/i,
              lookbehind: true
            },
            {
              pattern: /(^#\s*define\s+)\w+\b(?=\()/i,
              lookbehind: true,
              alias: "function"
            }
          ],
          // highlight macro directives as keywords
          "directive": {
            pattern: /^(#\s*)[a-z]+/,
            lookbehind: true,
            alias: "keyword"
          },
          "directive-hash": /^#/,
          "punctuation": /##|\\(?=[\r\n])/,
          "expression": {
            pattern: /\S[\s\S]*/,
            inside: Prism.languages.c
          }
        }
      }
    });
    Prism.languages.insertBefore("c", "function", {
      // highlight predefined macros as constants
      "constant": /\b(?:EOF|NULL|SEEK_CUR|SEEK_END|SEEK_SET|__DATE__|__FILE__|__LINE__|__TIMESTAMP__|__TIME__|__func__|stderr|stdin|stdout)\b/
    });
    delete Prism.languages.c["boolean"];
  }
});

// node_modules/prismjs/components/prism-clike.js
var require_prism_clike = __commonJS({
  "node_modules/prismjs/components/prism-clike.js"() {
    Prism.languages.clike = {
      "comment": [
        {
          pattern: /(^|[^\\])\/\*[\s\S]*?(?:\*\/|$)/,
          lookbehind: true,
          greedy: true
        },
        {
          pattern: /(^|[^\\:])\/\/.*/,
          lookbehind: true,
          greedy: true
        }
      ],
      "string": {
        pattern: /(["'])(?:\\(?:\r\n|[\s\S])|(?!\1)[^\\\r\n])*\1/,
        greedy: true
      },
      "class-name": {
        pattern: /(\b(?:class|extends|implements|instanceof|interface|new|trait)\s+|\bcatch\s+\()[\w.\\]+/i,
        lookbehind: true,
        inside: {
          "punctuation": /[.\\]/
        }
      },
      "keyword": /\b(?:break|catch|continue|do|else|finally|for|function|if|in|instanceof|new|null|return|throw|try|while)\b/,
      "boolean": /\b(?:false|true)\b/,
      "function": /\b\w+(?=\()/,
      "number": /\b0x[\da-f]+\b|(?:\b\d+(?:\.\d*)?|\B\.\d+)(?:e[+-]?\d+)?/i,
      "operator": /[<>]=?|[!=]=?=?|--?|\+\+?|&&?|\|\|?|[?*/~^%]/,
      "punctuation": /[{}[\];(),.:]/
    };
  }
});

// node_modules/prismjs/components/prism-coffeescript.js
var require_prism_coffeescript = __commonJS({
  "node_modules/prismjs/components/prism-coffeescript.js"() {
    (function(Prism2) {
      var comment2 = /#(?!\{).+/;
      var interpolation = {
        pattern: /#\{[^}]+\}/,
        alias: "variable"
      };
      Prism2.languages.coffeescript = Prism2.languages.extend("javascript", {
        "comment": comment2,
        "string": [
          // Strings are multiline
          {
            pattern: /'(?:\\[\s\S]|[^\\'])*'/,
            greedy: true
          },
          {
            // Strings are multiline
            pattern: /"(?:\\[\s\S]|[^\\"])*"/,
            greedy: true,
            inside: {
              "interpolation": interpolation
            }
          }
        ],
        "keyword": /\b(?:and|break|by|catch|class|continue|debugger|delete|do|each|else|extend|extends|false|finally|for|if|in|instanceof|is|isnt|let|loop|namespace|new|no|not|null|of|off|on|or|own|return|super|switch|then|this|throw|true|try|typeof|undefined|unless|until|when|while|window|with|yes|yield)\b/,
        "class-member": {
          pattern: /@(?!\d)\w+/,
          alias: "variable"
        }
      });
      Prism2.languages.insertBefore("coffeescript", "comment", {
        "multiline-comment": {
          pattern: /###[\s\S]+?###/,
          alias: "comment"
        },
        // Block regexp can contain comments and interpolation
        "block-regex": {
          pattern: /\/{3}[\s\S]*?\/{3}/,
          alias: "regex",
          inside: {
            "comment": comment2,
            "interpolation": interpolation
          }
        }
      });
      Prism2.languages.insertBefore("coffeescript", "string", {
        "inline-javascript": {
          pattern: /`(?:\\[\s\S]|[^\\`])*`/,
          inside: {
            "delimiter": {
              pattern: /^`|`$/,
              alias: "punctuation"
            },
            "script": {
              pattern: /[\s\S]+/,
              alias: "language-javascript",
              inside: Prism2.languages.javascript
            }
          }
        },
        // Block strings
        "multiline-string": [
          {
            pattern: /'''[\s\S]*?'''/,
            greedy: true,
            alias: "string"
          },
          {
            pattern: /"""[\s\S]*?"""/,
            greedy: true,
            alias: "string",
            inside: {
              interpolation
            }
          }
        ]
      });
      Prism2.languages.insertBefore("coffeescript", "keyword", {
        // Object property
        "property": /(?!\d)\w+(?=\s*:(?!:))/
      });
      delete Prism2.languages.coffeescript["template-string"];
      Prism2.languages.coffee = Prism2.languages.coffeescript;
    })(Prism);
  }
});

// node_modules/prismjs/components/prism-cpp.js
var require_prism_cpp = __commonJS({
  "node_modules/prismjs/components/prism-cpp.js"() {
    (function(Prism2) {
      var keyword = /\b(?:alignas|alignof|asm|auto|bool|break|case|catch|char|char16_t|char32_t|char8_t|class|co_await|co_return|co_yield|compl|concept|const|const_cast|consteval|constexpr|constinit|continue|decltype|default|delete|do|double|dynamic_cast|else|enum|explicit|export|extern|final|float|for|friend|goto|if|import|inline|int|int16_t|int32_t|int64_t|int8_t|long|module|mutable|namespace|new|noexcept|nullptr|operator|override|private|protected|public|register|reinterpret_cast|requires|return|short|signed|sizeof|static|static_assert|static_cast|struct|switch|template|this|thread_local|throw|try|typedef|typeid|typename|uint16_t|uint32_t|uint64_t|uint8_t|union|unsigned|using|virtual|void|volatile|wchar_t|while)\b/;
      var modName = /\b(?!<keyword>)\w+(?:\s*\.\s*\w+)*\b/.source.replace(/<keyword>/g, function() {
        return keyword.source;
      });
      Prism2.languages.cpp = Prism2.languages.extend("c", {
        "class-name": [
          {
            pattern: RegExp(/(\b(?:class|concept|enum|struct|typename)\s+)(?!<keyword>)\w+/.source.replace(/<keyword>/g, function() {
              return keyword.source;
            })),
            lookbehind: true
          },
          // This is intended to capture the class name of method implementations like:
          //   void foo::bar() const {}
          // However! The `foo` in the above example could also be a namespace, so we only capture the class name if
          // it starts with an uppercase letter. This approximation should give decent results.
          /\b[A-Z]\w*(?=\s*::\s*\w+\s*\()/,
          // This will capture the class name before destructors like:
          //   Foo::~Foo() {}
          /\b[A-Z_]\w*(?=\s*::\s*~\w+\s*\()/i,
          // This also intends to capture the class name of method implementations but here the class has template
          // parameters, so it can't be a namespace (until C++ adds generic namespaces).
          /\b\w+(?=\s*<(?:[^<>]|<(?:[^<>]|<[^<>]*>)*>)*>\s*::\s*\w+\s*\()/
        ],
        "keyword": keyword,
        "number": {
          pattern: /(?:\b0b[01']+|\b0x(?:[\da-f']+(?:\.[\da-f']*)?|\.[\da-f']+)(?:p[+-]?[\d']+)?|(?:\b[\d']+(?:\.[\d']*)?|\B\.[\d']+)(?:e[+-]?[\d']+)?)[ful]{0,4}/i,
          greedy: true
        },
        "operator": />>=?|<<=?|->|--|\+\+|&&|\|\||[?:~]|<=>|[-+*/%&|^!=<>]=?|\b(?:and|and_eq|bitand|bitor|not|not_eq|or|or_eq|xor|xor_eq)\b/,
        "boolean": /\b(?:false|true)\b/
      });
      Prism2.languages.insertBefore("cpp", "string", {
        "module": {
          // https://en.cppreference.com/w/cpp/language/modules
          pattern: RegExp(
            /(\b(?:import|module)\s+)/.source + "(?:" + // header-name
            /"(?:\\(?:\r\n|[\s\S])|[^"\\\r\n])*"|<[^<>\r\n]*>/.source + "|" + // module name or partition or both
            /<mod-name>(?:\s*:\s*<mod-name>)?|:\s*<mod-name>/.source.replace(/<mod-name>/g, function() {
              return modName;
            }) + ")"
          ),
          lookbehind: true,
          greedy: true,
          inside: {
            "string": /^[<"][\s\S]+/,
            "operator": /:/,
            "punctuation": /\./
          }
        },
        "raw-string": {
          pattern: /R"([^()\\ ]{0,16})\([\s\S]*?\)\1"/,
          alias: "string",
          greedy: true
        }
      });
      Prism2.languages.insertBefore("cpp", "keyword", {
        "generic-function": {
          pattern: /\b(?!operator\b)[a-z_]\w*\s*<(?:[^<>]|<[^<>]*>)*>(?=\s*\()/i,
          inside: {
            "function": /^\w+/,
            "generic": {
              pattern: /<[\s\S]+/,
              alias: "class-name",
              inside: Prism2.languages.cpp
            }
          }
        }
      });
      Prism2.languages.insertBefore("cpp", "operator", {
        "double-colon": {
          pattern: /::/,
          alias: "punctuation"
        }
      });
      Prism2.languages.insertBefore("cpp", "class-name", {
        // the base clause is an optional list of parent classes
        // https://en.cppreference.com/w/cpp/language/class
        "base-clause": {
          pattern: /(\b(?:class|struct)\s+\w+\s*:\s*)[^;{}"'\s]+(?:\s+[^;{}"'\s]+)*(?=\s*[;{])/,
          lookbehind: true,
          greedy: true,
          inside: Prism2.languages.extend("cpp", {})
        }
      });
      Prism2.languages.insertBefore("inside", "double-colon", {
        // All untokenized words that are not namespaces should be class names
        "class-name": /\b[a-z_]\w*\b(?!\s*::)/i
      }, Prism2.languages.cpp["base-clause"]);
    })(Prism);
  }
});

// node_modules/prismjs/components/prism-csharp.js
var require_prism_csharp = __commonJS({
  "node_modules/prismjs/components/prism-csharp.js"() {
    (function(Prism2) {
      function replace3(pattern, replacements) {
        return pattern.replace(/<<(\d+)>>/g, function(m2, index) {
          return "(?:" + replacements[+index] + ")";
        });
      }
      function re2(pattern, replacements, flags) {
        return RegExp(replace3(pattern, replacements), flags || "");
      }
      function nested(pattern, depthLog2) {
        for (var i2 = 0; i2 < depthLog2; i2++) {
          pattern = pattern.replace(/<<self>>/g, function() {
            return "(?:" + pattern + ")";
          });
        }
        return pattern.replace(/<<self>>/g, "[^\\s\\S]");
      }
      var keywordKinds = {
        // keywords which represent a return or variable type
        type: "bool byte char decimal double dynamic float int long object sbyte short string uint ulong ushort var void",
        // keywords which are used to declare a type
        typeDeclaration: "class enum interface record struct",
        // contextual keywords
        // ("var" and "dynamic" are missing because they are used like types)
        contextual: "add alias and ascending async await by descending from(?=\\s*(?:\\w|$)) get global group into init(?=\\s*;) join let nameof not notnull on or orderby partial remove select set unmanaged value when where with(?=\\s*{)",
        // all other keywords
        other: "abstract as base break case catch checked const continue default delegate do else event explicit extern finally fixed for foreach goto if implicit in internal is lock namespace new null operator out override params private protected public readonly ref return sealed sizeof stackalloc static switch this throw try typeof unchecked unsafe using virtual volatile while yield"
      };
      function keywordsToPattern(words) {
        return "\\b(?:" + words.trim().replace(/ /g, "|") + ")\\b";
      }
      var typeDeclarationKeywords = keywordsToPattern(keywordKinds.typeDeclaration);
      var keywords = RegExp(keywordsToPattern(keywordKinds.type + " " + keywordKinds.typeDeclaration + " " + keywordKinds.contextual + " " + keywordKinds.other));
      var nonTypeKeywords = keywordsToPattern(keywordKinds.typeDeclaration + " " + keywordKinds.contextual + " " + keywordKinds.other);
      var nonContextualKeywords = keywordsToPattern(keywordKinds.type + " " + keywordKinds.typeDeclaration + " " + keywordKinds.other);
      var generic = nested(/<(?:[^<>;=+\-*/%&|^]|<<self>>)*>/.source, 2);
      var nestedRound = nested(/\((?:[^()]|<<self>>)*\)/.source, 2);
      var name = /@?\b[A-Za-z_]\w*\b/.source;
      var genericName = replace3(/<<0>>(?:\s*<<1>>)?/.source, [name, generic]);
      var identifier2 = replace3(/(?!<<0>>)<<1>>(?:\s*\.\s*<<1>>)*/.source, [nonTypeKeywords, genericName]);
      var array2 = /\[\s*(?:,\s*)*\]/.source;
      var typeExpressionWithoutTuple = replace3(/<<0>>(?:\s*(?:\?\s*)?<<1>>)*(?:\s*\?)?/.source, [identifier2, array2]);
      var tupleElement = replace3(/[^,()<>[\];=+\-*/%&|^]|<<0>>|<<1>>|<<2>>/.source, [generic, nestedRound, array2]);
      var tuple = replace3(/\(<<0>>+(?:,<<0>>+)+\)/.source, [tupleElement]);
      var typeExpression = replace3(/(?:<<0>>|<<1>>)(?:\s*(?:\?\s*)?<<2>>)*(?:\s*\?)?/.source, [tuple, identifier2, array2]);
      var typeInside = {
        "keyword": keywords,
        "punctuation": /[<>()?,.:[\]]/
      };
      var character2 = /'(?:[^\r\n'\\]|\\.|\\[Uux][\da-fA-F]{1,8})'/.source;
      var regularString = /"(?:\\.|[^\\"\r\n])*"/.source;
      var verbatimString = /@"(?:""|\\[\s\S]|[^\\"])*"(?!")/.source;
      Prism2.languages.csharp = Prism2.languages.extend("clike", {
        "string": [
          {
            pattern: re2(/(^|[^$\\])<<0>>/.source, [verbatimString]),
            lookbehind: true,
            greedy: true
          },
          {
            pattern: re2(/(^|[^@$\\])<<0>>/.source, [regularString]),
            lookbehind: true,
            greedy: true
          }
        ],
        "class-name": [
          {
            // Using static
            // using static System.Math;
            pattern: re2(/(\busing\s+static\s+)<<0>>(?=\s*;)/.source, [identifier2]),
            lookbehind: true,
            inside: typeInside
          },
          {
            // Using alias (type)
            // using Project = PC.MyCompany.Project;
            pattern: re2(/(\busing\s+<<0>>\s*=\s*)<<1>>(?=\s*;)/.source, [name, typeExpression]),
            lookbehind: true,
            inside: typeInside
          },
          {
            // Using alias (alias)
            // using Project = PC.MyCompany.Project;
            pattern: re2(/(\busing\s+)<<0>>(?=\s*=)/.source, [name]),
            lookbehind: true
          },
          {
            // Type declarations
            // class Foo<A, B>
            // interface Foo<out A, B>
            pattern: re2(/(\b<<0>>\s+)<<1>>/.source, [typeDeclarationKeywords, genericName]),
            lookbehind: true,
            inside: typeInside
          },
          {
            // Single catch exception declaration
            // catch(Foo)
            // (things like catch(Foo e) is covered by variable declaration)
            pattern: re2(/(\bcatch\s*\(\s*)<<0>>/.source, [identifier2]),
            lookbehind: true,
            inside: typeInside
          },
          {
            // Name of the type parameter of generic constraints
            // where Foo : class
            pattern: re2(/(\bwhere\s+)<<0>>/.source, [name]),
            lookbehind: true
          },
          {
            // Casts and checks via as and is.
            // as Foo<A>, is Bar<B>
            // (things like if(a is Foo b) is covered by variable declaration)
            pattern: re2(/(\b(?:is(?:\s+not)?|as)\s+)<<0>>/.source, [typeExpressionWithoutTuple]),
            lookbehind: true,
            inside: typeInside
          },
          {
            // Variable, field and parameter declaration
            // (Foo bar, Bar baz, Foo[,,] bay, Foo<Bar, FooBar<Bar>> bax)
            pattern: re2(/\b<<0>>(?=\s+(?!<<1>>|with\s*\{)<<2>>(?:\s*[=,;:{)\]]|\s+(?:in|when)\b))/.source, [typeExpression, nonContextualKeywords, name]),
            inside: typeInside
          }
        ],
        "keyword": keywords,
        // https://docs.microsoft.com/en-us/dotnet/csharp/language-reference/language-specification/lexical-structure#literals
        "number": /(?:\b0(?:x[\da-f_]*[\da-f]|b[01_]*[01])|(?:\B\.\d+(?:_+\d+)*|\b\d+(?:_+\d+)*(?:\.\d+(?:_+\d+)*)?)(?:e[-+]?\d+(?:_+\d+)*)?)(?:[dflmu]|lu|ul)?\b/i,
        "operator": />>=?|<<=?|[-=]>|([-+&|])\1|~|\?\?=?|[-+*/%&|^!=<>]=?/,
        "punctuation": /\?\.?|::|[{}[\];(),.:]/
      });
      Prism2.languages.insertBefore("csharp", "number", {
        "range": {
          pattern: /\.\./,
          alias: "operator"
        }
      });
      Prism2.languages.insertBefore("csharp", "punctuation", {
        "named-parameter": {
          pattern: re2(/([(,]\s*)<<0>>(?=\s*:)/.source, [name]),
          lookbehind: true,
          alias: "punctuation"
        }
      });
      Prism2.languages.insertBefore("csharp", "class-name", {
        "namespace": {
          // namespace Foo.Bar {}
          // using Foo.Bar;
          pattern: re2(/(\b(?:namespace|using)\s+)<<0>>(?:\s*\.\s*<<0>>)*(?=\s*[;{])/.source, [name]),
          lookbehind: true,
          inside: {
            "punctuation": /\./
          }
        },
        "type-expression": {
          // default(Foo), typeof(Foo<Bar>), sizeof(int)
          pattern: re2(/(\b(?:default|sizeof|typeof)\s*\(\s*(?!\s))(?:[^()\s]|\s(?!\s)|<<0>>)*(?=\s*\))/.source, [nestedRound]),
          lookbehind: true,
          alias: "class-name",
          inside: typeInside
        },
        "return-type": {
          // Foo<Bar> ForBar(); Foo IFoo.Bar() => 0
          // int this[int index] => 0; T IReadOnlyList<T>.this[int index] => this[index];
          // int Foo => 0; int Foo { get; set } = 0;
          pattern: re2(/<<0>>(?=\s+(?:<<1>>\s*(?:=>|[({]|\.\s*this\s*\[)|this\s*\[))/.source, [typeExpression, identifier2]),
          inside: typeInside,
          alias: "class-name"
        },
        "constructor-invocation": {
          // new List<Foo<Bar[]>> { }
          pattern: re2(/(\bnew\s+)<<0>>(?=\s*[[({])/.source, [typeExpression]),
          lookbehind: true,
          inside: typeInside,
          alias: "class-name"
        },
        /*'explicit-implementation': {
        	// int IFoo<Foo>.Bar => 0; void IFoo<Foo<Foo>>.Foo<T>();
        	pattern: replace(/\b<<0>>(?=\.<<1>>)/, className, methodOrPropertyDeclaration),
        	inside: classNameInside,
        	alias: 'class-name'
        },*/
        "generic-method": {
          // foo<Bar>()
          pattern: re2(/<<0>>\s*<<1>>(?=\s*\()/.source, [name, generic]),
          inside: {
            "function": re2(/^<<0>>/.source, [name]),
            "generic": {
              pattern: RegExp(generic),
              alias: "class-name",
              inside: typeInside
            }
          }
        },
        "type-list": {
          // The list of types inherited or of generic constraints
          // class Foo<F> : Bar, IList<FooBar>
          // where F : Bar, IList<int>
          pattern: re2(
            /\b((?:<<0>>\s+<<1>>|record\s+<<1>>\s*<<5>>|where\s+<<2>>)\s*:\s*)(?:<<3>>|<<4>>|<<1>>\s*<<5>>|<<6>>)(?:\s*,\s*(?:<<3>>|<<4>>|<<6>>))*(?=\s*(?:where|[{;]|=>|$))/.source,
            [typeDeclarationKeywords, genericName, name, typeExpression, keywords.source, nestedRound, /\bnew\s*\(\s*\)/.source]
          ),
          lookbehind: true,
          inside: {
            "record-arguments": {
              pattern: re2(/(^(?!new\s*\()<<0>>\s*)<<1>>/.source, [genericName, nestedRound]),
              lookbehind: true,
              greedy: true,
              inside: Prism2.languages.csharp
            },
            "keyword": keywords,
            "class-name": {
              pattern: RegExp(typeExpression),
              greedy: true,
              inside: typeInside
            },
            "punctuation": /[,()]/
          }
        },
        "preprocessor": {
          pattern: /(^[\t ]*)#.*/m,
          lookbehind: true,
          alias: "property",
          inside: {
            // highlight preprocessor directives as keywords
            "directive": {
              pattern: /(#)\b(?:define|elif|else|endif|endregion|error|if|line|nullable|pragma|region|undef|warning)\b/,
              lookbehind: true,
              alias: "keyword"
            }
          }
        }
      });
      var regularStringOrCharacter = regularString + "|" + character2;
      var regularStringCharacterOrComment = replace3(/\/(?![*/])|\/\/[^\r\n]*[\r\n]|\/\*(?:[^*]|\*(?!\/))*\*\/|<<0>>/.source, [regularStringOrCharacter]);
      var roundExpression = nested(replace3(/[^"'/()]|<<0>>|\(<<self>>*\)/.source, [regularStringCharacterOrComment]), 2);
      var attrTarget = /\b(?:assembly|event|field|method|module|param|property|return|type)\b/.source;
      var attr = replace3(/<<0>>(?:\s*\(<<1>>*\))?/.source, [identifier2, roundExpression]);
      Prism2.languages.insertBefore("csharp", "class-name", {
        "attribute": {
          // Attributes
          // [Foo], [Foo(1), Bar(2, Prop = "foo")], [return: Foo(1), Bar(2)], [assembly: Foo(Bar)]
          pattern: re2(/((?:^|[^\s\w>)?])\s*\[\s*)(?:<<0>>\s*:\s*)?<<1>>(?:\s*,\s*<<1>>)*(?=\s*\])/.source, [attrTarget, attr]),
          lookbehind: true,
          greedy: true,
          inside: {
            "target": {
              pattern: re2(/^<<0>>(?=\s*:)/.source, [attrTarget]),
              alias: "keyword"
            },
            "attribute-arguments": {
              pattern: re2(/\(<<0>>*\)/.source, [roundExpression]),
              inside: Prism2.languages.csharp
            },
            "class-name": {
              pattern: RegExp(identifier2),
              inside: {
                "punctuation": /\./
              }
            },
            "punctuation": /[:,]/
          }
        }
      });
      var formatString = /:[^}\r\n]+/.source;
      var mInterpolationRound = nested(replace3(/[^"'/()]|<<0>>|\(<<self>>*\)/.source, [regularStringCharacterOrComment]), 2);
      var mInterpolation = replace3(/\{(?!\{)(?:(?![}:])<<0>>)*<<1>>?\}/.source, [mInterpolationRound, formatString]);
      var sInterpolationRound = nested(replace3(/[^"'/()]|\/(?!\*)|\/\*(?:[^*]|\*(?!\/))*\*\/|<<0>>|\(<<self>>*\)/.source, [regularStringOrCharacter]), 2);
      var sInterpolation = replace3(/\{(?!\{)(?:(?![}:])<<0>>)*<<1>>?\}/.source, [sInterpolationRound, formatString]);
      function createInterpolationInside(interpolation, interpolationRound) {
        return {
          "interpolation": {
            pattern: re2(/((?:^|[^{])(?:\{\{)*)<<0>>/.source, [interpolation]),
            lookbehind: true,
            inside: {
              "format-string": {
                pattern: re2(/(^\{(?:(?![}:])<<0>>)*)<<1>>(?=\}$)/.source, [interpolationRound, formatString]),
                lookbehind: true,
                inside: {
                  "punctuation": /^:/
                }
              },
              "punctuation": /^\{|\}$/,
              "expression": {
                pattern: /[\s\S]+/,
                alias: "language-csharp",
                inside: Prism2.languages.csharp
              }
            }
          },
          "string": /[\s\S]+/
        };
      }
      Prism2.languages.insertBefore("csharp", "string", {
        "interpolation-string": [
          {
            pattern: re2(/(^|[^\\])(?:\$@|@\$)"(?:""|\\[\s\S]|\{\{|<<0>>|[^\\{"])*"/.source, [mInterpolation]),
            lookbehind: true,
            greedy: true,
            inside: createInterpolationInside(mInterpolation, mInterpolationRound)
          },
          {
            pattern: re2(/(^|[^@\\])\$"(?:\\.|\{\{|<<0>>|[^\\"{])*"/.source, [sInterpolation]),
            lookbehind: true,
            greedy: true,
            inside: createInterpolationInside(sInterpolation, sInterpolationRound)
          }
        ],
        "char": {
          pattern: RegExp(character2),
          greedy: true
        }
      });
      Prism2.languages.dotnet = Prism2.languages.cs = Prism2.languages.csharp;
    })(Prism);
  }
});

// node_modules/prismjs/components/prism-go.js
var require_prism_go = __commonJS({
  "node_modules/prismjs/components/prism-go.js"() {
    Prism.languages.go = Prism.languages.extend("clike", {
      "string": {
        pattern: /(^|[^\\])"(?:\\.|[^"\\\r\n])*"|`[^`]*`/,
        lookbehind: true,
        greedy: true
      },
      "keyword": /\b(?:break|case|chan|const|continue|default|defer|else|fallthrough|for|func|go(?:to)?|if|import|interface|map|package|range|return|select|struct|switch|type|var)\b/,
      "boolean": /\b(?:_|false|iota|nil|true)\b/,
      "number": [
        // binary and octal integers
        /\b0(?:b[01_]+|o[0-7_]+)i?\b/i,
        // hexadecimal integers and floats
        /\b0x(?:[a-f\d_]+(?:\.[a-f\d_]*)?|\.[a-f\d_]+)(?:p[+-]?\d+(?:_\d+)*)?i?(?!\w)/i,
        // decimal integers and floats
        /(?:\b\d[\d_]*(?:\.[\d_]*)?|\B\.\d[\d_]*)(?:e[+-]?[\d_]+)?i?(?!\w)/i
      ],
      "operator": /[*\/%^!=]=?|\+[=+]?|-[=-]?|\|[=|]?|&(?:=|&|\^=?)?|>(?:>=?|=)?|<(?:<=?|=|-)?|:=|\.\.\./,
      "builtin": /\b(?:append|bool|byte|cap|close|complex|complex(?:64|128)|copy|delete|error|float(?:32|64)|u?int(?:8|16|32|64)?|imag|len|make|new|panic|print(?:ln)?|real|recover|rune|string|uintptr)\b/
    });
    Prism.languages.insertBefore("go", "string", {
      "char": {
        pattern: /'(?:\\.|[^'\\\r\n]){0,10}'/,
        greedy: true
      }
    });
    delete Prism.languages.go["class-name"];
  }
});

// node_modules/prismjs/components/prism-http.js
var require_prism_http = __commonJS({
  "node_modules/prismjs/components/prism-http.js"() {
    (function(Prism2) {
      function headerValueOf(name) {
        return RegExp("(^(?:" + name + "):[ 	]*(?![ 	]))[^]+", "i");
      }
      Prism2.languages.http = {
        "request-line": {
          pattern: /^(?:CONNECT|DELETE|GET|HEAD|OPTIONS|PATCH|POST|PRI|PUT|SEARCH|TRACE)\s(?:https?:\/\/|\/)\S*\sHTTP\/[\d.]+/m,
          inside: {
            // HTTP Method
            "method": {
              pattern: /^[A-Z]+\b/,
              alias: "property"
            },
            // Request Target e.g. http://example.com, /path/to/file
            "request-target": {
              pattern: /^(\s)(?:https?:\/\/|\/)\S*(?=\s)/,
              lookbehind: true,
              alias: "url",
              inside: Prism2.languages.uri
            },
            // HTTP Version
            "http-version": {
              pattern: /^(\s)HTTP\/[\d.]+/,
              lookbehind: true,
              alias: "property"
            }
          }
        },
        "response-status": {
          pattern: /^HTTP\/[\d.]+ \d+ .+/m,
          inside: {
            // HTTP Version
            "http-version": {
              pattern: /^HTTP\/[\d.]+/,
              alias: "property"
            },
            // Status Code
            "status-code": {
              pattern: /^(\s)\d+(?=\s)/,
              lookbehind: true,
              alias: "number"
            },
            // Reason Phrase
            "reason-phrase": {
              pattern: /^(\s).+/,
              lookbehind: true,
              alias: "string"
            }
          }
        },
        "header": {
          pattern: /^[\w-]+:.+(?:(?:\r\n?|\n)[ \t].+)*/m,
          inside: {
            "header-value": [
              {
                pattern: headerValueOf(/Content-Security-Policy/.source),
                lookbehind: true,
                alias: ["csp", "languages-csp"],
                inside: Prism2.languages.csp
              },
              {
                pattern: headerValueOf(/Public-Key-Pins(?:-Report-Only)?/.source),
                lookbehind: true,
                alias: ["hpkp", "languages-hpkp"],
                inside: Prism2.languages.hpkp
              },
              {
                pattern: headerValueOf(/Strict-Transport-Security/.source),
                lookbehind: true,
                alias: ["hsts", "languages-hsts"],
                inside: Prism2.languages.hsts
              },
              {
                pattern: headerValueOf(/[^:]+/.source),
                lookbehind: true
              }
            ],
            "header-name": {
              pattern: /^[^:]+/,
              alias: "keyword"
            },
            "punctuation": /^:/
          }
        }
      };
      var langs = Prism2.languages;
      var httpLanguages = {
        "application/javascript": langs.javascript,
        "application/json": langs.json || langs.javascript,
        "application/xml": langs.xml,
        "text/xml": langs.xml,
        "text/html": langs.html,
        "text/css": langs.css,
        "text/plain": langs.plain
      };
      var suffixTypes = {
        "application/json": true,
        "application/xml": true
      };
      function getSuffixPattern(contentType2) {
        var suffix = contentType2.replace(/^[a-z]+\//, "");
        var suffixPattern = "\\w+/(?:[\\w.-]+\\+)+" + suffix + "(?![+\\w.-])";
        return "(?:" + contentType2 + "|" + suffixPattern + ")";
      }
      var options;
      for (var contentType in httpLanguages) {
        if (httpLanguages[contentType]) {
          options = options || {};
          var pattern = suffixTypes[contentType] ? getSuffixPattern(contentType) : contentType;
          options[contentType.replace(/\//g, "-")] = {
            pattern: RegExp(
              "(" + /content-type:\s*/.source + pattern + /(?:(?:\r\n?|\n)[\w-].*)*(?:\r(?:\n|(?!\n))|\n)/.source + ")" + // This is a little interesting:
              // The HTTP format spec required 1 empty line before the body to make everything unambiguous.
              // However, when writing code by hand (e.g. to display on a website) people can forget about this,
              // so we want to be liberal here. We will allow the empty line to be omitted if the first line of
              // the body does not start with a [\w-] character (as headers do).
              /[^ \t\w-][\s\S]*/.source,
              "i"
            ),
            lookbehind: true,
            inside: httpLanguages[contentType]
          };
        }
      }
      if (options) {
        Prism2.languages.insertBefore("http", "header", options);
      }
    })(Prism);
  }
});

// node_modules/prismjs/components/prism-java.js
var require_prism_java = __commonJS({
  "node_modules/prismjs/components/prism-java.js"() {
    (function(Prism2) {
      var keywords = /\b(?:abstract|assert|boolean|break|byte|case|catch|char|class|const|continue|default|do|double|else|enum|exports|extends|final|finally|float|for|goto|if|implements|import|instanceof|int|interface|long|module|native|new|non-sealed|null|open|opens|package|permits|private|protected|provides|public|record(?!\s*[(){}[\]<>=%~.:,;?+\-*/&|^])|requires|return|sealed|short|static|strictfp|super|switch|synchronized|this|throw|throws|to|transient|transitive|try|uses|var|void|volatile|while|with|yield)\b/;
      var classNamePrefix = /(?:[a-z]\w*\s*\.\s*)*(?:[A-Z]\w*\s*\.\s*)*/.source;
      var className = {
        pattern: RegExp(/(^|[^\w.])/.source + classNamePrefix + /[A-Z](?:[\d_A-Z]*[a-z]\w*)?\b/.source),
        lookbehind: true,
        inside: {
          "namespace": {
            pattern: /^[a-z]\w*(?:\s*\.\s*[a-z]\w*)*(?:\s*\.)?/,
            inside: {
              "punctuation": /\./
            }
          },
          "punctuation": /\./
        }
      };
      Prism2.languages.java = Prism2.languages.extend("clike", {
        "string": {
          pattern: /(^|[^\\])"(?:\\.|[^"\\\r\n])*"/,
          lookbehind: true,
          greedy: true
        },
        "class-name": [
          className,
          {
            // variables, parameters, and constructor references
            // this to support class names (or generic parameters) which do not contain a lower case letter (also works for methods)
            pattern: RegExp(/(^|[^\w.])/.source + classNamePrefix + /[A-Z]\w*(?=\s+\w+\s*[;,=()]|\s*(?:\[[\s,]*\]\s*)?::\s*new\b)/.source),
            lookbehind: true,
            inside: className.inside
          },
          {
            // class names based on keyword
            // this to support class names (or generic parameters) which do not contain a lower case letter (also works for methods)
            pattern: RegExp(/(\b(?:class|enum|extends|implements|instanceof|interface|new|record|throws)\s+)/.source + classNamePrefix + /[A-Z]\w*\b/.source),
            lookbehind: true,
            inside: className.inside
          }
        ],
        "keyword": keywords,
        "function": [
          Prism2.languages.clike.function,
          {
            pattern: /(::\s*)[a-z_]\w*/,
            lookbehind: true
          }
        ],
        "number": /\b0b[01][01_]*L?\b|\b0x(?:\.[\da-f_p+-]+|[\da-f_]+(?:\.[\da-f_p+-]+)?)\b|(?:\b\d[\d_]*(?:\.[\d_]*)?|\B\.\d[\d_]*)(?:e[+-]?\d[\d_]*)?[dfl]?/i,
        "operator": {
          pattern: /(^|[^.])(?:<<=?|>>>?=?|->|--|\+\+|&&|\|\||::|[?:~]|[-+*/%&|^!=<>]=?)/m,
          lookbehind: true
        },
        "constant": /\b[A-Z][A-Z_\d]+\b/
      });
      Prism2.languages.insertBefore("java", "string", {
        "triple-quoted-string": {
          // http://openjdk.java.net/jeps/355#Description
          pattern: /"""[ \t]*[\r\n](?:(?:"|"")?(?:\\.|[^"\\]))*"""/,
          greedy: true,
          alias: "string"
        },
        "char": {
          pattern: /'(?:\\.|[^'\\\r\n]){1,6}'/,
          greedy: true
        }
      });
      Prism2.languages.insertBefore("java", "class-name", {
        "annotation": {
          pattern: /(^|[^.])@\w+(?:\s*\.\s*\w+)*/,
          lookbehind: true,
          alias: "punctuation"
        },
        "generics": {
          pattern: /<(?:[\w\s,.?]|&(?!&)|<(?:[\w\s,.?]|&(?!&)|<(?:[\w\s,.?]|&(?!&)|<(?:[\w\s,.?]|&(?!&))*>)*>)*>)*>/,
          inside: {
            "class-name": className,
            "keyword": keywords,
            "punctuation": /[<>(),.:]/,
            "operator": /[?&|]/
          }
        },
        "import": [
          {
            pattern: RegExp(/(\bimport\s+)/.source + classNamePrefix + /(?:[A-Z]\w*|\*)(?=\s*;)/.source),
            lookbehind: true,
            inside: {
              "namespace": className.inside.namespace,
              "punctuation": /\./,
              "operator": /\*/,
              "class-name": /\w+/
            }
          },
          {
            pattern: RegExp(/(\bimport\s+static\s+)/.source + classNamePrefix + /(?:\w+|\*)(?=\s*;)/.source),
            lookbehind: true,
            alias: "static",
            inside: {
              "namespace": className.inside.namespace,
              "static": /\b\w+$/,
              "punctuation": /\./,
              "operator": /\*/,
              "class-name": /\w+/
            }
          }
        ],
        "namespace": {
          pattern: RegExp(
            /(\b(?:exports|import(?:\s+static)?|module|open|opens|package|provides|requires|to|transitive|uses|with)\s+)(?!<keyword>)[a-z]\w*(?:\.[a-z]\w*)*\.?/.source.replace(/<keyword>/g, function() {
              return keywords.source;
            })
          ),
          lookbehind: true,
          inside: {
            "punctuation": /\./
          }
        }
      });
    })(Prism);
  }
});

// node_modules/prismjs/components/prism-lua.js
var require_prism_lua = __commonJS({
  "node_modules/prismjs/components/prism-lua.js"() {
    Prism.languages.lua = {
      "comment": /^#!.+|--(?:\[(=*)\[[\s\S]*?\]\1\]|.*)/m,
      // \z may be used to skip the following space
      "string": {
        pattern: /(["'])(?:(?!\1)[^\\\r\n]|\\z(?:\r\n|\s)|\\(?:\r\n|[^z]))*\1|\[(=*)\[[\s\S]*?\]\2\]/,
        greedy: true
      },
      "number": /\b0x[a-f\d]+(?:\.[a-f\d]*)?(?:p[+-]?\d+)?\b|\b\d+(?:\.\B|(?:\.\d*)?(?:e[+-]?\d+)?\b)|\B\.\d+(?:e[+-]?\d+)?\b/i,
      "keyword": /\b(?:and|break|do|else|elseif|end|false|for|function|goto|if|in|local|nil|not|or|repeat|return|then|true|until|while)\b/,
      "function": /(?!\d)\w+(?=\s*(?:[({]))/,
      "operator": [
        /[-+*%^&|#]|\/\/?|<[<=]?|>[>=]?|[=~]=?/,
        {
          // Match ".." but don't break "..."
          pattern: /(^|[^.])\.\.(?!\.)/,
          lookbehind: true
        }
      ],
      "punctuation": /[\[\](){},;]|\.+|:+/
    };
  }
});

// node_modules/prismjs/components/prism-markup-templating.js
var require_prism_markup_templating = __commonJS({
  "node_modules/prismjs/components/prism-markup-templating.js"() {
    (function(Prism2) {
      function getPlaceholder(language, index) {
        return "___" + language.toUpperCase() + index + "___";
      }
      Object.defineProperties(Prism2.languages["markup-templating"] = {}, {
        buildPlaceholders: {
          /**
           * Tokenize all inline templating expressions matching `placeholderPattern`.
           *
           * If `replaceFilter` is provided, only matches of `placeholderPattern` for which `replaceFilter` returns
           * `true` will be replaced.
           *
           * @param {object} env The environment of the `before-tokenize` hook.
           * @param {string} language The language id.
           * @param {RegExp} placeholderPattern The matches of this pattern will be replaced by placeholders.
           * @param {(match: string) => boolean} [replaceFilter]
           */
          value: function(env2, language, placeholderPattern, replaceFilter) {
            if (env2.language !== language) {
              return;
            }
            var tokenStack = env2.tokenStack = [];
            env2.code = env2.code.replace(placeholderPattern, function(match2) {
              if (typeof replaceFilter === "function" && !replaceFilter(match2)) {
                return match2;
              }
              var i2 = tokenStack.length;
              var placeholder;
              while (env2.code.indexOf(placeholder = getPlaceholder(language, i2)) !== -1) {
                ++i2;
              }
              tokenStack[i2] = match2;
              return placeholder;
            });
            env2.grammar = Prism2.languages.markup;
          }
        },
        tokenizePlaceholders: {
          /**
           * Replace placeholders with proper tokens after tokenizing.
           *
           * @param {object} env The environment of the `after-tokenize` hook.
           * @param {string} language The language id.
           */
          value: function(env2, language) {
            if (env2.language !== language || !env2.tokenStack) {
              return;
            }
            env2.grammar = Prism2.languages[language];
            var j2 = 0;
            var keys2 = Object.keys(env2.tokenStack);
            function walkTokens(tokens) {
              for (var i2 = 0; i2 < tokens.length; i2++) {
                if (j2 >= keys2.length) {
                  break;
                }
                var token2 = tokens[i2];
                if (typeof token2 === "string" || token2.content && typeof token2.content === "string") {
                  var k2 = keys2[j2];
                  var t = env2.tokenStack[k2];
                  var s2 = typeof token2 === "string" ? token2 : token2.content;
                  var placeholder = getPlaceholder(language, k2);
                  var index = s2.indexOf(placeholder);
                  if (index > -1) {
                    ++j2;
                    var before = s2.substring(0, index);
                    var middle = new Prism2.Token(language, Prism2.tokenize(t, env2.grammar), "language-" + language, t);
                    var after = s2.substring(index + placeholder.length);
                    var replacement = [];
                    if (before) {
                      replacement.push.apply(replacement, walkTokens([before]));
                    }
                    replacement.push(middle);
                    if (after) {
                      replacement.push.apply(replacement, walkTokens([after]));
                    }
                    if (typeof token2 === "string") {
                      tokens.splice.apply(tokens, [i2, 1].concat(replacement));
                    } else {
                      token2.content = replacement;
                    }
                  }
                } else if (token2.content) {
                  walkTokens(token2.content);
                }
              }
              return tokens;
            }
            walkTokens(env2.tokens);
          }
        }
      });
    })(Prism);
  }
});

// node_modules/prismjs/components/prism-markup.js
var require_prism_markup = __commonJS({
  "node_modules/prismjs/components/prism-markup.js"() {
    Prism.languages.markup = {
      "comment": {
        pattern: /<!--(?:(?!<!--)[\s\S])*?-->/,
        greedy: true
      },
      "prolog": {
        pattern: /<\?[\s\S]+?\?>/,
        greedy: true
      },
      "doctype": {
        // https://www.w3.org/TR/xml/#NT-doctypedecl
        pattern: /<!DOCTYPE(?:[^>"'[\]]|"[^"]*"|'[^']*')+(?:\[(?:[^<"'\]]|"[^"]*"|'[^']*'|<(?!!--)|<!--(?:[^-]|-(?!->))*-->)*\]\s*)?>/i,
        greedy: true,
        inside: {
          "internal-subset": {
            pattern: /(^[^\[]*\[)[\s\S]+(?=\]>$)/,
            lookbehind: true,
            greedy: true,
            inside: null
            // see below
          },
          "string": {
            pattern: /"[^"]*"|'[^']*'/,
            greedy: true
          },
          "punctuation": /^<!|>$|[[\]]/,
          "doctype-tag": /^DOCTYPE/i,
          "name": /[^\s<>'"]+/
        }
      },
      "cdata": {
        pattern: /<!\[CDATA\[[\s\S]*?\]\]>/i,
        greedy: true
      },
      "tag": {
        pattern: /<\/?(?!\d)[^\s>\/=$<%]+(?:\s(?:\s*[^\s>\/=]+(?:\s*=\s*(?:"[^"]*"|'[^']*'|[^\s'">=]+(?=[\s>]))|(?=[\s/>])))+)?\s*\/?>/,
        greedy: true,
        inside: {
          "tag": {
            pattern: /^<\/?[^\s>\/]+/,
            inside: {
              "punctuation": /^<\/?/,
              "namespace": /^[^\s>\/:]+:/
            }
          },
          "special-attr": [],
          "attr-value": {
            pattern: /=\s*(?:"[^"]*"|'[^']*'|[^\s'">=]+)/,
            inside: {
              "punctuation": [
                {
                  pattern: /^=/,
                  alias: "attr-equals"
                },
                {
                  pattern: /^(\s*)["']|["']$/,
                  lookbehind: true
                }
              ]
            }
          },
          "punctuation": /\/?>/,
          "attr-name": {
            pattern: /[^\s>\/]+/,
            inside: {
              "namespace": /^[^\s>\/:]+:/
            }
          }
        }
      },
      "entity": [
        {
          pattern: /&[\da-z]{1,8};/i,
          alias: "named-entity"
        },
        /&#x?[\da-f]{1,8};/i
      ]
    };
    Prism.languages.markup["tag"].inside["attr-value"].inside["entity"] = Prism.languages.markup["entity"];
    Prism.languages.markup["doctype"].inside["internal-subset"].inside = Prism.languages.markup;
    Prism.hooks.add("wrap", function(env2) {
      if (env2.type === "entity") {
        env2.attributes["title"] = env2.content.replace(/&amp;/, "&");
      }
    });
    Object.defineProperty(Prism.languages.markup.tag, "addInlined", {
      /**
       * Adds an inlined language to markup.
       *
       * An example of an inlined language is CSS with `<style>` tags.
       *
       * @param {string} tagName The name of the tag that contains the inlined language. This name will be treated as
       * case insensitive.
       * @param {string} lang The language key.
       * @example
       * addInlined('style', 'css');
       */
      value: function addInlined(tagName, lang) {
        var includedCdataInside = {};
        includedCdataInside["language-" + lang] = {
          pattern: /(^<!\[CDATA\[)[\s\S]+?(?=\]\]>$)/i,
          lookbehind: true,
          inside: Prism.languages[lang]
        };
        includedCdataInside["cdata"] = /^<!\[CDATA\[|\]\]>$/i;
        var inside = {
          "included-cdata": {
            pattern: /<!\[CDATA\[[\s\S]*?\]\]>/i,
            inside: includedCdataInside
          }
        };
        inside["language-" + lang] = {
          pattern: /[\s\S]+/,
          inside: Prism.languages[lang]
        };
        var def = {};
        def[tagName] = {
          pattern: RegExp(/(<__[^>]*>)(?:<!\[CDATA\[(?:[^\]]|\](?!\]>))*\]\]>|(?!<!\[CDATA\[)[\s\S])*?(?=<\/__>)/.source.replace(/__/g, function() {
            return tagName;
          }), "i"),
          lookbehind: true,
          greedy: true,
          inside
        };
        Prism.languages.insertBefore("markup", "cdata", def);
      }
    });
    Object.defineProperty(Prism.languages.markup.tag, "addAttribute", {
      /**
       * Adds an pattern to highlight languages embedded in HTML attributes.
       *
       * An example of an inlined language is CSS with `style` attributes.
       *
       * @param {string} attrName The name of the tag that contains the inlined language. This name will be treated as
       * case insensitive.
       * @param {string} lang The language key.
       * @example
       * addAttribute('style', 'css');
       */
      value: function(attrName, lang) {
        Prism.languages.markup.tag.inside["special-attr"].push({
          pattern: RegExp(
            /(^|["'\s])/.source + "(?:" + attrName + ")" + /\s*=\s*(?:"[^"]*"|'[^']*'|[^\s'">=]+(?=[\s>]))/.source,
            "i"
          ),
          lookbehind: true,
          inside: {
            "attr-name": /^[^\s=]+/,
            "attr-value": {
              pattern: /=[\s\S]+/,
              inside: {
                "value": {
                  pattern: /(^=\s*(["']|(?!["'])))\S[\s\S]*(?=\2$)/,
                  lookbehind: true,
                  alias: [lang, "language-" + lang],
                  inside: Prism.languages[lang]
                },
                "punctuation": [
                  {
                    pattern: /^=/,
                    alias: "attr-equals"
                  },
                  /"|'/
                ]
              }
            }
          }
        });
      }
    });
    Prism.languages.html = Prism.languages.markup;
    Prism.languages.mathml = Prism.languages.markup;
    Prism.languages.svg = Prism.languages.markup;
    Prism.languages.xml = Prism.languages.extend("markup", {});
    Prism.languages.ssml = Prism.languages.xml;
    Prism.languages.atom = Prism.languages.xml;
    Prism.languages.rss = Prism.languages.xml;
  }
});

// node_modules/prismjs/components/prism-objectivec.js
var require_prism_objectivec = __commonJS({
  "node_modules/prismjs/components/prism-objectivec.js"() {
    Prism.languages.objectivec = Prism.languages.extend("c", {
      "string": {
        pattern: /@?"(?:\\(?:\r\n|[\s\S])|[^"\\\r\n])*"/,
        greedy: true
      },
      "keyword": /\b(?:asm|auto|break|case|char|const|continue|default|do|double|else|enum|extern|float|for|goto|if|in|inline|int|long|register|return|self|short|signed|sizeof|static|struct|super|switch|typedef|typeof|union|unsigned|void|volatile|while)\b|(?:@interface|@end|@implementation|@protocol|@class|@public|@protected|@private|@property|@try|@catch|@finally|@throw|@synthesize|@dynamic|@selector)\b/,
      "operator": /-[->]?|\+\+?|!=?|<<?=?|>>?=?|==?|&&?|\|\|?|[~^%?*\/@]/
    });
    delete Prism.languages.objectivec["class-name"];
    Prism.languages.objc = Prism.languages.objectivec;
  }
});

// node_modules/prismjs/components/prism-perl.js
var require_prism_perl = __commonJS({
  "node_modules/prismjs/components/prism-perl.js"() {
    (function(Prism2) {
      var brackets = /(?:\((?:[^()\\]|\\[\s\S])*\)|\{(?:[^{}\\]|\\[\s\S])*\}|\[(?:[^[\]\\]|\\[\s\S])*\]|<(?:[^<>\\]|\\[\s\S])*>)/.source;
      Prism2.languages.perl = {
        "comment": [
          {
            // POD
            pattern: /(^\s*)=\w[\s\S]*?=cut.*/m,
            lookbehind: true,
            greedy: true
          },
          {
            pattern: /(^|[^\\$])#.*/,
            lookbehind: true,
            greedy: true
          }
        ],
        // TODO Could be nice to handle Heredoc too.
        "string": [
          {
            pattern: RegExp(
              /\b(?:q|qq|qw|qx)(?![a-zA-Z0-9])\s*/.source + "(?:" + [
                // q/.../
                /([^a-zA-Z0-9\s{(\[<])(?:(?!\1)[^\\]|\\[\s\S])*\1/.source,
                // q a...a
                // eslint-disable-next-line regexp/strict
                /([a-zA-Z0-9])(?:(?!\2)[^\\]|\\[\s\S])*\2/.source,
                // q(...)
                // q{...}
                // q[...]
                // q<...>
                brackets
              ].join("|") + ")"
            ),
            greedy: true
          },
          // "...", `...`
          {
            pattern: /("|`)(?:(?!\1)[^\\]|\\[\s\S])*\1/,
            greedy: true
          },
          // '...'
          // FIXME Multi-line single-quoted strings are not supported as they would break variables containing '
          {
            pattern: /'(?:[^'\\\r\n]|\\.)*'/,
            greedy: true
          }
        ],
        "regex": [
          {
            pattern: RegExp(
              /\b(?:m|qr)(?![a-zA-Z0-9])\s*/.source + "(?:" + [
                // m/.../
                /([^a-zA-Z0-9\s{(\[<])(?:(?!\1)[^\\]|\\[\s\S])*\1/.source,
                // m a...a
                // eslint-disable-next-line regexp/strict
                /([a-zA-Z0-9])(?:(?!\2)[^\\]|\\[\s\S])*\2/.source,
                // m(...)
                // m{...}
                // m[...]
                // m<...>
                brackets
              ].join("|") + ")" + /[msixpodualngc]*/.source
            ),
            greedy: true
          },
          // The lookbehinds prevent -s from breaking
          {
            pattern: RegExp(
              /(^|[^-])\b(?:s|tr|y)(?![a-zA-Z0-9])\s*/.source + "(?:" + [
                // s/.../.../
                // eslint-disable-next-line regexp/strict
                /([^a-zA-Z0-9\s{(\[<])(?:(?!\2)[^\\]|\\[\s\S])*\2(?:(?!\2)[^\\]|\\[\s\S])*\2/.source,
                // s a...a...a
                // eslint-disable-next-line regexp/strict
                /([a-zA-Z0-9])(?:(?!\3)[^\\]|\\[\s\S])*\3(?:(?!\3)[^\\]|\\[\s\S])*\3/.source,
                // s(...)(...)
                // s{...}{...}
                // s[...][...]
                // s<...><...>
                // s(...)[...]
                brackets + /\s*/.source + brackets
              ].join("|") + ")" + /[msixpodualngcer]*/.source
            ),
            lookbehind: true,
            greedy: true
          },
          // /.../
          // The look-ahead tries to prevent two divisions on
          // the same line from being highlighted as regex.
          // This does not support multi-line regex.
          {
            pattern: /\/(?:[^\/\\\r\n]|\\.)*\/[msixpodualngc]*(?=\s*(?:$|[\r\n,.;})&|\-+*~<>!?^]|(?:and|cmp|eq|ge|gt|le|lt|ne|not|or|x|xor)\b))/,
            greedy: true
          }
        ],
        // FIXME Not sure about the handling of ::, ', and #
        "variable": [
          // ${^POSTMATCH}
          /[&*$@%]\{\^[A-Z]+\}/,
          // $^V
          /[&*$@%]\^[A-Z_]/,
          // ${...}
          /[&*$@%]#?(?=\{)/,
          // $foo
          /[&*$@%]#?(?:(?:::)*'?(?!\d)[\w$]+(?![\w$]))+(?:::)*/,
          // $1
          /[&*$@%]\d+/,
          // $_, @_, %!
          // The negative lookahead prevents from breaking the %= operator
          /(?!%=)[$@%][!"#$%&'()*+,\-.\/:;<=>?@[\\\]^_`{|}~]/
        ],
        "filehandle": {
          // <>, <FOO>, _
          pattern: /<(?![<=])\S*?>|\b_\b/,
          alias: "symbol"
        },
        "v-string": {
          // v1.2, 1.2.3
          pattern: /v\d+(?:\.\d+)*|\d+(?:\.\d+){2,}/,
          alias: "string"
        },
        "function": {
          pattern: /(\bsub[ \t]+)\w+/,
          lookbehind: true
        },
        "keyword": /\b(?:any|break|continue|default|delete|die|do|else|elsif|eval|for|foreach|given|goto|if|last|local|my|next|our|package|print|redo|require|return|say|state|sub|switch|undef|unless|until|use|when|while)\b/,
        "number": /\b(?:0x[\dA-Fa-f](?:_?[\dA-Fa-f])*|0b[01](?:_?[01])*|(?:(?:\d(?:_?\d)*)?\.)?\d(?:_?\d)*(?:[Ee][+-]?\d+)?)\b/,
        "operator": /-[rwxoRWXOezsfdlpSbctugkTBMAC]\b|\+[+=]?|-[-=>]?|\*\*?=?|\/\/?=?|=[=~>]?|~[~=]?|\|\|?=?|&&?=?|<(?:=>?|<=?)?|>>?=?|![~=]?|[%^]=?|\.(?:=|\.\.?)?|[\\?]|\bx(?:=|\b)|\b(?:and|cmp|eq|ge|gt|le|lt|ne|not|or|xor)\b/,
        "punctuation": /[{}[\];(),:]/
      };
    })(Prism);
  }
});

// node_modules/prismjs/components/prism-php.js
var require_prism_php = __commonJS({
  "node_modules/prismjs/components/prism-php.js"() {
    (function(Prism2) {
      var comment2 = /\/\*[\s\S]*?\*\/|\/\/.*|#(?!\[).*/;
      var constant = [
        {
          pattern: /\b(?:false|true)\b/i,
          alias: "boolean"
        },
        {
          pattern: /(::\s*)\b[a-z_]\w*\b(?!\s*\()/i,
          greedy: true,
          lookbehind: true
        },
        {
          pattern: /(\b(?:case|const)\s+)\b[a-z_]\w*(?=\s*[;=])/i,
          greedy: true,
          lookbehind: true
        },
        /\b(?:null)\b/i,
        /\b[A-Z_][A-Z0-9_]*\b(?!\s*\()/
      ];
      var number = /\b0b[01]+(?:_[01]+)*\b|\b0o[0-7]+(?:_[0-7]+)*\b|\b0x[\da-f]+(?:_[\da-f]+)*\b|(?:\b\d+(?:_\d+)*\.?(?:\d+(?:_\d+)*)?|\B\.\d+)(?:e[+-]?\d+)?/i;
      var operator = /<?=>|\?\?=?|\.{3}|\??->|[!=]=?=?|::|\*\*=?|--|\+\+|&&|\|\||<<|>>|[?~]|[/^|%*&<>.+-]=?/;
      var punctuation = /[{}\[\](),:;]/;
      Prism2.languages.php = {
        "delimiter": {
          pattern: /\?>$|^<\?(?:php(?=\s)|=)?/i,
          alias: "important"
        },
        "comment": comment2,
        "variable": /\$+(?:\w+\b|(?=\{))/,
        "package": {
          pattern: /(namespace\s+|use\s+(?:function\s+)?)(?:\\?\b[a-z_]\w*)+\b(?!\\)/i,
          lookbehind: true,
          inside: {
            "punctuation": /\\/
          }
        },
        "class-name-definition": {
          pattern: /(\b(?:class|enum|interface|trait)\s+)\b[a-z_]\w*(?!\\)\b/i,
          lookbehind: true,
          alias: "class-name"
        },
        "function-definition": {
          pattern: /(\bfunction\s+)[a-z_]\w*(?=\s*\()/i,
          lookbehind: true,
          alias: "function"
        },
        "keyword": [
          {
            pattern: /(\(\s*)\b(?:array|bool|boolean|float|int|integer|object|string)\b(?=\s*\))/i,
            alias: "type-casting",
            greedy: true,
            lookbehind: true
          },
          {
            pattern: /([(,?]\s*)\b(?:array(?!\s*\()|bool|callable|(?:false|null)(?=\s*\|)|float|int|iterable|mixed|object|self|static|string)\b(?=\s*\$)/i,
            alias: "type-hint",
            greedy: true,
            lookbehind: true
          },
          {
            pattern: /(\)\s*:\s*(?:\?\s*)?)\b(?:array(?!\s*\()|bool|callable|(?:false|null)(?=\s*\|)|float|int|iterable|mixed|never|object|self|static|string|void)\b/i,
            alias: "return-type",
            greedy: true,
            lookbehind: true
          },
          {
            pattern: /\b(?:array(?!\s*\()|bool|float|int|iterable|mixed|object|string|void)\b/i,
            alias: "type-declaration",
            greedy: true
          },
          {
            pattern: /(\|\s*)(?:false|null)\b|\b(?:false|null)(?=\s*\|)/i,
            alias: "type-declaration",
            greedy: true,
            lookbehind: true
          },
          {
            pattern: /\b(?:parent|self|static)(?=\s*::)/i,
            alias: "static-context",
            greedy: true
          },
          {
            // yield from
            pattern: /(\byield\s+)from\b/i,
            lookbehind: true
          },
          // `class` is always a keyword unlike other keywords
          /\bclass\b/i,
          {
            // https://www.php.net/manual/en/reserved.keywords.php
            //
            // keywords cannot be preceded by "->"
            // the complex lookbehind means `(?<!(?:->|::)\s*)`
            pattern: /((?:^|[^\s>:]|(?:^|[^-])>|(?:^|[^:]):)\s*)\b(?:abstract|and|array|as|break|callable|case|catch|clone|const|continue|declare|default|die|do|echo|else|elseif|empty|enddeclare|endfor|endforeach|endif|endswitch|endwhile|enum|eval|exit|extends|final|finally|fn|for|foreach|function|global|goto|if|implements|include|include_once|instanceof|insteadof|interface|isset|list|match|namespace|never|new|or|parent|print|private|protected|public|readonly|require|require_once|return|self|static|switch|throw|trait|try|unset|use|var|while|xor|yield|__halt_compiler)\b/i,
            lookbehind: true
          }
        ],
        "argument-name": {
          pattern: /([(,]\s*)\b[a-z_]\w*(?=\s*:(?!:))/i,
          lookbehind: true
        },
        "class-name": [
          {
            pattern: /(\b(?:extends|implements|instanceof|new(?!\s+self|\s+static))\s+|\bcatch\s*\()\b[a-z_]\w*(?!\\)\b/i,
            greedy: true,
            lookbehind: true
          },
          {
            pattern: /(\|\s*)\b[a-z_]\w*(?!\\)\b/i,
            greedy: true,
            lookbehind: true
          },
          {
            pattern: /\b[a-z_]\w*(?!\\)\b(?=\s*\|)/i,
            greedy: true
          },
          {
            pattern: /(\|\s*)(?:\\?\b[a-z_]\w*)+\b/i,
            alias: "class-name-fully-qualified",
            greedy: true,
            lookbehind: true,
            inside: {
              "punctuation": /\\/
            }
          },
          {
            pattern: /(?:\\?\b[a-z_]\w*)+\b(?=\s*\|)/i,
            alias: "class-name-fully-qualified",
            greedy: true,
            inside: {
              "punctuation": /\\/
            }
          },
          {
            pattern: /(\b(?:extends|implements|instanceof|new(?!\s+self\b|\s+static\b))\s+|\bcatch\s*\()(?:\\?\b[a-z_]\w*)+\b(?!\\)/i,
            alias: "class-name-fully-qualified",
            greedy: true,
            lookbehind: true,
            inside: {
              "punctuation": /\\/
            }
          },
          {
            pattern: /\b[a-z_]\w*(?=\s*\$)/i,
            alias: "type-declaration",
            greedy: true
          },
          {
            pattern: /(?:\\?\b[a-z_]\w*)+(?=\s*\$)/i,
            alias: ["class-name-fully-qualified", "type-declaration"],
            greedy: true,
            inside: {
              "punctuation": /\\/
            }
          },
          {
            pattern: /\b[a-z_]\w*(?=\s*::)/i,
            alias: "static-context",
            greedy: true
          },
          {
            pattern: /(?:\\?\b[a-z_]\w*)+(?=\s*::)/i,
            alias: ["class-name-fully-qualified", "static-context"],
            greedy: true,
            inside: {
              "punctuation": /\\/
            }
          },
          {
            pattern: /([(,?]\s*)[a-z_]\w*(?=\s*\$)/i,
            alias: "type-hint",
            greedy: true,
            lookbehind: true
          },
          {
            pattern: /([(,?]\s*)(?:\\?\b[a-z_]\w*)+(?=\s*\$)/i,
            alias: ["class-name-fully-qualified", "type-hint"],
            greedy: true,
            lookbehind: true,
            inside: {
              "punctuation": /\\/
            }
          },
          {
            pattern: /(\)\s*:\s*(?:\?\s*)?)\b[a-z_]\w*(?!\\)\b/i,
            alias: "return-type",
            greedy: true,
            lookbehind: true
          },
          {
            pattern: /(\)\s*:\s*(?:\?\s*)?)(?:\\?\b[a-z_]\w*)+\b(?!\\)/i,
            alias: ["class-name-fully-qualified", "return-type"],
            greedy: true,
            lookbehind: true,
            inside: {
              "punctuation": /\\/
            }
          }
        ],
        "constant": constant,
        "function": {
          pattern: /(^|[^\\\w])\\?[a-z_](?:[\w\\]*\w)?(?=\s*\()/i,
          lookbehind: true,
          inside: {
            "punctuation": /\\/
          }
        },
        "property": {
          pattern: /(->\s*)\w+/,
          lookbehind: true
        },
        "number": number,
        "operator": operator,
        "punctuation": punctuation
      };
      var string_interpolation = {
        pattern: /\{\$(?:\{(?:\{[^{}]+\}|[^{}]+)\}|[^{}])+\}|(^|[^\\{])\$+(?:\w+(?:\[[^\r\n\[\]]+\]|->\w+)?)/,
        lookbehind: true,
        inside: Prism2.languages.php
      };
      var string2 = [
        {
          pattern: /<<<'([^']+)'[\r\n](?:.*[\r\n])*?\1;/,
          alias: "nowdoc-string",
          greedy: true,
          inside: {
            "delimiter": {
              pattern: /^<<<'[^']+'|[a-z_]\w*;$/i,
              alias: "symbol",
              inside: {
                "punctuation": /^<<<'?|[';]$/
              }
            }
          }
        },
        {
          pattern: /<<<(?:"([^"]+)"[\r\n](?:.*[\r\n])*?\1;|([a-z_]\w*)[\r\n](?:.*[\r\n])*?\2;)/i,
          alias: "heredoc-string",
          greedy: true,
          inside: {
            "delimiter": {
              pattern: /^<<<(?:"[^"]+"|[a-z_]\w*)|[a-z_]\w*;$/i,
              alias: "symbol",
              inside: {
                "punctuation": /^<<<"?|[";]$/
              }
            },
            "interpolation": string_interpolation
          }
        },
        {
          pattern: /`(?:\\[\s\S]|[^\\`])*`/,
          alias: "backtick-quoted-string",
          greedy: true
        },
        {
          pattern: /'(?:\\[\s\S]|[^\\'])*'/,
          alias: "single-quoted-string",
          greedy: true
        },
        {
          pattern: /"(?:\\[\s\S]|[^\\"])*"/,
          alias: "double-quoted-string",
          greedy: true,
          inside: {
            "interpolation": string_interpolation
          }
        }
      ];
      Prism2.languages.insertBefore("php", "variable", {
        "string": string2,
        "attribute": {
          pattern: /#\[(?:[^"'\/#]|\/(?![*/])|\/\/.*$|#(?!\[).*$|\/\*(?:[^*]|\*(?!\/))*\*\/|"(?:\\[\s\S]|[^\\"])*"|'(?:\\[\s\S]|[^\\'])*')+\](?=\s*[a-z$#])/im,
          greedy: true,
          inside: {
            "attribute-content": {
              pattern: /^(#\[)[\s\S]+(?=\]$)/,
              lookbehind: true,
              // inside can appear subset of php
              inside: {
                "comment": comment2,
                "string": string2,
                "attribute-class-name": [
                  {
                    pattern: /([^:]|^)\b[a-z_]\w*(?!\\)\b/i,
                    alias: "class-name",
                    greedy: true,
                    lookbehind: true
                  },
                  {
                    pattern: /([^:]|^)(?:\\?\b[a-z_]\w*)+/i,
                    alias: [
                      "class-name",
                      "class-name-fully-qualified"
                    ],
                    greedy: true,
                    lookbehind: true,
                    inside: {
                      "punctuation": /\\/
                    }
                  }
                ],
                "constant": constant,
                "number": number,
                "operator": operator,
                "punctuation": punctuation
              }
            },
            "delimiter": {
              pattern: /^#\[|\]$/,
              alias: "punctuation"
            }
          }
        }
      });
      Prism2.hooks.add("before-tokenize", function(env2) {
        if (!/<\?/.test(env2.code)) {
          return;
        }
        var phpPattern = /<\?(?:[^"'/#]|\/(?![*/])|("|')(?:\\[\s\S]|(?!\1)[^\\])*\1|(?:\/\/|#(?!\[))(?:[^?\n\r]|\?(?!>))*(?=$|\?>|[\r\n])|#\[|\/\*(?:[^*]|\*(?!\/))*(?:\*\/|$))*?(?:\?>|$)/g;
        Prism2.languages["markup-templating"].buildPlaceholders(env2, "php", phpPattern);
      });
      Prism2.hooks.add("after-tokenize", function(env2) {
        Prism2.languages["markup-templating"].tokenizePlaceholders(env2, "php");
      });
    })(Prism);
  }
});

// node_modules/prismjs/components/prism-python.js
var require_prism_python = __commonJS({
  "node_modules/prismjs/components/prism-python.js"() {
    Prism.languages.python = {
      "comment": {
        pattern: /(^|[^\\])#.*/,
        lookbehind: true,
        greedy: true
      },
      "string-interpolation": {
        pattern: /(?:f|fr|rf)(?:("""|''')[\s\S]*?\1|("|')(?:\\.|(?!\2)[^\\\r\n])*\2)/i,
        greedy: true,
        inside: {
          "interpolation": {
            // "{" <expression> <optional "!s", "!r", or "!a"> <optional ":" format specifier> "}"
            pattern: /((?:^|[^{])(?:\{\{)*)\{(?!\{)(?:[^{}]|\{(?!\{)(?:[^{}]|\{(?!\{)(?:[^{}])+\})+\})+\}/,
            lookbehind: true,
            inside: {
              "format-spec": {
                pattern: /(:)[^:(){}]+(?=\}$)/,
                lookbehind: true
              },
              "conversion-option": {
                pattern: /![sra](?=[:}]$)/,
                alias: "punctuation"
              },
              rest: null
            }
          },
          "string": /[\s\S]+/
        }
      },
      "triple-quoted-string": {
        pattern: /(?:[rub]|br|rb)?("""|''')[\s\S]*?\1/i,
        greedy: true,
        alias: "string"
      },
      "string": {
        pattern: /(?:[rub]|br|rb)?("|')(?:\\.|(?!\1)[^\\\r\n])*\1/i,
        greedy: true
      },
      "function": {
        pattern: /((?:^|\s)def[ \t]+)[a-zA-Z_]\w*(?=\s*\()/g,
        lookbehind: true
      },
      "class-name": {
        pattern: /(\bclass\s+)\w+/i,
        lookbehind: true
      },
      "decorator": {
        pattern: /(^[\t ]*)@\w+(?:\.\w+)*/m,
        lookbehind: true,
        alias: ["annotation", "punctuation"],
        inside: {
          "punctuation": /\./
        }
      },
      "keyword": /\b(?:_(?=\s*:)|and|as|assert|async|await|break|case|class|continue|def|del|elif|else|except|exec|finally|for|from|global|if|import|in|is|lambda|match|nonlocal|not|or|pass|print|raise|return|try|while|with|yield)\b/,
      "builtin": /\b(?:__import__|abs|all|any|apply|ascii|basestring|bin|bool|buffer|bytearray|bytes|callable|chr|classmethod|cmp|coerce|compile|complex|delattr|dict|dir|divmod|enumerate|eval|execfile|file|filter|float|format|frozenset|getattr|globals|hasattr|hash|help|hex|id|input|int|intern|isinstance|issubclass|iter|len|list|locals|long|map|max|memoryview|min|next|object|oct|open|ord|pow|property|range|raw_input|reduce|reload|repr|reversed|round|set|setattr|slice|sorted|staticmethod|str|sum|super|tuple|type|unichr|unicode|vars|xrange|zip)\b/,
      "boolean": /\b(?:False|None|True)\b/,
      "number": /\b0(?:b(?:_?[01])+|o(?:_?[0-7])+|x(?:_?[a-f0-9])+)\b|(?:\b\d+(?:_\d+)*(?:\.(?:\d+(?:_\d+)*)?)?|\B\.\d+(?:_\d+)*)(?:e[+-]?\d+(?:_\d+)*)?j?(?!\w)/i,
      "operator": /[-+%=]=?|!=|:=|\*\*?=?|\/\/?=?|<[<=>]?|>[=>]?|[&|^~]/,
      "punctuation": /[{}[\];(),.:]/
    };
    Prism.languages.python["string-interpolation"].inside["interpolation"].inside.rest = Prism.languages.python;
    Prism.languages.py = Prism.languages.python;
  }
});

// node_modules/prismjs/components/prism-q.js
var require_prism_q = __commonJS({
  "node_modules/prismjs/components/prism-q.js"() {
    Prism.languages.q = {
      "string": /"(?:\\.|[^"\\\r\n])*"/,
      "comment": [
        // From http://code.kx.com/wiki/Reference/Slash:
        // When / is following a space (or a right parenthesis, bracket, or brace), it is ignored with the rest of the line.
        {
          pattern: /([\t )\]}])\/.*/,
          lookbehind: true,
          greedy: true
        },
        // From http://code.kx.com/wiki/Reference/Slash:
        // A line which has / as its first character and contains at least one other non-whitespace character is a whole-line comment and is ignored entirely.
        // A / on a line by itself begins a multiline comment which is terminated by the next \ on a line by itself.
        // If a / is not matched by a \, the multiline comment is unterminated and continues to end of file.
        // The / and \ must be the first char on the line, but may be followed by any amount of whitespace.
        {
          pattern: /(^|\r?\n|\r)\/[\t ]*(?:(?:\r?\n|\r)(?:.*(?:\r?\n|\r(?!\n)))*?(?:\\(?=[\t ]*(?:\r?\n|\r))|$)|\S.*)/,
          lookbehind: true,
          greedy: true
        },
        // From http://code.kx.com/wiki/Reference/Slash:
        // A \ on a line by itself with no preceding matching / will comment to end of file.
        {
          pattern: /^\\[\t ]*(?:\r?\n|\r)[\s\S]+/m,
          greedy: true
        },
        {
          pattern: /^#!.+/m,
          greedy: true
        }
      ],
      "symbol": /`(?::\S+|[\w.]*)/,
      "datetime": {
        pattern: /0N[mdzuvt]|0W[dtz]|\d{4}\.\d\d(?:m|\.\d\d(?:T(?:\d\d(?::\d\d(?::\d\d(?:[.:]\d\d\d)?)?)?)?)?[dz]?)|\d\d:\d\d(?::\d\d(?:[.:]\d\d\d)?)?[uvt]?/,
        alias: "number"
      },
      // The negative look-ahead prevents bad highlighting
      // of verbs 0: and 1:
      "number": /\b(?![01]:)(?:0N[hje]?|0W[hj]?|0[wn]|0x[\da-fA-F]+|\d+(?:\.\d*)?(?:e[+-]?\d+)?[hjfeb]?)/,
      "keyword": /\\\w+\b|\b(?:abs|acos|aj0?|all|and|any|asc|asin|asof|atan|attr|avgs?|binr?|by|ceiling|cols|cor|cos|count|cov|cross|csv|cut|delete|deltas|desc|dev|differ|distinct|div|do|dsave|ej|enlist|eval|except|exec|exit|exp|fby|fills|first|fkeys|flip|floor|from|get|getenv|group|gtime|hclose|hcount|hdel|hopen|hsym|iasc|identity|idesc|if|ij|in|insert|inter|inv|keys?|last|like|list|ljf?|load|log|lower|lsq|ltime|ltrim|mavg|maxs?|mcount|md5|mdev|med|meta|mins?|mmax|mmin|mmu|mod|msum|neg|next|not|null|or|over|parse|peach|pj|plist|prds?|prev|prior|rand|rank|ratios|raze|read0|read1|reciprocal|reval|reverse|rload|rotate|rsave|rtrim|save|scan|scov|sdev|select|set|setenv|show|signum|sin|sqrt|ssr?|string|sublist|sums?|sv|svar|system|tables|tan|til|trim|txf|type|uj|ungroup|union|update|upper|upsert|value|var|views?|vs|wavg|where|while|within|wj1?|wsum|ww|xasc|xbar|xcols?|xdesc|xexp|xgroup|xkey|xlog|xprev|xrank)\b/,
      "adverb": {
        pattern: /['\/\\]:?|\beach\b/,
        alias: "function"
      },
      "verb": {
        pattern: /(?:\B\.\B|\b[01]:|<[=>]?|>=?|[:+\-*%,!?~=|$&#@^]):?|\b_\b:?/,
        alias: "operator"
      },
      "punctuation": /[(){}\[\];.]/
    };
  }
});

// node_modules/prismjs/components/prism-ruby.js
var require_prism_ruby = __commonJS({
  "node_modules/prismjs/components/prism-ruby.js"() {
    (function(Prism2) {
      Prism2.languages.ruby = Prism2.languages.extend("clike", {
        "comment": {
          pattern: /#.*|^=begin\s[\s\S]*?^=end/m,
          greedy: true
        },
        "class-name": {
          pattern: /(\b(?:class|module)\s+|\bcatch\s+\()[\w.\\]+|\b[A-Z_]\w*(?=\s*\.\s*new\b)/,
          lookbehind: true,
          inside: {
            "punctuation": /[.\\]/
          }
        },
        "keyword": /\b(?:BEGIN|END|alias|and|begin|break|case|class|def|define_method|defined|do|each|else|elsif|end|ensure|extend|for|if|in|include|module|new|next|nil|not|or|prepend|private|protected|public|raise|redo|require|rescue|retry|return|self|super|then|throw|undef|unless|until|when|while|yield)\b/,
        "operator": /\.{2,3}|&\.|===|<?=>|[!=]?~|(?:&&|\|\||<<|>>|\*\*|[+\-*/%<>!^&|=])=?|[?:]/,
        "punctuation": /[(){}[\].,;]/
      });
      Prism2.languages.insertBefore("ruby", "operator", {
        "double-colon": {
          pattern: /::/,
          alias: "punctuation"
        }
      });
      var interpolation = {
        pattern: /((?:^|[^\\])(?:\\{2})*)#\{(?:[^{}]|\{[^{}]*\})*\}/,
        lookbehind: true,
        inside: {
          "content": {
            pattern: /^(#\{)[\s\S]+(?=\}$)/,
            lookbehind: true,
            inside: Prism2.languages.ruby
          },
          "delimiter": {
            pattern: /^#\{|\}$/,
            alias: "punctuation"
          }
        }
      };
      delete Prism2.languages.ruby.function;
      var percentExpression = "(?:" + [
        /([^a-zA-Z0-9\s{(\[<=])(?:(?!\1)[^\\]|\\[\s\S])*\1/.source,
        /\((?:[^()\\]|\\[\s\S]|\((?:[^()\\]|\\[\s\S])*\))*\)/.source,
        /\{(?:[^{}\\]|\\[\s\S]|\{(?:[^{}\\]|\\[\s\S])*\})*\}/.source,
        /\[(?:[^\[\]\\]|\\[\s\S]|\[(?:[^\[\]\\]|\\[\s\S])*\])*\]/.source,
        /<(?:[^<>\\]|\\[\s\S]|<(?:[^<>\\]|\\[\s\S])*>)*>/.source
      ].join("|") + ")";
      var symbolName = /(?:"(?:\\.|[^"\\\r\n])*"|(?:\b[a-zA-Z_]\w*|[^\s\0-\x7F]+)[?!]?|\$.)/.source;
      Prism2.languages.insertBefore("ruby", "keyword", {
        "regex-literal": [
          {
            pattern: RegExp(/%r/.source + percentExpression + /[egimnosux]{0,6}/.source),
            greedy: true,
            inside: {
              "interpolation": interpolation,
              "regex": /[\s\S]+/
            }
          },
          {
            pattern: /(^|[^/])\/(?!\/)(?:\[[^\r\n\]]+\]|\\.|[^[/\\\r\n])+\/[egimnosux]{0,6}(?=\s*(?:$|[\r\n,.;})#]))/,
            lookbehind: true,
            greedy: true,
            inside: {
              "interpolation": interpolation,
              "regex": /[\s\S]+/
            }
          }
        ],
        "variable": /[@$]+[a-zA-Z_]\w*(?:[?!]|\b)/,
        "symbol": [
          {
            pattern: RegExp(/(^|[^:]):/.source + symbolName),
            lookbehind: true,
            greedy: true
          },
          {
            pattern: RegExp(/([\r\n{(,][ \t]*)/.source + symbolName + /(?=:(?!:))/.source),
            lookbehind: true,
            greedy: true
          }
        ],
        "method-definition": {
          pattern: /(\bdef\s+)\w+(?:\s*\.\s*\w+)?/,
          lookbehind: true,
          inside: {
            "function": /\b\w+$/,
            "keyword": /^self\b/,
            "class-name": /^\w+/,
            "punctuation": /\./
          }
        }
      });
      Prism2.languages.insertBefore("ruby", "string", {
        "string-literal": [
          {
            pattern: RegExp(/%[qQiIwWs]?/.source + percentExpression),
            greedy: true,
            inside: {
              "interpolation": interpolation,
              "string": /[\s\S]+/
            }
          },
          {
            pattern: /("|')(?:#\{[^}]+\}|#(?!\{)|\\(?:\r\n|[\s\S])|(?!\1)[^\\#\r\n])*\1/,
            greedy: true,
            inside: {
              "interpolation": interpolation,
              "string": /[\s\S]+/
            }
          },
          {
            pattern: /<<[-~]?([a-z_]\w*)[\r\n](?:.*[\r\n])*?[\t ]*\1/i,
            alias: "heredoc-string",
            greedy: true,
            inside: {
              "delimiter": {
                pattern: /^<<[-~]?[a-z_]\w*|\b[a-z_]\w*$/i,
                inside: {
                  "symbol": /\b\w+/,
                  "punctuation": /^<<[-~]?/
                }
              },
              "interpolation": interpolation,
              "string": /[\s\S]+/
            }
          },
          {
            pattern: /<<[-~]?'([a-z_]\w*)'[\r\n](?:.*[\r\n])*?[\t ]*\1/i,
            alias: "heredoc-string",
            greedy: true,
            inside: {
              "delimiter": {
                pattern: /^<<[-~]?'[a-z_]\w*'|\b[a-z_]\w*$/i,
                inside: {
                  "symbol": /\b\w+/,
                  "punctuation": /^<<[-~]?'|'$/
                }
              },
              "string": /[\s\S]+/
            }
          }
        ],
        "command-literal": [
          {
            pattern: RegExp(/%x/.source + percentExpression),
            greedy: true,
            inside: {
              "interpolation": interpolation,
              "command": {
                pattern: /[\s\S]+/,
                alias: "string"
              }
            }
          },
          {
            pattern: /`(?:#\{[^}]+\}|#(?!\{)|\\(?:\r\n|[\s\S])|[^\\`#\r\n])*`/,
            greedy: true,
            inside: {
              "interpolation": interpolation,
              "command": {
                pattern: /[\s\S]+/,
                alias: "string"
              }
            }
          }
        ]
      });
      delete Prism2.languages.ruby.string;
      Prism2.languages.insertBefore("ruby", "number", {
        "builtin": /\b(?:Array|Bignum|Binding|Class|Continuation|Dir|Exception|FalseClass|File|Fixnum|Float|Hash|IO|Integer|MatchData|Method|Module|NilClass|Numeric|Object|Proc|Range|Regexp|Stat|String|Struct|Symbol|TMS|Thread|ThreadGroup|Time|TrueClass)\b/,
        "constant": /\b[A-Z][A-Z0-9_]*(?:[?!]|\b)/
      });
      Prism2.languages.rb = Prism2.languages.ruby;
    })(Prism);
  }
});

// node_modules/prismjs/components/prism-scala.js
var require_prism_scala = __commonJS({
  "node_modules/prismjs/components/prism-scala.js"() {
    Prism.languages.scala = Prism.languages.extend("java", {
      "triple-quoted-string": {
        pattern: /"""[\s\S]*?"""/,
        greedy: true,
        alias: "string"
      },
      "string": {
        pattern: /("|')(?:\\.|(?!\1)[^\\\r\n])*\1/,
        greedy: true
      },
      "keyword": /<-|=>|\b(?:abstract|case|catch|class|def|derives|do|else|enum|extends|extension|final|finally|for|forSome|given|if|implicit|import|infix|inline|lazy|match|new|null|object|opaque|open|override|package|private|protected|return|sealed|self|super|this|throw|trait|transparent|try|type|using|val|var|while|with|yield)\b/,
      "number": /\b0x(?:[\da-f]*\.)?[\da-f]+|(?:\b\d+(?:\.\d*)?|\B\.\d+)(?:e\d+)?[dfl]?/i,
      "builtin": /\b(?:Any|AnyRef|AnyVal|Boolean|Byte|Char|Double|Float|Int|Long|Nothing|Short|String|Unit)\b/,
      "symbol": /'[^\d\s\\]\w*/
    });
    Prism.languages.insertBefore("scala", "triple-quoted-string", {
      "string-interpolation": {
        pattern: /\b[a-z]\w*(?:"""(?:[^$]|\$(?:[^{]|\{(?:[^{}]|\{[^{}]*\})*\}))*?"""|"(?:[^$"\r\n]|\$(?:[^{]|\{(?:[^{}]|\{[^{}]*\})*\}))*")/i,
        greedy: true,
        inside: {
          "id": {
            pattern: /^\w+/,
            greedy: true,
            alias: "function"
          },
          "escape": {
            pattern: /\\\$"|\$[$"]/,
            greedy: true,
            alias: "symbol"
          },
          "interpolation": {
            pattern: /\$(?:\w+|\{(?:[^{}]|\{[^{}]*\})*\})/,
            greedy: true,
            inside: {
              "punctuation": /^\$\{?|\}$/,
              "expression": {
                pattern: /[\s\S]+/,
                inside: Prism.languages.scala
              }
            }
          },
          "string": /[\s\S]+/
        }
      }
    });
    delete Prism.languages.scala["class-name"];
    delete Prism.languages.scala["function"];
    delete Prism.languages.scala["constant"];
  }
});

// node_modules/prismjs/components/prism-sql.js
var require_prism_sql = __commonJS({
  "node_modules/prismjs/components/prism-sql.js"() {
    Prism.languages.sql = {
      "comment": {
        pattern: /(^|[^\\])(?:\/\*[\s\S]*?\*\/|(?:--|\/\/|#).*)/,
        lookbehind: true
      },
      "variable": [
        {
          pattern: /@(["'`])(?:\\[\s\S]|(?!\1)[^\\])+\1/,
          greedy: true
        },
        /@[\w.$]+/
      ],
      "string": {
        pattern: /(^|[^@\\])("|')(?:\\[\s\S]|(?!\2)[^\\]|\2\2)*\2/,
        greedy: true,
        lookbehind: true
      },
      "identifier": {
        pattern: /(^|[^@\\])`(?:\\[\s\S]|[^`\\]|``)*`/,
        greedy: true,
        lookbehind: true,
        inside: {
          "punctuation": /^`|`$/
        }
      },
      "function": /\b(?:AVG|COUNT|FIRST|FORMAT|LAST|LCASE|LEN|MAX|MID|MIN|MOD|NOW|ROUND|SUM|UCASE)(?=\s*\()/i,
      // Should we highlight user defined functions too?
      "keyword": /\b(?:ACTION|ADD|AFTER|ALGORITHM|ALL|ALTER|ANALYZE|ANY|APPLY|AS|ASC|AUTHORIZATION|AUTO_INCREMENT|BACKUP|BDB|BEGIN|BERKELEYDB|BIGINT|BINARY|BIT|BLOB|BOOL|BOOLEAN|BREAK|BROWSE|BTREE|BULK|BY|CALL|CASCADED?|CASE|CHAIN|CHAR(?:ACTER|SET)?|CHECK(?:POINT)?|CLOSE|CLUSTERED|COALESCE|COLLATE|COLUMNS?|COMMENT|COMMIT(?:TED)?|COMPUTE|CONNECT|CONSISTENT|CONSTRAINT|CONTAINS(?:TABLE)?|CONTINUE|CONVERT|CREATE|CROSS|CURRENT(?:_DATE|_TIME|_TIMESTAMP|_USER)?|CURSOR|CYCLE|DATA(?:BASES?)?|DATE(?:TIME)?|DAY|DBCC|DEALLOCATE|DEC|DECIMAL|DECLARE|DEFAULT|DEFINER|DELAYED|DELETE|DELIMITERS?|DENY|DESC|DESCRIBE|DETERMINISTIC|DISABLE|DISCARD|DISK|DISTINCT|DISTINCTROW|DISTRIBUTED|DO|DOUBLE|DROP|DUMMY|DUMP(?:FILE)?|DUPLICATE|ELSE(?:IF)?|ENABLE|ENCLOSED|END|ENGINE|ENUM|ERRLVL|ERRORS|ESCAPED?|EXCEPT|EXEC(?:UTE)?|EXISTS|EXIT|EXPLAIN|EXTENDED|FETCH|FIELDS|FILE|FILLFACTOR|FIRST|FIXED|FLOAT|FOLLOWING|FOR(?: EACH ROW)?|FORCE|FOREIGN|FREETEXT(?:TABLE)?|FROM|FULL|FUNCTION|GEOMETRY(?:COLLECTION)?|GLOBAL|GOTO|GRANT|GROUP|HANDLER|HASH|HAVING|HOLDLOCK|HOUR|IDENTITY(?:COL|_INSERT)?|IF|IGNORE|IMPORT|INDEX|INFILE|INNER|INNODB|INOUT|INSERT|INT|INTEGER|INTERSECT|INTERVAL|INTO|INVOKER|ISOLATION|ITERATE|JOIN|KEYS?|KILL|LANGUAGE|LAST|LEAVE|LEFT|LEVEL|LIMIT|LINENO|LINES|LINESTRING|LOAD|LOCAL|LOCK|LONG(?:BLOB|TEXT)|LOOP|MATCH(?:ED)?|MEDIUM(?:BLOB|INT|TEXT)|MERGE|MIDDLEINT|MINUTE|MODE|MODIFIES|MODIFY|MONTH|MULTI(?:LINESTRING|POINT|POLYGON)|NATIONAL|NATURAL|NCHAR|NEXT|NO|NONCLUSTERED|NULLIF|NUMERIC|OFF?|OFFSETS?|ON|OPEN(?:DATASOURCE|QUERY|ROWSET)?|OPTIMIZE|OPTION(?:ALLY)?|ORDER|OUT(?:ER|FILE)?|OVER|PARTIAL|PARTITION|PERCENT|PIVOT|PLAN|POINT|POLYGON|PRECEDING|PRECISION|PREPARE|PREV|PRIMARY|PRINT|PRIVILEGES|PROC(?:EDURE)?|PUBLIC|PURGE|QUICK|RAISERROR|READS?|REAL|RECONFIGURE|REFERENCES|RELEASE|RENAME|REPEAT(?:ABLE)?|REPLACE|REPLICATION|REQUIRE|RESIGNAL|RESTORE|RESTRICT|RETURN(?:ING|S)?|REVOKE|RIGHT|ROLLBACK|ROUTINE|ROW(?:COUNT|GUIDCOL|S)?|RTREE|RULE|SAVE(?:POINT)?|SCHEMA|SECOND|SELECT|SERIAL(?:IZABLE)?|SESSION(?:_USER)?|SET(?:USER)?|SHARE|SHOW|SHUTDOWN|SIMPLE|SMALLINT|SNAPSHOT|SOME|SONAME|SQL|START(?:ING)?|STATISTICS|STATUS|STRIPED|SYSTEM_USER|TABLES?|TABLESPACE|TEMP(?:ORARY|TABLE)?|TERMINATED|TEXT(?:SIZE)?|THEN|TIME(?:STAMP)?|TINY(?:BLOB|INT|TEXT)|TOP?|TRAN(?:SACTIONS?)?|TRIGGER|TRUNCATE|TSEQUAL|TYPES?|UNBOUNDED|UNCOMMITTED|UNDEFINED|UNION|UNIQUE|UNLOCK|UNPIVOT|UNSIGNED|UPDATE(?:TEXT)?|USAGE|USE|USER|USING|VALUES?|VAR(?:BINARY|CHAR|CHARACTER|YING)|VIEW|WAITFOR|WARNINGS|WHEN|WHERE|WHILE|WITH(?: ROLLUP|IN)?|WORK|WRITE(?:TEXT)?|YEAR)\b/i,
      "boolean": /\b(?:FALSE|NULL|TRUE)\b/i,
      "number": /\b0x[\da-f]+\b|\b\d+(?:\.\d*)?|\B\.\d+\b/i,
      "operator": /[-+*\/=%^~]|&&?|\|\|?|!=?|<(?:=>?|<|>)?|>[>=]?|\b(?:AND|BETWEEN|DIV|ILIKE|IN|IS|LIKE|NOT|OR|REGEXP|RLIKE|SOUNDS LIKE|XOR)\b/i,
      "punctuation": /[;[\]()`,.]/
    };
  }
});

// node_modules/prismjs/components/prism-swift.js
var require_prism_swift = __commonJS({
  "node_modules/prismjs/components/prism-swift.js"() {
    Prism.languages.swift = {
      "comment": {
        // Nested comments are supported up to 2 levels
        pattern: /(^|[^\\:])(?:\/\/.*|\/\*(?:[^/*]|\/(?!\*)|\*(?!\/)|\/\*(?:[^*]|\*(?!\/))*\*\/)*\*\/)/,
        lookbehind: true,
        greedy: true
      },
      "string-literal": [
        // https://docs.swift.org/swift-book/LanguageGuide/StringsAndCharacters.html
        {
          pattern: RegExp(
            /(^|[^"#])/.source + "(?:" + /"(?:\\(?:\((?:[^()]|\([^()]*\))*\)|\r\n|[^(])|[^\\\r\n"])*"/.source + "|" + /"""(?:\\(?:\((?:[^()]|\([^()]*\))*\)|[^(])|[^\\"]|"(?!""))*"""/.source + ")" + /(?!["#])/.source
          ),
          lookbehind: true,
          greedy: true,
          inside: {
            "interpolation": {
              pattern: /(\\\()(?:[^()]|\([^()]*\))*(?=\))/,
              lookbehind: true,
              inside: null
              // see below
            },
            "interpolation-punctuation": {
              pattern: /^\)|\\\($/,
              alias: "punctuation"
            },
            "punctuation": /\\(?=[\r\n])/,
            "string": /[\s\S]+/
          }
        },
        {
          pattern: RegExp(
            /(^|[^"#])(#+)/.source + "(?:" + /"(?:\\(?:#+\((?:[^()]|\([^()]*\))*\)|\r\n|[^#])|[^\\\r\n])*?"/.source + "|" + /"""(?:\\(?:#+\((?:[^()]|\([^()]*\))*\)|[^#])|[^\\])*?"""/.source + ")\\2"
          ),
          lookbehind: true,
          greedy: true,
          inside: {
            "interpolation": {
              pattern: /(\\#+\()(?:[^()]|\([^()]*\))*(?=\))/,
              lookbehind: true,
              inside: null
              // see below
            },
            "interpolation-punctuation": {
              pattern: /^\)|\\#+\($/,
              alias: "punctuation"
            },
            "string": /[\s\S]+/
          }
        }
      ],
      "directive": {
        // directives with conditions
        pattern: RegExp(
          /#/.source + "(?:" + (/(?:elseif|if)\b/.source + "(?:[ 	]*" + /(?:![ \t]*)?(?:\b\w+\b(?:[ \t]*\((?:[^()]|\([^()]*\))*\))?|\((?:[^()]|\([^()]*\))*\))(?:[ \t]*(?:&&|\|\|))?/.source + ")+") + "|" + /(?:else|endif)\b/.source + ")"
        ),
        alias: "property",
        inside: {
          "directive-name": /^#\w+/,
          "boolean": /\b(?:false|true)\b/,
          "number": /\b\d+(?:\.\d+)*\b/,
          "operator": /!|&&|\|\||[<>]=?/,
          "punctuation": /[(),]/
        }
      },
      "literal": {
        pattern: /#(?:colorLiteral|column|dsohandle|file(?:ID|Literal|Path)?|function|imageLiteral|line)\b/,
        alias: "constant"
      },
      "other-directive": {
        pattern: /#\w+\b/,
        alias: "property"
      },
      "attribute": {
        pattern: /@\w+/,
        alias: "atrule"
      },
      "function-definition": {
        pattern: /(\bfunc\s+)\w+/,
        lookbehind: true,
        alias: "function"
      },
      "label": {
        // https://docs.swift.org/swift-book/LanguageGuide/ControlFlow.html#ID141
        pattern: /\b(break|continue)\s+\w+|\b[a-zA-Z_]\w*(?=\s*:\s*(?:for|repeat|while)\b)/,
        lookbehind: true,
        alias: "important"
      },
      "keyword": /\b(?:Any|Protocol|Self|Type|actor|as|assignment|associatedtype|associativity|async|await|break|case|catch|class|continue|convenience|default|defer|deinit|didSet|do|dynamic|else|enum|extension|fallthrough|fileprivate|final|for|func|get|guard|higherThan|if|import|in|indirect|infix|init|inout|internal|is|isolated|lazy|left|let|lowerThan|mutating|none|nonisolated|nonmutating|open|operator|optional|override|postfix|precedencegroup|prefix|private|protocol|public|repeat|required|rethrows|return|right|safe|self|set|some|static|struct|subscript|super|switch|throw|throws|try|typealias|unowned|unsafe|var|weak|where|while|willSet)\b/,
      "boolean": /\b(?:false|true)\b/,
      "nil": {
        pattern: /\bnil\b/,
        alias: "constant"
      },
      "short-argument": /\$\d+\b/,
      "omit": {
        pattern: /\b_\b/,
        alias: "keyword"
      },
      "number": /\b(?:[\d_]+(?:\.[\de_]+)?|0x[a-f0-9_]+(?:\.[a-f0-9p_]+)?|0b[01_]+|0o[0-7_]+)\b/i,
      // A class name must start with an upper-case letter and be either 1 letter long or contain a lower-case letter.
      "class-name": /\b[A-Z](?:[A-Z_\d]*[a-z]\w*)?\b/,
      "function": /\b[a-z_]\w*(?=\s*\()/i,
      "constant": /\b(?:[A-Z_]{2,}|k[A-Z][A-Za-z_]+)\b/,
      // Operators are generic in Swift. Developers can even create new operators (e.g. +++).
      // https://docs.swift.org/swift-book/ReferenceManual/zzSummaryOfTheGrammar.html#ID481
      // This regex only supports ASCII operators.
      "operator": /[-+*/%=!<>&|^~?]+|\.[.\-+*/%=!<>&|^~?]+/,
      "punctuation": /[{}[\]();,.:\\]/
    };
    Prism.languages.swift["string-literal"].forEach(function(rule) {
      rule.inside["interpolation"].inside = Prism.languages.swift;
    });
  }
});

// node_modules/prismjs/components/prism-yaml.js
var require_prism_yaml = __commonJS({
  "node_modules/prismjs/components/prism-yaml.js"() {
    (function(Prism2) {
      var anchorOrAlias = /[*&][^\s[\]{},]+/;
      var tag = /!(?:<[\w\-%#;/?:@&=+$,.!~*'()[\]]+>|(?:[a-zA-Z\d-]*!)?[\w\-%#;/?:@&=+$.~*'()]+)?/;
      var properties = "(?:" + tag.source + "(?:[ 	]+" + anchorOrAlias.source + ")?|" + anchorOrAlias.source + "(?:[ 	]+" + tag.source + ")?)";
      var plainKey = /(?:[^\s\x00-\x08\x0e-\x1f!"#%&'*,\-:>?@[\]`{|}\x7f-\x84\x86-\x9f\ud800-\udfff\ufffe\uffff]|[?:-]<PLAIN>)(?:[ \t]*(?:(?![#:])<PLAIN>|:<PLAIN>))*/.source.replace(/<PLAIN>/g, function() {
        return /[^\s\x00-\x08\x0e-\x1f,[\]{}\x7f-\x84\x86-\x9f\ud800-\udfff\ufffe\uffff]/.source;
      });
      var string2 = /"(?:[^"\\\r\n]|\\.)*"|'(?:[^'\\\r\n]|\\.)*'/.source;
      function createValuePattern(value, flags) {
        flags = (flags || "").replace(/m/g, "") + "m";
        var pattern = /([:\-,[{]\s*(?:\s<<prop>>[ \t]+)?)(?:<<value>>)(?=[ \t]*(?:$|,|\]|\}|(?:[\r\n]\s*)?#))/.source.replace(/<<prop>>/g, function() {
          return properties;
        }).replace(/<<value>>/g, function() {
          return value;
        });
        return RegExp(pattern, flags);
      }
      Prism2.languages.yaml = {
        "scalar": {
          pattern: RegExp(/([\-:]\s*(?:\s<<prop>>[ \t]+)?[|>])[ \t]*(?:((?:\r?\n|\r)[ \t]+)\S[^\r\n]*(?:\2[^\r\n]+)*)/.source.replace(/<<prop>>/g, function() {
            return properties;
          })),
          lookbehind: true,
          alias: "string"
        },
        "comment": /#.*/,
        "key": {
          pattern: RegExp(/((?:^|[:\-,[{\r\n?])[ \t]*(?:<<prop>>[ \t]+)?)<<key>>(?=\s*:\s)/.source.replace(/<<prop>>/g, function() {
            return properties;
          }).replace(/<<key>>/g, function() {
            return "(?:" + plainKey + "|" + string2 + ")";
          })),
          lookbehind: true,
          greedy: true,
          alias: "atrule"
        },
        "directive": {
          pattern: /(^[ \t]*)%.+/m,
          lookbehind: true,
          alias: "important"
        },
        "datetime": {
          pattern: createValuePattern(/\d{4}-\d\d?-\d\d?(?:[tT]|[ \t]+)\d\d?:\d{2}:\d{2}(?:\.\d*)?(?:[ \t]*(?:Z|[-+]\d\d?(?::\d{2})?))?|\d{4}-\d{2}-\d{2}|\d\d?:\d{2}(?::\d{2}(?:\.\d*)?)?/.source),
          lookbehind: true,
          alias: "number"
        },
        "boolean": {
          pattern: createValuePattern(/false|true/.source, "i"),
          lookbehind: true,
          alias: "important"
        },
        "null": {
          pattern: createValuePattern(/null|~/.source, "i"),
          lookbehind: true,
          alias: "important"
        },
        "string": {
          pattern: createValuePattern(string2),
          lookbehind: true,
          greedy: true
        },
        "number": {
          pattern: createValuePattern(/[+-]?(?:0x[\da-f]+|0o[0-7]+|(?:\d+(?:\.\d*)?|\.\d+)(?:e[+-]?\d+)?|\.inf|\.nan)/.source, "i"),
          lookbehind: true
        },
        "tag": tag,
        "important": anchorOrAlias,
        "punctuation": /---|[:[\]{}\-,|>?]|\.\.\./
      };
      Prism2.languages.yml = Prism2.languages.yaml;
    })(Prism);
  }
});

// node_modules/prismjs/components/prism-csv.js
var require_prism_csv = __commonJS({
  "node_modules/prismjs/components/prism-csv.js"() {
    Prism.languages.csv = {
      "value": /[^\r\n,"]+|"(?:[^"]|"")*"(?!")/,
      "punctuation": /,/
    };
  }
});

// node_modules/mark.js/dist/mark.js
var require_mark = __commonJS({
  "node_modules/mark.js/dist/mark.js"(exports, module) {
    (function(global2, factory) {
      typeof exports === "object" && typeof module !== "undefined" ? module.exports = factory() : typeof define === "function" && define.amd ? define(factory) : global2.Mark = factory();
    })(exports, (function() {
      "use strict";
      var _typeof = typeof Symbol === "function" && typeof Symbol.iterator === "symbol" ? function(obj) {
        return typeof obj;
      } : function(obj) {
        return obj && typeof Symbol === "function" && obj.constructor === Symbol && obj !== Symbol.prototype ? "symbol" : typeof obj;
      };
      var classCallCheck = function(instance, Constructor) {
        if (!(instance instanceof Constructor)) {
          throw new TypeError("Cannot call a class as a function");
        }
      };
      var createClass = /* @__PURE__ */ (function() {
        function defineProperties(target, props) {
          for (var i2 = 0; i2 < props.length; i2++) {
            var descriptor = props[i2];
            descriptor.enumerable = descriptor.enumerable || false;
            descriptor.configurable = true;
            if ("value" in descriptor) descriptor.writable = true;
            Object.defineProperty(target, descriptor.key, descriptor);
          }
        }
        return function(Constructor, protoProps, staticProps) {
          if (protoProps) defineProperties(Constructor.prototype, protoProps);
          if (staticProps) defineProperties(Constructor, staticProps);
          return Constructor;
        };
      })();
      var _extends4 = Object.assign || function(target) {
        for (var i2 = 1; i2 < arguments.length; i2++) {
          var source = arguments[i2];
          for (var key2 in source) {
            if (Object.prototype.hasOwnProperty.call(source, key2)) {
              target[key2] = source[key2];
            }
          }
        }
        return target;
      };
      var DOMIterator = (function() {
        function DOMIterator2(ctx) {
          var iframes = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : true;
          var exclude = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : [];
          var iframesTimeout = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : 5e3;
          classCallCheck(this, DOMIterator2);
          this.ctx = ctx;
          this.iframes = iframes;
          this.exclude = exclude;
          this.iframesTimeout = iframesTimeout;
        }
        createClass(DOMIterator2, [{
          key: "getContexts",
          value: function getContexts() {
            var ctx = void 0, filteredCtx = [];
            if (typeof this.ctx === "undefined" || !this.ctx) {
              ctx = [];
            } else if (NodeList.prototype.isPrototypeOf(this.ctx)) {
              ctx = Array.prototype.slice.call(this.ctx);
            } else if (Array.isArray(this.ctx)) {
              ctx = this.ctx;
            } else if (typeof this.ctx === "string") {
              ctx = Array.prototype.slice.call(document.querySelectorAll(this.ctx));
            } else {
              ctx = [this.ctx];
            }
            ctx.forEach(function(ctx2) {
              var isDescendant = filteredCtx.filter(function(contexts) {
                return contexts.contains(ctx2);
              }).length > 0;
              if (filteredCtx.indexOf(ctx2) === -1 && !isDescendant) {
                filteredCtx.push(ctx2);
              }
            });
            return filteredCtx;
          }
        }, {
          key: "getIframeContents",
          value: function getIframeContents(ifr, successFn) {
            var errorFn = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : function() {
            };
            var doc = void 0;
            try {
              var ifrWin = ifr.contentWindow;
              doc = ifrWin.document;
              if (!ifrWin || !doc) {
                throw new Error("iframe inaccessible");
              }
            } catch (e) {
              errorFn();
            }
            if (doc) {
              successFn(doc);
            }
          }
        }, {
          key: "isIframeBlank",
          value: function isIframeBlank(ifr) {
            var bl = "about:blank", src = ifr.getAttribute("src").trim(), href = ifr.contentWindow.location.href;
            return href === bl && src !== bl && src;
          }
        }, {
          key: "observeIframeLoad",
          value: function observeIframeLoad(ifr, successFn, errorFn) {
            var _this = this;
            var called = false, tout = null;
            var listener = function listener2() {
              if (called) {
                return;
              }
              called = true;
              clearTimeout(tout);
              try {
                if (!_this.isIframeBlank(ifr)) {
                  ifr.removeEventListener("load", listener2);
                  _this.getIframeContents(ifr, successFn, errorFn);
                }
              } catch (e) {
                errorFn();
              }
            };
            ifr.addEventListener("load", listener);
            tout = setTimeout(listener, this.iframesTimeout);
          }
        }, {
          key: "onIframeReady",
          value: function onIframeReady(ifr, successFn, errorFn) {
            try {
              if (ifr.contentWindow.document.readyState === "complete") {
                if (this.isIframeBlank(ifr)) {
                  this.observeIframeLoad(ifr, successFn, errorFn);
                } else {
                  this.getIframeContents(ifr, successFn, errorFn);
                }
              } else {
                this.observeIframeLoad(ifr, successFn, errorFn);
              }
            } catch (e) {
              errorFn();
            }
          }
        }, {
          key: "waitForIframes",
          value: function waitForIframes(ctx, done) {
            var _this2 = this;
            var eachCalled = 0;
            this.forEachIframe(ctx, function() {
              return true;
            }, function(ifr) {
              eachCalled++;
              _this2.waitForIframes(ifr.querySelector("html"), function() {
                if (!--eachCalled) {
                  done();
                }
              });
            }, function(handled) {
              if (!handled) {
                done();
              }
            });
          }
        }, {
          key: "forEachIframe",
          value: function forEachIframe(ctx, filter2, each) {
            var _this3 = this;
            var end = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : function() {
            };
            var ifr = ctx.querySelectorAll("iframe"), open = ifr.length, handled = 0;
            ifr = Array.prototype.slice.call(ifr);
            var checkEnd = function checkEnd2() {
              if (--open <= 0) {
                end(handled);
              }
            };
            if (!open) {
              checkEnd();
            }
            ifr.forEach(function(ifr2) {
              if (DOMIterator2.matches(ifr2, _this3.exclude)) {
                checkEnd();
              } else {
                _this3.onIframeReady(ifr2, function(con) {
                  if (filter2(ifr2)) {
                    handled++;
                    each(con);
                  }
                  checkEnd();
                }, checkEnd);
              }
            });
          }
        }, {
          key: "createIterator",
          value: function createIterator(ctx, whatToShow, filter2) {
            return document.createNodeIterator(ctx, whatToShow, filter2, false);
          }
        }, {
          key: "createInstanceOnIframe",
          value: function createInstanceOnIframe(contents) {
            return new DOMIterator2(contents.querySelector("html"), this.iframes);
          }
        }, {
          key: "compareNodeIframe",
          value: function compareNodeIframe(node2, prevNode, ifr) {
            var compCurr = node2.compareDocumentPosition(ifr), prev2 = Node.DOCUMENT_POSITION_PRECEDING;
            if (compCurr & prev2) {
              if (prevNode !== null) {
                var compPrev = prevNode.compareDocumentPosition(ifr), after = Node.DOCUMENT_POSITION_FOLLOWING;
                if (compPrev & after) {
                  return true;
                }
              } else {
                return true;
              }
            }
            return false;
          }
        }, {
          key: "getIteratorNode",
          value: function getIteratorNode(itr) {
            var prevNode = itr.previousNode();
            var node2 = void 0;
            if (prevNode === null) {
              node2 = itr.nextNode();
            } else {
              node2 = itr.nextNode() && itr.nextNode();
            }
            return {
              prevNode,
              node: node2
            };
          }
        }, {
          key: "checkIframeFilter",
          value: function checkIframeFilter(node2, prevNode, currIfr, ifr) {
            var key2 = false, handled = false;
            ifr.forEach(function(ifrDict, i2) {
              if (ifrDict.val === currIfr) {
                key2 = i2;
                handled = ifrDict.handled;
              }
            });
            if (this.compareNodeIframe(node2, prevNode, currIfr)) {
              if (key2 === false && !handled) {
                ifr.push({
                  val: currIfr,
                  handled: true
                });
              } else if (key2 !== false && !handled) {
                ifr[key2].handled = true;
              }
              return true;
            }
            if (key2 === false) {
              ifr.push({
                val: currIfr,
                handled: false
              });
            }
            return false;
          }
        }, {
          key: "handleOpenIframes",
          value: function handleOpenIframes(ifr, whatToShow, eCb, fCb) {
            var _this4 = this;
            ifr.forEach(function(ifrDict) {
              if (!ifrDict.handled) {
                _this4.getIframeContents(ifrDict.val, function(con) {
                  _this4.createInstanceOnIframe(con).forEachNode(whatToShow, eCb, fCb);
                });
              }
            });
          }
        }, {
          key: "iterateThroughNodes",
          value: function iterateThroughNodes(whatToShow, ctx, eachCb, filterCb, doneCb) {
            var _this5 = this;
            var itr = this.createIterator(ctx, whatToShow, filterCb);
            var ifr = [], elements = [], node2 = void 0, prevNode = void 0, retrieveNodes = function retrieveNodes2() {
              var _getIteratorNode = _this5.getIteratorNode(itr);
              prevNode = _getIteratorNode.prevNode;
              node2 = _getIteratorNode.node;
              return node2;
            };
            while (retrieveNodes()) {
              if (this.iframes) {
                this.forEachIframe(ctx, function(currIfr) {
                  return _this5.checkIframeFilter(node2, prevNode, currIfr, ifr);
                }, function(con) {
                  _this5.createInstanceOnIframe(con).forEachNode(whatToShow, function(ifrNode) {
                    return elements.push(ifrNode);
                  }, filterCb);
                });
              }
              elements.push(node2);
            }
            elements.forEach(function(node3) {
              eachCb(node3);
            });
            if (this.iframes) {
              this.handleOpenIframes(ifr, whatToShow, eachCb, filterCb);
            }
            doneCb();
          }
        }, {
          key: "forEachNode",
          value: function forEachNode(whatToShow, each, filter2) {
            var _this6 = this;
            var done = arguments.length > 3 && arguments[3] !== void 0 ? arguments[3] : function() {
            };
            var contexts = this.getContexts();
            var open = contexts.length;
            if (!open) {
              done();
            }
            contexts.forEach(function(ctx) {
              var ready = function ready2() {
                _this6.iterateThroughNodes(whatToShow, ctx, each, filter2, function() {
                  if (--open <= 0) {
                    done();
                  }
                });
              };
              if (_this6.iframes) {
                _this6.waitForIframes(ctx, ready);
              } else {
                ready();
              }
            });
          }
        }], [{
          key: "matches",
          value: function matches2(element, selector) {
            var selectors = typeof selector === "string" ? [selector] : selector, fn = element.matches || element.matchesSelector || element.msMatchesSelector || element.mozMatchesSelector || element.oMatchesSelector || element.webkitMatchesSelector;
            if (fn) {
              var match2 = false;
              selectors.every(function(sel) {
                if (fn.call(element, sel)) {
                  match2 = true;
                  return false;
                }
                return true;
              });
              return match2;
            } else {
              return false;
            }
          }
        }]);
        return DOMIterator2;
      })();
      var Mark$1 = (function() {
        function Mark2(ctx) {
          classCallCheck(this, Mark2);
          this.ctx = ctx;
          this.ie = false;
          var ua = window.navigator.userAgent;
          if (ua.indexOf("MSIE") > -1 || ua.indexOf("Trident") > -1) {
            this.ie = true;
          }
        }
        createClass(Mark2, [{
          key: "log",
          value: function log(msg) {
            var level = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : "debug";
            var log2 = this.opt.log;
            if (!this.opt.debug) {
              return;
            }
            if ((typeof log2 === "undefined" ? "undefined" : _typeof(log2)) === "object" && typeof log2[level] === "function") {
              log2[level]("mark.js: " + msg);
            }
          }
        }, {
          key: "escapeStr",
          value: function escapeStr(str) {
            return str.replace(/[\-\[\]\/\{\}\(\)\*\+\?\.\\\^\$\|]/g, "\\$&");
          }
        }, {
          key: "createRegExp",
          value: function createRegExp(str) {
            if (this.opt.wildcards !== "disabled") {
              str = this.setupWildcardsRegExp(str);
            }
            str = this.escapeStr(str);
            if (Object.keys(this.opt.synonyms).length) {
              str = this.createSynonymsRegExp(str);
            }
            if (this.opt.ignoreJoiners || this.opt.ignorePunctuation.length) {
              str = this.setupIgnoreJoinersRegExp(str);
            }
            if (this.opt.diacritics) {
              str = this.createDiacriticsRegExp(str);
            }
            str = this.createMergedBlanksRegExp(str);
            if (this.opt.ignoreJoiners || this.opt.ignorePunctuation.length) {
              str = this.createJoinersRegExp(str);
            }
            if (this.opt.wildcards !== "disabled") {
              str = this.createWildcardsRegExp(str);
            }
            str = this.createAccuracyRegExp(str);
            return str;
          }
        }, {
          key: "createSynonymsRegExp",
          value: function createSynonymsRegExp(str) {
            var syn = this.opt.synonyms, sens = this.opt.caseSensitive ? "" : "i", joinerPlaceholder = this.opt.ignoreJoiners || this.opt.ignorePunctuation.length ? "\0" : "";
            for (var index in syn) {
              if (syn.hasOwnProperty(index)) {
                var value = syn[index], k1 = this.opt.wildcards !== "disabled" ? this.setupWildcardsRegExp(index) : this.escapeStr(index), k2 = this.opt.wildcards !== "disabled" ? this.setupWildcardsRegExp(value) : this.escapeStr(value);
                if (k1 !== "" && k2 !== "") {
                  str = str.replace(new RegExp("(" + this.escapeStr(k1) + "|" + this.escapeStr(k2) + ")", "gm" + sens), joinerPlaceholder + ("(" + this.processSynomyms(k1) + "|") + (this.processSynomyms(k2) + ")") + joinerPlaceholder);
                }
              }
            }
            return str;
          }
        }, {
          key: "processSynomyms",
          value: function processSynomyms(str) {
            if (this.opt.ignoreJoiners || this.opt.ignorePunctuation.length) {
              str = this.setupIgnoreJoinersRegExp(str);
            }
            return str;
          }
        }, {
          key: "setupWildcardsRegExp",
          value: function setupWildcardsRegExp(str) {
            str = str.replace(/(?:\\)*\?/g, function(val) {
              return val.charAt(0) === "\\" ? "?" : "";
            });
            return str.replace(/(?:\\)*\*/g, function(val) {
              return val.charAt(0) === "\\" ? "*" : "";
            });
          }
        }, {
          key: "createWildcardsRegExp",
          value: function createWildcardsRegExp(str) {
            var spaces = this.opt.wildcards === "withSpaces";
            return str.replace(/\u0001/g, spaces ? "[\\S\\s]?" : "\\S?").replace(/\u0002/g, spaces ? "[\\S\\s]*?" : "\\S*");
          }
        }, {
          key: "setupIgnoreJoinersRegExp",
          value: function setupIgnoreJoinersRegExp(str) {
            return str.replace(/[^(|)\\]/g, function(val, indx, original) {
              var nextChar = original.charAt(indx + 1);
              if (/[(|)\\]/.test(nextChar) || nextChar === "") {
                return val;
              } else {
                return val + "\0";
              }
            });
          }
        }, {
          key: "createJoinersRegExp",
          value: function createJoinersRegExp(str) {
            var joiner = [];
            var ignorePunctuation = this.opt.ignorePunctuation;
            if (Array.isArray(ignorePunctuation) && ignorePunctuation.length) {
              joiner.push(this.escapeStr(ignorePunctuation.join("")));
            }
            if (this.opt.ignoreJoiners) {
              joiner.push("\\u00ad\\u200b\\u200c\\u200d");
            }
            return joiner.length ? str.split(/\u0000+/).join("[" + joiner.join("") + "]*") : str;
          }
        }, {
          key: "createDiacriticsRegExp",
          value: function createDiacriticsRegExp(str) {
            var sens = this.opt.caseSensitive ? "" : "i", dct = this.opt.caseSensitive ? ["aàáảãạăằắẳẵặâầấẩẫậäåāą", "AÀÁẢÃẠĂẰẮẲẴẶÂẦẤẨẪẬÄÅĀĄ", "cçćč", "CÇĆČ", "dđď", "DĐĎ", "eèéẻẽẹêềếểễệëěēę", "EÈÉẺẼẸÊỀẾỂỄỆËĚĒĘ", "iìíỉĩịîïī", "IÌÍỈĨỊÎÏĪ", "lł", "LŁ", "nñňń", "NÑŇŃ", "oòóỏõọôồốổỗộơởỡớờợöøō", "OÒÓỎÕỌÔỒỐỔỖỘƠỞỠỚỜỢÖØŌ", "rř", "RŘ", "sšśșş", "SŠŚȘŞ", "tťțţ", "TŤȚŢ", "uùúủũụưừứửữựûüůū", "UÙÚỦŨỤƯỪỨỬỮỰÛÜŮŪ", "yýỳỷỹỵÿ", "YÝỲỶỸỴŸ", "zžżź", "ZŽŻŹ"] : ["aàáảãạăằắẳẵặâầấẩẫậäåāąAÀÁẢÃẠĂẰẮẲẴẶÂẦẤẨẪẬÄÅĀĄ", "cçćčCÇĆČ", "dđďDĐĎ", "eèéẻẽẹêềếểễệëěēęEÈÉẺẼẸÊỀẾỂỄỆËĚĒĘ", "iìíỉĩịîïīIÌÍỈĨỊÎÏĪ", "lłLŁ", "nñňńNÑŇŃ", "oòóỏõọôồốổỗộơởỡớờợöøōOÒÓỎÕỌÔỒỐỔỖỘƠỞỠỚỜỢÖØŌ", "rřRŘ", "sšśșşSŠŚȘŞ", "tťțţTŤȚŢ", "uùúủũụưừứửữựûüůūUÙÚỦŨỤƯỪỨỬỮỰÛÜŮŪ", "yýỳỷỹỵÿYÝỲỶỸỴŸ", "zžżźZŽŻŹ"];
            var handled = [];
            str.split("").forEach(function(ch) {
              dct.every(function(dct2) {
                if (dct2.indexOf(ch) !== -1) {
                  if (handled.indexOf(dct2) > -1) {
                    return false;
                  }
                  str = str.replace(new RegExp("[" + dct2 + "]", "gm" + sens), "[" + dct2 + "]");
                  handled.push(dct2);
                }
                return true;
              });
            });
            return str;
          }
        }, {
          key: "createMergedBlanksRegExp",
          value: function createMergedBlanksRegExp(str) {
            return str.replace(/[\s]+/gmi, "[\\s]+");
          }
        }, {
          key: "createAccuracyRegExp",
          value: function createAccuracyRegExp(str) {
            var _this = this;
            var chars = "!\"#$%&'()*+,-./:;<=>?@[\\]^_`{|}~¡¿";
            var acc = this.opt.accuracy, val = typeof acc === "string" ? acc : acc.value, ls = typeof acc === "string" ? [] : acc.limiters, lsJoin = "";
            ls.forEach(function(limiter) {
              lsJoin += "|" + _this.escapeStr(limiter);
            });
            switch (val) {
              case "partially":
              default:
                return "()(" + str + ")";
              case "complementary":
                lsJoin = "\\s" + (lsJoin ? lsJoin : this.escapeStr(chars));
                return "()([^" + lsJoin + "]*" + str + "[^" + lsJoin + "]*)";
              case "exactly":
                return "(^|\\s" + lsJoin + ")(" + str + ")(?=$|\\s" + lsJoin + ")";
            }
          }
        }, {
          key: "getSeparatedKeywords",
          value: function getSeparatedKeywords(sv) {
            var _this2 = this;
            var stack = [];
            sv.forEach(function(kw) {
              if (!_this2.opt.separateWordSearch) {
                if (kw.trim() && stack.indexOf(kw) === -1) {
                  stack.push(kw);
                }
              } else {
                kw.split(" ").forEach(function(kwSplitted) {
                  if (kwSplitted.trim() && stack.indexOf(kwSplitted) === -1) {
                    stack.push(kwSplitted);
                  }
                });
              }
            });
            return {
              "keywords": stack.sort(function(a2, b2) {
                return b2.length - a2.length;
              }),
              "length": stack.length
            };
          }
        }, {
          key: "isNumeric",
          value: function isNumeric(value) {
            return Number(parseFloat(value)) == value;
          }
        }, {
          key: "checkRanges",
          value: function checkRanges(array2) {
            var _this3 = this;
            if (!Array.isArray(array2) || Object.prototype.toString.call(array2[0]) !== "[object Object]") {
              this.log("markRanges() will only accept an array of objects");
              this.opt.noMatch(array2);
              return [];
            }
            var stack = [];
            var last2 = 0;
            array2.sort(function(a2, b2) {
              return a2.start - b2.start;
            }).forEach(function(item) {
              var _callNoMatchOnInvalid = _this3.callNoMatchOnInvalidRanges(item, last2), start = _callNoMatchOnInvalid.start, end = _callNoMatchOnInvalid.end, valid = _callNoMatchOnInvalid.valid;
              if (valid) {
                item.start = start;
                item.length = end - start;
                stack.push(item);
                last2 = end;
              }
            });
            return stack;
          }
        }, {
          key: "callNoMatchOnInvalidRanges",
          value: function callNoMatchOnInvalidRanges(range, last2) {
            var start = void 0, end = void 0, valid = false;
            if (range && typeof range.start !== "undefined") {
              start = parseInt(range.start, 10);
              end = start + parseInt(range.length, 10);
              if (this.isNumeric(range.start) && this.isNumeric(range.length) && end - last2 > 0 && end - start > 0) {
                valid = true;
              } else {
                this.log("Ignoring invalid or overlapping range: " + ("" + JSON.stringify(range)));
                this.opt.noMatch(range);
              }
            } else {
              this.log("Ignoring invalid range: " + JSON.stringify(range));
              this.opt.noMatch(range);
            }
            return {
              start,
              end,
              valid
            };
          }
        }, {
          key: "checkWhitespaceRanges",
          value: function checkWhitespaceRanges(range, originalLength, string2) {
            var end = void 0, valid = true, max2 = string2.length, offset = originalLength - max2, start = parseInt(range.start, 10) - offset;
            start = start > max2 ? max2 : start;
            end = start + parseInt(range.length, 10);
            if (end > max2) {
              end = max2;
              this.log("End range automatically set to the max value of " + max2);
            }
            if (start < 0 || end - start < 0 || start > max2 || end > max2) {
              valid = false;
              this.log("Invalid range: " + JSON.stringify(range));
              this.opt.noMatch(range);
            } else if (string2.substring(start, end).replace(/\s+/g, "") === "") {
              valid = false;
              this.log("Skipping whitespace only range: " + JSON.stringify(range));
              this.opt.noMatch(range);
            }
            return {
              start,
              end,
              valid
            };
          }
        }, {
          key: "getTextNodes",
          value: function getTextNodes(cb) {
            var _this4 = this;
            var val = "", nodes = [];
            this.iterator.forEachNode(NodeFilter.SHOW_TEXT, function(node2) {
              nodes.push({
                start: val.length,
                end: (val += node2.textContent).length,
                node: node2
              });
            }, function(node2) {
              if (_this4.matchesExclude(node2.parentNode)) {
                return NodeFilter.FILTER_REJECT;
              } else {
                return NodeFilter.FILTER_ACCEPT;
              }
            }, function() {
              cb({
                value: val,
                nodes
              });
            });
          }
        }, {
          key: "matchesExclude",
          value: function matchesExclude(el) {
            return DOMIterator.matches(el, this.opt.exclude.concat(["script", "style", "title", "head", "html"]));
          }
        }, {
          key: "wrapRangeInTextNode",
          value: function wrapRangeInTextNode(node2, start, end) {
            var hEl = !this.opt.element ? "mark" : this.opt.element, startNode = node2.splitText(start), ret = startNode.splitText(end - start);
            var repl = document.createElement(hEl);
            repl.setAttribute("data-markjs", "true");
            if (this.opt.className) {
              repl.setAttribute("class", this.opt.className);
            }
            repl.textContent = startNode.textContent;
            startNode.parentNode.replaceChild(repl, startNode);
            return ret;
          }
        }, {
          key: "wrapRangeInMappedTextNode",
          value: function wrapRangeInMappedTextNode(dict, start, end, filterCb, eachCb) {
            var _this5 = this;
            dict.nodes.every(function(n, i2) {
              var sibl = dict.nodes[i2 + 1];
              if (typeof sibl === "undefined" || sibl.start > start) {
                if (!filterCb(n.node)) {
                  return false;
                }
                var s2 = start - n.start, e = (end > n.end ? n.end : end) - n.start, startStr = dict.value.substr(0, n.start), endStr = dict.value.substr(e + n.start);
                n.node = _this5.wrapRangeInTextNode(n.node, s2, e);
                dict.value = startStr + endStr;
                dict.nodes.forEach(function(k2, j2) {
                  if (j2 >= i2) {
                    if (dict.nodes[j2].start > 0 && j2 !== i2) {
                      dict.nodes[j2].start -= e;
                    }
                    dict.nodes[j2].end -= e;
                  }
                });
                end -= e;
                eachCb(n.node.previousSibling, n.start);
                if (end > n.end) {
                  start = n.end;
                } else {
                  return false;
                }
              }
              return true;
            });
          }
        }, {
          key: "wrapMatches",
          value: function wrapMatches(regex, ignoreGroups, filterCb, eachCb, endCb) {
            var _this6 = this;
            var matchIdx = ignoreGroups === 0 ? 0 : ignoreGroups + 1;
            this.getTextNodes(function(dict) {
              dict.nodes.forEach(function(node2) {
                node2 = node2.node;
                var match2 = void 0;
                while ((match2 = regex.exec(node2.textContent)) !== null && match2[matchIdx] !== "") {
                  if (!filterCb(match2[matchIdx], node2)) {
                    continue;
                  }
                  var pos = match2.index;
                  if (matchIdx !== 0) {
                    for (var i2 = 1; i2 < matchIdx; i2++) {
                      pos += match2[i2].length;
                    }
                  }
                  node2 = _this6.wrapRangeInTextNode(node2, pos, pos + match2[matchIdx].length);
                  eachCb(node2.previousSibling);
                  regex.lastIndex = 0;
                }
              });
              endCb();
            });
          }
        }, {
          key: "wrapMatchesAcrossElements",
          value: function wrapMatchesAcrossElements(regex, ignoreGroups, filterCb, eachCb, endCb) {
            var _this7 = this;
            var matchIdx = ignoreGroups === 0 ? 0 : ignoreGroups + 1;
            this.getTextNodes(function(dict) {
              var match2 = void 0;
              while ((match2 = regex.exec(dict.value)) !== null && match2[matchIdx] !== "") {
                var start = match2.index;
                if (matchIdx !== 0) {
                  for (var i2 = 1; i2 < matchIdx; i2++) {
                    start += match2[i2].length;
                  }
                }
                var end = start + match2[matchIdx].length;
                _this7.wrapRangeInMappedTextNode(dict, start, end, function(node2) {
                  return filterCb(match2[matchIdx], node2);
                }, function(node2, lastIndex) {
                  regex.lastIndex = lastIndex;
                  eachCb(node2);
                });
              }
              endCb();
            });
          }
        }, {
          key: "wrapRangeFromIndex",
          value: function wrapRangeFromIndex(ranges, filterCb, eachCb, endCb) {
            var _this8 = this;
            this.getTextNodes(function(dict) {
              var originalLength = dict.value.length;
              ranges.forEach(function(range, counter) {
                var _checkWhitespaceRange = _this8.checkWhitespaceRanges(range, originalLength, dict.value), start = _checkWhitespaceRange.start, end = _checkWhitespaceRange.end, valid = _checkWhitespaceRange.valid;
                if (valid) {
                  _this8.wrapRangeInMappedTextNode(dict, start, end, function(node2) {
                    return filterCb(node2, range, dict.value.substring(start, end), counter);
                  }, function(node2) {
                    eachCb(node2, range);
                  });
                }
              });
              endCb();
            });
          }
        }, {
          key: "unwrapMatches",
          value: function unwrapMatches(node2) {
            var parent = node2.parentNode;
            var docFrag = document.createDocumentFragment();
            while (node2.firstChild) {
              docFrag.appendChild(node2.removeChild(node2.firstChild));
            }
            parent.replaceChild(docFrag, node2);
            if (!this.ie) {
              parent.normalize();
            } else {
              this.normalizeTextNode(parent);
            }
          }
        }, {
          key: "normalizeTextNode",
          value: function normalizeTextNode(node2) {
            if (!node2) {
              return;
            }
            if (node2.nodeType === 3) {
              while (node2.nextSibling && node2.nextSibling.nodeType === 3) {
                node2.nodeValue += node2.nextSibling.nodeValue;
                node2.parentNode.removeChild(node2.nextSibling);
              }
            } else {
              this.normalizeTextNode(node2.firstChild);
            }
            this.normalizeTextNode(node2.nextSibling);
          }
        }, {
          key: "markRegExp",
          value: function markRegExp(regexp, opt) {
            var _this9 = this;
            this.opt = opt;
            this.log('Searching with expression "' + regexp + '"');
            var totalMatches = 0, fn = "wrapMatches";
            var eachCb = function eachCb2(element) {
              totalMatches++;
              _this9.opt.each(element);
            };
            if (this.opt.acrossElements) {
              fn = "wrapMatchesAcrossElements";
            }
            this[fn](regexp, this.opt.ignoreGroups, function(match2, node2) {
              return _this9.opt.filter(node2, match2, totalMatches);
            }, eachCb, function() {
              if (totalMatches === 0) {
                _this9.opt.noMatch(regexp);
              }
              _this9.opt.done(totalMatches);
            });
          }
        }, {
          key: "mark",
          value: function mark(sv, opt) {
            var _this10 = this;
            this.opt = opt;
            var totalMatches = 0, fn = "wrapMatches";
            var _getSeparatedKeywords = this.getSeparatedKeywords(typeof sv === "string" ? [sv] : sv), kwArr = _getSeparatedKeywords.keywords, kwArrLen = _getSeparatedKeywords.length, sens = this.opt.caseSensitive ? "" : "i", handler = function handler2(kw) {
              var regex = new RegExp(_this10.createRegExp(kw), "gm" + sens), matches2 = 0;
              _this10.log('Searching with expression "' + regex + '"');
              _this10[fn](regex, 1, function(term, node2) {
                return _this10.opt.filter(node2, kw, totalMatches, matches2);
              }, function(element) {
                matches2++;
                totalMatches++;
                _this10.opt.each(element);
              }, function() {
                if (matches2 === 0) {
                  _this10.opt.noMatch(kw);
                }
                if (kwArr[kwArrLen - 1] === kw) {
                  _this10.opt.done(totalMatches);
                } else {
                  handler2(kwArr[kwArr.indexOf(kw) + 1]);
                }
              });
            };
            if (this.opt.acrossElements) {
              fn = "wrapMatchesAcrossElements";
            }
            if (kwArrLen === 0) {
              this.opt.done(totalMatches);
            } else {
              handler(kwArr[0]);
            }
          }
        }, {
          key: "markRanges",
          value: function markRanges(rawRanges, opt) {
            var _this11 = this;
            this.opt = opt;
            var totalMatches = 0, ranges = this.checkRanges(rawRanges);
            if (ranges && ranges.length) {
              this.log("Starting to mark with the following ranges: " + JSON.stringify(ranges));
              this.wrapRangeFromIndex(ranges, function(node2, range, match2, counter) {
                return _this11.opt.filter(node2, range, match2, counter);
              }, function(element, range) {
                totalMatches++;
                _this11.opt.each(element, range);
              }, function() {
                _this11.opt.done(totalMatches);
              });
            } else {
              this.opt.done(totalMatches);
            }
          }
        }, {
          key: "unmark",
          value: function unmark(opt) {
            var _this12 = this;
            this.opt = opt;
            var sel = this.opt.element ? this.opt.element : "*";
            sel += "[data-markjs]";
            if (this.opt.className) {
              sel += "." + this.opt.className;
            }
            this.log('Removal selector "' + sel + '"');
            this.iterator.forEachNode(NodeFilter.SHOW_ELEMENT, function(node2) {
              _this12.unwrapMatches(node2);
            }, function(node2) {
              var matchesSel = DOMIterator.matches(node2, sel), matchesExclude = _this12.matchesExclude(node2);
              if (!matchesSel || matchesExclude) {
                return NodeFilter.FILTER_REJECT;
              } else {
                return NodeFilter.FILTER_ACCEPT;
              }
            }, this.opt.done);
          }
        }, {
          key: "opt",
          set: function set$$1(val) {
            this._opt = _extends4({}, {
              "element": "",
              "className": "",
              "exclude": [],
              "iframes": false,
              "iframesTimeout": 5e3,
              "separateWordSearch": true,
              "diacritics": true,
              "synonyms": {},
              "accuracy": "partially",
              "acrossElements": false,
              "caseSensitive": false,
              "ignoreJoiners": false,
              "ignoreGroups": 0,
              "ignorePunctuation": [],
              "wildcards": "disabled",
              "each": function each() {
              },
              "noMatch": function noMatch() {
              },
              "filter": function filter2() {
                return true;
              },
              "done": function done() {
              },
              "debug": false,
              "log": window.console
            }, val);
          },
          get: function get$$1() {
            return this._opt;
          }
        }, {
          key: "iterator",
          get: function get$$1() {
            return new DOMIterator(this.ctx, this.opt.iframes, this.opt.exclude, this.opt.iframesTimeout);
          }
        }]);
        return Mark2;
      })();
      function Mark(ctx) {
        var _this = this;
        var instance = new Mark$1(ctx);
        this.mark = function(sv, opt) {
          instance.mark(sv, opt);
          return _this;
        };
        this.markRegExp = function(sv, opt) {
          instance.markRegExp(sv, opt);
          return _this;
        };
        this.markRanges = function(sv, opt) {
          instance.markRanges(sv, opt);
          return _this;
        };
        this.unmark = function(opt) {
          instance.unmark(opt);
          return _this;
        };
        return this;
      }
      return Mark;
    }));
  }
});

// node_modules/openapi-sampler/src/utils.js
function pad(number) {
  if (number < 10) {
    return "0" + number;
  }
  return number;
}
function toRFCDateTime(date, omitTime, omitDate, milliseconds) {
  var res = omitDate ? "" : date.getUTCFullYear() + "-" + pad(date.getUTCMonth() + 1) + "-" + pad(date.getUTCDate());
  if (!omitTime) {
    res += "T" + pad(date.getUTCHours()) + ":" + pad(date.getUTCMinutes()) + ":" + pad(date.getUTCSeconds()) + (milliseconds ? "." + (date.getUTCMilliseconds() / 1e3).toFixed(3).slice(2, 5) : "") + "Z";
  }
  return res;
}
function ensureMinLength(sample2, min2) {
  if (min2 > sample2.length) {
    return sample2.repeat(Math.trunc(min2 / sample2.length) + 1).substring(0, min2);
  }
  return sample2;
}
function mergeDeep(...objects) {
  const isObject2 = (obj) => obj && typeof obj === "object";
  return objects.reduce((prev2, obj) => {
    Object.keys(obj || {}).forEach((key2) => {
      const pVal = prev2[key2];
      const oVal = obj[key2];
      if (isObject2(pVal) && isObject2(oVal)) {
        prev2[key2] = mergeDeep(pVal, oVal);
      } else {
        prev2[key2] = oVal;
      }
    });
    return prev2;
  }, Array.isArray(objects[objects.length - 1]) ? [] : {});
}
function uuid(str) {
  var hash2 = hashCode(str);
  var random = jsf32(hash2, hash2, hash2, hash2);
  var uuid2 = "xxxxxxxx-xxxx-4xxx-yxxx-xxxxxxxxxxxx".replace(/[xy]/g, (c2) => {
    var r2 = random() * 16 % 16 | 0;
    return (c2 == "x" ? r2 : r2 & 3 | 8).toString(16);
  });
  return uuid2;
}
function getResultForCircular(type) {
  return {
    value: type === "object" ? {} : type === "array" ? [] : void 0
  };
}
function popSchemaStack(seenSchemasStack2, context) {
  if (context) seenSchemasStack2.pop();
}
function getXMLAttributes(schema4) {
  return {
    name: schema4?.xml?.name || "",
    prefix: schema4?.xml?.prefix || "",
    namespace: schema4?.xml?.namespace || null,
    attribute: schema4?.xml?.attribute ?? false,
    wrapped: schema4?.xml?.wrapped ?? false
  };
}
function applyXMLAttributes(result, schema4 = {}, context = {}) {
  const { value: oldValue } = result;
  const { propertyName: oldPropertyName } = context;
  const { name, prefix: prefix2, namespace, attribute, wrapped } = getXMLAttributes(schema4);
  let propertyName = name || oldPropertyName ? `${prefix2 ? prefix2 + ":" : ""}${name || oldPropertyName}` : null;
  let value = typeof oldValue === "object" ? Array.isArray(oldValue) ? [...oldValue] : { ...oldValue } : oldValue;
  if (attribute && propertyName) {
    propertyName = `$${propertyName}`;
  }
  if (namespace) {
    if (typeof value === "object") {
      value[`$xmlns${prefix2 ? ":" + prefix2 : ""}`] = namespace;
    } else {
      value = { [`$xmlns${prefix2 ? ":" + prefix2 : ""}`]: namespace, ["#text"]: value };
    }
  }
  if (schema4.type === "array") {
    if (wrapped && Array.isArray(value)) {
      value = { [propertyName]: [...value] };
    }
    if (!wrapped) {
      propertyName = null;
    }
    if (schema4.example !== void 0 && !wrapped) {
      propertyName = schema4.items?.xml?.name || propertyName;
    }
  }
  if (schema4.oneOf || schema4.anyOf || schema4.allOf || schema4.$ref) {
    propertyName = null;
  }
  return {
    propertyName,
    value
  };
}
function hashCode(str) {
  var hash2 = 0;
  if (str.length == 0) return hash2;
  for (var i2 = 0; i2 < str.length; i2++) {
    var char2 = str.charCodeAt(i2);
    hash2 = (hash2 << 5) - hash2 + char2;
    hash2 = hash2 & hash2;
  }
  return hash2;
}
function jsf32(a2, b2, c2, d) {
  return function() {
    a2 |= 0;
    b2 |= 0;
    c2 |= 0;
    d |= 0;
    var t = a2 - (b2 << 27 | b2 >>> 5) | 0;
    a2 = b2 ^ (c2 << 17 | c2 >>> 15);
    b2 = c2 + d | 0;
    c2 = d + t | 0;
    d = a2 + t | 0;
    return (d >>> 0) / 4294967296;
  };
}
var init_utils = __esm({
  "node_modules/openapi-sampler/src/utils.js"() {
    "use strict";
  }
});

// node_modules/openapi-sampler/src/allOf.js
function allOfSample(into, children, options, spec, context) {
  let res = traverse(into, options, spec);
  const subSamples = [];
  for (let subSchema of children) {
    const { type, readOnly, writeOnly, value } = traverse({ type: res.type, ...subSchema }, options, spec, context);
    if (res.type && type && type !== res.type) {
      console.warn("allOf: schemas with different types can't be merged");
      res.type = type;
    }
    res.type = res.type || type;
    res.readOnly = res.readOnly || readOnly;
    res.writeOnly = res.writeOnly || writeOnly;
    if (value != null) subSamples.push(value);
  }
  if (res.type === "object") {
    res.value = mergeDeep(res.value || {}, ...subSamples.filter((sample2) => typeof sample2 === "object"));
    return res;
  } else {
    if (res.type === "array") {
      if (!options.quiet) console.warn('OpenAPI Sampler: found allOf with "array" type. Result may be incorrect');
    }
    const lastSample = subSamples[subSamples.length - 1];
    res.value = lastSample != null ? lastSample : res.value;
    return res;
  }
}
var init_allOf = __esm({
  "node_modules/openapi-sampler/src/allOf.js"() {
    init_traverse();
    init_utils();
  }
});

// node_modules/openapi-sampler/src/infer.js
function inferType(schema4) {
  if (schema4.type !== void 0) {
    return Array.isArray(schema4.type) ? schema4.type.length === 0 ? null : schema4.type[0] : schema4.type;
  }
  const keywords = Object.keys(schemaKeywordTypes);
  for (var i2 = 0; i2 < keywords.length; i2++) {
    let keyword = keywords[i2];
    let type = schemaKeywordTypes[keyword];
    if (schema4[keyword] !== void 0) {
      return type;
    }
  }
  return null;
}
var schemaKeywordTypes;
var init_infer = __esm({
  "node_modules/openapi-sampler/src/infer.js"() {
    schemaKeywordTypes = {
      multipleOf: "number",
      maximum: "number",
      exclusiveMaximum: "number",
      minimum: "number",
      exclusiveMinimum: "number",
      maxLength: "string",
      minLength: "string",
      pattern: "string",
      items: "array",
      maxItems: "array",
      minItems: "array",
      uniqueItems: "array",
      additionalItems: "array",
      maxProperties: "object",
      minProperties: "object",
      required: "object",
      additionalProperties: "object",
      properties: "object",
      patternProperties: "object",
      dependencies: "object"
    };
  }
});

// node_modules/openapi-sampler/src/traverse.js
function clearCache() {
  $refCache = {};
  seenSchemasStack = [];
}
function inferExample(schema4) {
  let example;
  if (schema4.const !== void 0) {
    example = schema4.const;
  } else if (schema4.examples !== void 0 && schema4.examples.length) {
    example = schema4.examples[0];
  } else if (schema4.enum !== void 0 && schema4.enum.length) {
    example = schema4.enum[0];
  } else if (schema4.default !== void 0) {
    example = schema4.default;
  }
  return example;
}
function tryInferExample(schema4) {
  const example = inferExample(schema4);
  if (example !== void 0) {
    return {
      value: example,
      readOnly: schema4.readOnly,
      writeOnly: schema4.writeOnly,
      type: null
    };
  }
  return;
}
function traverse(schema4, options, spec, context) {
  if (context) {
    if (seenSchemasStack.includes(schema4)) return getResultForCircular(inferType(schema4));
    seenSchemasStack.push(schema4);
  }
  if (context && context.depth > options.maxSampleDepth) {
    popSchemaStack(seenSchemasStack, context);
    return getResultForCircular(inferType(schema4));
  }
  if (schema4.$ref) {
    if (!spec) {
      throw new Error("Your schema contains $ref. You must provide full specification in the third parameter.");
    }
    let ref = decodeURIComponent(schema4.$ref);
    if (ref.startsWith("#")) {
      ref = ref.substring(1);
    }
    const referenced = import_json_pointer.default.get(spec, ref);
    let result;
    if ($refCache[ref] !== true) {
      $refCache[ref] = true;
      const traverseResult = traverse(referenced, options, spec, context);
      if (options.format === "xml") {
        const { propertyName, value } = applyXMLAttributes(traverseResult, referenced, context);
        result = { ...traverseResult, value: { [propertyName || "root"]: value } };
      } else {
        result = traverseResult;
      }
      $refCache[ref] = false;
    } else {
      const referencedType = inferType(referenced);
      result = getResultForCircular(referencedType);
    }
    popSchemaStack(seenSchemasStack, context);
    return result;
  }
  if (schema4.example !== void 0) {
    popSchemaStack(seenSchemasStack, context);
    return {
      value: schema4.example,
      readOnly: schema4.readOnly,
      writeOnly: schema4.writeOnly,
      type: schema4.type
    };
  }
  if (schema4.allOf !== void 0) {
    popSchemaStack(seenSchemasStack, context);
    return tryInferExample(schema4) || allOfSample(
      { ...schema4, allOf: void 0 },
      schema4.allOf,
      options,
      spec,
      context
    );
  }
  if (schema4.oneOf && schema4.oneOf.length) {
    if (schema4.anyOf) {
      if (!options.quiet) console.warn("oneOf and anyOf are not supported on the same level. Skipping anyOf");
    }
    popSchemaStack(seenSchemasStack, context);
    const firstOneOf = Object.assign({
      readOnly: schema4.readOnly,
      writeOnly: schema4.writeOnly
    }, schema4.oneOf[0]);
    return traverseOneOrAnyOf(schema4, firstOneOf);
  }
  if (schema4.anyOf && schema4.anyOf.length) {
    popSchemaStack(seenSchemasStack, context);
    const firstAnyOf = Object.assign({
      readOnly: schema4.readOnly,
      writeOnly: schema4.writeOnly
    }, schema4.anyOf[0]);
    return traverseOneOrAnyOf(schema4, firstAnyOf);
  }
  if (schema4.if && schema4.then) {
    popSchemaStack(seenSchemasStack, context);
    const { if: ifSchema, then, ...rest } = schema4;
    return traverse(mergeDeep(rest, ifSchema, then), options, spec, context);
  }
  let example = inferExample(schema4);
  let type = null;
  if (example === void 0) {
    example = null;
    type = schema4.type;
    if (Array.isArray(type) && schema4.type.length > 0) {
      type = schema4.type[0];
    }
    if (!type) {
      type = inferType(schema4);
    }
    let sampler = _samplers[type];
    if (sampler) {
      example = sampler(schema4, options, spec, context);
    }
  }
  popSchemaStack(seenSchemasStack, context);
  return {
    value: example,
    readOnly: schema4.readOnly,
    writeOnly: schema4.writeOnly,
    type
  };
  function traverseOneOrAnyOf(schema5, selectedSubSchema) {
    const inferred = tryInferExample(schema5);
    if (inferred !== void 0) {
      return inferred;
    }
    const localExample = traverse({ ...schema5, oneOf: void 0, anyOf: void 0 }, options, spec, context);
    const subSchemaExample = traverse(selectedSubSchema, options, spec, context);
    if (typeof localExample.value === "object" && typeof subSchemaExample.value === "object") {
      const mergedExample = mergeDeep(localExample.value, subSchemaExample.value);
      return { ...subSchemaExample, value: mergedExample };
    }
    return subSchemaExample;
  }
}
var import_json_pointer, $refCache, seenSchemasStack;
var init_traverse = __esm({
  "node_modules/openapi-sampler/src/traverse.js"() {
    init_openapi_sampler();
    init_allOf();
    init_infer();
    init_utils();
    import_json_pointer = __toESM(require_json_pointer());
    init_utils();
    $refCache = {};
    seenSchemasStack = [];
  }
});

// node_modules/openapi-sampler/src/samplers/array.js
function sampleArray(schema4, options = {}, spec, context) {
  const depth = context && context.depth || 1;
  let arrayLength = Math.min(schema4.maxItems != void 0 ? schema4.maxItems : Infinity, schema4.minItems || 1);
  const items = schema4.prefixItems || schema4.items || schema4.contains;
  if (Array.isArray(items)) {
    arrayLength = Math.max(arrayLength, items.length);
  }
  let itemSchemaGetter = (itemNumber) => {
    if (Array.isArray(items)) {
      return items[itemNumber] || {};
    }
    return items || {};
  };
  let res = [];
  if (!items) return res;
  for (let i2 = 0; i2 < arrayLength; i2++) {
    let itemSchema = itemSchemaGetter(i2);
    let { value: sample2 } = traverse(itemSchema, options, spec, { depth: depth + 1 });
    if (options?.format === "xml") {
      const { value, propertyName } = applyXMLAttributes({ value: sample2 }, itemSchema, context);
      if (propertyName) {
        if (!res?.[propertyName]) {
          res = { ...res, [propertyName]: [] };
        }
        res[propertyName].push(value);
      } else {
        res = { ...res, ...value };
      }
    } else {
      res.push(sample2);
    }
  }
  if (options?.format === "xml" && depth === 1) {
    const { value, propertyName } = applyXMLAttributes({ value: null }, schema4, context);
    if (propertyName) {
      if (value) {
        res = Array.isArray(res) ? { [propertyName]: { ...value, ...res.map((item) => ({ ["#text"]: { ...item } })) } } : { [propertyName]: { ...res, ...value } };
      } else {
        res = { [propertyName]: res };
      }
    }
  }
  return res;
}
var init_array = __esm({
  "node_modules/openapi-sampler/src/samplers/array.js"() {
    init_traverse();
    init_utils();
  }
});

// node_modules/openapi-sampler/src/samplers/boolean.js
function sampleBoolean(schema4) {
  return true;
}
var init_boolean = __esm({
  "node_modules/openapi-sampler/src/samplers/boolean.js"() {
  }
});

// node_modules/openapi-sampler/src/samplers/number.js
function sampleNumber(schema4) {
  let res = 0;
  if (schema4.type === "number" && (schema4.format === "float" || schema4.format === "double")) {
    res = 0.1;
  }
  if (typeof schema4.exclusiveMinimum === "boolean" || typeof schema4.exclusiveMaximum === "boolean") {
    if (schema4.maximum && schema4.minimum) {
      res = schema4.exclusiveMinimum ? Math.floor(schema4.minimum) + 1 : schema4.minimum;
      if (schema4.exclusiveMaximum && res >= schema4.maximum || !schema4.exclusiveMaximum && res > schema4.maximum) {
        res = (schema4.maximum + schema4.minimum) / 2;
      }
      return res;
    }
    if (schema4.minimum) {
      if (schema4.exclusiveMinimum) {
        return Math.floor(schema4.minimum) + 1;
      } else {
        return schema4.minimum;
      }
    }
    if (schema4.maximum) {
      if (schema4.exclusiveMaximum) {
        return schema4.maximum > 0 ? 0 : Math.floor(schema4.maximum) - 1;
      } else {
        return schema4.maximum > 0 ? 0 : schema4.maximum;
      }
    }
  } else {
    if (schema4.minimum) {
      return schema4.minimum;
    }
    if (schema4.exclusiveMinimum) {
      res = Math.floor(schema4.exclusiveMinimum) + 1;
      if (res === schema4.exclusiveMaximum) {
        res = (res + Math.floor(schema4.exclusiveMaximum) - 1) / 2;
      }
    } else if (schema4.exclusiveMaximum) {
      res = Math.floor(schema4.exclusiveMaximum) - 1;
    } else if (schema4.maximum) {
      res = schema4.maximum;
    }
  }
  return res;
}
var init_number = __esm({
  "node_modules/openapi-sampler/src/samplers/number.js"() {
  }
});

// node_modules/openapi-sampler/src/samplers/object.js
function sampleObject(schema4, options = {}, spec, context) {
  let res = {};
  const depth = context && context.depth || 1;
  if (schema4 && typeof schema4.properties === "object") {
    const requiredProperties = Array.isArray(schema4.required) ? schema4.required : [];
    const requiredPropertiesMap = {};
    for (const requiredProperty of requiredProperties) {
      requiredPropertiesMap[requiredProperty] = true;
    }
    Object.keys(schema4.properties).forEach((propertyName) => {
      if (options.skipNonRequired && !requiredPropertiesMap.hasOwnProperty(propertyName)) {
        return;
      }
      const sample2 = traverse(schema4.properties[propertyName], options, spec, { propertyName, depth: depth + 1 });
      if (options.skipReadOnly && sample2.readOnly) {
        return;
      }
      if (options.skipWriteOnly && sample2.writeOnly) {
        return;
      }
      if (options?.format === "xml") {
        const { propertyName: newPropertyName, value } = applyXMLAttributes(sample2, schema4.properties[propertyName], { propertyName });
        if (newPropertyName) {
          res[newPropertyName] = value;
        } else {
          res = { ...res, ...value };
        }
      } else {
        res[propertyName] = sample2.value;
      }
    });
  }
  if (schema4 && typeof schema4.additionalProperties === "object") {
    const propertyName = schema4.additionalProperties["x-additionalPropertiesName"] || "property";
    res[`${String(propertyName)}1`] = traverse(schema4.additionalProperties, options, spec, { depth: depth + 1 }).value;
    res[`${String(propertyName)}2`] = traverse(schema4.additionalProperties, options, spec, { depth: depth + 1 }).value;
  }
  if (schema4 && typeof schema4.properties === "object" && schema4.maxProperties !== void 0 && Object.keys(res).length > schema4.maxProperties) {
    const filteredResult = {};
    let propertiesAdded = 0;
    const requiredProperties = Array.isArray(schema4.required) ? schema4.required : [];
    requiredProperties.forEach((propName) => {
      if (res[propName] !== void 0) {
        filteredResult[propName] = res[propName];
        propertiesAdded++;
      }
    });
    Object.keys(res).forEach((propName) => {
      if (propertiesAdded < schema4.maxProperties && !filteredResult.hasOwnProperty(propName)) {
        filteredResult[propName] = res[propName];
        propertiesAdded++;
      }
    });
    res = filteredResult;
  }
  return res;
}
var init_object = __esm({
  "node_modules/openapi-sampler/src/samplers/object.js"() {
    init_traverse();
    init_utils();
  }
});

// node_modules/openapi-sampler/src/samplers/string-regex.js
function boolSample() {
  return true;
}
function intSample(min2, _max) {
  return min2;
}
function getRepetitionsBasedOnQuantifierParameters(quantifierSymbol, quantifierMin, quantifierMax) {
  let repetitions = 1;
  if (quantifierSymbol) {
    switch (quantifierSymbol) {
      case "?": {
        repetitions = boolSample() ? 0 : 1;
        break;
      }
      case "*": {
        const limit = 8;
        repetitions = intSample(0, limit);
        break;
      }
      case "+": {
        const limit = 8;
        repetitions = intSample(1, limit);
        break;
      }
      default:
        throw new Error("Unknown quantifier symbol provided.");
    }
  } else if (quantifierMin != null && quantifierMax != null) {
    repetitions = intSample(parseInt(quantifierMin), parseInt(quantifierMax));
  } else if (quantifierMin != null && quantifierMax == null) {
    repetitions = parseInt(quantifierMin);
  }
  return repetitions;
}
function regexSample(pattern) {
  let isCaseInsensitive = false;
  if (pattern instanceof RegExp) {
    isCaseInsensitive = pattern.flags.includes("i");
    pattern = pattern.toString();
    pattern = pattern.match(/\/(.+?)\//)?.[1] ?? "";
  }
  let min2;
  let max2;
  let repetitions;
  const SINGLE_CHAR_REG = /([.A-Za-z0-9])(?:\{(\d+)(?:\,(\d+)|)\}|(\?|\*|\+))(?![^[]*]|[^{]*})/;
  let token2 = pattern.match(SINGLE_CHAR_REG);
  while (token2 != null) {
    const quantifierMin = token2[2];
    const quantifierMax = token2[3];
    const quantifierSymbol = token2[4];
    repetitions = getRepetitionsBasedOnQuantifierParameters(
      quantifierSymbol,
      quantifierMin,
      quantifierMax
    );
    pattern = pattern.slice(0, token2.index) + token2[1].repeat(repetitions) + pattern.slice(token2.index + token2[0].length);
    token2 = pattern.match(SINGLE_CHAR_REG);
  }
  const SINGLE_RANGE_REG = /(\d-\d|\w-\w|\d|\w|[-!@#$&()`.+,/"])/;
  const RANGE_ALPHANUMEMRIC_REG = /\[(\^|)(-|)(.+?)\](?:\{(\d+)(?:\,(\d+)|)\}|(\?|\*|\+)|)/;
  token2 = pattern.match(RANGE_ALPHANUMEMRIC_REG);
  while (token2 != null) {
    const isNegated = token2[1] === "^";
    const includesDash = token2[2] === "-";
    const quantifierMin = token2[4];
    const quantifierMax = token2[5];
    const quantifierSymbol = token2[6];
    const rangeCodes = [];
    let ranges = token2[3];
    let range = ranges.match(SINGLE_RANGE_REG);
    if (includesDash) {
      rangeCodes.push(45);
    }
    while (range != null) {
      if (range[0].indexOf("-") === -1) {
        if (isCaseInsensitive && isNaN(Number(range[0]))) {
          rangeCodes.push(range[0].toUpperCase().charCodeAt(0));
          rangeCodes.push(range[0].toLowerCase().charCodeAt(0));
        } else {
          rangeCodes.push(range[0].charCodeAt(0));
        }
      } else {
        const rangeMinMax = range[0].split("-").map((x2) => x2.charCodeAt(0));
        min2 = rangeMinMax[0];
        max2 = rangeMinMax[1];
        if (min2 > max2) {
          throw new Error("Character range provided is out of order.");
        }
        for (let i2 = min2; i2 <= max2; i2++) {
          if (isCaseInsensitive && isNaN(Number(String.fromCharCode(i2)))) {
            const ch = String.fromCharCode(i2);
            rangeCodes.push(ch.toUpperCase().charCodeAt(0));
            rangeCodes.push(ch.toLowerCase().charCodeAt(0));
          } else {
            rangeCodes.push(i2);
          }
        }
      }
      ranges = ranges.substring(range[0].length);
      range = ranges.match(SINGLE_RANGE_REG);
    }
    repetitions = getRepetitionsBasedOnQuantifierParameters(
      quantifierSymbol,
      quantifierMin,
      quantifierMax
    );
    if (isNegated) {
      let index = -1;
      for (let i2 = 48; i2 <= 57; i2++) {
        index = rangeCodes.indexOf(i2);
        if (index > -1) {
          rangeCodes.splice(index, 1);
          continue;
        }
        rangeCodes.push(i2);
      }
      for (let i2 = 65; i2 <= 90; i2++) {
        index = rangeCodes.indexOf(i2);
        if (index > -1) {
          rangeCodes.splice(index, 1);
          continue;
        }
        rangeCodes.push(i2);
      }
      for (let i2 = 97; i2 <= 122; i2++) {
        index = rangeCodes.indexOf(i2);
        if (index > -1) {
          rangeCodes.splice(index, 1);
          continue;
        }
        rangeCodes.push(i2);
      }
    }
    const generatedString = Array.from(
      { length: repetitions },
      () => String.fromCharCode(rangeCodes[intSample(0, rangeCodes.length - 1)])
    ).join("");
    pattern = pattern.slice(0, token2.index) + generatedString + pattern.slice(token2.index + token2[0].length);
    token2 = pattern.match(RANGE_ALPHANUMEMRIC_REG);
  }
  const RANGE_REP_REG = /(.)\{(\d+)\,(\d+)\}/;
  token2 = pattern.match(RANGE_REP_REG);
  while (token2 != null) {
    min2 = parseInt(token2[2]);
    max2 = parseInt(token2[3]);
    if (min2 > max2) {
      throw new Error("Numbers out of order in {} quantifier.");
    }
    repetitions = intSample(min2, max2);
    pattern = pattern.slice(0, token2.index) + token2[1].repeat(repetitions) + pattern.slice(token2.index + token2[0].length);
    token2 = pattern.match(RANGE_REP_REG);
  }
  const REP_REG = /(.)\{(\d+)\}/;
  token2 = pattern.match(REP_REG);
  while (token2 != null) {
    repetitions = parseInt(token2[2]);
    pattern = pattern.slice(0, token2.index) + token2[1].repeat(repetitions) + pattern.slice(token2.index + token2[0].length);
    token2 = pattern.match(REP_REG);
  }
  return pattern;
}
var init_string_regex = __esm({
  "node_modules/openapi-sampler/src/samplers/string-regex.js"() {
  }
});

// node_modules/openapi-sampler/src/samplers/string.js
function emailSample() {
  return "user@example.com";
}
function idnEmailSample() {
  return "пошта@укр.нет";
}
function passwordSample(min2, max2) {
  let res = "pa$$word";
  if (min2 > res.length) {
    res += "_";
    res += ensureMinLength(passwordSymbols, min2 - res.length).substring(0, min2 - res.length);
  }
  return res;
}
function commonDateTimeSample({ min: min2, max: max2, omitTime, omitDate }) {
  let res = toRFCDateTime(/* @__PURE__ */ new Date("2019-08-24T14:15:22.123Z"), omitTime, omitDate, false);
  if (res.length < min2) {
    console.warn(`Using minLength = ${min2} is incorrect with format "date-time"`);
  }
  if (max2 && res.length > max2) {
    console.warn(`Using maxLength = ${max2} is incorrect with format "date-time"`);
  }
  return res;
}
function dateTimeSample(min2, max2) {
  return commonDateTimeSample({ min: min2, max: max2, omitTime: false, omitDate: false });
}
function dateSample(min2, max2) {
  return commonDateTimeSample({ min: min2, max: max2, omitTime: true, omitDate: false });
}
function timeSample(min2, max2) {
  return commonDateTimeSample({ min: min2, max: max2, omitTime: false, omitDate: true }).slice(1);
}
function defaultSample(min2, max2, _propertyName, pattern, enablePatterns = false) {
  if (pattern && enablePatterns) {
    return regexSample(pattern);
  }
  let res = ensureMinLength("string", min2);
  if (max2 && res.length > max2) {
    res = res.substring(0, max2);
  }
  return res;
}
function ipv4Sample() {
  return "192.168.0.1";
}
function ipv6Sample() {
  return "2001:0db8:85a3:0000:0000:8a2e:0370:7334";
}
function hostnameSample() {
  return "example.com";
}
function idnHostnameSample() {
  return "приклад.укр";
}
function uriSample() {
  return "http://example.com";
}
function uriReferenceSample() {
  return "../dictionary";
}
function uriTemplateSample() {
  return "http://example.com/{endpoint}";
}
function iriSample() {
  return "http://example.com/entity/1";
}
function iriReferenceSample() {
  return "/entity/1";
}
function uuidSample(_min, _max, propertyName) {
  return uuid(propertyName || "id");
}
function jsonPointerSample() {
  return "/json/pointer";
}
function relativeJsonPointerSample() {
  return "1/relative/json/pointer";
}
function regexSample2() {
  return "/regex/";
}
function sampleString(schema4, options, spec, context) {
  let format2 = schema4.format || "default";
  let sampler = stringFormats[format2] || defaultSample;
  let propertyName = context && context.propertyName;
  return sampler(
    schema4.minLength || 0,
    schema4.maxLength,
    propertyName,
    schema4.pattern,
    options?.enablePatterns
  );
}
var passwordSymbols, stringFormats;
var init_string2 = __esm({
  "node_modules/openapi-sampler/src/samplers/string.js"() {
    "use strict";
    init_utils();
    init_string_regex();
    passwordSymbols = "qwerty!@#$%^123456";
    stringFormats = {
      "email": emailSample,
      "idn-email": idnEmailSample,
      // https://tools.ietf.org/html/rfc6531#section-3.3
      "password": passwordSample,
      "date-time": dateTimeSample,
      "date": dateSample,
      "time": timeSample,
      // full-time in https://tools.ietf.org/html/rfc3339#section-5.6
      "ipv4": ipv4Sample,
      "ipv6": ipv6Sample,
      "hostname": hostnameSample,
      "idn-hostname": idnHostnameSample,
      // https://tools.ietf.org/html/rfc5890#section-2.3.2.3
      "iri": iriSample,
      // https://tools.ietf.org/html/rfc3987
      "iri-reference": iriReferenceSample,
      "uri": uriSample,
      "uri-reference": uriReferenceSample,
      // either a URI or relative-reference https://tools.ietf.org/html/rfc3986#section-4.1
      "uri-template": uriTemplateSample,
      "uuid": uuidSample,
      "default": defaultSample,
      "json-pointer": jsonPointerSample,
      "relative-json-pointer": relativeJsonPointerSample,
      // https://tools.ietf.org/html/draft-handrews-relative-json-pointer-01
      "regex": regexSample2
    };
  }
});

// node_modules/openapi-sampler/src/samplers/index.js
var init_samplers = __esm({
  "node_modules/openapi-sampler/src/samplers/index.js"() {
    init_array();
    init_boolean();
    init_number();
    init_object();
    init_string2();
  }
});

// node_modules/fast-xml-parser/src/util.js
var require_util2 = __commonJS({
  "node_modules/fast-xml-parser/src/util.js"(exports) {
    "use strict";
    var nameStartChar = ":A-Za-z_\\u00C0-\\u00D6\\u00D8-\\u00F6\\u00F8-\\u02FF\\u0370-\\u037D\\u037F-\\u1FFF\\u200C-\\u200D\\u2070-\\u218F\\u2C00-\\u2FEF\\u3001-\\uD7FF\\uF900-\\uFDCF\\uFDF0-\\uFFFD";
    var nameChar = nameStartChar + "\\-.\\d\\u00B7\\u0300-\\u036F\\u203F-\\u2040";
    var nameRegexp = "[" + nameStartChar + "][" + nameChar + "]*";
    var regexName = new RegExp("^" + nameRegexp + "$");
    var getAllMatches = function(string2, regex) {
      const matches2 = [];
      let match2 = regex.exec(string2);
      while (match2) {
        const allmatches = [];
        allmatches.startIndex = regex.lastIndex - match2[0].length;
        const len = match2.length;
        for (let index = 0; index < len; index++) {
          allmatches.push(match2[index]);
        }
        matches2.push(allmatches);
        match2 = regex.exec(string2);
      }
      return matches2;
    };
    var isName = function(string2) {
      const match2 = regexName.exec(string2);
      return !(match2 === null || typeof match2 === "undefined");
    };
    exports.isExist = function(v2) {
      return typeof v2 !== "undefined";
    };
    exports.isEmptyObject = function(obj) {
      return Object.keys(obj).length === 0;
    };
    exports.merge = function(target, a2, arrayMode) {
      if (a2) {
        const keys2 = Object.keys(a2);
        const len = keys2.length;
        for (let i2 = 0; i2 < len; i2++) {
          if (arrayMode === "strict") {
            target[keys2[i2]] = [a2[keys2[i2]]];
          } else {
            target[keys2[i2]] = a2[keys2[i2]];
          }
        }
      }
    };
    exports.getValue = function(v2) {
      if (exports.isExist(v2)) {
        return v2;
      } else {
        return "";
      }
    };
    exports.isName = isName;
    exports.getAllMatches = getAllMatches;
    exports.nameRegexp = nameRegexp;
  }
});

// node_modules/fast-xml-parser/src/validator.js
var require_validator = __commonJS({
  "node_modules/fast-xml-parser/src/validator.js"(exports) {
    "use strict";
    var util = require_util2();
    var defaultOptions = {
      allowBooleanAttributes: false,
      //A tag can have attributes without any value
      unpairedTags: []
    };
    exports.validate = function(xmlData, options) {
      options = Object.assign({}, defaultOptions, options);
      const tags = [];
      let tagFound = false;
      let reachedRoot = false;
      if (xmlData[0] === "\uFEFF") {
        xmlData = xmlData.substr(1);
      }
      for (let i2 = 0; i2 < xmlData.length; i2++) {
        if (xmlData[i2] === "<" && xmlData[i2 + 1] === "?") {
          i2 += 2;
          i2 = readPI(xmlData, i2);
          if (i2.err) return i2;
        } else if (xmlData[i2] === "<") {
          let tagStartPos = i2;
          i2++;
          if (xmlData[i2] === "!") {
            i2 = readCommentAndCDATA(xmlData, i2);
            continue;
          } else {
            let closingTag = false;
            if (xmlData[i2] === "/") {
              closingTag = true;
              i2++;
            }
            let tagName = "";
            for (; i2 < xmlData.length && xmlData[i2] !== ">" && xmlData[i2] !== " " && xmlData[i2] !== "	" && xmlData[i2] !== "\n" && xmlData[i2] !== "\r"; i2++) {
              tagName += xmlData[i2];
            }
            tagName = tagName.trim();
            if (tagName[tagName.length - 1] === "/") {
              tagName = tagName.substring(0, tagName.length - 1);
              i2--;
            }
            if (!validateTagName(tagName)) {
              let msg;
              if (tagName.trim().length === 0) {
                msg = "Invalid space after '<'.";
              } else {
                msg = "Tag '" + tagName + "' is an invalid name.";
              }
              return getErrorObject("InvalidTag", msg, getLineNumberForPosition(xmlData, i2));
            }
            const result = readAttributeStr(xmlData, i2);
            if (result === false) {
              return getErrorObject("InvalidAttr", "Attributes for '" + tagName + "' have open quote.", getLineNumberForPosition(xmlData, i2));
            }
            let attrStr = result.value;
            i2 = result.index;
            if (attrStr[attrStr.length - 1] === "/") {
              const attrStrStart = i2 - attrStr.length;
              attrStr = attrStr.substring(0, attrStr.length - 1);
              const isValid = validateAttributeString(attrStr, options);
              if (isValid === true) {
                tagFound = true;
              } else {
                return getErrorObject(isValid.err.code, isValid.err.msg, getLineNumberForPosition(xmlData, attrStrStart + isValid.err.line));
              }
            } else if (closingTag) {
              if (!result.tagClosed) {
                return getErrorObject("InvalidTag", "Closing tag '" + tagName + "' doesn't have proper closing.", getLineNumberForPosition(xmlData, i2));
              } else if (attrStr.trim().length > 0) {
                return getErrorObject("InvalidTag", "Closing tag '" + tagName + "' can't have attributes or invalid starting.", getLineNumberForPosition(xmlData, tagStartPos));
              } else if (tags.length === 0) {
                return getErrorObject("InvalidTag", "Closing tag '" + tagName + "' has not been opened.", getLineNumberForPosition(xmlData, tagStartPos));
              } else {
                const otg = tags.pop();
                if (tagName !== otg.tagName) {
                  let openPos = getLineNumberForPosition(xmlData, otg.tagStartPos);
                  return getErrorObject(
                    "InvalidTag",
                    "Expected closing tag '" + otg.tagName + "' (opened in line " + openPos.line + ", col " + openPos.col + ") instead of closing tag '" + tagName + "'.",
                    getLineNumberForPosition(xmlData, tagStartPos)
                  );
                }
                if (tags.length == 0) {
                  reachedRoot = true;
                }
              }
            } else {
              const isValid = validateAttributeString(attrStr, options);
              if (isValid !== true) {
                return getErrorObject(isValid.err.code, isValid.err.msg, getLineNumberForPosition(xmlData, i2 - attrStr.length + isValid.err.line));
              }
              if (reachedRoot === true) {
                return getErrorObject("InvalidXml", "Multiple possible root nodes found.", getLineNumberForPosition(xmlData, i2));
              } else if (options.unpairedTags.indexOf(tagName) !== -1) {
              } else {
                tags.push({ tagName, tagStartPos });
              }
              tagFound = true;
            }
            for (i2++; i2 < xmlData.length; i2++) {
              if (xmlData[i2] === "<") {
                if (xmlData[i2 + 1] === "!") {
                  i2++;
                  i2 = readCommentAndCDATA(xmlData, i2);
                  continue;
                } else if (xmlData[i2 + 1] === "?") {
                  i2 = readPI(xmlData, ++i2);
                  if (i2.err) return i2;
                } else {
                  break;
                }
              } else if (xmlData[i2] === "&") {
                const afterAmp = validateAmpersand(xmlData, i2);
                if (afterAmp == -1)
                  return getErrorObject("InvalidChar", "char '&' is not expected.", getLineNumberForPosition(xmlData, i2));
                i2 = afterAmp;
              } else {
                if (reachedRoot === true && !isWhiteSpace(xmlData[i2])) {
                  return getErrorObject("InvalidXml", "Extra text at the end", getLineNumberForPosition(xmlData, i2));
                }
              }
            }
            if (xmlData[i2] === "<") {
              i2--;
            }
          }
        } else {
          if (isWhiteSpace(xmlData[i2])) {
            continue;
          }
          return getErrorObject("InvalidChar", "char '" + xmlData[i2] + "' is not expected.", getLineNumberForPosition(xmlData, i2));
        }
      }
      if (!tagFound) {
        return getErrorObject("InvalidXml", "Start tag expected.", 1);
      } else if (tags.length == 1) {
        return getErrorObject("InvalidTag", "Unclosed tag '" + tags[0].tagName + "'.", getLineNumberForPosition(xmlData, tags[0].tagStartPos));
      } else if (tags.length > 0) {
        return getErrorObject("InvalidXml", "Invalid '" + JSON.stringify(tags.map((t) => t.tagName), null, 4).replace(/\r?\n/g, "") + "' found.", { line: 1, col: 1 });
      }
      return true;
    };
    function isWhiteSpace(char2) {
      return char2 === " " || char2 === "	" || char2 === "\n" || char2 === "\r";
    }
    function readPI(xmlData, i2) {
      const start = i2;
      for (; i2 < xmlData.length; i2++) {
        if (xmlData[i2] == "?" || xmlData[i2] == " ") {
          const tagname = xmlData.substr(start, i2 - start);
          if (i2 > 5 && tagname === "xml") {
            return getErrorObject("InvalidXml", "XML declaration allowed only at the start of the document.", getLineNumberForPosition(xmlData, i2));
          } else if (xmlData[i2] == "?" && xmlData[i2 + 1] == ">") {
            i2++;
            break;
          } else {
            continue;
          }
        }
      }
      return i2;
    }
    function readCommentAndCDATA(xmlData, i2) {
      if (xmlData.length > i2 + 5 && xmlData[i2 + 1] === "-" && xmlData[i2 + 2] === "-") {
        for (i2 += 3; i2 < xmlData.length; i2++) {
          if (xmlData[i2] === "-" && xmlData[i2 + 1] === "-" && xmlData[i2 + 2] === ">") {
            i2 += 2;
            break;
          }
        }
      } else if (xmlData.length > i2 + 8 && xmlData[i2 + 1] === "D" && xmlData[i2 + 2] === "O" && xmlData[i2 + 3] === "C" && xmlData[i2 + 4] === "T" && xmlData[i2 + 5] === "Y" && xmlData[i2 + 6] === "P" && xmlData[i2 + 7] === "E") {
        let angleBracketsCount = 1;
        for (i2 += 8; i2 < xmlData.length; i2++) {
          if (xmlData[i2] === "<") {
            angleBracketsCount++;
          } else if (xmlData[i2] === ">") {
            angleBracketsCount--;
            if (angleBracketsCount === 0) {
              break;
            }
          }
        }
      } else if (xmlData.length > i2 + 9 && xmlData[i2 + 1] === "[" && xmlData[i2 + 2] === "C" && xmlData[i2 + 3] === "D" && xmlData[i2 + 4] === "A" && xmlData[i2 + 5] === "T" && xmlData[i2 + 6] === "A" && xmlData[i2 + 7] === "[") {
        for (i2 += 8; i2 < xmlData.length; i2++) {
          if (xmlData[i2] === "]" && xmlData[i2 + 1] === "]" && xmlData[i2 + 2] === ">") {
            i2 += 2;
            break;
          }
        }
      }
      return i2;
    }
    var doubleQuote = '"';
    var singleQuote = "'";
    function readAttributeStr(xmlData, i2) {
      let attrStr = "";
      let startChar = "";
      let tagClosed = false;
      for (; i2 < xmlData.length; i2++) {
        if (xmlData[i2] === doubleQuote || xmlData[i2] === singleQuote) {
          if (startChar === "") {
            startChar = xmlData[i2];
          } else if (startChar !== xmlData[i2]) {
          } else {
            startChar = "";
          }
        } else if (xmlData[i2] === ">") {
          if (startChar === "") {
            tagClosed = true;
            break;
          }
        }
        attrStr += xmlData[i2];
      }
      if (startChar !== "") {
        return false;
      }
      return {
        value: attrStr,
        index: i2,
        tagClosed
      };
    }
    var validAttrStrRegxp = new RegExp(`(\\s*)([^\\s=]+)(\\s*=)?(\\s*(['"])(([\\s\\S])*?)\\5)?`, "g");
    function validateAttributeString(attrStr, options) {
      const matches2 = util.getAllMatches(attrStr, validAttrStrRegxp);
      const attrNames = {};
      for (let i2 = 0; i2 < matches2.length; i2++) {
        if (matches2[i2][1].length === 0) {
          return getErrorObject("InvalidAttr", "Attribute '" + matches2[i2][2] + "' has no space in starting.", getPositionFromMatch(matches2[i2]));
        } else if (matches2[i2][3] !== void 0 && matches2[i2][4] === void 0) {
          return getErrorObject("InvalidAttr", "Attribute '" + matches2[i2][2] + "' is without value.", getPositionFromMatch(matches2[i2]));
        } else if (matches2[i2][3] === void 0 && !options.allowBooleanAttributes) {
          return getErrorObject("InvalidAttr", "boolean attribute '" + matches2[i2][2] + "' is not allowed.", getPositionFromMatch(matches2[i2]));
        }
        const attrName = matches2[i2][2];
        if (!validateAttrName(attrName)) {
          return getErrorObject("InvalidAttr", "Attribute '" + attrName + "' is an invalid name.", getPositionFromMatch(matches2[i2]));
        }
        if (!attrNames.hasOwnProperty(attrName)) {
          attrNames[attrName] = 1;
        } else {
          return getErrorObject("InvalidAttr", "Attribute '" + attrName + "' is repeated.", getPositionFromMatch(matches2[i2]));
        }
      }
      return true;
    }
    function validateNumberAmpersand(xmlData, i2) {
      let re2 = /\d/;
      if (xmlData[i2] === "x") {
        i2++;
        re2 = /[\da-fA-F]/;
      }
      for (; i2 < xmlData.length; i2++) {
        if (xmlData[i2] === ";")
          return i2;
        if (!xmlData[i2].match(re2))
          break;
      }
      return -1;
    }
    function validateAmpersand(xmlData, i2) {
      i2++;
      if (xmlData[i2] === ";")
        return -1;
      if (xmlData[i2] === "#") {
        i2++;
        return validateNumberAmpersand(xmlData, i2);
      }
      let count = 0;
      for (; i2 < xmlData.length; i2++, count++) {
        if (xmlData[i2].match(/\w/) && count < 20)
          continue;
        if (xmlData[i2] === ";")
          break;
        return -1;
      }
      return i2;
    }
    function getErrorObject(code, message, lineNumber) {
      return {
        err: {
          code,
          msg: message,
          line: lineNumber.line || lineNumber,
          col: lineNumber.col
        }
      };
    }
    function validateAttrName(attrName) {
      return util.isName(attrName);
    }
    function validateTagName(tagname) {
      return util.isName(tagname);
    }
    function getLineNumberForPosition(xmlData, index) {
      const lines = xmlData.substring(0, index).split(/\r?\n/);
      return {
        line: lines.length,
        // column number is last line's length + 1, because column numbering starts at 1:
        col: lines[lines.length - 1].length + 1
      };
    }
    function getPositionFromMatch(match2) {
      return match2.startIndex + match2[1].length;
    }
  }
});

// node_modules/fast-xml-parser/src/xmlparser/OptionsBuilder.js
var require_OptionsBuilder = __commonJS({
  "node_modules/fast-xml-parser/src/xmlparser/OptionsBuilder.js"(exports) {
    var defaultOptions = {
      preserveOrder: false,
      attributeNamePrefix: "@_",
      attributesGroupName: false,
      textNodeName: "#text",
      ignoreAttributes: true,
      removeNSPrefix: false,
      // remove NS from tag name or attribute name if true
      allowBooleanAttributes: false,
      //a tag can have attributes without any value
      //ignoreRootElement : false,
      parseTagValue: true,
      parseAttributeValue: false,
      trimValues: true,
      //Trim string values of tag and attributes
      cdataPropName: false,
      numberParseOptions: {
        hex: true,
        leadingZeros: true,
        eNotation: true
      },
      tagValueProcessor: function(tagName, val) {
        return val;
      },
      attributeValueProcessor: function(attrName, val) {
        return val;
      },
      stopNodes: [],
      //nested tags will not be parsed even for errors
      alwaysCreateTextNode: false,
      isArray: () => false,
      commentPropName: false,
      unpairedTags: [],
      processEntities: true,
      htmlEntities: false,
      ignoreDeclaration: false,
      ignorePiTags: false,
      transformTagName: false,
      transformAttributeName: false,
      updateTag: function(tagName, jPath, attrs) {
        return tagName;
      }
      // skipEmptyListItem: false
    };
    var buildOptions = function(options) {
      return Object.assign({}, defaultOptions, options);
    };
    exports.buildOptions = buildOptions;
    exports.defaultOptions = defaultOptions;
  }
});

// node_modules/fast-xml-parser/src/xmlparser/xmlNode.js
var require_xmlNode = __commonJS({
  "node_modules/fast-xml-parser/src/xmlparser/xmlNode.js"(exports, module) {
    "use strict";
    var XmlNode = class {
      constructor(tagname) {
        this.tagname = tagname;
        this.child = [];
        this[":@"] = {};
      }
      add(key2, val) {
        if (key2 === "__proto__") key2 = "#__proto__";
        this.child.push({ [key2]: val });
      }
      addChild(node2) {
        if (node2.tagname === "__proto__") node2.tagname = "#__proto__";
        if (node2[":@"] && Object.keys(node2[":@"]).length > 0) {
          this.child.push({ [node2.tagname]: node2.child, [":@"]: node2[":@"] });
        } else {
          this.child.push({ [node2.tagname]: node2.child });
        }
      }
    };
    module.exports = XmlNode;
  }
});

// node_modules/fast-xml-parser/src/xmlparser/DocTypeReader.js
var require_DocTypeReader = __commonJS({
  "node_modules/fast-xml-parser/src/xmlparser/DocTypeReader.js"(exports, module) {
    var util = require_util2();
    function readDocType(xmlData, i2) {
      const entities = {};
      if (xmlData[i2 + 3] === "O" && xmlData[i2 + 4] === "C" && xmlData[i2 + 5] === "T" && xmlData[i2 + 6] === "Y" && xmlData[i2 + 7] === "P" && xmlData[i2 + 8] === "E") {
        i2 = i2 + 9;
        let angleBracketsCount = 1;
        let hasBody = false, comment2 = false;
        let exp = "";
        for (; i2 < xmlData.length; i2++) {
          if (xmlData[i2] === "<" && !comment2) {
            if (hasBody && isEntity(xmlData, i2)) {
              i2 += 7;
              let entityName, val;
              [entityName, val, i2] = readEntityExp(xmlData, i2 + 1);
              if (val.indexOf("&") === -1)
                entities[validateEntityName(entityName)] = {
                  regx: RegExp(`&${entityName};`, "g"),
                  val
                };
            } else if (hasBody && isElement(xmlData, i2)) i2 += 8;
            else if (hasBody && isAttlist(xmlData, i2)) i2 += 8;
            else if (hasBody && isNotation(xmlData, i2)) i2 += 9;
            else if (isComment) comment2 = true;
            else throw new Error("Invalid DOCTYPE");
            angleBracketsCount++;
            exp = "";
          } else if (xmlData[i2] === ">") {
            if (comment2) {
              if (xmlData[i2 - 1] === "-" && xmlData[i2 - 2] === "-") {
                comment2 = false;
                angleBracketsCount--;
              }
            } else {
              angleBracketsCount--;
            }
            if (angleBracketsCount === 0) {
              break;
            }
          } else if (xmlData[i2] === "[") {
            hasBody = true;
          } else {
            exp += xmlData[i2];
          }
        }
        if (angleBracketsCount !== 0) {
          throw new Error(`Unclosed DOCTYPE`);
        }
      } else {
        throw new Error(`Invalid Tag instead of DOCTYPE`);
      }
      return { entities, i: i2 };
    }
    function readEntityExp(xmlData, i2) {
      let entityName = "";
      for (; i2 < xmlData.length && (xmlData[i2] !== "'" && xmlData[i2] !== '"'); i2++) {
        entityName += xmlData[i2];
      }
      entityName = entityName.trim();
      if (entityName.indexOf(" ") !== -1) throw new Error("External entites are not supported");
      const startChar = xmlData[i2++];
      let val = "";
      for (; i2 < xmlData.length && xmlData[i2] !== startChar; i2++) {
        val += xmlData[i2];
      }
      return [entityName, val, i2];
    }
    function isComment(xmlData, i2) {
      if (xmlData[i2 + 1] === "!" && xmlData[i2 + 2] === "-" && xmlData[i2 + 3] === "-") return true;
      return false;
    }
    function isEntity(xmlData, i2) {
      if (xmlData[i2 + 1] === "!" && xmlData[i2 + 2] === "E" && xmlData[i2 + 3] === "N" && xmlData[i2 + 4] === "T" && xmlData[i2 + 5] === "I" && xmlData[i2 + 6] === "T" && xmlData[i2 + 7] === "Y") return true;
      return false;
    }
    function isElement(xmlData, i2) {
      if (xmlData[i2 + 1] === "!" && xmlData[i2 + 2] === "E" && xmlData[i2 + 3] === "L" && xmlData[i2 + 4] === "E" && xmlData[i2 + 5] === "M" && xmlData[i2 + 6] === "E" && xmlData[i2 + 7] === "N" && xmlData[i2 + 8] === "T") return true;
      return false;
    }
    function isAttlist(xmlData, i2) {
      if (xmlData[i2 + 1] === "!" && xmlData[i2 + 2] === "A" && xmlData[i2 + 3] === "T" && xmlData[i2 + 4] === "T" && xmlData[i2 + 5] === "L" && xmlData[i2 + 6] === "I" && xmlData[i2 + 7] === "S" && xmlData[i2 + 8] === "T") return true;
      return false;
    }
    function isNotation(xmlData, i2) {
      if (xmlData[i2 + 1] === "!" && xmlData[i2 + 2] === "N" && xmlData[i2 + 3] === "O" && xmlData[i2 + 4] === "T" && xmlData[i2 + 5] === "A" && xmlData[i2 + 6] === "T" && xmlData[i2 + 7] === "I" && xmlData[i2 + 8] === "O" && xmlData[i2 + 9] === "N") return true;
      return false;
    }
    function validateEntityName(name) {
      if (util.isName(name))
        return name;
      else
        throw new Error(`Invalid entity name ${name}`);
    }
    module.exports = readDocType;
  }
});

// node_modules/strnum/strnum.js
var require_strnum = __commonJS({
  "node_modules/strnum/strnum.js"(exports, module) {
    var hexRegex2 = /^[-+]?0x[a-fA-F0-9]+$/;
    var numRegex = /^([\-\+])?(0*)([0-9]*(\.[0-9]*)?)$/;
    var consider = {
      hex: true,
      // oct: false,
      leadingZeros: true,
      decimalPoint: ".",
      eNotation: true
      //skipLike: /regex/
    };
    function toNumber(str, options = {}) {
      options = Object.assign({}, consider, options);
      if (!str || typeof str !== "string") return str;
      let trimmedStr = str.trim();
      if (options.skipLike !== void 0 && options.skipLike.test(trimmedStr)) return str;
      else if (str === "0") return 0;
      else if (options.hex && hexRegex2.test(trimmedStr)) {
        return parse_int(trimmedStr, 16);
      } else if (trimmedStr.search(/[eE]/) !== -1) {
        const notation = trimmedStr.match(/^([-\+])?(0*)([0-9]*(\.[0-9]*)?[eE][-\+]?[0-9]+)$/);
        if (notation) {
          if (options.leadingZeros) {
            trimmedStr = (notation[1] || "") + notation[3];
          } else {
            if (notation[2] === "0" && notation[3][0] === ".") {
            } else {
              return str;
            }
          }
          return options.eNotation ? Number(trimmedStr) : str;
        } else {
          return str;
        }
      } else {
        const match2 = numRegex.exec(trimmedStr);
        if (match2) {
          const sign = match2[1];
          const leadingZeros = match2[2];
          let numTrimmedByZeros = trimZeros(match2[3]);
          if (!options.leadingZeros && leadingZeros.length > 0 && sign && trimmedStr[2] !== ".") return str;
          else if (!options.leadingZeros && leadingZeros.length > 0 && !sign && trimmedStr[1] !== ".") return str;
          else if (options.leadingZeros && leadingZeros === str) return 0;
          else {
            const num = Number(trimmedStr);
            const numStr = "" + num;
            if (numStr.search(/[eE]/) !== -1) {
              if (options.eNotation) return num;
              else return str;
            } else if (trimmedStr.indexOf(".") !== -1) {
              if (numStr === "0" && numTrimmedByZeros === "") return num;
              else if (numStr === numTrimmedByZeros) return num;
              else if (sign && numStr === "-" + numTrimmedByZeros) return num;
              else return str;
            }
            if (leadingZeros) {
              return numTrimmedByZeros === numStr || sign + numTrimmedByZeros === numStr ? num : str;
            } else {
              return trimmedStr === numStr || trimmedStr === sign + numStr ? num : str;
            }
          }
        } else {
          return str;
        }
      }
    }
    function trimZeros(numStr) {
      if (numStr && numStr.indexOf(".") !== -1) {
        numStr = numStr.replace(/0+$/, "");
        if (numStr === ".") numStr = "0";
        else if (numStr[0] === ".") numStr = "0" + numStr;
        else if (numStr[numStr.length - 1] === ".") numStr = numStr.substr(0, numStr.length - 1);
        return numStr;
      }
      return numStr;
    }
    function parse_int(numStr, base) {
      if (parseInt) return parseInt(numStr, base);
      else if (Number.parseInt) return Number.parseInt(numStr, base);
      else if (window && window.parseInt) return window.parseInt(numStr, base);
      else throw new Error("parseInt, Number.parseInt, window.parseInt are not supported");
    }
    module.exports = toNumber;
  }
});

// node_modules/fast-xml-parser/src/ignoreAttributes.js
var require_ignoreAttributes = __commonJS({
  "node_modules/fast-xml-parser/src/ignoreAttributes.js"(exports, module) {
    function getIgnoreAttributesFn(ignoreAttributes) {
      if (typeof ignoreAttributes === "function") {
        return ignoreAttributes;
      }
      if (Array.isArray(ignoreAttributes)) {
        return (attrName) => {
          for (const pattern of ignoreAttributes) {
            if (typeof pattern === "string" && attrName === pattern) {
              return true;
            }
            if (pattern instanceof RegExp && pattern.test(attrName)) {
              return true;
            }
          }
        };
      }
      return () => false;
    }
    module.exports = getIgnoreAttributesFn;
  }
});

// node_modules/fast-xml-parser/src/xmlparser/OrderedObjParser.js
var require_OrderedObjParser = __commonJS({
  "node_modules/fast-xml-parser/src/xmlparser/OrderedObjParser.js"(exports, module) {
    "use strict";
    var util = require_util2();
    var xmlNode = require_xmlNode();
    var readDocType = require_DocTypeReader();
    var toNumber = require_strnum();
    var getIgnoreAttributesFn = require_ignoreAttributes();
    var OrderedObjParser = class {
      constructor(options) {
        this.options = options;
        this.currentNode = null;
        this.tagsNodeStack = [];
        this.docTypeEntities = {};
        this.lastEntities = {
          "apos": { regex: /&(apos|#39|#x27);/g, val: "'" },
          "gt": { regex: /&(gt|#62|#x3E);/g, val: ">" },
          "lt": { regex: /&(lt|#60|#x3C);/g, val: "<" },
          "quot": { regex: /&(quot|#34|#x22);/g, val: '"' }
        };
        this.ampEntity = { regex: /&(amp|#38|#x26);/g, val: "&" };
        this.htmlEntities = {
          "space": { regex: /&(nbsp|#160);/g, val: " " },
          // "lt" : { regex: /&(lt|#60);/g, val: "<" },
          // "gt" : { regex: /&(gt|#62);/g, val: ">" },
          // "amp" : { regex: /&(amp|#38);/g, val: "&" },
          // "quot" : { regex: /&(quot|#34);/g, val: "\"" },
          // "apos" : { regex: /&(apos|#39);/g, val: "'" },
          "cent": { regex: /&(cent|#162);/g, val: "¢" },
          "pound": { regex: /&(pound|#163);/g, val: "£" },
          "yen": { regex: /&(yen|#165);/g, val: "¥" },
          "euro": { regex: /&(euro|#8364);/g, val: "€" },
          "copyright": { regex: /&(copy|#169);/g, val: "©" },
          "reg": { regex: /&(reg|#174);/g, val: "®" },
          "inr": { regex: /&(inr|#8377);/g, val: "₹" },
          "num_dec": { regex: /&#([0-9]{1,7});/g, val: (_15, str) => String.fromCharCode(Number.parseInt(str, 10)) },
          "num_hex": { regex: /&#x([0-9a-fA-F]{1,6});/g, val: (_15, str) => String.fromCharCode(Number.parseInt(str, 16)) }
        };
        this.addExternalEntities = addExternalEntities;
        this.parseXml = parseXml;
        this.parseTextData = parseTextData;
        this.resolveNameSpace = resolveNameSpace;
        this.buildAttributesMap = buildAttributesMap;
        this.isItStopNode = isItStopNode;
        this.replaceEntitiesValue = replaceEntitiesValue;
        this.readStopNodeData = readStopNodeData;
        this.saveTextToParentTag = saveTextToParentTag;
        this.addChild = addChild;
        this.ignoreAttributesFn = getIgnoreAttributesFn(this.options.ignoreAttributes);
      }
    };
    function addExternalEntities(externalEntities) {
      const entKeys = Object.keys(externalEntities);
      for (let i2 = 0; i2 < entKeys.length; i2++) {
        const ent = entKeys[i2];
        this.lastEntities[ent] = {
          regex: new RegExp("&" + ent + ";", "g"),
          val: externalEntities[ent]
        };
      }
    }
    function parseTextData(val, tagName, jPath, dontTrim, hasAttributes, isLeafNode, escapeEntities) {
      if (val !== void 0) {
        if (this.options.trimValues && !dontTrim) {
          val = val.trim();
        }
        if (val.length > 0) {
          if (!escapeEntities) val = this.replaceEntitiesValue(val);
          const newval = this.options.tagValueProcessor(tagName, val, jPath, hasAttributes, isLeafNode);
          if (newval === null || newval === void 0) {
            return val;
          } else if (typeof newval !== typeof val || newval !== val) {
            return newval;
          } else if (this.options.trimValues) {
            return parseValue(val, this.options.parseTagValue, this.options.numberParseOptions);
          } else {
            const trimmedVal = val.trim();
            if (trimmedVal === val) {
              return parseValue(val, this.options.parseTagValue, this.options.numberParseOptions);
            } else {
              return val;
            }
          }
        }
      }
    }
    function resolveNameSpace(tagname) {
      if (this.options.removeNSPrefix) {
        const tags = tagname.split(":");
        const prefix2 = tagname.charAt(0) === "/" ? "/" : "";
        if (tags[0] === "xmlns") {
          return "";
        }
        if (tags.length === 2) {
          tagname = prefix2 + tags[1];
        }
      }
      return tagname;
    }
    var attrsRegx = new RegExp(`([^\\s=]+)\\s*(=\\s*(['"])([\\s\\S]*?)\\3)?`, "gm");
    function buildAttributesMap(attrStr, jPath, tagName) {
      if (this.options.ignoreAttributes !== true && typeof attrStr === "string") {
        const matches2 = util.getAllMatches(attrStr, attrsRegx);
        const len = matches2.length;
        const attrs = {};
        for (let i2 = 0; i2 < len; i2++) {
          const attrName = this.resolveNameSpace(matches2[i2][1]);
          if (this.ignoreAttributesFn(attrName, jPath)) {
            continue;
          }
          let oldVal = matches2[i2][4];
          let aName = this.options.attributeNamePrefix + attrName;
          if (attrName.length) {
            if (this.options.transformAttributeName) {
              aName = this.options.transformAttributeName(aName);
            }
            if (aName === "__proto__") aName = "#__proto__";
            if (oldVal !== void 0) {
              if (this.options.trimValues) {
                oldVal = oldVal.trim();
              }
              oldVal = this.replaceEntitiesValue(oldVal);
              const newVal = this.options.attributeValueProcessor(attrName, oldVal, jPath);
              if (newVal === null || newVal === void 0) {
                attrs[aName] = oldVal;
              } else if (typeof newVal !== typeof oldVal || newVal !== oldVal) {
                attrs[aName] = newVal;
              } else {
                attrs[aName] = parseValue(
                  oldVal,
                  this.options.parseAttributeValue,
                  this.options.numberParseOptions
                );
              }
            } else if (this.options.allowBooleanAttributes) {
              attrs[aName] = true;
            }
          }
        }
        if (!Object.keys(attrs).length) {
          return;
        }
        if (this.options.attributesGroupName) {
          const attrCollection = {};
          attrCollection[this.options.attributesGroupName] = attrs;
          return attrCollection;
        }
        return attrs;
      }
    }
    var parseXml = function(xmlData) {
      xmlData = xmlData.replace(/\r\n?/g, "\n");
      const xmlObj = new xmlNode("!xml");
      let currentNode = xmlObj;
      let textData = "";
      let jPath = "";
      for (let i2 = 0; i2 < xmlData.length; i2++) {
        const ch = xmlData[i2];
        if (ch === "<") {
          if (xmlData[i2 + 1] === "/") {
            const closeIndex = findClosingIndex(xmlData, ">", i2, "Closing Tag is not closed.");
            let tagName = xmlData.substring(i2 + 2, closeIndex).trim();
            if (this.options.removeNSPrefix) {
              const colonIndex = tagName.indexOf(":");
              if (colonIndex !== -1) {
                tagName = tagName.substr(colonIndex + 1);
              }
            }
            if (this.options.transformTagName) {
              tagName = this.options.transformTagName(tagName);
            }
            if (currentNode) {
              textData = this.saveTextToParentTag(textData, currentNode, jPath);
            }
            const lastTagName = jPath.substring(jPath.lastIndexOf(".") + 1);
            if (tagName && this.options.unpairedTags.indexOf(tagName) !== -1) {
              throw new Error(`Unpaired tag can not be used as closing tag: </${tagName}>`);
            }
            let propIndex = 0;
            if (lastTagName && this.options.unpairedTags.indexOf(lastTagName) !== -1) {
              propIndex = jPath.lastIndexOf(".", jPath.lastIndexOf(".") - 1);
              this.tagsNodeStack.pop();
            } else {
              propIndex = jPath.lastIndexOf(".");
            }
            jPath = jPath.substring(0, propIndex);
            currentNode = this.tagsNodeStack.pop();
            textData = "";
            i2 = closeIndex;
          } else if (xmlData[i2 + 1] === "?") {
            let tagData = readTagExp(xmlData, i2, false, "?>");
            if (!tagData) throw new Error("Pi Tag is not closed.");
            textData = this.saveTextToParentTag(textData, currentNode, jPath);
            if (this.options.ignoreDeclaration && tagData.tagName === "?xml" || this.options.ignorePiTags) {
            } else {
              const childNode = new xmlNode(tagData.tagName);
              childNode.add(this.options.textNodeName, "");
              if (tagData.tagName !== tagData.tagExp && tagData.attrExpPresent) {
                childNode[":@"] = this.buildAttributesMap(tagData.tagExp, jPath, tagData.tagName);
              }
              this.addChild(currentNode, childNode, jPath);
            }
            i2 = tagData.closeIndex + 1;
          } else if (xmlData.substr(i2 + 1, 3) === "!--") {
            const endIndex = findClosingIndex(xmlData, "-->", i2 + 4, "Comment is not closed.");
            if (this.options.commentPropName) {
              const comment2 = xmlData.substring(i2 + 4, endIndex - 2);
              textData = this.saveTextToParentTag(textData, currentNode, jPath);
              currentNode.add(this.options.commentPropName, [{ [this.options.textNodeName]: comment2 }]);
            }
            i2 = endIndex;
          } else if (xmlData.substr(i2 + 1, 2) === "!D") {
            const result = readDocType(xmlData, i2);
            this.docTypeEntities = result.entities;
            i2 = result.i;
          } else if (xmlData.substr(i2 + 1, 2) === "![") {
            const closeIndex = findClosingIndex(xmlData, "]]>", i2, "CDATA is not closed.") - 2;
            const tagExp = xmlData.substring(i2 + 9, closeIndex);
            textData = this.saveTextToParentTag(textData, currentNode, jPath);
            let val = this.parseTextData(tagExp, currentNode.tagname, jPath, true, false, true, true);
            if (val == void 0) val = "";
            if (this.options.cdataPropName) {
              currentNode.add(this.options.cdataPropName, [{ [this.options.textNodeName]: tagExp }]);
            } else {
              currentNode.add(this.options.textNodeName, val);
            }
            i2 = closeIndex + 2;
          } else {
            let result = readTagExp(xmlData, i2, this.options.removeNSPrefix);
            let tagName = result.tagName;
            const rawTagName = result.rawTagName;
            let tagExp = result.tagExp;
            let attrExpPresent = result.attrExpPresent;
            let closeIndex = result.closeIndex;
            if (this.options.transformTagName) {
              tagName = this.options.transformTagName(tagName);
            }
            if (currentNode && textData) {
              if (currentNode.tagname !== "!xml") {
                textData = this.saveTextToParentTag(textData, currentNode, jPath, false);
              }
            }
            const lastTag = currentNode;
            if (lastTag && this.options.unpairedTags.indexOf(lastTag.tagname) !== -1) {
              currentNode = this.tagsNodeStack.pop();
              jPath = jPath.substring(0, jPath.lastIndexOf("."));
            }
            if (tagName !== xmlObj.tagname) {
              jPath += jPath ? "." + tagName : tagName;
            }
            if (this.isItStopNode(this.options.stopNodes, jPath, tagName)) {
              let tagContent = "";
              if (tagExp.length > 0 && tagExp.lastIndexOf("/") === tagExp.length - 1) {
                if (tagName[tagName.length - 1] === "/") {
                  tagName = tagName.substr(0, tagName.length - 1);
                  jPath = jPath.substr(0, jPath.length - 1);
                  tagExp = tagName;
                } else {
                  tagExp = tagExp.substr(0, tagExp.length - 1);
                }
                i2 = result.closeIndex;
              } else if (this.options.unpairedTags.indexOf(tagName) !== -1) {
                i2 = result.closeIndex;
              } else {
                const result2 = this.readStopNodeData(xmlData, rawTagName, closeIndex + 1);
                if (!result2) throw new Error(`Unexpected end of ${rawTagName}`);
                i2 = result2.i;
                tagContent = result2.tagContent;
              }
              const childNode = new xmlNode(tagName);
              if (tagName !== tagExp && attrExpPresent) {
                childNode[":@"] = this.buildAttributesMap(tagExp, jPath, tagName);
              }
              if (tagContent) {
                tagContent = this.parseTextData(tagContent, tagName, jPath, true, attrExpPresent, true, true);
              }
              jPath = jPath.substr(0, jPath.lastIndexOf("."));
              childNode.add(this.options.textNodeName, tagContent);
              this.addChild(currentNode, childNode, jPath);
            } else {
              if (tagExp.length > 0 && tagExp.lastIndexOf("/") === tagExp.length - 1) {
                if (tagName[tagName.length - 1] === "/") {
                  tagName = tagName.substr(0, tagName.length - 1);
                  jPath = jPath.substr(0, jPath.length - 1);
                  tagExp = tagName;
                } else {
                  tagExp = tagExp.substr(0, tagExp.length - 1);
                }
                if (this.options.transformTagName) {
                  tagName = this.options.transformTagName(tagName);
                }
                const childNode = new xmlNode(tagName);
                if (tagName !== tagExp && attrExpPresent) {
                  childNode[":@"] = this.buildAttributesMap(tagExp, jPath, tagName);
                }
                this.addChild(currentNode, childNode, jPath);
                jPath = jPath.substr(0, jPath.lastIndexOf("."));
              } else {
                const childNode = new xmlNode(tagName);
                this.tagsNodeStack.push(currentNode);
                if (tagName !== tagExp && attrExpPresent) {
                  childNode[":@"] = this.buildAttributesMap(tagExp, jPath, tagName);
                }
                this.addChild(currentNode, childNode, jPath);
                currentNode = childNode;
              }
              textData = "";
              i2 = closeIndex;
            }
          }
        } else {
          textData += xmlData[i2];
        }
      }
      return xmlObj.child;
    };
    function addChild(currentNode, childNode, jPath) {
      const result = this.options.updateTag(childNode.tagname, jPath, childNode[":@"]);
      if (result === false) {
      } else if (typeof result === "string") {
        childNode.tagname = result;
        currentNode.addChild(childNode);
      } else {
        currentNode.addChild(childNode);
      }
    }
    var replaceEntitiesValue = function(val) {
      if (this.options.processEntities) {
        for (let entityName in this.docTypeEntities) {
          const entity = this.docTypeEntities[entityName];
          val = val.replace(entity.regx, entity.val);
        }
        for (let entityName in this.lastEntities) {
          const entity = this.lastEntities[entityName];
          val = val.replace(entity.regex, entity.val);
        }
        if (this.options.htmlEntities) {
          for (let entityName in this.htmlEntities) {
            const entity = this.htmlEntities[entityName];
            val = val.replace(entity.regex, entity.val);
          }
        }
        val = val.replace(this.ampEntity.regex, this.ampEntity.val);
      }
      return val;
    };
    function saveTextToParentTag(textData, currentNode, jPath, isLeafNode) {
      if (textData) {
        if (isLeafNode === void 0) isLeafNode = currentNode.child.length === 0;
        textData = this.parseTextData(
          textData,
          currentNode.tagname,
          jPath,
          false,
          currentNode[":@"] ? Object.keys(currentNode[":@"]).length !== 0 : false,
          isLeafNode
        );
        if (textData !== void 0 && textData !== "")
          currentNode.add(this.options.textNodeName, textData);
        textData = "";
      }
      return textData;
    }
    function isItStopNode(stopNodes, jPath, currentTagName) {
      const allNodesExp = "*." + currentTagName;
      for (const stopNodePath in stopNodes) {
        const stopNodeExp = stopNodes[stopNodePath];
        if (allNodesExp === stopNodeExp || jPath === stopNodeExp) return true;
      }
      return false;
    }
    function tagExpWithClosingIndex(xmlData, i2, closingChar = ">") {
      let attrBoundary;
      let tagExp = "";
      for (let index = i2; index < xmlData.length; index++) {
        let ch = xmlData[index];
        if (attrBoundary) {
          if (ch === attrBoundary) attrBoundary = "";
        } else if (ch === '"' || ch === "'") {
          attrBoundary = ch;
        } else if (ch === closingChar[0]) {
          if (closingChar[1]) {
            if (xmlData[index + 1] === closingChar[1]) {
              return {
                data: tagExp,
                index
              };
            }
          } else {
            return {
              data: tagExp,
              index
            };
          }
        } else if (ch === "	") {
          ch = " ";
        }
        tagExp += ch;
      }
    }
    function findClosingIndex(xmlData, str, i2, errMsg) {
      const closingIndex = xmlData.indexOf(str, i2);
      if (closingIndex === -1) {
        throw new Error(errMsg);
      } else {
        return closingIndex + str.length - 1;
      }
    }
    function readTagExp(xmlData, i2, removeNSPrefix, closingChar = ">") {
      const result = tagExpWithClosingIndex(xmlData, i2 + 1, closingChar);
      if (!result) return;
      let tagExp = result.data;
      const closeIndex = result.index;
      const separatorIndex = tagExp.search(/\s/);
      let tagName = tagExp;
      let attrExpPresent = true;
      if (separatorIndex !== -1) {
        tagName = tagExp.substring(0, separatorIndex);
        tagExp = tagExp.substring(separatorIndex + 1).trimStart();
      }
      const rawTagName = tagName;
      if (removeNSPrefix) {
        const colonIndex = tagName.indexOf(":");
        if (colonIndex !== -1) {
          tagName = tagName.substr(colonIndex + 1);
          attrExpPresent = tagName !== result.data.substr(colonIndex + 1);
        }
      }
      return {
        tagName,
        tagExp,
        closeIndex,
        attrExpPresent,
        rawTagName
      };
    }
    function readStopNodeData(xmlData, tagName, i2) {
      const startIndex = i2;
      let openTagCount = 1;
      for (; i2 < xmlData.length; i2++) {
        if (xmlData[i2] === "<") {
          if (xmlData[i2 + 1] === "/") {
            const closeIndex = findClosingIndex(xmlData, ">", i2, `${tagName} is not closed`);
            let closeTagName = xmlData.substring(i2 + 2, closeIndex).trim();
            if (closeTagName === tagName) {
              openTagCount--;
              if (openTagCount === 0) {
                return {
                  tagContent: xmlData.substring(startIndex, i2),
                  i: closeIndex
                };
              }
            }
            i2 = closeIndex;
          } else if (xmlData[i2 + 1] === "?") {
            const closeIndex = findClosingIndex(xmlData, "?>", i2 + 1, "StopNode is not closed.");
            i2 = closeIndex;
          } else if (xmlData.substr(i2 + 1, 3) === "!--") {
            const closeIndex = findClosingIndex(xmlData, "-->", i2 + 3, "StopNode is not closed.");
            i2 = closeIndex;
          } else if (xmlData.substr(i2 + 1, 2) === "![") {
            const closeIndex = findClosingIndex(xmlData, "]]>", i2, "StopNode is not closed.") - 2;
            i2 = closeIndex;
          } else {
            const tagData = readTagExp(xmlData, i2, ">");
            if (tagData) {
              const openTagName = tagData && tagData.tagName;
              if (openTagName === tagName && tagData.tagExp[tagData.tagExp.length - 1] !== "/") {
                openTagCount++;
              }
              i2 = tagData.closeIndex;
            }
          }
        }
      }
    }
    function parseValue(val, shouldParse, options) {
      if (shouldParse && typeof val === "string") {
        const newval = val.trim();
        if (newval === "true") return true;
        else if (newval === "false") return false;
        else return toNumber(val, options);
      } else {
        if (util.isExist(val)) {
          return val;
        } else {
          return "";
        }
      }
    }
    module.exports = OrderedObjParser;
  }
});

// node_modules/fast-xml-parser/src/xmlparser/node2json.js
var require_node2json = __commonJS({
  "node_modules/fast-xml-parser/src/xmlparser/node2json.js"(exports) {
    "use strict";
    function prettify(node2, options) {
      return compress(node2, options);
    }
    function compress(arr, options, jPath) {
      let text;
      const compressedObj = {};
      for (let i2 = 0; i2 < arr.length; i2++) {
        const tagObj = arr[i2];
        const property = propName(tagObj);
        let newJpath = "";
        if (jPath === void 0) newJpath = property;
        else newJpath = jPath + "." + property;
        if (property === options.textNodeName) {
          if (text === void 0) text = tagObj[property];
          else text += "" + tagObj[property];
        } else if (property === void 0) {
          continue;
        } else if (tagObj[property]) {
          let val = compress(tagObj[property], options, newJpath);
          const isLeaf = isLeafTag(val, options);
          if (tagObj[":@"]) {
            assignAttributes(val, tagObj[":@"], newJpath, options);
          } else if (Object.keys(val).length === 1 && val[options.textNodeName] !== void 0 && !options.alwaysCreateTextNode) {
            val = val[options.textNodeName];
          } else if (Object.keys(val).length === 0) {
            if (options.alwaysCreateTextNode) val[options.textNodeName] = "";
            else val = "";
          }
          if (compressedObj[property] !== void 0 && compressedObj.hasOwnProperty(property)) {
            if (!Array.isArray(compressedObj[property])) {
              compressedObj[property] = [compressedObj[property]];
            }
            compressedObj[property].push(val);
          } else {
            if (options.isArray(property, newJpath, isLeaf)) {
              compressedObj[property] = [val];
            } else {
              compressedObj[property] = val;
            }
          }
        }
      }
      if (typeof text === "string") {
        if (text.length > 0) compressedObj[options.textNodeName] = text;
      } else if (text !== void 0) compressedObj[options.textNodeName] = text;
      return compressedObj;
    }
    function propName(obj) {
      const keys2 = Object.keys(obj);
      for (let i2 = 0; i2 < keys2.length; i2++) {
        const key2 = keys2[i2];
        if (key2 !== ":@") return key2;
      }
    }
    function assignAttributes(obj, attrMap, jpath, options) {
      if (attrMap) {
        const keys2 = Object.keys(attrMap);
        const len = keys2.length;
        for (let i2 = 0; i2 < len; i2++) {
          const atrrName = keys2[i2];
          if (options.isArray(atrrName, jpath + "." + atrrName, true, true)) {
            obj[atrrName] = [attrMap[atrrName]];
          } else {
            obj[atrrName] = attrMap[atrrName];
          }
        }
      }
    }
    function isLeafTag(obj, options) {
      const { textNodeName } = options;
      const propCount = Object.keys(obj).length;
      if (propCount === 0) {
        return true;
      }
      if (propCount === 1 && (obj[textNodeName] || typeof obj[textNodeName] === "boolean" || obj[textNodeName] === 0)) {
        return true;
      }
      return false;
    }
    exports.prettify = prettify;
  }
});

// node_modules/fast-xml-parser/src/xmlparser/XMLParser.js
var require_XMLParser = __commonJS({
  "node_modules/fast-xml-parser/src/xmlparser/XMLParser.js"(exports, module) {
    var { buildOptions } = require_OptionsBuilder();
    var OrderedObjParser = require_OrderedObjParser();
    var { prettify } = require_node2json();
    var validator = require_validator();
    var XMLParser = class {
      constructor(options) {
        this.externalEntities = {};
        this.options = buildOptions(options);
      }
      /**
       * Parse XML dats to JS object 
       * @param {string|Buffer} xmlData 
       * @param {boolean|Object} validationOption 
       */
      parse(xmlData, validationOption) {
        if (typeof xmlData === "string") {
        } else if (xmlData.toString) {
          xmlData = xmlData.toString();
        } else {
          throw new Error("XML data is accepted in String or Bytes[] form.");
        }
        if (validationOption) {
          if (validationOption === true) validationOption = {};
          const result = validator.validate(xmlData, validationOption);
          if (result !== true) {
            throw Error(`${result.err.msg}:${result.err.line}:${result.err.col}`);
          }
        }
        const orderedObjParser = new OrderedObjParser(this.options);
        orderedObjParser.addExternalEntities(this.externalEntities);
        const orderedResult = orderedObjParser.parseXml(xmlData);
        if (this.options.preserveOrder || orderedResult === void 0) return orderedResult;
        else return prettify(orderedResult, this.options);
      }
      /**
       * Add Entity which is not by default supported by this library
       * @param {string} key 
       * @param {string} value 
       */
      addEntity(key2, value) {
        if (value.indexOf("&") !== -1) {
          throw new Error("Entity value can't have '&'");
        } else if (key2.indexOf("&") !== -1 || key2.indexOf(";") !== -1) {
          throw new Error("An entity must be set without '&' and ';'. Eg. use '#xD' for '&#xD;'");
        } else if (value === "&") {
          throw new Error("An entity with value '&' is not permitted");
        } else {
          this.externalEntities[key2] = value;
        }
      }
    };
    module.exports = XMLParser;
  }
});

// node_modules/fast-xml-parser/src/xmlbuilder/orderedJs2Xml.js
var require_orderedJs2Xml = __commonJS({
  "node_modules/fast-xml-parser/src/xmlbuilder/orderedJs2Xml.js"(exports, module) {
    var EOL = "\n";
    function toXml(jArray, options) {
      let indentation = "";
      if (options.format && options.indentBy.length > 0) {
        indentation = EOL;
      }
      return arrToStr(jArray, options, "", indentation);
    }
    function arrToStr(arr, options, jPath, indentation) {
      let xmlStr = "";
      let isPreviousElementTag = false;
      for (let i2 = 0; i2 < arr.length; i2++) {
        const tagObj = arr[i2];
        const tagName = propName(tagObj);
        if (tagName === void 0) continue;
        let newJPath = "";
        if (jPath.length === 0) newJPath = tagName;
        else newJPath = `${jPath}.${tagName}`;
        if (tagName === options.textNodeName) {
          let tagText = tagObj[tagName];
          if (!isStopNode(newJPath, options)) {
            tagText = options.tagValueProcessor(tagName, tagText);
            tagText = replaceEntitiesValue(tagText, options);
          }
          if (isPreviousElementTag) {
            xmlStr += indentation;
          }
          xmlStr += tagText;
          isPreviousElementTag = false;
          continue;
        } else if (tagName === options.cdataPropName) {
          if (isPreviousElementTag) {
            xmlStr += indentation;
          }
          xmlStr += `<![CDATA[${tagObj[tagName][0][options.textNodeName]}]]>`;
          isPreviousElementTag = false;
          continue;
        } else if (tagName === options.commentPropName) {
          xmlStr += indentation + `<!--${tagObj[tagName][0][options.textNodeName]}-->`;
          isPreviousElementTag = true;
          continue;
        } else if (tagName[0] === "?") {
          const attStr2 = attr_to_str(tagObj[":@"], options);
          const tempInd = tagName === "?xml" ? "" : indentation;
          let piTextNodeName = tagObj[tagName][0][options.textNodeName];
          piTextNodeName = piTextNodeName.length !== 0 ? " " + piTextNodeName : "";
          xmlStr += tempInd + `<${tagName}${piTextNodeName}${attStr2}?>`;
          isPreviousElementTag = true;
          continue;
        }
        let newIdentation = indentation;
        if (newIdentation !== "") {
          newIdentation += options.indentBy;
        }
        const attStr = attr_to_str(tagObj[":@"], options);
        const tagStart = indentation + `<${tagName}${attStr}`;
        const tagValue = arrToStr(tagObj[tagName], options, newJPath, newIdentation);
        if (options.unpairedTags.indexOf(tagName) !== -1) {
          if (options.suppressUnpairedNode) xmlStr += tagStart + ">";
          else xmlStr += tagStart + "/>";
        } else if ((!tagValue || tagValue.length === 0) && options.suppressEmptyNode) {
          xmlStr += tagStart + "/>";
        } else if (tagValue && tagValue.endsWith(">")) {
          xmlStr += tagStart + `>${tagValue}${indentation}</${tagName}>`;
        } else {
          xmlStr += tagStart + ">";
          if (tagValue && indentation !== "" && (tagValue.includes("/>") || tagValue.includes("</"))) {
            xmlStr += indentation + options.indentBy + tagValue + indentation;
          } else {
            xmlStr += tagValue;
          }
          xmlStr += `</${tagName}>`;
        }
        isPreviousElementTag = true;
      }
      return xmlStr;
    }
    function propName(obj) {
      const keys2 = Object.keys(obj);
      for (let i2 = 0; i2 < keys2.length; i2++) {
        const key2 = keys2[i2];
        if (!obj.hasOwnProperty(key2)) continue;
        if (key2 !== ":@") return key2;
      }
    }
    function attr_to_str(attrMap, options) {
      let attrStr = "";
      if (attrMap && !options.ignoreAttributes) {
        for (let attr in attrMap) {
          if (!attrMap.hasOwnProperty(attr)) continue;
          let attrVal = options.attributeValueProcessor(attr, attrMap[attr]);
          attrVal = replaceEntitiesValue(attrVal, options);
          if (attrVal === true && options.suppressBooleanAttributes) {
            attrStr += ` ${attr.substr(options.attributeNamePrefix.length)}`;
          } else {
            attrStr += ` ${attr.substr(options.attributeNamePrefix.length)}="${attrVal}"`;
          }
        }
      }
      return attrStr;
    }
    function isStopNode(jPath, options) {
      jPath = jPath.substr(0, jPath.length - options.textNodeName.length - 1);
      let tagName = jPath.substr(jPath.lastIndexOf(".") + 1);
      for (let index in options.stopNodes) {
        if (options.stopNodes[index] === jPath || options.stopNodes[index] === "*." + tagName) return true;
      }
      return false;
    }
    function replaceEntitiesValue(textValue, options) {
      if (textValue && textValue.length > 0 && options.processEntities) {
        for (let i2 = 0; i2 < options.entities.length; i2++) {
          const entity = options.entities[i2];
          textValue = textValue.replace(entity.regex, entity.val);
        }
      }
      return textValue;
    }
    module.exports = toXml;
  }
});

// node_modules/fast-xml-parser/src/xmlbuilder/json2xml.js
var require_json2xml = __commonJS({
  "node_modules/fast-xml-parser/src/xmlbuilder/json2xml.js"(exports, module) {
    "use strict";
    var buildFromOrderedJs = require_orderedJs2Xml();
    var getIgnoreAttributesFn = require_ignoreAttributes();
    var defaultOptions = {
      attributeNamePrefix: "@_",
      attributesGroupName: false,
      textNodeName: "#text",
      ignoreAttributes: true,
      cdataPropName: false,
      format: false,
      indentBy: "  ",
      suppressEmptyNode: false,
      suppressUnpairedNode: true,
      suppressBooleanAttributes: true,
      tagValueProcessor: function(key2, a2) {
        return a2;
      },
      attributeValueProcessor: function(attrName, a2) {
        return a2;
      },
      preserveOrder: false,
      commentPropName: false,
      unpairedTags: [],
      entities: [
        { regex: new RegExp("&", "g"), val: "&amp;" },
        //it must be on top
        { regex: new RegExp(">", "g"), val: "&gt;" },
        { regex: new RegExp("<", "g"), val: "&lt;" },
        { regex: new RegExp("'", "g"), val: "&apos;" },
        { regex: new RegExp('"', "g"), val: "&quot;" }
      ],
      processEntities: true,
      stopNodes: [],
      // transformTagName: false,
      // transformAttributeName: false,
      oneListGroup: false
    };
    function Builder(options) {
      this.options = Object.assign({}, defaultOptions, options);
      if (this.options.ignoreAttributes === true || this.options.attributesGroupName) {
        this.isAttribute = function() {
          return false;
        };
      } else {
        this.ignoreAttributesFn = getIgnoreAttributesFn(this.options.ignoreAttributes);
        this.attrPrefixLen = this.options.attributeNamePrefix.length;
        this.isAttribute = isAttribute;
      }
      this.processTextOrObjNode = processTextOrObjNode;
      if (this.options.format) {
        this.indentate = indentate;
        this.tagEndChar = ">\n";
        this.newLine = "\n";
      } else {
        this.indentate = function() {
          return "";
        };
        this.tagEndChar = ">";
        this.newLine = "";
      }
    }
    Builder.prototype.build = function(jObj) {
      if (this.options.preserveOrder) {
        return buildFromOrderedJs(jObj, this.options);
      } else {
        if (Array.isArray(jObj) && this.options.arrayNodeName && this.options.arrayNodeName.length > 1) {
          jObj = {
            [this.options.arrayNodeName]: jObj
          };
        }
        return this.j2x(jObj, 0, []).val;
      }
    };
    Builder.prototype.j2x = function(jObj, level, ajPath) {
      let attrStr = "";
      let val = "";
      const jPath = ajPath.join(".");
      for (let key2 in jObj) {
        if (!Object.prototype.hasOwnProperty.call(jObj, key2)) continue;
        if (typeof jObj[key2] === "undefined") {
          if (this.isAttribute(key2)) {
            val += "";
          }
        } else if (jObj[key2] === null) {
          if (this.isAttribute(key2)) {
            val += "";
          } else if (key2 === this.options.cdataPropName) {
            val += "";
          } else if (key2[0] === "?") {
            val += this.indentate(level) + "<" + key2 + "?" + this.tagEndChar;
          } else {
            val += this.indentate(level) + "<" + key2 + "/" + this.tagEndChar;
          }
        } else if (jObj[key2] instanceof Date) {
          val += this.buildTextValNode(jObj[key2], key2, "", level);
        } else if (typeof jObj[key2] !== "object") {
          const attr = this.isAttribute(key2);
          if (attr && !this.ignoreAttributesFn(attr, jPath)) {
            attrStr += this.buildAttrPairStr(attr, "" + jObj[key2]);
          } else if (!attr) {
            if (key2 === this.options.textNodeName) {
              let newval = this.options.tagValueProcessor(key2, "" + jObj[key2]);
              val += this.replaceEntitiesValue(newval);
            } else {
              val += this.buildTextValNode(jObj[key2], key2, "", level);
            }
          }
        } else if (Array.isArray(jObj[key2])) {
          const arrLen = jObj[key2].length;
          let listTagVal = "";
          let listTagAttr = "";
          for (let j2 = 0; j2 < arrLen; j2++) {
            const item = jObj[key2][j2];
            if (typeof item === "undefined") {
            } else if (item === null) {
              if (key2[0] === "?") val += this.indentate(level) + "<" + key2 + "?" + this.tagEndChar;
              else val += this.indentate(level) + "<" + key2 + "/" + this.tagEndChar;
            } else if (typeof item === "object") {
              if (this.options.oneListGroup) {
                const result = this.j2x(item, level + 1, ajPath.concat(key2));
                listTagVal += result.val;
                if (this.options.attributesGroupName && item.hasOwnProperty(this.options.attributesGroupName)) {
                  listTagAttr += result.attrStr;
                }
              } else {
                listTagVal += this.processTextOrObjNode(item, key2, level, ajPath);
              }
            } else {
              if (this.options.oneListGroup) {
                let textValue = this.options.tagValueProcessor(key2, item);
                textValue = this.replaceEntitiesValue(textValue);
                listTagVal += textValue;
              } else {
                listTagVal += this.buildTextValNode(item, key2, "", level);
              }
            }
          }
          if (this.options.oneListGroup) {
            listTagVal = this.buildObjectNode(listTagVal, key2, listTagAttr, level);
          }
          val += listTagVal;
        } else {
          if (this.options.attributesGroupName && key2 === this.options.attributesGroupName) {
            const Ks = Object.keys(jObj[key2]);
            const L2 = Ks.length;
            for (let j2 = 0; j2 < L2; j2++) {
              attrStr += this.buildAttrPairStr(Ks[j2], "" + jObj[key2][Ks[j2]]);
            }
          } else {
            val += this.processTextOrObjNode(jObj[key2], key2, level, ajPath);
          }
        }
      }
      return { attrStr, val };
    };
    Builder.prototype.buildAttrPairStr = function(attrName, val) {
      val = this.options.attributeValueProcessor(attrName, "" + val);
      val = this.replaceEntitiesValue(val);
      if (this.options.suppressBooleanAttributes && val === "true") {
        return " " + attrName;
      } else return " " + attrName + '="' + val + '"';
    };
    function processTextOrObjNode(object2, key2, level, ajPath) {
      const result = this.j2x(object2, level + 1, ajPath.concat(key2));
      if (object2[this.options.textNodeName] !== void 0 && Object.keys(object2).length === 1) {
        return this.buildTextValNode(object2[this.options.textNodeName], key2, result.attrStr, level);
      } else {
        return this.buildObjectNode(result.val, key2, result.attrStr, level);
      }
    }
    Builder.prototype.buildObjectNode = function(val, key2, attrStr, level) {
      if (val === "") {
        if (key2[0] === "?") return this.indentate(level) + "<" + key2 + attrStr + "?" + this.tagEndChar;
        else {
          return this.indentate(level) + "<" + key2 + attrStr + this.closeTag(key2) + this.tagEndChar;
        }
      } else {
        let tagEndExp = "</" + key2 + this.tagEndChar;
        let piClosingChar = "";
        if (key2[0] === "?") {
          piClosingChar = "?";
          tagEndExp = "";
        }
        if ((attrStr || attrStr === "") && val.indexOf("<") === -1) {
          return this.indentate(level) + "<" + key2 + attrStr + piClosingChar + ">" + val + tagEndExp;
        } else if (this.options.commentPropName !== false && key2 === this.options.commentPropName && piClosingChar.length === 0) {
          return this.indentate(level) + `<!--${val}-->` + this.newLine;
        } else {
          return this.indentate(level) + "<" + key2 + attrStr + piClosingChar + this.tagEndChar + val + this.indentate(level) + tagEndExp;
        }
      }
    };
    Builder.prototype.closeTag = function(key2) {
      let closeTag = "";
      if (this.options.unpairedTags.indexOf(key2) !== -1) {
        if (!this.options.suppressUnpairedNode) closeTag = "/";
      } else if (this.options.suppressEmptyNode) {
        closeTag = "/";
      } else {
        closeTag = `></${key2}`;
      }
      return closeTag;
    };
    Builder.prototype.buildTextValNode = function(val, key2, attrStr, level) {
      if (this.options.cdataPropName !== false && key2 === this.options.cdataPropName) {
        return this.indentate(level) + `<![CDATA[${val}]]>` + this.newLine;
      } else if (this.options.commentPropName !== false && key2 === this.options.commentPropName) {
        return this.indentate(level) + `<!--${val}-->` + this.newLine;
      } else if (key2[0] === "?") {
        return this.indentate(level) + "<" + key2 + attrStr + "?" + this.tagEndChar;
      } else {
        let textValue = this.options.tagValueProcessor(key2, val);
        textValue = this.replaceEntitiesValue(textValue);
        if (textValue === "") {
          return this.indentate(level) + "<" + key2 + attrStr + this.closeTag(key2) + this.tagEndChar;
        } else {
          return this.indentate(level) + "<" + key2 + attrStr + ">" + textValue + "</" + key2 + this.tagEndChar;
        }
      }
    };
    Builder.prototype.replaceEntitiesValue = function(textValue) {
      if (textValue && textValue.length > 0 && this.options.processEntities) {
        for (let i2 = 0; i2 < this.options.entities.length; i2++) {
          const entity = this.options.entities[i2];
          textValue = textValue.replace(entity.regex, entity.val);
        }
      }
      return textValue;
    };
    function indentate(level) {
      return this.options.indentBy.repeat(level);
    }
    function isAttribute(name) {
      if (name.startsWith(this.options.attributeNamePrefix) && name !== this.options.textNodeName) {
        return name.substr(this.attrPrefixLen);
      } else {
        return false;
      }
    }
    module.exports = Builder;
  }
});

// node_modules/fast-xml-parser/src/fxp.js
var require_fxp = __commonJS({
  "node_modules/fast-xml-parser/src/fxp.js"(exports, module) {
    "use strict";
    var validator = require_validator();
    var XMLParser = require_XMLParser();
    var XMLBuilder2 = require_json2xml();
    module.exports = {
      XMLParser,
      XMLValidator: validator,
      XMLBuilder: XMLBuilder2
    };
  }
});

// node_modules/openapi-sampler/src/openapi-sampler.js
var openapi_sampler_exports = {};
__export(openapi_sampler_exports, {
  _registerSampler: () => _registerSampler,
  _samplers: () => _samplers,
  inferType: () => inferType,
  sample: () => sample
});
function convertJsonToXml(obj, schema4) {
  if (!obj) {
    throw new Error("Unknown format output for building XML.");
  }
  if (Array.isArray(obj) || Object.keys(obj).length > 1) {
    obj = { [schema4?.xml?.name || "root"]: obj };
  }
  const builder = new import_fast_xml_parser.XMLBuilder({
    ignoreAttributes: false,
    format: true,
    attributeNamePrefix: "$",
    textNodeName: "#text"
  });
  return builder.build(obj);
}
function sample(schema4, options, spec) {
  let opts = Object.assign({}, defaults, options);
  clearCache();
  let result = traverse(schema4, opts, spec).value;
  if (opts?.format === "xml") {
    return convertJsonToXml(result, schema4);
  }
  return result;
}
function _registerSampler(type, sampler) {
  _samplers[type] = sampler;
}
var import_fast_xml_parser, _samplers, defaults;
var init_openapi_sampler = __esm({
  "node_modules/openapi-sampler/src/openapi-sampler.js"() {
    init_traverse();
    init_samplers();
    import_fast_xml_parser = __toESM(require_fxp());
    init_infer();
    _samplers = {};
    defaults = {
      skipReadOnly: false,
      maxSampleDepth: 15
    };
    _registerSampler("array", sampleArray);
    _registerSampler("boolean", sampleBoolean);
    _registerSampler("integer", sampleNumber);
    _registerSampler("number", sampleNumber);
    _registerSampler("object", sampleObject);
    _registerSampler("string", sampleString);
  }
});

// node_modules/react-tabs/esm/helpers/elementTypes.js
function makeTypeChecker(tabsRole) {
  return (element) => !!element.type && element.type.tabsRole === tabsRole;
}
var isTab, isTabList, isTabPanel;
var init_elementTypes = __esm({
  "node_modules/react-tabs/esm/helpers/elementTypes.js"() {
    isTab = makeTypeChecker("Tab");
    isTabList = makeTypeChecker("TabList");
    isTabPanel = makeTypeChecker("TabPanel");
  }
});

// node_modules/react-tabs/esm/helpers/childrenDeepMap.js
function isTabChild(child) {
  return isTab(child) || isTabList(child) || isTabPanel(child);
}
function deepMap(children, callback) {
  return import_react2.Children.map(children, (child) => {
    if (child === null) return null;
    if (isTabChild(child)) {
      return callback(child);
    }
    if (child.props && child.props.children && typeof child.props.children === "object") {
      return (0, import_react2.cloneElement)(child, Object.assign({}, child.props, { children: deepMap(child.props.children, callback) }));
    }
    return child;
  });
}
function deepForEach(children, callback) {
  return import_react2.Children.forEach(children, (child) => {
    if (child === null) return;
    if (isTab(child) || isTabPanel(child)) {
      callback(child);
    } else if (child.props && child.props.children && typeof child.props.children === "object") {
      if (isTabList(child)) callback(child);
      deepForEach(child.props.children, callback);
    }
  });
}
var import_react2;
var init_childrenDeepMap = __esm({
  "node_modules/react-tabs/esm/helpers/childrenDeepMap.js"() {
    import_react2 = __toESM(require_react());
    init_elementTypes();
  }
});

// node_modules/react-tabs/esm/helpers/propTypes.js
function childrenPropType(props, propName, componentName) {
  let error;
  let tabsCount = 0;
  let panelsCount = 0;
  let tabListFound = false;
  const listTabs = [];
  const children = props[propName];
  deepForEach(children, (child) => {
    if (isTabList(child)) {
      if (child.props && child.props.children && typeof child.props.children === "object") {
        deepForEach(child.props.children, (listChild) => listTabs.push(listChild));
      }
      if (tabListFound) {
        error = new Error("Found multiple 'TabList' components inside 'Tabs'. Only one is allowed.");
      }
      tabListFound = true;
    }
    if (isTab(child)) {
      if (!tabListFound || listTabs.indexOf(child) === -1) {
        error = new Error("Found a 'Tab' component outside of the 'TabList' component. 'Tab' components have to be inside the 'TabList' component.");
      }
      tabsCount++;
    } else if (isTabPanel(child)) {
      panelsCount++;
    }
  });
  if (!error && tabsCount !== panelsCount) {
    error = new Error(`There should be an equal number of 'Tab' and 'TabPanel' in \`${componentName}\`. Received ${tabsCount} 'Tab' and ${panelsCount} 'TabPanel'.`);
  }
  return error;
}
function onSelectPropType(props, propName, componentName, location, propFullName) {
  const prop = props[propName];
  const name = propFullName || propName;
  let error = null;
  if (prop && typeof prop !== "function") {
    error = new Error(`Invalid ${location} \`${name}\` of type \`${typeof prop}\` supplied to \`${componentName}\`, expected \`function\`.`);
  } else if (props.selectedIndex != null && prop == null) {
    error = new Error(`The ${location} \`${name}\` is marked as required in \`${componentName}\`, but its value is \`undefined\` or \`null\`.
\`onSelect\` is required when \`selectedIndex\` is also set. Not doing so will make the tabs not do anything, as \`selectedIndex\` indicates that you want to handle the selected tab yourself.
If you only want to set the inital tab replace \`selectedIndex\` with \`defaultIndex\`.`);
  }
  return error;
}
function selectedIndexPropType(props, propName, componentName, location, propFullName) {
  const prop = props[propName];
  const name = propFullName || propName;
  let error = null;
  if (prop != null && typeof prop !== "number") {
    error = new Error(`Invalid ${location} \`${name}\` of type \`${typeof prop}\` supplied to \`${componentName}\`, expected \`number\`.`);
  } else if (props.defaultIndex != null && prop != null) {
    return new Error(`The ${location} \`${name}\` cannot be used together with \`defaultIndex\` in \`${componentName}\`.
Either remove \`${name}\` to let \`${componentName}\` handle the selected tab internally or remove \`defaultIndex\` to handle it yourself.`);
  }
  return error;
}
var init_propTypes = __esm({
  "node_modules/react-tabs/esm/helpers/propTypes.js"() {
    init_childrenDeepMap();
    init_elementTypes();
  }
});

// node_modules/react-tabs/esm/helpers/count.js
function getTabsCount(children) {
  let tabCount = 0;
  deepForEach(children, (child) => {
    if (isTab(child)) tabCount++;
  });
  return tabCount;
}
var init_count = __esm({
  "node_modules/react-tabs/esm/helpers/count.js"() {
    init_childrenDeepMap();
    init_elementTypes();
  }
});

// node_modules/react-tabs/esm/components/UncontrolledTabs.js
function _objectWithoutPropertiesLoose(r2, e) {
  if (null == r2) return {};
  var t = {};
  for (var n in r2) if ({}.hasOwnProperty.call(r2, n)) {
    if (e.includes(n)) continue;
    t[n] = r2[n];
  }
  return t;
}
function isNode2(node2) {
  return node2 && "getAttribute" in node2;
}
function isTabNode(node2) {
  return isNode2(node2) && node2.getAttribute("data-rttab");
}
function isTabDisabled(node2) {
  return isNode2(node2) && node2.getAttribute("aria-disabled") === "true";
}
function determineCanUseActiveElement(environment) {
  const env2 = environment || (typeof window !== "undefined" ? window : void 0);
  try {
    canUseActiveElement = !!(typeof env2 !== "undefined" && env2.document && env2.document.activeElement);
  } catch (e) {
    canUseActiveElement = false;
  }
}
var import_prop_types, import_react3, _excluded, canUseActiveElement, defaultProps, propTypes, UncontrolledTabs, UncontrolledTabs_default;
var init_UncontrolledTabs = __esm({
  "node_modules/react-tabs/esm/components/UncontrolledTabs.js"() {
    import_prop_types = __toESM(require_prop_types());
    import_react3 = __toESM(require_react());
    init_clsx();
    init_propTypes();
    init_count();
    init_childrenDeepMap();
    init_elementTypes();
    _excluded = ["children", "className", "disabledTabClassName", "domRef", "focus", "forceRenderTabPanel", "onSelect", "selectedIndex", "selectedTabClassName", "selectedTabPanelClassName", "environment", "disableUpDownKeys", "disableLeftRightKeys"];
    defaultProps = { className: "react-tabs", focus: false };
    propTypes = { children: childrenPropType };
    UncontrolledTabs = (props) => {
      (0, import_prop_types.checkPropTypes)(propTypes, props, "prop", "UncontrolledTabs");
      let tabNodes = (0, import_react3.useRef)([]);
      let tabIds = (0, import_react3.useRef)([]);
      const ref = (0, import_react3.useRef)();
      function setSelected(index, event) {
        if (index < 0 || index >= getTabsCount2()) return;
        const { onSelect, selectedIndex } = props;
        onSelect(index, selectedIndex, event);
      }
      function getNextTab(index) {
        const count = getTabsCount2();
        for (let i2 = index + 1; i2 < count; i2++) {
          if (!isTabDisabled(getTab(i2))) {
            return i2;
          }
        }
        for (let i2 = 0; i2 < index; i2++) {
          if (!isTabDisabled(getTab(i2))) {
            return i2;
          }
        }
        return index;
      }
      function getPrevTab(index) {
        let i2 = index;
        while (i2--) {
          if (!isTabDisabled(getTab(i2))) {
            return i2;
          }
        }
        i2 = getTabsCount2();
        while (i2-- > index) {
          if (!isTabDisabled(getTab(i2))) {
            return i2;
          }
        }
        return index;
      }
      function getFirstTab() {
        const count = getTabsCount2();
        for (let i2 = 0; i2 < count; i2++) {
          if (!isTabDisabled(getTab(i2))) {
            return i2;
          }
        }
        return null;
      }
      function getLastTab() {
        let i2 = getTabsCount2();
        while (i2--) {
          if (!isTabDisabled(getTab(i2))) {
            return i2;
          }
        }
        return null;
      }
      function getTabsCount2() {
        const { children } = props;
        return getTabsCount(children);
      }
      function getTab(index) {
        return tabNodes.current[`tabs-${index}`];
      }
      function getChildren() {
        let index = 0;
        const { children, disabledTabClassName, focus, forceRenderTabPanel, selectedIndex, selectedTabClassName, selectedTabPanelClassName, environment } = props;
        tabIds.current = tabIds.current || [];
        let diff = tabIds.current.length - getTabsCount2();
        const id = (0, import_react3.useId)();
        while (diff++ < 0) {
          tabIds.current.push(`${id}${tabIds.current.length}`);
        }
        return deepMap(children, (child) => {
          let result = child;
          if (isTabList(child)) {
            let listIndex = 0;
            let wasTabFocused = false;
            if (canUseActiveElement == null) {
              determineCanUseActiveElement(environment);
            }
            const env2 = environment || (typeof window !== "undefined" ? window : void 0);
            if (canUseActiveElement && env2) {
              wasTabFocused = import_react3.default.Children.toArray(child.props.children).filter(isTab).some((tab, i2) => env2.document.activeElement === getTab(i2));
            }
            result = (0, import_react3.cloneElement)(child, { children: deepMap(child.props.children, (tab) => {
              const key2 = `tabs-${listIndex}`;
              const selected = selectedIndex === listIndex;
              const props2 = { tabRef: (node2) => {
                tabNodes.current[key2] = node2;
              }, id: tabIds.current[listIndex], selected, focus: selected && (focus || wasTabFocused) };
              if (selectedTabClassName) props2.selectedClassName = selectedTabClassName;
              if (disabledTabClassName) props2.disabledClassName = disabledTabClassName;
              listIndex++;
              return (0, import_react3.cloneElement)(tab, props2);
            }) });
          } else if (isTabPanel(child)) {
            const props2 = { id: tabIds.current[index], selected: selectedIndex === index };
            if (forceRenderTabPanel) props2.forceRender = forceRenderTabPanel;
            if (selectedTabPanelClassName) props2.selectedClassName = selectedTabPanelClassName;
            index++;
            result = (0, import_react3.cloneElement)(child, props2);
          }
          return result;
        });
      }
      function handleKeyDown(e) {
        const { direction, disableUpDownKeys, disableLeftRightKeys } = props;
        if (isTabFromContainer(e.target)) {
          let { selectedIndex: index } = props;
          let preventDefault = false;
          let useSelectedIndex = false;
          if (e.code === "Space" || e.keyCode === 32 || e.code === "Enter" || e.keyCode === 13) {
            preventDefault = true;
            useSelectedIndex = false;
            handleClick(e);
          }
          if (!disableLeftRightKeys && (e.keyCode === 37 || e.code === "ArrowLeft") || !disableUpDownKeys && (e.keyCode === 38 || e.code === "ArrowUp")) {
            if (direction === "rtl") {
              index = getNextTab(index);
            } else {
              index = getPrevTab(index);
            }
            preventDefault = true;
            useSelectedIndex = true;
          } else if (!disableLeftRightKeys && (e.keyCode === 39 || e.code === "ArrowRight") || !disableUpDownKeys && (e.keyCode === 40 || e.code === "ArrowDown")) {
            if (direction === "rtl") {
              index = getPrevTab(index);
            } else {
              index = getNextTab(index);
            }
            preventDefault = true;
            useSelectedIndex = true;
          } else if (e.keyCode === 35 || e.code === "End") {
            index = getLastTab();
            preventDefault = true;
            useSelectedIndex = true;
          } else if (e.keyCode === 36 || e.code === "Home") {
            index = getFirstTab();
            preventDefault = true;
            useSelectedIndex = true;
          }
          if (preventDefault) {
            e.preventDefault();
          }
          if (useSelectedIndex) {
            setSelected(index, e);
          }
        }
      }
      function handleClick(e) {
        let node2 = e.target;
        do {
          if (isTabFromContainer(node2)) {
            if (isTabDisabled(node2)) {
              return;
            }
            const index = [].slice.call(node2.parentNode.children).filter(isTabNode).indexOf(node2);
            setSelected(index, e);
            return;
          }
        } while ((node2 = node2.parentNode) != null);
      }
      function isTabFromContainer(node2) {
        if (!isTabNode(node2)) {
          return false;
        }
        let nodeAncestor = node2.parentElement;
        do {
          if (nodeAncestor === ref.current) return true;
          if (nodeAncestor.getAttribute("data-rttabs")) break;
          nodeAncestor = nodeAncestor.parentElement;
        } while (nodeAncestor);
        return false;
      }
      const _defaultProps$props = Object.assign({}, defaultProps, props), { className, domRef } = _defaultProps$props, attributes = _objectWithoutPropertiesLoose(_defaultProps$props, _excluded);
      return import_react3.default.createElement("div", Object.assign({}, attributes, { className: clsx_default(className), onClick: handleClick, onKeyDown: handleKeyDown, ref: (node2) => {
        ref.current = node2;
        if (domRef) domRef(node2);
      }, "data-rttabs": true }), getChildren());
    };
    UncontrolledTabs_default = UncontrolledTabs;
  }
});

// node_modules/react-tabs/esm/components/Tabs.js
function _objectWithoutPropertiesLoose2(r2, e) {
  if (null == r2) return {};
  var t = {};
  for (var n in r2) if ({}.hasOwnProperty.call(r2, n)) {
    if (e.includes(n)) continue;
    t[n] = r2[n];
  }
  return t;
}
var import_prop_types2, import_react4, _excluded2, MODE_CONTROLLED, MODE_UNCONTROLLED, propTypes2, defaultProps2, getModeFromProps, checkForIllegalModeChange, Tabs, Tabs_default;
var init_Tabs = __esm({
  "node_modules/react-tabs/esm/components/Tabs.js"() {
    import_prop_types2 = __toESM(require_prop_types());
    import_react4 = __toESM(require_react());
    init_propTypes();
    init_UncontrolledTabs();
    init_count();
    _excluded2 = ["children", "defaultFocus", "defaultIndex", "focusTabOnClick", "onSelect"];
    MODE_CONTROLLED = 0;
    MODE_UNCONTROLLED = 1;
    propTypes2 = { children: childrenPropType, onSelect: onSelectPropType, selectedIndex: selectedIndexPropType };
    defaultProps2 = { defaultFocus: false, focusTabOnClick: true, forceRenderTabPanel: false, selectedIndex: null, defaultIndex: null, environment: null, disableUpDownKeys: false, disableLeftRightKeys: false };
    getModeFromProps = (props) => {
      return props.selectedIndex === null ? MODE_UNCONTROLLED : MODE_CONTROLLED;
    };
    checkForIllegalModeChange = (props, mode) => {
      if (mode != void 0 && mode !== getModeFromProps(props)) {
        throw new Error(`Switching between controlled mode (by using \`selectedIndex\`) and uncontrolled mode is not supported in \`Tabs\`.
For more information about controlled and uncontrolled mode of react-tabs see https://github.com/reactjs/react-tabs#controlled-vs-uncontrolled-mode.`);
      }
    };
    Tabs = (props) => {
      (0, import_prop_types2.checkPropTypes)(propTypes2, props, "prop", "Tabs");
      const _defaultProps$props = Object.assign({}, defaultProps2, props), { children, defaultFocus, defaultIndex, focusTabOnClick, onSelect } = _defaultProps$props, attributes = _objectWithoutPropertiesLoose2(_defaultProps$props, _excluded2);
      const [focus, setFocus] = (0, import_react4.useState)(defaultFocus);
      const [mode] = (0, import_react4.useState)(getModeFromProps(attributes));
      const [selectedIndex, setSelectedIndex] = (0, import_react4.useState)(mode === MODE_UNCONTROLLED ? defaultIndex || 0 : null);
      (0, import_react4.useEffect)(() => {
        setFocus(false);
      }, []);
      if (mode === MODE_UNCONTROLLED) {
        const tabsCount = getTabsCount(children);
        (0, import_react4.useEffect)(() => {
          if (selectedIndex != null) {
            const maxTabIndex = Math.max(0, tabsCount - 1);
            setSelectedIndex(Math.min(selectedIndex, maxTabIndex));
          }
        }, [tabsCount]);
      }
      checkForIllegalModeChange(attributes, mode);
      const handleSelected = (index, last2, event) => {
        if (typeof onSelect === "function") {
          if (onSelect(index, last2, event) === false) return;
        }
        if (focusTabOnClick) {
          setFocus(true);
        }
        if (mode === MODE_UNCONTROLLED) {
          setSelectedIndex(index);
        }
      };
      let subProps = Object.assign({}, props, attributes);
      subProps.focus = focus;
      subProps.onSelect = handleSelected;
      if (selectedIndex != null) {
        subProps.selectedIndex = selectedIndex;
      }
      delete subProps.defaultFocus;
      delete subProps.defaultIndex;
      delete subProps.focusTabOnClick;
      return import_react4.default.createElement(UncontrolledTabs_default, subProps, children);
    };
    Tabs.tabsRole = "Tabs";
    Tabs_default = Tabs;
  }
});

// node_modules/react-tabs/esm/components/TabList.js
function _objectWithoutPropertiesLoose3(r2, e) {
  if (null == r2) return {};
  var t = {};
  for (var n in r2) if ({}.hasOwnProperty.call(r2, n)) {
    if (e.includes(n)) continue;
    t[n] = r2[n];
  }
  return t;
}
var import_react5, _excluded3, defaultProps3, TabList, TabList_default;
var init_TabList = __esm({
  "node_modules/react-tabs/esm/components/TabList.js"() {
    import_react5 = __toESM(require_react());
    init_clsx();
    _excluded3 = ["children", "className"];
    defaultProps3 = { className: "react-tabs__tab-list" };
    TabList = (props) => {
      const _defaultProps$props = Object.assign({}, defaultProps3, props), { children, className } = _defaultProps$props, attributes = _objectWithoutPropertiesLoose3(_defaultProps$props, _excluded3);
      return import_react5.default.createElement("ul", Object.assign({}, attributes, { className: clsx_default(className), role: "tablist" }), children);
    };
    TabList.tabsRole = "TabList";
    TabList_default = TabList;
  }
});

// node_modules/react-tabs/esm/components/Tab.js
function _objectWithoutPropertiesLoose4(r2, e) {
  if (null == r2) return {};
  var t = {};
  for (var n in r2) if ({}.hasOwnProperty.call(r2, n)) {
    if (e.includes(n)) continue;
    t[n] = r2[n];
  }
  return t;
}
var import_react6, _excluded4, DEFAULT_CLASS, defaultProps4, Tab, Tab_default;
var init_Tab = __esm({
  "node_modules/react-tabs/esm/components/Tab.js"() {
    import_react6 = __toESM(require_react());
    init_clsx();
    _excluded4 = ["children", "className", "disabled", "disabledClassName", "focus", "id", "selected", "selectedClassName", "tabIndex", "tabRef"];
    DEFAULT_CLASS = "react-tabs__tab";
    defaultProps4 = { className: DEFAULT_CLASS, disabledClassName: `${DEFAULT_CLASS}--disabled`, focus: false, id: null, selected: false, selectedClassName: `${DEFAULT_CLASS}--selected` };
    Tab = (props) => {
      let nodeRef = (0, import_react6.useRef)();
      const _defaultProps$props = Object.assign({}, defaultProps4, props), { children, className, disabled, disabledClassName, focus, id, selected, selectedClassName, tabIndex, tabRef } = _defaultProps$props, attributes = _objectWithoutPropertiesLoose4(_defaultProps$props, _excluded4);
      (0, import_react6.useEffect)(() => {
        if (selected && focus) {
          nodeRef.current.focus();
        }
      }, [selected, focus]);
      return import_react6.default.createElement("li", Object.assign({}, attributes, { className: clsx_default(className, { [selectedClassName]: selected, [disabledClassName]: disabled }), ref: (node2) => {
        nodeRef.current = node2;
        if (tabRef) tabRef(node2);
      }, role: "tab", id: `tab${id}`, "aria-selected": selected ? "true" : "false", "aria-disabled": disabled ? "true" : "false", "aria-controls": `panel${id}`, tabIndex: tabIndex || (selected ? "0" : null), "data-rttab": true }), children);
    };
    Tab.tabsRole = "Tab";
    Tab_default = Tab;
  }
});

// node_modules/react-tabs/esm/components/TabPanel.js
function _objectWithoutPropertiesLoose5(r2, e) {
  if (null == r2) return {};
  var t = {};
  for (var n in r2) if ({}.hasOwnProperty.call(r2, n)) {
    if (e.includes(n)) continue;
    t[n] = r2[n];
  }
  return t;
}
var import_react7, _excluded5, DEFAULT_CLASS2, defaultProps5, TabPanel, TabPanel_default;
var init_TabPanel = __esm({
  "node_modules/react-tabs/esm/components/TabPanel.js"() {
    import_react7 = __toESM(require_react());
    init_clsx();
    _excluded5 = ["children", "className", "forceRender", "id", "selected", "selectedClassName"];
    DEFAULT_CLASS2 = "react-tabs__tab-panel";
    defaultProps5 = { className: DEFAULT_CLASS2, forceRender: false, selectedClassName: `${DEFAULT_CLASS2}--selected` };
    TabPanel = (props) => {
      const _defaultProps$props = Object.assign({}, defaultProps5, props), { children, className, forceRender, id, selected, selectedClassName } = _defaultProps$props, attributes = _objectWithoutPropertiesLoose5(_defaultProps$props, _excluded5);
      return import_react7.default.createElement("div", Object.assign({}, attributes, { className: clsx_default(className, { [selectedClassName]: selected }), role: "tabpanel", id: `panel${id}`, "aria-labelledby": `tab${id}` }), forceRender || selected ? children : null);
    };
    TabPanel.tabsRole = "TabPanel";
    TabPanel_default = TabPanel;
  }
});

// node_modules/react-tabs/esm/index.js
var esm_exports = {};
__export(esm_exports, {
  Tab: () => Tab_default,
  TabList: () => TabList_default,
  TabPanel: () => TabPanel_default,
  Tabs: () => Tabs_default
});
var init_esm = __esm({
  "node_modules/react-tabs/esm/index.js"() {
    init_Tabs();
    init_TabList();
    init_Tab();
    init_TabPanel();
  }
});

// node_modules/perfect-scrollbar/dist/perfect-scrollbar.esm.js
var perfect_scrollbar_esm_exports = {};
__export(perfect_scrollbar_esm_exports, {
  default: () => perfect_scrollbar_esm_default
});
function get4(element) {
  return getComputedStyle(element);
}
function set6(element, obj) {
  for (var key2 in obj) {
    var val = obj[key2];
    if (typeof val === "number") {
      val = val + "px";
    }
    element.style[key2] = val;
  }
  return element;
}
function div(className) {
  var div2 = document.createElement("div");
  div2.className = className;
  return div2;
}
function matches(element, query) {
  if (!elMatches) {
    throw new Error("No element matching method supported");
  }
  return elMatches.call(element, query);
}
function remove3(element) {
  if (element.remove) {
    element.remove();
  } else {
    if (element.parentNode) {
      element.parentNode.removeChild(element);
    }
  }
}
function queryChildren(element, selector) {
  return Array.prototype.filter.call(
    element.children,
    function(child) {
      return matches(child, selector);
    }
  );
}
function addScrollingClass(i2, x2) {
  var classList = i2.element.classList;
  var className = cls.state.scrolling(x2);
  if (classList.contains(className)) {
    clearTimeout(scrollingClassTimeout[x2]);
  } else {
    classList.add(className);
  }
}
function removeScrollingClass(i2, x2) {
  scrollingClassTimeout[x2] = setTimeout(
    function() {
      return i2.isAlive && i2.element.classList.remove(cls.state.scrolling(x2));
    },
    i2.settings.scrollingThreshold
  );
}
function setScrollingClassInstantly(i2, x2) {
  addScrollingClass(i2, x2);
  removeScrollingClass(i2, x2);
}
function createEvent(name) {
  if (typeof window.CustomEvent === "function") {
    return new CustomEvent(name);
  }
  var evt = document.createEvent("CustomEvent");
  evt.initCustomEvent(name, false, false, void 0);
  return evt;
}
function processScrollDiff(i2, axis, diff, useScrollingClass, forceFireReachEvent) {
  if (useScrollingClass === void 0) useScrollingClass = true;
  if (forceFireReachEvent === void 0) forceFireReachEvent = false;
  var fields;
  if (axis === "top") {
    fields = ["contentHeight", "containerHeight", "scrollTop", "y", "up", "down"];
  } else if (axis === "left") {
    fields = ["contentWidth", "containerWidth", "scrollLeft", "x", "left", "right"];
  } else {
    throw new Error("A proper axis should be provided");
  }
  processScrollDiff$1(i2, diff, fields, useScrollingClass, forceFireReachEvent);
}
function processScrollDiff$1(i2, diff, ref, useScrollingClass, forceFireReachEvent) {
  var contentHeight = ref[0];
  var containerHeight = ref[1];
  var scrollTop = ref[2];
  var y2 = ref[3];
  var up = ref[4];
  var down = ref[5];
  if (useScrollingClass === void 0) useScrollingClass = true;
  if (forceFireReachEvent === void 0) forceFireReachEvent = false;
  var element = i2.element;
  i2.reach[y2] = null;
  if (element[scrollTop] < 1) {
    i2.reach[y2] = "start";
  }
  if (element[scrollTop] > i2[contentHeight] - i2[containerHeight] - 1) {
    i2.reach[y2] = "end";
  }
  if (diff) {
    element.dispatchEvent(createEvent("ps-scroll-" + y2));
    if (diff < 0) {
      element.dispatchEvent(createEvent("ps-scroll-" + up));
    } else if (diff > 0) {
      element.dispatchEvent(createEvent("ps-scroll-" + down));
    }
    if (useScrollingClass) {
      setScrollingClassInstantly(i2, y2);
    }
  }
  if (i2.reach[y2] && (diff || forceFireReachEvent)) {
    element.dispatchEvent(createEvent("ps-" + y2 + "-reach-" + i2.reach[y2]));
  }
}
function toInt(x2) {
  return parseInt(x2, 10) || 0;
}
function isEditable(el) {
  return matches(el, "input,[contenteditable]") || matches(el, "select,[contenteditable]") || matches(el, "textarea,[contenteditable]") || matches(el, "button,[contenteditable]");
}
function outerWidth(element) {
  var styles = get4(element);
  return toInt(styles.width) + toInt(styles.paddingLeft) + toInt(styles.paddingRight) + toInt(styles.borderLeftWidth) + toInt(styles.borderRightWidth);
}
function updateGeometry(i2) {
  var element = i2.element;
  var roundedScrollTop = Math.floor(element.scrollTop);
  var rect = element.getBoundingClientRect();
  i2.containerWidth = Math.floor(rect.width);
  i2.containerHeight = Math.floor(rect.height);
  i2.contentWidth = element.scrollWidth;
  i2.contentHeight = element.scrollHeight;
  if (!element.contains(i2.scrollbarXRail)) {
    queryChildren(element, cls.element.rail("x")).forEach(function(el) {
      return remove3(el);
    });
    element.appendChild(i2.scrollbarXRail);
  }
  if (!element.contains(i2.scrollbarYRail)) {
    queryChildren(element, cls.element.rail("y")).forEach(function(el) {
      return remove3(el);
    });
    element.appendChild(i2.scrollbarYRail);
  }
  if (!i2.settings.suppressScrollX && i2.containerWidth + i2.settings.scrollXMarginOffset < i2.contentWidth) {
    i2.scrollbarXActive = true;
    i2.railXWidth = i2.containerWidth - i2.railXMarginWidth;
    i2.railXRatio = i2.containerWidth / i2.railXWidth;
    i2.scrollbarXWidth = getThumbSize(i2, toInt(i2.railXWidth * i2.containerWidth / i2.contentWidth));
    i2.scrollbarXLeft = toInt(
      (i2.negativeScrollAdjustment + element.scrollLeft) * (i2.railXWidth - i2.scrollbarXWidth) / (i2.contentWidth - i2.containerWidth)
    );
  } else {
    i2.scrollbarXActive = false;
  }
  if (!i2.settings.suppressScrollY && i2.containerHeight + i2.settings.scrollYMarginOffset < i2.contentHeight) {
    i2.scrollbarYActive = true;
    i2.railYHeight = i2.containerHeight - i2.railYMarginHeight;
    i2.railYRatio = i2.containerHeight / i2.railYHeight;
    i2.scrollbarYHeight = getThumbSize(
      i2,
      toInt(i2.railYHeight * i2.containerHeight / i2.contentHeight)
    );
    i2.scrollbarYTop = toInt(
      roundedScrollTop * (i2.railYHeight - i2.scrollbarYHeight) / (i2.contentHeight - i2.containerHeight)
    );
  } else {
    i2.scrollbarYActive = false;
  }
  if (i2.scrollbarXLeft >= i2.railXWidth - i2.scrollbarXWidth) {
    i2.scrollbarXLeft = i2.railXWidth - i2.scrollbarXWidth;
  }
  if (i2.scrollbarYTop >= i2.railYHeight - i2.scrollbarYHeight) {
    i2.scrollbarYTop = i2.railYHeight - i2.scrollbarYHeight;
  }
  updateCss(element, i2);
  if (i2.scrollbarXActive) {
    element.classList.add(cls.state.active("x"));
  } else {
    element.classList.remove(cls.state.active("x"));
    i2.scrollbarXWidth = 0;
    i2.scrollbarXLeft = 0;
    element.scrollLeft = i2.isRtl === true ? i2.contentWidth : 0;
  }
  if (i2.scrollbarYActive) {
    element.classList.add(cls.state.active("y"));
  } else {
    element.classList.remove(cls.state.active("y"));
    i2.scrollbarYHeight = 0;
    i2.scrollbarYTop = 0;
    element.scrollTop = 0;
  }
}
function getThumbSize(i2, thumbSize) {
  if (i2.settings.minScrollbarLength) {
    thumbSize = Math.max(thumbSize, i2.settings.minScrollbarLength);
  }
  if (i2.settings.maxScrollbarLength) {
    thumbSize = Math.min(thumbSize, i2.settings.maxScrollbarLength);
  }
  return thumbSize;
}
function updateCss(element, i2) {
  var xRailOffset = { width: i2.railXWidth };
  var roundedScrollTop = Math.floor(element.scrollTop);
  if (i2.isRtl) {
    xRailOffset.left = i2.negativeScrollAdjustment + element.scrollLeft + i2.containerWidth - i2.contentWidth;
  } else {
    xRailOffset.left = element.scrollLeft;
  }
  if (i2.isScrollbarXUsingBottom) {
    xRailOffset.bottom = i2.scrollbarXBottom - roundedScrollTop;
  } else {
    xRailOffset.top = i2.scrollbarXTop + roundedScrollTop;
  }
  set6(i2.scrollbarXRail, xRailOffset);
  var yRailOffset = { top: roundedScrollTop, height: i2.railYHeight };
  if (i2.isScrollbarYUsingRight) {
    if (i2.isRtl) {
      yRailOffset.right = i2.contentWidth - (i2.negativeScrollAdjustment + element.scrollLeft) - i2.scrollbarYRight - i2.scrollbarYOuterWidth - 9;
    } else {
      yRailOffset.right = i2.scrollbarYRight - element.scrollLeft;
    }
  } else {
    if (i2.isRtl) {
      yRailOffset.left = i2.negativeScrollAdjustment + element.scrollLeft + i2.containerWidth * 2 - i2.contentWidth - i2.scrollbarYLeft - i2.scrollbarYOuterWidth;
    } else {
      yRailOffset.left = i2.scrollbarYLeft + element.scrollLeft;
    }
  }
  set6(i2.scrollbarYRail, yRailOffset);
  set6(i2.scrollbarX, {
    left: i2.scrollbarXLeft,
    width: i2.scrollbarXWidth - i2.railBorderXWidth
  });
  set6(i2.scrollbarY, {
    top: i2.scrollbarYTop,
    height: i2.scrollbarYHeight - i2.railBorderYWidth
  });
}
function clickRail(i2) {
  i2.event.bind(i2.scrollbarY, "mousedown", function(e) {
    return e.stopPropagation();
  });
  i2.event.bind(i2.scrollbarYRail, "mousedown", function(e) {
    var positionTop = e.pageY - window.pageYOffset - i2.scrollbarYRail.getBoundingClientRect().top;
    var direction = positionTop > i2.scrollbarYTop ? 1 : -1;
    i2.element.scrollTop += direction * i2.containerHeight;
    updateGeometry(i2);
    e.stopPropagation();
  });
  i2.event.bind(i2.scrollbarX, "mousedown", function(e) {
    return e.stopPropagation();
  });
  i2.event.bind(i2.scrollbarXRail, "mousedown", function(e) {
    var positionLeft = e.pageX - window.pageXOffset - i2.scrollbarXRail.getBoundingClientRect().left;
    var direction = positionLeft > i2.scrollbarXLeft ? 1 : -1;
    i2.element.scrollLeft += direction * i2.containerWidth;
    updateGeometry(i2);
    e.stopPropagation();
  });
}
function setupScrollHandlers(i2) {
  bindMouseScrollHandler(i2, [
    "containerHeight",
    "contentHeight",
    "pageY",
    "railYHeight",
    "scrollbarY",
    "scrollbarYHeight",
    "scrollTop",
    "y",
    "scrollbarYRail"
  ]);
  bindMouseScrollHandler(i2, [
    "containerWidth",
    "contentWidth",
    "pageX",
    "railXWidth",
    "scrollbarX",
    "scrollbarXWidth",
    "scrollLeft",
    "x",
    "scrollbarXRail"
  ]);
}
function bindMouseScrollHandler(i2, ref) {
  var containerDimension = ref[0];
  var contentDimension = ref[1];
  var pageAxis = ref[2];
  var railDimension = ref[3];
  var scrollbarAxis = ref[4];
  var scrollbarDimension = ref[5];
  var scrollAxis = ref[6];
  var axis = ref[7];
  var scrollbarRail = ref[8];
  var element = i2.element;
  var startingScrollPosition = null;
  var startingMousePagePosition = null;
  var scrollBy = null;
  function moveHandler(e) {
    if (e.touches && e.touches[0]) {
      e[pageAxis] = e.touches[0]["page" + axis.toUpperCase()];
    }
    if (activeSlider === scrollbarAxis) {
      element[scrollAxis] = startingScrollPosition + scrollBy * (e[pageAxis] - startingMousePagePosition);
      addScrollingClass(i2, axis);
      updateGeometry(i2);
      e.stopPropagation();
      e.preventDefault();
    }
  }
  function endHandler() {
    removeScrollingClass(i2, axis);
    i2[scrollbarRail].classList.remove(cls.state.clicking);
    document.removeEventListener("mousemove", moveHandler);
    document.removeEventListener("mouseup", endHandler);
    document.removeEventListener("touchmove", moveHandler);
    document.removeEventListener("touchend", endHandler);
    activeSlider = null;
  }
  function bindMoves(e) {
    if (activeSlider === null) {
      activeSlider = scrollbarAxis;
      startingScrollPosition = element[scrollAxis];
      if (e.touches) {
        e[pageAxis] = e.touches[0]["page" + axis.toUpperCase()];
      }
      startingMousePagePosition = e[pageAxis];
      scrollBy = (i2[contentDimension] - i2[containerDimension]) / (i2[railDimension] - i2[scrollbarDimension]);
      if (!e.touches) {
        document.addEventListener("mousemove", moveHandler);
        document.addEventListener("mouseup", endHandler);
      } else {
        document.addEventListener("touchmove", moveHandler, { passive: false });
        document.addEventListener("touchend", endHandler);
      }
      i2[scrollbarRail].classList.add(cls.state.clicking);
    }
    e.stopPropagation();
    if (e.cancelable) {
      e.preventDefault();
    }
  }
  i2[scrollbarAxis].addEventListener("mousedown", bindMoves);
  i2[scrollbarAxis].addEventListener("touchstart", bindMoves);
}
function keyboard(i2) {
  var element = i2.element;
  var elementHovered = function() {
    return matches(element, ":hover");
  };
  var scrollbarFocused = function() {
    return matches(i2.scrollbarX, ":focus") || matches(i2.scrollbarY, ":focus");
  };
  function shouldPreventDefault(deltaX, deltaY) {
    var scrollTop = Math.floor(element.scrollTop);
    if (deltaX === 0) {
      if (!i2.scrollbarYActive) {
        return false;
      }
      if (scrollTop === 0 && deltaY > 0 || scrollTop >= i2.contentHeight - i2.containerHeight && deltaY < 0) {
        return !i2.settings.wheelPropagation;
      }
    }
    var scrollLeft = element.scrollLeft;
    if (deltaY === 0) {
      if (!i2.scrollbarXActive) {
        return false;
      }
      if (scrollLeft === 0 && deltaX < 0 || scrollLeft >= i2.contentWidth - i2.containerWidth && deltaX > 0) {
        return !i2.settings.wheelPropagation;
      }
    }
    return true;
  }
  i2.event.bind(i2.ownerDocument, "keydown", function(e) {
    if (e.isDefaultPrevented && e.isDefaultPrevented() || e.defaultPrevented) {
      return;
    }
    if (!elementHovered() && !scrollbarFocused()) {
      return;
    }
    var activeElement = document.activeElement ? document.activeElement : i2.ownerDocument.activeElement;
    if (activeElement) {
      if (activeElement.tagName === "IFRAME") {
        activeElement = activeElement.contentDocument.activeElement;
      } else {
        while (activeElement.shadowRoot) {
          activeElement = activeElement.shadowRoot.activeElement;
        }
      }
      if (isEditable(activeElement)) {
        return;
      }
    }
    var deltaX = 0;
    var deltaY = 0;
    switch (e.which) {
      case 37:
        if (e.metaKey) {
          deltaX = -i2.contentWidth;
        } else if (e.altKey) {
          deltaX = -i2.containerWidth;
        } else {
          deltaX = -30;
        }
        break;
      case 38:
        if (e.metaKey) {
          deltaY = i2.contentHeight;
        } else if (e.altKey) {
          deltaY = i2.containerHeight;
        } else {
          deltaY = 30;
        }
        break;
      case 39:
        if (e.metaKey) {
          deltaX = i2.contentWidth;
        } else if (e.altKey) {
          deltaX = i2.containerWidth;
        } else {
          deltaX = 30;
        }
        break;
      case 40:
        if (e.metaKey) {
          deltaY = -i2.contentHeight;
        } else if (e.altKey) {
          deltaY = -i2.containerHeight;
        } else {
          deltaY = -30;
        }
        break;
      case 32:
        if (e.shiftKey) {
          deltaY = i2.containerHeight;
        } else {
          deltaY = -i2.containerHeight;
        }
        break;
      case 33:
        deltaY = i2.containerHeight;
        break;
      case 34:
        deltaY = -i2.containerHeight;
        break;
      case 36:
        deltaY = i2.contentHeight;
        break;
      case 35:
        deltaY = -i2.contentHeight;
        break;
      default:
        return;
    }
    if (i2.settings.suppressScrollX && deltaX !== 0) {
      return;
    }
    if (i2.settings.suppressScrollY && deltaY !== 0) {
      return;
    }
    element.scrollTop -= deltaY;
    element.scrollLeft += deltaX;
    updateGeometry(i2);
    if (shouldPreventDefault(deltaX, deltaY)) {
      e.preventDefault();
    }
  });
}
function wheel(i2) {
  var element = i2.element;
  function shouldPreventDefault(deltaX, deltaY) {
    var roundedScrollTop = Math.floor(element.scrollTop);
    var isTop = element.scrollTop === 0;
    var isBottom = roundedScrollTop + element.offsetHeight === element.scrollHeight;
    var isLeft = element.scrollLeft === 0;
    var isRight = element.scrollLeft + element.offsetWidth === element.scrollWidth;
    var hitsBound;
    if (Math.abs(deltaY) > Math.abs(deltaX)) {
      hitsBound = isTop || isBottom;
    } else {
      hitsBound = isLeft || isRight;
    }
    return hitsBound ? !i2.settings.wheelPropagation : true;
  }
  function getDeltaFromEvent(e) {
    var deltaX = e.deltaX;
    var deltaY = -1 * e.deltaY;
    if (typeof deltaX === "undefined" || typeof deltaY === "undefined") {
      deltaX = -1 * e.wheelDeltaX / 6;
      deltaY = e.wheelDeltaY / 6;
    }
    if (e.deltaMode && e.deltaMode === 1) {
      deltaX *= 10;
      deltaY *= 10;
    }
    if (deltaX !== deltaX && deltaY !== deltaY) {
      deltaX = 0;
      deltaY = e.wheelDelta;
    }
    if (e.shiftKey) {
      return [-deltaY, -deltaX];
    }
    return [deltaX, deltaY];
  }
  function shouldBeConsumedByChild(target, deltaX, deltaY) {
    if (!env.isWebKit && element.querySelector("select:focus")) {
      return true;
    }
    if (!element.contains(target)) {
      return false;
    }
    var cursor = target;
    while (cursor && cursor !== element) {
      if (cursor.classList.contains(cls.element.consuming)) {
        return true;
      }
      var style = get4(cursor);
      if (deltaY && style.overflowY.match(/(scroll|auto)/)) {
        var maxScrollTop = cursor.scrollHeight - cursor.clientHeight;
        if (maxScrollTop > 0) {
          if (cursor.scrollTop > 0 && deltaY < 0 || cursor.scrollTop < maxScrollTop && deltaY > 0) {
            return true;
          }
        }
      }
      if (deltaX && style.overflowX.match(/(scroll|auto)/)) {
        var maxScrollLeft = cursor.scrollWidth - cursor.clientWidth;
        if (maxScrollLeft > 0) {
          if (cursor.scrollLeft > 0 && deltaX < 0 || cursor.scrollLeft < maxScrollLeft && deltaX > 0) {
            return true;
          }
        }
      }
      cursor = cursor.parentNode;
    }
    return false;
  }
  function mousewheelHandler(e) {
    var ref = getDeltaFromEvent(e);
    var deltaX = ref[0];
    var deltaY = ref[1];
    if (shouldBeConsumedByChild(e.target, deltaX, deltaY)) {
      return;
    }
    var shouldPrevent = false;
    if (!i2.settings.useBothWheelAxes) {
      element.scrollTop -= deltaY * i2.settings.wheelSpeed;
      element.scrollLeft += deltaX * i2.settings.wheelSpeed;
    } else if (i2.scrollbarYActive && !i2.scrollbarXActive) {
      if (deltaY) {
        element.scrollTop -= deltaY * i2.settings.wheelSpeed;
      } else {
        element.scrollTop += deltaX * i2.settings.wheelSpeed;
      }
      shouldPrevent = true;
    } else if (i2.scrollbarXActive && !i2.scrollbarYActive) {
      if (deltaX) {
        element.scrollLeft += deltaX * i2.settings.wheelSpeed;
      } else {
        element.scrollLeft -= deltaY * i2.settings.wheelSpeed;
      }
      shouldPrevent = true;
    }
    updateGeometry(i2);
    shouldPrevent = shouldPrevent || shouldPreventDefault(deltaX, deltaY);
    if (shouldPrevent && !e.ctrlKey) {
      e.stopPropagation();
      e.preventDefault();
    }
  }
  if (typeof window.onwheel !== "undefined") {
    i2.event.bind(element, "wheel", mousewheelHandler);
  } else if (typeof window.onmousewheel !== "undefined") {
    i2.event.bind(element, "mousewheel", mousewheelHandler);
  }
}
function touch(i2) {
  if (!env.supportsTouch && !env.supportsIePointer) {
    return;
  }
  var element = i2.element;
  var state = {
    startOffset: {},
    startTime: 0,
    speed: {},
    easingLoop: null
  };
  function shouldPrevent(deltaX, deltaY) {
    var scrollTop = Math.floor(element.scrollTop);
    var scrollLeft = element.scrollLeft;
    var magnitudeX = Math.abs(deltaX);
    var magnitudeY = Math.abs(deltaY);
    if (magnitudeY > magnitudeX) {
      if (deltaY < 0 && scrollTop === i2.contentHeight - i2.containerHeight || deltaY > 0 && scrollTop === 0) {
        return window.scrollY === 0 && deltaY > 0 && env.isChrome;
      }
    } else if (magnitudeX > magnitudeY) {
      if (deltaX < 0 && scrollLeft === i2.contentWidth - i2.containerWidth || deltaX > 0 && scrollLeft === 0) {
        return true;
      }
    }
    return true;
  }
  function applyTouchMove(differenceX, differenceY) {
    element.scrollTop -= differenceY;
    element.scrollLeft -= differenceX;
    updateGeometry(i2);
  }
  function getTouch(e) {
    if (e.targetTouches) {
      return e.targetTouches[0];
    }
    return e;
  }
  function shouldHandle(e) {
    if (e.target === i2.scrollbarX || e.target === i2.scrollbarY) {
      return false;
    }
    if (e.pointerType && e.pointerType === "pen" && e.buttons === 0) {
      return false;
    }
    if (e.targetTouches && e.targetTouches.length === 1) {
      return true;
    }
    if (e.pointerType && e.pointerType !== "mouse" && e.pointerType !== e.MSPOINTER_TYPE_MOUSE) {
      return true;
    }
    return false;
  }
  function touchStart(e) {
    if (!shouldHandle(e)) {
      return;
    }
    var touch2 = getTouch(e);
    state.startOffset.pageX = touch2.pageX;
    state.startOffset.pageY = touch2.pageY;
    state.startTime = (/* @__PURE__ */ new Date()).getTime();
    if (state.easingLoop !== null) {
      clearInterval(state.easingLoop);
    }
  }
  function shouldBeConsumedByChild(target, deltaX, deltaY) {
    if (!element.contains(target)) {
      return false;
    }
    var cursor = target;
    while (cursor && cursor !== element) {
      if (cursor.classList.contains(cls.element.consuming)) {
        return true;
      }
      var style = get4(cursor);
      if (deltaY && style.overflowY.match(/(scroll|auto)/)) {
        var maxScrollTop = cursor.scrollHeight - cursor.clientHeight;
        if (maxScrollTop > 0) {
          if (cursor.scrollTop > 0 && deltaY < 0 || cursor.scrollTop < maxScrollTop && deltaY > 0) {
            return true;
          }
        }
      }
      if (deltaX && style.overflowX.match(/(scroll|auto)/)) {
        var maxScrollLeft = cursor.scrollWidth - cursor.clientWidth;
        if (maxScrollLeft > 0) {
          if (cursor.scrollLeft > 0 && deltaX < 0 || cursor.scrollLeft < maxScrollLeft && deltaX > 0) {
            return true;
          }
        }
      }
      cursor = cursor.parentNode;
    }
    return false;
  }
  function touchMove(e) {
    if (shouldHandle(e)) {
      var touch2 = getTouch(e);
      var currentOffset = { pageX: touch2.pageX, pageY: touch2.pageY };
      var differenceX = currentOffset.pageX - state.startOffset.pageX;
      var differenceY = currentOffset.pageY - state.startOffset.pageY;
      if (shouldBeConsumedByChild(e.target, differenceX, differenceY)) {
        return;
      }
      applyTouchMove(differenceX, differenceY);
      state.startOffset = currentOffset;
      var currentTime = (/* @__PURE__ */ new Date()).getTime();
      var timeGap = currentTime - state.startTime;
      if (timeGap > 0) {
        state.speed.x = differenceX / timeGap;
        state.speed.y = differenceY / timeGap;
        state.startTime = currentTime;
      }
      if (shouldPrevent(differenceX, differenceY)) {
        if (e.cancelable) {
          e.preventDefault();
        }
      }
    }
  }
  function touchEnd() {
    if (i2.settings.swipeEasing) {
      clearInterval(state.easingLoop);
      state.easingLoop = setInterval(function() {
        if (i2.isInitialized) {
          clearInterval(state.easingLoop);
          return;
        }
        if (!state.speed.x && !state.speed.y) {
          clearInterval(state.easingLoop);
          return;
        }
        if (Math.abs(state.speed.x) < 0.01 && Math.abs(state.speed.y) < 0.01) {
          clearInterval(state.easingLoop);
          return;
        }
        applyTouchMove(state.speed.x * 30, state.speed.y * 30);
        state.speed.x *= 0.8;
        state.speed.y *= 0.8;
      }, 10);
    }
  }
  if (env.supportsTouch) {
    i2.event.bind(element, "touchstart", touchStart);
    i2.event.bind(element, "touchmove", touchMove);
    i2.event.bind(element, "touchend", touchEnd);
  } else if (env.supportsIePointer) {
    if (window.PointerEvent) {
      i2.event.bind(element, "pointerdown", touchStart);
      i2.event.bind(element, "pointermove", touchMove);
      i2.event.bind(element, "pointerup", touchEnd);
    } else if (window.MSPointerEvent) {
      i2.event.bind(element, "MSPointerDown", touchStart);
      i2.event.bind(element, "MSPointerMove", touchMove);
      i2.event.bind(element, "MSPointerUp", touchEnd);
    }
  }
}
var elMatches, cls, scrollingClassTimeout, EventElement, prototypeAccessors, EventManager, env, activeSlider, defaultSettings, handlers, PerfectScrollbar, perfect_scrollbar_esm_default;
var init_perfect_scrollbar_esm = __esm({
  "node_modules/perfect-scrollbar/dist/perfect-scrollbar.esm.js"() {
    elMatches = typeof Element !== "undefined" && (Element.prototype.matches || Element.prototype.webkitMatchesSelector || Element.prototype.mozMatchesSelector || Element.prototype.msMatchesSelector);
    cls = {
      main: "ps",
      rtl: "ps__rtl",
      element: {
        thumb: function(x2) {
          return "ps__thumb-" + x2;
        },
        rail: function(x2) {
          return "ps__rail-" + x2;
        },
        consuming: "ps__child--consume"
      },
      state: {
        focus: "ps--focus",
        clicking: "ps--clicking",
        active: function(x2) {
          return "ps--active-" + x2;
        },
        scrolling: function(x2) {
          return "ps--scrolling-" + x2;
        }
      }
    };
    scrollingClassTimeout = { x: null, y: null };
    EventElement = function EventElement2(element) {
      this.element = element;
      this.handlers = {};
    };
    prototypeAccessors = { isEmpty: { configurable: true } };
    EventElement.prototype.bind = function bind(eventName, handler) {
      if (typeof this.handlers[eventName] === "undefined") {
        this.handlers[eventName] = [];
      }
      this.handlers[eventName].push(handler);
      this.element.addEventListener(eventName, handler, false);
    };
    EventElement.prototype.unbind = function unbind(eventName, target) {
      var this$1 = this;
      this.handlers[eventName] = this.handlers[eventName].filter(function(handler) {
        if (target && handler !== target) {
          return true;
        }
        this$1.element.removeEventListener(eventName, handler, false);
        return false;
      });
    };
    EventElement.prototype.unbindAll = function unbindAll() {
      for (var name in this.handlers) {
        this.unbind(name);
      }
    };
    prototypeAccessors.isEmpty.get = function() {
      var this$1 = this;
      return Object.keys(this.handlers).every(
        function(key2) {
          return this$1.handlers[key2].length === 0;
        }
      );
    };
    Object.defineProperties(EventElement.prototype, prototypeAccessors);
    EventManager = function EventManager2() {
      this.eventElements = [];
    };
    EventManager.prototype.eventElement = function eventElement(element) {
      var ee2 = this.eventElements.filter(function(ee3) {
        return ee3.element === element;
      })[0];
      if (!ee2) {
        ee2 = new EventElement(element);
        this.eventElements.push(ee2);
      }
      return ee2;
    };
    EventManager.prototype.bind = function bind2(element, eventName, handler) {
      this.eventElement(element).bind(eventName, handler);
    };
    EventManager.prototype.unbind = function unbind2(element, eventName, handler) {
      var ee2 = this.eventElement(element);
      ee2.unbind(eventName, handler);
      if (ee2.isEmpty) {
        this.eventElements.splice(this.eventElements.indexOf(ee2), 1);
      }
    };
    EventManager.prototype.unbindAll = function unbindAll2() {
      this.eventElements.forEach(function(e) {
        return e.unbindAll();
      });
      this.eventElements = [];
    };
    EventManager.prototype.once = function once2(element, eventName, handler) {
      var ee2 = this.eventElement(element);
      var onceHandler = function(evt) {
        ee2.unbind(eventName, onceHandler);
        handler(evt);
      };
      ee2.bind(eventName, onceHandler);
    };
    env = {
      isWebKit: typeof document !== "undefined" && "WebkitAppearance" in document.documentElement.style,
      supportsTouch: typeof window !== "undefined" && ("ontouchstart" in window || "maxTouchPoints" in window.navigator && window.navigator.maxTouchPoints > 0 || window.DocumentTouch && document instanceof window.DocumentTouch),
      supportsIePointer: typeof navigator !== "undefined" && navigator.msMaxTouchPoints,
      isChrome: typeof navigator !== "undefined" && /Chrome/i.test(navigator && navigator.userAgent)
    };
    activeSlider = null;
    defaultSettings = function() {
      return {
        handlers: ["click-rail", "drag-thumb", "keyboard", "wheel", "touch"],
        maxScrollbarLength: null,
        minScrollbarLength: null,
        scrollingThreshold: 1e3,
        scrollXMarginOffset: 0,
        scrollYMarginOffset: 0,
        suppressScrollX: false,
        suppressScrollY: false,
        swipeEasing: true,
        useBothWheelAxes: false,
        wheelPropagation: true,
        wheelSpeed: 1
      };
    };
    handlers = {
      "click-rail": clickRail,
      "drag-thumb": setupScrollHandlers,
      keyboard,
      wheel,
      touch
    };
    PerfectScrollbar = function PerfectScrollbar2(element, userSettings) {
      var this$1 = this;
      if (userSettings === void 0) userSettings = {};
      if (typeof element === "string") {
        element = document.querySelector(element);
      }
      if (!element || !element.nodeName) {
        throw new Error("no element is specified to initialize PerfectScrollbar");
      }
      this.element = element;
      element.classList.add(cls.main);
      this.settings = defaultSettings();
      for (var key2 in userSettings) {
        this.settings[key2] = userSettings[key2];
      }
      this.containerWidth = null;
      this.containerHeight = null;
      this.contentWidth = null;
      this.contentHeight = null;
      var focus = function() {
        return element.classList.add(cls.state.focus);
      };
      var blur = function() {
        return element.classList.remove(cls.state.focus);
      };
      this.isRtl = get4(element).direction === "rtl";
      if (this.isRtl === true) {
        element.classList.add(cls.rtl);
      }
      this.isNegativeScroll = (function() {
        var originalScrollLeft = element.scrollLeft;
        var result = null;
        element.scrollLeft = -1;
        result = element.scrollLeft < 0;
        element.scrollLeft = originalScrollLeft;
        return result;
      })();
      this.negativeScrollAdjustment = this.isNegativeScroll ? element.scrollWidth - element.clientWidth : 0;
      this.event = new EventManager();
      this.ownerDocument = element.ownerDocument || document;
      this.scrollbarXRail = div(cls.element.rail("x"));
      element.appendChild(this.scrollbarXRail);
      this.scrollbarX = div(cls.element.thumb("x"));
      this.scrollbarXRail.appendChild(this.scrollbarX);
      this.scrollbarX.setAttribute("tabindex", 0);
      this.event.bind(this.scrollbarX, "focus", focus);
      this.event.bind(this.scrollbarX, "blur", blur);
      this.scrollbarXActive = null;
      this.scrollbarXWidth = null;
      this.scrollbarXLeft = null;
      var railXStyle = get4(this.scrollbarXRail);
      this.scrollbarXBottom = parseInt(railXStyle.bottom, 10);
      if (isNaN(this.scrollbarXBottom)) {
        this.isScrollbarXUsingBottom = false;
        this.scrollbarXTop = toInt(railXStyle.top);
      } else {
        this.isScrollbarXUsingBottom = true;
      }
      this.railBorderXWidth = toInt(railXStyle.borderLeftWidth) + toInt(railXStyle.borderRightWidth);
      set6(this.scrollbarXRail, { display: "block" });
      this.railXMarginWidth = toInt(railXStyle.marginLeft) + toInt(railXStyle.marginRight);
      set6(this.scrollbarXRail, { display: "" });
      this.railXWidth = null;
      this.railXRatio = null;
      this.scrollbarYRail = div(cls.element.rail("y"));
      element.appendChild(this.scrollbarYRail);
      this.scrollbarY = div(cls.element.thumb("y"));
      this.scrollbarYRail.appendChild(this.scrollbarY);
      this.scrollbarY.setAttribute("tabindex", 0);
      this.event.bind(this.scrollbarY, "focus", focus);
      this.event.bind(this.scrollbarY, "blur", blur);
      this.scrollbarYActive = null;
      this.scrollbarYHeight = null;
      this.scrollbarYTop = null;
      var railYStyle = get4(this.scrollbarYRail);
      this.scrollbarYRight = parseInt(railYStyle.right, 10);
      if (isNaN(this.scrollbarYRight)) {
        this.isScrollbarYUsingRight = false;
        this.scrollbarYLeft = toInt(railYStyle.left);
      } else {
        this.isScrollbarYUsingRight = true;
      }
      this.scrollbarYOuterWidth = this.isRtl ? outerWidth(this.scrollbarY) : null;
      this.railBorderYWidth = toInt(railYStyle.borderTopWidth) + toInt(railYStyle.borderBottomWidth);
      set6(this.scrollbarYRail, { display: "block" });
      this.railYMarginHeight = toInt(railYStyle.marginTop) + toInt(railYStyle.marginBottom);
      set6(this.scrollbarYRail, { display: "" });
      this.railYHeight = null;
      this.railYRatio = null;
      this.reach = {
        x: element.scrollLeft <= 0 ? "start" : element.scrollLeft >= this.contentWidth - this.containerWidth ? "end" : null,
        y: element.scrollTop <= 0 ? "start" : element.scrollTop >= this.contentHeight - this.containerHeight ? "end" : null
      };
      this.isAlive = true;
      this.settings.handlers.forEach(function(handlerName) {
        return handlers[handlerName](this$1);
      });
      this.lastScrollTop = Math.floor(element.scrollTop);
      this.lastScrollLeft = element.scrollLeft;
      this.event.bind(this.element, "scroll", function(e) {
        return this$1.onScroll(e);
      });
      updateGeometry(this);
    };
    PerfectScrollbar.prototype.update = function update() {
      if (!this.isAlive) {
        return;
      }
      this.negativeScrollAdjustment = this.isNegativeScroll ? this.element.scrollWidth - this.element.clientWidth : 0;
      set6(this.scrollbarXRail, { display: "block" });
      set6(this.scrollbarYRail, { display: "block" });
      this.railXMarginWidth = toInt(get4(this.scrollbarXRail).marginLeft) + toInt(get4(this.scrollbarXRail).marginRight);
      this.railYMarginHeight = toInt(get4(this.scrollbarYRail).marginTop) + toInt(get4(this.scrollbarYRail).marginBottom);
      set6(this.scrollbarXRail, { display: "none" });
      set6(this.scrollbarYRail, { display: "none" });
      updateGeometry(this);
      processScrollDiff(this, "top", 0, false, true);
      processScrollDiff(this, "left", 0, false, true);
      set6(this.scrollbarXRail, { display: "" });
      set6(this.scrollbarYRail, { display: "" });
    };
    PerfectScrollbar.prototype.onScroll = function onScroll(e) {
      if (!this.isAlive) {
        return;
      }
      updateGeometry(this);
      processScrollDiff(this, "top", this.element.scrollTop - this.lastScrollTop);
      processScrollDiff(this, "left", this.element.scrollLeft - this.lastScrollLeft);
      this.lastScrollTop = Math.floor(this.element.scrollTop);
      this.lastScrollLeft = this.element.scrollLeft;
    };
    PerfectScrollbar.prototype.destroy = function destroy() {
      if (!this.isAlive) {
        return;
      }
      this.event.unbindAll();
      remove3(this.scrollbarX);
      remove3(this.scrollbarY);
      remove3(this.scrollbarXRail);
      remove3(this.scrollbarYRail);
      this.removePsClasses();
      this.element = null;
      this.scrollbarX = null;
      this.scrollbarY = null;
      this.scrollbarXRail = null;
      this.scrollbarYRail = null;
      this.isAlive = false;
    };
    PerfectScrollbar.prototype.removePsClasses = function removePsClasses() {
      this.element.className = this.element.className.split(" ").filter(function(name) {
        return !name.match(/^ps([-_].+|)$/);
      }).join(" ");
    };
    perfect_scrollbar_esm_default = PerfectScrollbar;
  }
});

// node_modules/dompurify/dist/purify.cjs.js
var require_purify_cjs = __commonJS({
  "node_modules/dompurify/dist/purify.cjs.js"(exports, module) {
    "use strict";
    var {
      entries: entries2,
      setPrototypeOf,
      isFrozen,
      getPrototypeOf,
      getOwnPropertyDescriptor
    } = Object;
    var {
      freeze,
      seal,
      create
    } = Object;
    var {
      apply,
      construct
    } = typeof Reflect !== "undefined" && Reflect;
    if (!freeze) {
      freeze = function freeze2(x2) {
        return x2;
      };
    }
    if (!seal) {
      seal = function seal2(x2) {
        return x2;
      };
    }
    if (!apply) {
      apply = function apply2(func, thisArg) {
        for (var _len = arguments.length, args = new Array(_len > 2 ? _len - 2 : 0), _key = 2; _key < _len; _key++) {
          args[_key - 2] = arguments[_key];
        }
        return func.apply(thisArg, args);
      };
    }
    if (!construct) {
      construct = function construct2(Func) {
        for (var _len2 = arguments.length, args = new Array(_len2 > 1 ? _len2 - 1 : 0), _key2 = 1; _key2 < _len2; _key2++) {
          args[_key2 - 1] = arguments[_key2];
        }
        return new Func(...args);
      };
    }
    var arrayForEach = unapply(Array.prototype.forEach);
    var arrayLastIndexOf = unapply(Array.prototype.lastIndexOf);
    var arrayPop = unapply(Array.prototype.pop);
    var arrayPush = unapply(Array.prototype.push);
    var arraySplice = unapply(Array.prototype.splice);
    var stringToLowerCase = unapply(String.prototype.toLowerCase);
    var stringToString = unapply(String.prototype.toString);
    var stringMatch = unapply(String.prototype.match);
    var stringReplace = unapply(String.prototype.replace);
    var stringIndexOf = unapply(String.prototype.indexOf);
    var stringTrim = unapply(String.prototype.trim);
    var objectHasOwnProperty = unapply(Object.prototype.hasOwnProperty);
    var regExpTest = unapply(RegExp.prototype.test);
    var typeErrorCreate = unconstruct(TypeError);
    function unapply(func) {
      return function(thisArg) {
        if (thisArg instanceof RegExp) {
          thisArg.lastIndex = 0;
        }
        for (var _len3 = arguments.length, args = new Array(_len3 > 1 ? _len3 - 1 : 0), _key3 = 1; _key3 < _len3; _key3++) {
          args[_key3 - 1] = arguments[_key3];
        }
        return apply(func, thisArg, args);
      };
    }
    function unconstruct(Func) {
      return function() {
        for (var _len4 = arguments.length, args = new Array(_len4), _key4 = 0; _key4 < _len4; _key4++) {
          args[_key4] = arguments[_key4];
        }
        return construct(Func, args);
      };
    }
    function addToSet(set7, array2) {
      let transformCaseFunc = arguments.length > 2 && arguments[2] !== void 0 ? arguments[2] : stringToLowerCase;
      if (setPrototypeOf) {
        setPrototypeOf(set7, null);
      }
      let l2 = array2.length;
      while (l2--) {
        let element = array2[l2];
        if (typeof element === "string") {
          const lcElement = transformCaseFunc(element);
          if (lcElement !== element) {
            if (!isFrozen(array2)) {
              array2[l2] = lcElement;
            }
            element = lcElement;
          }
        }
        set7[element] = true;
      }
      return set7;
    }
    function cleanArray(array2) {
      for (let index = 0; index < array2.length; index++) {
        const isPropertyExist = objectHasOwnProperty(array2, index);
        if (!isPropertyExist) {
          array2[index] = null;
        }
      }
      return array2;
    }
    function clone(object2) {
      const newObject = create(null);
      for (const [property, value] of entries2(object2)) {
        const isPropertyExist = objectHasOwnProperty(object2, property);
        if (isPropertyExist) {
          if (Array.isArray(value)) {
            newObject[property] = cleanArray(value);
          } else if (value && typeof value === "object" && value.constructor === Object) {
            newObject[property] = clone(value);
          } else {
            newObject[property] = value;
          }
        }
      }
      return newObject;
    }
    function lookupGetter(object2, prop) {
      while (object2 !== null) {
        const desc = getOwnPropertyDescriptor(object2, prop);
        if (desc) {
          if (desc.get) {
            return unapply(desc.get);
          }
          if (typeof desc.value === "function") {
            return unapply(desc.value);
          }
        }
        object2 = getPrototypeOf(object2);
      }
      function fallbackValue() {
        return null;
      }
      return fallbackValue;
    }
    var html$1 = freeze(["a", "abbr", "acronym", "address", "area", "article", "aside", "audio", "b", "bdi", "bdo", "big", "blink", "blockquote", "body", "br", "button", "canvas", "caption", "center", "cite", "code", "col", "colgroup", "content", "data", "datalist", "dd", "decorator", "del", "details", "dfn", "dialog", "dir", "div", "dl", "dt", "element", "em", "fieldset", "figcaption", "figure", "font", "footer", "form", "h1", "h2", "h3", "h4", "h5", "h6", "head", "header", "hgroup", "hr", "html", "i", "img", "input", "ins", "kbd", "label", "legend", "li", "main", "map", "mark", "marquee", "menu", "menuitem", "meter", "nav", "nobr", "ol", "optgroup", "option", "output", "p", "picture", "pre", "progress", "q", "rp", "rt", "ruby", "s", "samp", "search", "section", "select", "shadow", "slot", "small", "source", "spacer", "span", "strike", "strong", "style", "sub", "summary", "sup", "table", "tbody", "td", "template", "textarea", "tfoot", "th", "thead", "time", "tr", "track", "tt", "u", "ul", "var", "video", "wbr"]);
    var svg$1 = freeze(["svg", "a", "altglyph", "altglyphdef", "altglyphitem", "animatecolor", "animatemotion", "animatetransform", "circle", "clippath", "defs", "desc", "ellipse", "enterkeyhint", "exportparts", "filter", "font", "g", "glyph", "glyphref", "hkern", "image", "inputmode", "line", "lineargradient", "marker", "mask", "metadata", "mpath", "part", "path", "pattern", "polygon", "polyline", "radialgradient", "rect", "slot", "stop", "style", "switch", "symbol", "text", "textpath", "title", "tref", "tspan", "view", "vkern"]);
    var svgFilters = freeze(["feBlend", "feColorMatrix", "feComponentTransfer", "feComposite", "feConvolveMatrix", "feDiffuseLighting", "feDisplacementMap", "feDistantLight", "feDropShadow", "feFlood", "feFuncA", "feFuncB", "feFuncG", "feFuncR", "feGaussianBlur", "feImage", "feMerge", "feMergeNode", "feMorphology", "feOffset", "fePointLight", "feSpecularLighting", "feSpotLight", "feTile", "feTurbulence"]);
    var svgDisallowed = freeze(["animate", "color-profile", "cursor", "discard", "font-face", "font-face-format", "font-face-name", "font-face-src", "font-face-uri", "foreignobject", "hatch", "hatchpath", "mesh", "meshgradient", "meshpatch", "meshrow", "missing-glyph", "script", "set", "solidcolor", "unknown", "use"]);
    var mathMl$1 = freeze(["math", "menclose", "merror", "mfenced", "mfrac", "mglyph", "mi", "mlabeledtr", "mmultiscripts", "mn", "mo", "mover", "mpadded", "mphantom", "mroot", "mrow", "ms", "mspace", "msqrt", "mstyle", "msub", "msup", "msubsup", "mtable", "mtd", "mtext", "mtr", "munder", "munderover", "mprescripts"]);
    var mathMlDisallowed = freeze(["maction", "maligngroup", "malignmark", "mlongdiv", "mscarries", "mscarry", "msgroup", "mstack", "msline", "msrow", "semantics", "annotation", "annotation-xml", "mprescripts", "none"]);
    var text = freeze(["#text"]);
    var html = freeze(["accept", "action", "align", "alt", "autocapitalize", "autocomplete", "autopictureinpicture", "autoplay", "background", "bgcolor", "border", "capture", "cellpadding", "cellspacing", "checked", "cite", "class", "clear", "color", "cols", "colspan", "controls", "controlslist", "coords", "crossorigin", "datetime", "decoding", "default", "dir", "disabled", "disablepictureinpicture", "disableremoteplayback", "download", "draggable", "enctype", "enterkeyhint", "exportparts", "face", "for", "headers", "height", "hidden", "high", "href", "hreflang", "id", "inert", "inputmode", "integrity", "ismap", "kind", "label", "lang", "list", "loading", "loop", "low", "max", "maxlength", "media", "method", "min", "minlength", "multiple", "muted", "name", "nonce", "noshade", "novalidate", "nowrap", "open", "optimum", "part", "pattern", "placeholder", "playsinline", "popover", "popovertarget", "popovertargetaction", "poster", "preload", "pubdate", "radiogroup", "readonly", "rel", "required", "rev", "reversed", "role", "rows", "rowspan", "spellcheck", "scope", "selected", "shape", "size", "sizes", "slot", "span", "srclang", "start", "src", "srcset", "step", "style", "summary", "tabindex", "title", "translate", "type", "usemap", "valign", "value", "width", "wrap", "xmlns", "slot"]);
    var svg = freeze(["accent-height", "accumulate", "additive", "alignment-baseline", "amplitude", "ascent", "attributename", "attributetype", "azimuth", "basefrequency", "baseline-shift", "begin", "bias", "by", "class", "clip", "clippathunits", "clip-path", "clip-rule", "color", "color-interpolation", "color-interpolation-filters", "color-profile", "color-rendering", "cx", "cy", "d", "dx", "dy", "diffuseconstant", "direction", "display", "divisor", "dur", "edgemode", "elevation", "end", "exponent", "fill", "fill-opacity", "fill-rule", "filter", "filterunits", "flood-color", "flood-opacity", "font-family", "font-size", "font-size-adjust", "font-stretch", "font-style", "font-variant", "font-weight", "fx", "fy", "g1", "g2", "glyph-name", "glyphref", "gradientunits", "gradienttransform", "height", "href", "id", "image-rendering", "in", "in2", "intercept", "k", "k1", "k2", "k3", "k4", "kerning", "keypoints", "keysplines", "keytimes", "lang", "lengthadjust", "letter-spacing", "kernelmatrix", "kernelunitlength", "lighting-color", "local", "marker-end", "marker-mid", "marker-start", "markerheight", "markerunits", "markerwidth", "maskcontentunits", "maskunits", "max", "mask", "media", "method", "mode", "min", "name", "numoctaves", "offset", "operator", "opacity", "order", "orient", "orientation", "origin", "overflow", "paint-order", "path", "pathlength", "patterncontentunits", "patterntransform", "patternunits", "points", "preservealpha", "preserveaspectratio", "primitiveunits", "r", "rx", "ry", "radius", "refx", "refy", "repeatcount", "repeatdur", "restart", "result", "rotate", "scale", "seed", "shape-rendering", "slope", "specularconstant", "specularexponent", "spreadmethod", "startoffset", "stddeviation", "stitchtiles", "stop-color", "stop-opacity", "stroke-dasharray", "stroke-dashoffset", "stroke-linecap", "stroke-linejoin", "stroke-miterlimit", "stroke-opacity", "stroke", "stroke-width", "style", "surfacescale", "systemlanguage", "tabindex", "tablevalues", "targetx", "targety", "transform", "transform-origin", "text-anchor", "text-decoration", "text-rendering", "textlength", "type", "u1", "u2", "unicode", "values", "viewbox", "visibility", "version", "vert-adv-y", "vert-origin-x", "vert-origin-y", "width", "word-spacing", "wrap", "writing-mode", "xchannelselector", "ychannelselector", "x", "x1", "x2", "xmlns", "y", "y1", "y2", "z", "zoomandpan"]);
    var mathMl = freeze(["accent", "accentunder", "align", "bevelled", "close", "columnsalign", "columnlines", "columnspan", "denomalign", "depth", "dir", "display", "displaystyle", "encoding", "fence", "frame", "height", "href", "id", "largeop", "length", "linethickness", "lspace", "lquote", "mathbackground", "mathcolor", "mathsize", "mathvariant", "maxsize", "minsize", "movablelimits", "notation", "numalign", "open", "rowalign", "rowlines", "rowspacing", "rowspan", "rspace", "rquote", "scriptlevel", "scriptminsize", "scriptsizemultiplier", "selection", "separator", "separators", "stretchy", "subscriptshift", "supscriptshift", "symmetric", "voffset", "width", "xmlns"]);
    var xml = freeze(["xlink:href", "xml:id", "xlink:title", "xml:space", "xmlns:xlink"]);
    var MUSTACHE_EXPR = seal(/\{\{[\w\W]*|[\w\W]*\}\}/gm);
    var ERB_EXPR = seal(/<%[\w\W]*|[\w\W]*%>/gm);
    var TMPLIT_EXPR = seal(/\$\{[\w\W]*/gm);
    var DATA_ATTR = seal(/^data-[\-\w.\u00B7-\uFFFF]+$/);
    var ARIA_ATTR = seal(/^aria-[\-\w]+$/);
    var IS_ALLOWED_URI = seal(
      /^(?:(?:(?:f|ht)tps?|mailto|tel|callto|sms|cid|xmpp|matrix):|[^a-z]|[a-z+.\-]+(?:[^a-z+.\-:]|$))/i
      // eslint-disable-line no-useless-escape
    );
    var IS_SCRIPT_OR_DATA = seal(/^(?:\w+script|data):/i);
    var ATTR_WHITESPACE = seal(
      /[\u0000-\u0020\u00A0\u1680\u180E\u2000-\u2029\u205F\u3000]/g
      // eslint-disable-line no-control-regex
    );
    var DOCTYPE_NAME = seal(/^html$/i);
    var CUSTOM_ELEMENT = seal(/^[a-z][.\w]*(-[.\w]+)+$/i);
    var EXPRESSIONS = Object.freeze({
      __proto__: null,
      ARIA_ATTR,
      ATTR_WHITESPACE,
      CUSTOM_ELEMENT,
      DATA_ATTR,
      DOCTYPE_NAME,
      ERB_EXPR,
      IS_ALLOWED_URI,
      IS_SCRIPT_OR_DATA,
      MUSTACHE_EXPR,
      TMPLIT_EXPR
    });
    var NODE_TYPE2 = {
      element: 1,
      attribute: 2,
      text: 3,
      cdataSection: 4,
      entityReference: 5,
      // Deprecated
      entityNode: 6,
      // Deprecated
      progressingInstruction: 7,
      comment: 8,
      document: 9,
      documentType: 10,
      documentFragment: 11,
      notation: 12
      // Deprecated
    };
    var getGlobal2 = function getGlobal3() {
      return typeof window === "undefined" ? null : window;
    };
    var _createTrustedTypesPolicy = function _createTrustedTypesPolicy2(trustedTypes, purifyHostElement) {
      if (typeof trustedTypes !== "object" || typeof trustedTypes.createPolicy !== "function") {
        return null;
      }
      let suffix = null;
      const ATTR_NAME = "data-tt-policy-suffix";
      if (purifyHostElement && purifyHostElement.hasAttribute(ATTR_NAME)) {
        suffix = purifyHostElement.getAttribute(ATTR_NAME);
      }
      const policyName = "dompurify" + (suffix ? "#" + suffix : "");
      try {
        return trustedTypes.createPolicy(policyName, {
          createHTML(html2) {
            return html2;
          },
          createScriptURL(scriptUrl) {
            return scriptUrl;
          }
        });
      } catch (_15) {
        console.warn("TrustedTypes policy " + policyName + " could not be created.");
        return null;
      }
    };
    var _createHooksMap = function _createHooksMap2() {
      return {
        afterSanitizeAttributes: [],
        afterSanitizeElements: [],
        afterSanitizeShadowDOM: [],
        beforeSanitizeAttributes: [],
        beforeSanitizeElements: [],
        beforeSanitizeShadowDOM: [],
        uponSanitizeAttribute: [],
        uponSanitizeElement: [],
        uponSanitizeShadowNode: []
      };
    };
    function createDOMPurify() {
      let window2 = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : getGlobal2();
      const DOMPurify = (root) => createDOMPurify(root);
      DOMPurify.version = "3.2.7";
      DOMPurify.removed = [];
      if (!window2 || !window2.document || window2.document.nodeType !== NODE_TYPE2.document || !window2.Element) {
        DOMPurify.isSupported = false;
        return DOMPurify;
      }
      let {
        document: document2
      } = window2;
      const originalDocument = document2;
      const currentScript = originalDocument.currentScript;
      const {
        DocumentFragment,
        HTMLTemplateElement,
        Node: Node2,
        Element: Element2,
        NodeFilter: NodeFilter2,
        NamedNodeMap = window2.NamedNodeMap || window2.MozNamedAttrMap,
        HTMLFormElement,
        DOMParser,
        trustedTypes
      } = window2;
      const ElementPrototype = Element2.prototype;
      const cloneNode = lookupGetter(ElementPrototype, "cloneNode");
      const remove4 = lookupGetter(ElementPrototype, "remove");
      const getNextSibling = lookupGetter(ElementPrototype, "nextSibling");
      const getChildNodes = lookupGetter(ElementPrototype, "childNodes");
      const getParentNode = lookupGetter(ElementPrototype, "parentNode");
      if (typeof HTMLTemplateElement === "function") {
        const template2 = document2.createElement("template");
        if (template2.content && template2.content.ownerDocument) {
          document2 = template2.content.ownerDocument;
        }
      }
      let trustedTypesPolicy;
      let emptyHTML = "";
      const {
        implementation,
        createNodeIterator,
        createDocumentFragment,
        getElementsByTagName
      } = document2;
      const {
        importNode
      } = originalDocument;
      let hooks = _createHooksMap();
      DOMPurify.isSupported = typeof entries2 === "function" && typeof getParentNode === "function" && implementation && implementation.createHTMLDocument !== void 0;
      const {
        MUSTACHE_EXPR: MUSTACHE_EXPR2,
        ERB_EXPR: ERB_EXPR2,
        TMPLIT_EXPR: TMPLIT_EXPR2,
        DATA_ATTR: DATA_ATTR2,
        ARIA_ATTR: ARIA_ATTR2,
        IS_SCRIPT_OR_DATA: IS_SCRIPT_OR_DATA2,
        ATTR_WHITESPACE: ATTR_WHITESPACE2,
        CUSTOM_ELEMENT: CUSTOM_ELEMENT2
      } = EXPRESSIONS;
      let {
        IS_ALLOWED_URI: IS_ALLOWED_URI$1
      } = EXPRESSIONS;
      let ALLOWED_TAGS = null;
      const DEFAULT_ALLOWED_TAGS = addToSet({}, [...html$1, ...svg$1, ...svgFilters, ...mathMl$1, ...text]);
      let ALLOWED_ATTR = null;
      const DEFAULT_ALLOWED_ATTR = addToSet({}, [...html, ...svg, ...mathMl, ...xml]);
      let CUSTOM_ELEMENT_HANDLING = Object.seal(create(null, {
        tagNameCheck: {
          writable: true,
          configurable: false,
          enumerable: true,
          value: null
        },
        attributeNameCheck: {
          writable: true,
          configurable: false,
          enumerable: true,
          value: null
        },
        allowCustomizedBuiltInElements: {
          writable: true,
          configurable: false,
          enumerable: true,
          value: false
        }
      }));
      let FORBID_TAGS = null;
      let FORBID_ATTR = null;
      let ALLOW_ARIA_ATTR = true;
      let ALLOW_DATA_ATTR = true;
      let ALLOW_UNKNOWN_PROTOCOLS = false;
      let ALLOW_SELF_CLOSE_IN_ATTR = true;
      let SAFE_FOR_TEMPLATES = false;
      let SAFE_FOR_XML = true;
      let WHOLE_DOCUMENT = false;
      let SET_CONFIG = false;
      let FORCE_BODY = false;
      let RETURN_DOM = false;
      let RETURN_DOM_FRAGMENT = false;
      let RETURN_TRUSTED_TYPE = false;
      let SANITIZE_DOM = true;
      let SANITIZE_NAMED_PROPS = false;
      const SANITIZE_NAMED_PROPS_PREFIX = "user-content-";
      let KEEP_CONTENT = true;
      let IN_PLACE = false;
      let USE_PROFILES = {};
      let FORBID_CONTENTS = null;
      const DEFAULT_FORBID_CONTENTS = addToSet({}, ["annotation-xml", "audio", "colgroup", "desc", "foreignobject", "head", "iframe", "math", "mi", "mn", "mo", "ms", "mtext", "noembed", "noframes", "noscript", "plaintext", "script", "style", "svg", "template", "thead", "title", "video", "xmp"]);
      let DATA_URI_TAGS = null;
      const DEFAULT_DATA_URI_TAGS = addToSet({}, ["audio", "video", "img", "source", "image", "track"]);
      let URI_SAFE_ATTRIBUTES = null;
      const DEFAULT_URI_SAFE_ATTRIBUTES = addToSet({}, ["alt", "class", "for", "id", "label", "name", "pattern", "placeholder", "role", "summary", "title", "value", "style", "xmlns"]);
      const MATHML_NAMESPACE = "http://www.w3.org/1998/Math/MathML";
      const SVG_NAMESPACE = "http://www.w3.org/2000/svg";
      const HTML_NAMESPACE = "http://www.w3.org/1999/xhtml";
      let NAMESPACE = HTML_NAMESPACE;
      let IS_EMPTY_INPUT = false;
      let ALLOWED_NAMESPACES = null;
      const DEFAULT_ALLOWED_NAMESPACES = addToSet({}, [MATHML_NAMESPACE, SVG_NAMESPACE, HTML_NAMESPACE], stringToString);
      let MATHML_TEXT_INTEGRATION_POINTS = addToSet({}, ["mi", "mo", "mn", "ms", "mtext"]);
      let HTML_INTEGRATION_POINTS = addToSet({}, ["annotation-xml"]);
      const COMMON_SVG_AND_HTML_ELEMENTS = addToSet({}, ["title", "style", "font", "a", "script"]);
      let PARSER_MEDIA_TYPE = null;
      const SUPPORTED_PARSER_MEDIA_TYPES = ["application/xhtml+xml", "text/html"];
      const DEFAULT_PARSER_MEDIA_TYPE = "text/html";
      let transformCaseFunc = null;
      let CONFIG = null;
      const formElement = document2.createElement("form");
      const isRegexOrFunction = function isRegexOrFunction2(testValue) {
        return testValue instanceof RegExp || testValue instanceof Function;
      };
      const _parseConfig = function _parseConfig2() {
        let cfg = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
        if (CONFIG && CONFIG === cfg) {
          return;
        }
        if (!cfg || typeof cfg !== "object") {
          cfg = {};
        }
        cfg = clone(cfg);
        PARSER_MEDIA_TYPE = // eslint-disable-next-line unicorn/prefer-includes
        SUPPORTED_PARSER_MEDIA_TYPES.indexOf(cfg.PARSER_MEDIA_TYPE) === -1 ? DEFAULT_PARSER_MEDIA_TYPE : cfg.PARSER_MEDIA_TYPE;
        transformCaseFunc = PARSER_MEDIA_TYPE === "application/xhtml+xml" ? stringToString : stringToLowerCase;
        ALLOWED_TAGS = objectHasOwnProperty(cfg, "ALLOWED_TAGS") ? addToSet({}, cfg.ALLOWED_TAGS, transformCaseFunc) : DEFAULT_ALLOWED_TAGS;
        ALLOWED_ATTR = objectHasOwnProperty(cfg, "ALLOWED_ATTR") ? addToSet({}, cfg.ALLOWED_ATTR, transformCaseFunc) : DEFAULT_ALLOWED_ATTR;
        ALLOWED_NAMESPACES = objectHasOwnProperty(cfg, "ALLOWED_NAMESPACES") ? addToSet({}, cfg.ALLOWED_NAMESPACES, stringToString) : DEFAULT_ALLOWED_NAMESPACES;
        URI_SAFE_ATTRIBUTES = objectHasOwnProperty(cfg, "ADD_URI_SAFE_ATTR") ? addToSet(clone(DEFAULT_URI_SAFE_ATTRIBUTES), cfg.ADD_URI_SAFE_ATTR, transformCaseFunc) : DEFAULT_URI_SAFE_ATTRIBUTES;
        DATA_URI_TAGS = objectHasOwnProperty(cfg, "ADD_DATA_URI_TAGS") ? addToSet(clone(DEFAULT_DATA_URI_TAGS), cfg.ADD_DATA_URI_TAGS, transformCaseFunc) : DEFAULT_DATA_URI_TAGS;
        FORBID_CONTENTS = objectHasOwnProperty(cfg, "FORBID_CONTENTS") ? addToSet({}, cfg.FORBID_CONTENTS, transformCaseFunc) : DEFAULT_FORBID_CONTENTS;
        FORBID_TAGS = objectHasOwnProperty(cfg, "FORBID_TAGS") ? addToSet({}, cfg.FORBID_TAGS, transformCaseFunc) : clone({});
        FORBID_ATTR = objectHasOwnProperty(cfg, "FORBID_ATTR") ? addToSet({}, cfg.FORBID_ATTR, transformCaseFunc) : clone({});
        USE_PROFILES = objectHasOwnProperty(cfg, "USE_PROFILES") ? cfg.USE_PROFILES : false;
        ALLOW_ARIA_ATTR = cfg.ALLOW_ARIA_ATTR !== false;
        ALLOW_DATA_ATTR = cfg.ALLOW_DATA_ATTR !== false;
        ALLOW_UNKNOWN_PROTOCOLS = cfg.ALLOW_UNKNOWN_PROTOCOLS || false;
        ALLOW_SELF_CLOSE_IN_ATTR = cfg.ALLOW_SELF_CLOSE_IN_ATTR !== false;
        SAFE_FOR_TEMPLATES = cfg.SAFE_FOR_TEMPLATES || false;
        SAFE_FOR_XML = cfg.SAFE_FOR_XML !== false;
        WHOLE_DOCUMENT = cfg.WHOLE_DOCUMENT || false;
        RETURN_DOM = cfg.RETURN_DOM || false;
        RETURN_DOM_FRAGMENT = cfg.RETURN_DOM_FRAGMENT || false;
        RETURN_TRUSTED_TYPE = cfg.RETURN_TRUSTED_TYPE || false;
        FORCE_BODY = cfg.FORCE_BODY || false;
        SANITIZE_DOM = cfg.SANITIZE_DOM !== false;
        SANITIZE_NAMED_PROPS = cfg.SANITIZE_NAMED_PROPS || false;
        KEEP_CONTENT = cfg.KEEP_CONTENT !== false;
        IN_PLACE = cfg.IN_PLACE || false;
        IS_ALLOWED_URI$1 = cfg.ALLOWED_URI_REGEXP || IS_ALLOWED_URI;
        NAMESPACE = cfg.NAMESPACE || HTML_NAMESPACE;
        MATHML_TEXT_INTEGRATION_POINTS = cfg.MATHML_TEXT_INTEGRATION_POINTS || MATHML_TEXT_INTEGRATION_POINTS;
        HTML_INTEGRATION_POINTS = cfg.HTML_INTEGRATION_POINTS || HTML_INTEGRATION_POINTS;
        CUSTOM_ELEMENT_HANDLING = cfg.CUSTOM_ELEMENT_HANDLING || {};
        if (cfg.CUSTOM_ELEMENT_HANDLING && isRegexOrFunction(cfg.CUSTOM_ELEMENT_HANDLING.tagNameCheck)) {
          CUSTOM_ELEMENT_HANDLING.tagNameCheck = cfg.CUSTOM_ELEMENT_HANDLING.tagNameCheck;
        }
        if (cfg.CUSTOM_ELEMENT_HANDLING && isRegexOrFunction(cfg.CUSTOM_ELEMENT_HANDLING.attributeNameCheck)) {
          CUSTOM_ELEMENT_HANDLING.attributeNameCheck = cfg.CUSTOM_ELEMENT_HANDLING.attributeNameCheck;
        }
        if (cfg.CUSTOM_ELEMENT_HANDLING && typeof cfg.CUSTOM_ELEMENT_HANDLING.allowCustomizedBuiltInElements === "boolean") {
          CUSTOM_ELEMENT_HANDLING.allowCustomizedBuiltInElements = cfg.CUSTOM_ELEMENT_HANDLING.allowCustomizedBuiltInElements;
        }
        if (SAFE_FOR_TEMPLATES) {
          ALLOW_DATA_ATTR = false;
        }
        if (RETURN_DOM_FRAGMENT) {
          RETURN_DOM = true;
        }
        if (USE_PROFILES) {
          ALLOWED_TAGS = addToSet({}, text);
          ALLOWED_ATTR = [];
          if (USE_PROFILES.html === true) {
            addToSet(ALLOWED_TAGS, html$1);
            addToSet(ALLOWED_ATTR, html);
          }
          if (USE_PROFILES.svg === true) {
            addToSet(ALLOWED_TAGS, svg$1);
            addToSet(ALLOWED_ATTR, svg);
            addToSet(ALLOWED_ATTR, xml);
          }
          if (USE_PROFILES.svgFilters === true) {
            addToSet(ALLOWED_TAGS, svgFilters);
            addToSet(ALLOWED_ATTR, svg);
            addToSet(ALLOWED_ATTR, xml);
          }
          if (USE_PROFILES.mathMl === true) {
            addToSet(ALLOWED_TAGS, mathMl$1);
            addToSet(ALLOWED_ATTR, mathMl);
            addToSet(ALLOWED_ATTR, xml);
          }
        }
        if (cfg.ADD_TAGS) {
          if (ALLOWED_TAGS === DEFAULT_ALLOWED_TAGS) {
            ALLOWED_TAGS = clone(ALLOWED_TAGS);
          }
          addToSet(ALLOWED_TAGS, cfg.ADD_TAGS, transformCaseFunc);
        }
        if (cfg.ADD_ATTR) {
          if (ALLOWED_ATTR === DEFAULT_ALLOWED_ATTR) {
            ALLOWED_ATTR = clone(ALLOWED_ATTR);
          }
          addToSet(ALLOWED_ATTR, cfg.ADD_ATTR, transformCaseFunc);
        }
        if (cfg.ADD_URI_SAFE_ATTR) {
          addToSet(URI_SAFE_ATTRIBUTES, cfg.ADD_URI_SAFE_ATTR, transformCaseFunc);
        }
        if (cfg.FORBID_CONTENTS) {
          if (FORBID_CONTENTS === DEFAULT_FORBID_CONTENTS) {
            FORBID_CONTENTS = clone(FORBID_CONTENTS);
          }
          addToSet(FORBID_CONTENTS, cfg.FORBID_CONTENTS, transformCaseFunc);
        }
        if (KEEP_CONTENT) {
          ALLOWED_TAGS["#text"] = true;
        }
        if (WHOLE_DOCUMENT) {
          addToSet(ALLOWED_TAGS, ["html", "head", "body"]);
        }
        if (ALLOWED_TAGS.table) {
          addToSet(ALLOWED_TAGS, ["tbody"]);
          delete FORBID_TAGS.tbody;
        }
        if (cfg.TRUSTED_TYPES_POLICY) {
          if (typeof cfg.TRUSTED_TYPES_POLICY.createHTML !== "function") {
            throw typeErrorCreate('TRUSTED_TYPES_POLICY configuration option must provide a "createHTML" hook.');
          }
          if (typeof cfg.TRUSTED_TYPES_POLICY.createScriptURL !== "function") {
            throw typeErrorCreate('TRUSTED_TYPES_POLICY configuration option must provide a "createScriptURL" hook.');
          }
          trustedTypesPolicy = cfg.TRUSTED_TYPES_POLICY;
          emptyHTML = trustedTypesPolicy.createHTML("");
        } else {
          if (trustedTypesPolicy === void 0) {
            trustedTypesPolicy = _createTrustedTypesPolicy(trustedTypes, currentScript);
          }
          if (trustedTypesPolicy !== null && typeof emptyHTML === "string") {
            emptyHTML = trustedTypesPolicy.createHTML("");
          }
        }
        if (freeze) {
          freeze(cfg);
        }
        CONFIG = cfg;
      };
      const ALL_SVG_TAGS = addToSet({}, [...svg$1, ...svgFilters, ...svgDisallowed]);
      const ALL_MATHML_TAGS = addToSet({}, [...mathMl$1, ...mathMlDisallowed]);
      const _checkValidNamespace = function _checkValidNamespace2(element) {
        let parent = getParentNode(element);
        if (!parent || !parent.tagName) {
          parent = {
            namespaceURI: NAMESPACE,
            tagName: "template"
          };
        }
        const tagName = stringToLowerCase(element.tagName);
        const parentTagName = stringToLowerCase(parent.tagName);
        if (!ALLOWED_NAMESPACES[element.namespaceURI]) {
          return false;
        }
        if (element.namespaceURI === SVG_NAMESPACE) {
          if (parent.namespaceURI === HTML_NAMESPACE) {
            return tagName === "svg";
          }
          if (parent.namespaceURI === MATHML_NAMESPACE) {
            return tagName === "svg" && (parentTagName === "annotation-xml" || MATHML_TEXT_INTEGRATION_POINTS[parentTagName]);
          }
          return Boolean(ALL_SVG_TAGS[tagName]);
        }
        if (element.namespaceURI === MATHML_NAMESPACE) {
          if (parent.namespaceURI === HTML_NAMESPACE) {
            return tagName === "math";
          }
          if (parent.namespaceURI === SVG_NAMESPACE) {
            return tagName === "math" && HTML_INTEGRATION_POINTS[parentTagName];
          }
          return Boolean(ALL_MATHML_TAGS[tagName]);
        }
        if (element.namespaceURI === HTML_NAMESPACE) {
          if (parent.namespaceURI === SVG_NAMESPACE && !HTML_INTEGRATION_POINTS[parentTagName]) {
            return false;
          }
          if (parent.namespaceURI === MATHML_NAMESPACE && !MATHML_TEXT_INTEGRATION_POINTS[parentTagName]) {
            return false;
          }
          return !ALL_MATHML_TAGS[tagName] && (COMMON_SVG_AND_HTML_ELEMENTS[tagName] || !ALL_SVG_TAGS[tagName]);
        }
        if (PARSER_MEDIA_TYPE === "application/xhtml+xml" && ALLOWED_NAMESPACES[element.namespaceURI]) {
          return true;
        }
        return false;
      };
      const _forceRemove = function _forceRemove2(node2) {
        arrayPush(DOMPurify.removed, {
          element: node2
        });
        try {
          getParentNode(node2).removeChild(node2);
        } catch (_15) {
          remove4(node2);
        }
      };
      const _removeAttribute = function _removeAttribute2(name, element) {
        try {
          arrayPush(DOMPurify.removed, {
            attribute: element.getAttributeNode(name),
            from: element
          });
        } catch (_15) {
          arrayPush(DOMPurify.removed, {
            attribute: null,
            from: element
          });
        }
        element.removeAttribute(name);
        if (name === "is") {
          if (RETURN_DOM || RETURN_DOM_FRAGMENT) {
            try {
              _forceRemove(element);
            } catch (_15) {
            }
          } else {
            try {
              element.setAttribute(name, "");
            } catch (_15) {
            }
          }
        }
      };
      const _initDocument = function _initDocument2(dirty) {
        let doc = null;
        let leadingWhitespace = null;
        if (FORCE_BODY) {
          dirty = "<remove></remove>" + dirty;
        } else {
          const matches2 = stringMatch(dirty, /^[\r\n\t ]+/);
          leadingWhitespace = matches2 && matches2[0];
        }
        if (PARSER_MEDIA_TYPE === "application/xhtml+xml" && NAMESPACE === HTML_NAMESPACE) {
          dirty = '<html xmlns="http://www.w3.org/1999/xhtml"><head></head><body>' + dirty + "</body></html>";
        }
        const dirtyPayload = trustedTypesPolicy ? trustedTypesPolicy.createHTML(dirty) : dirty;
        if (NAMESPACE === HTML_NAMESPACE) {
          try {
            doc = new DOMParser().parseFromString(dirtyPayload, PARSER_MEDIA_TYPE);
          } catch (_15) {
          }
        }
        if (!doc || !doc.documentElement) {
          doc = implementation.createDocument(NAMESPACE, "template", null);
          try {
            doc.documentElement.innerHTML = IS_EMPTY_INPUT ? emptyHTML : dirtyPayload;
          } catch (_15) {
          }
        }
        const body = doc.body || doc.documentElement;
        if (dirty && leadingWhitespace) {
          body.insertBefore(document2.createTextNode(leadingWhitespace), body.childNodes[0] || null);
        }
        if (NAMESPACE === HTML_NAMESPACE) {
          return getElementsByTagName.call(doc, WHOLE_DOCUMENT ? "html" : "body")[0];
        }
        return WHOLE_DOCUMENT ? doc.documentElement : body;
      };
      const _createNodeIterator = function _createNodeIterator2(root) {
        return createNodeIterator.call(
          root.ownerDocument || root,
          root,
          // eslint-disable-next-line no-bitwise
          NodeFilter2.SHOW_ELEMENT | NodeFilter2.SHOW_COMMENT | NodeFilter2.SHOW_TEXT | NodeFilter2.SHOW_PROCESSING_INSTRUCTION | NodeFilter2.SHOW_CDATA_SECTION,
          null
        );
      };
      const _isClobbered = function _isClobbered2(element) {
        return element instanceof HTMLFormElement && (typeof element.nodeName !== "string" || typeof element.textContent !== "string" || typeof element.removeChild !== "function" || !(element.attributes instanceof NamedNodeMap) || typeof element.removeAttribute !== "function" || typeof element.setAttribute !== "function" || typeof element.namespaceURI !== "string" || typeof element.insertBefore !== "function" || typeof element.hasChildNodes !== "function");
      };
      const _isNode = function _isNode2(value) {
        return typeof Node2 === "function" && value instanceof Node2;
      };
      function _executeHooks(hooks2, currentNode, data) {
        arrayForEach(hooks2, (hook) => {
          hook.call(DOMPurify, currentNode, data, CONFIG);
        });
      }
      const _sanitizeElements = function _sanitizeElements2(currentNode) {
        let content = null;
        _executeHooks(hooks.beforeSanitizeElements, currentNode, null);
        if (_isClobbered(currentNode)) {
          _forceRemove(currentNode);
          return true;
        }
        const tagName = transformCaseFunc(currentNode.nodeName);
        _executeHooks(hooks.uponSanitizeElement, currentNode, {
          tagName,
          allowedTags: ALLOWED_TAGS
        });
        if (SAFE_FOR_XML && currentNode.hasChildNodes() && !_isNode(currentNode.firstElementChild) && regExpTest(/<[/\w!]/g, currentNode.innerHTML) && regExpTest(/<[/\w!]/g, currentNode.textContent)) {
          _forceRemove(currentNode);
          return true;
        }
        if (currentNode.nodeType === NODE_TYPE2.progressingInstruction) {
          _forceRemove(currentNode);
          return true;
        }
        if (SAFE_FOR_XML && currentNode.nodeType === NODE_TYPE2.comment && regExpTest(/<[/\w]/g, currentNode.data)) {
          _forceRemove(currentNode);
          return true;
        }
        if (!ALLOWED_TAGS[tagName] || FORBID_TAGS[tagName]) {
          if (!FORBID_TAGS[tagName] && _isBasicCustomElement(tagName)) {
            if (CUSTOM_ELEMENT_HANDLING.tagNameCheck instanceof RegExp && regExpTest(CUSTOM_ELEMENT_HANDLING.tagNameCheck, tagName)) {
              return false;
            }
            if (CUSTOM_ELEMENT_HANDLING.tagNameCheck instanceof Function && CUSTOM_ELEMENT_HANDLING.tagNameCheck(tagName)) {
              return false;
            }
          }
          if (KEEP_CONTENT && !FORBID_CONTENTS[tagName]) {
            const parentNode = getParentNode(currentNode) || currentNode.parentNode;
            const childNodes = getChildNodes(currentNode) || currentNode.childNodes;
            if (childNodes && parentNode) {
              const childCount = childNodes.length;
              for (let i2 = childCount - 1; i2 >= 0; --i2) {
                const childClone = cloneNode(childNodes[i2], true);
                childClone.__removalCount = (currentNode.__removalCount || 0) + 1;
                parentNode.insertBefore(childClone, getNextSibling(currentNode));
              }
            }
          }
          _forceRemove(currentNode);
          return true;
        }
        if (currentNode instanceof Element2 && !_checkValidNamespace(currentNode)) {
          _forceRemove(currentNode);
          return true;
        }
        if ((tagName === "noscript" || tagName === "noembed" || tagName === "noframes") && regExpTest(/<\/no(script|embed|frames)/i, currentNode.innerHTML)) {
          _forceRemove(currentNode);
          return true;
        }
        if (SAFE_FOR_TEMPLATES && currentNode.nodeType === NODE_TYPE2.text) {
          content = currentNode.textContent;
          arrayForEach([MUSTACHE_EXPR2, ERB_EXPR2, TMPLIT_EXPR2], (expr) => {
            content = stringReplace(content, expr, " ");
          });
          if (currentNode.textContent !== content) {
            arrayPush(DOMPurify.removed, {
              element: currentNode.cloneNode()
            });
            currentNode.textContent = content;
          }
        }
        _executeHooks(hooks.afterSanitizeElements, currentNode, null);
        return false;
      };
      const _isValidAttribute = function _isValidAttribute2(lcTag, lcName, value) {
        if (SANITIZE_DOM && (lcName === "id" || lcName === "name") && (value in document2 || value in formElement)) {
          return false;
        }
        if (ALLOW_DATA_ATTR && !FORBID_ATTR[lcName] && regExpTest(DATA_ATTR2, lcName)) ;
        else if (ALLOW_ARIA_ATTR && regExpTest(ARIA_ATTR2, lcName)) ;
        else if (!ALLOWED_ATTR[lcName] || FORBID_ATTR[lcName]) {
          if (
            // First condition does a very basic check if a) it's basically a valid custom element tagname AND
            // b) if the tagName passes whatever the user has configured for CUSTOM_ELEMENT_HANDLING.tagNameCheck
            // and c) if the attribute name passes whatever the user has configured for CUSTOM_ELEMENT_HANDLING.attributeNameCheck
            _isBasicCustomElement(lcTag) && (CUSTOM_ELEMENT_HANDLING.tagNameCheck instanceof RegExp && regExpTest(CUSTOM_ELEMENT_HANDLING.tagNameCheck, lcTag) || CUSTOM_ELEMENT_HANDLING.tagNameCheck instanceof Function && CUSTOM_ELEMENT_HANDLING.tagNameCheck(lcTag)) && (CUSTOM_ELEMENT_HANDLING.attributeNameCheck instanceof RegExp && regExpTest(CUSTOM_ELEMENT_HANDLING.attributeNameCheck, lcName) || CUSTOM_ELEMENT_HANDLING.attributeNameCheck instanceof Function && CUSTOM_ELEMENT_HANDLING.attributeNameCheck(lcName, lcTag)) || // Alternative, second condition checks if it's an `is`-attribute, AND
            // the value passes whatever the user has configured for CUSTOM_ELEMENT_HANDLING.tagNameCheck
            lcName === "is" && CUSTOM_ELEMENT_HANDLING.allowCustomizedBuiltInElements && (CUSTOM_ELEMENT_HANDLING.tagNameCheck instanceof RegExp && regExpTest(CUSTOM_ELEMENT_HANDLING.tagNameCheck, value) || CUSTOM_ELEMENT_HANDLING.tagNameCheck instanceof Function && CUSTOM_ELEMENT_HANDLING.tagNameCheck(value))
          ) ;
          else {
            return false;
          }
        } else if (URI_SAFE_ATTRIBUTES[lcName]) ;
        else if (regExpTest(IS_ALLOWED_URI$1, stringReplace(value, ATTR_WHITESPACE2, ""))) ;
        else if ((lcName === "src" || lcName === "xlink:href" || lcName === "href") && lcTag !== "script" && stringIndexOf(value, "data:") === 0 && DATA_URI_TAGS[lcTag]) ;
        else if (ALLOW_UNKNOWN_PROTOCOLS && !regExpTest(IS_SCRIPT_OR_DATA2, stringReplace(value, ATTR_WHITESPACE2, ""))) ;
        else if (value) {
          return false;
        } else ;
        return true;
      };
      const _isBasicCustomElement = function _isBasicCustomElement2(tagName) {
        return tagName !== "annotation-xml" && stringMatch(tagName, CUSTOM_ELEMENT2);
      };
      const _sanitizeAttributes = function _sanitizeAttributes2(currentNode) {
        _executeHooks(hooks.beforeSanitizeAttributes, currentNode, null);
        const {
          attributes
        } = currentNode;
        if (!attributes || _isClobbered(currentNode)) {
          return;
        }
        const hookEvent = {
          attrName: "",
          attrValue: "",
          keepAttr: true,
          allowedAttributes: ALLOWED_ATTR,
          forceKeepAttr: void 0
        };
        let l2 = attributes.length;
        while (l2--) {
          const attr = attributes[l2];
          const {
            name,
            namespaceURI,
            value: attrValue
          } = attr;
          const lcName = transformCaseFunc(name);
          const initValue = attrValue;
          let value = name === "value" ? initValue : stringTrim(initValue);
          hookEvent.attrName = lcName;
          hookEvent.attrValue = value;
          hookEvent.keepAttr = true;
          hookEvent.forceKeepAttr = void 0;
          _executeHooks(hooks.uponSanitizeAttribute, currentNode, hookEvent);
          value = hookEvent.attrValue;
          if (SANITIZE_NAMED_PROPS && (lcName === "id" || lcName === "name")) {
            _removeAttribute(name, currentNode);
            value = SANITIZE_NAMED_PROPS_PREFIX + value;
          }
          if (SAFE_FOR_XML && regExpTest(/((--!?|])>)|<\/(style|title|textarea)/i, value)) {
            _removeAttribute(name, currentNode);
            continue;
          }
          if (lcName === "attributename" && stringMatch(value, "href")) {
            _removeAttribute(name, currentNode);
            continue;
          }
          if (hookEvent.forceKeepAttr) {
            continue;
          }
          if (!hookEvent.keepAttr) {
            _removeAttribute(name, currentNode);
            continue;
          }
          if (!ALLOW_SELF_CLOSE_IN_ATTR && regExpTest(/\/>/i, value)) {
            _removeAttribute(name, currentNode);
            continue;
          }
          if (SAFE_FOR_TEMPLATES) {
            arrayForEach([MUSTACHE_EXPR2, ERB_EXPR2, TMPLIT_EXPR2], (expr) => {
              value = stringReplace(value, expr, " ");
            });
          }
          const lcTag = transformCaseFunc(currentNode.nodeName);
          if (!_isValidAttribute(lcTag, lcName, value)) {
            _removeAttribute(name, currentNode);
            continue;
          }
          if (trustedTypesPolicy && typeof trustedTypes === "object" && typeof trustedTypes.getAttributeType === "function") {
            if (namespaceURI) ;
            else {
              switch (trustedTypes.getAttributeType(lcTag, lcName)) {
                case "TrustedHTML": {
                  value = trustedTypesPolicy.createHTML(value);
                  break;
                }
                case "TrustedScriptURL": {
                  value = trustedTypesPolicy.createScriptURL(value);
                  break;
                }
              }
            }
          }
          if (value !== initValue) {
            try {
              if (namespaceURI) {
                currentNode.setAttributeNS(namespaceURI, name, value);
              } else {
                currentNode.setAttribute(name, value);
              }
              if (_isClobbered(currentNode)) {
                _forceRemove(currentNode);
              } else {
                arrayPop(DOMPurify.removed);
              }
            } catch (_15) {
              _removeAttribute(name, currentNode);
            }
          }
        }
        _executeHooks(hooks.afterSanitizeAttributes, currentNode, null);
      };
      const _sanitizeShadowDOM = function _sanitizeShadowDOM2(fragment) {
        let shadowNode = null;
        const shadowIterator = _createNodeIterator(fragment);
        _executeHooks(hooks.beforeSanitizeShadowDOM, fragment, null);
        while (shadowNode = shadowIterator.nextNode()) {
          _executeHooks(hooks.uponSanitizeShadowNode, shadowNode, null);
          _sanitizeElements(shadowNode);
          _sanitizeAttributes(shadowNode);
          if (shadowNode.content instanceof DocumentFragment) {
            _sanitizeShadowDOM2(shadowNode.content);
          }
        }
        _executeHooks(hooks.afterSanitizeShadowDOM, fragment, null);
      };
      DOMPurify.sanitize = function(dirty) {
        let cfg = arguments.length > 1 && arguments[1] !== void 0 ? arguments[1] : {};
        let body = null;
        let importedNode = null;
        let currentNode = null;
        let returnNode = null;
        IS_EMPTY_INPUT = !dirty;
        if (IS_EMPTY_INPUT) {
          dirty = "<!-->";
        }
        if (typeof dirty !== "string" && !_isNode(dirty)) {
          if (typeof dirty.toString === "function") {
            dirty = dirty.toString();
            if (typeof dirty !== "string") {
              throw typeErrorCreate("dirty is not a string, aborting");
            }
          } else {
            throw typeErrorCreate("toString is not a function");
          }
        }
        if (!DOMPurify.isSupported) {
          return dirty;
        }
        if (!SET_CONFIG) {
          _parseConfig(cfg);
        }
        DOMPurify.removed = [];
        if (typeof dirty === "string") {
          IN_PLACE = false;
        }
        if (IN_PLACE) {
          if (dirty.nodeName) {
            const tagName = transformCaseFunc(dirty.nodeName);
            if (!ALLOWED_TAGS[tagName] || FORBID_TAGS[tagName]) {
              throw typeErrorCreate("root node is forbidden and cannot be sanitized in-place");
            }
          }
        } else if (dirty instanceof Node2) {
          body = _initDocument("<!---->");
          importedNode = body.ownerDocument.importNode(dirty, true);
          if (importedNode.nodeType === NODE_TYPE2.element && importedNode.nodeName === "BODY") {
            body = importedNode;
          } else if (importedNode.nodeName === "HTML") {
            body = importedNode;
          } else {
            body.appendChild(importedNode);
          }
        } else {
          if (!RETURN_DOM && !SAFE_FOR_TEMPLATES && !WHOLE_DOCUMENT && // eslint-disable-next-line unicorn/prefer-includes
          dirty.indexOf("<") === -1) {
            return trustedTypesPolicy && RETURN_TRUSTED_TYPE ? trustedTypesPolicy.createHTML(dirty) : dirty;
          }
          body = _initDocument(dirty);
          if (!body) {
            return RETURN_DOM ? null : RETURN_TRUSTED_TYPE ? emptyHTML : "";
          }
        }
        if (body && FORCE_BODY) {
          _forceRemove(body.firstChild);
        }
        const nodeIterator = _createNodeIterator(IN_PLACE ? dirty : body);
        while (currentNode = nodeIterator.nextNode()) {
          _sanitizeElements(currentNode);
          _sanitizeAttributes(currentNode);
          if (currentNode.content instanceof DocumentFragment) {
            _sanitizeShadowDOM(currentNode.content);
          }
        }
        if (IN_PLACE) {
          return dirty;
        }
        if (RETURN_DOM) {
          if (RETURN_DOM_FRAGMENT) {
            returnNode = createDocumentFragment.call(body.ownerDocument);
            while (body.firstChild) {
              returnNode.appendChild(body.firstChild);
            }
          } else {
            returnNode = body;
          }
          if (ALLOWED_ATTR.shadowroot || ALLOWED_ATTR.shadowrootmode) {
            returnNode = importNode.call(originalDocument, returnNode, true);
          }
          return returnNode;
        }
        let serializedHTML = WHOLE_DOCUMENT ? body.outerHTML : body.innerHTML;
        if (WHOLE_DOCUMENT && ALLOWED_TAGS["!doctype"] && body.ownerDocument && body.ownerDocument.doctype && body.ownerDocument.doctype.name && regExpTest(DOCTYPE_NAME, body.ownerDocument.doctype.name)) {
          serializedHTML = "<!DOCTYPE " + body.ownerDocument.doctype.name + ">\n" + serializedHTML;
        }
        if (SAFE_FOR_TEMPLATES) {
          arrayForEach([MUSTACHE_EXPR2, ERB_EXPR2, TMPLIT_EXPR2], (expr) => {
            serializedHTML = stringReplace(serializedHTML, expr, " ");
          });
        }
        return trustedTypesPolicy && RETURN_TRUSTED_TYPE ? trustedTypesPolicy.createHTML(serializedHTML) : serializedHTML;
      };
      DOMPurify.setConfig = function() {
        let cfg = arguments.length > 0 && arguments[0] !== void 0 ? arguments[0] : {};
        _parseConfig(cfg);
        SET_CONFIG = true;
      };
      DOMPurify.clearConfig = function() {
        CONFIG = null;
        SET_CONFIG = false;
      };
      DOMPurify.isValidAttribute = function(tag, attr, value) {
        if (!CONFIG) {
          _parseConfig({});
        }
        const lcTag = transformCaseFunc(tag);
        const lcName = transformCaseFunc(attr);
        return _isValidAttribute(lcTag, lcName, value);
      };
      DOMPurify.addHook = function(entryPoint, hookFunction) {
        if (typeof hookFunction !== "function") {
          return;
        }
        arrayPush(hooks[entryPoint], hookFunction);
      };
      DOMPurify.removeHook = function(entryPoint, hookFunction) {
        if (hookFunction !== void 0) {
          const index = arrayLastIndexOf(hooks[entryPoint], hookFunction);
          return index === -1 ? void 0 : arraySplice(hooks[entryPoint], index, 1)[0];
        }
        return arrayPop(hooks[entryPoint]);
      };
      DOMPurify.removeHooks = function(entryPoint) {
        hooks[entryPoint] = [];
      };
      DOMPurify.removeAllHooks = function() {
        hooks = _createHooksMap();
      };
      return DOMPurify;
    }
    var purify = createDOMPurify();
    module.exports = purify;
  }
});

// node_modules/mobx-react-lite/es/utils/assertEnvironment.js
var import_react8;
var init_assertEnvironment = __esm({
  "node_modules/mobx-react-lite/es/utils/assertEnvironment.js"() {
    init_mobx_esm();
    import_react8 = __toESM(require_react());
    if (!import_react8.useState) {
      throw new Error("mobx-react-lite requires React with Hooks support");
    }
    if (!makeObservable) {
      throw new Error("mobx-react-lite@3 requires mobx at least version 6 to be available");
    }
  }
});

// node_modules/mobx-react-lite/es/utils/reactBatchedUpdates.js
var import_react_dom;
var init_reactBatchedUpdates = __esm({
  "node_modules/mobx-react-lite/es/utils/reactBatchedUpdates.js"() {
    import_react_dom = __toESM(require_react_dom());
  }
});

// node_modules/mobx-react-lite/es/utils/observerBatching.js
function defaultNoopBatch(callback) {
  callback();
}
function observerBatching(reactionScheduler3) {
  if (!reactionScheduler3) {
    reactionScheduler3 = defaultNoopBatch;
    if (true) {
      console.warn("[MobX] Failed to get unstable_batched updates from react-dom / react-native");
    }
  }
  configure({ reactionScheduler: reactionScheduler3 });
}
var init_observerBatching = __esm({
  "node_modules/mobx-react-lite/es/utils/observerBatching.js"() {
    init_mobx_esm();
  }
});

// node_modules/mobx-react-lite/es/utils/utils.js
function useDeprecated(msg) {
  if (!deprecatedMessages.includes(msg)) {
    deprecatedMessages.push(msg);
    console.warn(msg);
  }
}
var deprecatedMessages;
var init_utils2 = __esm({
  "node_modules/mobx-react-lite/es/utils/utils.js"() {
    deprecatedMessages = [];
  }
});

// node_modules/mobx-react-lite/es/utils/printDebugValue.js
function printDebugValue(v2) {
  return getDependencyTree(v2);
}
var init_printDebugValue = __esm({
  "node_modules/mobx-react-lite/es/utils/printDebugValue.js"() {
    init_mobx_esm();
  }
});

// node_modules/mobx-react-lite/es/staticRendering.js
function enableStaticRendering(enable) {
  globalIsUsingStaticRendering = enable;
}
function isUsingStaticRendering() {
  return globalIsUsingStaticRendering;
}
var globalIsUsingStaticRendering;
var init_staticRendering = __esm({
  "node_modules/mobx-react-lite/es/staticRendering.js"() {
    globalIsUsingStaticRendering = false;
  }
});

// node_modules/mobx-react-lite/es/utils/UniversalFinalizationRegistry.js
var REGISTRY_FINALIZE_AFTER, REGISTRY_SWEEP_INTERVAL, TimerBasedFinalizationRegistry, UniversalFinalizationRegistry;
var init_UniversalFinalizationRegistry = __esm({
  "node_modules/mobx-react-lite/es/utils/UniversalFinalizationRegistry.js"() {
    REGISTRY_FINALIZE_AFTER = 1e4;
    REGISTRY_SWEEP_INTERVAL = 1e4;
    TimerBasedFinalizationRegistry = /** @class */
    (function() {
      function TimerBasedFinalizationRegistry2(finalize) {
        var _this = this;
        Object.defineProperty(this, "finalize", {
          enumerable: true,
          configurable: true,
          writable: true,
          value: finalize
        });
        Object.defineProperty(this, "registrations", {
          enumerable: true,
          configurable: true,
          writable: true,
          value: /* @__PURE__ */ new Map()
        });
        Object.defineProperty(this, "sweepTimeout", {
          enumerable: true,
          configurable: true,
          writable: true,
          value: void 0
        });
        Object.defineProperty(this, "sweep", {
          enumerable: true,
          configurable: true,
          writable: true,
          value: function(maxAge) {
            if (maxAge === void 0) {
              maxAge = REGISTRY_FINALIZE_AFTER;
            }
            clearTimeout(_this.sweepTimeout);
            _this.sweepTimeout = void 0;
            var now = Date.now();
            _this.registrations.forEach(function(registration, token2) {
              if (now - registration.registeredAt >= maxAge) {
                _this.finalize(registration.value);
                _this.registrations.delete(token2);
              }
            });
            if (_this.registrations.size > 0) {
              _this.scheduleSweep();
            }
          }
        });
        Object.defineProperty(this, "finalizeAllImmediately", {
          enumerable: true,
          configurable: true,
          writable: true,
          value: function() {
            _this.sweep(0);
          }
        });
      }
      Object.defineProperty(TimerBasedFinalizationRegistry2.prototype, "register", {
        enumerable: false,
        configurable: true,
        writable: true,
        value: function(target, value, token2) {
          this.registrations.set(token2, {
            value,
            registeredAt: Date.now()
          });
          this.scheduleSweep();
        }
      });
      Object.defineProperty(TimerBasedFinalizationRegistry2.prototype, "unregister", {
        enumerable: false,
        configurable: true,
        writable: true,
        value: function(token2) {
          this.registrations.delete(token2);
        }
      });
      Object.defineProperty(TimerBasedFinalizationRegistry2.prototype, "scheduleSweep", {
        enumerable: false,
        configurable: true,
        writable: true,
        value: function() {
          if (this.sweepTimeout === void 0) {
            this.sweepTimeout = setTimeout(this.sweep, REGISTRY_SWEEP_INTERVAL);
          }
        }
      });
      return TimerBasedFinalizationRegistry2;
    })();
    UniversalFinalizationRegistry = typeof FinalizationRegistry !== "undefined" ? FinalizationRegistry : TimerBasedFinalizationRegistry;
  }
});

// node_modules/mobx-react-lite/es/utils/observerFinalizationRegistry.js
var observerFinalizationRegistry;
var init_observerFinalizationRegistry = __esm({
  "node_modules/mobx-react-lite/es/utils/observerFinalizationRegistry.js"() {
    init_UniversalFinalizationRegistry();
    observerFinalizationRegistry = new UniversalFinalizationRegistry(function(adm) {
      var _a3;
      (_a3 = adm.reaction) === null || _a3 === void 0 ? void 0 : _a3.dispose();
      adm.reaction = null;
    });
  }
});

// node_modules/mobx-react-lite/es/useObserver.js
function createReaction(adm) {
  adm.reaction = new Reaction("observer".concat(adm.name), function() {
    var _a3;
    adm.stateVersion = Symbol();
    (_a3 = adm.onStoreChange) === null || _a3 === void 0 ? void 0 : _a3.call(adm);
  });
}
function useObserver(render, baseComponentName) {
  if (baseComponentName === void 0) {
    baseComponentName = "observed";
  }
  if (isUsingStaticRendering()) {
    return render();
  }
  var admRef = import_react9.default.useRef(null);
  if (!admRef.current) {
    var adm_1 = {
      reaction: null,
      onStoreChange: null,
      stateVersion: Symbol(),
      name: baseComponentName,
      subscribe: function(onStoreChange) {
        observerFinalizationRegistry.unregister(adm_1);
        adm_1.onStoreChange = onStoreChange;
        if (!adm_1.reaction) {
          createReaction(adm_1);
          adm_1.stateVersion = Symbol();
        }
        return function() {
          var _a3;
          adm_1.onStoreChange = null;
          (_a3 = adm_1.reaction) === null || _a3 === void 0 ? void 0 : _a3.dispose();
          adm_1.reaction = null;
        };
      },
      getSnapshot: function() {
        return adm_1.stateVersion;
      }
    };
    admRef.current = adm_1;
  }
  var adm = admRef.current;
  if (!adm.reaction) {
    createReaction(adm);
    observerFinalizationRegistry.register(admRef, adm, adm);
  }
  import_react9.default.useDebugValue(adm.reaction, printDebugValue);
  (0, import_shim.useSyncExternalStore)(
    // Both of these must be stable, otherwise it would keep resubscribing every render.
    adm.subscribe,
    adm.getSnapshot,
    adm.getSnapshot
  );
  var renderResult;
  var exception;
  adm.reaction.track(function() {
    try {
      renderResult = render();
    } catch (e) {
      exception = e;
    }
  });
  if (exception) {
    throw exception;
  }
  return renderResult;
}
var import_react9, import_shim;
var init_useObserver = __esm({
  "node_modules/mobx-react-lite/es/useObserver.js"() {
    init_mobx_esm();
    import_react9 = __toESM(require_react());
    init_printDebugValue();
    init_staticRendering();
    init_observerFinalizationRegistry();
    import_shim = __toESM(require_shim());
  }
});

// node_modules/mobx-react-lite/es/observer.js
function observer(baseComponent, options) {
  var _a3;
  if (warnObserverOptionsDeprecated && options) {
    warnObserverOptionsDeprecated = false;
    console.warn("[mobx-react-lite] `observer(fn, { forwardRef: true })` is deprecated, use `observer(React.forwardRef(fn))`");
  }
  if (ReactMemoSymbol && baseComponent["$$typeof"] === ReactMemoSymbol) {
    throw new Error("[mobx-react-lite] You are trying to use `observer` on a function component wrapped in either another `observer` or `React.memo`. The observer already applies 'React.memo' for you.");
  }
  if (isUsingStaticRendering()) {
    return baseComponent;
  }
  var useForwardRef = (_a3 = options === null || options === void 0 ? void 0 : options.forwardRef) !== null && _a3 !== void 0 ? _a3 : false;
  var render = baseComponent;
  var baseComponentName = baseComponent.displayName || baseComponent.name;
  if (ReactForwardRefSymbol && baseComponent["$$typeof"] === ReactForwardRefSymbol) {
    useForwardRef = true;
    render = baseComponent["render"];
    if (typeof render !== "function") {
      throw new Error("[mobx-react-lite] `render` property of ForwardRef was not a function");
    }
  }
  var observerComponent = function(props, ref) {
    return useObserver(function() {
      return render(props, ref);
    }, baseComponentName);
  };
  observerComponent.displayName = baseComponent.displayName;
  if (isFunctionNameConfigurable2) {
    Object.defineProperty(observerComponent, "name", {
      value: baseComponent.name,
      writable: true,
      configurable: true
    });
  }
  if (baseComponent.contextTypes) {
    ;
    observerComponent.contextTypes = baseComponent.contextTypes;
    if (warnLegacyContextTypes) {
      warnLegacyContextTypes = false;
      console.warn("[mobx-react-lite] Support for Legacy Context in function components will be removed in the next major release.");
    }
  }
  if (useForwardRef) {
    observerComponent = (0, import_react10.forwardRef)(observerComponent);
  }
  observerComponent = (0, import_react10.memo)(observerComponent);
  copyStaticProperties(baseComponent, observerComponent);
  if (true) {
    Object.defineProperty(observerComponent, "contextTypes", {
      set: function() {
        var _a4, _b2;
        throw new Error("[mobx-react-lite] `".concat(this.displayName || ((_a4 = this.type) === null || _a4 === void 0 ? void 0 : _a4.displayName) || ((_b2 = this.type) === null || _b2 === void 0 ? void 0 : _b2.name) || "Component", ".contextTypes` must be set before applying `observer`."));
      }
    });
  }
  return observerComponent;
}
function copyStaticProperties(base, target) {
  Object.keys(base).forEach(function(key2) {
    if (!hoistBlackList[key2]) {
      Object.defineProperty(target, key2, Object.getOwnPropertyDescriptor(base, key2));
    }
  });
}
var import_react10, _a, _b, warnObserverOptionsDeprecated, warnLegacyContextTypes, hasSymbol, isFunctionNameConfigurable2, ReactForwardRefSymbol, ReactMemoSymbol, hoistBlackList;
var init_observer = __esm({
  "node_modules/mobx-react-lite/es/observer.js"() {
    import_react10 = __toESM(require_react());
    init_staticRendering();
    init_useObserver();
    warnObserverOptionsDeprecated = true;
    warnLegacyContextTypes = true;
    hasSymbol = typeof Symbol === "function" && Symbol.for;
    isFunctionNameConfigurable2 = (_b = (_a = Object.getOwnPropertyDescriptor(function() {
    }, "name")) === null || _a === void 0 ? void 0 : _a.configurable) !== null && _b !== void 0 ? _b : false;
    ReactForwardRefSymbol = hasSymbol ? Symbol.for("react.forward_ref") : typeof import_react10.forwardRef === "function" && (0, import_react10.forwardRef)(function(props) {
      return null;
    })["$$typeof"];
    ReactMemoSymbol = hasSymbol ? Symbol.for("react.memo") : typeof import_react10.memo === "function" && (0, import_react10.memo)(function(props) {
      return null;
    })["$$typeof"];
    hoistBlackList = {
      $$typeof: true,
      render: true,
      compare: true,
      type: true,
      // Don't redefine `displayName`,
      // it's defined as getter-setter pair on `memo` (see #3192).
      displayName: true
    };
  }
});

// node_modules/mobx-react-lite/es/ObserverComponent.js
function ObserverComponent(_a3) {
  var children = _a3.children, render = _a3.render;
  if (children && render) {
    console.error("MobX Observer: Do not use children and render in the same time in `Observer`");
  }
  var component = children || render;
  if (typeof component !== "function") {
    return null;
  }
  return useObserver(component);
}
function ObserverPropsCheck(props, key2, componentName, location, propFullName) {
  var extraKey = key2 === "children" ? "render" : "children";
  var hasProp2 = typeof props[key2] === "function";
  var hasExtraProp = typeof props[extraKey] === "function";
  if (hasProp2 && hasExtraProp) {
    return new Error("MobX Observer: Do not use children and render in the same time in`" + componentName);
  }
  if (hasProp2 || hasExtraProp) {
    return null;
  }
  return new Error("Invalid prop `" + propFullName + "` of type `" + typeof props[key2] + "` supplied to `" + componentName + "`, expected `function`.");
}
var init_ObserverComponent = __esm({
  "node_modules/mobx-react-lite/es/ObserverComponent.js"() {
    init_useObserver();
    if (true) {
      ObserverComponent.propTypes = {
        children: ObserverPropsCheck,
        render: ObserverPropsCheck
      };
    }
    ObserverComponent.displayName = "Observer";
  }
});

// node_modules/mobx-react-lite/es/useLocalObservable.js
function useLocalObservable(initializer, annotations) {
  return (0, import_react11.useState)(function() {
    return observable(initializer(), annotations, { autoBind: true });
  })[0];
}
var import_react11;
var init_useLocalObservable = __esm({
  "node_modules/mobx-react-lite/es/useLocalObservable.js"() {
    init_mobx_esm();
    import_react11 = __toESM(require_react());
  }
});

// node_modules/mobx-react-lite/es/useAsObservableSource.js
function useAsObservableSource(current) {
  if (true)
    useDeprecated("[mobx-react-lite] 'useAsObservableSource' is deprecated, please store the values directly in an observable, for example by using 'useLocalObservable', and sync future updates using 'useEffect' when needed. See the README for examples.");
  var res = (0, import_react12.useState)(function() {
    return observable(current, {}, { deep: false });
  })[0];
  runInAction(function() {
    Object.assign(res, current);
  });
  return res;
}
var import_react12;
var init_useAsObservableSource = __esm({
  "node_modules/mobx-react-lite/es/useAsObservableSource.js"() {
    init_utils2();
    init_mobx_esm();
    import_react12 = __toESM(require_react());
  }
});

// node_modules/mobx-react-lite/es/useLocalStore.js
function useLocalStore(initializer, current) {
  if (true) {
    useDeprecated("[mobx-react-lite] 'useLocalStore' is deprecated, use 'useLocalObservable' instead.");
  }
  var source = current && useAsObservableSource(current);
  return (0, import_react13.useState)(function() {
    return observable(initializer(source), void 0, { autoBind: true });
  })[0];
}
var import_react13;
var init_useLocalStore = __esm({
  "node_modules/mobx-react-lite/es/useLocalStore.js"() {
    init_mobx_esm();
    import_react13 = __toESM(require_react());
    init_utils2();
    init_useAsObservableSource();
  }
});

// node_modules/mobx-react-lite/es/index.js
function useObserver2(fn, baseComponentName) {
  if (baseComponentName === void 0) {
    baseComponentName = "observed";
  }
  if (true) {
    useDeprecated("[mobx-react-lite] 'useObserver(fn)' is deprecated. Use `<Observer>{fn}</Observer>` instead, or wrap the entire component in `observer`.");
  }
  return useObserver(fn, baseComponentName);
}
function useStaticRendering(enable) {
  if (true) {
    console.warn("[mobx-react-lite] 'useStaticRendering' is deprecated, use 'enableStaticRendering' instead");
  }
  enableStaticRendering(enable);
}
var _a2, clearTimers;
var init_es = __esm({
  "node_modules/mobx-react-lite/es/index.js"() {
    init_assertEnvironment();
    init_reactBatchedUpdates();
    init_observerBatching();
    init_utils2();
    init_useObserver();
    init_staticRendering();
    init_observerFinalizationRegistry();
    init_staticRendering();
    init_observer();
    init_ObserverComponent();
    init_useLocalObservable();
    init_useLocalStore();
    init_useAsObservableSource();
    init_observerBatching();
    observerBatching(import_react_dom.unstable_batchedUpdates);
    clearTimers = (_a2 = observerFinalizationRegistry["finalizeAllImmediately"]) !== null && _a2 !== void 0 ? _a2 : (function() {
    });
  }
});

// node_modules/mobx-react/dist/mobxreact.esm.js
var mobxreact_esm_exports = {};
__export(mobxreact_esm_exports, {
  MobXProviderContext: () => MobXProviderContext,
  Observer: () => ObserverComponent,
  PropTypes: () => PropTypes,
  Provider: () => Provider,
  disposeOnUnmount: () => disposeOnUnmount,
  enableStaticRendering: () => enableStaticRendering,
  inject: () => inject,
  isUsingStaticRendering: () => isUsingStaticRendering,
  observer: () => observer2,
  observerBatching: () => observerBatching,
  useAsObservableSource: () => useAsObservableSource,
  useLocalObservable: () => useLocalObservable,
  useLocalStore: () => useLocalStore,
  useObserver: () => useObserver2,
  useStaticRendering: () => useStaticRendering
});
function shallowEqual(objA, objB) {
  if (is(objA, objB)) {
    return true;
  }
  if (typeof objA !== "object" || objA === null || typeof objB !== "object" || objB === null) {
    return false;
  }
  var keysA = Object.keys(objA);
  var keysB = Object.keys(objB);
  if (keysA.length !== keysB.length) {
    return false;
  }
  for (var i2 = 0; i2 < keysA.length; i2++) {
    if (!Object.hasOwnProperty.call(objB, keysA[i2]) || !is(objA[keysA[i2]], objB[keysA[i2]])) {
      return false;
    }
  }
  return true;
}
function is(x2, y2) {
  if (x2 === y2) {
    return x2 !== 0 || 1 / x2 === 1 / y2;
  } else {
    return x2 !== x2 && y2 !== y2;
  }
}
function copyStaticProperties2(base, target) {
  var protoProps = Object.getOwnPropertyNames(Object.getPrototypeOf(base));
  Object.getOwnPropertyNames(base).forEach(function(key2) {
    if (!hoistBlackList2[key2] && protoProps.indexOf(key2) === -1) {
      Object.defineProperty(target, key2, Object.getOwnPropertyDescriptor(base, key2));
    }
  });
}
function getMixins(target, methodName) {
  var mixins = target[mobxMixins] = target[mobxMixins] || {};
  var methodMixins = mixins[methodName] = mixins[methodName] || {};
  methodMixins.locks = methodMixins.locks || 0;
  methodMixins.methods = methodMixins.methods || [];
  return methodMixins;
}
function wrapper(realMethod, mixins) {
  var _this = this;
  for (var _len = arguments.length, args = new Array(_len > 2 ? _len - 2 : 0), _key = 2; _key < _len; _key++) {
    args[_key - 2] = arguments[_key];
  }
  mixins.locks++;
  try {
    var retVal;
    if (realMethod !== void 0 && realMethod !== null) {
      retVal = realMethod.apply(this, args);
    }
    return retVal;
  } finally {
    mixins.locks--;
    if (mixins.locks === 0) {
      mixins.methods.forEach(function(mx) {
        mx.apply(_this, args);
      });
    }
  }
}
function wrapFunction(realMethod, mixins) {
  var fn = function fn2() {
    for (var _len2 = arguments.length, args = new Array(_len2), _key2 = 0; _key2 < _len2; _key2++) {
      args[_key2] = arguments[_key2];
    }
    wrapper.call.apply(wrapper, [this, realMethod, mixins].concat(args));
  };
  return fn;
}
function patch(target, methodName, mixinMethod) {
  var mixins = getMixins(target, methodName);
  if (mixins.methods.indexOf(mixinMethod) < 0) {
    mixins.methods.push(mixinMethod);
  }
  var oldDefinition = Object.getOwnPropertyDescriptor(target, methodName);
  if (oldDefinition && oldDefinition[mobxPatchedDefinition]) {
    return;
  }
  var originalMethod = target[methodName];
  var newDefinition = createDefinition(target, methodName, oldDefinition ? oldDefinition.enumerable : void 0, mixins, originalMethod);
  Object.defineProperty(target, methodName, newDefinition);
}
function createDefinition(target, methodName, enumerable, mixins, originalMethod) {
  var _ref;
  var wrappedFunc = wrapFunction(originalMethod, mixins);
  return _ref = {}, _ref[mobxPatchedDefinition] = true, _ref.get = function get5() {
    return wrappedFunc;
  }, _ref.set = function set7(value) {
    if (this === target) {
      wrappedFunc = wrapFunction(value, mixins);
    } else {
      var newDefinition = createDefinition(this, methodName, enumerable, mixins, value);
      Object.defineProperty(this, methodName, newDefinition);
    }
  }, _ref.configurable = true, _ref.enumerable = enumerable, _ref;
}
function getAdministration2(component) {
  var _component$administra;
  return (_component$administra = component[administrationSymbol]) != null ? _component$administra : component[administrationSymbol] = {
    reaction: null,
    mounted: false,
    reactionInvalidatedBeforeMount: false,
    forceUpdate: null,
    name: getDisplayName(component.constructor),
    state: void 0,
    props: void 0,
    context: void 0
  };
}
function makeClassComponentObserver(componentClass) {
  var prototype = componentClass.prototype;
  if (componentClass[isMobXReactObserverSymbol]) {
    var displayName = getDisplayName(componentClass);
    throw new Error("The provided component class (" + displayName + ") has already been declared as an observer component.");
  } else {
    componentClass[isMobXReactObserverSymbol] = true;
  }
  if (prototype.componentWillReact) {
    throw new Error("The componentWillReact life-cycle event is no longer supported");
  }
  if (componentClass["__proto__"] !== import_react14.PureComponent) {
    if (!prototype.shouldComponentUpdate) {
      prototype.shouldComponentUpdate = observerSCU;
    } else if (prototype.shouldComponentUpdate !== observerSCU) {
      throw new Error("It is not allowed to use shouldComponentUpdate in observer based components.");
    }
  }
  if (true) {
    Object.defineProperties(prototype, observablePropDescriptors);
  }
  var originalRender = prototype.render;
  if (typeof originalRender !== "function") {
    var _displayName = getDisplayName(componentClass);
    throw new Error("[mobx-react] class component (" + _displayName + ") is missing `render` method.\n`observer` requires `render` being a function defined on prototype.\n`render = () => {}` or `render = function() {}` is not supported.");
  }
  prototype.render = function() {
    Object.defineProperty(this, "render", {
      // There is no safe way to replace render, therefore it's forbidden.
      configurable: false,
      writable: false,
      value: isUsingStaticRendering() ? originalRender : createReactiveRender.call(this, originalRender)
    });
    return this.render();
  };
  var originalComponentDidMount = prototype.componentDidMount;
  prototype.componentDidMount = function() {
    var _this = this;
    if (this.componentDidMount !== Object.getPrototypeOf(this).componentDidMount) {
      var _displayName2 = getDisplayName(componentClass);
      throw new Error("[mobx-react] `observer(" + _displayName2 + ").componentDidMount` must be defined on prototype.\n`componentDidMount = () => {}` or `componentDidMount = function() {}` is not supported.");
    }
    var admin = getAdministration2(this);
    admin.mounted = true;
    observerFinalizationRegistry.unregister(this);
    admin.forceUpdate = function() {
      return _this.forceUpdate();
    };
    if (!admin.reaction || admin.reactionInvalidatedBeforeMount) {
      admin.forceUpdate();
    }
    return originalComponentDidMount == null ? void 0 : originalComponentDidMount.apply(this, arguments);
  };
  patch(prototype, "componentWillUnmount", function() {
    var _admin$reaction;
    if (isUsingStaticRendering()) {
      return;
    }
    var admin = getAdministration2(this);
    (_admin$reaction = admin.reaction) == null || _admin$reaction.dispose();
    admin.reaction = null;
    admin.forceUpdate = null;
    admin.mounted = false;
    admin.reactionInvalidatedBeforeMount = false;
  });
  return componentClass;
}
function getDisplayName(componentClass) {
  return componentClass.displayName || componentClass.name || "<component>";
}
function createReactiveRender(originalRender) {
  var boundOriginalRender = originalRender.bind(this);
  var admin = getAdministration2(this);
  function reactiveRender() {
    if (!admin.reaction) {
      admin.reaction = createReaction2(admin);
      if (!admin.mounted) {
        observerFinalizationRegistry.register(this, admin, this);
      }
    }
    var error = void 0;
    var renderResult = void 0;
    admin.reaction.track(function() {
      try {
        renderResult = allowStateChanges(false, boundOriginalRender);
      } catch (e) {
        error = e;
      }
    });
    if (error) {
      throw error;
    }
    return renderResult;
  }
  return reactiveRender;
}
function createReaction2(admin) {
  return new Reaction(admin.name + ".render()", function() {
    if (!admin.mounted) {
      admin.reactionInvalidatedBeforeMount = true;
      return;
    }
    try {
      admin.forceUpdate == null || admin.forceUpdate();
    } catch (error) {
      var _admin$reaction2;
      (_admin$reaction2 = admin.reaction) == null || _admin$reaction2.dispose();
      admin.reaction = null;
    }
  });
}
function observerSCU(nextProps, nextState) {
  if (isUsingStaticRendering()) {
    console.warn("[mobx-react] It seems that a re-rendering of a React component is triggered while in static (server-side) mode. Please make sure components are rendered only once server-side.");
  }
  if (this.state !== nextState) {
    return true;
  }
  return !shallowEqual(this.props, nextProps);
}
function createObservablePropDescriptor(key2) {
  return {
    configurable: true,
    enumerable: true,
    get: function get5() {
      var admin = getAdministration2(this);
      var derivation = getGlobalState().trackingDerivation;
      if (derivation && derivation !== admin.reaction) {
        throw new Error('[mobx-react] Cannot read "' + admin.name + "." + key2 + `" in a reactive context, as it isn't observable.
                    Please use component lifecycle method to copy the value into a local observable first.
                    See https://github.com/mobxjs/mobx/blob/main/packages/mobx-react/README.md#note-on-using-props-and-state-in-derivations`);
      }
      return admin[key2];
    },
    set: function set7(value) {
      getAdministration2(this)[key2] = value;
    }
  };
}
function observer2(component, context) {
  if (context && context.kind !== "class") {
    throw new Error("The @observer decorator can be used on classes only");
  }
  if (component["isMobxInjector"] === true) {
    console.warn("Mobx observer: You are trying to use `observer` on a component that already has `inject`. Please apply `observer` before applying `inject`");
  }
  if (Object.prototype.isPrototypeOf.call(import_react14.Component, component) || Object.prototype.isPrototypeOf.call(import_react14.PureComponent, component)) {
    return makeClassComponentObserver(component);
  } else {
    return observer(component);
  }
}
function _extends3() {
  return _extends3 = Object.assign ? Object.assign.bind() : function(n) {
    for (var e = 1; e < arguments.length; e++) {
      var t = arguments[e];
      for (var r2 in t) ({}).hasOwnProperty.call(t, r2) && (n[r2] = t[r2]);
    }
    return n;
  }, _extends3.apply(null, arguments);
}
function _objectWithoutPropertiesLoose6(r2, e) {
  if (null == r2) return {};
  var t = {};
  for (var n in r2) if ({}.hasOwnProperty.call(r2, n)) {
    if (e.indexOf(n) >= 0) continue;
    t[n] = r2[n];
  }
  return t;
}
function Provider(props) {
  var children = props.children, stores = _objectWithoutPropertiesLoose6(props, _excluded6);
  var parentValue = import_react14.default.useContext(MobXProviderContext);
  var mutableProviderRef = import_react14.default.useRef(_extends3({}, parentValue, stores));
  var value = mutableProviderRef.current;
  if (true) {
    var newValue = _extends3({}, value, stores);
    if (!shallowEqual(value, newValue)) {
      throw new Error("MobX Provider: The set of provided stores has changed. See: https://github.com/mobxjs/mobx-react#the-set-of-provided-stores-has-changed-error.");
    }
  }
  return import_react14.default.createElement(MobXProviderContext.Provider, {
    value
  }, children);
}
function createStoreInjector(grabStoresFn, component, injectNames, makeReactive) {
  var Injector = import_react14.default.forwardRef(function(props, ref) {
    var newProps = _extends3({}, props);
    var context = import_react14.default.useContext(MobXProviderContext);
    Object.assign(newProps, grabStoresFn(context || {}, newProps) || {});
    if (ref) {
      newProps.ref = ref;
    }
    return import_react14.default.createElement(component, newProps);
  });
  if (makeReactive) Injector = observer2(Injector);
  Injector["isMobxInjector"] = true;
  copyStaticProperties2(component, Injector);
  Injector["wrappedComponent"] = component;
  Injector.displayName = getInjectName(component, injectNames);
  return Injector;
}
function getInjectName(component, injectNames) {
  var displayName;
  var componentName = component.displayName || component.name || component.constructor && component.constructor.name || "Component";
  if (injectNames) displayName = "inject-with-" + injectNames + "(" + componentName + ")";
  else displayName = "inject(" + componentName + ")";
  return displayName;
}
function grabStoresByName(storeNames) {
  return function(baseStores, nextProps) {
    storeNames.forEach(function(storeName) {
      if (storeName in nextProps) return;
      if (!(storeName in baseStores)) throw new Error("MobX injector: Store '" + storeName + "' is not available! Make sure it is provided by some Provider");
      nextProps[storeName] = baseStores[storeName];
    });
    return nextProps;
  };
}
function inject() {
  for (var _len = arguments.length, storeNames = new Array(_len), _key = 0; _key < _len; _key++) {
    storeNames[_key] = arguments[_key];
  }
  if (typeof arguments[0] === "function") {
    var grabStoresFn = arguments[0];
    return function(componentClass) {
      return createStoreInjector(grabStoresFn, componentClass, grabStoresFn.name, true);
    };
  } else {
    return function(componentClass) {
      return createStoreInjector(grabStoresByName(storeNames), componentClass, storeNames.join("-"), false);
    };
  }
}
function runDisposersOnWillUnmount() {
  var _this = this;
  [].concat(this[protoStoreKey] || [], this[instStoreKey] || []).forEach(function(propKeyOrFunction) {
    var prop = typeof propKeyOrFunction === "string" ? _this[propKeyOrFunction] : propKeyOrFunction;
    if (prop !== void 0 && prop !== null) {
      if (Array.isArray(prop)) prop.map(function(f2) {
        return f2();
      });
      else prop();
    }
  });
}
function disposeOnUnmount(target, propertyKeyOrFunction) {
  if (Array.isArray(propertyKeyOrFunction)) {
    return propertyKeyOrFunction.map(function(fn) {
      return disposeOnUnmount(target, fn);
    });
  }
  if (!warnedAboutDisposeOnUnmountDeprecated) {
    if (reactMajorVersion >= 18) {
      console.error("[mobx-react] disposeOnUnmount is not compatible with React 18 and higher. Don't use it.");
    } else {
      console.warn("[mobx-react] disposeOnUnmount is deprecated. It won't work correctly with React 18 and higher.");
    }
    warnedAboutDisposeOnUnmountDeprecated = true;
  }
  var c2 = Object.getPrototypeOf(target).constructor;
  var c22 = Object.getPrototypeOf(target.constructor);
  var c3 = Object.getPrototypeOf(Object.getPrototypeOf(target));
  if (!(c2 === import_react14.default.Component || c2 === import_react14.default.PureComponent || c22 === import_react14.default.Component || c22 === import_react14.default.PureComponent || c3 === import_react14.default.Component || c3 === import_react14.default.PureComponent)) {
    throw new Error("[mobx-react] disposeOnUnmount only supports direct subclasses of React.Component or React.PureComponent.");
  }
  if (typeof propertyKeyOrFunction !== "string" && typeof propertyKeyOrFunction !== "function" && !Array.isArray(propertyKeyOrFunction)) {
    throw new Error("[mobx-react] disposeOnUnmount only works if the parameter is either a property key or a function.");
  }
  var isDecorator = typeof propertyKeyOrFunction === "string";
  var componentWasAlreadyModified = !!target[protoStoreKey] || !!target[instStoreKey];
  var store = isDecorator ? (
    // decorators are added to the prototype store
    target[protoStoreKey] || (target[protoStoreKey] = [])
  ) : (
    // functions are added to the instance store
    target[instStoreKey] || (target[instStoreKey] = [])
  );
  store.push(propertyKeyOrFunction);
  if (!componentWasAlreadyModified) {
    patch(target, "componentWillUnmount", runDisposersOnWillUnmount);
  }
  if (typeof propertyKeyOrFunction !== "string") {
    return propertyKeyOrFunction;
  }
}
function createChainableTypeChecker(validator) {
  function checkType(isRequired, props, propName, componentName, location, propFullName) {
    for (var _len = arguments.length, rest = new Array(_len > 6 ? _len - 6 : 0), _key = 6; _key < _len; _key++) {
      rest[_key - 6] = arguments[_key];
    }
    return untracked(function() {
      componentName = componentName || "<<anonymous>>";
      propFullName = propFullName || propName;
      if (props[propName] == null) {
        if (isRequired) {
          var actual = props[propName] === null ? "null" : "undefined";
          return new Error("The " + location + " `" + propFullName + "` is marked as required in `" + componentName + "`, but its value is `" + actual + "`.");
        }
        return null;
      } else {
        return validator.apply(void 0, [props, propName, componentName, location, propFullName].concat(rest));
      }
    });
  }
  var chainedCheckType = checkType.bind(null, false);
  chainedCheckType.isRequired = checkType.bind(null, true);
  return chainedCheckType;
}
function isSymbol(propType, propValue) {
  if (propType === "symbol") {
    return true;
  }
  if (propValue["@@toStringTag"] === "Symbol") {
    return true;
  }
  if (typeof Symbol === "function" && propValue instanceof Symbol) {
    return true;
  }
  return false;
}
function getPropType(propValue) {
  var propType = typeof propValue;
  if (Array.isArray(propValue)) {
    return "array";
  }
  if (propValue instanceof RegExp) {
    return "object";
  }
  if (isSymbol(propType, propValue)) {
    return "symbol";
  }
  return propType;
}
function getPreciseType(propValue) {
  var propType = getPropType(propValue);
  if (propType === "object") {
    if (propValue instanceof Date) {
      return "date";
    } else if (propValue instanceof RegExp) {
      return "regexp";
    }
  }
  return propType;
}
function createObservableTypeCheckerCreator(allowNativeType, mobxType) {
  return createChainableTypeChecker(function(props, propName, componentName, location, propFullName) {
    return untracked(function() {
      if (allowNativeType) {
        if (getPropType(props[propName]) === mobxType.toLowerCase()) return null;
      }
      var mobxChecker;
      switch (mobxType) {
        case "Array":
          mobxChecker = isObservableArray;
          break;
        case "Object":
          mobxChecker = isObservableObject;
          break;
        case "Map":
          mobxChecker = isObservableMap;
          break;
        default:
          throw new Error("Unexpected mobxType: " + mobxType);
      }
      var propValue = props[propName];
      if (!mobxChecker(propValue)) {
        var preciseType = getPreciseType(propValue);
        var nativeTypeExpectationMessage = allowNativeType ? " or javascript `" + mobxType.toLowerCase() + "`" : "";
        return new Error("Invalid prop `" + propFullName + "` of type `" + preciseType + "` supplied to `" + componentName + "`, expected `mobx.Observable" + mobxType + "`" + nativeTypeExpectationMessage + ".");
      }
      return null;
    });
  });
}
function createObservableArrayOfTypeChecker(allowNativeType, typeChecker) {
  return createChainableTypeChecker(function(props, propName, componentName, location, propFullName) {
    for (var _len2 = arguments.length, rest = new Array(_len2 > 5 ? _len2 - 5 : 0), _key2 = 5; _key2 < _len2; _key2++) {
      rest[_key2 - 5] = arguments[_key2];
    }
    return untracked(function() {
      if (typeof typeChecker !== "function") {
        return new Error("Property `" + propFullName + "` of component `" + componentName + "` has invalid PropType notation.");
      } else {
        var error = createObservableTypeCheckerCreator(allowNativeType, "Array")(props, propName, componentName, location, propFullName);
        if (error instanceof Error) return error;
        var propValue = props[propName];
        for (var i2 = 0; i2 < propValue.length; i2++) {
          error = typeChecker.apply(void 0, [propValue, i2, componentName, location, propFullName + "[" + i2 + "]"].concat(rest));
          if (error instanceof Error) return error;
        }
        return null;
      }
    });
  });
}
var import_react14, hoistBlackList2, mobxMixins, mobxPatchedDefinition, administrationSymbol, isMobXReactObserverSymbol, observablePropDescriptors, _excluded6, MobXProviderContext, reactMajorVersion, warnedAboutDisposeOnUnmountDeprecated, protoStoreKey, instStoreKey, observableArray, observableArrayOf, observableMap, observableObject, arrayOrObservableArray, arrayOrObservableArrayOf, objectOrObservableObject, PropTypes;
var init_mobxreact_esm = __esm({
  "node_modules/mobx-react/dist/mobxreact.esm.js"() {
    init_mobx_esm();
    import_react14 = __toESM(require_react());
    init_es();
    init_es();
    hoistBlackList2 = {
      $$typeof: 1,
      render: 1,
      compare: 1,
      type: 1,
      childContextTypes: 1,
      contextType: 1,
      contextTypes: 1,
      defaultProps: 1,
      getDefaultProps: 1,
      getDerivedStateFromError: 1,
      getDerivedStateFromProps: 1,
      mixins: 1,
      displayName: 1,
      propTypes: 1
    };
    mobxMixins = Symbol("patchMixins");
    mobxPatchedDefinition = Symbol("patchedDefinition");
    administrationSymbol = Symbol("ObserverAdministration");
    isMobXReactObserverSymbol = Symbol("isMobXReactObserver");
    if (true) {
      observablePropDescriptors = {
        props: createObservablePropDescriptor("props"),
        state: createObservablePropDescriptor("state"),
        context: createObservablePropDescriptor("context")
      };
    }
    _excluded6 = ["children"];
    MobXProviderContext = import_react14.default.createContext({});
    Provider.displayName = "MobXProvider";
    reactMajorVersion = Number.parseInt(import_react14.default.version.split(".")[0]);
    warnedAboutDisposeOnUnmountDeprecated = false;
    protoStoreKey = Symbol("disposeOnUnmountProto");
    instStoreKey = Symbol("disposeOnUnmountInst");
    observableArray = createObservableTypeCheckerCreator(false, "Array");
    observableArrayOf = createObservableArrayOfTypeChecker.bind(null, false);
    observableMap = createObservableTypeCheckerCreator(false, "Map");
    observableObject = createObservableTypeCheckerCreator(false, "Object");
    arrayOrObservableArray = createObservableTypeCheckerCreator(true, "Array");
    arrayOrObservableArrayOf = createObservableArrayOfTypeChecker.bind(null, true);
    objectOrObservableObject = createObservableTypeCheckerCreator(true, "Object");
    PropTypes = {
      observableArray,
      observableArrayOf,
      observableMap,
      observableObject,
      arrayOrObservableArray,
      arrayOrObservableArrayOf,
      objectOrObservableObject
    };
    if (!import_react14.Component) {
      throw new Error("mobx-react requires React to be available");
    }
    if (!observable) {
      throw new Error("mobx-react requires mobx to be available");
    }
  }
});

// node_modules/classnames/index.js
var require_classnames = __commonJS({
  "node_modules/classnames/index.js"(exports, module) {
    (function() {
      "use strict";
      var hasOwn = {}.hasOwnProperty;
      function classNames() {
        var classes = "";
        for (var i2 = 0; i2 < arguments.length; i2++) {
          var arg = arguments[i2];
          if (arg) {
            classes = appendClass(classes, parseValue(arg));
          }
        }
        return classes;
      }
      function parseValue(arg) {
        if (typeof arg === "string" || typeof arg === "number") {
          return arg;
        }
        if (typeof arg !== "object") {
          return "";
        }
        if (Array.isArray(arg)) {
          return classNames.apply(null, arg);
        }
        if (arg.toString !== Object.prototype.toString && !arg.toString.toString().includes("[native code]")) {
          return arg.toString();
        }
        var classes = "";
        for (var key2 in arg) {
          if (hasOwn.call(arg, key2) && arg[key2]) {
            classes = appendClass(classes, key2);
          }
        }
        return classes;
      }
      function appendClass(value, newClass) {
        if (!newClass) {
          return value;
        }
        if (value) {
          return value + " " + newClass;
        }
        return value + newClass;
      }
      if (typeof module !== "undefined" && module.exports) {
        classNames.default = classNames;
        module.exports = classNames;
      } else if (typeof define === "function" && typeof define.amd === "object" && define.amd) {
        define("classnames", [], function() {
          return classNames;
        });
      } else {
        window.classNames = classNames;
      }
    })();
  }
});

// node_modules/redoc/bundles/redoc.browser.lib.js
var require_redoc_browser_lib = __commonJS({
  "node_modules/redoc/bundles/redoc.browser.lib.js"(exports, module) {
    !(function(e, t) {
      "object" == typeof exports && "object" == typeof module ? module.exports = t() : "function" == typeof define && define.amd ? define([], t) : "object" == typeof exports ? exports.Redoc = t() : e.Redoc = t();
    })(exports, (function() {
      return (function() {
        var e = { 997: function(e2, t2, r3) {
          "use strict";
          var n2 = r3(991), o2 = r3.n(n2), i2 = r3(314), s2 = r3.n(i2)()(o2());
          s2.push([e2.id, ".ps{overflow:hidden!important;overflow-anchor:none;-ms-overflow-style:none;touch-action:auto;-ms-touch-action:auto}.ps__rail-x{display:none;opacity:0;transition:background-color .2s linear,opacity .2s linear;-webkit-transition:background-color .2s linear,opacity .2s linear;height:15px;bottom:0;position:absolute}.ps__rail-y{display:none;opacity:0;transition:background-color .2s linear,opacity .2s linear;-webkit-transition:background-color .2s linear,opacity .2s linear;width:15px;right:0;position:absolute}.ps--active-x>.ps__rail-x,.ps--active-y>.ps__rail-y{display:block;background-color:transparent}.ps:hover>.ps__rail-x,.ps:hover>.ps__rail-y,.ps--focus>.ps__rail-x,.ps--focus>.ps__rail-y,.ps--scrolling-x>.ps__rail-x,.ps--scrolling-y>.ps__rail-y{opacity:.6}.ps .ps__rail-x:hover,.ps .ps__rail-y:hover,.ps .ps__rail-x:focus,.ps .ps__rail-y:focus,.ps .ps__rail-x.ps--clicking,.ps .ps__rail-y.ps--clicking{background-color:#eee;opacity:.9}.ps__thumb-x{background-color:#aaa;border-radius:6px;transition:background-color .2s linear,height .2s ease-in-out;-webkit-transition:background-color .2s linear,height .2s ease-in-out;height:6px;bottom:2px;position:absolute}.ps__thumb-y{background-color:#aaa;border-radius:6px;transition:background-color .2s linear,width .2s ease-in-out;-webkit-transition:background-color .2s linear,width .2s ease-in-out;width:6px;right:2px;position:absolute}.ps__rail-x:hover>.ps__thumb-x,.ps__rail-x:focus>.ps__thumb-x,.ps__rail-x.ps--clicking .ps__thumb-x{background-color:#999;height:11px}.ps__rail-y:hover>.ps__thumb-y,.ps__rail-y:focus>.ps__thumb-y,.ps__rail-y.ps--clicking .ps__thumb-y{background-color:#999;width:11px}@supports (-ms-overflow-style: none){.ps{overflow:auto!important}}@media screen and (-ms-high-contrast: active),(-ms-high-contrast: none){.ps{overflow:auto!important}}\n", "", { version: 3, sources: ["webpack://./node_modules/perfect-scrollbar/css/perfect-scrollbar.css"], names: [], mappings: "AAGA,IACE,yBAAU,CACV,oBAAiB,CACjB,uBAAoB,CACpB,iBAAc,CACd,qBACF,CAKA,YACE,YAAS,CACT,SAAS,CACT,yDAAqD,CACrD,iEAA6D,CAC7D,WAAQ,CAER,QAAQ,CAER,iBACF,CAEA,YACE,YAAS,CACT,SAAS,CACT,yDAAqD,CACrD,iEAA6D,CAC7D,UAAO,CAEP,OAAO,CAEP,iBACF,CAEA,oDAEE,aAAS,CACT,4BACF,CAEA,oJAME,UACF,CAEA,kJAME,qBAAkB,CAClB,UACF,CAKA,aACE,qBAAkB,CAnEpB,iBAoEiB,CACf,6DAAoD,CACpD,qEAA4D,CAC5D,UAAQ,CAER,UAAQ,CAER,iBACF,CAEA,aACE,qBAAkB,CA/EpB,iBAgFiB,CACf,4DAAmD,CACnD,oEAA2D,CAC3D,SAAO,CAEP,SAAO,CAEP,iBACF,CAEA,oGAGE,qBAAkB,CAClB,WACF,CAEA,oGAGE,qBAAkB,CAClB,UACF,CAGA,qCACE,IACE,uBACF,CACF,CAEA,wEACE,IACE,uBACF,CACF", sourcesContent: ["/*\n * Container style\n */\n.ps {\n  overflow: hidden !important;\n  overflow-anchor: none;\n  -ms-overflow-style: none;\n  touch-action: auto;\n  -ms-touch-action: auto;\n}\n\n/*\n * Scrollbar rail styles\n */\n.ps__rail-x {\n  display: none;\n  opacity: 0;\n  transition: background-color .2s linear, opacity .2s linear;\n  -webkit-transition: background-color .2s linear, opacity .2s linear;\n  height: 15px;\n  /* there must be 'bottom' or 'top' for ps__rail-x */\n  bottom: 0px;\n  /* please don't change 'position' */\n  position: absolute;\n}\n\n.ps__rail-y {\n  display: none;\n  opacity: 0;\n  transition: background-color .2s linear, opacity .2s linear;\n  -webkit-transition: background-color .2s linear, opacity .2s linear;\n  width: 15px;\n  /* there must be 'right' or 'left' for ps__rail-y */\n  right: 0;\n  /* please don't change 'position' */\n  position: absolute;\n}\n\n.ps--active-x > .ps__rail-x,\n.ps--active-y > .ps__rail-y {\n  display: block;\n  background-color: transparent;\n}\n\n.ps:hover > .ps__rail-x,\n.ps:hover > .ps__rail-y,\n.ps--focus > .ps__rail-x,\n.ps--focus > .ps__rail-y,\n.ps--scrolling-x > .ps__rail-x,\n.ps--scrolling-y > .ps__rail-y {\n  opacity: 0.6;\n}\n\n.ps .ps__rail-x:hover,\n.ps .ps__rail-y:hover,\n.ps .ps__rail-x:focus,\n.ps .ps__rail-y:focus,\n.ps .ps__rail-x.ps--clicking,\n.ps .ps__rail-y.ps--clicking {\n  background-color: #eee;\n  opacity: 0.9;\n}\n\n/*\n * Scrollbar thumb styles\n */\n.ps__thumb-x {\n  background-color: #aaa;\n  border-radius: 6px;\n  transition: background-color .2s linear, height .2s ease-in-out;\n  -webkit-transition: background-color .2s linear, height .2s ease-in-out;\n  height: 6px;\n  /* there must be 'bottom' for ps__thumb-x */\n  bottom: 2px;\n  /* please don't change 'position' */\n  position: absolute;\n}\n\n.ps__thumb-y {\n  background-color: #aaa;\n  border-radius: 6px;\n  transition: background-color .2s linear, width .2s ease-in-out;\n  -webkit-transition: background-color .2s linear, width .2s ease-in-out;\n  width: 6px;\n  /* there must be 'right' for ps__thumb-y */\n  right: 2px;\n  /* please don't change 'position' */\n  position: absolute;\n}\n\n.ps__rail-x:hover > .ps__thumb-x,\n.ps__rail-x:focus > .ps__thumb-x,\n.ps__rail-x.ps--clicking .ps__thumb-x {\n  background-color: #999;\n  height: 11px;\n}\n\n.ps__rail-y:hover > .ps__thumb-y,\n.ps__rail-y:focus > .ps__thumb-y,\n.ps__rail-y.ps--clicking .ps__thumb-y {\n  background-color: #999;\n  width: 11px;\n}\n\n/* MS supports */\n@supports (-ms-overflow-style: none) {\n  .ps {\n    overflow: auto !important;\n  }\n}\n\n@media screen and (-ms-high-contrast: active), (-ms-high-contrast: none) {\n  .ps {\n    overflow: auto !important;\n  }\n}\n"], sourceRoot: "" }]), t2.A = s2;
        }, 314: function(e2) {
          "use strict";
          e2.exports = function(e3) {
            var t2 = [];
            return t2.toString = function() {
              return this.map((function(t3) {
                var r3 = e3(t3);
                return t3[2] ? "@media ".concat(t3[2], " {").concat(r3, "}") : r3;
              })).join("");
            }, t2.i = function(e4, r3, n2) {
              "string" == typeof e4 && (e4 = [[null, e4, ""]]);
              var o2 = {};
              if (n2) for (var i2 = 0; i2 < this.length; i2++) {
                var s2 = this[i2][0];
                null != s2 && (o2[s2] = true);
              }
              for (var a2 = 0; a2 < e4.length; a2++) {
                var l2 = [].concat(e4[a2]);
                n2 && o2[l2[0]] || (r3 && (l2[2] ? l2[2] = "".concat(r3, " and ").concat(l2[2]) : l2[2] = r3), t2.push(l2));
              }
            }, t2;
          };
        }, 991: function(e2) {
          "use strict";
          function t2(e3, t3) {
            (null == t3 || t3 > e3.length) && (t3 = e3.length);
            for (var r3 = 0, n2 = new Array(t3); r3 < t3; r3++) n2[r3] = e3[r3];
            return n2;
          }
          e2.exports = function(e3) {
            var r3, n2, o2 = (n2 = 4, (function(e4) {
              if (Array.isArray(e4)) return e4;
            })(r3 = e3) || (function(e4, t3) {
              var r4 = e4 && ("undefined" != typeof Symbol && e4[Symbol.iterator] || e4["@@iterator"]);
              if (null != r4) {
                var n3, o3, i3 = [], s3 = true, a3 = false;
                try {
                  for (r4 = r4.call(e4); !(s3 = (n3 = r4.next()).done) && (i3.push(n3.value), !t3 || i3.length !== t3); s3 = true) ;
                } catch (e5) {
                  a3 = true, o3 = e5;
                } finally {
                  try {
                    s3 || null == r4.return || r4.return();
                  } finally {
                    if (a3) throw o3;
                  }
                }
                return i3;
              }
            })(r3, n2) || (function(e4, r4) {
              if (e4) {
                if ("string" == typeof e4) return t2(e4, r4);
                var n3 = Object.prototype.toString.call(e4).slice(8, -1);
                return "Object" === n3 && e4.constructor && (n3 = e4.constructor.name), "Map" === n3 || "Set" === n3 ? Array.from(e4) : "Arguments" === n3 || /^(?:Ui|I)nt(?:8|16|32)(?:Clamped)?Array$/.test(n3) ? t2(e4, r4) : void 0;
              }
            })(r3, n2) || (function() {
              throw new TypeError("Invalid attempt to destructure non-iterable instance.\nIn order to be iterable, non-array objects must have a [Symbol.iterator]() method.");
            })()), i2 = o2[1], s2 = o2[3];
            if ("function" == typeof btoa) {
              var a2 = btoa(unescape(encodeURIComponent(JSON.stringify(s2)))), l2 = "sourceMappingURL=data:application/json;charset=utf-8;base64,".concat(a2), c2 = "/*# ".concat(l2, " */"), p2 = s2.sources.map((function(e4) {
                return "/*# sourceURL=".concat(s2.sourceRoot || "").concat(e4, " */");
              }));
              return [i2].concat(p2).concat([c2]).join("\n");
            }
            return [i2].join("\n");
          };
        }, 364: function(e2) {
          e2.exports = {};
        }, 353: function(e2, t2, r3) {
          "use strict";
          r3.d(t2, { Ay: function() {
            return i2;
          } });
          var n2 = require_lunr(), o2 = (e3, t3, r4) => new Promise(((n3, o3) => {
            var i3 = (e4) => {
              try {
                a3(r4.next(e4));
              } catch (e5) {
                o3(e5);
              }
            }, s3 = (e4) => {
              try {
                a3(r4.throw(e4));
              } catch (e5) {
                o3(e5);
              }
            }, a3 = (e4) => e4.done ? n3(e4.value) : Promise.resolve(e4.value).then(i3, s3);
            a3((r4 = r4.apply(e3, t3)).next());
          }));
          class i2 {
            constructor() {
              this.add = d, this.done = h, this.search = b2, this.toJS = m2, this.load = g2, this.dispose = y2, this.fromExternalJS = f2;
            }
          }
          let s2, a2, l2, c2 = [];
          function p2() {
            s2 = new n2.Builder(), s2.field("title"), s2.field("description"), s2.ref("ref"), s2.pipeline.add(n2.trimmer, n2.stopWordFilter, n2.stemmer), l2 = new Promise(((e3) => {
              a2 = e3;
            }));
          }
          n2.tokenizer.separator = /\s+/, p2();
          const u2 = (e3) => {
            const t3 = n2.trimmer(new n2.Token(e3, {}));
            return "*" + n2.stemmer(t3) + "*";
          };
          function d(e3, t3, r4) {
            const n3 = c2.push(r4) - 1, o3 = { title: e3.toLowerCase(), description: t3.toLowerCase(), ref: n3 };
            s2.add(o3);
          }
          function h() {
            return o2(this, null, (function* () {
              a2(s2.build());
            }));
          }
          function m2() {
            return o2(this, null, (function* () {
              return { store: c2, index: (yield l2).toJSON() };
            }));
          }
          function f2(e3, t3) {
            return o2(this, null, (function* () {
              try {
                if (importScripts(e3), !self[t3]) throw new Error("Broken index file format");
                g2(self[t3]);
              } catch (e4) {
                console.error("Failed to load search index: " + e4.message);
              }
            }));
          }
          function g2(e3) {
            return o2(this, null, (function* () {
              c2 = e3.store, a2(n2.Index.load(e3.index));
            }));
          }
          function y2() {
            return o2(this, null, (function* () {
              c2 = [], p2();
            }));
          }
          function b2(e3, t3 = 0) {
            return o2(this, null, (function* () {
              if (0 === e3.trim().length) return [];
              let r4 = (yield l2).query(((t4) => {
                e3.trim().toLowerCase().split(/\s+/).forEach(((e4) => {
                  if (1 === e4.length) return;
                  const r5 = u2(e4);
                  t4.term(r5, {});
                }));
              }));
              return t3 > 0 && (r4 = r4.slice(0, t3)), r4.map(((e4) => ({ meta: c2[e4.ref], score: e4.score })));
            }));
          }
        }, 435: function(e2, t2, r3) {
          "use strict";
          const n2 = r3(648), o2 = {}.NODE_DISABLE_COLORS ? { red: "", yellow: "", green: "", normal: "" } : { red: "\x1B[31m", yellow: "\x1B[33;1m", green: "\x1B[32m", normal: "\x1B[0m" };
          function i2(e3, t3) {
            function r4(e4, t4) {
              return n2.stringify(e4) === n2.stringify(Object.assign({}, e4, t4));
            }
            return r4(e3, t3) && r4(t3, e3);
          }
          function s2(e3) {
            let t3 = (e3 = e3.replace("[]", "Array")).split("/");
            return t3[0] = t3[0].replace(/[^A-Za-z0-9_\-\.]+|\s+/gm, "_"), t3.join("/");
          }
          String.prototype.toCamelCase = function() {
            return this.toLowerCase().replace(/[-_ \/\.](.)/g, (function(e3, t3) {
              return t3.toUpperCase();
            }));
          }, e2.exports = { colour: o2, uniqueOnly: function(e3, t3, r4) {
            return r4.indexOf(e3) === t3;
          }, hasDuplicates: function(e3) {
            return new Set(e3).size !== e3.length;
          }, allSame: function(e3) {
            return new Set(e3).size <= 1;
          }, distinctArray: function(e3) {
            return e3.length === (function(e4) {
              let t3 = [];
              for (let r4 of e4) t3.find((function(e5, t4, n3) {
                return i2(e5, r4);
              })) || t3.push(r4);
              return t3;
            })(e3).length;
          }, firstDupe: function(e3) {
            return e3.find((function(t3, r4, n3) {
              return e3.indexOf(t3) < r4;
            }));
          }, hash: function(e3) {
            let t3, r4 = 0;
            if (0 === e3.length) return r4;
            for (let n3 = 0; n3 < e3.length; n3++) t3 = e3.charCodeAt(n3), r4 = (r4 << 5) - r4 + t3, r4 |= 0;
            return r4;
          }, parameterTypeProperties: ["format", "minimum", "maximum", "exclusiveMinimum", "exclusiveMaximum", "minLength", "maxLength", "multipleOf", "minItems", "maxItems", "uniqueItems", "minProperties", "maxProperties", "additionalProperties", "pattern", "enum", "default"], arrayProperties: ["items", "minItems", "maxItems", "uniqueItems"], httpMethods: ["get", "post", "put", "delete", "patch", "head", "options", "trace"], sanitise: s2, sanitiseAll: function(e3) {
            return s2(e3.split("/").join("_"));
          } };
        }, 751: function(e2, t2, r3) {
          "use strict";
          const n2 = r3(364), o2 = r3(975), i2 = r3(725), s2 = r3(725), a2 = r3(115), l2 = r3(33).jptr, c2 = r3(880).recurse, p2 = r3(920).clone, u2 = r3(737).dereference, d = r3(264).isRef, h = r3(435);
          function m2(e3, t3, r4, n3, o3, s3) {
            let a3 = s3.externalRefs[r4 + n3].paths[0], u3 = i2.parse(o3), m3 = {}, f3 = 1;
            for (; f3; ) f3 = 0, c2(e3, { identityDetection: true }, (function(e4, r5, n4) {
              if (d(e4, r5)) {
                if (e4[r5].startsWith("#")) if (m3[e4[r5]] || e4.$fixed) {
                  if (!e4.$fixed) {
                    let t4 = (a3 + "/" + m3[e4[r5]]).split("/#/").join("/");
                    n4.parent[n4.pkey] = { $ref: t4, "x-miro": e4[r5], $fixed: true }, s3.verbose > 1 && console.warn("Replacing with", t4), f3++;
                  }
                } else {
                  let o4 = p2(l2(t3, e4[r5]));
                  if (s3.verbose > 1 && console.warn((false === o4 ? h.colour.red : h.colour.green) + "Fragment resolution", e4[r5], h.colour.normal), false === o4) {
                    if (n4.parent[n4.pkey] = {}, s3.fatal) {
                      let t4 = new Error("Fragment $ref resolution failed " + e4[r5]);
                      if (!s3.promise) throw t4;
                      s3.promise.reject(t4);
                    }
                  } else f3++, n4.parent[n4.pkey] = o4, m3[e4[r5]] = n4.path.replace("/%24ref", "");
                }
                else if (u3.protocol) {
                  let t4 = i2.resolve(o3, e4[r5]).toString();
                  s3.verbose > 1 && console.warn(h.colour.yellow + "Rewriting external url ref", e4[r5], "as", t4, h.colour.normal), e4["x-miro"] = e4[r5], s3.externalRefs[e4[r5]] && (s3.externalRefs[t4] || (s3.externalRefs[t4] = s3.externalRefs[e4[r5]]), s3.externalRefs[t4].failed = s3.externalRefs[e4[r5]].failed), e4[r5] = t4;
                } else if (!e4["x-miro"]) {
                  let t4 = i2.resolve(o3, e4[r5]).toString(), n5 = false;
                  s3.externalRefs[e4[r5]] && (n5 = s3.externalRefs[e4[r5]].failed), n5 || (s3.verbose > 1 && console.warn(h.colour.yellow + "Rewriting external ref", e4[r5], "as", t4, h.colour.normal), e4["x-miro"] = e4[r5], e4[r5] = t4);
                }
              }
            }));
            return c2(e3, {}, (function(e4, t4, r5) {
              d(e4, t4) && void 0 !== e4.$fixed && delete e4.$fixed;
            })), s3.verbose > 1 && console.warn("Finished fragment resolution"), e3;
          }
          function f2(e3, t3) {
            if (!t3.filters || !t3.filters.length) return e3;
            for (let r4 of t3.filters) e3 = r4(e3, t3);
            return e3;
          }
          function g2(e3, t3, r4, s3) {
            var c3 = i2.parse(r4.source), u3 = r4.source.split("\\").join("/").split("/");
            u3.pop() || u3.pop();
            let d2 = "", h2 = t3.split("#");
            h2.length > 1 && (d2 = "#" + h2[1], t3 = h2[0]), u3 = u3.join("/");
            let g3 = (y3 = i2.parse(t3).protocol, b3 = c3.protocol, y3 && y3.length > 2 ? y3 : b3 && b3.length > 2 ? b3 : "file:");
            var y3, b3;
            let x3;
            if (x3 = "file:" === g3 ? o2.resolve(u3 ? u3 + "/" : "", t3) : i2.resolve(u3 ? u3 + "/" : "", t3), r4.cache[x3]) {
              r4.verbose && console.warn("CACHED", x3, d2);
              let e4 = p2(r4.cache[x3]), n3 = r4.externalRef = e4;
              if (d2 && (n3 = l2(n3, d2), false === n3 && (n3 = {}, r4.fatal))) {
                let e5 = new Error("Cached $ref resolution failed " + x3 + d2);
                if (!r4.promise) throw e5;
                r4.promise.reject(e5);
              }
              return n3 = m2(n3, e4, t3, d2, x3, r4), n3 = f2(n3, r4), s3(p2(n3), x3, r4), Promise.resolve(n3);
            }
            if (r4.verbose && console.warn("GET", x3, d2), r4.handlers && r4.handlers[g3]) return r4.handlers[g3](u3, t3, d2, r4).then((function(e4) {
              return r4.externalRef = e4, e4 = f2(e4, r4), r4.cache[x3] = e4, s3(e4, x3, r4), e4;
            })).catch((function(e4) {
              throw r4.verbose && console.warn(e4), e4;
            }));
            if (g3 && g3.startsWith("http")) {
              const e4 = Object.assign({}, r4.fetchOptions, { agent: r4.agent });
              return r4.fetch(x3, e4).then((function(e5) {
                if (200 !== e5.status) {
                  if (r4.ignoreIOErrors) return r4.verbose && console.warn("FAILED", t3), r4.externalRefs[t3].failed = true, '{"$ref":"' + t3 + '"}';
                  throw new Error(`Received status code ${e5.status}: ${x3}`);
                }
                return e5.text();
              })).then((function(e5) {
                try {
                  let n3 = a2.parse(e5, { schema: "core", prettyErrors: true });
                  if (e5 = r4.externalRef = n3, r4.cache[x3] = p2(e5), d2 && false === (e5 = l2(e5, d2)) && (e5 = {}, r4.fatal)) {
                    let e6 = new Error("Remote $ref resolution failed " + x3 + d2);
                    if (!r4.promise) throw e6;
                    r4.promise.reject(e6);
                  }
                  e5 = f2(e5 = m2(e5, n3, t3, d2, x3, r4), r4);
                } catch (e6) {
                  if (r4.verbose && console.warn(e6), !r4.promise || !r4.fatal) throw e6;
                  r4.promise.reject(e6);
                }
                return s3(e5, x3, r4), e5;
              })).catch((function(e5) {
                if (r4.verbose && console.warn(e5), r4.cache[x3] = {}, !r4.promise || !r4.fatal) throw e5;
                r4.promise.reject(e5);
              }));
            }
            {
              const e4 = '{"$ref":"' + t3 + '"}';
              return (function(e5, t4, r5, o3, i3) {
                return new Promise((function(s4, a3) {
                  n2.readFile(e5, t4, (function(e6, t5) {
                    e6 ? r5.ignoreIOErrors && i3 ? (r5.verbose && console.warn("FAILED", o3), r5.externalRefs[o3].failed = true, s4(i3)) : a3(e6) : s4(t5);
                  }));
                }));
              })(x3, r4.encoding || "utf8", r4, t3, e4).then((function(e5) {
                try {
                  let n3 = a2.parse(e5, { schema: "core", prettyErrors: true });
                  if (e5 = r4.externalRef = n3, r4.cache[x3] = p2(e5), d2 && false === (e5 = l2(e5, d2)) && (e5 = {}, r4.fatal)) {
                    let e6 = new Error("File $ref resolution failed " + x3 + d2);
                    if (!r4.promise) throw e6;
                    r4.promise.reject(e6);
                  }
                  e5 = f2(e5 = m2(e5, n3, t3, d2, x3, r4), r4);
                } catch (e6) {
                  if (r4.verbose && console.warn(e6), !r4.promise || !r4.fatal) throw e6;
                  r4.promise.reject(e6);
                }
                return s3(e5, x3, r4), e5;
              })).catch((function(e5) {
                if (r4.verbose && console.warn(e5), !r4.promise || !r4.fatal) throw e5;
                r4.promise.reject(e5);
              }));
            }
          }
          function y2(e3) {
            return new Promise((function(t3, r4) {
              (function(e4) {
                return new Promise((function(t4, r5) {
                  function n4(t5, r6, n5) {
                    if (t5[r6] && d(t5[r6], "$ref")) {
                      let i3 = t5[r6].$ref;
                      if (!i3.startsWith("#")) {
                        let s3 = "";
                        if (!o3[i3]) {
                          let t6 = Object.keys(o3).find((function(e5, t7, r7) {
                            return i3.startsWith(e5 + "/");
                          }));
                          t6 && (e4.verbose && console.warn("Found potential subschema at", t6), s3 = "/" + (i3.split("#")[1] || "").replace(t6.split("#")[1] || ""), s3 = s3.split("/undefined").join(""), i3 = t6);
                        }
                        if (o3[i3] || (o3[i3] = { resolved: false, paths: [], extras: {}, description: t5[r6].description }), o3[i3].resolved) if (o3[i3].failed) ;
                        else if (e4.rewriteRefs) {
                          let n6 = o3[i3].resolvedAt;
                          e4.verbose > 1 && console.warn("Rewriting ref", i3, n6), t5[r6]["x-miro"] = i3, t5[r6].$ref = n6 + s3;
                        } else t5[r6] = p2(o3[i3].data);
                        else o3[i3].paths.push(n5.path), o3[i3].extras[n5.path] = s3;
                      }
                    }
                  }
                  let o3 = e4.externalRefs;
                  if (e4.resolver.depth > 0 && e4.source === e4.resolver.base) return t4(o3);
                  c2(e4.openapi.definitions, { identityDetection: true, path: "#/definitions" }, n4), c2(e4.openapi.components, { identityDetection: true, path: "#/components" }, n4), c2(e4.openapi, { identityDetection: true }, n4), t4(o3);
                }));
              })(e3).then((function(t4) {
                for (let r5 in t4) if (!t4[r5].resolved) {
                  let n4 = e3.resolver.depth;
                  n4 > 0 && n4++, e3.resolver.actions[n4].push((function() {
                    return g2(e3.openapi, r5, e3, (function(e4, n5, o3) {
                      if (!t4[r5].resolved) {
                        let i4 = {};
                        i4.context = t4[r5], i4.$ref = r5, i4.original = p2(e4), i4.updated = e4, i4.source = n5, o3.externals.push(i4), t4[r5].resolved = true;
                      }
                      let i3 = Object.assign({}, o3, { source: "", resolver: { actions: o3.resolver.actions, depth: o3.resolver.actions.length - 1, base: o3.resolver.base } });
                      o3.patch && t4[r5].description && !e4.description && "object" == typeof e4 && (e4.description = t4[r5].description), t4[r5].data = e4;
                      let s3 = (a3 = t4[r5].paths, [...new Set(a3)]);
                      var a3;
                      s3 = s3.sort((function(e5, t5) {
                        const r6 = e5.startsWith("#/components/") || e5.startsWith("#/definitions/"), n6 = t5.startsWith("#/components/") || t5.startsWith("#/definitions/");
                        return r6 && !n6 ? -1 : n6 && !r6 ? 1 : 0;
                      }));
                      for (let n6 of s3) if (t4[r5].resolvedAt && n6 !== t4[r5].resolvedAt && n6.indexOf("x-ms-examples/") < 0) o3.verbose > 1 && console.warn("Creating pointer to data at", n6), l2(o3.openapi, n6, { $ref: t4[r5].resolvedAt + t4[r5].extras[n6], "x-miro": r5 + t4[r5].extras[n6] });
                      else {
                        t4[r5].resolvedAt ? o3.verbose > 1 && console.warn("Avoiding circular reference") : (t4[r5].resolvedAt = n6, o3.verbose > 1 && console.warn("Creating initial clone of data at", n6));
                        let i4 = p2(e4);
                        l2(o3.openapi, n6, i4);
                      }
                      0 === o3.resolver.actions[i3.resolver.depth].length && o3.resolver.actions[i3.resolver.depth].push((function() {
                        return y2(i3);
                      }));
                    }));
                  }));
                }
              })).catch((function(t4) {
                e3.verbose && console.warn(t4), r4(t4);
              }));
              let n3 = { options: e3 };
              n3.actions = e3.resolver.actions[e3.resolver.depth], t3(n3);
            }));
          }
          function b2(e3, t3, r4) {
            e3.resolver.actions.push([]), y2(e3).then((function(n3) {
              var o3;
              (o3 = n3.actions, o3.reduce(((e4, t4) => e4.then(((e5) => t4().then(Array.prototype.concat.bind(e5))))), Promise.resolve([]))).then((function() {
                if (e3.resolver.depth >= e3.resolver.actions.length) return console.warn("Ran off the end of resolver actions"), t3(true);
                e3.resolver.depth++, e3.resolver.actions[e3.resolver.depth].length ? setTimeout((function() {
                  b2(n3.options, t3, r4);
                }), 0) : (e3.verbose > 1 && console.warn(h.colour.yellow + "Finished external resolution!", h.colour.normal), e3.resolveInternal && (e3.verbose > 1 && console.warn(h.colour.yellow + "Starting internal resolution!", h.colour.normal), e3.openapi = u2(e3.openapi, e3.original, { verbose: e3.verbose - 1 }), e3.verbose > 1 && console.warn(h.colour.yellow + "Finished internal resolution!", h.colour.normal)), c2(e3.openapi, {}, (function(t4, r5, n4) {
                  d(t4, r5) && (e3.preserveMiro || delete t4["x-miro"]);
                })), t3(e3));
              })).catch((function(t4) {
                e3.verbose && console.warn(t4), r4(t4);
              }));
            })).catch((function(t4) {
              e3.verbose && console.warn(t4), r4(t4);
            }));
          }
          function x2(e3) {
            if (e3.cache || (e3.cache = {}), e3.fetch || (e3.fetch = s2), e3.source) {
              let t3 = i2.parse(e3.source);
              (!t3.protocol || t3.protocol.length <= 2) && (e3.source = o2.resolve(e3.source));
            }
            e3.externals = [], e3.externalRefs = {}, e3.rewriteRefs = true, e3.resolver = {}, e3.resolver.depth = 0, e3.resolver.base = e3.source, e3.resolver.actions = [[]];
          }
          e2.exports = { optionalResolve: function(e3) {
            return x2(e3), new Promise((function(t3, r4) {
              e3.resolve ? b2(e3, t3, r4) : t3(e3);
            }));
          }, resolve: function(e3, t3, r4) {
            return r4 || (r4 = {}), r4.openapi = e3, r4.source = t3, r4.resolve = true, x2(r4), new Promise((function(e4, t4) {
              b2(r4, e4, t4);
            }));
          } };
        }, 319: function(e2) {
          "use strict";
          function t2() {
            return { depth: 0, seen: /* @__PURE__ */ new WeakMap(), top: true, combine: false, allowRefSiblings: false };
          }
          e2.exports = { getDefaultState: t2, walkSchema: function e3(r3, n2, o2, i2) {
            if (void 0 === o2.depth && (o2 = t2()), null == r3) return r3;
            if (void 0 !== r3.$ref) {
              let e4 = { $ref: r3.$ref };
              return o2.allowRefSiblings && r3.description && (e4.description = r3.description), i2(e4, n2, o2), e4;
            }
            if (o2.combine && (r3.allOf && Array.isArray(r3.allOf) && 1 === r3.allOf.length && delete (r3 = Object.assign({}, r3.allOf[0], r3)).allOf, r3.anyOf && Array.isArray(r3.anyOf) && 1 === r3.anyOf.length && delete (r3 = Object.assign({}, r3.anyOf[0], r3)).anyOf, r3.oneOf && Array.isArray(r3.oneOf) && 1 === r3.oneOf.length && delete (r3 = Object.assign({}, r3.oneOf[0], r3)).oneOf), i2(r3, n2, o2), o2.seen.has(r3)) return r3;
            if ("object" == typeof r3 && null !== r3 && o2.seen.set(r3, true), o2.top = false, o2.depth++, void 0 !== r3.items && (o2.property = "items", e3(r3.items, r3, o2, i2)), r3.additionalItems && "object" == typeof r3.additionalItems && (o2.property = "additionalItems", e3(r3.additionalItems, r3, o2, i2)), r3.additionalProperties && "object" == typeof r3.additionalProperties && (o2.property = "additionalProperties", e3(r3.additionalProperties, r3, o2, i2)), r3.properties) for (let t3 in r3.properties) {
              let n3 = r3.properties[t3];
              o2.property = "properties/" + t3, e3(n3, r3, o2, i2);
            }
            if (r3.patternProperties) for (let t3 in r3.patternProperties) {
              let n3 = r3.patternProperties[t3];
              o2.property = "patternProperties/" + t3, e3(n3, r3, o2, i2);
            }
            if (r3.allOf) for (let t3 in r3.allOf) {
              let n3 = r3.allOf[t3];
              o2.property = "allOf/" + t3, e3(n3, r3, o2, i2);
            }
            if (r3.anyOf) for (let t3 in r3.anyOf) {
              let n3 = r3.anyOf[t3];
              o2.property = "anyOf/" + t3, e3(n3, r3, o2, i2);
            }
            if (r3.oneOf) for (let t3 in r3.oneOf) {
              let n3 = r3.oneOf[t3];
              o2.property = "oneOf/" + t3, e3(n3, r3, o2, i2);
            }
            return r3.not && (o2.property = "not", e3(r3.not, r3, o2, i2)), o2.depth--, r3;
          } };
        }, 975: function(e2) {
          "use strict";
          function t2(e3) {
            if ("string" != typeof e3) throw new TypeError("Path must be a string. Received " + JSON.stringify(e3));
          }
          function r3(e3, t3) {
            for (var r4, n3 = "", o2 = 0, i2 = -1, s2 = 0, a2 = 0; a2 <= e3.length; ++a2) {
              if (a2 < e3.length) r4 = e3.charCodeAt(a2);
              else {
                if (47 === r4) break;
                r4 = 47;
              }
              if (47 === r4) {
                if (i2 === a2 - 1 || 1 === s2) ;
                else if (i2 !== a2 - 1 && 2 === s2) {
                  if (n3.length < 2 || 2 !== o2 || 46 !== n3.charCodeAt(n3.length - 1) || 46 !== n3.charCodeAt(n3.length - 2)) {
                    if (n3.length > 2) {
                      var l2 = n3.lastIndexOf("/");
                      if (l2 !== n3.length - 1) {
                        -1 === l2 ? (n3 = "", o2 = 0) : o2 = (n3 = n3.slice(0, l2)).length - 1 - n3.lastIndexOf("/"), i2 = a2, s2 = 0;
                        continue;
                      }
                    } else if (2 === n3.length || 1 === n3.length) {
                      n3 = "", o2 = 0, i2 = a2, s2 = 0;
                      continue;
                    }
                  }
                  t3 && (n3.length > 0 ? n3 += "/.." : n3 = "..", o2 = 2);
                } else n3.length > 0 ? n3 += "/" + e3.slice(i2 + 1, a2) : n3 = e3.slice(i2 + 1, a2), o2 = a2 - i2 - 1;
                i2 = a2, s2 = 0;
              } else 46 === r4 && -1 !== s2 ? ++s2 : s2 = -1;
            }
            return n3;
          }
          var n2 = { resolve: function() {
            for (var e3, n3 = "", o2 = false, i2 = arguments.length - 1; i2 >= -1 && !o2; i2--) {
              var s2;
              i2 >= 0 ? s2 = arguments[i2] : (void 0 === e3 && (e3 = process.cwd()), s2 = e3), t2(s2), 0 !== s2.length && (n3 = s2 + "/" + n3, o2 = 47 === s2.charCodeAt(0));
            }
            return n3 = r3(n3, !o2), o2 ? n3.length > 0 ? "/" + n3 : "/" : n3.length > 0 ? n3 : ".";
          }, normalize: function(e3) {
            if (t2(e3), 0 === e3.length) return ".";
            var n3 = 47 === e3.charCodeAt(0), o2 = 47 === e3.charCodeAt(e3.length - 1);
            return 0 !== (e3 = r3(e3, !n3)).length || n3 || (e3 = "."), e3.length > 0 && o2 && (e3 += "/"), n3 ? "/" + e3 : e3;
          }, isAbsolute: function(e3) {
            return t2(e3), e3.length > 0 && 47 === e3.charCodeAt(0);
          }, join: function() {
            if (0 === arguments.length) return ".";
            for (var e3, r4 = 0; r4 < arguments.length; ++r4) {
              var o2 = arguments[r4];
              t2(o2), o2.length > 0 && (void 0 === e3 ? e3 = o2 : e3 += "/" + o2);
            }
            return void 0 === e3 ? "." : n2.normalize(e3);
          }, relative: function(e3, r4) {
            if (t2(e3), t2(r4), e3 === r4) return "";
            if ((e3 = n2.resolve(e3)) === (r4 = n2.resolve(r4))) return "";
            for (var o2 = 1; o2 < e3.length && 47 === e3.charCodeAt(o2); ++o2) ;
            for (var i2 = e3.length, s2 = i2 - o2, a2 = 1; a2 < r4.length && 47 === r4.charCodeAt(a2); ++a2) ;
            for (var l2 = r4.length - a2, c2 = s2 < l2 ? s2 : l2, p2 = -1, u2 = 0; u2 <= c2; ++u2) {
              if (u2 === c2) {
                if (l2 > c2) {
                  if (47 === r4.charCodeAt(a2 + u2)) return r4.slice(a2 + u2 + 1);
                  if (0 === u2) return r4.slice(a2 + u2);
                } else s2 > c2 && (47 === e3.charCodeAt(o2 + u2) ? p2 = u2 : 0 === u2 && (p2 = 0));
                break;
              }
              var d = e3.charCodeAt(o2 + u2);
              if (d !== r4.charCodeAt(a2 + u2)) break;
              47 === d && (p2 = u2);
            }
            var h = "";
            for (u2 = o2 + p2 + 1; u2 <= i2; ++u2) u2 !== i2 && 47 !== e3.charCodeAt(u2) || (0 === h.length ? h += ".." : h += "/..");
            return h.length > 0 ? h + r4.slice(a2 + p2) : (a2 += p2, 47 === r4.charCodeAt(a2) && ++a2, r4.slice(a2));
          }, _makeLong: function(e3) {
            return e3;
          }, dirname: function(e3) {
            if (t2(e3), 0 === e3.length) return ".";
            for (var r4 = e3.charCodeAt(0), n3 = 47 === r4, o2 = -1, i2 = true, s2 = e3.length - 1; s2 >= 1; --s2) if (47 === (r4 = e3.charCodeAt(s2))) {
              if (!i2) {
                o2 = s2;
                break;
              }
            } else i2 = false;
            return -1 === o2 ? n3 ? "/" : "." : n3 && 1 === o2 ? "//" : e3.slice(0, o2);
          }, basename: function(e3, r4) {
            if (void 0 !== r4 && "string" != typeof r4) throw new TypeError('"ext" argument must be a string');
            t2(e3);
            var n3, o2 = 0, i2 = -1, s2 = true;
            if (void 0 !== r4 && r4.length > 0 && r4.length <= e3.length) {
              if (r4.length === e3.length && r4 === e3) return "";
              var a2 = r4.length - 1, l2 = -1;
              for (n3 = e3.length - 1; n3 >= 0; --n3) {
                var c2 = e3.charCodeAt(n3);
                if (47 === c2) {
                  if (!s2) {
                    o2 = n3 + 1;
                    break;
                  }
                } else -1 === l2 && (s2 = false, l2 = n3 + 1), a2 >= 0 && (c2 === r4.charCodeAt(a2) ? -1 == --a2 && (i2 = n3) : (a2 = -1, i2 = l2));
              }
              return o2 === i2 ? i2 = l2 : -1 === i2 && (i2 = e3.length), e3.slice(o2, i2);
            }
            for (n3 = e3.length - 1; n3 >= 0; --n3) if (47 === e3.charCodeAt(n3)) {
              if (!s2) {
                o2 = n3 + 1;
                break;
              }
            } else -1 === i2 && (s2 = false, i2 = n3 + 1);
            return -1 === i2 ? "" : e3.slice(o2, i2);
          }, extname: function(e3) {
            t2(e3);
            for (var r4 = -1, n3 = 0, o2 = -1, i2 = true, s2 = 0, a2 = e3.length - 1; a2 >= 0; --a2) {
              var l2 = e3.charCodeAt(a2);
              if (47 !== l2) -1 === o2 && (i2 = false, o2 = a2 + 1), 46 === l2 ? -1 === r4 ? r4 = a2 : 1 !== s2 && (s2 = 1) : -1 !== r4 && (s2 = -1);
              else if (!i2) {
                n3 = a2 + 1;
                break;
              }
            }
            return -1 === r4 || -1 === o2 || 0 === s2 || 1 === s2 && r4 === o2 - 1 && r4 === n3 + 1 ? "" : e3.slice(r4, o2);
          }, format: function(e3) {
            if (null === e3 || "object" != typeof e3) throw new TypeError('The "pathObject" argument must be of type Object. Received type ' + typeof e3);
            return (function(e4, t3) {
              var r4 = t3.dir || t3.root, n3 = t3.base || (t3.name || "") + (t3.ext || "");
              return r4 ? r4 === t3.root ? r4 + n3 : r4 + "/" + n3 : n3;
            })(0, e3);
          }, parse: function(e3) {
            t2(e3);
            var r4 = { root: "", dir: "", base: "", ext: "", name: "" };
            if (0 === e3.length) return r4;
            var n3, o2 = e3.charCodeAt(0), i2 = 47 === o2;
            i2 ? (r4.root = "/", n3 = 1) : n3 = 0;
            for (var s2 = -1, a2 = 0, l2 = -1, c2 = true, p2 = e3.length - 1, u2 = 0; p2 >= n3; --p2) if (47 !== (o2 = e3.charCodeAt(p2))) -1 === l2 && (c2 = false, l2 = p2 + 1), 46 === o2 ? -1 === s2 ? s2 = p2 : 1 !== u2 && (u2 = 1) : -1 !== s2 && (u2 = -1);
            else if (!c2) {
              a2 = p2 + 1;
              break;
            }
            return -1 === s2 || -1 === l2 || 0 === u2 || 1 === u2 && s2 === l2 - 1 && s2 === a2 + 1 ? -1 !== l2 && (r4.base = r4.name = 0 === a2 && i2 ? e3.slice(1, l2) : e3.slice(a2, l2)) : (0 === a2 && i2 ? (r4.name = e3.slice(1, s2), r4.base = e3.slice(1, l2)) : (r4.name = e3.slice(a2, s2), r4.base = e3.slice(a2, l2)), r4.ext = e3.slice(s2, l2)), a2 > 0 ? r4.dir = e3.slice(0, a2 - 1) : i2 && (r4.dir = "/"), r4;
          }, sep: "/", delimiter: ":", win32: null, posix: null };
          n2.posix = n2, e2.exports = n2;
        }, 920: function(e2) {
          "use strict";
          e2.exports = { nop: function(e3) {
            return e3;
          }, clone: function(e3) {
            return JSON.parse(JSON.stringify(e3));
          }, shallowClone: function(e3) {
            let t2 = {};
            for (let r3 in e3) e3.hasOwnProperty(r3) && (t2[r3] = e3[r3]);
            return t2;
          }, deepClone: function e3(t2) {
            let r3 = Array.isArray(t2) ? [] : {};
            for (let n2 in t2) (t2.hasOwnProperty(n2) || Array.isArray(t2)) && (r3[n2] = "object" == typeof t2[n2] ? e3(t2[n2]) : t2[n2]);
            return r3;
          }, fastClone: function(e3) {
            return Object.assign({}, e3);
          }, circularClone: function e3(t2, r3) {
            if (r3 || (r3 = /* @__PURE__ */ new WeakMap()), Object(t2) !== t2 || t2 instanceof Function) return t2;
            if (r3.has(t2)) return r3.get(t2);
            try {
              var n2 = new t2.constructor();
            } catch (e4) {
              n2 = Object.create(Object.getPrototypeOf(t2));
            }
            return r3.set(t2, n2), Object.assign(n2, ...Object.keys(t2).map(((n3) => ({ [n3]: e3(t2[n3], r3) }))));
          } };
        }, 737: function(e2, t2, r3) {
          "use strict";
          const n2 = r3(880).recurse, o2 = r3(920).shallowClone, i2 = r3(33).jptr, s2 = r3(264).isRef;
          e2.exports = { dereference: function e3(t3, r4, a2) {
            a2 || (a2 = {}), a2.cache || (a2.cache = {}), a2.state || (a2.state = {}), a2.state.identityDetection = true, a2.depth = a2.depth ? a2.depth + 1 : 1;
            let l2 = a2.depth > 1 ? t3 : o2(t3), c2 = { data: l2 }, p2 = a2.depth > 1 ? r4 : o2(r4);
            a2.master || (a2.master = l2);
            let u2 = (function(e4) {
              return e4 && e4.verbose ? { warn: function() {
                var e5 = Array.prototype.slice.call(arguments);
                console.warn.apply(console, e5);
              } } : { warn: function() {
              } };
            })(a2), d = 1;
            for (; d > 0; ) d = 0, n2(c2, a2.state, (function(t4, r5, n3) {
              if (s2(t4, r5)) {
                let o3 = t4[r5];
                if (d++, a2.cache[o3]) {
                  let e4 = a2.cache[o3];
                  if (e4.resolved) u2.warn("Patching %s for %s", o3, e4.path), n3.parent[n3.pkey] = e4.data, a2.$ref && "object" == typeof n3.parent[n3.pkey] && null !== n3.parent[n3.pkey] && (n3.parent[n3.pkey][a2.$ref] = o3);
                  else {
                    if (o3 === e4.path) throw new Error(`Tight circle at ${e4.path}`);
                    u2.warn("Unresolved ref"), n3.parent[n3.pkey] = i2(e4.source, e4.path), false === n3.parent[n3.pkey] && (n3.parent[n3.pkey] = i2(e4.source, e4.key)), a2.$ref && "object" == typeof n3.parent[n3.pkey] && null !== n3.parent[n3.pkey] && (n3.parent[a2.$ref] = o3);
                  }
                } else {
                  let t5 = {};
                  t5.path = n3.path.split("/$ref")[0], t5.key = o3, u2.warn("Dereffing %s at %s", o3, t5.path), t5.source = p2, t5.data = i2(t5.source, t5.key), false === t5.data && (t5.data = i2(a2.master, t5.key), t5.source = a2.master), false === t5.data && u2.warn("Missing $ref target", t5.key), a2.cache[o3] = t5, t5.data = n3.parent[n3.pkey] = e3(i2(t5.source, t5.key), t5.source, a2), a2.$ref && "object" == typeof n3.parent[n3.pkey] && null !== n3.parent[n3.pkey] && (n3.parent[n3.pkey][a2.$ref] = o3), t5.resolved = true;
                }
              }
            }));
            return c2.data;
          } };
        }, 264: function(e2) {
          "use strict";
          e2.exports = { isRef: function(e3, t2) {
            return "$ref" === t2 && !!e3 && "string" == typeof e3[t2];
          } };
        }, 33: function(e2) {
          "use strict";
          function t2(e3) {
            return e3.replace(/\~1/g, "/").replace(/~0/g, "~");
          }
          e2.exports = { jptr: function(e3, r3, n2) {
            if (void 0 === e3) return false;
            if (!r3 || "string" != typeof r3 || "#" === r3) return void 0 !== n2 ? n2 : e3;
            if (r3.indexOf("#") >= 0) {
              let e4 = r3.split("#");
              if (e4[0]) return false;
              r3 = e4[1], r3 = decodeURIComponent(r3.slice(1).split("+").join(" "));
            }
            r3.startsWith("/") && (r3 = r3.slice(1));
            let o2 = r3.split("/");
            for (let r4 = 0; r4 < o2.length; r4++) {
              o2[r4] = t2(o2[r4]);
              let i2 = void 0 !== n2 && r4 == o2.length - 1, s2 = parseInt(o2[r4], 10);
              if (!Array.isArray(e3) || isNaN(s2) || s2.toString() !== o2[r4] ? s2 = Array.isArray(e3) && "-" === o2[r4] ? -2 : -1 : o2[r4] = r4 > 0 ? o2[r4 - 1] : "", -1 != s2 || e3 && e3.hasOwnProperty(o2[r4])) if (s2 >= 0) i2 && (e3[s2] = n2), e3 = e3[s2];
              else {
                if (-2 === s2) return i2 ? (Array.isArray(e3) && e3.push(n2), n2) : void 0;
                i2 && (e3[o2[r4]] = n2), e3 = e3[o2[r4]];
              }
              else {
                if (void 0 === n2 || "object" != typeof e3 || Array.isArray(e3)) return false;
                e3[o2[r4]] = i2 ? n2 : "0" === o2[r4 + 1] || "-" === o2[r4 + 1] ? [] : {}, e3 = e3[o2[r4]];
              }
            }
            return e3;
          }, jpescape: function(e3) {
            return e3.replace(/\~/g, "~0").replace(/\//g, "~1");
          }, jpunescape: t2 };
        }, 880: function(e2, t2, r3) {
          "use strict";
          const n2 = r3(33).jpescape;
          e2.exports = { recurse: function e3(t3, r4, o2) {
            if (r4 || (r4 = { depth: 0 }), r4.depth || (r4 = Object.assign({}, { path: "#", depth: 0, pkey: "", parent: {}, payload: {}, seen: /* @__PURE__ */ new WeakMap(), identity: false, identityDetection: false }, r4)), "object" != typeof t3) return;
            let i2 = r4.path;
            for (let s2 in t3) {
              if (r4.key = s2, r4.path = r4.path + "/" + encodeURIComponent(n2(s2)), r4.identityPath = r4.seen.get(t3[s2]), r4.identity = void 0 !== r4.identityPath, t3.hasOwnProperty(s2) && o2(t3, s2, r4), "object" == typeof t3[s2] && !r4.identity) {
                r4.identityDetection && !Array.isArray(t3[s2]) && null !== t3[s2] && r4.seen.set(t3[s2], r4.path);
                let n3 = {};
                n3.parent = t3, n3.path = r4.path, n3.depth = r4.depth ? r4.depth + 1 : 1, n3.pkey = s2, n3.payload = r4.payload, n3.seen = r4.seen, n3.identity = false, n3.identityDetection = r4.identityDetection, e3(t3[s2], n3, o2);
              }
              r4.path = i2;
            }
          } };
        }, 494: function(e2, t2, r3) {
          "use strict";
          r3.r(t2);
          var n2 = r3(72), o2 = r3.n(n2), i2 = r3(825), s2 = r3.n(i2), a2 = r3(659), l2 = r3.n(a2), c2 = r3(56), p2 = r3.n(c2), u2 = r3(540), d = r3.n(u2), h = r3(113), m2 = r3.n(h), f2 = r3(997), g2 = {};
          g2.styleTagTransform = m2(), g2.setAttributes = p2(), g2.insert = l2().bind(null, "head"), g2.domAPI = s2(), g2.insertStyleElement = d(), o2()(f2.A, g2), t2.default = f2.A && f2.A.locals ? f2.A.locals : void 0;
        }, 72: function(e2) {
          "use strict";
          var t2 = [];
          function r3(e3) {
            for (var r4 = -1, n3 = 0; n3 < t2.length; n3++) if (t2[n3].identifier === e3) {
              r4 = n3;
              break;
            }
            return r4;
          }
          function n2(e3, n3) {
            for (var i2 = {}, s2 = [], a2 = 0; a2 < e3.length; a2++) {
              var l2 = e3[a2], c2 = n3.base ? l2[0] + n3.base : l2[0], p2 = i2[c2] || 0, u2 = "".concat(c2, " ").concat(p2);
              i2[c2] = p2 + 1;
              var d = r3(u2), h = { css: l2[1], media: l2[2], sourceMap: l2[3], supports: l2[4], layer: l2[5] };
              if (-1 !== d) t2[d].references++, t2[d].updater(h);
              else {
                var m2 = o2(h, n3);
                n3.byIndex = a2, t2.splice(a2, 0, { identifier: u2, updater: m2, references: 1 });
              }
              s2.push(u2);
            }
            return s2;
          }
          function o2(e3, t3) {
            var r4 = t3.domAPI(t3);
            return r4.update(e3), function(t4) {
              if (t4) {
                if (t4.css === e3.css && t4.media === e3.media && t4.sourceMap === e3.sourceMap && t4.supports === e3.supports && t4.layer === e3.layer) return;
                r4.update(e3 = t4);
              } else r4.remove();
            };
          }
          e2.exports = function(e3, o3) {
            var i2 = n2(e3 = e3 || [], o3 = o3 || {});
            return function(e4) {
              e4 = e4 || [];
              for (var s2 = 0; s2 < i2.length; s2++) {
                var a2 = r3(i2[s2]);
                t2[a2].references--;
              }
              for (var l2 = n2(e4, o3), c2 = 0; c2 < i2.length; c2++) {
                var p2 = r3(i2[c2]);
                0 === t2[p2].references && (t2[p2].updater(), t2.splice(p2, 1));
              }
              i2 = l2;
            };
          };
        }, 659: function(e2) {
          "use strict";
          var t2 = {};
          e2.exports = function(e3, r3) {
            var n2 = (function(e4) {
              if (void 0 === t2[e4]) {
                var r4 = document.querySelector(e4);
                if (window.HTMLIFrameElement && r4 instanceof window.HTMLIFrameElement) try {
                  r4 = r4.contentDocument.head;
                } catch (e5) {
                  r4 = null;
                }
                t2[e4] = r4;
              }
              return t2[e4];
            })(e3);
            if (!n2) throw new Error("Couldn't find a style target. This probably means that the value for the 'insert' parameter is invalid.");
            n2.appendChild(r3);
          };
        }, 540: function(e2) {
          "use strict";
          e2.exports = function(e3) {
            var t2 = document.createElement("style");
            return e3.setAttributes(t2, e3.attributes), e3.insert(t2, e3.options), t2;
          };
        }, 56: function(e2, t2, r3) {
          "use strict";
          e2.exports = function(e3) {
            var t3 = r3.nc;
            t3 && e3.setAttribute("nonce", t3);
          };
        }, 825: function(e2) {
          "use strict";
          e2.exports = function(e3) {
            var t2 = e3.insertStyleElement(e3);
            return { update: function(r3) {
              !(function(e4, t3, r4) {
                var n2 = "";
                r4.supports && (n2 += "@supports (".concat(r4.supports, ") {")), r4.media && (n2 += "@media ".concat(r4.media, " {"));
                var o2 = void 0 !== r4.layer;
                o2 && (n2 += "@layer".concat(r4.layer.length > 0 ? " ".concat(r4.layer) : "", " {")), n2 += r4.css, o2 && (n2 += "}"), r4.media && (n2 += "}"), r4.supports && (n2 += "}");
                var i2 = r4.sourceMap;
                i2 && "undefined" != typeof btoa && (n2 += "\n/*# sourceMappingURL=data:application/json;base64,".concat(btoa(unescape(encodeURIComponent(JSON.stringify(i2)))), " */")), t3.styleTagTransform(n2, e4, t3.options);
              })(t2, e3, r3);
            }, remove: function() {
              !(function(e4) {
                if (null === e4.parentNode) return false;
                e4.parentNode.removeChild(e4);
              })(t2);
            } };
          };
        }, 113: function(e2) {
          "use strict";
          e2.exports = function(e3, t2) {
            if (t2.styleSheet) t2.styleSheet.cssText = e3;
            else {
              for (; t2.firstChild; ) t2.removeChild(t2.firstChild);
              t2.appendChild(document.createTextNode(e3));
            }
          };
        }, 65: function(e2, t2, r3) {
          "use strict";
          const n2 = r3(364), o2 = r3(725), i2 = (r3(975), r3(884)), s2 = r3(725), a2 = r3(115), l2 = r3(33), c2 = l2.jptr, p2 = r3(264).isRef, u2 = r3(920).clone, d = r3(920).circularClone, h = r3(880).recurse, m2 = r3(751), f2 = r3(319), g2 = r3(435), y2 = r3(665).statusCodes, b2 = r3(430).rE, x2 = "3.0.0";
          let v2;
          class w2 extends Error {
            constructor(e3) {
              super(e3), this.name = "S2OError";
            }
          }
          function k2(e3, t3) {
            let r4 = new w2(e3);
            if (r4.options = t3, !t3.promise) throw r4;
            t3.promise.reject(r4);
          }
          function E2(e3, t3, r4) {
            r4.warnOnly ? t3[r4.warnProperty || "x-s2o-warning"] = e3 : k2(e3, r4);
          }
          function O2(e3, t3) {
            f2.walkSchema(e3, {}, {}, (function(e4, r4, n3) {
              !(function(e5) {
                if (e5["x-required"] && Array.isArray(e5["x-required"]) && (e5.required || (e5.required = []), e5.required = e5.required.concat(e5["x-required"]), delete e5["x-required"]), e5["x-anyOf"] && (e5.anyOf = e5["x-anyOf"], delete e5["x-anyOf"]), e5["x-oneOf"] && (e5.oneOf = e5["x-oneOf"], delete e5["x-oneOf"]), e5["x-not"] && (e5.not = e5["x-not"], delete e5["x-not"]), "boolean" == typeof e5["x-nullable"] && (e5.nullable = e5["x-nullable"], delete e5["x-nullable"]), "object" == typeof e5["x-discriminator"] && "string" == typeof e5["x-discriminator"].propertyName) {
                  e5.discriminator = e5["x-discriminator"], delete e5["x-discriminator"];
                  for (let t4 in e5.discriminator.mapping) {
                    let r5 = e5.discriminator.mapping[t4];
                    r5.startsWith("#/definitions/") && (e5.discriminator.mapping[t4] = r5.replace("#/definitions/", "#/components/schemas/"));
                  }
                }
              })(e4), (function(e5, t4, r5) {
                if (e5.nullable && r5.patches++, e5.discriminator && "string" == typeof e5.discriminator && (e5.discriminator = { propertyName: e5.discriminator }), e5.items && Array.isArray(e5.items) && (0 === e5.items.length ? e5.items = {} : 1 === e5.items.length ? e5.items = e5.items[0] : e5.items = { anyOf: e5.items }), e5.type && Array.isArray(e5.type)) if (r5.patch) {
                  if (r5.patches++, 0 === e5.type.length) delete e5.type;
                  else {
                    e5.oneOf || (e5.oneOf = []);
                    for (let t5 of e5.type) {
                      let r6 = {};
                      if ("null" === t5) e5.nullable = true;
                      else {
                        r6.type = t5;
                        for (let t6 of g2.arrayProperties) void 0 !== e5.prop && (r6[t6] = e5[t6], delete e5[t6]);
                      }
                      r6.type && e5.oneOf.push(r6);
                    }
                    delete e5.type, 0 === e5.oneOf.length ? delete e5.oneOf : e5.oneOf.length < 2 && (e5.type = e5.oneOf[0].type, Object.keys(e5.oneOf[0]).length > 1 && E2("Lost properties from oneOf", e5, r5), delete e5.oneOf);
                  }
                  e5.type && Array.isArray(e5.type) && 1 === e5.type.length && (e5.type = e5.type[0]);
                } else k2("(Patchable) schema type must not be an array", r5);
                e5.type && "null" === e5.type && (delete e5.type, e5.nullable = true), "array" !== e5.type || e5.items || (e5.items = {}), "file" === e5.type && (e5.type = "string", e5.format = "binary"), "boolean" == typeof e5.required && (e5.required && e5.name && (void 0 === t4.required && (t4.required = []), Array.isArray(t4.required) && t4.required.push(e5.name)), delete e5.required), e5.xml && "string" == typeof e5.xml.namespace && (e5.xml.namespace || delete e5.xml.namespace), void 0 !== e5.allowEmptyValue && (r5.patches++, delete e5.allowEmptyValue);
              })(e4, r4, t3);
            }));
          }
          function j2(e3, t3, r4) {
            let n3 = r4.payload.options;
            if (p2(e3, t3)) {
              if (e3[t3].startsWith("#/components/")) ;
              else if ("#/consumes" === e3[t3]) delete e3[t3], r4.parent[r4.pkey] = u2(n3.openapi.consumes);
              else if ("#/produces" === e3[t3]) delete e3[t3], r4.parent[r4.pkey] = u2(n3.openapi.produces);
              else if (e3[t3].startsWith("#/definitions/")) {
                let r5 = e3[t3].replace("#/definitions/", "").split("/");
                const o4 = l2.jpunescape(r5[0]);
                let i3 = v2.schemas[decodeURIComponent(o4)];
                i3 ? r5[0] = i3 : E2("Could not resolve reference " + e3[t3], e3, n3), e3[t3] = "#/components/schemas/" + r5.join("/");
              } else if (e3[t3].startsWith("#/parameters/")) e3[t3] = "#/components/parameters/" + g2.sanitise(e3[t3].replace("#/parameters/", ""));
              else if (e3[t3].startsWith("#/responses/")) e3[t3] = "#/components/responses/" + g2.sanitise(e3[t3].replace("#/responses/", ""));
              else if (e3[t3].startsWith("#")) {
                let r5 = u2(l2.jptr(n3.openapi, e3[t3]));
                if (false === r5) E2("direct $ref not found " + e3[t3], e3, n3);
                else if (n3.refmap[e3[t3]]) e3[t3] = n3.refmap[e3[t3]];
                else {
                  let i3 = e3[t3];
                  i3 = i3.replace("/properties/headers/", ""), i3 = i3.replace("/properties/responses/", ""), i3 = i3.replace("/properties/parameters/", ""), i3 = i3.replace("/properties/schemas/", "");
                  let s3 = "schemas", a3 = i3.lastIndexOf("/schema");
                  if (s3 = i3.indexOf("/headers/") > a3 ? "headers" : i3.indexOf("/responses/") > a3 ? "responses" : i3.indexOf("/example") > a3 ? "examples" : i3.indexOf("/x-") > a3 ? "extensions" : i3.indexOf("/parameters/") > a3 ? "parameters" : "schemas", "schemas" === s3 && O2(r5, n3), "responses" !== s3 && "extensions" !== s3) {
                    let i4 = s3.substr(0, s3.length - 1);
                    "parameter" === i4 && r5.name && r5.name === g2.sanitise(r5.name) && (i4 = encodeURIComponent(r5.name));
                    let a4 = 1;
                    for (e3["x-miro"] && (o3 = (o3 = e3["x-miro"]).indexOf("#") >= 0 ? o3.split("#")[1].split("/").pop() : o3.split("/").pop().split(".")[0], i4 = encodeURIComponent(g2.sanitise(o3)), a4 = ""); l2.jptr(n3.openapi, "#/components/" + s3 + "/" + i4 + a4); ) a4 = "" === a4 ? 2 : ++a4;
                    let c3 = "#/components/" + s3 + "/" + i4 + a4, p3 = "";
                    "examples" === s3 && (r5 = { value: r5 }, p3 = "/value"), l2.jptr(n3.openapi, c3, r5), n3.refmap[e3[t3]] = c3 + p3, e3[t3] = c3 + p3;
                  }
                }
              }
              if (delete e3["x-miro"], Object.keys(e3).length > 1) {
                const o4 = e3[t3], i3 = r4.path.indexOf("/schema") >= 0;
                "preserve" === n3.refSiblings || (i3 && "allOf" === n3.refSiblings ? (delete e3.$ref, r4.parent[r4.pkey] = { allOf: [{ $ref: o4 }, e3] }) : r4.parent[r4.pkey] = { $ref: o4 });
              }
            }
            var o3;
            if ("x-ms-odata" === t3 && "string" == typeof e3[t3] && e3[t3].startsWith("#/")) {
              let r5 = e3[t3].replace("#/definitions/", "").replace("#/components/schemas/", "").split("/"), o4 = v2.schemas[decodeURIComponent(r5[0])];
              o4 ? r5[0] = o4 : E2("Could not resolve reference " + e3[t3], e3, n3), e3[t3] = "#/components/schemas/" + r5.join("/");
            }
          }
          function $2(e3) {
            for (let t3 in e3) for (let r4 in e3[t3]) {
              let n3 = g2.sanitise(r4);
              r4 !== n3 && (e3[t3][n3] = e3[t3][r4], delete e3[t3][r4]);
            }
          }
          function S2(e3, t3) {
            if ("basic" === e3.type && (e3.type = "http", e3.scheme = "basic"), "oauth2" === e3.type) {
              let r4 = {}, n3 = e3.flow;
              "application" === e3.flow && (n3 = "clientCredentials"), "accessCode" === e3.flow && (n3 = "authorizationCode"), void 0 !== e3.authorizationUrl && (r4.authorizationUrl = e3.authorizationUrl.split("?")[0].trim() || "/"), "string" == typeof e3.tokenUrl && (r4.tokenUrl = e3.tokenUrl.split("?")[0].trim() || "/"), r4.scopes = e3.scopes || {}, e3.flows = {}, e3.flows[n3] = r4, delete e3.flow, delete e3.authorizationUrl, delete e3.tokenUrl, delete e3.scopes, void 0 !== e3.name && (t3.patch ? (t3.patches++, delete e3.name) : k2("(Patchable) oauth2 securitySchemes should not have name property", t3));
            }
          }
          function C2(e3) {
            return e3 && !e3["x-s2o-delete"];
          }
          function P2(e3, t3) {
            if (e3.$ref) e3.$ref = e3.$ref.replace("#/responses/", "#/components/responses/");
            else {
              e3.type && !e3.schema && (e3.schema = {}), e3.type && (e3.schema.type = e3.type), e3.items && "array" !== e3.items.type && (e3.items.collectionFormat !== e3.collectionFormat && E2("Nested collectionFormats are not supported", e3, t3), delete e3.items.collectionFormat), "array" === e3.type ? ("ssv" === e3.collectionFormat ? E2("collectionFormat:ssv is no longer supported for headers", e3, t3) : "pipes" === e3.collectionFormat ? E2("collectionFormat:pipes is no longer supported for headers", e3, t3) : "multi" === e3.collectionFormat ? e3.explode = true : "tsv" === e3.collectionFormat ? (E2("collectionFormat:tsv is no longer supported", e3, t3), e3["x-collectionFormat"] = "tsv") : e3.style = "simple", delete e3.collectionFormat) : e3.collectionFormat && (t3.patch ? (t3.patches++, delete e3.collectionFormat) : k2("(Patchable) collectionFormat is only applicable to header.type array", t3)), delete e3.type;
              for (let t4 of g2.parameterTypeProperties) void 0 !== e3[t4] && (e3.schema[t4] = e3[t4], delete e3[t4]);
              for (let t4 of g2.arrayProperties) void 0 !== e3[t4] && (e3.schema[t4] = e3[t4], delete e3[t4]);
            }
          }
          function I2(e3, t3) {
            if (e3.$ref.indexOf("#/parameters/") >= 0) {
              let t4 = e3.$ref.split("#/parameters/");
              e3.$ref = t4[0] + "#/components/parameters/" + g2.sanitise(t4[1]);
            }
            e3.$ref.indexOf("#/definitions/") >= 0 && E2("Definition used as parameter", e3, t3);
          }
          function A2(e3, t3, r4, n3, o3, i3, s3) {
            let a3, l3 = {}, p3 = true;
            if (t3 && t3.consumes && "string" == typeof t3.consumes) {
              if (!s3.patch) return k2("(Patchable) operation.consumes must be an array", s3);
              s3.patches++, t3.consumes = [t3.consumes];
            }
            Array.isArray(i3.consumes) || delete i3.consumes;
            let d2 = ((t3 ? t3.consumes : null) || i3.consumes || []).filter(g2.uniqueOnly);
            if (e3 && e3.$ref && "string" == typeof e3.$ref) {
              I2(e3, s3);
              let t4 = decodeURIComponent(e3.$ref.replace("#/components/parameters/", "")), r5 = false, n4 = i3.components.parameters[t4];
              if (n4 && !n4["x-s2o-delete"] || !e3.$ref.startsWith("#/") || (e3["x-s2o-delete"] = true, r5 = true), r5) {
                let t5 = e3.$ref, r6 = c2(i3, e3.$ref);
                !r6 && t5.startsWith("#/") ? E2("Could not resolve reference " + t5, e3, s3) : r6 && (e3 = r6);
              }
            }
            if (e3 && (e3.name || e3.in)) {
              "boolean" == typeof e3["x-deprecated"] && (e3.deprecated = e3["x-deprecated"], delete e3["x-deprecated"]), void 0 !== e3["x-example"] && (e3.example = e3["x-example"], delete e3["x-example"]), "body" === e3.in || e3.type || (s3.patch ? (s3.patches++, e3.type = "string") : k2("(Patchable) parameter.type is mandatory for non-body parameters", s3)), e3.type && "object" == typeof e3.type && e3.type.$ref && (e3.type = c2(i3, e3.type.$ref)), "file" === e3.type && (e3["x-s2o-originalType"] = e3.type, a3 = e3.type), e3.description && "object" == typeof e3.description && e3.description.$ref && (e3.description = c2(i3, e3.description.$ref)), null === e3.description && delete e3.description;
              let t4 = e3.collectionFormat;
              if ("array" !== e3.type || t4 || (t4 = "csv"), t4 && ("array" !== e3.type && (s3.patch ? (s3.patches++, delete e3.collectionFormat) : k2("(Patchable) collectionFormat is only applicable to param.type array", s3)), "csv" !== t4 || "query" !== e3.in && "cookie" !== e3.in || (e3.style = "form", e3.explode = false), "csv" !== t4 || "path" !== e3.in && "header" !== e3.in || (e3.style = "simple"), "ssv" === t4 && ("query" === e3.in ? e3.style = "spaceDelimited" : E2("collectionFormat:ssv is no longer supported except for in:query parameters", e3, s3)), "pipes" === t4 && ("query" === e3.in ? e3.style = "pipeDelimited" : E2("collectionFormat:pipes is no longer supported except for in:query parameters", e3, s3)), "multi" === t4 && (e3.explode = true), "tsv" === t4 && (E2("collectionFormat:tsv is no longer supported", e3, s3), e3["x-collectionFormat"] = "tsv"), delete e3.collectionFormat), e3.type && "body" !== e3.type && "formData" !== e3.in) if (e3.items && e3.schema) E2("parameter has array,items and schema", e3, s3);
              else {
                e3.schema && s3.patches++, e3.schema && "object" == typeof e3.schema || (e3.schema = {}), e3.schema.type = e3.type, e3.items && (e3.schema.items = e3.items, delete e3.items, h(e3.schema.items, null, (function(r5, n4, o4) {
                  "collectionFormat" === n4 && "string" == typeof r5[n4] && (t4 && r5[n4] !== t4 && E2("Nested collectionFormats are not supported", e3, s3), delete r5[n4]);
                })));
                for (let t5 of g2.parameterTypeProperties) void 0 !== e3[t5] && (e3.schema[t5] = e3[t5]), delete e3[t5];
              }
              e3.schema && O2(e3.schema, s3), e3["x-ms-skip-url-encoding"] && "query" === e3.in && (e3.allowReserved = true, delete e3["x-ms-skip-url-encoding"]);
            }
            if (e3 && "formData" === e3.in) {
              p3 = false, l3.content = {};
              let t4 = "application/x-www-form-urlencoded";
              if (d2.length && d2.indexOf("multipart/form-data") >= 0 && (t4 = "multipart/form-data"), l3.content[t4] = {}, e3.schema) l3.content[t4].schema = e3.schema, e3.schema.$ref && (l3["x-s2o-name"] = decodeURIComponent(e3.schema.$ref.replace("#/components/schemas/", "")));
              else {
                l3.content[t4].schema = {}, l3.content[t4].schema.type = "object", l3.content[t4].schema.properties = {}, l3.content[t4].schema.properties[e3.name] = {};
                let r5 = l3.content[t4].schema, n4 = l3.content[t4].schema.properties[e3.name];
                e3.description && (n4.description = e3.description), e3.example && (n4.example = e3.example), e3.type && (n4.type = e3.type);
                for (let t5 of g2.parameterTypeProperties) void 0 !== e3[t5] && (n4[t5] = e3[t5]);
                true === e3.required && (r5.required || (r5.required = []), r5.required.push(e3.name), l3.required = true), void 0 !== e3.default && (n4.default = e3.default), n4.properties && (n4.properties = e3.properties), e3.allOf && (n4.allOf = e3.allOf), "array" === e3.type && e3.items && (n4.items = e3.items, n4.items.collectionFormat && delete n4.items.collectionFormat), "file" !== a3 && "file" !== e3["x-s2o-originalType"] || (n4.type = "string", n4.format = "binary"), T2(e3, n4);
              }
            } else e3 && "file" === e3.type && (e3.required && (l3.required = e3.required), l3.content = {}, l3.content["application/octet-stream"] = {}, l3.content["application/octet-stream"].schema = {}, l3.content["application/octet-stream"].schema.type = "string", l3.content["application/octet-stream"].schema.format = "binary", T2(e3, l3));
            if (e3 && "body" === e3.in) {
              l3.content = {}, e3.name && (l3["x-s2o-name"] = (t3 && t3.operationId ? g2.sanitiseAll(t3.operationId) : "") + ("_" + e3.name).toCamelCase()), e3.description && (l3.description = e3.description), e3.required && (l3.required = e3.required), t3 && s3.rbname && e3.name && (t3[s3.rbname] = e3.name), e3.schema && e3.schema.$ref ? l3["x-s2o-name"] = decodeURIComponent(e3.schema.$ref.replace("#/components/schemas/", "")) : e3.schema && "array" === e3.schema.type && e3.schema.items && e3.schema.items.$ref && (l3["x-s2o-name"] = decodeURIComponent(e3.schema.items.$ref.replace("#/components/schemas/", "")) + "Array"), d2.length || d2.push("application/json");
              for (let t4 of d2) l3.content[t4] = {}, l3.content[t4].schema = u2(e3.schema || {}), O2(l3.content[t4].schema, s3);
              T2(e3, l3);
            }
            if (Object.keys(l3).length > 0 && (e3["x-s2o-delete"] = true, t3) && (t3.requestBody && p3 ? (t3.requestBody["x-s2o-overloaded"] = true, E2("Operation " + (t3.operationId || o3) + " has multiple requestBodies", t3, s3)) : (t3.requestBody || (t3 = r4[n3] = (function(e4, t4) {
              let r5 = {};
              for (let n4 of Object.keys(e4)) r5[n4] = e4[n4], "parameters" === n4 && (r5.requestBody = {}, t4.rbname && (r5[t4.rbname] = ""));
              return r5.requestBody = {}, r5;
            })(t3, s3)), t3.requestBody.content && t3.requestBody.content["multipart/form-data"] && t3.requestBody.content["multipart/form-data"].schema && t3.requestBody.content["multipart/form-data"].schema.properties && l3.content["multipart/form-data"] && l3.content["multipart/form-data"].schema && l3.content["multipart/form-data"].schema.properties ? (t3.requestBody.content["multipart/form-data"].schema.properties = Object.assign(t3.requestBody.content["multipart/form-data"].schema.properties, l3.content["multipart/form-data"].schema.properties), t3.requestBody.content["multipart/form-data"].schema.required = (t3.requestBody.content["multipart/form-data"].schema.required || []).concat(l3.content["multipart/form-data"].schema.required || []), t3.requestBody.content["multipart/form-data"].schema.required.length || delete t3.requestBody.content["multipart/form-data"].schema.required) : t3.requestBody.content && t3.requestBody.content["application/x-www-form-urlencoded"] && t3.requestBody.content["application/x-www-form-urlencoded"].schema && t3.requestBody.content["application/x-www-form-urlencoded"].schema.properties && l3.content["application/x-www-form-urlencoded"] && l3.content["application/x-www-form-urlencoded"].schema && l3.content["application/x-www-form-urlencoded"].schema.properties ? (t3.requestBody.content["application/x-www-form-urlencoded"].schema.properties = Object.assign(t3.requestBody.content["application/x-www-form-urlencoded"].schema.properties, l3.content["application/x-www-form-urlencoded"].schema.properties), t3.requestBody.content["application/x-www-form-urlencoded"].schema.required = (t3.requestBody.content["application/x-www-form-urlencoded"].schema.required || []).concat(l3.content["application/x-www-form-urlencoded"].schema.required || []), t3.requestBody.content["application/x-www-form-urlencoded"].schema.required.length || delete t3.requestBody.content["application/x-www-form-urlencoded"].schema.required) : (t3.requestBody = Object.assign(t3.requestBody, l3), t3.requestBody["x-s2o-name"] || (t3.requestBody.schema && t3.requestBody.schema.$ref ? t3.requestBody["x-s2o-name"] = decodeURIComponent(t3.requestBody.schema.$ref.replace("#/components/schemas/", "")).split("/").join("") : t3.operationId && (t3.requestBody["x-s2o-name"] = g2.sanitiseAll(t3.operationId)))))), e3 && !e3["x-s2o-delete"]) {
              delete e3.type;
              for (let t4 of g2.parameterTypeProperties) delete e3[t4];
              "path" !== e3.in || void 0 !== e3.required && true === e3.required || (s3.patch ? (s3.patches++, e3.required = true) : k2("(Patchable) path parameters must be required:true [" + e3.name + " in " + o3 + "]", s3));
            }
            return t3;
          }
          function T2(e3, t3) {
            for (let r4 in e3) r4.startsWith("x-") && !r4.startsWith("x-s2o") && (t3[r4] = e3[r4]);
          }
          function _15(e3, t3, r4, n3, o3) {
            if (!e3) return false;
            if (e3.$ref && "string" == typeof e3.$ref) e3.$ref.indexOf("#/definitions/") >= 0 ? E2("definition used as response: " + e3.$ref, e3, o3) : e3.$ref.startsWith("#/responses/") && (e3.$ref = "#/components/responses/" + g2.sanitise(decodeURIComponent(e3.$ref.replace("#/responses/", ""))));
            else {
              if ((void 0 === e3.description || null === e3.description || "" === e3.description && o3.patch) && (o3.patch ? "object" != typeof e3 || Array.isArray(e3) || (o3.patches++, e3.description = y2[e3] || "") : k2("(Patchable) response.description is mandatory", o3)), void 0 !== e3.schema) {
                if (O2(e3.schema, o3), e3.schema.$ref && "string" == typeof e3.schema.$ref && e3.schema.$ref.startsWith("#/responses/") && (e3.schema.$ref = "#/components/responses/" + g2.sanitise(decodeURIComponent(e3.schema.$ref.replace("#/responses/", "")))), r4 && r4.produces && "string" == typeof r4.produces) {
                  if (!o3.patch) return k2("(Patchable) operation.produces must be an array", o3);
                  o3.patches++, r4.produces = [r4.produces];
                }
                n3.produces && !Array.isArray(n3.produces) && delete n3.produces;
                let t4 = ((r4 ? r4.produces : null) || n3.produces || []).filter(g2.uniqueOnly);
                t4.length || t4.push("*/*"), e3.content = {};
                for (let r5 of t4) {
                  if (e3.content[r5] = {}, e3.content[r5].schema = u2(e3.schema), e3.examples && e3.examples[r5]) {
                    let t5 = {};
                    t5.value = e3.examples[r5], e3.content[r5].examples = {}, e3.content[r5].examples.response = t5, delete e3.examples[r5];
                  }
                  "file" === e3.content[r5].schema.type && (e3.content[r5].schema = { type: "string", format: "binary" });
                }
                delete e3.schema;
              }
              for (let t4 in e3.examples) e3.content || (e3.content = {}), e3.content[t4] || (e3.content[t4] = {}), e3.content[t4].examples = {}, e3.content[t4].examples.response = {}, e3.content[t4].examples.response.value = e3.examples[t4];
              if (delete e3.examples, e3.headers) for (let t4 in e3.headers) "status code" === t4.toLowerCase() ? o3.patch ? (o3.patches++, delete e3.headers[t4]) : k2('(Patchable) "Status Code" is not a valid header', o3) : P2(e3.headers[t4], o3);
            }
          }
          function R2(e3, t3, r4, n3, i3) {
            for (let s3 in e3) {
              let a3 = e3[s3];
              a3 && a3["x-trace"] && "object" == typeof a3["x-trace"] && (a3.trace = a3["x-trace"], delete a3["x-trace"]), a3 && a3["x-summary"] && "string" == typeof a3["x-summary"] && (a3.summary = a3["x-summary"], delete a3["x-summary"]), a3 && a3["x-description"] && "string" == typeof a3["x-description"] && (a3.description = a3["x-description"], delete a3["x-description"]), a3 && a3["x-servers"] && Array.isArray(a3["x-servers"]) && (a3.servers = a3["x-servers"], delete a3["x-servers"]);
              for (let e4 in a3) if (g2.httpMethods.indexOf(e4) >= 0 || "x-amazon-apigateway-any-method" === e4) {
                let p3 = a3[e4];
                if (p3 && p3.parameters && Array.isArray(p3.parameters)) {
                  if (a3.parameters) for (let t4 of a3.parameters) "string" == typeof t4.$ref && (I2(t4, r4), t4 = c2(i3, t4.$ref)), p3.parameters.find((function(e5, r5, n4) {
                    return e5.name === t4.name && e5.in === t4.in;
                  })) || "formData" !== t4.in && "body" !== t4.in && "file" !== t4.type || (p3 = A2(t4, p3, a3, e4, s3, i3, r4), r4.rbname && "" === p3[r4.rbname] && delete p3[r4.rbname]);
                  for (let t4 of p3.parameters) p3 = A2(t4, p3, a3, e4, e4 + ":" + s3, i3, r4);
                  r4.rbname && "" === p3[r4.rbname] && delete p3[r4.rbname], r4.debug || p3.parameters && (p3.parameters = p3.parameters.filter(C2));
                }
                if (p3 && p3.security && $2(p3.security), "object" == typeof p3) {
                  if (!p3.responses) {
                    let e5 = { description: "Default response" };
                    p3.responses = { default: e5 };
                  }
                  for (let e5 in p3.responses) _15(p3.responses[e5], 0, p3, i3, r4);
                }
                if (p3 && p3["x-servers"] && Array.isArray(p3["x-servers"])) p3.servers = p3["x-servers"], delete p3["x-servers"];
                else if (p3 && p3.schemes && p3.schemes.length) {
                  for (let e5 of p3.schemes) if ((!i3.schemes || i3.schemes.indexOf(e5) < 0) && (p3.servers || (p3.servers = []), Array.isArray(i3.servers))) for (let t4 of i3.servers) {
                    let r5 = u2(t4), n4 = o2.parse(r5.url);
                    n4.protocol = e5, r5.url = n4.format(), p3.servers.push(r5);
                  }
                }
                if (r4.debug && (p3["x-s2o-consumes"] = p3.consumes || [], p3["x-s2o-produces"] = p3.produces || []), p3) {
                  if (delete p3.consumes, delete p3.produces, delete p3.schemes, p3["x-ms-examples"]) {
                    for (let e5 in p3["x-ms-examples"]) {
                      let t4 = p3["x-ms-examples"][e5], r5 = g2.sanitiseAll(e5);
                      if (t4.parameters) for (let r6 in t4.parameters) {
                        let n4 = t4.parameters[r6];
                        for (let t5 of (p3.parameters || []).concat(a3.parameters || [])) t5.$ref && (t5 = l2.jptr(i3, t5.$ref)), t5.name !== r6 || t5.example || (t5.examples || (t5.examples = {}), t5.examples[e5] = { value: n4 });
                      }
                      if (t4.responses) for (let n4 in t4.responses) {
                        if (t4.responses[n4].headers) for (let e6 in t4.responses[n4].headers) {
                          let r6 = t4.responses[n4].headers[e6];
                          for (let t5 in p3.responses[n4].headers) t5 === e6 && (p3.responses[n4].headers[t5].example = r6);
                        }
                        if (t4.responses[n4].body && (i3.components.examples[r5] = { value: u2(t4.responses[n4].body) }, p3.responses[n4] && p3.responses[n4].content)) for (let t5 in p3.responses[n4].content) {
                          let o3 = p3.responses[n4].content[t5];
                          o3.examples || (o3.examples = {}), o3.examples[e5] = { $ref: "#/components/examples/" + r5 };
                        }
                      }
                    }
                    delete p3["x-ms-examples"];
                  }
                  if (p3.parameters && 0 === p3.parameters.length && delete p3.parameters, p3.requestBody) {
                    let r5 = p3.operationId ? g2.sanitiseAll(p3.operationId) : g2.sanitiseAll(e4 + s3).toCamelCase(), o3 = g2.sanitise(p3.requestBody["x-s2o-name"] || r5 || "");
                    delete p3.requestBody["x-s2o-name"];
                    let i4 = JSON.stringify(p3.requestBody), a4 = g2.hash(i4);
                    if (!n3[a4]) {
                      let e5 = {};
                      e5.name = o3, e5.body = p3.requestBody, e5.refs = [], n3[a4] = e5;
                    }
                    let c3 = "#/" + t3 + "/" + encodeURIComponent(l2.jpescape(s3)) + "/" + e4 + "/requestBody";
                    n3[a4].refs.push(c3);
                  }
                }
              }
              if (a3 && a3.parameters) {
                for (let e4 in a3.parameters) A2(a3.parameters[e4], null, a3, null, s3, i3, r4);
                !r4.debug && Array.isArray(a3.parameters) && (a3.parameters = a3.parameters.filter(C2));
              }
            }
          }
          function D2(e3) {
            return e3 && e3.url && "string" == typeof e3.url ? (e3.url = e3.url.split("{{").join("{"), e3.url = e3.url.split("}}").join("}"), e3.url.replace(/\{(.+?)\}/g, (function(t3, r4) {
              e3.variables || (e3.variables = {}), e3.variables[r4] = { default: "unknown" };
            })), e3) : e3;
          }
          function z2(e3, t3, r4) {
            if (void 0 === e3.info || null === e3.info) {
              if (!t3.patch) return r4(new w2("(Patchable) info object is mandatory"));
              t3.patches++, e3.info = { version: "", title: "" };
            }
            if ("object" != typeof e3.info || Array.isArray(e3.info)) return r4(new w2("info must be an object"));
            if (void 0 === e3.info.title || null === e3.info.title) {
              if (!t3.patch) return r4(new w2("(Patchable) info.title cannot be null"));
              t3.patches++, e3.info.title = "";
            }
            if (void 0 === e3.info.version || null === e3.info.version) {
              if (!t3.patch) return r4(new w2("(Patchable) info.version cannot be null"));
              t3.patches++, e3.info.version = "";
            }
            if ("string" != typeof e3.info.version) {
              if (!t3.patch) return r4(new w2("(Patchable) info.version must be a string"));
              t3.patches++, e3.info.version = e3.info.version.toString();
            }
            if (void 0 !== e3.info.logo) {
              if (!t3.patch) return r4(new w2("(Patchable) info should not have logo property"));
              t3.patches++, e3.info["x-logo"] = e3.info.logo, delete e3.info.logo;
            }
            if (void 0 !== e3.info.termsOfService) {
              if (null === e3.info.termsOfService) {
                if (!t3.patch) return r4(new w2("(Patchable) info.termsOfService cannot be null"));
                t3.patches++, e3.info.termsOfService = "";
              }
              try {
                new URL(e3.info.termsOfService);
              } catch (n3) {
                if (!t3.patch) return r4(new w2("(Patchable) info.termsOfService must be a URL"));
                t3.patches++, delete e3.info.termsOfService;
              }
            }
          }
          function L2(e3, t3, r4) {
            if (void 0 === e3.paths) {
              if (!t3.patch) return r4(new w2("(Patchable) paths object is mandatory"));
              t3.patches++, e3.paths = {};
            }
          }
          function N2(e3, t3, r4) {
            return i2(r4, new Promise((function(r5, n3) {
              if (e3 || (e3 = {}), t3.original = e3, t3.text || (t3.text = a2.stringify(e3)), t3.externals = [], t3.externalRefs = {}, t3.rewriteRefs = true, t3.preserveMiro = true, t3.promise = {}, t3.promise.resolve = r5, t3.promise.reject = n3, t3.patches = 0, t3.cache || (t3.cache = {}), t3.source && (t3.cache[t3.source] = t3.original), (function(e4, t4) {
                const r6 = /* @__PURE__ */ new WeakSet();
                h(e4, { identityDetection: true }, (function(e5, n4, o4) {
                  "object" == typeof e5[n4] && null !== e5[n4] && (r6.has(e5[n4]) ? t4.anchors ? e5[n4] = u2(e5[n4]) : k2("YAML anchor or merge key at " + o4.path, t4) : r6.add(e5[n4]));
                }));
              })(e3, t3), e3.openapi && "string" == typeof e3.openapi && e3.openapi.startsWith("3.")) return t3.openapi = d(e3), z2(t3.openapi, t3, n3), L2(t3.openapi, t3, n3), void m2.optionalResolve(t3).then((function() {
                return t3.direct ? r5(t3.openapi) : r5(t3);
              })).catch((function(e4) {
                console.warn(e4), n3(e4);
              }));
              if (!e3.swagger || "2.0" != e3.swagger) return n3(new w2("Unsupported swagger/OpenAPI version: " + (e3.openapi ? e3.openapi : e3.swagger)));
              let o3 = t3.openapi = {};
              if (o3.openapi = "string" == typeof t3.targetVersion && t3.targetVersion.startsWith("3.") ? t3.targetVersion : x2, t3.origin) {
                o3["x-origin"] || (o3["x-origin"] = []);
                let r6 = {};
                r6.url = t3.source || t3.origin, r6.format = "swagger", r6.version = e3.swagger, r6.converter = {}, r6.converter.url = "https://github.com/mermade/oas-kit", r6.converter.version = b2, o3["x-origin"].push(r6);
              }
              if (o3 = Object.assign(o3, d(e3)), delete o3.swagger, h(o3, {}, (function(e4, t4, r6) {
                null === e4[t4] && !t4.startsWith("x-") && "default" !== t4 && r6.path.indexOf("/example") < 0 && delete e4[t4];
              })), e3.host) for (let t4 of Array.isArray(e3.schemes) ? e3.schemes : [""]) {
                let r6 = {}, n4 = (e3.basePath || "").replace(/\/$/, "");
                r6.url = (t4 ? t4 + ":" : "") + "//" + e3.host + n4, D2(r6), o3.servers || (o3.servers = []), o3.servers.push(r6);
              }
              else if (e3.basePath) {
                let t4 = {};
                t4.url = e3.basePath, D2(t4), o3.servers || (o3.servers = []), o3.servers.push(t4);
              }
              if (delete o3.host, delete o3.basePath, o3["x-servers"] && Array.isArray(o3["x-servers"]) && (o3.servers = o3["x-servers"], delete o3["x-servers"]), e3["x-ms-parameterized-host"]) {
                let t4 = e3["x-ms-parameterized-host"], r6 = {};
                r6.url = t4.hostTemplate + (e3.basePath ? e3.basePath : ""), r6.variables = {};
                const n4 = r6.url.match(/\{\w+\}/g);
                for (let e4 in t4.parameters) {
                  let i3 = t4.parameters[e4];
                  i3.$ref && (i3 = u2(c2(o3, i3.$ref))), e4.startsWith("x-") || (delete i3.required, delete i3.type, delete i3.in, void 0 === i3.default && (i3.enum ? i3.default = i3.enum[0] : i3.default = "none"), i3.name || (i3.name = n4[e4].replace("{", "").replace("}", "")), r6.variables[i3.name] = i3, delete i3.name);
                }
                o3.servers || (o3.servers = []), false === t4.useSchemePrefix ? o3.servers.push(r6) : e3.schemes.forEach(((e4) => {
                  o3.servers.push(Object.assign({}, r6, { url: e4 + "://" + r6.url }));
                })), delete o3["x-ms-parameterized-host"];
              }
              z2(o3, t3, n3), L2(o3, t3, n3), "string" == typeof o3.consumes && (o3.consumes = [o3.consumes]), "string" == typeof o3.produces && (o3.produces = [o3.produces]), o3.components = {}, o3["x-callbacks"] && (o3.components.callbacks = o3["x-callbacks"], delete o3["x-callbacks"]), o3.components.examples = {}, o3.components.headers = {}, o3["x-links"] && (o3.components.links = o3["x-links"], delete o3["x-links"]), o3.components.parameters = o3.parameters || {}, o3.components.responses = o3.responses || {}, o3.components.requestBodies = {}, o3.components.securitySchemes = o3.securityDefinitions || {}, o3.components.schemas = o3.definitions || {}, delete o3.definitions, delete o3.responses, delete o3.parameters, delete o3.securityDefinitions, m2.optionalResolve(t3).then((function() {
                (function(e4, t4) {
                  let r6 = {};
                  v2 = { schemas: {} }, e4.security && $2(e4.security);
                  for (let r7 in e4.components.securitySchemes) {
                    let n5 = g2.sanitise(r7);
                    r7 !== n5 && (e4.components.securitySchemes[n5] && k2("Duplicate sanitised securityScheme name " + n5, t4), e4.components.securitySchemes[n5] = e4.components.securitySchemes[r7], delete e4.components.securitySchemes[r7]), S2(e4.components.securitySchemes[n5], t4);
                  }
                  for (let r7 in e4.components.schemas) {
                    let n5 = g2.sanitiseAll(r7), o4 = "";
                    if (r7 !== n5) {
                      for (; e4.components.schemas[n5 + o4]; ) o4 = o4 ? ++o4 : 2;
                      e4.components.schemas[n5 + o4] = e4.components.schemas[r7], delete e4.components.schemas[r7];
                    }
                    v2.schemas[r7] = n5 + o4, O2(e4.components.schemas[n5 + o4], t4);
                  }
                  t4.refmap = {}, h(e4, { payload: { options: t4 } }, j2), (function(e5, t5) {
                    for (let r7 in t5.refmap) l2.jptr(e5, r7, { $ref: t5.refmap[r7] });
                  })(e4, t4);
                  for (let r7 in e4.components.parameters) {
                    let n5 = g2.sanitise(r7);
                    r7 !== n5 && (e4.components.parameters[n5] && k2("Duplicate sanitised parameter name " + n5, t4), e4.components.parameters[n5] = e4.components.parameters[r7], delete e4.components.parameters[r7]), A2(e4.components.parameters[n5], null, null, null, n5, e4, t4);
                  }
                  for (let r7 in e4.components.responses) {
                    let n5 = g2.sanitise(r7);
                    r7 !== n5 && (e4.components.responses[n5] && k2("Duplicate sanitised response name " + n5, t4), e4.components.responses[n5] = e4.components.responses[r7], delete e4.components.responses[r7]);
                    let o4 = e4.components.responses[n5];
                    if (_15(o4, 0, null, e4, t4), o4.headers) for (let e5 in o4.headers) "status code" === e5.toLowerCase() ? t4.patch ? (t4.patches++, delete o4.headers[e5]) : k2('(Patchable) "Status Code" is not a valid header', t4) : P2(o4.headers[e5], t4);
                  }
                  for (let t5 in e4.components.requestBodies) {
                    let n5 = e4.components.requestBodies[t5], o4 = JSON.stringify(n5), i3 = g2.hash(o4), s3 = {};
                    s3.name = t5, s3.body = n5, s3.refs = [], r6[i3] = s3;
                  }
                  if (R2(e4.paths, "paths", t4, r6, e4), e4["x-ms-paths"] && R2(e4["x-ms-paths"], "x-ms-paths", t4, r6, e4), !t4.debug) for (let t5 in e4.components.parameters) e4.components.parameters[t5]["x-s2o-delete"] && delete e4.components.parameters[t5];
                  t4.debug && (e4["x-s2o-consumes"] = e4.consumes || [], e4["x-s2o-produces"] = e4.produces || []), delete e4.consumes, delete e4.produces, delete e4.schemes;
                  let n4 = [];
                  if (e4.components.requestBodies = {}, !t4.resolveInternal) {
                    let t5 = 1;
                    for (let o4 in r6) {
                      let i3 = r6[o4];
                      if (i3.refs.length > 1) {
                        let r7 = "";
                        for (i3.name || (i3.name = "requestBody", r7 = t5++); n4.indexOf(i3.name + r7) >= 0; ) r7 = r7 ? ++r7 : 2;
                        i3.name = i3.name + r7, n4.push(i3.name), e4.components.requestBodies[i3.name] = u2(i3.body);
                        for (let t6 in i3.refs) {
                          let r8 = {};
                          r8.$ref = "#/components/requestBodies/" + i3.name, l2.jptr(e4, i3.refs[t6], r8);
                        }
                      }
                    }
                  }
                  e4.components.responses && 0 === Object.keys(e4.components.responses).length && delete e4.components.responses, e4.components.parameters && 0 === Object.keys(e4.components.parameters).length && delete e4.components.parameters, e4.components.examples && 0 === Object.keys(e4.components.examples).length && delete e4.components.examples, e4.components.requestBodies && 0 === Object.keys(e4.components.requestBodies).length && delete e4.components.requestBodies, e4.components.securitySchemes && 0 === Object.keys(e4.components.securitySchemes).length && delete e4.components.securitySchemes, e4.components.headers && 0 === Object.keys(e4.components.headers).length && delete e4.components.headers, e4.components.schemas && 0 === Object.keys(e4.components.schemas).length && delete e4.components.schemas, e4.components && 0 === Object.keys(e4.components).length && delete e4.components;
                })(t3.openapi, t3), t3.direct ? r5(t3.openapi) : r5(t3);
              })).catch((function(e4) {
                console.warn(e4), n3(e4);
              }));
            })));
          }
          function M2(e3, t3, r4) {
            return i2(r4, new Promise((function(r5, n3) {
              let o3 = null, i3 = null;
              try {
                o3 = JSON.parse(e3), t3.text = JSON.stringify(o3, null, 2);
              } catch (r6) {
                i3 = r6;
                try {
                  o3 = a2.parse(e3, { schema: "core", prettyErrors: true }), t3.sourceYaml = true, t3.text = e3;
                } catch (e4) {
                  i3 = e4;
                }
              }
              o3 ? N2(o3, t3).then(((e4) => r5(e4))).catch(((e4) => n3(e4))) : n3(new w2(i3 ? i3.message : "Could not parse string"));
            })));
          }
          e2.exports = { S2OError: w2, targetVersion: x2, convert: N2, convertObj: N2, convertUrl: function(e3, t3, r4) {
            return i2(r4, new Promise((function(r5, n3) {
              t3.origin = true, t3.source || (t3.source = e3), t3.verbose && console.warn("GET " + e3), t3.fetch || (t3.fetch = s2);
              const o3 = Object.assign({}, t3.fetchOptions, { agent: t3.agent });
              t3.fetch(e3, o3).then((function(t4) {
                if (200 !== t4.status) throw new w2(`Received status code ${t4.status}: ${e3}`);
                return t4.text();
              })).then((function(e4) {
                M2(e4, t3).then(((e5) => r5(e5))).catch(((e5) => n3(e5)));
              })).catch((function(e4) {
                n3(e4);
              }));
            })));
          }, convertStr: M2, convertFile: function(e3, t3, r4) {
            return i2(r4, new Promise((function(r5, o3) {
              n2.readFile(e3, t3.encoding || "utf8", (function(n3, i3) {
                n3 ? o3(n3) : (t3.sourceFile = e3, M2(i3, t3).then(((e4) => r5(e4))).catch(((e4) => o3(e4))));
              }));
            })));
          }, convertStream: function(e3, t3, r4) {
            return i2(r4, new Promise((function(r5, n3) {
              let o3 = "";
              e3.on("data", (function(e4) {
                o3 += e4;
              })).on("end", (function() {
                M2(o3, t3).then(((e4) => r5(e4))).catch(((e4) => n3(e4)));
              }));
            })));
          } };
        }, 665: function(e2, t2, r3) {
          "use strict";
          const n2 = r3(375);
          e2.exports = { statusCodes: Object.assign({}, { default: "Default response", "1XX": "Informational", 103: "Early hints", "2XX": "Successful", "3XX": "Redirection", "4XX": "Client Error", "5XX": "Server Error", "7XX": "Developer Error" }, n2.STATUS_CODES) };
        }, 988: function(e2, t2, r3) {
          var n2 = r3(7), o2 = ["add", "done", "toJS", "fromExternalJS", "load", "dispose", "search", "Worker"];
          e2.exports = function() {
            var e3 = new Worker(URL.createObjectURL(new Blob([`/*! For license information please see cfb294d7f6536ffa8d42.worker.js.LICENSE.txt */
!function(){var e={291:function(e,t,r){var n,i;!function(){var s,o,a,u,l,c,h,d,f,p,y,m,g,x,v,w,Q,k,S,E,L,P,b,T,O,I,R,F,C,N,j=function(e){var t=new j.Builder;return t.pipeline.add(j.trimmer,j.stopWordFilter,j.stemmer),t.searchPipeline.add(j.stemmer),e.call(t,t),t.build()};j.version="2.3.9",j.utils={},j.utils.warn=(s=this,function(e){s.console&&console.warn&&console.warn(e)}),j.utils.asString=function(e){return null==e?"":e.toString()},j.utils.clone=function(e){if(null==e)return e;for(var t=Object.create(null),r=Object.keys(e),n=0;n<r.length;n++){var i=r[n],s=e[i];if(Array.isArray(s))t[i]=s.slice();else{if("string"!=typeof s&&"number"!=typeof s&&"boolean"!=typeof s)throw new TypeError("clone is not deep and does not support nested objects");t[i]=s}}return t},j.FieldRef=function(e,t,r){this.docRef=e,this.fieldName=t,this._stringValue=r},j.FieldRef.joiner="/",j.FieldRef.fromString=function(e){var t=e.indexOf(j.FieldRef.joiner);if(-1===t)throw"malformed field ref string";var r=e.slice(0,t),n=e.slice(t+1);return new j.FieldRef(n,r,e)},j.FieldRef.prototype.toString=function(){return null==this._stringValue&&(this._stringValue=this.fieldName+j.FieldRef.joiner+this.docRef),this._stringValue},j.Set=function(e){if(this.elements=Object.create(null),e){this.length=e.length;for(var t=0;t<this.length;t++)this.elements[e[t]]=!0}else this.length=0},j.Set.complete={intersect:function(e){return e},union:function(){return this},contains:function(){return!0}},j.Set.empty={intersect:function(){return this},union:function(e){return e},contains:function(){return!1}},j.Set.prototype.contains=function(e){return!!this.elements[e]},j.Set.prototype.intersect=function(e){var t,r,n,i=[];if(e===j.Set.complete)return this;if(e===j.Set.empty)return e;this.length<e.length?(t=this,r=e):(t=e,r=this),n=Object.keys(t.elements);for(var s=0;s<n.length;s++){var o=n[s];o in r.elements&&i.push(o)}return new j.Set(i)},j.Set.prototype.union=function(e){return e===j.Set.complete?j.Set.complete:e===j.Set.empty?this:new j.Set(Object.keys(this.elements).concat(Object.keys(e.elements)))},j.idf=function(e,t){var r=0;for(var n in e)"_index"!=n&&(r+=Object.keys(e[n]).length);var i=(t-r+.5)/(r+.5);return Math.log(1+Math.abs(i))},j.Token=function(e,t){this.str=e||"",this.metadata=t||{}},j.Token.prototype.toString=function(){return this.str},j.Token.prototype.update=function(e){return this.str=e(this.str,this.metadata),this},j.Token.prototype.clone=function(e){return e=e||function(e){return e},new j.Token(e(this.str,this.metadata),this.metadata)},j.tokenizer=function(e,t){if(null==e||null==e)return[];if(Array.isArray(e))return e.map((function(e){return new j.Token(j.utils.asString(e).toLowerCase(),j.utils.clone(t))}));for(var r=e.toString().toLowerCase(),n=r.length,i=[],s=0,o=0;s<=n;s++){var a=s-o;if(r.charAt(s).match(j.tokenizer.separator)||s==n){if(a>0){var u=j.utils.clone(t)||{};u.position=[o,a],u.index=i.length,i.push(new j.Token(r.slice(o,s),u))}o=s+1}}return i},j.tokenizer.separator=/[\\s\\-]+/,j.Pipeline=function(){this._stack=[]},j.Pipeline.registeredFunctions=Object.create(null),j.Pipeline.registerFunction=function(e,t){t in this.registeredFunctions&&j.utils.warn("Overwriting existing registered function: "+t),e.label=t,j.Pipeline.registeredFunctions[e.label]=e},j.Pipeline.warnIfFunctionNotRegistered=function(e){e.label&&e.label in this.registeredFunctions||j.utils.warn("Function is not registered with pipeline. This may cause problems when serialising the index.\\n",e)},j.Pipeline.load=function(e){var t=new j.Pipeline;return e.forEach((function(e){var r=j.Pipeline.registeredFunctions[e];if(!r)throw new Error("Cannot load unregistered function: "+e);t.add(r)})),t},j.Pipeline.prototype.add=function(){Array.prototype.slice.call(arguments).forEach((function(e){j.Pipeline.warnIfFunctionNotRegistered(e),this._stack.push(e)}),this)},j.Pipeline.prototype.after=function(e,t){j.Pipeline.warnIfFunctionNotRegistered(t);var r=this._stack.indexOf(e);if(-1==r)throw new Error("Cannot find existingFn");r+=1,this._stack.splice(r,0,t)},j.Pipeline.prototype.before=function(e,t){j.Pipeline.warnIfFunctionNotRegistered(t);var r=this._stack.indexOf(e);if(-1==r)throw new Error("Cannot find existingFn");this._stack.splice(r,0,t)},j.Pipeline.prototype.remove=function(e){var t=this._stack.indexOf(e);-1!=t&&this._stack.splice(t,1)},j.Pipeline.prototype.run=function(e){for(var t=this._stack.length,r=0;r<t;r++){for(var n=this._stack[r],i=[],s=0;s<e.length;s++){var o=n(e[s],s,e);if(null!=o&&""!==o)if(Array.isArray(o))for(var a=0;a<o.length;a++)i.push(o[a]);else i.push(o)}e=i}return e},j.Pipeline.prototype.runString=function(e,t){var r=new j.Token(e,t);return this.run([r]).map((function(e){return e.toString()}))},j.Pipeline.prototype.reset=function(){this._stack=[]},j.Pipeline.prototype.toJSON=function(){return this._stack.map((function(e){return j.Pipeline.warnIfFunctionNotRegistered(e),e.label}))},j.Vector=function(e){this._magnitude=0,this.elements=e||[]},j.Vector.prototype.positionForIndex=function(e){if(0==this.elements.length)return 0;for(var t=0,r=this.elements.length/2,n=r-t,i=Math.floor(n/2),s=this.elements[2*i];n>1&&(s<e&&(t=i),s>e&&(r=i),s!=e);)n=r-t,i=t+Math.floor(n/2),s=this.elements[2*i];return s==e||s>e?2*i:s<e?2*(i+1):void 0},j.Vector.prototype.insert=function(e,t){this.upsert(e,t,(function(){throw"duplicate index"}))},j.Vector.prototype.upsert=function(e,t,r){this._magnitude=0;var n=this.positionForIndex(e);this.elements[n]==e?this.elements[n+1]=r(this.elements[n+1],t):this.elements.splice(n,0,e,t)},j.Vector.prototype.magnitude=function(){if(this._magnitude)return this._magnitude;for(var e=0,t=this.elements.length,r=1;r<t;r+=2){var n=this.elements[r];e+=n*n}return this._magnitude=Math.sqrt(e)},j.Vector.prototype.dot=function(e){for(var t=0,r=this.elements,n=e.elements,i=r.length,s=n.length,o=0,a=0,u=0,l=0;u<i&&l<s;)(o=r[u])<(a=n[l])?u+=2:o>a?l+=2:o==a&&(t+=r[u+1]*n[l+1],u+=2,l+=2);return t},j.Vector.prototype.similarity=function(e){return this.dot(e)/this.magnitude()||0},j.Vector.prototype.toArray=function(){for(var e=new Array(this.elements.length/2),t=1,r=0;t<this.elements.length;t+=2,r++)e[r]=this.elements[t];return e},j.Vector.prototype.toJSON=function(){return this.elements},j.stemmer=(o={ational:"ate",tional:"tion",enci:"ence",anci:"ance",izer:"ize",bli:"ble",alli:"al",entli:"ent",eli:"e",ousli:"ous",ization:"ize",ation:"ate",ator:"ate",alism:"al",iveness:"ive",fulness:"ful",ousness:"ous",aliti:"al",iviti:"ive",biliti:"ble",logi:"log"},a={icate:"ic",ative:"",alize:"al",iciti:"ic",ical:"ic",ful:"",ness:""},h="^("+(l="[^aeiou][^aeiouy]*")+")?"+(c=(u="[aeiouy]")+"[aeiou]*")+l+"("+c+")?$",d="^("+l+")?"+c+l+c+l,f="^("+l+")?"+u,p=new RegExp("^("+l+")?"+c+l),y=new RegExp(d),m=new RegExp(h),g=new RegExp(f),x=/^(.+?)(ss|i)es$/,v=/^(.+?)([^s])s$/,w=/^(.+?)eed$/,Q=/^(.+?)(ed|ing)$/,k=/.$/,S=/(at|bl|iz)$/,E=new RegExp("([^aeiouylsz])\\\\1$"),L=new RegExp("^"+l+u+"[^aeiouwxy]$"),P=/^(.+?[^aeiou])y$/,b=/^(.+?)(ational|tional|enci|anci|izer|bli|alli|entli|eli|ousli|ization|ation|ator|alism|iveness|fulness|ousness|aliti|iviti|biliti|logi)$/,T=/^(.+?)(icate|ative|alize|iciti|ical|ful|ness)$/,O=/^(.+?)(al|ance|ence|er|ic|able|ible|ant|ement|ment|ent|ou|ism|ate|iti|ous|ive|ize)$/,I=/^(.+?)(s|t)(ion)$/,R=/^(.+?)e$/,F=/ll$/,C=new RegExp("^"+l+u+"[^aeiouwxy]$"),N=function(e){var t,r,n,i,s,u,l;if(e.length<3)return e;if("y"==(n=e.substr(0,1))&&(e=n.toUpperCase()+e.substr(1)),s=v,(i=x).test(e)?e=e.replace(i,"$1$2"):s.test(e)&&(e=e.replace(s,"$1$2")),s=Q,(i=w).test(e)){var c=i.exec(e);(i=p).test(c[1])&&(i=k,e=e.replace(i,""))}else s.test(e)&&(t=(c=s.exec(e))[1],(s=g).test(t)&&(u=E,l=L,(s=S).test(e=t)?e+="e":u.test(e)?(i=k,e=e.replace(i,"")):l.test(e)&&(e+="e")));return(i=P).test(e)&&(e=(t=(c=i.exec(e))[1])+"i"),(i=b).test(e)&&(t=(c=i.exec(e))[1],r=c[2],(i=p).test(t)&&(e=t+o[r])),(i=T).test(e)&&(t=(c=i.exec(e))[1],r=c[2],(i=p).test(t)&&(e=t+a[r])),s=I,(i=O).test(e)?(t=(c=i.exec(e))[1],(i=y).test(t)&&(e=t)):s.test(e)&&(t=(c=s.exec(e))[1]+c[2],(s=y).test(t)&&(e=t)),(i=R).test(e)&&(t=(c=i.exec(e))[1],s=m,u=C,((i=y).test(t)||s.test(t)&&!u.test(t))&&(e=t)),s=y,(i=F).test(e)&&s.test(e)&&(i=k,e=e.replace(i,"")),"y"==n&&(e=n.toLowerCase()+e.substr(1)),e},function(e){return e.update(N)}),j.Pipeline.registerFunction(j.stemmer,"stemmer"),j.generateStopWordFilter=function(e){var t=e.reduce((function(e,t){return e[t]=t,e}),{});return function(e){if(e&&t[e.toString()]!==e.toString())return e}},j.stopWordFilter=j.generateStopWordFilter(["a","able","about","across","after","all","almost","also","am","among","an","and","any","are","as","at","be","because","been","but","by","can","cannot","could","dear","did","do","does","either","else","ever","every","for","from","get","got","had","has","have","he","her","hers","him","his","how","however","i","if","in","into","is","it","its","just","least","let","like","likely","may","me","might","most","must","my","neither","no","nor","not","of","off","often","on","only","or","other","our","own","rather","said","say","says","she","should","since","so","some","than","that","the","their","them","then","there","these","they","this","tis","to","too","twas","us","wants","was","we","were","what","when","where","which","while","who","whom","why","will","with","would","yet","you","your"]),j.Pipeline.registerFunction(j.stopWordFilter,"stopWordFilter"),j.trimmer=function(e){return e.update((function(e){return e.replace(/^\\W+/,"").replace(/\\W+$/,"")}))},j.Pipeline.registerFunction(j.trimmer,"trimmer"),j.TokenSet=function(){this.final=!1,this.edges={},this.id=j.TokenSet._nextId,j.TokenSet._nextId+=1},j.TokenSet._nextId=1,j.TokenSet.fromArray=function(e){for(var t=new j.TokenSet.Builder,r=0,n=e.length;r<n;r++)t.insert(e[r]);return t.finish(),t.root},j.TokenSet.fromClause=function(e){return"editDistance"in e?j.TokenSet.fromFuzzyString(e.term,e.editDistance):j.TokenSet.fromString(e.term)},j.TokenSet.fromFuzzyString=function(e,t){for(var r=new j.TokenSet,n=[{node:r,editsRemaining:t,str:e}];n.length;){var i=n.pop();if(i.str.length>0){var s,o=i.str.charAt(0);o in i.node.edges?s=i.node.edges[o]:(s=new j.TokenSet,i.node.edges[o]=s),1==i.str.length&&(s.final=!0),n.push({node:s,editsRemaining:i.editsRemaining,str:i.str.slice(1)})}if(0!=i.editsRemaining){if("*"in i.node.edges)var a=i.node.edges["*"];else a=new j.TokenSet,i.node.edges["*"]=a;if(0==i.str.length&&(a.final=!0),n.push({node:a,editsRemaining:i.editsRemaining-1,str:i.str}),i.str.length>1&&n.push({node:i.node,editsRemaining:i.editsRemaining-1,str:i.str.slice(1)}),1==i.str.length&&(i.node.final=!0),i.str.length>=1){if("*"in i.node.edges)var u=i.node.edges["*"];else u=new j.TokenSet,i.node.edges["*"]=u;1==i.str.length&&(u.final=!0),n.push({node:u,editsRemaining:i.editsRemaining-1,str:i.str.slice(1)})}if(i.str.length>1){var l,c=i.str.charAt(0),h=i.str.charAt(1);h in i.node.edges?l=i.node.edges[h]:(l=new j.TokenSet,i.node.edges[h]=l),1==i.str.length&&(l.final=!0),n.push({node:l,editsRemaining:i.editsRemaining-1,str:c+i.str.slice(2)})}}}return r},j.TokenSet.fromString=function(e){for(var t=new j.TokenSet,r=t,n=0,i=e.length;n<i;n++){var s=e[n],o=n==i-1;if("*"==s)t.edges[s]=t,t.final=o;else{var a=new j.TokenSet;a.final=o,t.edges[s]=a,t=a}}return r},j.TokenSet.prototype.toArray=function(){for(var e=[],t=[{prefix:"",node:this}];t.length;){var r=t.pop(),n=Object.keys(r.node.edges),i=n.length;r.node.final&&(r.prefix.charAt(0),e.push(r.prefix));for(var s=0;s<i;s++){var o=n[s];t.push({prefix:r.prefix.concat(o),node:r.node.edges[o]})}}return e},j.TokenSet.prototype.toString=function(){if(this._str)return this._str;for(var e=this.final?"1":"0",t=Object.keys(this.edges).sort(),r=t.length,n=0;n<r;n++){var i=t[n];e=e+i+this.edges[i].id}return e},j.TokenSet.prototype.intersect=function(e){for(var t=new j.TokenSet,r=void 0,n=[{qNode:e,output:t,node:this}];n.length;){r=n.pop();for(var i=Object.keys(r.qNode.edges),s=i.length,o=Object.keys(r.node.edges),a=o.length,u=0;u<s;u++)for(var l=i[u],c=0;c<a;c++){var h=o[c];if(h==l||"*"==l){var d=r.node.edges[h],f=r.qNode.edges[l],p=d.final&&f.final,y=void 0;h in r.output.edges?(y=r.output.edges[h]).final=y.final||p:((y=new j.TokenSet).final=p,r.output.edges[h]=y),n.push({qNode:f,output:y,node:d})}}}return t},j.TokenSet.Builder=function(){this.previousWord="",this.root=new j.TokenSet,this.uncheckedNodes=[],this.minimizedNodes={}},j.TokenSet.Builder.prototype.insert=function(e){var t,r=0;if(e<this.previousWord)throw new Error("Out of order word insertion");for(var n=0;n<e.length&&n<this.previousWord.length&&e[n]==this.previousWord[n];n++)r++;for(this.minimize(r),t=0==this.uncheckedNodes.length?this.root:this.uncheckedNodes[this.uncheckedNodes.length-1].child,n=r;n<e.length;n++){var i=new j.TokenSet,s=e[n];t.edges[s]=i,this.uncheckedNodes.push({parent:t,char:s,child:i}),t=i}t.final=!0,this.previousWord=e},j.TokenSet.Builder.prototype.finish=function(){this.minimize(0)},j.TokenSet.Builder.prototype.minimize=function(e){for(var t=this.uncheckedNodes.length-1;t>=e;t--){var r=this.uncheckedNodes[t],n=r.child.toString();n in this.minimizedNodes?r.parent.edges[r.char]=this.minimizedNodes[n]:(r.child._str=n,this.minimizedNodes[n]=r.child),this.uncheckedNodes.pop()}},j.Index=function(e){this.invertedIndex=e.invertedIndex,this.fieldVectors=e.fieldVectors,this.tokenSet=e.tokenSet,this.fields=e.fields,this.pipeline=e.pipeline},j.Index.prototype.search=function(e){return this.query((function(t){new j.QueryParser(e,t).parse()}))},j.Index.prototype.query=function(e){for(var t=new j.Query(this.fields),r=Object.create(null),n=Object.create(null),i=Object.create(null),s=Object.create(null),o=Object.create(null),a=0;a<this.fields.length;a++)n[this.fields[a]]=new j.Vector;for(e.call(t,t),a=0;a<t.clauses.length;a++){var u,l=t.clauses[a],c=j.Set.empty;u=l.usePipeline?this.pipeline.runString(l.term,{fields:l.fields}):[l.term];for(var h=0;h<u.length;h++){var d=u[h];l.term=d;var f=j.TokenSet.fromClause(l),p=this.tokenSet.intersect(f).toArray();if(0===p.length&&l.presence===j.Query.presence.REQUIRED){for(var y=0;y<l.fields.length;y++)s[R=l.fields[y]]=j.Set.empty;break}for(var m=0;m<p.length;m++){var g=p[m],x=this.invertedIndex[g],v=x._index;for(y=0;y<l.fields.length;y++){var w=x[R=l.fields[y]],Q=Object.keys(w),k=g+"/"+R,S=new j.Set(Q);if(l.presence==j.Query.presence.REQUIRED&&(c=c.union(S),void 0===s[R]&&(s[R]=j.Set.complete)),l.presence!=j.Query.presence.PROHIBITED){if(n[R].upsert(v,l.boost,(function(e,t){return e+t})),!i[k]){for(var E=0;E<Q.length;E++){var L,P=Q[E],b=new j.FieldRef(P,R),T=w[P];void 0===(L=r[b])?r[b]=new j.MatchData(g,R,T):L.add(g,R,T)}i[k]=!0}}else void 0===o[R]&&(o[R]=j.Set.empty),o[R]=o[R].union(S)}}}if(l.presence===j.Query.presence.REQUIRED)for(y=0;y<l.fields.length;y++)s[R=l.fields[y]]=s[R].intersect(c)}var O=j.Set.complete,I=j.Set.empty;for(a=0;a<this.fields.length;a++){var R;s[R=this.fields[a]]&&(O=O.intersect(s[R])),o[R]&&(I=I.union(o[R]))}var F=Object.keys(r),C=[],N=Object.create(null);if(t.isNegated())for(F=Object.keys(this.fieldVectors),a=0;a<F.length;a++){b=F[a];var _=j.FieldRef.fromString(b);r[b]=new j.MatchData}for(a=0;a<F.length;a++){var D=(_=j.FieldRef.fromString(F[a])).docRef;if(O.contains(D)&&!I.contains(D)){var A,B=this.fieldVectors[_],z=n[_.fieldName].similarity(B);if(void 0!==(A=N[D]))A.score+=z,A.matchData.combine(r[_]);else{var V={ref:D,score:z,matchData:r[_]};N[D]=V,C.push(V)}}}return C.sort((function(e,t){return t.score-e.score}))},j.Index.prototype.toJSON=function(){var e=Object.keys(this.invertedIndex).sort().map((function(e){return[e,this.invertedIndex[e]]}),this),t=Object.keys(this.fieldVectors).map((function(e){return[e,this.fieldVectors[e].toJSON()]}),this);return{version:j.version,fields:this.fields,fieldVectors:t,invertedIndex:e,pipeline:this.pipeline.toJSON()}},j.Index.load=function(e){var t={},r={},n=e.fieldVectors,i=Object.create(null),s=e.invertedIndex,o=new j.TokenSet.Builder,a=j.Pipeline.load(e.pipeline);e.version!=j.version&&j.utils.warn("Version mismatch when loading serialised index. Current version of lunr '"+j.version+"' does not match serialized index '"+e.version+"'");for(var u=0;u<n.length;u++){var l=(h=n[u])[0],c=h[1];r[l]=new j.Vector(c)}for(u=0;u<s.length;u++){var h,d=(h=s[u])[0],f=h[1];o.insert(d),i[d]=f}return o.finish(),t.fields=e.fields,t.fieldVectors=r,t.invertedIndex=i,t.tokenSet=o.root,t.pipeline=a,new j.Index(t)},j.Builder=function(){this._ref="id",this._fields=Object.create(null),this._documents=Object.create(null),this.invertedIndex=Object.create(null),this.fieldTermFrequencies={},this.fieldLengths={},this.tokenizer=j.tokenizer,this.pipeline=new j.Pipeline,this.searchPipeline=new j.Pipeline,this.documentCount=0,this._b=.75,this._k1=1.2,this.termIndex=0,this.metadataWhitelist=[]},j.Builder.prototype.ref=function(e){this._ref=e},j.Builder.prototype.field=function(e,t){if(/\\//.test(e))throw new RangeError("Field '"+e+"' contains illegal character '/'");this._fields[e]=t||{}},j.Builder.prototype.b=function(e){this._b=e<0?0:e>1?1:e},j.Builder.prototype.k1=function(e){this._k1=e},j.Builder.prototype.add=function(e,t){var r=e[this._ref],n=Object.keys(this._fields);this._documents[r]=t||{},this.documentCount+=1;for(var i=0;i<n.length;i++){var s=n[i],o=this._fields[s].extractor,a=o?o(e):e[s],u=this.tokenizer(a,{fields:[s]}),l=this.pipeline.run(u),c=new j.FieldRef(r,s),h=Object.create(null);this.fieldTermFrequencies[c]=h,this.fieldLengths[c]=0,this.fieldLengths[c]+=l.length;for(var d=0;d<l.length;d++){var f=l[d];if(null==h[f]&&(h[f]=0),h[f]+=1,null==this.invertedIndex[f]){var p=Object.create(null);p._index=this.termIndex,this.termIndex+=1;for(var y=0;y<n.length;y++)p[n[y]]=Object.create(null);this.invertedIndex[f]=p}null==this.invertedIndex[f][s][r]&&(this.invertedIndex[f][s][r]=Object.create(null));for(var m=0;m<this.metadataWhitelist.length;m++){var g=this.metadataWhitelist[m],x=f.metadata[g];null==this.invertedIndex[f][s][r][g]&&(this.invertedIndex[f][s][r][g]=[]),this.invertedIndex[f][s][r][g].push(x)}}}},j.Builder.prototype.calculateAverageFieldLengths=function(){for(var e=Object.keys(this.fieldLengths),t=e.length,r={},n={},i=0;i<t;i++){var s=j.FieldRef.fromString(e[i]),o=s.fieldName;n[o]||(n[o]=0),n[o]+=1,r[o]||(r[o]=0),r[o]+=this.fieldLengths[s]}var a=Object.keys(this._fields);for(i=0;i<a.length;i++){var u=a[i];r[u]=r[u]/n[u]}this.averageFieldLength=r},j.Builder.prototype.createFieldVectors=function(){for(var e={},t=Object.keys(this.fieldTermFrequencies),r=t.length,n=Object.create(null),i=0;i<r;i++){for(var s=j.FieldRef.fromString(t[i]),o=s.fieldName,a=this.fieldLengths[s],u=new j.Vector,l=this.fieldTermFrequencies[s],c=Object.keys(l),h=c.length,d=this._fields[o].boost||1,f=this._documents[s.docRef].boost||1,p=0;p<h;p++){var y,m,g,x=c[p],v=l[x],w=this.invertedIndex[x]._index;void 0===n[x]?(y=j.idf(this.invertedIndex[x],this.documentCount),n[x]=y):y=n[x],m=y*((this._k1+1)*v)/(this._k1*(1-this._b+this._b*(a/this.averageFieldLength[o]))+v),m*=d,m*=f,g=Math.round(1e3*m)/1e3,u.insert(w,g)}e[s]=u}this.fieldVectors=e},j.Builder.prototype.createTokenSet=function(){this.tokenSet=j.TokenSet.fromArray(Object.keys(this.invertedIndex).sort())},j.Builder.prototype.build=function(){return this.calculateAverageFieldLengths(),this.createFieldVectors(),this.createTokenSet(),new j.Index({invertedIndex:this.invertedIndex,fieldVectors:this.fieldVectors,tokenSet:this.tokenSet,fields:Object.keys(this._fields),pipeline:this.searchPipeline})},j.Builder.prototype.use=function(e){var t=Array.prototype.slice.call(arguments,1);t.unshift(this),e.apply(this,t)},j.MatchData=function(e,t,r){for(var n=Object.create(null),i=Object.keys(r||{}),s=0;s<i.length;s++){var o=i[s];n[o]=r[o].slice()}this.metadata=Object.create(null),void 0!==e&&(this.metadata[e]=Object.create(null),this.metadata[e][t]=n)},j.MatchData.prototype.combine=function(e){for(var t=Object.keys(e.metadata),r=0;r<t.length;r++){var n=t[r],i=Object.keys(e.metadata[n]);null==this.metadata[n]&&(this.metadata[n]=Object.create(null));for(var s=0;s<i.length;s++){var o=i[s],a=Object.keys(e.metadata[n][o]);null==this.metadata[n][o]&&(this.metadata[n][o]=Object.create(null));for(var u=0;u<a.length;u++){var l=a[u];null==this.metadata[n][o][l]?this.metadata[n][o][l]=e.metadata[n][o][l]:this.metadata[n][o][l]=this.metadata[n][o][l].concat(e.metadata[n][o][l])}}}},j.MatchData.prototype.add=function(e,t,r){if(!(e in this.metadata))return this.metadata[e]=Object.create(null),void(this.metadata[e][t]=r);if(t in this.metadata[e])for(var n=Object.keys(r),i=0;i<n.length;i++){var s=n[i];s in this.metadata[e][t]?this.metadata[e][t][s]=this.metadata[e][t][s].concat(r[s]):this.metadata[e][t][s]=r[s]}else this.metadata[e][t]=r},j.Query=function(e){this.clauses=[],this.allFields=e},j.Query.wildcard=new String("*"),j.Query.wildcard.NONE=0,j.Query.wildcard.LEADING=1,j.Query.wildcard.TRAILING=2,j.Query.presence={OPTIONAL:1,REQUIRED:2,PROHIBITED:3},j.Query.prototype.clause=function(e){return"fields"in e||(e.fields=this.allFields),"boost"in e||(e.boost=1),"usePipeline"in e||(e.usePipeline=!0),"wildcard"in e||(e.wildcard=j.Query.wildcard.NONE),e.wildcard&j.Query.wildcard.LEADING&&e.term.charAt(0)!=j.Query.wildcard&&(e.term="*"+e.term),e.wildcard&j.Query.wildcard.TRAILING&&e.term.slice(-1)!=j.Query.wildcard&&(e.term=e.term+"*"),"presence"in e||(e.presence=j.Query.presence.OPTIONAL),this.clauses.push(e),this},j.Query.prototype.isNegated=function(){for(var e=0;e<this.clauses.length;e++)if(this.clauses[e].presence!=j.Query.presence.PROHIBITED)return!1;return!0},j.Query.prototype.term=function(e,t){if(Array.isArray(e))return e.forEach((function(e){this.term(e,j.utils.clone(t))}),this),this;var r=t||{};return r.term=e.toString(),this.clause(r),this},j.QueryParseError=function(e,t,r){this.name="QueryParseError",this.message=e,this.start=t,this.end=r},j.QueryParseError.prototype=new Error,j.QueryLexer=function(e){this.lexemes=[],this.str=e,this.length=e.length,this.pos=0,this.start=0,this.escapeCharPositions=[]},j.QueryLexer.prototype.run=function(){for(var e=j.QueryLexer.lexText;e;)e=e(this)},j.QueryLexer.prototype.sliceString=function(){for(var e=[],t=this.start,r=this.pos,n=0;n<this.escapeCharPositions.length;n++)r=this.escapeCharPositions[n],e.push(this.str.slice(t,r)),t=r+1;return e.push(this.str.slice(t,this.pos)),this.escapeCharPositions.length=0,e.join("")},j.QueryLexer.prototype.emit=function(e){this.lexemes.push({type:e,str:this.sliceString(),start:this.start,end:this.pos}),this.start=this.pos},j.QueryLexer.prototype.escapeCharacter=function(){this.escapeCharPositions.push(this.pos-1),this.pos+=1},j.QueryLexer.prototype.next=function(){if(this.pos>=this.length)return j.QueryLexer.EOS;var e=this.str.charAt(this.pos);return this.pos+=1,e},j.QueryLexer.prototype.width=function(){return this.pos-this.start},j.QueryLexer.prototype.ignore=function(){this.start==this.pos&&(this.pos+=1),this.start=this.pos},j.QueryLexer.prototype.backup=function(){this.pos-=1},j.QueryLexer.prototype.acceptDigitRun=function(){var e,t;do{t=(e=this.next()).charCodeAt(0)}while(t>47&&t<58);e!=j.QueryLexer.EOS&&this.backup()},j.QueryLexer.prototype.more=function(){return this.pos<this.length},j.QueryLexer.EOS="EOS",j.QueryLexer.FIELD="FIELD",j.QueryLexer.TERM="TERM",j.QueryLexer.EDIT_DISTANCE="EDIT_DISTANCE",j.QueryLexer.BOOST="BOOST",j.QueryLexer.PRESENCE="PRESENCE",j.QueryLexer.lexField=function(e){return e.backup(),e.emit(j.QueryLexer.FIELD),e.ignore(),j.QueryLexer.lexText},j.QueryLexer.lexTerm=function(e){if(e.width()>1&&(e.backup(),e.emit(j.QueryLexer.TERM)),e.ignore(),e.more())return j.QueryLexer.lexText},j.QueryLexer.lexEditDistance=function(e){return e.ignore(),e.acceptDigitRun(),e.emit(j.QueryLexer.EDIT_DISTANCE),j.QueryLexer.lexText},j.QueryLexer.lexBoost=function(e){return e.ignore(),e.acceptDigitRun(),e.emit(j.QueryLexer.BOOST),j.QueryLexer.lexText},j.QueryLexer.lexEOS=function(e){e.width()>0&&e.emit(j.QueryLexer.TERM)},j.QueryLexer.termSeparator=j.tokenizer.separator,j.QueryLexer.lexText=function(e){for(;;){var t=e.next();if(t==j.QueryLexer.EOS)return j.QueryLexer.lexEOS;if(92!=t.charCodeAt(0)){if(":"==t)return j.QueryLexer.lexField;if("~"==t)return e.backup(),e.width()>0&&e.emit(j.QueryLexer.TERM),j.QueryLexer.lexEditDistance;if("^"==t)return e.backup(),e.width()>0&&e.emit(j.QueryLexer.TERM),j.QueryLexer.lexBoost;if("+"==t&&1===e.width())return e.emit(j.QueryLexer.PRESENCE),j.QueryLexer.lexText;if("-"==t&&1===e.width())return e.emit(j.QueryLexer.PRESENCE),j.QueryLexer.lexText;if(t.match(j.QueryLexer.termSeparator))return j.QueryLexer.lexTerm}else e.escapeCharacter()}},j.QueryParser=function(e,t){this.lexer=new j.QueryLexer(e),this.query=t,this.currentClause={},this.lexemeIdx=0},j.QueryParser.prototype.parse=function(){this.lexer.run(),this.lexemes=this.lexer.lexemes;for(var e=j.QueryParser.parseClause;e;)e=e(this);return this.query},j.QueryParser.prototype.peekLexeme=function(){return this.lexemes[this.lexemeIdx]},j.QueryParser.prototype.consumeLexeme=function(){var e=this.peekLexeme();return this.lexemeIdx+=1,e},j.QueryParser.prototype.nextClause=function(){var e=this.currentClause;this.query.clause(e),this.currentClause={}},j.QueryParser.parseClause=function(e){var t=e.peekLexeme();if(null!=t)switch(t.type){case j.QueryLexer.PRESENCE:return j.QueryParser.parsePresence;case j.QueryLexer.FIELD:return j.QueryParser.parseField;case j.QueryLexer.TERM:return j.QueryParser.parseTerm;default:var r="expected either a field or a term, found "+t.type;throw t.str.length>=1&&(r+=" with value '"+t.str+"'"),new j.QueryParseError(r,t.start,t.end)}},j.QueryParser.parsePresence=function(e){var t=e.consumeLexeme();if(null!=t){switch(t.str){case"-":e.currentClause.presence=j.Query.presence.PROHIBITED;break;case"+":e.currentClause.presence=j.Query.presence.REQUIRED;break;default:var r="unrecognised presence operator'"+t.str+"'";throw new j.QueryParseError(r,t.start,t.end)}var n=e.peekLexeme();if(null==n)throw r="expecting term or field, found nothing",new j.QueryParseError(r,t.start,t.end);switch(n.type){case j.QueryLexer.FIELD:return j.QueryParser.parseField;case j.QueryLexer.TERM:return j.QueryParser.parseTerm;default:throw r="expecting term or field, found '"+n.type+"'",new j.QueryParseError(r,n.start,n.end)}}},j.QueryParser.parseField=function(e){var t=e.consumeLexeme();if(null!=t){if(-1==e.query.allFields.indexOf(t.str)){var r=e.query.allFields.map((function(e){return"'"+e+"'"})).join(", "),n="unrecognised field '"+t.str+"', possible fields: "+r;throw new j.QueryParseError(n,t.start,t.end)}e.currentClause.fields=[t.str];var i=e.peekLexeme();if(null==i)throw n="expecting term, found nothing",new j.QueryParseError(n,t.start,t.end);if(i.type===j.QueryLexer.TERM)return j.QueryParser.parseTerm;throw n="expecting term, found '"+i.type+"'",new j.QueryParseError(n,i.start,i.end)}},j.QueryParser.parseTerm=function(e){var t=e.consumeLexeme();if(null!=t){e.currentClause.term=t.str.toLowerCase(),-1!=t.str.indexOf("*")&&(e.currentClause.usePipeline=!1);var r=e.peekLexeme();if(null!=r)switch(r.type){case j.QueryLexer.TERM:return e.nextClause(),j.QueryParser.parseTerm;case j.QueryLexer.FIELD:return e.nextClause(),j.QueryParser.parseField;case j.QueryLexer.EDIT_DISTANCE:return j.QueryParser.parseEditDistance;case j.QueryLexer.BOOST:return j.QueryParser.parseBoost;case j.QueryLexer.PRESENCE:return e.nextClause(),j.QueryParser.parsePresence;default:var n="Unexpected lexeme type '"+r.type+"'";throw new j.QueryParseError(n,r.start,r.end)}else e.nextClause()}},j.QueryParser.parseEditDistance=function(e){var t=e.consumeLexeme();if(null!=t){var r=parseInt(t.str,10);if(isNaN(r)){var n="edit distance must be numeric";throw new j.QueryParseError(n,t.start,t.end)}e.currentClause.editDistance=r;var i=e.peekLexeme();if(null!=i)switch(i.type){case j.QueryLexer.TERM:return e.nextClause(),j.QueryParser.parseTerm;case j.QueryLexer.FIELD:return e.nextClause(),j.QueryParser.parseField;case j.QueryLexer.EDIT_DISTANCE:return j.QueryParser.parseEditDistance;case j.QueryLexer.BOOST:return j.QueryParser.parseBoost;case j.QueryLexer.PRESENCE:return e.nextClause(),j.QueryParser.parsePresence;default:throw n="Unexpected lexeme type '"+i.type+"'",new j.QueryParseError(n,i.start,i.end)}else e.nextClause()}},j.QueryParser.parseBoost=function(e){var t=e.consumeLexeme();if(null!=t){var r=parseInt(t.str,10);if(isNaN(r)){var n="boost must be numeric";throw new j.QueryParseError(n,t.start,t.end)}e.currentClause.boost=r;var i=e.peekLexeme();if(null!=i)switch(i.type){case j.QueryLexer.TERM:return e.nextClause(),j.QueryParser.parseTerm;case j.QueryLexer.FIELD:return e.nextClause(),j.QueryParser.parseField;case j.QueryLexer.EDIT_DISTANCE:return j.QueryParser.parseEditDistance;case j.QueryLexer.BOOST:return j.QueryParser.parseBoost;case j.QueryLexer.PRESENCE:return e.nextClause(),j.QueryParser.parsePresence;default:throw n="Unexpected lexeme type '"+i.type+"'",new j.QueryParseError(n,i.start,i.end)}else e.nextClause()}},void 0===(i="function"==typeof(n=function(){return j})?n.call(t,r,t,e):n)||(e.exports=i)}()}},t={};function r(n){var i=t[n];if(void 0!==i)return i.exports;var s=t[n]={exports:{}};return e[n](s,s.exports,r),s.exports}r.n=function(e){var t=e&&e.__esModule?function(){return e.default}:function(){return e};return r.d(t,{a:t}),t},r.d=function(e,t){for(var n in t)r.o(t,n)&&!r.o(e,n)&&Object.defineProperty(e,n,{enumerable:!0,get:t[n]})},r.o=function(e,t){return Object.prototype.hasOwnProperty.call(e,t)};var n={};!function(){"use strict";r.d(n,{add:function(){return c},dispose:function(){return y},done:function(){return h},fromExternalJS:function(){return f},load:function(){return p},search:function(){return m},toJS:function(){return d}});var e=r(291),t=(e,t,r)=>new Promise(((n,i)=>{var s=e=>{try{a(r.next(e))}catch(e){i(e)}},o=e=>{try{a(r.throw(e))}catch(e){i(e)}},a=e=>e.done?n(e.value):Promise.resolve(e.value).then(s,o);a((r=r.apply(e,t)).next())}));let i,s,o,a=[];function u(){i=new e.Builder,i.field("title"),i.field("description"),i.ref("ref"),i.pipeline.add(e.trimmer,e.stopWordFilter,e.stemmer),o=new Promise((e=>{s=e}))}e.tokenizer.separator=/\\s+/,u();const l=t=>{const r=e.trimmer(new e.Token(t,{}));return"*"+e.stemmer(r)+"*"};function c(e,t,r){const n=a.push(r)-1,s={title:e.toLowerCase(),description:t.toLowerCase(),ref:n};i.add(s)}function h(){return t(this,null,(function*(){s(i.build())}))}function d(){return t(this,null,(function*(){return{store:a,index:(yield o).toJSON()}}))}function f(e,r){return t(this,null,(function*(){try{if(importScripts(e),!self[r])throw new Error("Broken index file format");p(self[r])}catch(e){console.error("Failed to load search index: "+e.message)}}))}function p(r){return t(this,null,(function*(){a=r.store,s(e.Index.load(r.index))}))}function y(){return t(this,null,(function*(){a=[],u()}))}function m(e,r=0){return t(this,null,(function*(){if(0===e.trim().length)return[];let t=(yield o).query((t=>{e.trim().toLowerCase().split(/\\s+/).forEach((e=>{if(1===e.length)return;const r=l(e);t.term(r,{})}))}));return r>0&&(t=t.slice(0,r)),t.map((e=>({meta:a[e.ref],score:e.score})))}))}addEventListener("message",(function(e){var t,r=e.data,i=r.type,s=r.method,o=r.id,a=r.params;"RPC"===i&&s&&((t=n[s])?Promise.resolve().then((function(){return t.apply(n,a)})):Promise.reject("No such method")).then((function(e){postMessage({type:"RPC",id:o,result:e})})).catch((function(e){var t={message:e};e.stack&&(t.message=e.message,t.stack=e.stack,t.name=e.name),postMessage({type:"RPC",id:o,error:t})}))})),postMessage({type:"RPC",method:"ready"})}()}();
//# sourceMappingURL=cfb294d7f6536ffa8d42.worker.js.map`])), { name: "[fullhash].worker.js" });
            return n2(e3, o2), e3;
          };
        }, 7: function(e2) {
          e2.exports = function(e3, t2) {
            var r3 = 0, n2 = {};
            e3.addEventListener("message", (function(t3) {
              var r4 = t3.data;
              if ("RPC" === r4.type) if (r4.id) {
                var o2 = n2[r4.id];
                o2 && (delete n2[r4.id], r4.error ? o2[1](Object.assign(Error(r4.error.message), r4.error)) : o2[0](r4.result));
              } else {
                var i2 = document.createEvent("Event");
                i2.initEvent(r4.method, false, false), i2.data = r4.params, e3.dispatchEvent(i2);
              }
            })), t2.forEach((function(t3) {
              e3[t3] = function() {
                var o2 = arguments;
                return new Promise((function(i2, s2) {
                  var a2 = ++r3;
                  n2[a2] = [i2, s2], e3.postMessage({ type: "RPC", id: a2, method: t3, params: [].slice.call(o2) });
                }));
              };
            }));
          };
        }, 884: function(e2) {
          "use strict";
          e2.exports = require_maybe();
        }, 648: function(e2) {
          "use strict";
          e2.exports = require_fast_safe_stringify();
        }, 230: function(e2) {
          "use strict";
          e2.exports = require_stickyfill();
        }, 115: function(e2) {
          "use strict";
          e2.exports = (init_browser(), __toCommonJS(browser_exports));
        }, 725: function(e2) {
          "use strict";
          e2.exports = void 0;
        }, 375: function() {
        }, 430: function(e2) {
          "use strict";
          e2.exports = { rE: "7.0.8" };
        } }, t = {};
        function r2(n2) {
          var o2 = t[n2];
          if (void 0 !== o2) return o2.exports;
          var i2 = t[n2] = { id: n2, exports: {} };
          return e[n2](i2, i2.exports, r2), i2.exports;
        }
        r2.n = function(e2) {
          var t2 = e2 && e2.__esModule ? function() {
            return e2.default;
          } : function() {
            return e2;
          };
          return r2.d(t2, { a: t2 }), t2;
        }, r2.d = function(e2, t2) {
          for (var n2 in t2) r2.o(t2, n2) && !r2.o(e2, n2) && Object.defineProperty(e2, n2, { enumerable: true, get: t2[n2] });
        }, r2.g = (function() {
          if ("object" == typeof globalThis) return globalThis;
          try {
            return this || new Function("return this")();
          } catch (e2) {
            if ("object" == typeof window) return window;
          }
        })(), r2.o = function(e2, t2) {
          return Object.prototype.hasOwnProperty.call(e2, t2);
        }, r2.r = function(e2) {
          "undefined" != typeof Symbol && Symbol.toStringTag && Object.defineProperty(e2, Symbol.toStringTag, { value: "Module" }), Object.defineProperty(e2, "__esModule", { value: true });
        }, r2.nc = void 0;
        var n = {};
        return (function() {
          "use strict";
          r2.r(n), r2.d(n, { AUTH_TYPES: function() {
            return pc;
          }, ApiContentWrap: function() {
            return od;
          }, ApiInfo: function() {
            return wc;
          }, ApiInfoModel: function() {
            return Vr;
          }, ApiLogo: function() {
            return jc;
          }, AppStore: function() {
            return mc;
          }, ArraySchema: function() {
            return yl;
          }, BackgroundStub: function() {
            return id;
          }, BodyContent: function() {
            return mp;
          }, COMPONENT_REGEXP: function() {
            return Br;
          }, CallbackModel: function() {
            return dn;
          }, ClipboardService: function() {
            return Us;
          }, ContentItem: function() {
            return Cu;
          }, ContentItems: function() {
            return Su;
          }, DiscriminatorDropdown: function() {
            return il;
          }, Dropdown: function() {
            return ds;
          }, DropdownLabel: function() {
            return ca;
          }, DropdownOrLabel: function() {
            return ws;
          }, DropdownWrapper: function() {
            return pa;
          }, ErrorBoundary: function() {
            return ne2;
          }, Example: function() {
            return sa;
          }, ExampleModel: function() {
            return Bn;
          }, ExternalExample: function() {
            return aa;
          }, FieldModel: function() {
            return Hn;
          }, GROUP_DEPTH: function() {
            return Yo;
          }, GroupModel: function() {
            return Mo;
          }, HistoryService: function() {
            return Ut;
          }, IS_BROWSER: function() {
            return s2;
          }, InvertedSimpleDropdown: function() {
            return ua;
          }, JsonPointer: function() {
            return Ee2;
          }, JsonViewer: function() {
            return ta;
          }, LEGACY_REGEXP: function() {
            return qr;
          }, Loading: function() {
            return ae2;
          }, MDX_COMPONENT_REGEXP: function() {
            return Fr;
          }, Markdown: function() {
            return zs;
          }, MarkdownRenderer: function() {
            return Ur;
          }, MarkerService: function() {
            return Ht;
          }, MediaContentModel: function() {
            return Kn;
          }, MediaTypeModel: function() {
            return Yn;
          }, MediaTypesSwitch: function() {
            return np;
          }, MenuBuilder: function() {
            return Go;
          }, MenuItem: function() {
            return Mu;
          }, MenuItemLabel: function() {
            return Mc;
          }, MenuItemLi: function() {
            return Lc;
          }, MenuItemTitle: function() {
            return qc;
          }, MenuItemUl: function() {
            return zc;
          }, MenuItems: function() {
            return Vu;
          }, MenuStore: function() {
            return ti;
          }, MiddlePanel: function() {
            return ai;
          }, MimeLabel: function() {
            return la;
          }, NoSampleLabel: function() {
            return da;
          }, OLD_SECURITY_DEFINITIONS_JSX_NAME: function() {
            return ht2;
          }, ObjectSchema: function() {
            return sl;
          }, OneOfButton: function() {
            return Sl;
          }, OneOfSchema: function() {
            return Cl;
          }, OpenAPIParser: function() {
            return jn;
          }, Operation: function() {
            return xu;
          }, OperationBadge: function() {
            return Rc;
          }, OperationItem: function() {
            return Au;
          }, OperationMenuItemContent: function() {
            return qu;
          }, OperationModel: function() {
            return yo;
          }, OptionsConsumer: function() {
            return ue2;
          }, OptionsContext: function() {
            return ce2;
          }, OptionsProvider: function() {
            return pe2;
          }, Parameters: function() {
            return dp;
          }, PayloadSamples: function() {
            return Zp;
          }, Redoc: function() {
            return md;
          }, RedocAttribution: function() {
            return Fc;
          }, RedocNormalizedOptions: function() {
            return W2;
          }, RedocStandalone: function() {
            return wd;
          }, RedocWrap: function() {
            return nd;
          }, RequestBodyModel: function() {
            return eo;
          }, ResponseDetails: function() {
            return Pp;
          }, ResponseHeaders: function() {
            return Ep;
          }, ResponseModel: function() {
            return po;
          }, ResponseSamples: function() {
            return pu;
          }, ResponseTitle: function() {
            return bp;
          }, ResponseView: function() {
            return Ip;
          }, ResponsesList: function() {
            return Tp;
          }, RightPanel: function() {
            return ci;
          }, Row: function() {
            return ui;
          }, SCHEMA_DEFINITION_JSX_NAME: function() {
            return mt2;
          }, SECTION_ATTR: function() {
            return ei;
          }, SECURITY_DEFINITIONS_JSX_NAME: function() {
            return dt2;
          }, SECURITY_SCHEMES_SECTION_PREFIX: function() {
            return ft2;
          }, Schema: function() {
            return Ml;
          }, SchemaDefinition: function() {
            return Hl;
          }, SchemaModel: function() {
            return Nn;
          }, ScrollService: function() {
            return ii;
          }, SearchBox: function() {
            return hd;
          }, SearchStore: function() {
            return si;
          }, Section: function() {
            return li;
          }, SectionItem: function() {
            return Iu;
          }, SecurityDefs: function() {
            return uc;
          }, SecuritySchemeModel: function() {
            return So;
          }, SecuritySchemesModel: function() {
            return Co;
          }, SideMenu: function() {
            return Ju;
          }, SideNavStyleEnum: function() {
            return R2;
          }, SimpleDropdown: function() {
            return hs;
          }, SourceCode: function() {
            return ra;
          }, SourceCodeWithCopy: function() {
            return na;
          }, SpecStore: function() {
            return Do;
          }, StickyResponsiveSidebar: function() {
            return rd;
          }, StoreBuilder: function() {
            return Ei;
          }, StoreConsumer: function() {
            return ki;
          }, StoreContext: function() {
            return vi;
          }, StoreProvider: function() {
            return wi;
          }, StyledMarkdownBlock: function() {
            return js;
          }, ThemeProvider: function() {
            return X2;
          }, Throttle: function() {
            return $t;
          }, alphabeticallyByProp: function() {
            return qt;
          }, appendToMdHeading: function() {
            return y2;
          }, argValueToBoolean: function() {
            return Q2;
          }, buildComponentComment: function() {
            return Qr;
          }, concatRefStacks: function() {
            return On;
          }, convertSwagger2OpenAPI: function() {
            return be2;
          }, createGlobalStyle: function() {
            return G2;
          }, createStore: function() {
            return hc;
          }, css: function() {
            return Y2;
          }, debugTime: function() {
            return St2;
          }, debugTimeEnd: function() {
            return Ct;
          }, detectType: function() {
            return Be2;
          }, escapeHTMLAttrChars: function() {
            return C2;
          }, expandDefaultServerVariables: function() {
            return pt2;
          }, extensionsHook: function() {
            return te2;
          }, extractExtensions: function() {
            return xt;
          }, flattenByProp: function() {
            return m2;
          }, getBasePath: function() {
            return O2;
          }, getContentWithLegacyExamples: function() {
            return wt;
          }, getDefinitionName: function() {
            return rt2;
          }, getOperationSummary: function() {
            return qe2;
          }, getSerializedValue: function() {
            return Xe2;
          }, getStatusCodeType: function() {
            return Le2;
          }, highlight: function() {
            return jt;
          }, history: function() {
            return Wt;
          }, html2Str: function() {
            return l2;
          }, humanizeConstraints: function() {
            return it2;
          }, humanizeNumberRange: function() {
            return ot2;
          }, isAbsoluteUrl: function() {
            return k2;
          }, isArray: function() {
            return I2;
          }, isBoolean: function() {
            return A2;
          }, isFormUrlEncoded: function() {
            return We2;
          }, isJsonLike: function() {
            return Ue2;
          }, isNamedDefinition: function() {
            return tt2;
          }, isNumeric: function() {
            return g2;
          }, isObject: function() {
            return x2;
          }, isOperationName: function() {
            return Me2;
          }, isPayloadSample: function() {
            return fo;
          }, isPrimitiveType: function() {
            return Qe2;
          }, isRedocExtension: function() {
            return bt;
          }, isStatusCode: function() {
            return ze2;
          }, keyframes: function() {
            return Z2;
          }, langFromMime: function() {
            return Ke2;
          }, loadAndBundleSpec: function() {
            return ye2;
          }, mapLang: function() {
            return Ot;
          }, mapValues: function() {
            return h;
          }, mapWithLast: function() {
            return d;
          }, media: function() {
            return K2;
          }, memoize: function() {
            return Mt;
          }, menuItemDepth: function() {
            return Nc;
          }, mergeObjects: function() {
            return b2;
          }, mergeParams: function() {
            return lt2;
          }, mergeSimilarMediaTypes: function() {
            return ct2;
          }, normalizeServers: function() {
            return ut2;
          }, pluralizeType: function() {
            return vt2;
          }, pushRef: function() {
            return En;
          }, querySelector: function() {
            return a2;
          }, removeQueryStringAndHash: function() {
            return $2;
          }, resolveUrl: function() {
            return E2;
          }, safeSlugify: function() {
            return w2;
          }, scrollIntoViewIfNeeded: function() {
            return c2;
          }, serializeParameterValue: function() {
            return Ze2;
          }, serializeParameterValueWithMime: function() {
            return Ge2;
          }, setSecuritySchemePrefix: function() {
            return gt2;
          }, shortenHTTPVerb: function() {
            return yt2;
          }, sortByField: function() {
            return at2;
          }, sortByRequired: function() {
            return st2;
          }, stripTrailingSlash: function() {
            return f2;
          }, styled: function() {
            return ee2;
          }, titleize: function() {
            return j2;
          }, unescapeHTMLChars: function() {
            return P2;
          }, urlFormEncodePayload: function() {
            return Ye2;
          }, useStore: function() {
            return Oi;
          } });
          var e2 = require_react(), t2 = (init_polished_esm(), __toCommonJS(polished_esm_exports));
          const o2 = { spacing: { unit: 5, sectionHorizontal: ({ spacing: e3 }) => 8 * e3.unit, sectionVertical: ({ spacing: e3 }) => 8 * e3.unit }, breakpoints: { small: "50rem", medium: "75rem", large: "105rem" }, colors: { tonalOffset: 0.2, primary: { main: "#32329f", light: ({ colors: e3 }) => (0, t2.lighten)(e3.tonalOffset, e3.primary.main), dark: ({ colors: e3 }) => (0, t2.darken)(e3.tonalOffset, e3.primary.main), contrastText: ({ colors: e3 }) => (0, t2.readableColor)(e3.primary.main) }, success: { main: "#1d8127", light: ({ colors: e3 }) => (0, t2.lighten)(2 * e3.tonalOffset, e3.success.main), dark: ({ colors: e3 }) => (0, t2.darken)(e3.tonalOffset, e3.success.main), contrastText: ({ colors: e3 }) => (0, t2.readableColor)(e3.success.main) }, warning: { main: "#ffa500", light: ({ colors: e3 }) => (0, t2.lighten)(e3.tonalOffset, e3.warning.main), dark: ({ colors: e3 }) => (0, t2.darken)(e3.tonalOffset, e3.warning.main), contrastText: "#ffffff" }, error: { main: "#d41f1c", light: ({ colors: e3 }) => (0, t2.lighten)(e3.tonalOffset, e3.error.main), dark: ({ colors: e3 }) => (0, t2.darken)(e3.tonalOffset, e3.error.main), contrastText: ({ colors: e3 }) => (0, t2.readableColor)(e3.error.main) }, gray: { 50: "#FAFAFA", 100: "#F5F5F5" }, text: { primary: "#333333", secondary: ({ colors: e3 }) => (0, t2.lighten)(e3.tonalOffset, e3.text.primary) }, border: { dark: "rgba(0,0,0, 0.1)", light: "#ffffff" }, responses: { success: { color: ({ colors: e3 }) => e3.success.main, backgroundColor: ({ colors: e3 }) => (0, t2.transparentize)(0.93, e3.success.main), tabTextColor: ({ colors: e3 }) => e3.responses.success.color }, error: { color: ({ colors: e3 }) => e3.error.main, backgroundColor: ({ colors: e3 }) => (0, t2.transparentize)(0.93, e3.error.main), tabTextColor: ({ colors: e3 }) => e3.responses.error.color }, redirect: { color: ({ colors: e3 }) => e3.warning.main, backgroundColor: ({ colors: e3 }) => (0, t2.transparentize)(0.9, e3.responses.redirect.color), tabTextColor: ({ colors: e3 }) => e3.responses.redirect.color }, info: { color: "#87ceeb", backgroundColor: ({ colors: e3 }) => (0, t2.transparentize)(0.9, e3.responses.info.color), tabTextColor: ({ colors: e3 }) => e3.responses.info.color } }, http: { get: "#2F8132", post: "#186FAF", put: "#95507c", options: "#947014", patch: "#bf581d", delete: "#cc3333", basic: "#707070", link: "#07818F", head: "#A23DAD" } }, schema: { linesColor: (e3) => (0, t2.lighten)(e3.colors.tonalOffset, (0, t2.desaturate)(e3.colors.tonalOffset, e3.colors.primary.main)), defaultDetailsWidth: "75%", typeNameColor: (e3) => e3.colors.text.secondary, typeTitleColor: (e3) => e3.schema.typeNameColor, requireLabelColor: (e3) => e3.colors.error.main, labelsTextSize: "0.9em", nestingSpacing: "1em", nestedBackground: "#fafafa", arrow: { size: "1.1em", color: (e3) => e3.colors.text.secondary } }, typography: { fontSize: "14px", lineHeight: "1.5em", fontWeightRegular: "400", fontWeightBold: "600", fontWeightLight: "300", fontFamily: "Roboto, sans-serif", smoothing: "antialiased", optimizeSpeed: true, headings: { fontFamily: "Montserrat, sans-serif", fontWeight: "400", lineHeight: "1.6em" }, code: { fontSize: "13px", fontFamily: "Courier, monospace", lineHeight: ({ typography: e3 }) => e3.lineHeight, fontWeight: ({ typography: e3 }) => e3.fontWeightRegular, color: "#e53935", backgroundColor: "rgba(38, 50, 56, 0.05)", wrap: false }, links: { color: ({ colors: e3 }) => e3.primary.main, visited: ({ typography: e3 }) => e3.links.color, hover: ({ typography: e3 }) => (0, t2.lighten)(0.2, e3.links.color), textDecoration: "auto", hoverTextDecoration: "auto" } }, sidebar: { width: "260px", backgroundColor: "#fafafa", textColor: "#333333", activeTextColor: (e3) => e3.sidebar.textColor !== o2.sidebar.textColor ? e3.sidebar.textColor : e3.colors.primary.main, groupItems: { activeBackgroundColor: (e3) => (0, t2.darken)(0.1, e3.sidebar.backgroundColor), activeTextColor: (e3) => e3.sidebar.activeTextColor, textTransform: "uppercase" }, level1Items: { activeBackgroundColor: (e3) => (0, t2.darken)(0.05, e3.sidebar.backgroundColor), activeTextColor: (e3) => e3.sidebar.activeTextColor, textTransform: "none" }, arrow: { size: "1.5em", color: (e3) => e3.sidebar.textColor } }, logo: { maxHeight: ({ sidebar: e3 }) => e3.width, maxWidth: ({ sidebar: e3 }) => e3.width, gutter: "2px" }, rightPanel: { backgroundColor: "#263238", width: "40%", textColor: "#ffffff", servers: { overlay: { backgroundColor: "#fafafa", textColor: "#263238" }, url: { backgroundColor: "#fff" } } }, codeBlock: { backgroundColor: ({ rightPanel: e3 }) => (0, t2.darken)(0.1, e3.backgroundColor) }, fab: { backgroundColor: "#f2f2f2", color: "#0065FB" } };
          var i2 = o2;
          const s2 = "undefined" != typeof window && "HTMLElement" in window;
          function a2(e3) {
            return "undefined" != typeof document ? document.querySelector(e3) : null;
          }
          function l2(e3) {
            return e3.split(/<[^>]+>/).map(((e4) => e4.trim())).filter(((e4) => e4.length > 0)).join(" ");
          }
          function c2(e3, t3 = true) {
            const r3 = e3.parentNode;
            if (!r3) return;
            const n2 = window.getComputedStyle(r3, void 0), o3 = parseInt(n2.getPropertyValue("border-top-width"), 10), i3 = parseInt(n2.getPropertyValue("border-left-width"), 10), s3 = e3.offsetTop - r3.offsetTop < r3.scrollTop, a3 = e3.offsetTop - r3.offsetTop + e3.clientHeight - o3 > r3.scrollTop + r3.clientHeight, l3 = e3.offsetLeft - r3.offsetLeft < r3.scrollLeft, c3 = e3.offsetLeft - r3.offsetLeft + e3.clientWidth - i3 > r3.scrollLeft + r3.clientWidth, p3 = s3 && !a3;
            (s3 || a3) && t3 && (r3.scrollTop = e3.offsetTop - r3.offsetTop - r3.clientHeight / 2 - o3 + e3.clientHeight / 2), (l3 || c3) && t3 && (r3.scrollLeft = e3.offsetLeft - r3.offsetLeft - r3.clientWidth / 2 - i3 + e3.clientWidth / 2), (s3 || a3 || l3 || c3) && !t3 && e3.scrollIntoView(p3);
          }
          var p2 = require_slugify(), u2 = r2.n(p2);
          function d(e3, t3) {
            const r3 = [];
            for (let n2 = 0; n2 < e3.length - 1; n2++) r3.push(t3(e3[n2], false));
            return 0 !== e3.length && r3.push(t3(e3[e3.length - 1], true)), r3;
          }
          function h(e3, t3) {
            const r3 = {};
            for (const n2 in e3) e3.hasOwnProperty(n2) && (r3[n2] = t3(e3[n2], n2, e3));
            return r3;
          }
          function m2(e3, t3) {
            const r3 = [], n2 = (e4) => {
              for (const o3 of e4) r3.push(o3), o3[t3] && n2(o3[t3]);
            };
            return n2(e3), r3;
          }
          function f2(e3) {
            return e3.endsWith("/") ? e3.substring(0, e3.length - 1) : e3;
          }
          function g2(e3) {
            return !isNaN(parseFloat(e3)) && isFinite(e3);
          }
          function y2(e3, t3, r3) {
            const n2 = new RegExp(`(^|\\n)#\\s?${t3}\\s*\\n`, "i"), o3 = new RegExp(`((\\n|^)#\\s*${t3}\\s*(\\n|$)(?:.|\\n)*?)(\\n#|$)`, "i");
            if (n2.test(e3)) return e3.replace(o3, `$1

${r3}
$4`);
            {
              const n3 = "" === e3 || e3.endsWith("\n\n") ? "" : e3.endsWith("\n") ? "\n" : "\n\n";
              return `${e3}${n3}# ${t3}

${r3}`;
            }
          }
          const b2 = (e3, ...t3) => {
            if (!t3.length) return e3;
            const r3 = t3.shift();
            return void 0 === r3 ? e3 : (v2(e3) && v2(r3) && Object.keys(r3).forEach(((t4) => {
              Object.prototype.hasOwnProperty.call(r3, t4) && "__proto__" !== t4 && (v2(r3[t4]) ? (e3[t4] || (e3[t4] = {}), b2(e3[t4], r3[t4])) : e3[t4] = r3[t4]);
            })), b2(e3, ...t3));
          }, x2 = (e3) => null !== e3 && "object" == typeof e3, v2 = (e3) => x2(e3) && !I2(e3);
          function w2(e3) {
            return u2()(e3) || e3.toString().toLowerCase().replace(/\s+/g, "-").replace(/&/g, "-and-").replace(/\--+/g, "-").replace(/^-+/, "").replace(/-+$/, "");
          }
          function k2(e3) {
            return /(?:^[a-z][a-z0-9+.-]*:|\/\/)/i.test(e3);
          }
          function E2(e3, t3) {
            let r3;
            if (t3.startsWith("//")) try {
              r3 = `${new URL(e3).protocol || "https:"}${t3}`;
            } catch (e4) {
              r3 = `https:${t3}`;
            }
            else if (k2(t3)) r3 = t3;
            else if (t3.startsWith("/")) try {
              const n2 = new URL(e3);
              n2.pathname = t3, r3 = n2.href;
            } catch (e4) {
              r3 = t3;
            }
            else r3 = f2(e3) + "/" + t3;
            return f2(r3);
          }
          function O2(e3) {
            try {
              return S2(e3).pathname;
            } catch (t3) {
              return e3;
            }
          }
          function j2(e3) {
            return e3.charAt(0).toUpperCase() + e3.slice(1);
          }
          function $2(e3) {
            try {
              const t3 = S2(e3);
              return t3.search = "", t3.hash = "", t3.toString();
            } catch (t3) {
              return e3;
            }
          }
          function S2(e3) {
            return "undefined" == typeof URL ? new (r2(725)).URL(e3) : new URL(e3);
          }
          function C2(e3) {
            return e3.replace(/["\\]/g, "\\$&");
          }
          function P2(e3) {
            return e3.replace(/&#(\d+);/g, ((e4, t3) => String.fromCharCode(parseInt(t3, 10)))).replace(/&amp;/g, "&").replace(/&quot;/g, '"');
          }
          function I2(e3) {
            return Array.isArray(e3);
          }
          function A2(e3) {
            return "boolean" == typeof e3;
          }
          const T2 = { enum: "Enum", enumSingleValue: "Value", enumArray: "Items", default: "Default", deprecated: "Deprecated", example: "Example", examples: "Examples", recursive: "Recursive", arrayOf: "Array of ", webhook: "Event", const: "Value", noResultsFound: "No results found", download: "Download", downloadSpecification: "Download OpenAPI specification", responses: "Responses", callbackResponses: "Callback responses", requestSamples: "Request samples", responseSamples: "Response samples" };
          function _15(e3, t3) {
            const r3 = T2[e3];
            return void 0 !== t3 ? r3[t3] : r3;
          }
          var R2 = ((e3) => (e3.SummaryOnly = "summary-only", e3.PathOnly = "path-only", e3.IdOnly = "id-only", e3))(R2 || {}), D2 = Object.defineProperty, z2 = Object.defineProperties, L2 = Object.getOwnPropertyDescriptors, N2 = Object.getOwnPropertySymbols, M2 = Object.prototype.hasOwnProperty, q2 = Object.prototype.propertyIsEnumerable, F2 = (e3, t3, r3) => t3 in e3 ? D2(e3, t3, { enumerable: true, configurable: true, writable: true, value: r3 }) : e3[t3] = r3, B2 = (e3, t3) => {
            for (var r3 in t3 || (t3 = {})) M2.call(t3, r3) && F2(e3, r3, t3[r3]);
            if (N2) for (var r3 of N2(t3)) q2.call(t3, r3) && F2(e3, r3, t3[r3]);
            return e3;
          };
          function Q2(e3, t3) {
            return void 0 === e3 ? t3 || false : "string" == typeof e3 ? "false" !== e3 : e3;
          }
          function U2(e3) {
            return "string" == typeof e3 ? parseInt(e3, 10) : "number" == typeof e3 ? e3 : void 0;
          }
          class W2 {
            static normalizeExpandResponses(e3) {
              if ("all" === e3) return "all";
              if ("string" == typeof e3) {
                const t3 = {};
                return e3.split(",").forEach(((e4) => {
                  t3[e4.trim()] = true;
                })), t3;
              }
              return void 0 !== e3 && console.warn(`expandResponses must be a string but received value "${e3}" of type ${typeof e3}`), {};
            }
            static normalizeHideHostname(e3) {
              return !!e3;
            }
            static normalizeScrollYOffset(e3) {
              if ("string" == typeof e3 && !g2(e3)) {
                const t3 = a2(e3);
                t3 || console.warn("scrollYOffset value is a selector to non-existing element. Using offset 0 by default");
                const r3 = t3 && t3.getBoundingClientRect().bottom || 0;
                return () => r3;
              }
              return "number" == typeof e3 || g2(e3) ? () => "number" == typeof e3 ? e3 : parseFloat(e3) : "function" == typeof e3 ? () => {
                const t3 = e3();
                return "number" != typeof t3 && console.warn(`scrollYOffset should return number but returned value "${t3}" of type ${typeof t3}`), t3;
              } : (void 0 !== e3 && console.warn("Wrong value for scrollYOffset ReDoc option: should be string, number or function"), () => 0);
            }
            static normalizeShowExtensions(e3) {
              if (void 0 === e3) return false;
              if ("" === e3) return true;
              if ("string" != typeof e3) return e3;
              switch (e3) {
                case "true":
                  return true;
                case "false":
                  return false;
                default:
                  return e3.split(",").map(((e4) => e4.trim()));
              }
            }
            static normalizeSideNavStyle(e3) {
              const t3 = R2.SummaryOnly;
              if ("string" != typeof e3) return t3;
              switch (e3) {
                case t3:
                  return e3;
                case R2.PathOnly:
                  return R2.PathOnly;
                case R2.IdOnly:
                  return R2.IdOnly;
                default:
                  return t3;
              }
            }
            static normalizePayloadSampleIdx(e3) {
              return "number" == typeof e3 ? Math.max(0, e3) : "string" == typeof e3 && isFinite(e3) ? parseInt(e3, 10) : 0;
            }
            static normalizeJsonSampleExpandLevel(e3) {
              return "all" === e3 ? 1 / 0 : isNaN(Number(e3)) ? 2 : Math.ceil(Number(e3));
            }
            static normalizeGeneratedPayloadSamplesMaxDepth(e3) {
              return isNaN(Number(e3)) ? 10 : Math.max(0, Number(e3));
            }
            constructor(e3, t3 = {}) {
              var r3, n2, o3, s3, a3;
              const l3 = (e3 = B2(B2({}, t3), e3)).theme && e3.theme.extensionsHook;
              var c3, p3;
              (null == (r3 = e3.theme) ? void 0 : r3.menu) && !(null == (n2 = e3.theme) ? void 0 : n2.sidebar) && (console.warn('Theme setting "menu" is deprecated. Rename to "sidebar"'), e3.theme.sidebar = e3.theme.menu), (null == (o3 = e3.theme) ? void 0 : o3.codeSample) && !(null == (s3 = e3.theme) ? void 0 : s3.codeBlock) && (console.warn('Theme setting "codeSample" is deprecated. Rename to "codeBlock"'), e3.theme.codeBlock = e3.theme.codeSample), this.theme = (function(e4) {
                const t4 = {};
                let r4 = 0;
                const n3 = (o4, i3) => {
                  Object.keys(o4).forEach(((s4) => {
                    const a4 = (i3 ? i3 + "." : "") + s4, l4 = o4[s4];
                    "function" == typeof l4 ? Object.defineProperty(o4, s4, { get() {
                      if (!t4[a4]) {
                        if (r4++, r4 > 1e3) throw new Error(`Theme probably contains circular dependency at ${a4}: ${l4.toString()}`);
                        t4[a4] = l4(e4);
                      }
                      return t4[a4];
                    }, enumerable: true }) : "object" == typeof l4 && n3(l4, a4);
                  }));
                };
                return n3(e4, ""), JSON.parse(JSON.stringify(e4));
              })(b2({}, i2, (c3 = B2({}, e3.theme), z2(c3, L2({ extensionsHook: void 0 }))))), this.theme.extensionsHook = l3, p3 = e3.labels, Object.assign(T2, p3), this.scrollYOffset = W2.normalizeScrollYOffset(e3.scrollYOffset), this.hideHostname = W2.normalizeHideHostname(e3.hideHostname), this.expandResponses = W2.normalizeExpandResponses(e3.expandResponses), this.sortRequiredPropsFirst = Q2(e3.sortRequiredPropsFirst || e3.requiredPropsFirst), this.sortPropsAlphabetically = Q2(e3.sortPropsAlphabetically), this.sortEnumValuesAlphabetically = Q2(e3.sortEnumValuesAlphabetically), this.sortOperationsAlphabetically = Q2(e3.sortOperationsAlphabetically), this.sortTagsAlphabetically = Q2(e3.sortTagsAlphabetically), this.nativeScrollbars = Q2(e3.nativeScrollbars), this.pathInMiddlePanel = Q2(e3.pathInMiddlePanel), this.sanitize = Q2(e3.sanitize || e3.untrustedSpec), this.hideDownloadButtons = Q2(e3.hideDownloadButtons || e3.hideDownloadButton), this.downloadFileName = e3.downloadFileName, this.downloadDefinitionUrl = e3.downloadDefinitionUrl, this.downloadUrls = e3.downloadUrls, this.disableSearch = Q2(e3.disableSearch), this.onlyRequiredInSamples = Q2(e3.onlyRequiredInSamples), this.showExtensions = W2.normalizeShowExtensions(e3.showExtensions), this.sideNavStyle = W2.normalizeSideNavStyle(e3.sideNavStyle), this.hideSingleRequestSampleTab = Q2(e3.hideSingleRequestSampleTab), this.hideRequestPayloadSample = Q2(e3.hideRequestPayloadSample), this.menuToggle = Q2(e3.menuToggle, true), this.jsonSamplesExpandLevel = W2.normalizeJsonSampleExpandLevel(e3.jsonSamplesExpandLevel || e3.jsonSampleExpandLevel), this.enumSkipQuotes = Q2(e3.enumSkipQuotes), this.hideSchemaTitles = Q2(e3.hideSchemaTitles), this.simpleOneOfTypeLabel = Q2(e3.simpleOneOfTypeLabel), this.payloadSampleIdx = W2.normalizePayloadSampleIdx(e3.payloadSampleIdx), this.expandSingleSchemaField = Q2(e3.expandSingleSchemaField), this.schemasExpansionLevel = (function(e4, t4 = 0) {
                return "all" === e4 ? 1 / 0 : U2(e4) || t4;
              })(e3.schemasExpansionLevel || e3.schemaExpansionLevel), this.schemaDefinitionsTagName = e3.schemaDefinitionsTagName, this.showObjectSchemaExamples = Q2(e3.showObjectSchemaExamples), this.showSecuritySchemeType = Q2(e3.showSecuritySchemeType), this.hideSecuritySection = Q2(e3.hideSecuritySection), this.unstable_ignoreMimeParameters = Q2(e3.unstable_ignoreMimeParameters), this.allowedMdComponents = e3.allowedMdComponents || {}, this.expandDefaultServerVariables = Q2(e3.expandDefaultServerVariables), this.maxDisplayedEnumValues = U2(e3.maxDisplayedEnumValues);
              const u3 = I2(e3.ignoreNamedSchemas) ? e3.ignoreNamedSchemas : null == (a3 = e3.ignoreNamedSchemas) ? void 0 : a3.split(",").map(((e4) => e4.trim()));
              this.ignoreNamedSchemas = new Set(u3), this.hideSchemaPattern = Q2(e3.hideSchemaPattern), this.generatedSamplesMaxDepth = W2.normalizeGeneratedPayloadSamplesMaxDepth(e3.generatedSamplesMaxDepth || e3.generatedPayloadSamplesMaxDepth), this.nonce = e3.nonce, this.hideFab = Q2(e3.hideFab), this.minCharacterLengthToInitSearch = U2(e3.minCharacterLengthToInitSearch) || 3, this.showWebhookVerb = Q2(e3.showWebhookVerb), this.hidePropertiesPrefix = Q2(e3.hidePropertiesPrefix, true);
            }
          }
          var V2 = (init_styled_components_browser_esm(), __toCommonJS(styled_components_browser_esm_exports)), H2 = r2.n(V2);
          const { default: J2, css: Y2, createGlobalStyle: G2, keyframes: Z2, ThemeProvider: X2 } = V2, K2 = { lessThan(e3, t3, r3) {
            return (...n2) => Y2`
      @media ${t3 ? "print, " : ""} screen and (max-width: ${(t4) => t4.theme.breakpoints[e3]}) ${r3 || ""} {
        ${Y2(...n2)};
      }
    `;
          }, greaterThan(e3) {
            return (...t3) => Y2`
      @media (min-width: ${(t4) => t4.theme.breakpoints[e3]}) {
        ${Y2(...t3)};
      }
    `;
          }, between(e3, t3) {
            return (...r3) => Y2`
      @media (min-width: ${(t4) => t4.theme.breakpoints[e3]}) and (max-width: ${(e4) => e4.theme.breakpoints[t3]}) {
        ${Y2(...r3)};
      }
    `;
          } };
          var ee2 = J2;
          function te2(e3) {
            return (t3) => {
              if (t3.theme.extensionsHook) return t3.theme.extensionsHook(e3, t3);
            };
          }
          const re2 = ee2.div`
  padding: 20px;
  color: red;
`;
          class ne2 extends e2.Component {
            constructor(e3) {
              super(e3), this.state = { error: void 0 };
            }
            componentDidCatch(e3) {
              return this.setState({ error: e3 }), false;
            }
            render() {
              return this.state.error ? e2.createElement(re2, null, e2.createElement("h1", null, "Something went wrong..."), e2.createElement("small", null, " ", this.state.error.message, " "), e2.createElement("p", null, e2.createElement("details", null, e2.createElement("summary", null, "Stack trace"), e2.createElement("pre", null, this.state.error.stack))), e2.createElement("small", null, " ReDoc Version: ", "2.5.1"), " ", e2.createElement("br", null), e2.createElement("small", null, " Commit: ", "d140213")) : e2.createElement(e2.Fragment, null, e2.Children.only(this.props.children));
            }
          }
          const oe2 = Z2`
  0% {
    transform: rotate(0deg); }
  100% {
    transform: rotate(360deg);
  }
`, ie2 = ee2(((t3) => e2.createElement("svg", { className: t3.className, version: "1.1", width: "512", height: "512", viewBox: "0 0 512 512" }, e2.createElement("path", { d: "M275.682 147.999c0 10.864-8.837 19.661-19.682 19.661v0c-10.875 0-19.681-8.796-19.681-19.661v-96.635c0-10.885 8.806-19.661 19.681-19.661v0c10.844 0 19.682 8.776 19.682 19.661v96.635z" }), e2.createElement("path", { d: "M275.682 460.615c0 10.865-8.837 19.682-19.682 19.682v0c-10.875 0-19.681-8.817-19.681-19.682v-96.604c0-10.885 8.806-19.681 19.681-19.681v0c10.844 0 19.682 8.796 19.682 19.682v96.604z" }), e2.createElement("path", { d: "M147.978 236.339c10.885 0 19.681 8.755 19.681 19.641v0c0 10.885-8.796 19.702-19.681 19.702h-96.624c-10.864 0-19.661-8.817-19.661-19.702v0c0-10.885 8.796-19.641 19.661-19.641h96.624z" }), e2.createElement("path", { d: "M460.615 236.339c10.865 0 19.682 8.755 19.682 19.641v0c0 10.885-8.817 19.702-19.682 19.702h-96.584c-10.885 0-19.722-8.817-19.722-19.702v0c0-10.885 8.837-19.641 19.722-19.641h96.584z" }), e2.createElement("path", { d: "M193.546 165.703c7.69 7.66 7.68 20.142 0 27.822v0c-7.701 7.701-20.162 7.701-27.853 0.020l-68.311-68.322c-7.68-7.701-7.68-20.142 0-27.863v0c7.68-7.68 20.121-7.68 27.822 0l68.342 68.342z" }), e2.createElement("path", { d: "M414.597 386.775c7.7 7.68 7.7 20.163 0.021 27.863v0c-7.7 7.659-20.142 7.659-27.843-0.062l-68.311-68.26c-7.68-7.7-7.68-20.204 0-27.863v0c7.68-7.7 20.163-7.7 27.842 0l68.291 68.322z" }), e2.createElement("path", { d: "M165.694 318.464c7.69-7.7 20.153-7.7 27.853 0v0c7.68 7.659 7.69 20.163 0 27.863l-68.342 68.322c-7.67 7.659-20.142 7.659-27.822-0.062v0c-7.68-7.68-7.68-20.122 0-27.801l68.311-68.322z" }), e2.createElement("path", { d: "M386.775 97.362c7.7-7.68 20.142-7.68 27.822 0v0c7.7 7.68 7.7 20.183 0.021 27.863l-68.322 68.311c-7.68 7.68-20.163 7.68-27.843-0.020v0c-7.68-7.68-7.68-20.162 0-27.822l68.322-68.332z" }))))`
  animation: 2s ${oe2} linear infinite;
  width: 50px;
  height: 50px;
  content: '';
  display: inline-block;
  margin-left: -25px;

  path {
    fill: ${(e3) => e3.color};
  }
`, se2 = ee2.div`
  font-family: helvetica, sans;
  width: 100%;
  text-align: center;
  font-size: 25px;
  margin: 30px 0 20px 0;
  color: ${(e3) => e3.color};
`;
          class ae2 extends e2.PureComponent {
            render() {
              return e2.createElement("div", { style: { textAlign: "center" } }, e2.createElement(se2, { color: this.props.color }, "Loading ..."), e2.createElement(ie2, { color: this.props.color }));
            }
          }
          var le2 = require_prop_types();
          const ce2 = e2.createContext(new W2({})), pe2 = ce2.Provider, ue2 = ce2.Consumer;
          var de2 = (init_mobx_esm(), __toCommonJS(mobx_esm_exports)), he2 = require_bundle(), me2 = require_config(), fe2 = r2(65), ge2 = (e3, t3, r3) => new Promise(((n2, o3) => {
            var i3 = (e4) => {
              try {
                a3(r3.next(e4));
              } catch (e5) {
                o3(e5);
              }
            }, s3 = (e4) => {
              try {
                a3(r3.throw(e4));
              } catch (e5) {
                o3(e5);
              }
            }, a3 = (e4) => e4.done ? n2(e4.value) : Promise.resolve(e4.value).then(i3, s3);
            a3((r3 = r3.apply(e3, t3)).next());
          }));
          function ye2(e3) {
            return ge2(this, null, (function* () {
              const t3 = new me2.Config({}), n2 = { config: t3, base: s2 ? window.location.href : process.cwd() };
              s2 && (t3.resolve.http.customFetch = r2.g.fetch), "object" == typeof e3 && null !== e3 ? n2.doc = { source: { absoluteRef: "" }, parsed: e3 } : n2.ref = e3;
              const { bundle: { parsed: o3 } } = yield (0, he2.bundle)(n2);
              return void 0 !== o3.swagger ? be2(o3) : o3;
            }));
          }
          function be2(e3) {
            return console.warn("[ReDoc Compatibility mode]: Converting OpenAPI 2.0 to OpenAPI 3.0"), new Promise(((t3, r3) => (0, fe2.convertObj)(e3, { patch: true, warnOnly: true, text: "{}", anchors: true }, ((e4, n2) => {
              if (e4) return r3(e4);
              t3(n2 && n2.openapi);
            }))));
          }
          var xe2 = require_decko(), ve2 = require_eventemitter3(), we2 = require_json_pointer();
          const ke2 = we2.parse;
          class Ee2 {
            static baseName(e3, t3 = 1) {
              const r3 = Ee2.parse(e3);
              return r3[r3.length - t3];
            }
            static dirName(e3, t3 = 1) {
              const r3 = Ee2.parse(e3);
              return we2.compile(r3.slice(0, r3.length - t3));
            }
            static relative(e3, t3) {
              const r3 = Ee2.parse(e3);
              return Ee2.parse(t3).slice(r3.length);
            }
            static parse(e3) {
              let t3 = e3;
              return "#" === t3.charAt(0) && (t3 = t3.substring(1)), ke2(t3);
            }
            static join(e3, t3) {
              const r3 = Ee2.parse(e3).concat(t3);
              return we2.compile(r3);
            }
            static get(e3, t3) {
              return we2.get(e3, t3);
            }
            static compile(e3) {
              return we2.compile(e3);
            }
            static escape(e3) {
              return we2.escape(e3);
            }
          }
          we2.parse = Ee2.parse, Object.assign(Ee2, we2);
          var Oe2 = r2(975), je2 = require_url_template(), $e2 = Object.defineProperty, Se2 = Object.defineProperties, Ce2 = Object.getOwnPropertyDescriptors, Pe2 = Object.getOwnPropertySymbols, Ie2 = Object.prototype.hasOwnProperty, Ae2 = Object.prototype.propertyIsEnumerable, Te2 = (e3, t3, r3) => t3 in e3 ? $e2(e3, t3, { enumerable: true, configurable: true, writable: true, value: r3 }) : e3[t3] = r3, _e2 = (e3, t3) => {
            for (var r3 in t3 || (t3 = {})) Ie2.call(t3, r3) && Te2(e3, r3, t3[r3]);
            if (Pe2) for (var r3 of Pe2(t3)) Ae2.call(t3, r3) && Te2(e3, r3, t3[r3]);
            return e3;
          }, Re2 = (e3, t3) => Se2(e3, Ce2(t3));
          function De2(e3) {
            return "string" == typeof e3 && /\dxx/i.test(e3);
          }
          function ze2(e3) {
            return "default" === e3 || g2(e3) || De2(e3);
          }
          function Le2(e3, t3 = false) {
            if ("default" === e3) return t3 ? "error" : "success";
            let r3 = "string" == typeof e3 ? parseInt(e3, 10) : e3;
            if (De2(e3) && (r3 *= 100), r3 < 100 || r3 > 599) throw new Error("invalid HTTP code");
            let n2 = "success";
            return r3 >= 300 && r3 < 400 ? n2 = "redirect" : r3 >= 400 ? n2 = "error" : r3 < 200 && (n2 = "info"), n2;
          }
          const Ne2 = { get: true, post: true, put: true, head: true, patch: true, delete: true, options: true, $ref: true };
          function Me2(e3) {
            return e3 in Ne2;
          }
          function qe2(e3) {
            return e3.summary || e3.operationId || e3.description && e3.description.substring(0, 50) || e3.pathName || "<no summary>";
          }
          const Fe2 = { multipleOf: "number", maximum: "number", exclusiveMaximum: "number", minimum: "number", exclusiveMinimum: "number", maxLength: "string", minLength: "string", pattern: "string", contentEncoding: "string", contentMediaType: "string", items: "array", maxItems: "array", minItems: "array", uniqueItems: "array", maxProperties: "object", minProperties: "object", required: "object", additionalProperties: "object", unevaluatedProperties: "object", properties: "object", patternProperties: "object" };
          function Be2(e3) {
            if (void 0 !== e3.type && !I2(e3.type)) return e3.type;
            const t3 = Object.keys(Fe2);
            for (const r3 of t3) {
              const t4 = Fe2[r3];
              if (void 0 !== e3[r3]) return t4;
            }
            return "any";
          }
          function Qe2(e3, t3 = e3.type) {
            if (e3["x-circular-ref"]) return true;
            if (void 0 !== e3.oneOf || void 0 !== e3.anyOf) return false;
            if (e3.if && e3.then || e3.if && e3.else) return false;
            let r3 = true;
            const n2 = I2(t3);
            return ("object" === t3 || n2 && (null == t3 ? void 0 : t3.includes("object"))) && (r3 = void 0 !== e3.properties ? 0 === Object.keys(e3.properties).length : void 0 === e3.additionalProperties && void 0 === e3.unevaluatedProperties && void 0 === e3.patternProperties), !I2(e3.items) && !I2(e3.prefixItems) && (void 0 !== e3.items && !A2(e3.items) && ("array" === t3 || n2 && (null == t3 ? void 0 : t3.includes("array"))) && (r3 = Qe2(e3.items, e3.items.type)), r3);
          }
          function Ue2(e3) {
            return -1 !== e3.search(/json/i);
          }
          function We2(e3) {
            return "application/x-www-form-urlencoded" === e3;
          }
          function Ve2(e3, t3, r3) {
            return I2(e3) ? e3.map(((e4) => e4.toString())).join(r3) : "object" == typeof e3 ? Object.keys(e3).map(((t4) => `${t4}${r3}${e3[t4]}`)).join(r3) : t3 + "=" + e3.toString();
          }
          function He2(e3, t3) {
            return I2(e3) ? (console.warn("deepObject style cannot be used with array value:" + e3.toString()), "") : "object" == typeof e3 ? Object.keys(e3).map(((r3) => `${t3}[${r3}]=${e3[r3]}`)).join("&") : (console.warn("deepObject style cannot be used with non-object value:" + e3.toString()), "");
          }
          function Je2(e3, t3, r3) {
            const n2 = "__redoc_param_name__", o3 = t3 ? "*" : "";
            return je2.parse(`{?${n2}${o3}}`).expand({ [n2]: r3 }).substring(1).replace(/__redoc_param_name__/g, e3);
          }
          function Ye2(e3, t3 = {}) {
            if (I2(e3)) throw new Error("Payload must have fields: " + e3.toString());
            return Object.keys(e3).map(((r3) => {
              const n2 = e3[r3], { style: o3 = "form", explode: i3 = true } = t3[r3] || {};
              switch (o3) {
                case "form":
                  return Je2(r3, i3, n2);
                case "spaceDelimited":
                  return Ve2(n2, r3, "%20");
                case "pipeDelimited":
                  return Ve2(n2, r3, "|");
                case "deepObject":
                  return He2(n2, r3);
                default:
                  return console.warn("Incorrect or unsupported encoding style: " + o3), "";
              }
            })).join("&");
          }
          function Ge2(e3, t3) {
            return Ue2(t3) ? JSON.stringify(e3) : (console.warn(`Parameter serialization as ${t3} is not supported`), "");
          }
          function Ze2(e3, t3) {
            const { name: r3, style: n2, explode: o3 = false, serializationMime: i3 } = e3;
            if (i3) switch (e3.in) {
              case "path":
              case "header":
                return Ge2(t3, i3);
              case "cookie":
              case "query":
                return `${r3}=${Ge2(t3, i3)}`;
              default:
                return console.warn("Unexpected parameter location: " + e3.in), "";
            }
            if (!n2) return console.warn(`Missing style attribute or content for parameter ${r3}`), "";
            switch (e3.in) {
              case "path":
                return (function(e4, t4, r4, n3) {
                  const o4 = r4 ? "*" : "";
                  let i4 = "";
                  "label" === t4 ? i4 = "." : "matrix" === t4 && (i4 = ";");
                  const s3 = "__redoc_param_name__";
                  return je2.parse(`{${i4}${s3}${o4}}`).expand({ [s3]: n3 }).replace(/__redoc_param_name__/g, e4);
                })(r3, n2, o3, t3);
              case "query":
                return (function(e4, t4, r4, n3) {
                  switch (t4) {
                    case "form":
                      return Je2(e4, r4, n3);
                    case "spaceDelimited":
                      return I2(n3) ? r4 ? Je2(e4, r4, n3) : `${e4}=${n3.join("%20")}` : (console.warn("The style spaceDelimited is only applicable to arrays"), "");
                    case "pipeDelimited":
                      return I2(n3) ? r4 ? Je2(e4, r4, n3) : `${e4}=${n3.join("|")}` : (console.warn("The style pipeDelimited is only applicable to arrays"), "");
                    case "deepObject":
                      return !r4 || I2(n3) || "object" != typeof n3 ? (console.warn("The style deepObject is only applicable for objects with explode=true"), "") : He2(n3, e4);
                    default:
                      return console.warn("Unexpected style for query: " + t4), "";
                  }
                })(r3, n2, o3, t3);
              case "header":
                return (function(e4, t4, r4) {
                  if ("simple" === e4) {
                    const e5 = t4 ? "*" : "", n3 = "__redoc_param_name__", o4 = je2.parse(`{${n3}${e5}}`);
                    return decodeURIComponent(o4.expand({ [n3]: r4 }));
                  }
                  return console.warn("Unexpected style for header: " + e4), "";
                })(n2, o3, t3);
              case "cookie":
                return (function(e4, t4, r4, n3) {
                  return "form" === t4 ? Je2(e4, r4, n3) : (console.warn("Unexpected style for cookie: " + t4), "");
                })(r3, n2, o3, t3);
              default:
                return console.warn("Unexpected parameter location: " + e3.in), "";
            }
          }
          function Xe2(e3, t3) {
            return e3.in ? decodeURIComponent(Ze2(e3, t3)) : "object" == typeof t3 ? t3 : String(t3);
          }
          function Ke2(e3) {
            return -1 !== e3.search(/xml/i) ? "xml" : -1 !== e3.search(/csv/i) ? "csv" : -1 !== e3.search(/plain/i) ? "tex" : "clike";
          }
          const et2 = /^#\/components\/(schemas|pathItems)\/([^/]+)$/;
          function tt2(e3) {
            return et2.test(e3 || "");
          }
          function rt2(e3) {
            var t3;
            const [r3] = (null == (t3 = null == e3 ? void 0 : e3.match(et2)) ? void 0 : t3.reverse()) || [];
            return r3;
          }
          function nt2(e3, t3, r3) {
            let n2;
            return void 0 !== t3 && void 0 !== r3 ? n2 = t3 === r3 ? `= ${t3} ${e3}` : `[ ${t3} .. ${r3} ] ${e3}` : void 0 !== r3 ? n2 = `<= ${r3} ${e3}` : void 0 !== t3 && (n2 = 1 === t3 ? "non-empty" : `>= ${t3} ${e3}`), n2;
          }
          function ot2(e3) {
            var t3, r3;
            const n2 = "number" == typeof e3.exclusiveMinimum ? Math.min(e3.exclusiveMinimum, null != (t3 = e3.minimum) ? t3 : 1 / 0) : e3.minimum, o3 = "number" == typeof e3.exclusiveMaximum ? Math.max(e3.exclusiveMaximum, null != (r3 = e3.maximum) ? r3 : -1 / 0) : e3.maximum, i3 = "number" == typeof e3.exclusiveMinimum || e3.exclusiveMinimum, s3 = "number" == typeof e3.exclusiveMaximum || e3.exclusiveMaximum;
            return void 0 !== n2 && void 0 !== o3 ? `${i3 ? "( " : "[ "}${n2} .. ${o3}${s3 ? " )" : " ]"}` : void 0 !== o3 ? `${s3 ? "< " : "<= "}${o3}` : void 0 !== n2 ? `${i3 ? "> " : ">= "}${n2}` : void 0;
          }
          function it2(e3) {
            const t3 = [], r3 = nt2("characters", e3.minLength, e3.maxLength);
            void 0 !== r3 && t3.push(r3);
            const n2 = nt2("items", e3.minItems, e3.maxItems);
            void 0 !== n2 && t3.push(n2);
            const o3 = nt2("properties", e3.minProperties, e3.maxProperties);
            void 0 !== o3 && t3.push(o3);
            const i3 = (function(e4) {
              if (void 0 === e4) return;
              const t4 = e4.toString(10);
              return /^0\.0*1$/.test(t4) ? `decimal places <= ${t4.split(".")[1].length}` : `multiple of ${t4}`;
            })(e3.multipleOf);
            void 0 !== i3 && t3.push(i3);
            const s3 = ot2(e3);
            return void 0 !== s3 && t3.push(s3), e3.uniqueItems && t3.push("unique"), t3;
          }
          function st2(e3, t3 = []) {
            const r3 = [], n2 = [], o3 = [];
            return e3.forEach(((e4) => {
              e4.required ? t3.includes(e4.name) ? n2.push(e4) : o3.push(e4) : r3.push(e4);
            })), n2.sort(((e4, r4) => t3.indexOf(e4.name) - t3.indexOf(r4.name))), [...n2, ...o3, ...r3];
          }
          function at2(e3, t3) {
            return [...e3].sort(((e4, r3) => e4[t3].localeCompare(r3[t3])));
          }
          function lt2(e3, t3 = [], r3 = []) {
            const n2 = {};
            return r3.forEach(((t4) => {
              ({ resolved: t4 } = e3.deref(t4)), n2[t4.name + "_" + t4.in] = true;
            })), (t3 = t3.filter(((t4) => ({ resolved: t4 } = e3.deref(t4), !n2[t4.name + "_" + t4.in])))).concat(r3);
          }
          function ct2(e3) {
            const t3 = {};
            return Object.keys(e3).forEach(((r3) => {
              const n2 = e3[r3], o3 = r3.split(";")[0].trim();
              t3[o3] ? t3[o3] = _e2(_e2({}, t3[o3]), n2) : t3[o3] = n2;
            })), t3;
          }
          function pt2(e3, t3 = {}) {
            return e3.replace(/(?:{)([\w-.]+)(?:})/g, ((e4, r3) => t3[r3] && t3[r3].default || e4));
          }
          function ut2(e3, t3) {
            const r3 = void 0 === e3 ? $2((() => {
              if (!s2) return "";
              const e4 = window.location.href;
              return e4.endsWith(".html") ? (0, Oe2.dirname)(e4) : e4;
            })()) : (0, Oe2.dirname)(e3);
            return 0 === t3.length && (t3 = [{ url: "/" }]), t3.map(((e4) => {
              return Re2(_e2({}, e4), { url: (t4 = e4.url, E2(r3, t4)), description: e4.description || "" });
              var t4;
            }));
          }
          const dt2 = "SecurityDefinitions", ht2 = "security-definitions", mt2 = "SchemaDefinition";
          let ft2 = "section/Authentication/";
          function gt2(e3) {
            ft2 = e3;
          }
          const yt2 = (e3) => ({ delete: "del", options: "opts" })[e3] || e3;
          function bt(e3) {
            return e3 in { "x-circular-ref": true, "x-parentRefs": true, "x-refsStack": true, "x-code-samples": true, "x-codeSamples": true, "x-displayName": true, "x-examples": true, "x-enumDescriptions": true, "x-logo": true, "x-nullable": true, "x-servers": true, "x-tagGroups": true, "x-traitTag": true, "x-badges": true, "x-additionalPropertiesName": true, "x-explicitMappingOnly": true };
          }
          function xt(e3, t3) {
            return Object.keys(e3).filter(((e4) => true === t3 ? e4.startsWith("x-") && !bt(e4) : e4.startsWith("x-") && t3.indexOf(e4) > -1)).reduce(((t4, r3) => (t4[r3] = e3[r3], t4)), {});
          }
          function vt2(e3) {
            return e3.split(" or ").map(((e4) => e4.replace(/^(string|object|number|integer|array|boolean)s?( ?.*)/, "$1s$2"))).join(" or ");
          }
          function wt(e3) {
            let t3 = e3.content;
            const r3 = e3["x-examples"], n2 = e3["x-example"];
            if (r3) {
              t3 = _e2({}, t3);
              for (const e4 of Object.keys(r3)) {
                const n3 = r3[e4];
                t3[e4] = Re2(_e2({}, t3[e4]), { examples: n3 });
              }
            } else if (n2) {
              t3 = _e2({}, t3);
              for (const e4 of Object.keys(n2)) {
                const r4 = n2[e4];
                t3[e4] = Re2(_e2({}, t3[e4]), { example: r4 });
              }
            }
            return t3;
          }
          var kt = require_prism();
          require_prism_bash(), require_prism_c(), require_prism_clike(), require_prism_coffeescript(), require_prism_cpp(), require_prism_csharp(), require_prism_go(), require_prism_http(), require_prism_java(), require_prism_lua(), require_prism_markup_templating(), require_prism_markup(), require_prism_objectivec(), require_prism_perl(), require_prism_php(), require_prism_python(), require_prism_q(), require_prism_ruby(), require_prism_scala(), require_prism_sql(), require_prism_swift(), require_prism_yaml(), require_prism_csv();
          const Et = "clike";
          function Ot(e3) {
            return { json: "js", "c++": "cpp", "c#": "csharp", "objective-c": "objectivec", shell: "bash", viml: "vim" }[e3] || Et;
          }
          function jt(e3, t3 = Et) {
            t3 = t3.toLowerCase();
            let r3 = kt.languages[t3];
            return r3 || (r3 = kt.languages[Ot(t3)]), kt.highlight(e3.toString(), r3, t3);
          }
          function $t(e3) {
            return (t3, r3, n2) => {
              n2.value = /* @__PURE__ */ (function(e4, t4) {
                let r4, n3, o3, i3 = null, s3 = 0;
                const a3 = () => {
                  s3 = (/* @__PURE__ */ new Date()).getTime(), i3 = null, o3 = e4.apply(r4, n3), i3 || (r4 = n3 = null);
                };
                return function() {
                  const l3 = (/* @__PURE__ */ new Date()).getTime(), c3 = t4 - (l3 - s3);
                  return r4 = this, n3 = arguments, c3 <= 0 || c3 > t4 ? (i3 && (clearTimeout(i3), i3 = null), s3 = l3, o3 = e4.apply(r4, n3), i3 || (r4 = n3 = null)) : i3 || (i3 = setTimeout(a3, c3)), o3;
                };
              })(n2.value, e3);
            };
          }
          function St2(e3) {
          }
          function Ct(e3) {
          }
          kt.languages.insertBefore("javascript", "string", { "property string": { pattern: /([{,]\s*)"(?:\\.|[^\\"\r\n])*"(?=\s*:)/i, lookbehind: true } }, void 0), kt.languages.insertBefore("javascript", "punctuation", { property: { pattern: /([{,]\s*)[a-z]\w*(?=\s*:)/i, lookbehind: true } }, void 0);
          var Pt = Object.defineProperty, It = Object.defineProperties, At = Object.getOwnPropertyDescriptors, Tt = Object.getOwnPropertySymbols, _t = Object.prototype.hasOwnProperty, Rt = Object.prototype.propertyIsEnumerable, Dt = (e3, t3, r3) => t3 in e3 ? Pt(e3, t3, { enumerable: true, configurable: true, writable: true, value: r3 }) : e3[t3] = r3, zt = (e3, t3) => {
            for (var r3 in t3 || (t3 = {})) _t.call(t3, r3) && Dt(e3, r3, t3[r3]);
            if (Tt) for (var r3 of Tt(t3)) Rt.call(t3, r3) && Dt(e3, r3, t3[r3]);
            return e3;
          }, Lt = (e3, t3) => It(e3, At(t3));
          const Nt = {};
          function Mt(e3, t3, r3) {
            if ("function" == typeof r3.value) return (function(e4, t4, r4) {
              if (!r4.value || r4.value.length > 0) throw new Error("@memoize decorator can only be applied to methods of zero arguments");
              const n2 = `_memoized_${t4}`, o3 = r4.value;
              return e4[n2] = Nt, Lt(zt({}, r4), { value() {
                return this[n2] === Nt && (this[n2] = o3.call(this)), this[n2];
              } });
            })(e3, t3, r3);
            if ("function" == typeof r3.get) return (function(e4, t4, r4) {
              const n2 = `_memoized_${t4}`, o3 = r4.get;
              return e4[n2] = Nt, Lt(zt({}, r4), { get() {
                return this[n2] === Nt && (this[n2] = o3.call(this)), this[n2];
              } });
            })(e3, t3, r3);
            throw new Error("@memoize decorator can be applied to methods or getters, got " + String(r3.value) + " instead");
          }
          function qt(e3) {
            let t3 = 1;
            return "-" === e3[0] && (t3 = -1, e3 = e3.substr(1)), (r3, n2) => -1 == t3 ? n2[e3].localeCompare(r3[e3]) : r3[e3].localeCompare(n2[e3]);
          }
          var Ft = Object.defineProperty, Bt = Object.getOwnPropertyDescriptor;
          const Qt = "hashchange";
          class Ut {
            constructor() {
              this.emit = () => {
                this._emiter.emit(Qt, this.currentId);
              }, this._emiter = new ve2.EventEmitter(), this.bind();
            }
            get currentId() {
              return s2 ? decodeURIComponent(window.location.hash.substring(1)) : "";
            }
            linkForId(e3) {
              return e3 ? "#" + e3 : "";
            }
            subscribe(e3) {
              const t3 = this._emiter.addListener(Qt, e3);
              return () => t3.removeListener(Qt, e3);
            }
            bind() {
              s2 && window.addEventListener("hashchange", this.emit, false);
            }
            dispose() {
              s2 && window.removeEventListener("hashchange", this.emit);
            }
            replace(e3, t3 = false) {
              s2 && null != e3 && e3 !== this.currentId && (t3 ? window.history.replaceState(null, "", window.location.href.split("#")[0] + this.linkForId(e3)) : (window.history.pushState(null, "", window.location.href.split("#")[0] + this.linkForId(e3)), this.emit()));
            }
          }
          ((e3, t3, r3) => {
            for (var n2, o3 = Bt(t3, r3), i3 = e3.length - 1; i3 >= 0; i3--) (n2 = e3[i3]) && (o3 = n2(t3, r3, o3) || o3);
            o3 && Ft(t3, r3, o3);
          })([xe2.bind, xe2.debounce], Ut.prototype, "replace");
          const Wt = new Ut();
          var Vt = require_mark();
          class Ht {
            constructor() {
              this.map = /* @__PURE__ */ new Map(), this.prevTerm = "";
            }
            add(e3) {
              this.map.set(e3, new Vt(e3));
            }
            delete(e3) {
              this.map.delete(e3);
            }
            addOnly(e3) {
              this.map.forEach(((t3, r3) => {
                -1 === e3.indexOf(r3) && (t3.unmark(), this.map.delete(r3));
              }));
              for (const t3 of e3) this.map.has(t3) || this.map.set(t3, new Vt(t3));
            }
            clearAll() {
              this.unmark(), this.map.clear();
            }
            mark(e3) {
              (e3 || this.prevTerm) && (this.map.forEach(((t3) => {
                t3.unmark(), t3.mark(e3 || this.prevTerm);
              })), this.prevTerm = e3 || this.prevTerm);
            }
            unmark() {
              this.map.forEach(((e3) => e3.unmark())), this.prevTerm = "";
            }
          }
          let Jt = { async: false, baseUrl: null, breaks: false, extensions: null, gfm: true, headerIds: true, headerPrefix: "", highlight: null, hooks: null, langPrefix: "language-", mangle: true, pedantic: false, renderer: null, sanitize: false, sanitizer: null, silent: false, smartypants: false, tokenizer: null, walkTokens: null, xhtml: false };
          const Yt = /[&<>"']/, Gt = new RegExp(Yt.source, "g"), Zt = /[<>"']|&(?!(#\d{1,7}|#[Xx][a-fA-F0-9]{1,6}|\w+);)/, Xt = new RegExp(Zt.source, "g"), Kt = { "&": "&amp;", "<": "&lt;", ">": "&gt;", '"': "&quot;", "'": "&#39;" }, er = (e3) => Kt[e3];
          function tr(e3, t3) {
            if (t3) {
              if (Yt.test(e3)) return e3.replace(Gt, er);
            } else if (Zt.test(e3)) return e3.replace(Xt, er);
            return e3;
          }
          const rr = /&(#(?:\d+)|(?:#x[0-9A-Fa-f]+)|(?:\w+));?/gi;
          function nr(e3) {
            return e3.replace(rr, ((e4, t3) => "colon" === (t3 = t3.toLowerCase()) ? ":" : "#" === t3.charAt(0) ? "x" === t3.charAt(1) ? String.fromCharCode(parseInt(t3.substring(2), 16)) : String.fromCharCode(+t3.substring(1)) : ""));
          }
          const or = /(^|[^\[])\^/g;
          function ir(e3, t3) {
            e3 = "string" == typeof e3 ? e3 : e3.source, t3 = t3 || "";
            const r3 = { replace: (t4, n2) => (n2 = (n2 = n2.source || n2).replace(or, "$1"), e3 = e3.replace(t4, n2), r3), getRegex: () => new RegExp(e3, t3) };
            return r3;
          }
          const sr = /[^\w:]/g, ar = /^$|^[a-z][a-z0-9+.-]*:|^[?#]/i;
          function lr(e3, t3, r3) {
            if (e3) {
              let e4;
              try {
                e4 = decodeURIComponent(nr(r3)).replace(sr, "").toLowerCase();
              } catch (e5) {
                return null;
              }
              if (0 === e4.indexOf("javascript:") || 0 === e4.indexOf("vbscript:") || 0 === e4.indexOf("data:")) return null;
            }
            t3 && !ar.test(r3) && (r3 = (function(e4, t4) {
              cr[" " + e4] || (pr.test(e4) ? cr[" " + e4] = e4 + "/" : cr[" " + e4] = fr(e4, "/", true));
              const r4 = -1 === (e4 = cr[" " + e4]).indexOf(":");
              return "//" === t4.substring(0, 2) ? r4 ? t4 : e4.replace(ur, "$1") + t4 : "/" === t4.charAt(0) ? r4 ? t4 : e4.replace(dr, "$1") + t4 : e4 + t4;
            })(t3, r3));
            try {
              r3 = encodeURI(r3).replace(/%25/g, "%");
            } catch (e4) {
              return null;
            }
            return r3;
          }
          const cr = {}, pr = /^[^:]+:\/*[^/]*$/, ur = /^([^:]+:)[\s\S]*$/, dr = /^([^:]+:\/*[^/]*)[\s\S]*$/, hr = { exec: function() {
          } };
          function mr(e3, t3) {
            const r3 = e3.replace(/\|/g, ((e4, t4, r4) => {
              let n3 = false, o3 = t4;
              for (; --o3 >= 0 && "\\" === r4[o3]; ) n3 = !n3;
              return n3 ? "|" : " |";
            })).split(/ \|/);
            let n2 = 0;
            if (r3[0].trim() || r3.shift(), r3.length > 0 && !r3[r3.length - 1].trim() && r3.pop(), r3.length > t3) r3.splice(t3);
            else for (; r3.length < t3; ) r3.push("");
            for (; n2 < r3.length; n2++) r3[n2] = r3[n2].trim().replace(/\\\|/g, "|");
            return r3;
          }
          function fr(e3, t3, r3) {
            const n2 = e3.length;
            if (0 === n2) return "";
            let o3 = 0;
            for (; o3 < n2; ) {
              const i3 = e3.charAt(n2 - o3 - 1);
              if (i3 !== t3 || r3) {
                if (i3 === t3 || !r3) break;
                o3++;
              } else o3++;
            }
            return e3.slice(0, n2 - o3);
          }
          function gr(e3, t3) {
            if (t3 < 1) return "";
            let r3 = "";
            for (; t3 > 1; ) 1 & t3 && (r3 += e3), t3 >>= 1, e3 += e3;
            return r3 + e3;
          }
          function yr(e3, t3, r3, n2) {
            const o3 = t3.href, i3 = t3.title ? tr(t3.title) : null, s3 = e3[1].replace(/\\([\[\]])/g, "$1");
            if ("!" !== e3[0].charAt(0)) {
              n2.state.inLink = true;
              const e4 = { type: "link", raw: r3, href: o3, title: i3, text: s3, tokens: n2.inlineTokens(s3) };
              return n2.state.inLink = false, e4;
            }
            return { type: "image", raw: r3, href: o3, title: i3, text: tr(s3) };
          }
          class br {
            constructor(e3) {
              this.options = e3 || Jt;
            }
            space(e3) {
              const t3 = this.rules.block.newline.exec(e3);
              if (t3 && t3[0].length > 0) return { type: "space", raw: t3[0] };
            }
            code(e3) {
              const t3 = this.rules.block.code.exec(e3);
              if (t3) {
                const e4 = t3[0].replace(/^ {1,4}/gm, "");
                return { type: "code", raw: t3[0], codeBlockStyle: "indented", text: this.options.pedantic ? e4 : fr(e4, "\n") };
              }
            }
            fences(e3) {
              const t3 = this.rules.block.fences.exec(e3);
              if (t3) {
                const e4 = t3[0], r3 = (function(e5, t4) {
                  const r4 = e5.match(/^(\s+)(?:```)/);
                  if (null === r4) return t4;
                  const n2 = r4[1];
                  return t4.split("\n").map(((e6) => {
                    const t5 = e6.match(/^\s+/);
                    if (null === t5) return e6;
                    const [r5] = t5;
                    return r5.length >= n2.length ? e6.slice(n2.length) : e6;
                  })).join("\n");
                })(e4, t3[3] || "");
                return { type: "code", raw: e4, lang: t3[2] ? t3[2].trim().replace(this.rules.inline._escapes, "$1") : t3[2], text: r3 };
              }
            }
            heading(e3) {
              const t3 = this.rules.block.heading.exec(e3);
              if (t3) {
                let e4 = t3[2].trim();
                if (/#$/.test(e4)) {
                  const t4 = fr(e4, "#");
                  this.options.pedantic ? e4 = t4.trim() : t4 && !/ $/.test(t4) || (e4 = t4.trim());
                }
                return { type: "heading", raw: t3[0], depth: t3[1].length, text: e4, tokens: this.lexer.inline(e4) };
              }
            }
            hr(e3) {
              const t3 = this.rules.block.hr.exec(e3);
              if (t3) return { type: "hr", raw: t3[0] };
            }
            blockquote(e3) {
              const t3 = this.rules.block.blockquote.exec(e3);
              if (t3) {
                const e4 = t3[0].replace(/^ *>[ \t]?/gm, ""), r3 = this.lexer.state.top;
                this.lexer.state.top = true;
                const n2 = this.lexer.blockTokens(e4);
                return this.lexer.state.top = r3, { type: "blockquote", raw: t3[0], tokens: n2, text: e4 };
              }
            }
            list(e3) {
              let t3 = this.rules.block.list.exec(e3);
              if (t3) {
                let r3, n2, o3, i3, s3, a3, l3, c3, p3, u3, d2, h2, m3 = t3[1].trim();
                const f3 = m3.length > 1, g3 = { type: "list", raw: "", ordered: f3, start: f3 ? +m3.slice(0, -1) : "", loose: false, items: [] };
                m3 = f3 ? `\\d{1,9}\\${m3.slice(-1)}` : `\\${m3}`, this.options.pedantic && (m3 = f3 ? m3 : "[*+-]");
                const y3 = new RegExp(`^( {0,3}${m3})((?:[	 ][^\\n]*)?(?:\\n|$))`);
                for (; e3 && (h2 = false, t3 = y3.exec(e3)) && !this.rules.block.hr.test(e3); ) {
                  if (r3 = t3[0], e3 = e3.substring(r3.length), c3 = t3[2].split("\n", 1)[0].replace(/^\t+/, ((e4) => " ".repeat(3 * e4.length))), p3 = e3.split("\n", 1)[0], this.options.pedantic ? (i3 = 2, d2 = c3.trimLeft()) : (i3 = t3[2].search(/[^ ]/), i3 = i3 > 4 ? 1 : i3, d2 = c3.slice(i3), i3 += t3[1].length), a3 = false, !c3 && /^ *$/.test(p3) && (r3 += p3 + "\n", e3 = e3.substring(p3.length + 1), h2 = true), !h2) {
                    const t4 = new RegExp(`^ {0,${Math.min(3, i3 - 1)}}(?:[*+-]|\\d{1,9}[.)])((?:[ 	][^\\n]*)?(?:\\n|$))`), n3 = new RegExp(`^ {0,${Math.min(3, i3 - 1)}}((?:- *){3,}|(?:_ *){3,}|(?:\\* *){3,})(?:\\n+|$)`), o4 = new RegExp(`^ {0,${Math.min(3, i3 - 1)}}(?:\`\`\`|~~~)`), s4 = new RegExp(`^ {0,${Math.min(3, i3 - 1)}}#`);
                    for (; e3 && (u3 = e3.split("\n", 1)[0], p3 = u3, this.options.pedantic && (p3 = p3.replace(/^ {1,4}(?=( {4})*[^ ])/g, "  ")), !o4.test(p3)) && !s4.test(p3) && !t4.test(p3) && !n3.test(e3); ) {
                      if (p3.search(/[^ ]/) >= i3 || !p3.trim()) d2 += "\n" + p3.slice(i3);
                      else {
                        if (a3) break;
                        if (c3.search(/[^ ]/) >= 4) break;
                        if (o4.test(c3)) break;
                        if (s4.test(c3)) break;
                        if (n3.test(c3)) break;
                        d2 += "\n" + p3;
                      }
                      a3 || p3.trim() || (a3 = true), r3 += u3 + "\n", e3 = e3.substring(u3.length + 1), c3 = p3.slice(i3);
                    }
                  }
                  g3.loose || (l3 ? g3.loose = true : /\n *\n *$/.test(r3) && (l3 = true)), this.options.gfm && (n2 = /^\[[ xX]\] /.exec(d2), n2 && (o3 = "[ ] " !== n2[0], d2 = d2.replace(/^\[[ xX]\] +/, ""))), g3.items.push({ type: "list_item", raw: r3, task: !!n2, checked: o3, loose: false, text: d2 }), g3.raw += r3;
                }
                g3.items[g3.items.length - 1].raw = r3.trimRight(), g3.items[g3.items.length - 1].text = d2.trimRight(), g3.raw = g3.raw.trimRight();
                const b3 = g3.items.length;
                for (s3 = 0; s3 < b3; s3++) if (this.lexer.state.top = false, g3.items[s3].tokens = this.lexer.blockTokens(g3.items[s3].text, []), !g3.loose) {
                  const e4 = g3.items[s3].tokens.filter(((e5) => "space" === e5.type)), t4 = e4.length > 0 && e4.some(((e5) => /\n.*\n/.test(e5.raw)));
                  g3.loose = t4;
                }
                if (g3.loose) for (s3 = 0; s3 < b3; s3++) g3.items[s3].loose = true;
                return g3;
              }
            }
            html(e3) {
              const t3 = this.rules.block.html.exec(e3);
              if (t3) {
                const e4 = { type: "html", raw: t3[0], pre: !this.options.sanitizer && ("pre" === t3[1] || "script" === t3[1] || "style" === t3[1]), text: t3[0] };
                if (this.options.sanitize) {
                  const r3 = this.options.sanitizer ? this.options.sanitizer(t3[0]) : tr(t3[0]);
                  e4.type = "paragraph", e4.text = r3, e4.tokens = this.lexer.inline(r3);
                }
                return e4;
              }
            }
            def(e3) {
              const t3 = this.rules.block.def.exec(e3);
              if (t3) {
                const e4 = t3[1].toLowerCase().replace(/\s+/g, " "), r3 = t3[2] ? t3[2].replace(/^<(.*)>$/, "$1").replace(this.rules.inline._escapes, "$1") : "", n2 = t3[3] ? t3[3].substring(1, t3[3].length - 1).replace(this.rules.inline._escapes, "$1") : t3[3];
                return { type: "def", tag: e4, raw: t3[0], href: r3, title: n2 };
              }
            }
            table(e3) {
              const t3 = this.rules.block.table.exec(e3);
              if (t3) {
                const e4 = { type: "table", header: mr(t3[1]).map(((e5) => ({ text: e5 }))), align: t3[2].replace(/^ *|\| *$/g, "").split(/ *\| */), rows: t3[3] && t3[3].trim() ? t3[3].replace(/\n[ \t]*$/, "").split("\n") : [] };
                if (e4.header.length === e4.align.length) {
                  e4.raw = t3[0];
                  let r3, n2, o3, i3, s3 = e4.align.length;
                  for (r3 = 0; r3 < s3; r3++) /^ *-+: *$/.test(e4.align[r3]) ? e4.align[r3] = "right" : /^ *:-+: *$/.test(e4.align[r3]) ? e4.align[r3] = "center" : /^ *:-+ *$/.test(e4.align[r3]) ? e4.align[r3] = "left" : e4.align[r3] = null;
                  for (s3 = e4.rows.length, r3 = 0; r3 < s3; r3++) e4.rows[r3] = mr(e4.rows[r3], e4.header.length).map(((e5) => ({ text: e5 })));
                  for (s3 = e4.header.length, n2 = 0; n2 < s3; n2++) e4.header[n2].tokens = this.lexer.inline(e4.header[n2].text);
                  for (s3 = e4.rows.length, n2 = 0; n2 < s3; n2++) for (i3 = e4.rows[n2], o3 = 0; o3 < i3.length; o3++) i3[o3].tokens = this.lexer.inline(i3[o3].text);
                  return e4;
                }
              }
            }
            lheading(e3) {
              const t3 = this.rules.block.lheading.exec(e3);
              if (t3) return { type: "heading", raw: t3[0], depth: "=" === t3[2].charAt(0) ? 1 : 2, text: t3[1], tokens: this.lexer.inline(t3[1]) };
            }
            paragraph(e3) {
              const t3 = this.rules.block.paragraph.exec(e3);
              if (t3) {
                const e4 = "\n" === t3[1].charAt(t3[1].length - 1) ? t3[1].slice(0, -1) : t3[1];
                return { type: "paragraph", raw: t3[0], text: e4, tokens: this.lexer.inline(e4) };
              }
            }
            text(e3) {
              const t3 = this.rules.block.text.exec(e3);
              if (t3) return { type: "text", raw: t3[0], text: t3[0], tokens: this.lexer.inline(t3[0]) };
            }
            escape(e3) {
              const t3 = this.rules.inline.escape.exec(e3);
              if (t3) return { type: "escape", raw: t3[0], text: tr(t3[1]) };
            }
            tag(e3) {
              const t3 = this.rules.inline.tag.exec(e3);
              if (t3) return !this.lexer.state.inLink && /^<a /i.test(t3[0]) ? this.lexer.state.inLink = true : this.lexer.state.inLink && /^<\/a>/i.test(t3[0]) && (this.lexer.state.inLink = false), !this.lexer.state.inRawBlock && /^<(pre|code|kbd|script)(\s|>)/i.test(t3[0]) ? this.lexer.state.inRawBlock = true : this.lexer.state.inRawBlock && /^<\/(pre|code|kbd|script)(\s|>)/i.test(t3[0]) && (this.lexer.state.inRawBlock = false), { type: this.options.sanitize ? "text" : "html", raw: t3[0], inLink: this.lexer.state.inLink, inRawBlock: this.lexer.state.inRawBlock, text: this.options.sanitize ? this.options.sanitizer ? this.options.sanitizer(t3[0]) : tr(t3[0]) : t3[0] };
            }
            link(e3) {
              const t3 = this.rules.inline.link.exec(e3);
              if (t3) {
                const e4 = t3[2].trim();
                if (!this.options.pedantic && /^</.test(e4)) {
                  if (!/>$/.test(e4)) return;
                  const t4 = fr(e4.slice(0, -1), "\\");
                  if ((e4.length - t4.length) % 2 == 0) return;
                } else {
                  const e5 = (function(e6, t4) {
                    if (-1 === e6.indexOf(t4[1])) return -1;
                    const r4 = e6.length;
                    let n3 = 0, o3 = 0;
                    for (; o3 < r4; o3++) if ("\\" === e6[o3]) o3++;
                    else if (e6[o3] === t4[0]) n3++;
                    else if (e6[o3] === t4[1] && (n3--, n3 < 0)) return o3;
                    return -1;
                  })(t3[2], "()");
                  if (e5 > -1) {
                    const r4 = (0 === t3[0].indexOf("!") ? 5 : 4) + t3[1].length + e5;
                    t3[2] = t3[2].substring(0, e5), t3[0] = t3[0].substring(0, r4).trim(), t3[3] = "";
                  }
                }
                let r3 = t3[2], n2 = "";
                if (this.options.pedantic) {
                  const e5 = /^([^'"]*[^\s])\s+(['"])(.*)\2/.exec(r3);
                  e5 && (r3 = e5[1], n2 = e5[3]);
                } else n2 = t3[3] ? t3[3].slice(1, -1) : "";
                return r3 = r3.trim(), /^</.test(r3) && (r3 = this.options.pedantic && !/>$/.test(e4) ? r3.slice(1) : r3.slice(1, -1)), yr(t3, { href: r3 ? r3.replace(this.rules.inline._escapes, "$1") : r3, title: n2 ? n2.replace(this.rules.inline._escapes, "$1") : n2 }, t3[0], this.lexer);
              }
            }
            reflink(e3, t3) {
              let r3;
              if ((r3 = this.rules.inline.reflink.exec(e3)) || (r3 = this.rules.inline.nolink.exec(e3))) {
                let e4 = (r3[2] || r3[1]).replace(/\s+/g, " ");
                if (e4 = t3[e4.toLowerCase()], !e4) {
                  const e5 = r3[0].charAt(0);
                  return { type: "text", raw: e5, text: e5 };
                }
                return yr(r3, e4, r3[0], this.lexer);
              }
            }
            emStrong(e3, t3, r3 = "") {
              let n2 = this.rules.inline.emStrong.lDelim.exec(e3);
              if (!n2) return;
              if (n2[3] && r3.match(/[\p{L}\p{N}]/u)) return;
              const o3 = n2[1] || n2[2] || "";
              if (!o3 || o3 && ("" === r3 || this.rules.inline.punctuation.exec(r3))) {
                const r4 = n2[0].length - 1;
                let o4, i3, s3 = r4, a3 = 0;
                const l3 = "*" === n2[0][0] ? this.rules.inline.emStrong.rDelimAst : this.rules.inline.emStrong.rDelimUnd;
                for (l3.lastIndex = 0, t3 = t3.slice(-1 * e3.length + r4); null != (n2 = l3.exec(t3)); ) {
                  if (o4 = n2[1] || n2[2] || n2[3] || n2[4] || n2[5] || n2[6], !o4) continue;
                  if (i3 = o4.length, n2[3] || n2[4]) {
                    s3 += i3;
                    continue;
                  }
                  if ((n2[5] || n2[6]) && r4 % 3 && !((r4 + i3) % 3)) {
                    a3 += i3;
                    continue;
                  }
                  if (s3 -= i3, s3 > 0) continue;
                  i3 = Math.min(i3, i3 + s3 + a3);
                  const t4 = e3.slice(0, r4 + n2.index + (n2[0].length - o4.length) + i3);
                  if (Math.min(r4, i3) % 2) {
                    const e4 = t4.slice(1, -1);
                    return { type: "em", raw: t4, text: e4, tokens: this.lexer.inlineTokens(e4) };
                  }
                  const l4 = t4.slice(2, -2);
                  return { type: "strong", raw: t4, text: l4, tokens: this.lexer.inlineTokens(l4) };
                }
              }
            }
            codespan(e3) {
              const t3 = this.rules.inline.code.exec(e3);
              if (t3) {
                let e4 = t3[2].replace(/\n/g, " ");
                const r3 = /[^ ]/.test(e4), n2 = /^ /.test(e4) && / $/.test(e4);
                return r3 && n2 && (e4 = e4.substring(1, e4.length - 1)), e4 = tr(e4, true), { type: "codespan", raw: t3[0], text: e4 };
              }
            }
            br(e3) {
              const t3 = this.rules.inline.br.exec(e3);
              if (t3) return { type: "br", raw: t3[0] };
            }
            del(e3) {
              const t3 = this.rules.inline.del.exec(e3);
              if (t3) return { type: "del", raw: t3[0], text: t3[2], tokens: this.lexer.inlineTokens(t3[2]) };
            }
            autolink(e3, t3) {
              const r3 = this.rules.inline.autolink.exec(e3);
              if (r3) {
                let e4, n2;
                return "@" === r3[2] ? (e4 = tr(this.options.mangle ? t3(r3[1]) : r3[1]), n2 = "mailto:" + e4) : (e4 = tr(r3[1]), n2 = e4), { type: "link", raw: r3[0], text: e4, href: n2, tokens: [{ type: "text", raw: e4, text: e4 }] };
              }
            }
            url(e3, t3) {
              let r3;
              if (r3 = this.rules.inline.url.exec(e3)) {
                let e4, n2;
                if ("@" === r3[2]) e4 = tr(this.options.mangle ? t3(r3[0]) : r3[0]), n2 = "mailto:" + e4;
                else {
                  let t4;
                  do {
                    t4 = r3[0], r3[0] = this.rules.inline._backpedal.exec(r3[0])[0];
                  } while (t4 !== r3[0]);
                  e4 = tr(r3[0]), n2 = "www." === r3[1] ? "http://" + r3[0] : r3[0];
                }
                return { type: "link", raw: r3[0], text: e4, href: n2, tokens: [{ type: "text", raw: e4, text: e4 }] };
              }
            }
            inlineText(e3, t3) {
              const r3 = this.rules.inline.text.exec(e3);
              if (r3) {
                let e4;
                return e4 = this.lexer.state.inRawBlock ? this.options.sanitize ? this.options.sanitizer ? this.options.sanitizer(r3[0]) : tr(r3[0]) : r3[0] : tr(this.options.smartypants ? t3(r3[0]) : r3[0]), { type: "text", raw: r3[0], text: e4 };
              }
            }
          }
          const xr = { newline: /^(?: *(?:\n|$))+/, code: /^( {4}[^\n]+(?:\n(?: *(?:\n|$))*)?)+/, fences: /^ {0,3}(`{3,}(?=[^`\n]*(?:\n|$))|~{3,})([^\n]*)(?:\n|$)(?:|([\s\S]*?)(?:\n|$))(?: {0,3}\1[~`]* *(?=\n|$)|$)/, hr: /^ {0,3}((?:-[\t ]*){3,}|(?:_[ \t]*){3,}|(?:\*[ \t]*){3,})(?:\n+|$)/, heading: /^ {0,3}(#{1,6})(?=\s|$)(.*)(?:\n+|$)/, blockquote: /^( {0,3}> ?(paragraph|[^\n]*)(?:\n|$))+/, list: /^( {0,3}bull)([ \t][^\n]+?)?(?:\n|$)/, html: "^ {0,3}(?:<(script|pre|style|textarea)[\\s>][\\s\\S]*?(?:</\\1>[^\\n]*\\n+|$)|comment[^\\n]*(\\n+|$)|<\\?[\\s\\S]*?(?:\\?>\\n*|$)|<![A-Z][\\s\\S]*?(?:>\\n*|$)|<!\\[CDATA\\[[\\s\\S]*?(?:\\]\\]>\\n*|$)|</?(tag)(?: +|\\n|/?>)[\\s\\S]*?(?:(?:\\n *)+\\n|$)|<(?!script|pre|style|textarea)([a-z][\\w-]*)(?:attribute)*? */?>(?=[ \\t]*(?:\\n|$))[\\s\\S]*?(?:(?:\\n *)+\\n|$)|</(?!script|pre|style|textarea)[a-z][\\w-]*\\s*>(?=[ \\t]*(?:\\n|$))[\\s\\S]*?(?:(?:\\n *)+\\n|$))", def: /^ {0,3}\[(label)\]: *(?:\n *)?([^<\s][^\s]*|<.*?>)(?:(?: +(?:\n *)?| *\n *)(title))? *(?:\n+|$)/, table: hr, lheading: /^((?:.|\n(?!\n))+?)\n {0,3}(=+|-+) *(?:\n+|$)/, _paragraph: /^([^\n]+(?:\n(?!hr|heading|lheading|blockquote|fences|list|html|table| +\n)[^\n]+)*)/, text: /^[^\n]+/, _label: /(?!\s*\])(?:\\.|[^\[\]\\])+/, _title: /(?:"(?:\\"?|[^"\\])*"|'[^'\n]*(?:\n[^'\n]+)*\n?'|\([^()]*\))/ };
          xr.def = ir(xr.def).replace("label", xr._label).replace("title", xr._title).getRegex(), xr.bullet = /(?:[*+-]|\d{1,9}[.)])/, xr.listItemStart = ir(/^( *)(bull) */).replace("bull", xr.bullet).getRegex(), xr.list = ir(xr.list).replace(/bull/g, xr.bullet).replace("hr", "\\n+(?=\\1?(?:(?:- *){3,}|(?:_ *){3,}|(?:\\* *){3,})(?:\\n+|$))").replace("def", "\\n+(?=" + xr.def.source + ")").getRegex(), xr._tag = "address|article|aside|base|basefont|blockquote|body|caption|center|col|colgroup|dd|details|dialog|dir|div|dl|dt|fieldset|figcaption|figure|footer|form|frame|frameset|h[1-6]|head|header|hr|html|iframe|legend|li|link|main|menu|menuitem|meta|nav|noframes|ol|optgroup|option|p|param|section|source|summary|table|tbody|td|tfoot|th|thead|title|tr|track|ul", xr._comment = /<!--(?!-?>)[\s\S]*?(?:-->|$)/, xr.html = ir(xr.html, "i").replace("comment", xr._comment).replace("tag", xr._tag).replace("attribute", / +[a-zA-Z:_][\w.:-]*(?: *= *"[^"\n]*"| *= *'[^'\n]*'| *= *[^\s"'=<>`]+)?/).getRegex(), xr.paragraph = ir(xr._paragraph).replace("hr", xr.hr).replace("heading", " {0,3}#{1,6} ").replace("|lheading", "").replace("|table", "").replace("blockquote", " {0,3}>").replace("fences", " {0,3}(?:`{3,}(?=[^`\\n]*\\n)|~{3,})[^\\n]*\\n").replace("list", " {0,3}(?:[*+-]|1[.)]) ").replace("html", "</?(?:tag)(?: +|\\n|/?>)|<(?:script|pre|style|textarea|!--)").replace("tag", xr._tag).getRegex(), xr.blockquote = ir(xr.blockquote).replace("paragraph", xr.paragraph).getRegex(), xr.normal = { ...xr }, xr.gfm = { ...xr.normal, table: "^ *([^\\n ].*\\|.*)\\n {0,3}(?:\\| *)?(:?-+:? *(?:\\| *:?-+:? *)*)(?:\\| *)?(?:\\n((?:(?! *\\n|hr|heading|blockquote|code|fences|list|html).*(?:\\n|$))*)\\n*|$)" }, xr.gfm.table = ir(xr.gfm.table).replace("hr", xr.hr).replace("heading", " {0,3}#{1,6} ").replace("blockquote", " {0,3}>").replace("code", " {4}[^\\n]").replace("fences", " {0,3}(?:`{3,}(?=[^`\\n]*\\n)|~{3,})[^\\n]*\\n").replace("list", " {0,3}(?:[*+-]|1[.)]) ").replace("html", "</?(?:tag)(?: +|\\n|/?>)|<(?:script|pre|style|textarea|!--)").replace("tag", xr._tag).getRegex(), xr.gfm.paragraph = ir(xr._paragraph).replace("hr", xr.hr).replace("heading", " {0,3}#{1,6} ").replace("|lheading", "").replace("table", xr.gfm.table).replace("blockquote", " {0,3}>").replace("fences", " {0,3}(?:`{3,}(?=[^`\\n]*\\n)|~{3,})[^\\n]*\\n").replace("list", " {0,3}(?:[*+-]|1[.)]) ").replace("html", "</?(?:tag)(?: +|\\n|/?>)|<(?:script|pre|style|textarea|!--)").replace("tag", xr._tag).getRegex(), xr.pedantic = { ...xr.normal, html: ir(`^ *(?:comment *(?:\\n|\\s*$)|<(tag)[\\s\\S]+?</\\1> *(?:\\n{2,}|\\s*$)|<tag(?:"[^"]*"|'[^']*'|\\s[^'"/>\\s]*)*?/?> *(?:\\n{2,}|\\s*$))`).replace("comment", xr._comment).replace(/tag/g, "(?!(?:a|em|strong|small|s|cite|q|dfn|abbr|data|time|code|var|samp|kbd|sub|sup|i|b|u|mark|ruby|rt|rp|bdi|bdo|span|br|wbr|ins|del|img)\\b)\\w+(?!:|[^\\w\\s@]*@)\\b").getRegex(), def: /^ *\[([^\]]+)\]: *<?([^\s>]+)>?(?: +(["(][^\n]+[")]))? *(?:\n+|$)/, heading: /^(#{1,6})(.*)(?:\n+|$)/, fences: hr, lheading: /^(.+?)\n {0,3}(=+|-+) *(?:\n+|$)/, paragraph: ir(xr.normal._paragraph).replace("hr", xr.hr).replace("heading", " *#{1,6} *[^\n]").replace("lheading", xr.lheading).replace("blockquote", " {0,3}>").replace("|fences", "").replace("|list", "").replace("|html", "").getRegex() };
          const vr = { escape: /^\\([!"#$%&'()*+,\-./:;<=>?@\[\]\\^_`{|}~])/, autolink: /^<(scheme:[^\s\x00-\x1f<>]*|email)>/, url: hr, tag: "^comment|^</[a-zA-Z][\\w:-]*\\s*>|^<[a-zA-Z][\\w-]*(?:attribute)*?\\s*/?>|^<\\?[\\s\\S]*?\\?>|^<![a-zA-Z]+\\s[\\s\\S]*?>|^<!\\[CDATA\\[[\\s\\S]*?\\]\\]>", link: /^!?\[(label)\]\(\s*(href)(?:\s+(title))?\s*\)/, reflink: /^!?\[(label)\]\[(ref)\]/, nolink: /^!?\[(ref)\](?:\[\])?/, reflinkSearch: "reflink|nolink(?!\\()", emStrong: { lDelim: /^(?:\*+(?:([punct_])|[^\s*]))|^_+(?:([punct*])|([^\s_]))/, rDelimAst: /^(?:[^_*\\]|\\.)*?\_\_(?:[^_*\\]|\\.)*?\*(?:[^_*\\]|\\.)*?(?=\_\_)|(?:[^*\\]|\\.)+(?=[^*])|[punct_](\*+)(?=[\s]|$)|(?:[^punct*_\s\\]|\\.)(\*+)(?=[punct_\s]|$)|[punct_\s](\*+)(?=[^punct*_\s])|[\s](\*+)(?=[punct_])|[punct_](\*+)(?=[punct_])|(?:[^punct*_\s\\]|\\.)(\*+)(?=[^punct*_\s])/, rDelimUnd: /^(?:[^_*\\]|\\.)*?\*\*(?:[^_*\\]|\\.)*?\_(?:[^_*\\]|\\.)*?(?=\*\*)|(?:[^_\\]|\\.)+(?=[^_])|[punct*](\_+)(?=[\s]|$)|(?:[^punct*_\s\\]|\\.)(\_+)(?=[punct*\s]|$)|[punct*\s](\_+)(?=[^punct*_\s])|[\s](\_+)(?=[punct*])|[punct*](\_+)(?=[punct*])/ }, code: /^(`+)([^`]|[^`][\s\S]*?[^`])\1(?!`)/, br: /^( {2,}|\\)\n(?!\s*$)/, del: hr, text: /^(`+|[^`])(?:(?= {2,}\n)|[\s\S]*?(?:(?=[\\<!\[`*_]|\b_|$)|[^ ](?= {2,}\n)))/, punctuation: /^([\spunctuation])/ };
          function wr(e3) {
            return e3.replace(/---/g, "—").replace(/--/g, "–").replace(/(^|[-\u2014/(\[{"\s])'/g, "$1‘").replace(/'/g, "’").replace(/(^|[-\u2014/(\[{\u2018\s])"/g, "$1“").replace(/"/g, "”").replace(/\.{3}/g, "…");
          }
          function kr(e3) {
            let t3, r3, n2 = "";
            const o3 = e3.length;
            for (t3 = 0; t3 < o3; t3++) r3 = e3.charCodeAt(t3), Math.random() > 0.5 && (r3 = "x" + r3.toString(16)), n2 += "&#" + r3 + ";";
            return n2;
          }
          vr._punctuation = "!\"#$%&'()+\\-.,/:;<=>?@\\[\\]`^{|}~", vr.punctuation = ir(vr.punctuation).replace(/punctuation/g, vr._punctuation).getRegex(), vr.blockSkip = /\[[^\]]*?\]\([^\)]*?\)|`[^`]*?`|<[^>]*?>/g, vr.escapedEmSt = /(?:^|[^\\])(?:\\\\)*\\[*_]/g, vr._comment = ir(xr._comment).replace("(?:-->|$)", "-->").getRegex(), vr.emStrong.lDelim = ir(vr.emStrong.lDelim).replace(/punct/g, vr._punctuation).getRegex(), vr.emStrong.rDelimAst = ir(vr.emStrong.rDelimAst, "g").replace(/punct/g, vr._punctuation).getRegex(), vr.emStrong.rDelimUnd = ir(vr.emStrong.rDelimUnd, "g").replace(/punct/g, vr._punctuation).getRegex(), vr._escapes = /\\([!"#$%&'()*+,\-./:;<=>?@\[\]\\^_`{|}~])/g, vr._scheme = /[a-zA-Z][a-zA-Z0-9+.-]{1,31}/, vr._email = /[a-zA-Z0-9.!#$%&'*+/=?^_`{|}~-]+(@)[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?(?:\.[a-zA-Z0-9](?:[a-zA-Z0-9-]{0,61}[a-zA-Z0-9])?)+(?![-_])/, vr.autolink = ir(vr.autolink).replace("scheme", vr._scheme).replace("email", vr._email).getRegex(), vr._attribute = /\s+[a-zA-Z:_][\w.:-]*(?:\s*=\s*"[^"]*"|\s*=\s*'[^']*'|\s*=\s*[^\s"'=<>`]+)?/, vr.tag = ir(vr.tag).replace("comment", vr._comment).replace("attribute", vr._attribute).getRegex(), vr._label = /(?:\[(?:\\.|[^\[\]\\])*\]|\\.|`[^`]*`|[^\[\]\\`])*?/, vr._href = /<(?:\\.|[^\n<>\\])+>|[^\s\x00-\x1f]*/, vr._title = /"(?:\\"?|[^"\\])*"|'(?:\\'?|[^'\\])*'|\((?:\\\)?|[^)\\])*\)/, vr.link = ir(vr.link).replace("label", vr._label).replace("href", vr._href).replace("title", vr._title).getRegex(), vr.reflink = ir(vr.reflink).replace("label", vr._label).replace("ref", xr._label).getRegex(), vr.nolink = ir(vr.nolink).replace("ref", xr._label).getRegex(), vr.reflinkSearch = ir(vr.reflinkSearch, "g").replace("reflink", vr.reflink).replace("nolink", vr.nolink).getRegex(), vr.normal = { ...vr }, vr.pedantic = { ...vr.normal, strong: { start: /^__|\*\*/, middle: /^__(?=\S)([\s\S]*?\S)__(?!_)|^\*\*(?=\S)([\s\S]*?\S)\*\*(?!\*)/, endAst: /\*\*(?!\*)/g, endUnd: /__(?!_)/g }, em: { start: /^_|\*/, middle: /^()\*(?=\S)([\s\S]*?\S)\*(?!\*)|^_(?=\S)([\s\S]*?\S)_(?!_)/, endAst: /\*(?!\*)/g, endUnd: /_(?!_)/g }, link: ir(/^!?\[(label)\]\((.*?)\)/).replace("label", vr._label).getRegex(), reflink: ir(/^!?\[(label)\]\s*\[([^\]]*)\]/).replace("label", vr._label).getRegex() }, vr.gfm = { ...vr.normal, escape: ir(vr.escape).replace("])", "~|])").getRegex(), _extended_email: /[A-Za-z0-9._+-]+(@)[a-zA-Z0-9-_]+(?:\.[a-zA-Z0-9-_]*[a-zA-Z0-9])+(?![-_])/, url: /^((?:ftp|https?):\/\/|www\.)(?:[a-zA-Z0-9\-]+\.?)+[^\s<]*|^email/, _backpedal: /(?:[^?!.,:;*_'"~()&]+|\([^)]*\)|&(?![a-zA-Z0-9]+;$)|[?!.,:;*_'"~)]+(?!$))+/, del: /^(~~?)(?=[^\s~])([\s\S]*?[^\s~])\1(?=[^~]|$)/, text: /^([`~]+|[^`~])(?:(?= {2,}\n)|(?=[a-zA-Z0-9.!#$%&'*+\/=?_`{\|}~-]+@)|[\s\S]*?(?:(?=[\\<!\[`*~_]|\b_|https?:\/\/|ftp:\/\/|www\.|$)|[^ ](?= {2,}\n)|[^a-zA-Z0-9.!#$%&'*+\/=?_`{\|}~-](?=[a-zA-Z0-9.!#$%&'*+\/=?_`{\|}~-]+@)))/ }, vr.gfm.url = ir(vr.gfm.url, "i").replace("email", vr.gfm._extended_email).getRegex(), vr.breaks = { ...vr.gfm, br: ir(vr.br).replace("{2,}", "*").getRegex(), text: ir(vr.gfm.text).replace("\\b_", "\\b_| {2,}\\n").replace(/\{2,\}/g, "*").getRegex() };
          class Er {
            constructor(e3) {
              this.tokens = [], this.tokens.links = /* @__PURE__ */ Object.create(null), this.options = e3 || Jt, this.options.tokenizer = this.options.tokenizer || new br(), this.tokenizer = this.options.tokenizer, this.tokenizer.options = this.options, this.tokenizer.lexer = this, this.inlineQueue = [], this.state = { inLink: false, inRawBlock: false, top: true };
              const t3 = { block: xr.normal, inline: vr.normal };
              this.options.pedantic ? (t3.block = xr.pedantic, t3.inline = vr.pedantic) : this.options.gfm && (t3.block = xr.gfm, this.options.breaks ? t3.inline = vr.breaks : t3.inline = vr.gfm), this.tokenizer.rules = t3;
            }
            static get rules() {
              return { block: xr, inline: vr };
            }
            static lex(e3, t3) {
              return new Er(t3).lex(e3);
            }
            static lexInline(e3, t3) {
              return new Er(t3).inlineTokens(e3);
            }
            lex(e3) {
              let t3;
              for (e3 = e3.replace(/\r\n|\r/g, "\n"), this.blockTokens(e3, this.tokens); t3 = this.inlineQueue.shift(); ) this.inlineTokens(t3.src, t3.tokens);
              return this.tokens;
            }
            blockTokens(e3, t3 = []) {
              let r3, n2, o3, i3;
              for (e3 = this.options.pedantic ? e3.replace(/\t/g, "    ").replace(/^ +$/gm, "") : e3.replace(/^( *)(\t+)/gm, ((e4, t4, r4) => t4 + "    ".repeat(r4.length))); e3; ) if (!(this.options.extensions && this.options.extensions.block && this.options.extensions.block.some(((n3) => !!(r3 = n3.call({ lexer: this }, e3, t3)) && (e3 = e3.substring(r3.raw.length), t3.push(r3), true))))) if (r3 = this.tokenizer.space(e3)) e3 = e3.substring(r3.raw.length), 1 === r3.raw.length && t3.length > 0 ? t3[t3.length - 1].raw += "\n" : t3.push(r3);
              else if (r3 = this.tokenizer.code(e3)) e3 = e3.substring(r3.raw.length), n2 = t3[t3.length - 1], !n2 || "paragraph" !== n2.type && "text" !== n2.type ? t3.push(r3) : (n2.raw += "\n" + r3.raw, n2.text += "\n" + r3.text, this.inlineQueue[this.inlineQueue.length - 1].src = n2.text);
              else if (r3 = this.tokenizer.fences(e3)) e3 = e3.substring(r3.raw.length), t3.push(r3);
              else if (r3 = this.tokenizer.heading(e3)) e3 = e3.substring(r3.raw.length), t3.push(r3);
              else if (r3 = this.tokenizer.hr(e3)) e3 = e3.substring(r3.raw.length), t3.push(r3);
              else if (r3 = this.tokenizer.blockquote(e3)) e3 = e3.substring(r3.raw.length), t3.push(r3);
              else if (r3 = this.tokenizer.list(e3)) e3 = e3.substring(r3.raw.length), t3.push(r3);
              else if (r3 = this.tokenizer.html(e3)) e3 = e3.substring(r3.raw.length), t3.push(r3);
              else if (r3 = this.tokenizer.def(e3)) e3 = e3.substring(r3.raw.length), n2 = t3[t3.length - 1], !n2 || "paragraph" !== n2.type && "text" !== n2.type ? this.tokens.links[r3.tag] || (this.tokens.links[r3.tag] = { href: r3.href, title: r3.title }) : (n2.raw += "\n" + r3.raw, n2.text += "\n" + r3.raw, this.inlineQueue[this.inlineQueue.length - 1].src = n2.text);
              else if (r3 = this.tokenizer.table(e3)) e3 = e3.substring(r3.raw.length), t3.push(r3);
              else if (r3 = this.tokenizer.lheading(e3)) e3 = e3.substring(r3.raw.length), t3.push(r3);
              else {
                if (o3 = e3, this.options.extensions && this.options.extensions.startBlock) {
                  let t4 = 1 / 0;
                  const r4 = e3.slice(1);
                  let n3;
                  this.options.extensions.startBlock.forEach((function(e4) {
                    n3 = e4.call({ lexer: this }, r4), "number" == typeof n3 && n3 >= 0 && (t4 = Math.min(t4, n3));
                  })), t4 < 1 / 0 && t4 >= 0 && (o3 = e3.substring(0, t4 + 1));
                }
                if (this.state.top && (r3 = this.tokenizer.paragraph(o3))) n2 = t3[t3.length - 1], i3 && "paragraph" === n2.type ? (n2.raw += "\n" + r3.raw, n2.text += "\n" + r3.text, this.inlineQueue.pop(), this.inlineQueue[this.inlineQueue.length - 1].src = n2.text) : t3.push(r3), i3 = o3.length !== e3.length, e3 = e3.substring(r3.raw.length);
                else if (r3 = this.tokenizer.text(e3)) e3 = e3.substring(r3.raw.length), n2 = t3[t3.length - 1], n2 && "text" === n2.type ? (n2.raw += "\n" + r3.raw, n2.text += "\n" + r3.text, this.inlineQueue.pop(), this.inlineQueue[this.inlineQueue.length - 1].src = n2.text) : t3.push(r3);
                else if (e3) {
                  const t4 = "Infinite loop on byte: " + e3.charCodeAt(0);
                  if (this.options.silent) {
                    console.error(t4);
                    break;
                  }
                  throw new Error(t4);
                }
              }
              return this.state.top = true, t3;
            }
            inline(e3, t3 = []) {
              return this.inlineQueue.push({ src: e3, tokens: t3 }), t3;
            }
            inlineTokens(e3, t3 = []) {
              let r3, n2, o3, i3, s3, a3, l3 = e3;
              if (this.tokens.links) {
                const e4 = Object.keys(this.tokens.links);
                if (e4.length > 0) for (; null != (i3 = this.tokenizer.rules.inline.reflinkSearch.exec(l3)); ) e4.includes(i3[0].slice(i3[0].lastIndexOf("[") + 1, -1)) && (l3 = l3.slice(0, i3.index) + "[" + gr("a", i3[0].length - 2) + "]" + l3.slice(this.tokenizer.rules.inline.reflinkSearch.lastIndex));
              }
              for (; null != (i3 = this.tokenizer.rules.inline.blockSkip.exec(l3)); ) l3 = l3.slice(0, i3.index) + "[" + gr("a", i3[0].length - 2) + "]" + l3.slice(this.tokenizer.rules.inline.blockSkip.lastIndex);
              for (; null != (i3 = this.tokenizer.rules.inline.escapedEmSt.exec(l3)); ) l3 = l3.slice(0, i3.index + i3[0].length - 2) + "++" + l3.slice(this.tokenizer.rules.inline.escapedEmSt.lastIndex), this.tokenizer.rules.inline.escapedEmSt.lastIndex--;
              for (; e3; ) if (s3 || (a3 = ""), s3 = false, !(this.options.extensions && this.options.extensions.inline && this.options.extensions.inline.some(((n3) => !!(r3 = n3.call({ lexer: this }, e3, t3)) && (e3 = e3.substring(r3.raw.length), t3.push(r3), true))))) if (r3 = this.tokenizer.escape(e3)) e3 = e3.substring(r3.raw.length), t3.push(r3);
              else if (r3 = this.tokenizer.tag(e3)) e3 = e3.substring(r3.raw.length), n2 = t3[t3.length - 1], n2 && "text" === r3.type && "text" === n2.type ? (n2.raw += r3.raw, n2.text += r3.text) : t3.push(r3);
              else if (r3 = this.tokenizer.link(e3)) e3 = e3.substring(r3.raw.length), t3.push(r3);
              else if (r3 = this.tokenizer.reflink(e3, this.tokens.links)) e3 = e3.substring(r3.raw.length), n2 = t3[t3.length - 1], n2 && "text" === r3.type && "text" === n2.type ? (n2.raw += r3.raw, n2.text += r3.text) : t3.push(r3);
              else if (r3 = this.tokenizer.emStrong(e3, l3, a3)) e3 = e3.substring(r3.raw.length), t3.push(r3);
              else if (r3 = this.tokenizer.codespan(e3)) e3 = e3.substring(r3.raw.length), t3.push(r3);
              else if (r3 = this.tokenizer.br(e3)) e3 = e3.substring(r3.raw.length), t3.push(r3);
              else if (r3 = this.tokenizer.del(e3)) e3 = e3.substring(r3.raw.length), t3.push(r3);
              else if (r3 = this.tokenizer.autolink(e3, kr)) e3 = e3.substring(r3.raw.length), t3.push(r3);
              else if (this.state.inLink || !(r3 = this.tokenizer.url(e3, kr))) {
                if (o3 = e3, this.options.extensions && this.options.extensions.startInline) {
                  let t4 = 1 / 0;
                  const r4 = e3.slice(1);
                  let n3;
                  this.options.extensions.startInline.forEach((function(e4) {
                    n3 = e4.call({ lexer: this }, r4), "number" == typeof n3 && n3 >= 0 && (t4 = Math.min(t4, n3));
                  })), t4 < 1 / 0 && t4 >= 0 && (o3 = e3.substring(0, t4 + 1));
                }
                if (r3 = this.tokenizer.inlineText(o3, wr)) e3 = e3.substring(r3.raw.length), "_" !== r3.raw.slice(-1) && (a3 = r3.raw.slice(-1)), s3 = true, n2 = t3[t3.length - 1], n2 && "text" === n2.type ? (n2.raw += r3.raw, n2.text += r3.text) : t3.push(r3);
                else if (e3) {
                  const t4 = "Infinite loop on byte: " + e3.charCodeAt(0);
                  if (this.options.silent) {
                    console.error(t4);
                    break;
                  }
                  throw new Error(t4);
                }
              } else e3 = e3.substring(r3.raw.length), t3.push(r3);
              return t3;
            }
          }
          class Or {
            constructor(e3) {
              this.options = e3 || Jt;
            }
            code(e3, t3, r3) {
              const n2 = (t3 || "").match(/\S*/)[0];
              if (this.options.highlight) {
                const t4 = this.options.highlight(e3, n2);
                null != t4 && t4 !== e3 && (r3 = true, e3 = t4);
              }
              return e3 = e3.replace(/\n$/, "") + "\n", n2 ? '<pre><code class="' + this.options.langPrefix + tr(n2) + '">' + (r3 ? e3 : tr(e3, true)) + "</code></pre>\n" : "<pre><code>" + (r3 ? e3 : tr(e3, true)) + "</code></pre>\n";
            }
            blockquote(e3) {
              return `<blockquote>
${e3}</blockquote>
`;
            }
            html(e3) {
              return e3;
            }
            heading(e3, t3, r3, n2) {
              return this.options.headerIds ? `<h${t3} id="${this.options.headerPrefix + n2.slug(r3)}">${e3}</h${t3}>
` : `<h${t3}>${e3}</h${t3}>
`;
            }
            hr() {
              return this.options.xhtml ? "<hr/>\n" : "<hr>\n";
            }
            list(e3, t3, r3) {
              const n2 = t3 ? "ol" : "ul";
              return "<" + n2 + (t3 && 1 !== r3 ? ' start="' + r3 + '"' : "") + ">\n" + e3 + "</" + n2 + ">\n";
            }
            listitem(e3) {
              return `<li>${e3}</li>
`;
            }
            checkbox(e3) {
              return "<input " + (e3 ? 'checked="" ' : "") + 'disabled="" type="checkbox"' + (this.options.xhtml ? " /" : "") + "> ";
            }
            paragraph(e3) {
              return `<p>${e3}</p>
`;
            }
            table(e3, t3) {
              return t3 && (t3 = `<tbody>${t3}</tbody>`), "<table>\n<thead>\n" + e3 + "</thead>\n" + t3 + "</table>\n";
            }
            tablerow(e3) {
              return `<tr>
${e3}</tr>
`;
            }
            tablecell(e3, t3) {
              const r3 = t3.header ? "th" : "td";
              return (t3.align ? `<${r3} align="${t3.align}">` : `<${r3}>`) + e3 + `</${r3}>
`;
            }
            strong(e3) {
              return `<strong>${e3}</strong>`;
            }
            em(e3) {
              return `<em>${e3}</em>`;
            }
            codespan(e3) {
              return `<code>${e3}</code>`;
            }
            br() {
              return this.options.xhtml ? "<br/>" : "<br>";
            }
            del(e3) {
              return `<del>${e3}</del>`;
            }
            link(e3, t3, r3) {
              if (null === (e3 = lr(this.options.sanitize, this.options.baseUrl, e3))) return r3;
              let n2 = '<a href="' + e3 + '"';
              return t3 && (n2 += ' title="' + t3 + '"'), n2 += ">" + r3 + "</a>", n2;
            }
            image(e3, t3, r3) {
              if (null === (e3 = lr(this.options.sanitize, this.options.baseUrl, e3))) return r3;
              let n2 = `<img src="${e3}" alt="${r3}"`;
              return t3 && (n2 += ` title="${t3}"`), n2 += this.options.xhtml ? "/>" : ">", n2;
            }
            text(e3) {
              return e3;
            }
          }
          class jr {
            strong(e3) {
              return e3;
            }
            em(e3) {
              return e3;
            }
            codespan(e3) {
              return e3;
            }
            del(e3) {
              return e3;
            }
            html(e3) {
              return e3;
            }
            text(e3) {
              return e3;
            }
            link(e3, t3, r3) {
              return "" + r3;
            }
            image(e3, t3, r3) {
              return "" + r3;
            }
            br() {
              return "";
            }
          }
          class $r {
            constructor() {
              this.seen = {};
            }
            serialize(e3) {
              return e3.toLowerCase().trim().replace(/<[!\/a-z].*?>/gi, "").replace(/[\u2000-\u206F\u2E00-\u2E7F\\'!"#$%&()*+,./:;<=>?@[\]^`{|}~]/g, "").replace(/\s/g, "-");
            }
            getNextSafeSlug(e3, t3) {
              let r3 = e3, n2 = 0;
              if (this.seen.hasOwnProperty(r3)) {
                n2 = this.seen[e3];
                do {
                  n2++, r3 = e3 + "-" + n2;
                } while (this.seen.hasOwnProperty(r3));
              }
              return t3 || (this.seen[e3] = n2, this.seen[r3] = 0), r3;
            }
            slug(e3, t3 = {}) {
              const r3 = this.serialize(e3);
              return this.getNextSafeSlug(r3, t3.dryrun);
            }
          }
          class Sr {
            constructor(e3) {
              this.options = e3 || Jt, this.options.renderer = this.options.renderer || new Or(), this.renderer = this.options.renderer, this.renderer.options = this.options, this.textRenderer = new jr(), this.slugger = new $r();
            }
            static parse(e3, t3) {
              return new Sr(t3).parse(e3);
            }
            static parseInline(e3, t3) {
              return new Sr(t3).parseInline(e3);
            }
            parse(e3, t3 = true) {
              let r3, n2, o3, i3, s3, a3, l3, c3, p3, u3, d2, h2, m3, f3, g3, y3, b3, x3, v3, w3 = "";
              const k3 = e3.length;
              for (r3 = 0; r3 < k3; r3++) if (u3 = e3[r3], this.options.extensions && this.options.extensions.renderers && this.options.extensions.renderers[u3.type] && (v3 = this.options.extensions.renderers[u3.type].call({ parser: this }, u3), false !== v3 || !["space", "hr", "heading", "code", "table", "blockquote", "list", "html", "paragraph", "text"].includes(u3.type))) w3 += v3 || "";
              else switch (u3.type) {
                case "space":
                  continue;
                case "hr":
                  w3 += this.renderer.hr();
                  continue;
                case "heading":
                  w3 += this.renderer.heading(this.parseInline(u3.tokens), u3.depth, nr(this.parseInline(u3.tokens, this.textRenderer)), this.slugger);
                  continue;
                case "code":
                  w3 += this.renderer.code(u3.text, u3.lang, u3.escaped);
                  continue;
                case "table":
                  for (c3 = "", l3 = "", i3 = u3.header.length, n2 = 0; n2 < i3; n2++) l3 += this.renderer.tablecell(this.parseInline(u3.header[n2].tokens), { header: true, align: u3.align[n2] });
                  for (c3 += this.renderer.tablerow(l3), p3 = "", i3 = u3.rows.length, n2 = 0; n2 < i3; n2++) {
                    for (a3 = u3.rows[n2], l3 = "", s3 = a3.length, o3 = 0; o3 < s3; o3++) l3 += this.renderer.tablecell(this.parseInline(a3[o3].tokens), { header: false, align: u3.align[o3] });
                    p3 += this.renderer.tablerow(l3);
                  }
                  w3 += this.renderer.table(c3, p3);
                  continue;
                case "blockquote":
                  p3 = this.parse(u3.tokens), w3 += this.renderer.blockquote(p3);
                  continue;
                case "list":
                  for (d2 = u3.ordered, h2 = u3.start, m3 = u3.loose, i3 = u3.items.length, p3 = "", n2 = 0; n2 < i3; n2++) g3 = u3.items[n2], y3 = g3.checked, b3 = g3.task, f3 = "", g3.task && (x3 = this.renderer.checkbox(y3), m3 ? g3.tokens.length > 0 && "paragraph" === g3.tokens[0].type ? (g3.tokens[0].text = x3 + " " + g3.tokens[0].text, g3.tokens[0].tokens && g3.tokens[0].tokens.length > 0 && "text" === g3.tokens[0].tokens[0].type && (g3.tokens[0].tokens[0].text = x3 + " " + g3.tokens[0].tokens[0].text)) : g3.tokens.unshift({ type: "text", text: x3 }) : f3 += x3), f3 += this.parse(g3.tokens, m3), p3 += this.renderer.listitem(f3, b3, y3);
                  w3 += this.renderer.list(p3, d2, h2);
                  continue;
                case "html":
                  w3 += this.renderer.html(u3.text);
                  continue;
                case "paragraph":
                  w3 += this.renderer.paragraph(this.parseInline(u3.tokens));
                  continue;
                case "text":
                  for (p3 = u3.tokens ? this.parseInline(u3.tokens) : u3.text; r3 + 1 < k3 && "text" === e3[r3 + 1].type; ) u3 = e3[++r3], p3 += "\n" + (u3.tokens ? this.parseInline(u3.tokens) : u3.text);
                  w3 += t3 ? this.renderer.paragraph(p3) : p3;
                  continue;
                default: {
                  const e4 = 'Token with "' + u3.type + '" type was not found.';
                  if (this.options.silent) return void console.error(e4);
                  throw new Error(e4);
                }
              }
              return w3;
            }
            parseInline(e3, t3) {
              t3 = t3 || this.renderer;
              let r3, n2, o3, i3 = "";
              const s3 = e3.length;
              for (r3 = 0; r3 < s3; r3++) if (n2 = e3[r3], this.options.extensions && this.options.extensions.renderers && this.options.extensions.renderers[n2.type] && (o3 = this.options.extensions.renderers[n2.type].call({ parser: this }, n2), false !== o3 || !["escape", "html", "link", "image", "strong", "em", "codespan", "br", "del", "text"].includes(n2.type))) i3 += o3 || "";
              else switch (n2.type) {
                case "escape":
                case "text":
                  i3 += t3.text(n2.text);
                  break;
                case "html":
                  i3 += t3.html(n2.text);
                  break;
                case "link":
                  i3 += t3.link(n2.href, n2.title, this.parseInline(n2.tokens, t3));
                  break;
                case "image":
                  i3 += t3.image(n2.href, n2.title, n2.text);
                  break;
                case "strong":
                  i3 += t3.strong(this.parseInline(n2.tokens, t3));
                  break;
                case "em":
                  i3 += t3.em(this.parseInline(n2.tokens, t3));
                  break;
                case "codespan":
                  i3 += t3.codespan(n2.text);
                  break;
                case "br":
                  i3 += t3.br();
                  break;
                case "del":
                  i3 += t3.del(this.parseInline(n2.tokens, t3));
                  break;
                default: {
                  const e4 = 'Token with "' + n2.type + '" type was not found.';
                  if (this.options.silent) return void console.error(e4);
                  throw new Error(e4);
                }
              }
              return i3;
            }
          }
          class Cr {
            constructor(e3) {
              this.options = e3 || Jt;
            }
            static passThroughHooks = /* @__PURE__ */ new Set(["preprocess", "postprocess"]);
            preprocess(e3) {
              return e3;
            }
            postprocess(e3) {
              return e3;
            }
          }
          function Pr(e3, t3) {
            return (r3, n2, o3) => {
              "function" == typeof n2 && (o3 = n2, n2 = null);
              const i3 = { ...n2 }, s3 = /* @__PURE__ */ (function(e4, t4, r4) {
                return (n3) => {
                  if (n3.message += "\nPlease report this to https://github.com/markedjs/marked.", e4) {
                    const e5 = "<p>An error occurred:</p><pre>" + tr(n3.message + "", true) + "</pre>";
                    return t4 ? Promise.resolve(e5) : r4 ? void r4(null, e5) : e5;
                  }
                  if (t4) return Promise.reject(n3);
                  if (!r4) throw n3;
                  r4(n3);
                };
              })((n2 = { ...Ir.defaults, ...i3 }).silent, n2.async, o3);
              if (null == r3) return s3(new Error("marked(): input parameter is undefined or null"));
              if ("string" != typeof r3) return s3(new Error("marked(): input parameter is of type " + Object.prototype.toString.call(r3) + ", string expected"));
              if ((function(e4) {
                e4 && e4.sanitize && !e4.silent && console.warn("marked(): sanitize and sanitizer parameters are deprecated since version 0.7.0, should not be used and will be removed in the future. Read more here: https://marked.js.org/#/USING_ADVANCED.md#options");
              })(n2), n2.hooks && (n2.hooks.options = n2), o3) {
                const i4 = n2.highlight;
                let a3;
                try {
                  n2.hooks && (r3 = n2.hooks.preprocess(r3)), a3 = e3(r3, n2);
                } catch (e4) {
                  return s3(e4);
                }
                const l3 = function(e4) {
                  let r4;
                  if (!e4) try {
                    n2.walkTokens && Ir.walkTokens(a3, n2.walkTokens), r4 = t3(a3, n2), n2.hooks && (r4 = n2.hooks.postprocess(r4));
                  } catch (t4) {
                    e4 = t4;
                  }
                  return n2.highlight = i4, e4 ? s3(e4) : o3(null, r4);
                };
                if (!i4 || i4.length < 3) return l3();
                if (delete n2.highlight, !a3.length) return l3();
                let c3 = 0;
                return Ir.walkTokens(a3, (function(e4) {
                  "code" === e4.type && (c3++, setTimeout((() => {
                    i4(e4.text, e4.lang, (function(t4, r4) {
                      if (t4) return l3(t4);
                      null != r4 && r4 !== e4.text && (e4.text = r4, e4.escaped = true), c3--, 0 === c3 && l3();
                    }));
                  }), 0));
                })), void (0 === c3 && l3());
              }
              if (n2.async) return Promise.resolve(n2.hooks ? n2.hooks.preprocess(r3) : r3).then(((t4) => e3(t4, n2))).then(((e4) => n2.walkTokens ? Promise.all(Ir.walkTokens(e4, n2.walkTokens)).then((() => e4)) : e4)).then(((e4) => t3(e4, n2))).then(((e4) => n2.hooks ? n2.hooks.postprocess(e4) : e4)).catch(s3);
              try {
                n2.hooks && (r3 = n2.hooks.preprocess(r3));
                const o4 = e3(r3, n2);
                n2.walkTokens && Ir.walkTokens(o4, n2.walkTokens);
                let i4 = t3(o4, n2);
                return n2.hooks && (i4 = n2.hooks.postprocess(i4)), i4;
              } catch (e4) {
                return s3(e4);
              }
            };
          }
          function Ir(e3, t3, r3) {
            return Pr(Er.lex, Sr.parse)(e3, t3, r3);
          }
          Ir.options = Ir.setOptions = function(e3) {
            var t3;
            return Ir.defaults = { ...Ir.defaults, ...e3 }, t3 = Ir.defaults, Jt = t3, Ir;
          }, Ir.getDefaults = function() {
            return { async: false, baseUrl: null, breaks: false, extensions: null, gfm: true, headerIds: true, headerPrefix: "", highlight: null, hooks: null, langPrefix: "language-", mangle: true, pedantic: false, renderer: null, sanitize: false, sanitizer: null, silent: false, smartypants: false, tokenizer: null, walkTokens: null, xhtml: false };
          }, Ir.defaults = Jt, Ir.use = function(...e3) {
            const t3 = Ir.defaults.extensions || { renderers: {}, childTokens: {} };
            e3.forEach(((e4) => {
              const r3 = { ...e4 };
              if (r3.async = Ir.defaults.async || r3.async || false, e4.extensions && (e4.extensions.forEach(((e5) => {
                if (!e5.name) throw new Error("extension name required");
                if (e5.renderer) {
                  const r4 = t3.renderers[e5.name];
                  t3.renderers[e5.name] = r4 ? function(...t4) {
                    let n2 = e5.renderer.apply(this, t4);
                    return false === n2 && (n2 = r4.apply(this, t4)), n2;
                  } : e5.renderer;
                }
                if (e5.tokenizer) {
                  if (!e5.level || "block" !== e5.level && "inline" !== e5.level) throw new Error("extension level must be 'block' or 'inline'");
                  t3[e5.level] ? t3[e5.level].unshift(e5.tokenizer) : t3[e5.level] = [e5.tokenizer], e5.start && ("block" === e5.level ? t3.startBlock ? t3.startBlock.push(e5.start) : t3.startBlock = [e5.start] : "inline" === e5.level && (t3.startInline ? t3.startInline.push(e5.start) : t3.startInline = [e5.start]));
                }
                e5.childTokens && (t3.childTokens[e5.name] = e5.childTokens);
              })), r3.extensions = t3), e4.renderer) {
                const t4 = Ir.defaults.renderer || new Or();
                for (const r4 in e4.renderer) {
                  const n2 = t4[r4];
                  t4[r4] = (...o3) => {
                    let i3 = e4.renderer[r4].apply(t4, o3);
                    return false === i3 && (i3 = n2.apply(t4, o3)), i3;
                  };
                }
                r3.renderer = t4;
              }
              if (e4.tokenizer) {
                const t4 = Ir.defaults.tokenizer || new br();
                for (const r4 in e4.tokenizer) {
                  const n2 = t4[r4];
                  t4[r4] = (...o3) => {
                    let i3 = e4.tokenizer[r4].apply(t4, o3);
                    return false === i3 && (i3 = n2.apply(t4, o3)), i3;
                  };
                }
                r3.tokenizer = t4;
              }
              if (e4.hooks) {
                const t4 = Ir.defaults.hooks || new Cr();
                for (const r4 in e4.hooks) {
                  const n2 = t4[r4];
                  Cr.passThroughHooks.has(r4) ? t4[r4] = (o3) => {
                    if (Ir.defaults.async) return Promise.resolve(e4.hooks[r4].call(t4, o3)).then(((e5) => n2.call(t4, e5)));
                    const i3 = e4.hooks[r4].call(t4, o3);
                    return n2.call(t4, i3);
                  } : t4[r4] = (...o3) => {
                    let i3 = e4.hooks[r4].apply(t4, o3);
                    return false === i3 && (i3 = n2.apply(t4, o3)), i3;
                  };
                }
                r3.hooks = t4;
              }
              if (e4.walkTokens) {
                const t4 = Ir.defaults.walkTokens;
                r3.walkTokens = function(r4) {
                  let n2 = [];
                  return n2.push(e4.walkTokens.call(this, r4)), t4 && (n2 = n2.concat(t4.call(this, r4))), n2;
                };
              }
              Ir.setOptions(r3);
            }));
          }, Ir.walkTokens = function(e3, t3) {
            let r3 = [];
            for (const n2 of e3) switch (r3 = r3.concat(t3.call(Ir, n2)), n2.type) {
              case "table":
                for (const e4 of n2.header) r3 = r3.concat(Ir.walkTokens(e4.tokens, t3));
                for (const e4 of n2.rows) for (const n3 of e4) r3 = r3.concat(Ir.walkTokens(n3.tokens, t3));
                break;
              case "list":
                r3 = r3.concat(Ir.walkTokens(n2.items, t3));
                break;
              default:
                Ir.defaults.extensions && Ir.defaults.extensions.childTokens && Ir.defaults.extensions.childTokens[n2.type] ? Ir.defaults.extensions.childTokens[n2.type].forEach((function(e4) {
                  r3 = r3.concat(Ir.walkTokens(n2[e4], t3));
                })) : n2.tokens && (r3 = r3.concat(Ir.walkTokens(n2.tokens, t3)));
            }
            return r3;
          }, Ir.parseInline = Pr(Er.lexInline, Sr.parseInline), Ir.Parser = Sr, Ir.parser = Sr.parse, Ir.Renderer = Or, Ir.TextRenderer = jr, Ir.Lexer = Er, Ir.lexer = Er.lex, Ir.Tokenizer = br, Ir.Slugger = $r, Ir.Hooks = Cr, Ir.parse = Ir, Ir.options, Ir.setOptions, Ir.use, Ir.walkTokens, Ir.parseInline, Sr.parse, Er.lex;
          var Ar = Object.defineProperty, Tr = Object.defineProperties, _r = Object.getOwnPropertyDescriptors, Rr = Object.getOwnPropertySymbols, Dr = Object.prototype.hasOwnProperty, zr = Object.prototype.propertyIsEnumerable, Lr = (e3, t3, r3) => t3 in e3 ? Ar(e3, t3, { enumerable: true, configurable: true, writable: true, value: r3 }) : e3[t3] = r3, Nr = (e3, t3) => {
            for (var r3 in t3 || (t3 = {})) Dr.call(t3, r3) && Lr(e3, r3, t3[r3]);
            if (Rr) for (var r3 of Rr(t3)) zr.call(t3, r3) && Lr(e3, r3, t3[r3]);
            return e3;
          };
          const Mr = new Ir.Renderer();
          Ir.setOptions({ renderer: Mr, highlight: (e3, t3) => jt(e3, t3) });
          const qr = "^ {0,3}<!-- ReDoc-Inject:\\s+?<({component}).*?/?>\\s+?-->\\s*$", Fr = "(?:^ {0,3}<({component})([\\s\\S]*?)>([\\s\\S]*?)</\\2>|^ {0,3}<({component})([\\s\\S]*?)(?:/>|\\n{2,}))", Br = "(?:" + qr + "|" + Fr + ")";
          function Qr(e3) {
            return `<!-- ReDoc-Inject: <${e3}> -->`;
          }
          class Ur {
            constructor(e3, t3) {
              this.options = e3, this.parentId = t3, this.headings = [], this.headingRule = (e4, t4, r3, n2) => (1 === t4 ? this.currentTopHeading = this.saveHeading(e4, t4) : 2 === t4 && this.saveHeading(e4, t4, this.currentTopHeading && this.currentTopHeading.items, this.currentTopHeading && this.currentTopHeading.id), this.originalHeadingRule(e4, t4, r3, n2)), this.parentId = t3, this.parser = new Ir.Parser(), this.headingEnhanceRenderer = new Ir.Renderer(), this.originalHeadingRule = this.headingEnhanceRenderer.heading.bind(this.headingEnhanceRenderer), this.headingEnhanceRenderer.heading = this.headingRule;
            }
            static containsComponent(e3, t3) {
              return new RegExp(Br.replace(/{component}/g, t3), "gmi").test(e3);
            }
            static getTextBeforeHading(e3, t3) {
              const r3 = e3.search(new RegExp(`^##?\\s+${t3}`, "m"));
              return r3 > -1 ? e3.substring(0, r3) : e3;
            }
            saveHeading(e3, t3, r3 = this.headings, n2) {
              e3 = P2(e3);
              const o3 = { id: n2 ? `${n2}/${w2(e3)}` : `${this.parentId || "section"}/${w2(e3)}`, name: e3, level: t3, items: [] };
              return r3.push(o3), o3;
            }
            flattenHeadings(e3) {
              if (void 0 === e3) return [];
              const t3 = [];
              for (const r3 of e3) t3.push(r3), t3.push(...this.flattenHeadings(r3.items));
              return t3;
            }
            attachHeadingsDescriptions(e3) {
              const t3 = (e4) => new RegExp(`##?\\s+${e4.name.replace(/[-\/\\^$*+?.()|[\]{}]/g, "\\$&")}s*(
|\r
|$|s*)`), r3 = this.flattenHeadings(this.headings);
              if (r3.length < 1) return;
              let n2 = r3[0], o3 = t3(n2), i3 = e3.search(o3);
              for (let s3 = 1; s3 < r3.length; s3++) {
                const a3 = r3[s3], l3 = t3(a3), c3 = e3.substr(i3 + 1).search(l3) + i3 + 1;
                n2.description = e3.substring(i3, c3).replace(o3, "").trim(), n2 = a3, o3 = l3, i3 = c3;
              }
              n2.description = e3.substring(i3).replace(o3, "").trim();
            }
            renderMd(e3, t3 = false) {
              const r3 = t3 ? { renderer: this.headingEnhanceRenderer } : void 0;
              return Ir(e3.toString(), r3);
            }
            extractHeadings(e3) {
              this.renderMd(e3, true), this.attachHeadingsDescriptions(e3);
              const t3 = this.headings;
              return this.headings = [], t3;
            }
            renderMdWithComponents(e3) {
              const t3 = this.options && this.options.allowedMdComponents;
              if (!t3 || 0 === Object.keys(t3).length) return [this.renderMd(e3)];
              const r3 = Object.keys(t3).join("|"), n2 = new RegExp(Br.replace(/{component}/g, r3), "mig"), o3 = [], i3 = [];
              let s3 = n2.exec(e3), a3 = 0;
              for (; s3; ) {
                o3.push(e3.substring(a3, s3.index)), a3 = n2.lastIndex;
                const r4 = t3[s3[1] || s3[2] || s3[5]], p4 = s3[3] || s3[6], u3 = s3[4];
                r4 && i3.push({ component: r4.component, propsSelector: r4.propsSelector, props: (l3 = Nr(Nr({}, Wr(p4)), r4.props), c3 = { children: u3 }, Tr(l3, _r(c3))) }), s3 = n2.exec(e3);
              }
              var l3, c3;
              o3.push(e3.substring(a3));
              const p3 = [];
              for (let e4 = 0; e4 < o3.length; e4++) {
                const t4 = o3[e4];
                t4 && p3.push(this.renderMd(t4)), i3[e4] && p3.push(i3[e4]);
              }
              return p3;
            }
          }
          function Wr(e3) {
            if (!e3) return {};
            const t3 = /([\w-]+)\s*=\s*(?:{([^}]+?)}|"([^"]+?)")/gim, r3 = {};
            let n2;
            for (; null !== (n2 = t3.exec(e3)); ) if (n2[3]) r3[n2[1]] = n2[3];
            else if (n2[2]) {
              let e4;
              try {
                e4 = JSON.parse(n2[2]);
              } catch (e5) {
              }
              r3[n2[1]] = e4;
            }
            return r3;
          }
          class Vr {
            constructor(e3, t3 = new W2({})) {
              this.parser = e3, this.options = t3, Object.assign(this, e3.spec.info), this.description = e3.spec.info.description || "", this.summary = e3.spec.info.summary || "";
              const r3 = this.description.search(/^\s*##?\s+/m);
              r3 > -1 && (this.description = this.description.substring(0, r3)), this.downloadUrls = this.getDownloadUrls(), this.downloadFileName = this.getDownloadFileName();
            }
            getDownloadUrls() {
              return (this.options.downloadUrls ? this.options.downloadUrls.map((({ title: e3, url: t3 }) => ({ title: e3 || _15("download"), url: this.getDownloadLink(t3) }))) : [{ title: _15("download"), url: this.getDownloadLink(this.options.downloadDefinitionUrl) }]).filter((({ title: e3, url: t3 }) => e3 && t3));
            }
            getDownloadLink(e3) {
              if (e3) return e3;
              if (this.parser.specUrl) return this.parser.specUrl;
              if (s2 && window.Blob && window.URL && window.URL.createObjectURL) {
                const e4 = new Blob([JSON.stringify(this.parser.spec, null, 2)], { type: "application/json" });
                return window.URL.createObjectURL(e4);
              }
            }
            getDownloadFileName() {
              return this.parser.specUrl || this.options.downloadDefinitionUrl ? this.options.downloadFileName : this.options.downloadFileName || "openapi.json";
            }
          }
          var Hr = Object.defineProperty, Jr = Object.defineProperties, Yr = Object.getOwnPropertyDescriptors, Gr = Object.getOwnPropertySymbols, Zr = Object.prototype.hasOwnProperty, Xr = Object.prototype.propertyIsEnumerable, Kr = (e3, t3, r3) => t3 in e3 ? Hr(e3, t3, { enumerable: true, configurable: true, writable: true, value: r3 }) : e3[t3] = r3;
          class en {
            constructor(e3, t3) {
              const r3 = t3.spec.components && t3.spec.components.securitySchemes || {};
              this.schemes = Object.keys(e3 || {}).map(((n2) => {
                const { resolved: o3 } = t3.deref(r3[n2]), i3 = e3[n2] || [];
                if (!o3) return void console.warn(`Non existing security scheme referenced: ${n2}. Skipping`);
                const s3 = o3["x-displayName"] || n2;
                return a3 = ((e4, t4) => {
                  for (var r4 in t4 || (t4 = {})) Zr.call(t4, r4) && Kr(e4, r4, t4[r4]);
                  if (Gr) for (var r4 of Gr(t4)) Xr.call(t4, r4) && Kr(e4, r4, t4[r4]);
                  return e4;
                })({}, o3), Jr(a3, Yr({ id: n2, sectionId: n2, displayName: s3, scopes: i3 }));
                var a3;
              })).filter(((e4) => void 0 !== e4));
            }
          }
          var tn = Object.defineProperty, rn = Object.defineProperties, nn = Object.getOwnPropertyDescriptor, on = Object.getOwnPropertyDescriptors, sn = Object.getOwnPropertySymbols, an = Object.prototype.hasOwnProperty, ln = Object.prototype.propertyIsEnumerable, cn = (e3, t3, r3) => t3 in e3 ? tn(e3, t3, { enumerable: true, configurable: true, writable: true, value: r3 }) : e3[t3] = r3, pn = (e3, t3) => {
            for (var r3 in t3 || (t3 = {})) an.call(t3, r3) && cn(e3, r3, t3[r3]);
            if (sn) for (var r3 of sn(t3)) ln.call(t3, r3) && cn(e3, r3, t3[r3]);
            return e3;
          }, un = (e3, t3, r3, n2) => {
            for (var o3, i3 = n2 > 1 ? void 0 : n2 ? nn(t3, r3) : t3, s3 = e3.length - 1; s3 >= 0; s3--) (o3 = e3[s3]) && (i3 = (n2 ? o3(t3, r3, i3) : o3(i3)) || i3);
            return n2 && i3 && tn(t3, r3, i3), i3;
          };
          class dn {
            constructor(e3, t3, r3, n2, o3) {
              this.expanded = false, this.operations = [], (0, de2.makeObservable)(this), this.name = t3;
              const { resolved: i3 } = e3.deref(r3);
              for (const r4 of Object.keys(i3)) {
                const l3 = i3[r4], c3 = Object.keys(l3).filter(Me2);
                for (const i4 of c3) {
                  const c4 = l3[i4], p3 = new yo(e3, (s3 = pn({}, c4), a3 = { pathName: r4, pointer: Ee2.compile([n2, t3, r4, i4]), httpVerb: i4, pathParameters: l3.parameters || [], pathServers: l3.servers }, rn(s3, on(a3))), void 0, o3, true);
                  this.operations.push(p3);
                }
              }
              var s3, a3;
            }
            toggle() {
              this.expanded = !this.expanded;
            }
          }
          un([de2.observable], dn.prototype, "expanded", 2), un([de2.action], dn.prototype, "toggle", 1);
          var hn = Object.defineProperty, mn = Object.defineProperties, fn = Object.getOwnPropertyDescriptors, gn = Object.getOwnPropertySymbols, yn = Object.prototype.hasOwnProperty, bn = Object.prototype.propertyIsEnumerable, xn = (e3, t3, r3) => t3 in e3 ? hn(e3, t3, { enumerable: true, configurable: true, writable: true, value: r3 }) : e3[t3] = r3, vn = (e3, t3) => {
            for (var r3 in t3 || (t3 = {})) yn.call(t3, r3) && xn(e3, r3, t3[r3]);
            if (gn) for (var r3 of gn(t3)) bn.call(t3, r3) && xn(e3, r3, t3[r3]);
            return e3;
          }, wn = (e3, t3) => mn(e3, fn(t3)), kn = (e3, t3) => {
            var r3 = {};
            for (var n2 in e3) yn.call(e3, n2) && t3.indexOf(n2) < 0 && (r3[n2] = e3[n2]);
            if (null != e3 && gn) for (var n2 of gn(e3)) t3.indexOf(n2) < 0 && bn.call(e3, n2) && (r3[n2] = e3[n2]);
            return r3;
          };
          function En(e3, t3) {
            return t3 && e3[e3.length - 1] !== t3 ? [...e3, t3] : e3;
          }
          function On(e3, t3) {
            return t3 ? e3.concat(t3) : e3;
          }
          class jn {
            constructor(e3, t3, r3 = new W2({})) {
              this.options = r3, this.allowMergeRefs = false, this.byRef = (e4) => {
                let t4;
                if (this.spec) {
                  "#" !== e4.charAt(0) && (e4 = "#" + e4), e4 = decodeURIComponent(e4);
                  try {
                    t4 = Ee2.get(this.spec, e4);
                  } catch (e5) {
                  }
                  return t4 || {};
                }
              }, this.validate(e3), this.spec = e3, this.allowMergeRefs = e3.openapi.startsWith("3.1");
              const n2 = s2 ? window.location.href : "";
              "string" == typeof t3 && (this.specUrl = n2 ? new URL(t3, n2).href : t3);
            }
            validate(e3) {
              if (void 0 === e3.openapi) throw new Error("Document must be valid OpenAPI 3.0.0 definition");
            }
            isRef(e3) {
              return !!e3 && void 0 !== e3.$ref && null !== e3.$ref;
            }
            deref(e3, t3 = [], r3 = false) {
              const n2 = null == e3 ? void 0 : e3["x-refsStack"];
              if (t3 = On(t3, n2), this.isRef(e3)) {
                const n3 = rt2(e3.$ref);
                if (n3 && this.options.ignoreNamedSchemas.has(n3)) return { resolved: { type: "object", title: n3 }, refsStack: t3 };
                let o3 = this.byRef(e3.$ref);
                if (!o3) throw new Error(`Failed to resolve $ref "${e3.$ref}"`);
                let i3 = t3;
                if (t3.includes(e3.$ref) || t3.length > 999) o3 = Object.assign({}, o3, { "x-circular-ref": true });
                else if (this.isRef(o3)) {
                  const e4 = this.deref(o3, t3, r3);
                  i3 = e4.refsStack, o3 = e4.resolved;
                }
                return i3 = En(t3, e3.$ref), o3 = this.allowMergeRefs ? this.mergeRefs(e3, o3, r3) : o3, { resolved: o3, refsStack: i3 };
              }
              return { resolved: e3, refsStack: On(t3, n2) };
            }
            mergeRefs(e3, t3, r3) {
              const n2 = e3, { $ref: o3 } = n2, i3 = kn(n2, ["$ref"]), s3 = Object.keys(i3);
              if (0 === s3.length) return t3;
              if (r3 && s3.some(((e4) => !["description", "title", "externalDocs", "x-refsStack", "x-parentRefs", "readOnly", "writeOnly"].includes(e4)))) {
                const e4 = i3, { description: r4, title: n3, readOnly: o4, writeOnly: s4 } = e4;
                return { allOf: [{ description: r4, title: n3, readOnly: o4, writeOnly: s4 }, t3, kn(e4, ["description", "title", "readOnly", "writeOnly"])] };
              }
              return vn(vn({}, t3), i3);
            }
            mergeAllOf(e3, t3, r3) {
              var n2;
              if (e3["x-circular-ref"]) return e3;
              if (void 0 === (e3 = this.hoistOneOfs(e3, r3)).allOf) return e3;
              let o3 = wn(vn({}, e3), { "x-parentRefs": [], allOf: void 0, title: e3.title || rt2(t3) });
              void 0 !== o3.properties && "object" == typeof o3.properties && (o3.properties = vn({}, o3.properties)), void 0 !== o3.items && "object" == typeof o3.items && (o3.items = vn({}, o3.items));
              const i3 = (function(e4) {
                const t4 = /* @__PURE__ */ new Set();
                return e4.filter(((e5) => {
                  const r4 = e5.$ref;
                  return !r4 || r4 && !t4.has(r4) && t4.add(r4);
                }));
              })(e3.allOf.map(((e4) => {
                var t4;
                const { resolved: n3, refsStack: i4 } = this.deref(e4, r3, true), s3 = e4.$ref || void 0, a3 = this.mergeAllOf(n3, s3, i4);
                if (!a3["x-circular-ref"] || !a3.allOf) return s3 && (null == (t4 = o3["x-parentRefs"]) || t4.push(...a3["x-parentRefs"] || [], s3)), { $ref: s3, refsStack: En(i4, s3), schema: a3 };
              })).filter(((e4) => void 0 !== e4)));
              for (const { schema: e4, refsStack: r4 } of i3) {
                const i4 = e4, { type: s3, enum: a3, properties: l3, items: c3, required: p3, title: u3, description: d2, readOnly: h2, writeOnly: m3, oneOf: f3, anyOf: g3, "x-circular-ref": y3 } = i4, b3 = kn(i4, ["type", "enum", "properties", "items", "required", "title", "description", "readOnly", "writeOnly", "oneOf", "anyOf", "x-circular-ref"]);
                if (o3.type !== s3 && void 0 !== o3.type && void 0 !== s3 && console.warn(`Incompatible types in allOf at "${t3}": "${o3.type}" and "${s3}"`), void 0 !== s3 && (Array.isArray(s3) && Array.isArray(o3.type) ? o3.type = [...s3, ...o3.type] : o3.type = s3), void 0 !== a3 && (Array.isArray(a3) && Array.isArray(o3.enum) ? o3.enum = Array.from(/* @__PURE__ */ new Set([...a3, ...o3.enum])) : o3.enum = a3), void 0 !== l3 && "object" == typeof l3) {
                  o3.properties = o3.properties || {};
                  for (const e5 in l3) {
                    const i5 = On(r4, null == (n2 = l3[e5]) ? void 0 : n2["x-refsStack"]);
                    if (o3.properties[e5]) {
                      if (!y3) {
                        const r5 = this.mergeAllOf({ allOf: [o3.properties[e5], wn(vn({}, l3[e5]), { "x-refsStack": i5 })], "x-refsStack": i5 }, t3 + "/properties/" + e5, i5);
                        o3.properties[e5] = r5;
                      }
                    } else o3.properties[e5] = wn(vn({}, l3[e5]), { "x-refsStack": i5 });
                  }
                }
                if (void 0 !== c3 && !y3) {
                  const n3 = "boolean" == typeof o3.items ? {} : Object.assign({}, o3.items), i5 = "boolean" == typeof e4.items ? {} : Object.assign({}, e4.items);
                  o3.items = this.mergeAllOf({ allOf: [n3, i5] }, t3 + "/items", r4);
                }
                void 0 !== f3 && (o3.oneOf = f3), void 0 !== g3 && (o3.anyOf = g3), void 0 !== p3 && (o3.required = [...o3.required || [], ...p3]), o3 = vn(wn(vn({}, o3), { title: o3.title || u3, description: o3.description || d2, readOnly: void 0 !== o3.readOnly ? o3.readOnly : h2, writeOnly: void 0 !== o3.writeOnly ? o3.writeOnly : m3, "x-circular-ref": o3["x-circular-ref"] || y3 }), b3);
              }
              return o3;
            }
            findDerived(e3) {
              const t3 = {}, r3 = this.spec.components && this.spec.components.schemas || {};
              for (const n2 in r3) {
                const { resolved: o3 } = this.deref(r3[n2]);
                void 0 !== o3.allOf && o3.allOf.find(((t4) => void 0 !== t4.$ref && e3.indexOf(t4.$ref) > -1)) && (t3["#/components/schemas/" + n2] = [o3["x-discriminator-value"] || n2]);
              }
              return t3;
            }
            hoistOneOfs(e3, t3) {
              if (void 0 === e3.allOf) return e3;
              const r3 = e3.allOf;
              for (let e4 = 0; e4 < r3.length; e4++) {
                const n2 = r3[e4], { oneOf: o3 } = n2, i3 = kn(n2, ["oneOf"]);
                if (o3 && Array.isArray(o3)) {
                  const n3 = r3.slice(0, e4), s3 = r3.slice(e4 + 1), a3 = Object.keys(i3).length > 0 ? [i3] : [];
                  return { oneOf: o3.map(((e5) => ({ allOf: [...n3, ...a3, e5, ...s3], "x-refsStack": t3 }))) };
                }
              }
              return e3;
            }
          }
          var $n = Object.defineProperty, Sn = Object.defineProperties, Cn = Object.getOwnPropertyDescriptor, Pn = Object.getOwnPropertyDescriptors, In = Object.getOwnPropertySymbols, An = Object.prototype.hasOwnProperty, Tn = Object.prototype.propertyIsEnumerable, _n = (e3, t3, r3) => t3 in e3 ? $n(e3, t3, { enumerable: true, configurable: true, writable: true, value: r3 }) : e3[t3] = r3, Rn = (e3, t3) => {
            for (var r3 in t3 || (t3 = {})) An.call(t3, r3) && _n(e3, r3, t3[r3]);
            if (In) for (var r3 of In(t3)) Tn.call(t3, r3) && _n(e3, r3, t3[r3]);
            return e3;
          }, Dn = (e3, t3) => Sn(e3, Pn(t3)), zn = (e3, t3, r3, n2) => {
            for (var o3, i3 = n2 > 1 ? void 0 : n2 ? Cn(t3, r3) : t3, s3 = e3.length - 1; s3 >= 0; s3--) (o3 = e3[s3]) && (i3 = (n2 ? o3(t3, r3, i3) : o3(i3)) || i3);
            return n2 && i3 && $n(t3, r3, i3), i3;
          };
          const Ln = class e3 {
            constructor(e4, t3, r3, n2, o3 = false, i3 = []) {
              this.options = n2, this.refsStack = i3, this.typePrefix = "", this.isCircular = false, this.activeOneOf = 0, (0, de2.makeObservable)(this), this.pointer = t3.$ref || r3 || "";
              const { resolved: s3, refsStack: a3 } = e4.deref(t3, i3, true);
              this.refsStack = En(a3, this.pointer), this.rawSchema = s3, this.schema = e4.mergeAllOf(this.rawSchema, this.pointer, this.refsStack), this.init(e4, o3), n2.showExtensions && (this.extensions = xt(this.schema, n2.showExtensions));
            }
            activateOneOf(e4) {
              this.activeOneOf = e4;
            }
            hasType(e4) {
              return this.type === e4 || I2(this.type) && this.type.includes(e4);
            }
            init(t3, r3) {
              var n2, o3, i3, s3, a3, l3, c3, p3;
              const u3 = this.schema;
              if (this.isCircular = !!u3["x-circular-ref"], this.title = u3.title || tt2(this.pointer) && Ee2.baseName(this.pointer) || "", this.description = u3.description || "", this.type = u3.type || Be2(u3), this.format = u3.format, this.enum = u3.enum || [], this["x-enumDescriptions"] = u3["x-enumDescriptions"], this.example = u3.example, this.examples = u3.examples, this.deprecated = !!u3.deprecated, this.pattern = u3.pattern, this.externalDocs = u3.externalDocs, this.constraints = it2(u3), this.displayFormat = this.format, this.isPrimitive = Qe2(u3, this.type), this.default = u3.default, this.readOnly = !!u3.readOnly, this.writeOnly = !!u3.writeOnly, this.const = u3.const || "", this.contentEncoding = u3.contentEncoding, this.contentMediaType = u3.contentMediaType, this.minItems = u3.minItems, this.maxItems = u3.maxItems, (u3.nullable || u3["x-nullable"]) && (I2(this.type) && !this.type.some(((e4) => null === e4 || "null" === e4)) ? this.type = [...this.type, "null"] : I2(this.type) || null === this.type && "null" === this.type || (this.type = [this.type, "null"])), this.displayType = I2(this.type) ? this.type.map(((e4) => null === e4 ? "null" : e4)).join(" or ") : this.type, !this.isCircular) if (u3.if && u3.then || u3.if && u3.else) this.initConditionalOperators(u3, t3);
              else if (r3 || void 0 === qn(u3)) {
                if (r3 && I2(u3.oneOf) && u3.oneOf.find(((e4) => e4.$ref === this.pointer)) && delete u3.oneOf, void 0 !== u3.oneOf) return this.initOneOf(u3.oneOf, t3), this.oneOfType = "One of", void (void 0 !== u3.anyOf && console.warn(`oneOf and anyOf are not supported on the same level. Skipping anyOf at ${this.pointer}`));
                if (void 0 !== u3.anyOf) return this.initOneOf(u3.anyOf, t3), void (this.oneOfType = "Any of");
                if (this.hasType("object")) this.fields = Mn(t3, u3, this.pointer, this.options, this.refsStack);
                else if (this.hasType("array") && (I2(u3.items) || I2(u3.prefixItems) ? this.fields = Mn(t3, u3, this.pointer, this.options, this.refsStack) : u3.items && (this.items = new e3(t3, u3.items, this.pointer + "/items", this.options, false, this.refsStack)), this.displayType = u3.prefixItems || I2(u3.items) ? "items" : vt2((null == (n2 = this.items) ? void 0 : n2.displayType) || this.displayType), this.displayFormat = (null == (o3 = this.items) ? void 0 : o3.format) || "", this.typePrefix = (null == (i3 = this.items) ? void 0 : i3.typePrefix) || "" + _15("arrayOf"), this.title = this.title || (null == (s3 = this.items) ? void 0 : s3.title) || "", this.isPrimitive = void 0 !== (null == (a3 = this.items) ? void 0 : a3.isPrimitive) ? null == (l3 = this.items) ? void 0 : l3.isPrimitive : this.isPrimitive, void 0 === this.example && void 0 !== (null == (c3 = this.items) ? void 0 : c3.example) && (this.example = [this.items.example]), (null == (p3 = this.items) ? void 0 : p3.isPrimitive) && (this.enum = this.items.enum, this["x-enumDescriptions"] = this.items["x-enumDescriptions"]), I2(this.type))) {
                  const e4 = this.type.filter(((e5) => "array" !== e5));
                  e4.length && (this.displayType += ` or ${e4.join(" or ")}`);
                }
                this.enum.length && this.options.sortEnumValuesAlphabetically && this.enum.sort();
              } else this.initDiscriminator(u3, t3);
            }
            initOneOf(t3, r3) {
              if (this.oneOf = t3.map(((t4, n2) => {
                const { resolved: o3, refsStack: i3 } = r3.deref(t4, this.refsStack, true), s3 = r3.mergeAllOf(o3, this.pointer + "/oneOf/" + n2, i3), a3 = tt2(t4.$ref) && !s3.title ? Ee2.baseName(t4.$ref) : `${s3.title || ""}${void 0 !== s3.const && JSON.stringify(s3.const) || ""}`;
                return new e3(r3, Dn(Rn({}, s3), { title: a3, allOf: [Dn(Rn({}, this.schema), { oneOf: void 0, anyOf: void 0 })], discriminator: o3.allOf ? void 0 : s3.discriminator }), t4.$ref || this.pointer + "/oneOf/" + n2, this.options, false, i3);
              })), this.options.simpleOneOfTypeLabel) {
                const e4 = (function(e5) {
                  const t4 = /* @__PURE__ */ new Set();
                  return (function e6(r4) {
                    for (const n2 of r4.oneOf || []) n2.oneOf ? e6(n2) : n2.type && t4.add(n2.type);
                  })(e5), Array.from(t4.values());
                })(this);
                this.displayType = e4.join(" or ");
              } else this.displayType = this.oneOf.map(((e4) => {
                let t4 = e4.typePrefix + (e4.title ? `${e4.title} (${e4.displayType})` : e4.displayType);
                return t4.indexOf(" or ") > -1 && (t4 = `(${t4})`), t4;
              })).join(" or ");
            }
            initDiscriminator(t3, r3) {
              const n2 = qn(t3);
              this.discriminatorProp = n2.propertyName;
              const o3 = r3.findDerived([...this.schema["x-parentRefs"] || [], this.pointer]);
              if (t3.oneOf) for (const e4 of t3.oneOf) {
                if (void 0 === e4.$ref) continue;
                const t4 = Ee2.baseName(e4.$ref);
                o3[e4.$ref] = t4;
              }
              const i3 = n2.mapping || {};
              let s3 = n2["x-explicitMappingOnly"] || false;
              0 === Object.keys(i3).length && (s3 = false);
              const a3 = {};
              for (const e4 in i3) {
                const t4 = i3[e4];
                I2(a3[t4]) ? a3[t4].push(e4) : a3[t4] = [e4];
              }
              const l3 = Rn(s3 ? {} : Rn({}, o3), a3);
              let c3 = [];
              for (const e4 of Object.keys(l3)) {
                const t4 = l3[e4];
                if (I2(t4)) for (const r4 of t4) c3.push({ $ref: e4, name: r4 });
                else c3.push({ $ref: e4, name: t4 });
              }
              const p3 = Object.keys(i3);
              0 !== p3.length && (c3 = c3.sort(((e4, t4) => {
                const r4 = p3.indexOf(e4.name), n3 = p3.indexOf(t4.name);
                return r4 < 0 && n3 < 0 ? e4.name.localeCompare(t4.name) : r4 < 0 ? 1 : n3 < 0 ? -1 : r4 - n3;
              }))), this.oneOf = c3.map((({ $ref: t4, name: n3 }) => {
                const o4 = new e3(r3, { $ref: t4 }, t4, this.options, true, this.refsStack.slice(0, -1));
                return o4.title = n3, o4;
              }));
            }
            initConditionalOperators(t3, r3) {
              const n2 = t3, { if: o3, else: i3 = {}, then: s3 = {} } = n2, a3 = ((e4, t4) => {
                var r4 = {};
                for (var n3 in e4) An.call(e4, n3) && t4.indexOf(n3) < 0 && (r4[n3] = e4[n3]);
                if (null != e4 && In) for (var n3 of In(e4)) t4.indexOf(n3) < 0 && Tn.call(e4, n3) && (r4[n3] = e4[n3]);
                return r4;
              })(n2, ["if", "else", "then"]), l3 = [{ allOf: [a3, s3, o3], title: o3 && o3["x-displayName"] || (null == o3 ? void 0 : o3.title) || "case 1" }, { allOf: [a3, i3], title: i3 && i3["x-displayName"] || (null == i3 ? void 0 : i3.title) || "case 2" }];
              this.oneOf = l3.map(((t4, n3) => new e3(r3, Rn({}, t4), this.pointer + "/oneOf/" + n3, this.options, false, this.refsStack))), this.oneOfType = "One of";
            }
          };
          zn([de2.observable], Ln.prototype, "activeOneOf", 2), zn([de2.action], Ln.prototype, "activateOneOf", 1);
          let Nn = Ln;
          function Mn(e3, t3, r3, n2, o3) {
            const i3 = t3.properties || t3.prefixItems || t3.items || {}, s3 = t3.patternProperties || {}, a3 = t3.additionalProperties || t3.unevaluatedProperties, l3 = t3.prefixItems ? t3.items : t3.additionalItems, c3 = t3.default;
            let p3 = Object.keys(i3 || []).map(((s4) => {
              let a4 = i3[s4];
              a4 || (console.warn(`Field "${s4}" is invalid, skipping.
 Field must be an object but got ${typeof a4} at "${r3}"`), a4 = {});
              const l4 = void 0 !== t3.required && t3.required.indexOf(s4) > -1;
              return new Hn(e3, { name: t3.properties ? s4 : `[${s4}]`, required: l4, schema: Dn(Rn({}, a4), { default: void 0 === a4.default && c3 ? c3[s4] : a4.default }) }, r3 + "/properties/" + s4, n2, o3);
            }));
            return n2.sortPropsAlphabetically && (p3 = at2(p3, "name")), n2.sortRequiredPropsFirst && (p3 = st2(p3, n2.sortPropsAlphabetically ? void 0 : t3.required)), p3.push(...Object.keys(s3).map(((t4) => {
              let i4 = s3[t4];
              return i4 || (console.warn(`Field "${t4}" is invalid, skipping.
 Field must be an object but got ${typeof i4} at "${r3}"`), i4 = {}), new Hn(e3, { name: t4, required: false, schema: i4, kind: "patternProperties" }, `${r3}/patternProperties/${t4}`, n2, o3);
            }))), "object" != typeof a3 && true !== a3 || p3.push(new Hn(e3, { name: ("object" == typeof a3 && a3["x-additionalPropertiesName"] || "property name").concat("*"), required: false, schema: true === a3 ? {} : a3, kind: "additionalProperties" }, r3 + "/additionalProperties", n2, o3)), p3.push(...(function({ parser: e4, schema: t4 = false, fieldsCount: r4, $ref: n3, options: o4, refsStack: i4 }) {
              return A2(t4) ? t4 ? [new Hn(e4, { name: `[${r4}...]`, schema: {} }, `${n3}/additionalItems`, o4, i4)] : [] : I2(t4) ? [...t4.map(((t5, s4) => new Hn(e4, { name: `[${r4 + s4}]`, schema: t5 }, `${n3}/additionalItems`, o4, i4)))] : x2(t4) ? [new Hn(e4, { name: `[${r4}...]`, schema: t4 }, `${n3}/additionalItems`, o4, i4)] : [];
            })({ parser: e3, schema: l3, fieldsCount: p3.length, $ref: r3, options: n2, refsStack: o3 })), p3;
          }
          function qn(e3) {
            return e3.discriminator || e3["x-discriminator"];
          }
          const Fn = {};
          class Bn {
            constructor(e3, t3, r3, n2) {
              this.mime = r3;
              const { resolved: o3 } = e3.deref(t3);
              this.value = o3.value, this.summary = o3.summary, this.description = o3.description, o3.externalValue && (this.externalValueUrl = new URL(o3.externalValue, e3.specUrl).href), We2(r3) && this.value && "object" == typeof this.value && (this.value = Ye2(this.value, n2));
            }
            getExternalValue(e3) {
              return this.externalValueUrl ? (this.externalValueUrl in Fn || (Fn[this.externalValueUrl] = fetch(this.externalValueUrl).then(((t3) => t3.text().then(((r3) => {
                if (!t3.ok) return Promise.reject(new Error(r3));
                if (!Ue2(e3)) return r3;
                try {
                  return JSON.parse(r3);
                } catch (e4) {
                  return r3;
                }
              }))))), Fn[this.externalValueUrl]) : Promise.resolve(void 0);
            }
          }
          var Qn = Object.defineProperty, Un = Object.getOwnPropertyDescriptor, Wn = (e3, t3, r3, n2) => {
            for (var o3, i3 = n2 > 1 ? void 0 : n2 ? Un(t3, r3) : t3, s3 = e3.length - 1; s3 >= 0; s3--) (o3 = e3[s3]) && (i3 = (n2 ? o3(t3, r3, i3) : o3(i3)) || i3);
            return n2 && i3 && Qn(t3, r3, i3), i3;
          };
          const Vn = { path: { style: "simple", explode: false }, query: { style: "form", explode: true }, header: { style: "simple", explode: false }, cookie: { style: "form", explode: true } };
          class Hn {
            constructor(e3, t3, r3, n2, o3) {
              var i3, s3, a3, l3, c3;
              this.expanded = void 0, (0, de2.makeObservable)(this);
              const { resolved: p3 } = e3.deref(t3);
              this.kind = t3.kind || "field", this.name = t3.name || p3.name, this.in = p3.in, this.required = !!p3.required;
              let u3 = p3.schema, d2 = "";
              if (!u3 && p3.in && p3.content && (d2 = Object.keys(p3.content)[0], u3 = p3.content[d2] && p3.content[d2].schema), this.schema = new Nn(e3, u3 || {}, r3, n2, false, o3), this.description = void 0 === p3.description ? this.schema.description || "" : p3.description, this.example = p3.example || this.schema.example, void 0 !== p3.examples || void 0 !== this.schema.examples) {
                const t4 = p3.examples || this.schema.examples;
                this.examples = I2(t4) ? t4 : h(t4, ((t5, r4) => new Bn(e3, t5, r4, p3.encoding)));
              }
              d2 ? this.serializationMime = d2 : p3.style ? this.style = p3.style : this.in && (this.style = null != (s3 = null == (i3 = Vn[this.in]) ? void 0 : i3.style) ? s3 : "form"), void 0 === p3.explode && this.in ? this.explode = null == (l3 = null == (a3 = Vn[this.in]) ? void 0 : a3.explode) || l3 : this.explode = !!p3.explode, this.deprecated = void 0 === p3.deprecated ? !!this.schema.deprecated : p3.deprecated, n2.showExtensions && (this.extensions = xt(p3, n2.showExtensions)), this.const = (null == (c3 = this.schema) ? void 0 : c3.const) || (null == p3 ? void 0 : p3.const) || "";
            }
            toggle() {
              this.expanded = !this.expanded;
            }
            collapse() {
              this.expanded = false;
            }
            expand() {
              this.expanded = true;
            }
          }
          Wn([de2.observable], Hn.prototype, "expanded", 2), Wn([de2.action], Hn.prototype, "toggle", 1), Wn([de2.action], Hn.prototype, "collapse", 1), Wn([de2.action], Hn.prototype, "expand", 1);
          var Jn = (init_openapi_sampler(), __toCommonJS(openapi_sampler_exports));
          class Yn {
            constructor(e3, t3, r3, n2, o3) {
              this.name = t3, this.isRequestType = r3, this.schema = n2.schema && new Nn(e3, n2.schema, "", o3), this.onlyRequiredInSamples = o3.onlyRequiredInSamples, this.generatedSamplesMaxDepth = o3.generatedSamplesMaxDepth, void 0 !== n2.examples ? this.examples = h(n2.examples, ((r4) => new Bn(e3, r4, t3, n2.encoding))) : void 0 !== n2.example ? this.examples = { default: new Bn(e3, { value: e3.deref(n2.example).resolved }, t3, n2.encoding) } : Ue2(t3) && this.generateExample(e3, n2);
            }
            generateExample(e3, t3) {
              const r3 = { skipReadOnly: this.isRequestType, skipWriteOnly: !this.isRequestType, skipNonRequired: this.isRequestType && this.onlyRequiredInSamples, maxSampleDepth: this.generatedSamplesMaxDepth };
              if (this.schema && this.schema.oneOf) {
                this.examples = {};
                for (const n2 of this.schema.oneOf) {
                  const o3 = Jn.sample(n2.rawSchema, r3, e3.spec);
                  this.schema.discriminatorProp && "object" == typeof o3 && o3 && (o3[this.schema.discriminatorProp] = n2.title), this.examples[n2.title] = new Bn(e3, { value: o3 }, this.name, t3.encoding);
                }
              } else this.schema && (this.examples = { default: new Bn(e3, { value: Jn.sample(t3.schema, r3, e3.spec) }, this.name, t3.encoding) });
            }
          }
          var Gn = Object.defineProperty, Zn = Object.getOwnPropertyDescriptor, Xn = (e3, t3, r3, n2) => {
            for (var o3, i3 = n2 > 1 ? void 0 : n2 ? Zn(t3, r3) : t3, s3 = e3.length - 1; s3 >= 0; s3--) (o3 = e3[s3]) && (i3 = (n2 ? o3(t3, r3, i3) : o3(i3)) || i3);
            return n2 && i3 && Gn(t3, r3, i3), i3;
          };
          class Kn {
            constructor(e3, t3, r3, n2) {
              this.isRequestType = r3, this.activeMimeIdx = 0, (0, de2.makeObservable)(this), n2.unstable_ignoreMimeParameters && (t3 = ct2(t3)), this.mediaTypes = Object.keys(t3).map(((o3) => {
                const i3 = t3[o3];
                return new Yn(e3, o3, r3, i3, n2);
              }));
            }
            activate(e3) {
              this.activeMimeIdx = e3;
            }
            get active() {
              return this.mediaTypes[this.activeMimeIdx];
            }
            get hasSample() {
              return this.mediaTypes.filter(((e3) => !!e3.examples)).length > 0;
            }
          }
          Xn([de2.observable], Kn.prototype, "activeMimeIdx", 2), Xn([de2.action], Kn.prototype, "activate", 1), Xn([de2.computed], Kn.prototype, "active", 1);
          class eo {
            constructor({ parser: e3, infoOrRef: t3, options: r3, isEvent: n2 }) {
              const o3 = !n2, { resolved: i3 } = e3.deref(t3);
              this.description = i3.description || "", this.required = i3.required;
              const s3 = wt(i3);
              void 0 !== s3 && (this.content = new Kn(e3, s3, o3, r3));
            }
          }
          var to = Object.defineProperty, ro = Object.defineProperties, no = Object.getOwnPropertyDescriptor, oo = Object.getOwnPropertyDescriptors, io = Object.getOwnPropertySymbols, so = Object.prototype.hasOwnProperty, ao = Object.prototype.propertyIsEnumerable, lo = (e3, t3, r3) => t3 in e3 ? to(e3, t3, { enumerable: true, configurable: true, writable: true, value: r3 }) : e3[t3] = r3, co = (e3, t3, r3, n2) => {
            for (var o3, i3 = n2 > 1 ? void 0 : n2 ? no(t3, r3) : t3, s3 = e3.length - 1; s3 >= 0; s3--) (o3 = e3[s3]) && (i3 = (n2 ? o3(t3, r3, i3) : o3(i3)) || i3);
            return n2 && i3 && to(t3, r3, i3), i3;
          };
          class po {
            constructor({ parser: e3, code: t3, defaultAsError: r3, infoOrRef: n2, options: o3, isEvent: i3 }) {
              this.expanded = false, this.headers = [], (0, de2.makeObservable)(this), this.expanded = "all" === o3.expandResponses || o3.expandResponses[t3];
              const { resolved: s3 } = e3.deref(n2);
              this.code = t3, void 0 !== s3.content && (this.content = new Kn(e3, s3.content, i3, o3)), void 0 !== s3["x-summary"] ? (this.summary = s3["x-summary"], this.description = s3.description || "") : (this.summary = s3.description || "", this.description = ""), this.type = Le2(t3, r3);
              const a3 = s3.headers;
              void 0 !== a3 && (this.headers = Object.keys(a3).map(((t4) => {
                const r4 = a3[t4];
                return new Hn(e3, (n3 = ((e4, t5) => {
                  for (var r5 in t5 || (t5 = {})) so.call(t5, r5) && lo(e4, r5, t5[r5]);
                  if (io) for (var r5 of io(t5)) ao.call(t5, r5) && lo(e4, r5, t5[r5]);
                  return e4;
                })({}, r4), ro(n3, oo({ name: t4 }))), "", o3);
                var n3;
              }))), o3.showExtensions && (this.extensions = xt(s3, o3.showExtensions));
            }
            toggle() {
              this.expanded = !this.expanded;
            }
          }
          co([de2.observable], po.prototype, "expanded", 2), co([de2.action], po.prototype, "toggle", 1);
          var uo = Object.defineProperty, ho = Object.getOwnPropertyDescriptor, mo = (e3, t3, r3, n2) => {
            for (var o3, i3 = n2 > 1 ? void 0 : n2 ? ho(t3, r3) : t3, s3 = e3.length - 1; s3 >= 0; s3--) (o3 = e3[s3]) && (i3 = (n2 ? o3(t3, r3, i3) : o3(i3)) || i3);
            return n2 && i3 && uo(t3, r3, i3), i3;
          };
          function fo(e3) {
            return "payload" === e3.lang && e3.requestBodyContent;
          }
          let go = false;
          class yo {
            constructor(e3, t3, r3, n2, o3 = false) {
              var i3;
              this.parser = e3, this.operationSpec = t3, this.options = n2, this.type = "operation", this.items = [], this.ready = true, this.active = false, this.expanded = false, (0, de2.makeObservable)(this), this.pointer = t3.pointer, this.description = t3.description, this.parent = r3, this.externalDocs = t3.externalDocs, this.deprecated = !!t3.deprecated, this.httpVerb = t3.httpVerb, this.deprecated = !!t3.deprecated, this.operationId = t3.operationId, this.path = t3.pathName, this.isCallback = o3, this.isWebhook = t3.isWebhook, this.isEvent = this.isCallback || this.isWebhook, this.name = qe2(t3), this.sidebarLabel = n2.sideNavStyle === R2.IdOnly ? this.operationId || this.path : n2.sideNavStyle === R2.PathOnly ? this.path : this.name, this.badges = (null == (i3 = t3["x-badges"]) ? void 0 : i3.map((({ name: e4, color: t4, position: r4 }) => ({ name: e4, color: t4, position: r4 || "after" })))) || [], this.isCallback ? (this.security = (t3.security || []).map(((t4) => new en(t4, e3))), this.servers = ut2("", t3.servers || t3.pathServers || [])) : (this.operationHash = t3.operationId && "operation/" + t3.operationId, this.id = void 0 !== t3.operationId ? (r3 ? r3.id + "/" : "") + this.operationHash : void 0 !== r3 ? r3.id + this.pointer : this.pointer, this.security = (t3.security || e3.spec.security || []).map(((t4) => new en(t4, e3))), this.servers = ut2(e3.specUrl, t3.servers || t3.pathServers || e3.spec.servers || [])), n2.showExtensions && (this.extensions = xt(t3, n2.showExtensions));
            }
            activate() {
              this.active = true;
            }
            deactivate() {
              this.active = false;
            }
            toggle() {
              this.expanded = !this.expanded;
            }
            expand() {
              this.parent && this.parent.expand();
            }
            collapse() {
            }
            get requestBody() {
              return this.operationSpec.requestBody && new eo({ parser: this.parser, infoOrRef: this.operationSpec.requestBody, options: this.options, isEvent: this.isEvent });
            }
            get codeSamples() {
              const { payloadSampleIdx: e3, hideRequestPayloadSample: t3 } = this.options;
              let r3 = this.operationSpec["x-codeSamples"] || this.operationSpec["x-code-samples"] || [];
              this.operationSpec["x-code-samples"] && !go && (go = true, console.warn('"x-code-samples" is deprecated. Use "x-codeSamples" instead'));
              const n2 = this.requestBody && this.requestBody.content;
              if (n2 && n2.hasSample && !t3) {
                const t4 = Math.min(r3.length, e3);
                r3 = [...r3.slice(0, t4), { lang: "payload", label: "Payload", source: "", requestBodyContent: n2 }, ...r3.slice(t4)];
              }
              return r3;
            }
            get parameters() {
              const e3 = lt2(this.parser, this.operationSpec.pathParameters, this.operationSpec.parameters).map(((e4) => new Hn(this.parser, e4, this.pointer, this.options)));
              return this.options.sortPropsAlphabetically ? at2(e3, "name") : this.options.sortRequiredPropsFirst ? st2(e3) : e3;
            }
            get responses() {
              let e3 = false;
              return Object.keys(this.operationSpec.responses || []).filter(((t3) => "default" === t3 || ("success" === Le2(t3) && (e3 = true), ze2(t3)))).map(((t3) => new po({ parser: this.parser, code: t3, defaultAsError: e3, infoOrRef: this.operationSpec.responses[t3], options: this.options, isEvent: this.isEvent })));
            }
            get callbacks() {
              return Object.keys(this.operationSpec.callbacks || []).map(((e3) => new dn(this.parser, e3, this.operationSpec.callbacks[e3], this.pointer, this.options)));
            }
          }
          mo([de2.observable], yo.prototype, "ready", 2), mo([de2.observable], yo.prototype, "active", 2), mo([de2.observable], yo.prototype, "expanded", 2), mo([de2.action], yo.prototype, "activate", 1), mo([de2.action], yo.prototype, "deactivate", 1), mo([de2.action], yo.prototype, "toggle", 1), mo([Mt], yo.prototype, "requestBody", 1), mo([Mt], yo.prototype, "codeSamples", 1), mo([Mt], yo.prototype, "parameters", 1), mo([Mt], yo.prototype, "responses", 1), mo([Mt], yo.prototype, "callbacks", 1);
          var bo = Object.defineProperty, xo = Object.defineProperties, vo = Object.getOwnPropertyDescriptors, wo = Object.getOwnPropertySymbols, ko = Object.prototype.hasOwnProperty, Eo = Object.prototype.propertyIsEnumerable, Oo = (e3, t3, r3) => t3 in e3 ? bo(e3, t3, { enumerable: true, configurable: true, writable: true, value: r3 }) : e3[t3] = r3, jo = (e3, t3) => {
            for (var r3 in t3 || (t3 = {})) ko.call(t3, r3) && Oo(e3, r3, t3[r3]);
            if (wo) for (var r3 of wo(t3)) Eo.call(t3, r3) && Oo(e3, r3, t3[r3]);
            return e3;
          };
          class $o {
            constructor(e3, t3, r3) {
              this.operations = [];
              const { resolved: n2 } = e3.deref(r3 || {});
              this.initWebhooks(e3, n2, t3);
            }
            initWebhooks(e3, t3, r3) {
              for (const o3 of Object.keys(t3)) {
                const i3 = t3[o3], s3 = Object.keys(i3).filter(Me2);
                for (const t4 of s3) {
                  const o4 = i3[t4];
                  if (i3.$ref) {
                    const n3 = e3.deref(i3 || {});
                    this.initWebhooks(e3, { [t4]: n3 }, r3);
                  }
                  if (!o4) continue;
                  const s4 = new yo(e3, (n2 = jo({}, o4), xo(n2, vo({ httpVerb: t4 }))), void 0, r3, false);
                  this.operations.push(s4);
                }
              }
              var n2;
            }
          }
          class So {
            constructor(e3, t3, r3) {
              const { resolved: n2 } = e3.deref(r3);
              this.id = t3, this.sectionId = ft2 + t3, this.type = n2.type, this.displayName = n2["x-displayName"] || t3, this.description = n2.description || "", "apiKey" === n2.type && (this.apiKey = { name: n2.name, in: n2.in }), "http" === n2.type && (this.http = { scheme: n2.scheme, bearerFormat: n2.bearerFormat }), "openIdConnect" === n2.type && (this.openId = { connectUrl: n2.openIdConnectUrl }), "oauth2" === n2.type && n2.flows && (this.flows = n2.flows);
            }
          }
          class Co {
            constructor(e3) {
              const t3 = e3.spec.components && e3.spec.components.securitySchemes || {};
              this.schemes = Object.keys(t3).map(((r3) => new So(e3, r3, t3[r3])));
            }
          }
          var Po = Object.defineProperty, Io = Object.getOwnPropertySymbols, Ao = Object.prototype.hasOwnProperty, To = Object.prototype.propertyIsEnumerable, _o = (e3, t3, r3) => t3 in e3 ? Po(e3, t3, { enumerable: true, configurable: true, writable: true, value: r3 }) : e3[t3] = r3, Ro = (e3, t3) => {
            for (var r3 in t3 || (t3 = {})) Ao.call(t3, r3) && _o(e3, r3, t3[r3]);
            if (Io) for (var r3 of Io(t3)) To.call(t3, r3) && _o(e3, r3, t3[r3]);
            return e3;
          };
          class Do {
            constructor(e3, t3, r3) {
              var n2, o3, i3;
              this.options = r3, this.parser = new jn(e3, t3, r3), this.info = new Vr(this.parser, this.options), this.externalDocs = this.parser.spec.externalDocs, this.contentItems = Go.buildStructure(this.parser, this.options), this.securitySchemes = new Co(this.parser);
              const s3 = Ro(Ro({}, null == (o3 = null == (n2 = this.parser) ? void 0 : n2.spec) ? void 0 : o3["x-webhooks"]), null == (i3 = this.parser) ? void 0 : i3.spec.webhooks);
              this.webhooks = new $o(this.parser, r3, s3);
            }
          }
          var zo = Object.defineProperty, Lo = Object.getOwnPropertyDescriptor, No = (e3, t3, r3, n2) => {
            for (var o3, i3 = n2 > 1 ? void 0 : n2 ? Lo(t3, r3) : t3, s3 = e3.length - 1; s3 >= 0; s3--) (o3 = e3[s3]) && (i3 = (n2 ? o3(t3, r3, i3) : o3(i3)) || i3);
            return n2 && i3 && zo(t3, r3, i3), i3;
          };
          class Mo {
            constructor(e3, t3, r3) {
              this.items = [], this.active = false, this.expanded = false, (0, de2.makeObservable)(this), this.id = t3.id || e3 + "/" + w2(t3.name), this.type = e3, this.name = t3["x-displayName"] || t3.name, this.level = t3.level || 1, this.sidebarLabel = this.name, this.description = t3.description || "";
              const n2 = t3.items;
              n2 && n2.length && (this.description = Ur.getTextBeforeHading(this.description, n2[0].name)), this.parent = r3, this.externalDocs = t3.externalDocs, "group" === this.type && (this.expanded = true);
            }
            activate() {
              this.active = true;
            }
            expand() {
              this.parent && this.parent.expand(), this.expanded = true;
            }
            collapse() {
              "group" !== this.type && (this.expanded = false);
            }
            deactivate() {
              this.active = false;
            }
          }
          No([de2.observable], Mo.prototype, "active", 2), No([de2.observable], Mo.prototype, "expanded", 2), No([de2.action], Mo.prototype, "activate", 1), No([de2.action], Mo.prototype, "expand", 1), No([de2.action], Mo.prototype, "collapse", 1), No([de2.action], Mo.prototype, "deactivate", 1);
          var qo = Object.defineProperty, Fo = Object.defineProperties, Bo = Object.getOwnPropertyDescriptors, Qo = Object.getOwnPropertySymbols, Uo = Object.prototype.hasOwnProperty, Wo = Object.prototype.propertyIsEnumerable, Vo = (e3, t3, r3) => t3 in e3 ? qo(e3, t3, { enumerable: true, configurable: true, writable: true, value: r3 }) : e3[t3] = r3, Ho = (e3, t3) => {
            for (var r3 in t3 || (t3 = {})) Uo.call(t3, r3) && Vo(e3, r3, t3[r3]);
            if (Qo) for (var r3 of Qo(t3)) Wo.call(t3, r3) && Vo(e3, r3, t3[r3]);
            return e3;
          }, Jo = (e3, t3) => Fo(e3, Bo(t3));
          const Yo = 0;
          class Go {
            static buildStructure(e3, t3) {
              const r3 = e3.spec, { schemaDefinitionsTagName: n2 } = t3, o3 = [], i3 = [...r3.tags || []];
              !i3.find(((e4) => (null == e4 ? void 0 : e4.name) === n2)) && n2 && i3.push({ name: n2 });
              const s3 = Go.getTagsWithOperations(e3, i3);
              return o3.push(...Go.addMarkdownItems(r3.info.description || "", void 0, 1, t3)), r3["x-tagGroups"] && r3["x-tagGroups"].length > 0 ? o3.push(...Go.getTagGroupsItems(e3, void 0, r3["x-tagGroups"], s3, t3)) : o3.push(...Go.getTagsItems(e3, s3, void 0, void 0, t3)), o3;
            }
            static addMarkdownItems(e3, t3, r3, n2) {
              const o3 = new Ur(n2, null == t3 ? void 0 : t3.id).extractHeadings(e3 || "");
              o3.length && t3 && t3.description && (t3.description = Ur.getTextBeforeHading(t3.description, o3[0].name));
              const i3 = (e4, t4, r4 = 1) => t4.map(((t5) => {
                const n3 = new Mo("section", t5, e4);
                return n3.depth = r4, t5.items && (n3.items = i3(n3, t5.items, r4 + 1)), n3;
              }));
              return i3(t3, o3, r3);
            }
            static getTagGroupsItems(e3, t3, r3, n2, o3) {
              const i3 = [];
              for (const s3 of r3) {
                const r4 = new Mo("group", s3, t3);
                r4.depth = Yo, r4.items = Go.getTagsItems(e3, n2, r4, s3, o3), i3.push(r4);
              }
              return i3;
            }
            static getTagsItems(e3, t3, r3, n2, o3) {
              let i3;
              i3 = void 0 === n2 ? Object.keys(t3) : n2.tags;
              const s3 = i3.map(((e4) => t3[e4] ? (t3[e4].used = true, t3[e4]) : (console.warn(`Non-existing tag "${e4}" is added to the group "${n2.name}"`), null))), a3 = [];
              for (const t4 of s3) {
                if (!t4) continue;
                const n3 = new Mo("tag", t4, r3);
                if (n3.depth = Yo + 1, "" === t4.name) {
                  const r4 = [...Go.addMarkdownItems(t4.description || "", n3, n3.depth + 1, o3), ...this.getOperationsItems(e3, void 0, t4, n3.depth + 1, o3)];
                  a3.push(...r4);
                  continue;
                }
                const i4 = this.getTagRelatedSchema({ parser: e3, tag: t4, parent: n3, schemaDefinitionsTagName: o3.schemaDefinitionsTagName });
                n3.items = [...i4, ...Go.addMarkdownItems(t4.description || "", n3, n3.depth + 1, o3), ...this.getOperationsItems(e3, n3, t4, n3.depth + 1, o3)], a3.push(n3);
              }
              return o3.sortTagsAlphabetically && a3.sort(qt("name")), a3;
            }
            static getOperationsItems(e3, t3, r3, n2, o3) {
              if (0 === r3.operations.length) return [];
              const i3 = [];
              for (const s3 of r3.operations) {
                const r4 = new yo(e3, s3, t3, o3);
                r4.depth = n2, i3.push(r4);
              }
              return o3.sortOperationsAlphabetically && i3.sort(qt("name")), i3;
            }
            static getTagsWithOperations(e3, t3) {
              const { spec: r3 } = e3, n2 = {}, o3 = r3["x-webhooks"] || r3.webhooks;
              for (const e4 of t3 || []) n2[e4.name] = Jo(Ho({}, e4), { operations: [] });
              function i3(e4, t4, r4) {
                for (const o4 of Object.keys(t4)) {
                  const s3 = t4[o4], a3 = Object.keys(s3).filter(Me2);
                  for (const t5 of a3) {
                    const a4 = s3[t5];
                    if (s3.$ref) {
                      const { resolved: t6 } = e4.deref(s3);
                      i3(e4, { [o4]: t6 }, r4);
                      continue;
                    }
                    let l3 = null == a4 ? void 0 : a4.tags;
                    l3 && l3.length || (l3 = [""]);
                    for (const e5 of l3) {
                      let i4 = n2[e5];
                      void 0 === i4 && (i4 = { name: e5, operations: [] }, n2[e5] = i4), i4["x-traitTag"] || i4.operations.push(Jo(Ho({}, a4), { pathName: o4, pointer: Ee2.compile(["paths", o4, t5]), httpVerb: t5, pathParameters: s3.parameters || [], pathServers: s3.servers, isWebhook: !!r4 }));
                    }
                  }
                }
              }
              return o3 && i3(e3, o3, true), r3.paths && i3(e3, r3.paths), n2;
            }
            static getTagRelatedSchema({ parser: e3, tag: t3, parent: r3, schemaDefinitionsTagName: n2 }) {
              var o3;
              const i3 = n2 ? [n2] : [];
              return Object.entries((null == (o3 = e3.spec.components) ? void 0 : o3.schemas) || {}).map((([e4, n3]) => {
                const o4 = n3["x-tags"] || i3;
                if (!(null == o4 ? void 0 : o4.includes(t3.name))) return null;
                const s3 = new Mo("schema", { name: e4, "x-displayName": `${n3.title || e4}`, description: `<SchemaDefinition showWriteOnly={true} schemaRef="#/components/schemas/${e4}" />` }, r3);
                return s3.depth = r3.depth + 1, s3;
              })).filter(Boolean);
            }
          }
          var Zo = Object.defineProperty, Xo = Object.getOwnPropertyDescriptor, Ko = (e3, t3, r3, n2) => {
            for (var o3, i3 = n2 > 1 ? void 0 : n2 ? Xo(t3, r3) : t3, s3 = e3.length - 1; s3 >= 0; s3--) (o3 = e3[s3]) && (i3 = (n2 ? o3(t3, r3, i3) : o3(i3)) || i3);
            return n2 && i3 && Zo(t3, r3, i3), i3;
          };
          const ei = "data-section-id";
          class ti {
            constructor(e3, t3, r3) {
              this.scroll = t3, this.history = r3, this.activeItemIdx = -1, this.sideBarOpened = false, this.updateOnScroll = (e4) => {
                const t4 = e4 ? 1 : -1;
                let r4 = this.activeItemIdx;
                for (; (-1 !== r4 || e4) && !(r4 >= this.flatItems.length - 1 && e4); ) {
                  if (e4) {
                    const e5 = this.getElementAtOrFirstChild(r4 + 1);
                    if (this.scroll.isElementBellow(e5)) break;
                  } else {
                    const e5 = this.getElementAt(r4);
                    if (this.scroll.isElementAbove(e5)) break;
                  }
                  r4 += t4;
                }
                this.activate(this.flatItems[r4], true, true);
              }, this.updateOnHistory = (e4 = this.history.currentId) => {
                if (!e4) return;
                let t4;
                t4 = this.flatItems.find(((t5) => t5.id === e4)), t4 ? this.activateAndScroll(t4, false) : (e4.startsWith(ft2) && (t4 = this.flatItems.find(((e5) => ft2.startsWith(e5.id))), this.activateAndScroll(t4, false)), this.scroll.scrollIntoViewBySelector(`[${ei}="${C2(e4)}"]`));
              }, this.getItemById = (e4) => this.flatItems.find(((t4) => t4.id === e4)), (0, de2.makeObservable)(this), this.items = e3.contentItems, this.flatItems = m2(this.items || [], "items"), this.flatItems.forEach(((e4, t4) => e4.absoluteIdx = t4)), this.subscribe();
            }
            static updateOnHistory(e3 = Wt.currentId, t3) {
              e3 && t3.scrollIntoViewBySelector(`[${ei}="${C2(e3)}"]`);
            }
            subscribe() {
              this._unsubscribe = this.scroll.subscribe(this.updateOnScroll), this._hashUnsubscribe = this.history.subscribe(this.updateOnHistory);
            }
            toggleSidebar() {
              this.sideBarOpened = !this.sideBarOpened;
            }
            closeSidebar() {
              this.sideBarOpened = false;
            }
            getElementAt(e3) {
              const t3 = this.flatItems[e3];
              return t3 && a2(`[${ei}="${C2(t3.id)}"]`) || null;
            }
            getElementAtOrFirstChild(e3) {
              let t3 = this.flatItems[e3];
              return t3 && "group" === t3.type && (t3 = t3.items[0]), t3 && a2(`[${ei}="${C2(t3.id)}"]`) || null;
            }
            get activeItem() {
              return this.flatItems[this.activeItemIdx] || void 0;
            }
            activate(e3, t3 = true, r3 = false) {
              if ((this.activeItem && this.activeItem.id) !== (e3 && e3.id) && (!e3 || "group" !== e3.type)) {
                if (this.deactivate(this.activeItem), !e3) return this.activeItemIdx = -1, void this.history.replace("", r3);
                e3.depth <= Yo || (this.activeItemIdx = e3.absoluteIdx, t3 && this.history.replace(encodeURI(e3.id), r3), e3.activate(), e3.expand());
              }
            }
            deactivate(e3) {
              if (void 0 !== e3) for (e3.deactivate(); void 0 !== e3; ) e3.collapse(), e3 = e3.parent;
            }
            activateAndScroll(e3, t3, r3) {
              const n2 = e3 && this.getItemById(e3.id) || e3;
              this.activate(n2, t3, r3), this.scrollToActive(), n2 && n2.items.length || this.closeSidebar();
            }
            scrollToActive() {
              this.scroll.scrollIntoView(this.getElementAt(this.activeItemIdx));
            }
            dispose() {
              this._unsubscribe(), this._hashUnsubscribe();
            }
          }
          Ko([de2.observable], ti.prototype, "activeItemIdx", 2), Ko([de2.observable], ti.prototype, "sideBarOpened", 2), Ko([de2.action], ti.prototype, "toggleSidebar", 1), Ko([de2.action], ti.prototype, "closeSidebar", 1), Ko([de2.action], ti.prototype, "activate", 1), Ko([de2.action.bound], ti.prototype, "activateAndScroll", 1);
          var ri = Object.defineProperty, ni = Object.getOwnPropertyDescriptor;
          const oi = "scroll";
          class ii {
            constructor(e3) {
              this.options = e3, this._prevOffsetY = 0, this._scrollParent = s2 ? window : void 0, this._emiter = new ve2.EventEmitter(), this.bind();
            }
            bind() {
              this._prevOffsetY = this.scrollY(), this._scrollParent && this._scrollParent.addEventListener("scroll", this.handleScroll);
            }
            dispose() {
              this._scrollParent && this._scrollParent.removeEventListener("scroll", this.handleScroll), this._emiter.removeAllListeners(oi);
            }
            scrollY() {
              return "undefined" != typeof HTMLElement && this._scrollParent instanceof HTMLElement ? this._scrollParent.scrollTop : void 0 !== this._scrollParent ? this._scrollParent.pageYOffset : 0;
            }
            isElementBellow(e3) {
              if (null !== e3) return e3.getBoundingClientRect().top > this.options.scrollYOffset();
            }
            isElementAbove(e3) {
              if (null === e3) return;
              const t3 = e3.getBoundingClientRect().top;
              return (t3 > 0 ? Math.floor(t3) : Math.ceil(t3)) <= this.options.scrollYOffset();
            }
            subscribe(e3) {
              const t3 = this._emiter.addListener(oi, e3);
              return () => t3.removeListener(oi, e3);
            }
            scrollIntoView(e3) {
              null !== e3 && (e3.scrollIntoView(), this._scrollParent && this._scrollParent.scrollBy && this._scrollParent.scrollBy(0, 1 - this.options.scrollYOffset()));
            }
            scrollIntoViewBySelector(e3) {
              const t3 = a2(e3);
              this.scrollIntoView(t3);
            }
            handleScroll() {
              const e3 = this.scrollY() - this._prevOffsetY > 0;
              this._prevOffsetY = this.scrollY(), this._emiter.emit(oi, e3);
            }
          }
          ((e3, t3, r3) => {
            for (var n2, o3 = ni(t3, r3), i3 = e3.length - 1; i3 >= 0; i3--) (n2 = e3[i3]) && (o3 = n2(t3, r3, o3) || o3);
            o3 && ri(t3, r3, o3);
          })([xe2.bind, $t(100)], ii.prototype, "handleScroll");
          class si {
            constructor() {
              this.searchWorker = (function() {
                let e3;
                if (s2) try {
                  e3 = r2(988);
                } catch (t3) {
                  e3 = r2(353).Ay;
                }
                else e3 = r2(353).Ay;
                return new e3();
              })();
            }
            indexItems(e3) {
              const t3 = (e4) => {
                e4.forEach(((e5) => {
                  "group" !== e5.type && this.add(e5.name, (e5.description || "").concat(" ", e5.path || ""), e5.id), t3(e5.items);
                }));
              };
              t3(e3), this.searchWorker.done();
            }
            add(e3, t3, r3) {
              this.searchWorker.add(e3, t3, r3);
            }
            dispose() {
              this.searchWorker.terminate(), this.searchWorker.dispose();
            }
            search(e3) {
              return this.searchWorker.search(e3);
            }
            toJS() {
              return e3 = this, t3 = function* () {
                return this.searchWorker.toJS();
              }, new Promise(((r3, n2) => {
                var o3 = (e4) => {
                  try {
                    s3(t3.next(e4));
                  } catch (e5) {
                    n2(e5);
                  }
                }, i3 = (e4) => {
                  try {
                    s3(t3.throw(e4));
                  } catch (e5) {
                    n2(e5);
                  }
                }, s3 = (e4) => e4.done ? r3(e4.value) : Promise.resolve(e4.value).then(o3, i3);
                s3((t3 = t3.apply(e3, null)).next());
              }));
              var e3, t3;
            }
            load(e3) {
              this.searchWorker.load(e3);
            }
            fromExternalJS(e3, t3) {
              e3 && t3 && this.searchWorker.fromExternalJS(e3, t3);
            }
          }
          const ai = ee2.div`
  width: calc(100% - ${(e3) => e3.theme.rightPanel.width});
  padding: 0 ${(e3) => e3.theme.spacing.sectionHorizontal}px;

  ${({ $compact: e3, theme: t3 }) => K2.lessThan("medium", true)`
    width: 100%;
    padding: ${`${e3 ? 0 : t3.spacing.sectionVertical}px ${t3.spacing.sectionHorizontal}px`};
  `};
`, li = ee2.div.attrs(((e3) => ({ [ei]: e3.id })))`
  padding: ${(e3) => e3.theme.spacing.sectionVertical}px 0;

  &:last-child {
    min-height: calc(100vh + 1px);
  }

  & > &:last-child {
    min-height: initial;
  }

  ${K2.lessThan("medium", true)`
    padding: 0;
  `}
  ${({ $underlined: e3 }) => e3 ? "\n    position: relative;\n\n    &:not(:last-of-type):after {\n      position: absolute;\n      bottom: 0;\n      width: 100%;\n      display: block;\n      content: '';\n      border-bottom: 1px solid rgba(0, 0, 0, 0.2);\n    }\n  " : ""}
`, ci = ee2.div`
  width: ${(e3) => e3.theme.rightPanel.width};
  color: ${({ theme: e3 }) => e3.rightPanel.textColor};
  background-color: ${(e3) => e3.theme.rightPanel.backgroundColor};
  padding: 0 ${(e3) => e3.theme.spacing.sectionHorizontal}px;

  ${K2.lessThan("medium", true)`
    width: 100%;
    padding: ${(e3) => `${e3.theme.spacing.sectionVertical}px ${e3.theme.spacing.sectionHorizontal}px`};
  `};
`, pi = ee2(ci)`
  background-color: ${(e3) => e3.theme.rightPanel.backgroundColor};
`, ui = ee2.div`
  display: flex;
  width: 100%;
  padding: 0;

  ${K2.lessThan("medium", true)`
    flex-direction: column;
  `};
`, di = { 1: "1.85714em", 2: "1.57143em", 3: "1.27em" }, hi = (e3) => Y2`
  font-family: ${({ theme: e4 }) => e4.typography.headings.fontFamily};
  font-weight: ${({ theme: e4 }) => e4.typography.headings.fontWeight};
  font-size: ${di[e3]};
  line-height: ${({ theme: e4 }) => e4.typography.headings.lineHeight};
`, mi = ee2.h1`
  ${hi(1)};
  color: ${({ theme: e3 }) => e3.colors.text.primary};

  ${te2("H1")};
`, fi = ee2.h2`
  ${hi(2)};
  color: ${({ theme: e3 }) => e3.colors.text.primary};
  margin: 0 0 20px;

  ${te2("H2")};
`, gi = ee2.h2`
  ${hi(3)};
  color: ${({ theme: e3 }) => e3.colors.text.primary};

  ${te2("H3")};
`, yi = ee2.h3`
  color: ${({ theme: e3 }) => e3.rightPanel.textColor};

  ${te2("RightPanelHeader")};
`, bi = ee2.h5`
  border-bottom: 1px solid rgba(38, 50, 56, 0.3);
  margin: 1em 0 1em 0;
  color: rgba(38, 50, 56, 0.5);
  font-weight: normal;
  text-transform: uppercase;
  font-size: 0.929em;
  line-height: 20px;

  ${te2("UnderlinedHeader")};
`;
          var xi = (e3, t3, r3) => new Promise(((n2, o3) => {
            var i3 = (e4) => {
              try {
                a3(r3.next(e4));
              } catch (e5) {
                o3(e5);
              }
            }, s3 = (e4) => {
              try {
                a3(r3.throw(e4));
              } catch (e5) {
                o3(e5);
              }
            }, a3 = (e4) => e4.done ? n2(e4.value) : Promise.resolve(e4.value).then(i3, s3);
            a3((r3 = r3.apply(e3, t3)).next());
          }));
          const vi = (0, e2.createContext)(void 0), { Provider: wi, Consumer: ki } = vi;
          function Ei(t3) {
            const { spec: r3, specUrl: n2, options: o3, onLoaded: i3, children: s3 } = t3, [a3, l3] = e2.useState(null), [c3, p3] = e2.useState(null);
            if (c3) throw c3;
            e2.useEffect((() => {
              !(function() {
                xi(this, null, (function* () {
                  if (r3 || n2) {
                    l3(null);
                    try {
                      const e3 = yield ye2(r3 || n2);
                      l3(e3);
                    } catch (e3) {
                      throw i3 && i3(e3), p3(e3), e3;
                    }
                  }
                }));
              })();
            }), [r3, n2]);
            const u3 = e2.useMemo((() => {
              if (!a3) return null;
              try {
                return new mc(a3, n2, o3);
              } catch (e3) {
                throw i3 && i3(e3), e3;
              }
            }), [a3, n2, o3]);
            return e2.useEffect((() => {
              u3 && i3 && i3();
            }), [u3, i3]), s3({ loading: !u3, store: u3 });
          }
          function Oi() {
            return (0, e2.useContext)(vi);
          }
          const ji = (e3) => Y2`
  ${e3} {
    cursor: pointer;
    margin-left: -20px;
    padding: 0;
    line-height: 1;
    width: 20px;
    display: inline-block;
    outline: 0;
  }
  ${e3}:before {
    content: '';
    width: 15px;
    height: 15px;
    background-size: contain;
    background-image: url('data:image/svg+xml;base64,');
    opacity: 0.5;
    visibility: hidden;
    display: inline-block;
    vertical-align: middle;
  }

  h1:hover > ${e3}::before, h2:hover > ${e3}::before, ${e3}:hover::before {
    visibility: visible;
  }
`, $i = ee2((function(t3) {
            const r3 = e2.useContext(vi), n2 = e2.useCallback(((e3) => {
              r3 && (function(e4, t4, r4) {
                t4.defaultPrevented || 0 !== t4.button || ((e5) => !!(e5.metaKey || e5.altKey || e5.ctrlKey || e5.shiftKey))(t4) || (t4.preventDefault(), e4.replace(encodeURI(r4)));
              })(r3.menu.history, e3, t3.to);
            }), [r3, t3.to]);
            return r3 ? e2.createElement("a", { className: t3.className, href: r3.menu.history.linkForId(t3.to), onClick: n2, "aria-label": t3.to }, t3.children) : null;
          }))`
  ${ji("&")};
`;
          function Si(t3) {
            return e2.createElement($i, { to: t3.to });
          }
          const Ci = { left: "90deg", right: "-90deg", up: "-180deg", down: "0" }, Pi = ee2(((t3) => e2.createElement("svg", { className: t3.className, style: t3.style, version: "1.1", viewBox: "0 0 24 24", x: "0", xmlns: "http://www.w3.org/2000/svg", y: "0", "aria-hidden": "true" }, e2.createElement("polygon", { points: "17.3 8.3 12 13.6 6.7 8.3 5.3 9.7 12 16.4 18.7 9.7 " }))))`
  height: ${(e3) => e3.size || "18px"};
  width: ${(e3) => e3.size || "18px"};
  min-width: ${(e3) => e3.size || "18px"};
  vertical-align: middle;
  float: ${(e3) => e3.float || ""};
  transition: transform 0.2s ease-out;
  transform: rotateZ(${(e3) => Ci[e3.direction || "down"]});

  polygon {
    fill: ${({ color: e3, theme: t3 }) => e3 && t3.colors.responses[e3] && t3.colors.responses[e3].color || e3};
  }
`, Ii = ee2.span`
  display: inline-block;
  padding: 2px 8px;
  margin: 0;
  background-color: ${(e3) => e3.color || e3.theme.colors[e3.type].main};
  color: ${(e3) => e3.theme.colors[e3.type].contrastText};
  font-size: ${(e3) => e3.theme.typography.code.fontSize};
  vertical-align: middle;
  line-height: 1.6;
  border-radius: 4px;
  font-weight: ${({ theme: e3 }) => e3.typography.fontWeightBold};
  font-size: 12px;
  + span[type] {
    margin-left: 4px;
  }
`, Ai = Y2`
  text-decoration: line-through;
  color: #707070;
`, Ti = ee2.caption`
  text-align: right;
  font-size: 0.9em;
  font-weight: normal;
  color: ${(e3) => e3.theme.colors.text.secondary};
`, _i = ee2.td`
  border-left: 1px solid ${(e3) => e3.theme.schema.linesColor};
  box-sizing: border-box;
  position: relative;
  padding: 10px 10px 10px 0;

  ${K2.lessThan("small")`
    display: block;
    overflow: hidden;
  `}

  tr:first-of-type > &,
  tr.last > & {
    border-left-width: 0;
    background-position: top left;
    background-repeat: no-repeat;
    background-size: 1px 100%;
  }

  tr:first-of-type > & {
    background-image: linear-gradient(
      to bottom,
      transparent 0%,
      transparent 22px,
      ${(e3) => e3.theme.schema.linesColor} 22px,
      ${(e3) => e3.theme.schema.linesColor} 100%
    );
  }

  tr.last > & {
    background-image: linear-gradient(
      to bottom,
      ${(e3) => e3.theme.schema.linesColor} 0%,
      ${(e3) => e3.theme.schema.linesColor} 22px,
      transparent 22px,
      transparent 100%
    );
  }

  tr.last + tr > & {
    border-left-color: transparent;
  }

  tr.last:first-child > & {
    background: none;
    border-left-color: transparent;
  }
`, Ri = ee2(_i)`
  padding: 0;
`, Di = ee2(_i)`
  vertical-align: top;
  line-height: 20px;
  white-space: nowrap;
  font-size: 13px;
  font-family: ${(e3) => e3.theme.typography.code.fontFamily};

  &.deprecated {
    ${Ai};
  }

  ${({ kind: e3 }) => "patternProperties" === e3 && Y2`
      > span.property-name {
        display: inline-table;
        white-space: break-spaces;
        margin-right: 20px;

        ::before,
        ::after {
          content: '/';
          filter: opacity(0.2);
        }
      }
    `}

  ${({ kind: e3 = "" }) => ["field", "additionalProperties", "patternProperties"].includes(e3) ? "" : "font-style: italic"};

  ${te2("PropertyNameCell")};
`, zi = ee2.td`
  border-bottom: 1px solid #9fb4be;
  padding: 10px 0;
  width: ${(e3) => e3.theme.schema.defaultDetailsWidth};
  box-sizing: border-box;

  tr.expanded & {
    border-bottom: none;
  }

  ${K2.lessThan("small")`
    padding: 0 20px;
    border-bottom: none;
    border-left: 1px solid ${(e3) => e3.theme.schema.linesColor};

    tr.last > & {
      border-left: none;
    }
  `}

  ${te2("PropertyDetailsCell")};
`, Li = ee2.span`
  color: ${(e3) => e3.theme.schema.linesColor};
  font-family: ${(e3) => e3.theme.typography.code.fontFamily};
  margin-right: 10px;

  &::before {
    content: '';
    display: inline-block;
    vertical-align: middle;
    width: 10px;
    height: 1px;
    background: ${(e3) => e3.theme.schema.linesColor};
  }

  &::after {
    content: '';
    display: inline-block;
    vertical-align: middle;
    width: 1px;
    background: ${(e3) => e3.theme.schema.linesColor};
    height: 7px;
  }
`, Ni = ee2.div`
  padding: ${({ theme: e3 }) => e3.schema.nestingSpacing};
`, Mi = ee2.table`
  border-collapse: separate;
  border-radius: 3px;
  font-size: ${(e3) => e3.theme.typography.fontSize};

  border-spacing: 0;
  width: 100%;

  > tr {
    vertical-align: middle;
  }

  ${K2.lessThan("small")`
    display: block;
    > tr, > tbody > tr {
      display: block;
    }
  `}

  ${K2.lessThan("small", false, " and (-ms-high-contrast:none)")`
    td {
      float: left;
      width: 100%;
    }
  `}

  &
    ${Ni},
    &
    ${Ni}
    ${Ni}
    ${Ni},
    &
    ${Ni}
    ${Ni}
    ${Ni}
    ${Ni}
    ${Ni} {
    margin: ${({ theme: e3 }) => e3.schema.nestingSpacing};
    margin-right: 0;
    background: ${({ theme: e3 }) => e3.schema.nestedBackground};
  }

  &
    ${Ni}
    ${Ni},
    &
    ${Ni}
    ${Ni}
    ${Ni}
    ${Ni},
    &
    ${Ni}
    ${Ni}
    ${Ni}
    ${Ni}
    ${Ni}
    ${Ni} {
    background: #ffffff;
  }
`, qi = ee2.div`
  margin: 0 0 3px 0;
  display: inline-block;
`, Fi = ee2.span`
  font-size: 0.9em;
  margin-right: 10px;
  color: ${(e3) => e3.theme.colors.primary.main};
  font-family: ${(e3) => e3.theme.typography.headings.fontFamily};
}
`, Bi = ee2.button`
  display: inline-block;
  margin-right: 10px;
  margin-bottom: 5px;
  font-size: 0.8em;
  cursor: pointer;
  border: 1px solid ${(e3) => e3.theme.colors.primary.main};
  padding: 2px 10px;
  line-height: 1.5em;
  outline: none;
  &:focus {
    box-shadow: 0 0 0 1px ${(e3) => e3.theme.colors.primary.main};
  }

  ${({ $deprecated: e3 }) => e3 && Ai || ""};

  ${(e3) => e3.$active ? `
      color: white;
      background-color: ${e3.theme.colors.primary.main};
      &:focus {
        box-shadow: none;
        background-color: ${(0, t2.darken)(0.15, e3.theme.colors.primary.main)};
      }
      ` : `
        color: ${e3.theme.colors.primary.main};
        background-color: white;
      `}
`, Qi = ee2.div`
  font-size: 0.9em;
  font-family: ${(e3) => e3.theme.typography.code.fontFamily};
  &::after {
    content: ' [';
  }
`, Ui = ee2.div`
  font-size: 0.9em;
  font-family: ${(e3) => e3.theme.typography.code.fontFamily};
  &::after {
    content: ']';
  }
`;
          var Wi = (init_esm(), __toCommonJS(esm_exports));
          const Vi = ee2(Wi.Tabs)`
  > ul {
    list-style: none;
    padding: 0;
    margin: 0;
    margin: 0 -5px;

    > li {
      padding: 5px 10px;
      display: inline-block;

      background-color: ${({ theme: e3 }) => e3.codeBlock.backgroundColor};
      border-bottom: 1px solid rgba(0, 0, 0, 0.5);
      cursor: pointer;
      text-align: center;
      outline: none;
      color: ${({ theme: e3 }) => (0, t2.darken)(e3.colors.tonalOffset, e3.rightPanel.textColor)};
      margin: 0
        ${({ theme: e3 }) => `${e3.spacing.unit}px ${e3.spacing.unit}px ${e3.spacing.unit}px`};
      border: 1px solid ${({ theme: e3 }) => (0, t2.darken)(0.05, e3.codeBlock.backgroundColor)};
      border-radius: 5px;
      min-width: 60px;
      font-size: 0.9em;
      font-weight: bold;

      &.react-tabs__tab--selected {
        color: ${(e3) => e3.theme.colors.text.primary};
        background: ${({ theme: e3 }) => e3.rightPanel.textColor};
        &:focus {
          outline: auto;
        }
      }

      &:only-child {
        flex: none;
        min-width: 100px;
      }

      &.tab-success {
        color: ${(e3) => e3.theme.colors.responses.success.tabTextColor};
      }

      &.tab-redirect {
        color: ${(e3) => e3.theme.colors.responses.redirect.tabTextColor};
      }

      &.tab-info {
        color: ${(e3) => e3.theme.colors.responses.info.tabTextColor};
      }

      &.tab-error {
        color: ${(e3) => e3.theme.colors.responses.error.tabTextColor};
      }
    }
  }
  > .react-tabs__tab-panel {
    background: ${({ theme: e3 }) => e3.codeBlock.backgroundColor};
    & > div,
    & > pre {
      padding: ${(e3) => 4 * e3.theme.spacing.unit}px;
      margin: 0;
    }

    & > div > pre {
      padding: 0;
    }
  }
`, Hi = (ee2(Vi)`
  > ul {
    display: block;
    > li {
      padding: 2px 5px;
      min-width: auto;
      margin: 0 15px 0 0;
      font-size: 13px;
      font-weight: normal;
      border-bottom: 1px dashed;
      color: ${({ theme: e3 }) => (0, t2.darken)(e3.colors.tonalOffset, e3.rightPanel.textColor)};
      border-radius: 0;
      background: none;

      &:last-child {
        margin-right: 0;
      }

      &.react-tabs__tab--selected {
        color: ${({ theme: e3 }) => e3.rightPanel.textColor};
        background: none;
      }
    }
  }
  > .react-tabs__tab-panel {
    & > div,
    & > pre {
      padding: ${(e3) => 2 * e3.theme.spacing.unit}px 0;
    }
  }
`, ee2.div`
  /**
  * Based on prism-dark.css
  */

  code[class*='language-'],
  pre[class*='language-'] {
    /* color: white;
    background: none; */
    text-shadow: 0 -0.1em 0.2em black;
    text-align: left;
    white-space: pre;
    word-spacing: normal;
    word-break: normal;
    word-wrap: normal;
    line-height: 1.5;

    -moz-tab-size: 4;
    -o-tab-size: 4;
    tab-size: 4;

    -webkit-hyphens: none;
    -moz-hyphens: none;
    -ms-hyphens: none;
    hyphens: none;
  }

  @media print {
    code[class*='language-'],
    pre[class*='language-'] {
      text-shadow: none;
    }
  }

  /* Code blocks */
  pre[class*='language-'] {
    padding: 1em;
    margin: 0.5em 0;
    overflow: auto;
  }

  .token.comment,
  .token.prolog,
  .token.doctype,
  .token.cdata {
    color: hsl(30, 20%, 50%);
  }

  .token.punctuation {
    opacity: 0.7;
  }

  .namespace {
    opacity: 0.7;
  }

  .token.property,
  .token.tag,
  .token.number,
  .token.constant,
  .token.symbol {
    color: #4a8bb3;
  }

  .token.boolean {
    color: #e64441;
  }

  .token.selector,
  .token.attr-name,
  .token.string,
  .token.char,
  .token.builtin,
  .token.inserted {
    color: #a0fbaa;
    & + a,
    & + a:visited {
      color: #4ed2ba;
      text-decoration: underline;
    }
  }

  .token.property.string {
    color: white;
  }

  .token.operator,
  .token.entity,
  .token.url,
  .token.variable {
    color: hsl(40, 90%, 60%);
  }

  .token.atrule,
  .token.attr-value,
  .token.keyword {
    color: hsl(350, 40%, 70%);
  }

  .token.regex,
  .token.important {
    color: #e90;
  }

  .token.important,
  .token.bold {
    font-weight: bold;
  }
  .token.italic {
    font-style: italic;
  }

  .token.entity {
    cursor: help;
  }

  .token.deleted {
    color: red;
  }

  ${te2("Prism")};
`), Ji = ee2.div`
  opacity: 0.7;
  transition: opacity 0.3s ease;
  text-align: right;
  &:focus-within {
    opacity: 1;
  }
  > button {
    background-color: transparent;
    border: 0;
    color: inherit;
    padding: 2px 10px;
    font-family: ${({ theme: e3 }) => e3.typography.fontFamily};
    font-size: ${({ theme: e3 }) => e3.typography.fontSize};
    line-height: ${({ theme: e3 }) => e3.typography.lineHeight};
    cursor: pointer;
    outline: 0;

    :hover,
    :focus {
      background: rgba(255, 255, 255, 0.1);
    }
  }
`, Yi = ee2.div`
  &:hover ${Ji} {
    opacity: 1;
  }
`, Gi = ee2(Hi).attrs({ as: "pre" })`
  font-family: ${(e3) => e3.theme.typography.code.fontFamily};
  font-size: ${(e3) => e3.theme.typography.code.fontSize};
  overflow-x: auto;
  margin: 0;

  white-space: ${({ theme: e3 }) => e3.typography.code.wrap ? "pre-wrap" : "pre"};
`;
          var Zi = (init_perfect_scrollbar_esm(), __toCommonJS(perfect_scrollbar_esm_exports)), Xi = r2.n(Zi), Ki = Object.defineProperty, es = Object.getOwnPropertySymbols, ts = Object.prototype.hasOwnProperty, rs = Object.prototype.propertyIsEnumerable, ns = (e3, t3, r3) => t3 in e3 ? Ki(e3, t3, { enumerable: true, configurable: true, writable: true, value: r3 }) : e3[t3] = r3;
          const os = Xi() || Zi;
          let is2 = "";
          s2 && (is2 = r2(494), is2 = "function" == typeof is2.toString && is2.toString() || "", is2 = "[object Object]" === is2 ? "" : is2);
          const ss = G2`${is2}`, as = ee2.div`
  position: relative;
`;
          class ls extends e2.Component {
            constructor() {
              super(...arguments), this.handleRef = (e3) => {
                this._container = e3;
              };
            }
            componentDidMount() {
              const e3 = this._container.parentElement && this._container.parentElement.scrollTop || 0;
              this.inst = new os(this._container, this.props.options || {}), this._container.scrollTo && this._container.scrollTo(0, e3);
            }
            componentDidUpdate() {
              this.inst.update();
            }
            componentWillUnmount() {
              this.inst.destroy();
            }
            render() {
              const { children: t3, className: r3, updateFn: n2 } = this.props;
              return n2 && n2(this.componentDidUpdate.bind(this)), e2.createElement(e2.Fragment, null, is2 && e2.createElement(ss, null), e2.createElement(as, { className: `scrollbar-container ${r3}`, ref: this.handleRef }, t3));
            }
          }
          function cs(t3) {
            return e2.createElement(ce2.Consumer, null, ((r3) => r3.nativeScrollbars ? e2.createElement("div", { style: { overflow: "auto", overscrollBehavior: "contain", msOverflowStyle: "-ms-autohiding-scrollbar" } }, t3.children) : e2.createElement(ls, ((e3, t4) => {
              for (var r4 in t4 || (t4 = {})) ts.call(t4, r4) && ns(e3, r4, t4[r4]);
              if (es) for (var r4 of es(t4)) rs.call(t4, r4) && ns(e3, r4, t4[r4]);
              return e3;
            })({}, t3), t3.children)));
          }
          const ps = ee2((({ className: t3, style: r3 }) => e2.createElement("svg", { className: t3, style: r3, xmlns: "http://www.w3.org/2000/svg", width: "16", height: "16", viewBox: "0 0 24 24", fill: "none", stroke: "currentColor", strokeWidth: "2", strokeLinecap: "round", strokeLinejoin: "round" }, e2.createElement("polyline", { points: "6 9 12 15 18 9" }))))`
  position: absolute;
  pointer-events: none;
  z-index: 1;
  top: 50%;
  -webkit-transform: translateY(-50%);
  -ms-transform: translateY(-50%);
  transform: translateY(-50%);
  right: 8px;
  margin: auto;
  text-align: center;
  polyline {
    color: ${(e3) => "dark" === e3.variant && "white"};
  }
`, us = e2.memo(((t3) => {
            const { options: r3, onChange: n2, placeholder: o3, value: i3 = "", variant: s3, className: a3 } = t3;
            return e2.createElement("div", { className: a3 }, e2.createElement(ps, { variant: s3 }), e2.createElement("select", { onChange: (e3) => {
              const { selectedIndex: t4 } = e3.target;
              n2(r3[o3 ? t4 - 1 : t4]);
            }, value: i3, className: "dropdown-select" }, o3 && e2.createElement("option", { disabled: true, hidden: true, value: o3 }, o3), r3.map((({ idx: t4, value: r4, title: n3 }, o4) => e2.createElement("option", { key: t4 || r4 + o4, value: r4 }, n3 || r4)))), e2.createElement("label", null, i3));
          })), ds = H2()(us)`
  label {
    box-sizing: border-box;
    min-width: 100px;
    outline: none;
    display: inline-block;
    font-family: ${(e3) => e3.theme.typography.headings.fontFamily};
    color: ${({ theme: e3 }) => e3.colors.text.primary};
    vertical-align: bottom;
    width: ${({ fullWidth: e3 }) => e3 ? "100%" : "auto"};
    text-transform: none;
    padding: 0 22px 0 4px;

    font-size: 0.929em;
    line-height: 1.5em;
    font-family: inherit;
    text-overflow: ellipsis;
    overflow: hidden;
    white-space: nowrap;
  }
  .dropdown-select {
    position: absolute;
    top: 0;
    left: 0;
    width: 100%;
    height: 100%;
    opacity: 0;
    border: none;
    appearance: none;
    cursor: pointer;

    color: ${({ theme: e3 }) => e3.colors.text.primary};
    line-height: inherit;
    font-family: inherit;
  }
  box-sizing: border-box;
  min-width: 100px;
  outline: none;
  display: inline-block;
  border-radius: 2px;
  border: 1px solid rgba(38, 50, 56, 0.5);
  vertical-align: bottom;
  padding: 2px 0px 2px 6px;
  position: relative;
  width: auto;
  background: white;
  color: #263238;
  font-family: ${(e3) => e3.theme.typography.headings.fontFamily};
  font-size: 0.929em;
  line-height: 1.5em;
  cursor: pointer;
  transition: border 0.25s ease, color 0.25s ease, box-shadow 0.25s ease;

  &:hover,
  &:focus-within {
    border: 1px solid ${(e3) => e3.theme.colors.primary.main};
    color: ${(e3) => e3.theme.colors.primary.main};
    box-shadow: 0px 0px 0px 1px ${(e3) => e3.theme.colors.primary.main};
  }
`, hs = H2()(ds)`
  margin-left: 10px;
  text-transform: none;
  font-size: 0.969em;

  font-size: 1em;
  border: none;
  padding: 0 1.2em 0 0;
  background: transparent;

  &:hover,
  &:focus-within {
    border: none;
    box-shadow: none;
    label {
      color: ${(e3) => e3.theme.colors.primary.main};
      text-shadow: 0px 0px 0px ${(e3) => e3.theme.colors.primary.main};
    }
  }
`, ms = H2().span`
  margin-left: 10px;
  text-transform: none;
  font-size: 0.929em;
  color: black;
`;
          var fs = Object.defineProperty, gs = Object.getOwnPropertySymbols, ys = Object.prototype.hasOwnProperty, bs = Object.prototype.propertyIsEnumerable, xs = (e3, t3, r3) => t3 in e3 ? fs(e3, t3, { enumerable: true, configurable: true, writable: true, value: r3 }) : e3[t3] = r3, vs = (e3, t3) => {
            for (var r3 in t3 || (t3 = {})) ys.call(t3, r3) && xs(e3, r3, t3[r3]);
            if (gs) for (var r3 of gs(t3)) bs.call(t3, r3) && xs(e3, r3, t3[r3]);
            return e3;
          };
          function ws(t3) {
            const { Label: r3 = ms, Dropdown: n2 = hs } = t3;
            return 1 === t3.options.length ? e2.createElement(r3, null, t3.options[0].value) : e2.createElement(n2, vs({}, t3));
          }
          var ks = require_purify_cjs(), Es = r2.n(ks);
          const Os = Y2`
  a {
    text-decoration: ${(e3) => e3.theme.typography.links.textDecoration};
    color: ${(e3) => e3.theme.typography.links.color};

    &:visited {
      color: ${(e3) => e3.theme.typography.links.visited};
    }

    &:hover {
      color: ${(e3) => e3.theme.typography.links.hover};
      text-decoration: ${(e3) => e3.theme.typography.links.hoverTextDecoration};
    }
  }
`, js = ee2(Hi)`
  font-family: ${(e3) => e3.theme.typography.fontFamily};
  font-weight: ${(e3) => e3.theme.typography.fontWeightRegular};
  line-height: ${(e3) => e3.theme.typography.lineHeight};

  p {
    &:last-child {
      margin-bottom: 0;
    }
  }

  ${({ $compact: e3 }) => e3 && "\n    p:first-child {\n      margin-top: 0;\n    }\n    p:last-child {\n      margin-bottom: 0;\n    }\n  "}

  ${({ $inline: e3 }) => e3 && " p {\n    display: inline-block;\n  }"}

  h1 {
    ${hi(1)};
    color: ${(e3) => e3.theme.colors.primary.main};
    margin-top: 0;
  }

  h2 {
    ${hi(2)};
    color: ${(e3) => e3.theme.colors.text.primary};
  }

  code {
    color: ${({ theme: e3 }) => e3.typography.code.color};
    background-color: ${({ theme: e3 }) => e3.typography.code.backgroundColor};

    font-family: ${(e3) => e3.theme.typography.code.fontFamily};
    border-radius: 2px;
    border: 1px solid rgba(38, 50, 56, 0.1);
    padding: 0 ${({ theme: e3 }) => e3.spacing.unit}px;
    font-size: ${(e3) => e3.theme.typography.code.fontSize};
    font-weight: ${({ theme: e3 }) => e3.typography.code.fontWeight};

    word-break: break-word;
  }

  pre {
    font-family: ${(e3) => e3.theme.typography.code.fontFamily};
    white-space: ${({ theme: e3 }) => e3.typography.code.wrap ? "pre-wrap" : "pre"};
    background-color: ${({ theme: e3 }) => e3.codeBlock.backgroundColor};
    color: white;
    padding: ${(e3) => 4 * e3.theme.spacing.unit}px;
    overflow-x: auto;
    line-height: normal;
    border-radius: 0;
    border: 1px solid rgba(38, 50, 56, 0.1);

    code {
      background-color: transparent;
      color: white;
      padding: 0;

      &:before,
      &:after {
        content: none;
      }
    }
  }

  blockquote {
    margin: 0;
    margin-bottom: 1em;
    padding: 0 15px;
    color: #777;
    border-left: 4px solid #ddd;
  }

  img {
    max-width: 100%;
    box-sizing: content-box;
  }

  ul,
  ol {
    padding-left: 2em;
    margin: 0;
    margin-bottom: 1em;

    ul,
    ol {
      margin-bottom: 0;
      margin-top: 0;
    }
  }

  table {
    display: block;
    width: 100%;
    overflow: auto;
    word-break: normal;
    word-break: keep-all;
    border-collapse: collapse;
    border-spacing: 0;
    margin-top: 1.5em;
    margin-bottom: 1.5em;
  }

  table tr {
    background-color: #fff;
    border-top: 1px solid #ccc;

    &:nth-child(2n) {
      background-color: ${({ theme: e3 }) => e3.schema.nestedBackground};
    }
  }

  table th,
  table td {
    padding: 6px 13px;
    border: 1px solid #ddd;
  }

  table th {
    text-align: left;
    font-weight: bold;
  }

  ${ji(".share-link")};

  ${Os}

  ${te2("Markdown")};
`;
          var $s = Object.defineProperty, Ss = Object.defineProperties, Cs = Object.getOwnPropertyDescriptors, Ps = Object.getOwnPropertySymbols, Is = Object.prototype.hasOwnProperty, As = Object.prototype.propertyIsEnumerable, Ts = (e3, t3, r3) => t3 in e3 ? $s(e3, t3, { enumerable: true, configurable: true, writable: true, value: r3 }) : e3[t3] = r3;
          const _s = Es(), Rs = H2()(js)`
  display: inline;
`;
          function Ds(t3) {
            var r3 = t3, { inline: n2, compact: o3 } = r3, i3 = ((e3, t4) => {
              var r4 = {};
              for (var n3 in e3) Is.call(e3, n3) && t4.indexOf(n3) < 0 && (r4[n3] = e3[n3]);
              if (null != e3 && Ps) for (var n3 of Ps(e3)) t4.indexOf(n3) < 0 && As.call(e3, n3) && (r4[n3] = e3[n3]);
              return r4;
            })(r3, ["inline", "compact"]);
            const s3 = n2 ? Rs : js;
            return e2.createElement(ue2, null, ((t4) => {
              return e2.createElement(s3, (r4 = ((e3, t5) => {
                for (var r5 in t5 || (t5 = {})) Is.call(t5, r5) && Ts(e3, r5, t5[r5]);
                if (Ps) for (var r5 of Ps(t5)) As.call(t5, r5) && Ts(e3, r5, t5[r5]);
                return e3;
              })({ className: "redoc-markdown " + (i3.className || ""), dangerouslySetInnerHTML: { __html: (a3 = t4.sanitize, l3 = i3.html, a3 ? _s.sanitize(l3) : l3) }, "data-role": i3["data-role"] }, i3), Ss(r4, Cs({ $inline: n2, $compact: o3 }))));
              var r4, a3, l3;
            }));
          }
          class zs extends e2.Component {
            render() {
              const { source: t3, inline: r3, compact: n2, className: o3, "data-role": i3 } = this.props, s3 = new Ur();
              return e2.createElement(Ds, { html: s3.renderMd(t3), inline: r3, compact: n2, className: o3, "data-role": i3 });
            }
          }
          const Ls = ee2.div`
  position: relative;
`, Ns = ee2.div`
  position: absolute;
  min-width: 80px;
  max-width: 500px;
  background: #fff;
  bottom: 100%;
  left: 50%;
  margin-bottom: 10px;
  transform: translateX(-50%);

  border-radius: 4px;
  padding: 0.3em 0.6em;
  text-align: center;
  box-shadow: 0px 0px 5px 0px rgba(204, 204, 204, 1);
`, Ms = ee2.div`
  background: #fff;
  color: #000;
  display: inline;
  font-size: 0.85em;
  white-space: nowrap;
`, qs = ee2.div`
  position: absolute;
  width: 0;
  height: 0;
  bottom: -5px;
  left: 50%;
  margin-left: -5px;
  border-left: solid transparent 5px;
  border-right: solid transparent 5px;
  border-top: solid #fff 5px;
`, Fs = ee2.div`
  position: absolute;
  width: 100%;
  height: 20px;
  bottom: -20px;
`;
          class Bs extends e2.Component {
            render() {
              const { open: t3, title: r3, children: n2 } = this.props;
              return e2.createElement(Ls, null, n2, t3 && e2.createElement(Ns, null, e2.createElement(Ms, null, r3), e2.createElement(qs, null), e2.createElement(Fs, null)));
            }
          }
          const Qs = "undefined" != typeof document && document.queryCommandSupported && document.queryCommandSupported("copy");
          class Us {
            static isSupported() {
              return Qs;
            }
            static selectElement(e3) {
              let t3, r3;
              document.body.createTextRange ? (t3 = document.body.createTextRange(), t3.moveToElementText(e3), t3.select()) : document.createRange && window.getSelection && (r3 = window.getSelection(), t3 = document.createRange(), t3.selectNodeContents(e3), r3.removeAllRanges(), r3.addRange(t3));
            }
            static deselect() {
              if (document.selection) document.selection.empty();
              else if (window.getSelection) {
                const e3 = window.getSelection();
                e3 && e3.removeAllRanges();
              }
            }
            static copySelected() {
              let e3;
              try {
                e3 = document.execCommand("copy");
              } catch (t3) {
                e3 = false;
              }
              return e3;
            }
            static copyElement(e3) {
              Us.selectElement(e3);
              const t3 = Us.copySelected();
              return t3 && Us.deselect(), t3;
            }
            static copyCustom(e3) {
              const t3 = document.createElement("textarea");
              t3.style.position = "fixed", t3.style.top = "0", t3.style.left = "0", t3.style.width = "2em", t3.style.height = "2em", t3.style.padding = "0", t3.style.border = "none", t3.style.outline = "none", t3.style.boxShadow = "none", t3.style.background = "transparent", t3.value = e3, document.body.appendChild(t3), t3.select();
              const r3 = Us.copySelected();
              return document.body.removeChild(t3), r3;
            }
          }
          const Ws = (t3) => {
            const [r3, n2] = e2.useState(false), o3 = () => {
              const e3 = "string" == typeof t3.data ? t3.data : JSON.stringify(t3.data, null, 2);
              Us.copyCustom(e3), i3();
            }, i3 = () => {
              n2(true), setTimeout((() => {
                n2(false);
              }), 1500);
            };
            return t3.children({ renderCopyButton: () => e2.createElement("button", { onClick: o3 }, e2.createElement(Bs, { title: Us.isSupported() ? "Copied" : "Not supported in your browser", open: r3 }, "Copy")) });
          };
          let Vs = 1;
          function Hs(e3, t3) {
            Vs = 1;
            let r3 = "";
            return r3 += '<div class="redoc-json">', r3 += "<code>", r3 += Xs(e3, t3), r3 += "</code>", r3 += "</div>", r3;
          }
          function Js(e3) {
            return void 0 !== e3 ? e3.toString().replace(/&/g, "&amp;").replace(/"/g, "&quot;").replace(/</g, "&lt;").replace(/>/g, "&gt;") : "";
          }
          function Ys(e3) {
            return JSON.stringify(e3).slice(1, -1);
          }
          function Gs(e3, t3) {
            return '<span class="' + t3 + '">' + Js(e3) + "</span>";
          }
          function Zs(e3) {
            return '<span class="token punctuation">' + e3 + "</span>";
          }
          function Xs(e3, t3) {
            const r3 = typeof e3;
            let n2 = "";
            return null == e3 ? n2 += Gs("null", "token keyword") : e3 && e3.constructor === Array ? (Vs++, n2 += (function(e4, t4) {
              const r4 = Vs > t4 ? "collapsed" : "";
              let n3 = `<button class="collapser" aria-label="${Vs > t4 + 1 ? "expand" : "collapse"}"></button>${Zs("[")}<span class="ellipsis"></span><ul class="array collapsible">`, o3 = false;
              const i3 = e4.length;
              for (let s3 = 0; s3 < i3; s3++) o3 = true, n3 += '<li><div class="hoverable ' + r4 + '">', n3 += Xs(e4[s3], t4), s3 < i3 - 1 && (n3 += ","), n3 += "</div></li>";
              return n3 += `</ul>${Zs("]")}`, o3 || (n3 = Zs("[ ]")), n3;
            })(e3, t3), Vs--) : e3 && e3.constructor === Date ? n2 += Gs('"' + e3.toISOString() + '"', "token string") : "object" === r3 ? (Vs++, n2 += (function(e4, t4) {
              const r4 = Vs > t4 ? "collapsed" : "", n3 = Object.keys(e4), o3 = n3.length;
              let i3 = `<button class="collapser" aria-label="${Vs > t4 + 1 ? "expand" : "collapse"}"></button>${Zs("{")}<span class="ellipsis"></span><ul class="obj collapsible">`, s3 = false;
              for (let a3 = 0; a3 < o3; a3++) {
                const l3 = n3[a3];
                s3 = true, i3 += '<li><div class="hoverable ' + r4 + '">', i3 += '<span class="property token string">"' + Js(l3) + '"</span>: ', i3 += Xs(e4[l3], t4), a3 < o3 - 1 && (i3 += Zs(",")), i3 += "</div></li>";
              }
              return i3 += `</ul>${Zs("}")}`, s3 || (i3 = Zs("{ }")), i3;
            })(e3, t3), Vs--) : "number" === r3 ? n2 += Gs(e3, "token number") : "string" === r3 ? /^(http|https):\/\/[^\s]+$/.test(e3) ? n2 += Gs('"', "token string") + '<a href="' + encodeURI(e3) + '">' + Js(Ys(e3)) + "</a>" + Gs('"', "token string") : n2 += Gs('"' + Ys(e3) + '"', "token string") : "boolean" === r3 && (n2 += Gs(e3, "token boolean")), n2;
          }
          const Ks = Y2`
  .redoc-json code > .collapser {
    display: none;
    pointer-events: none;
  }

  font-family: ${(e3) => e3.theme.typography.code.fontFamily};
  font-size: ${(e3) => e3.theme.typography.code.fontSize};

  white-space: ${({ theme: e3 }) => e3.typography.code.wrap ? "pre-wrap" : "pre"};
  contain: content;
  overflow-x: auto;

  .callback-function {
    color: gray;
  }

  .collapser:after {
    content: '-';
    cursor: pointer;
  }

  .collapsed > .collapser:after {
    content: '+';
    cursor: pointer;
  }

  .ellipsis:after {
    content: ' … ';
  }

  .collapsible {
    margin-left: 2em;
  }

  .hoverable {
    padding-top: 1px;
    padding-bottom: 1px;
    padding-left: 2px;
    padding-right: 2px;
    border-radius: 2px;
  }

  .hovered {
    background-color: rgba(235, 238, 249, 1);
  }

  .collapser {
    background-color: transparent;
    border: 0;
    color: #fff;
    font-family: ${(e3) => e3.theme.typography.code.fontFamily};
    font-size: ${(e3) => e3.theme.typography.code.fontSize};
    padding-right: 6px;
    padding-left: 6px;
    padding-top: 0;
    padding-bottom: 0;
    display: flex;
    align-items: center;
    justify-content: center;
    width: 15px;
    height: 15px;
    position: absolute;
    top: 4px;
    left: -1.5em;
    cursor: default;
    user-select: none;
    -webkit-user-select: none;
    padding: 2px;
    &:focus {
      outline-color: #fff;
      outline-style: dotted;
      outline-width: 1px;
    }
  }

  ul {
    list-style-type: none;
    padding: 0px;
    margin: 0px 0px 0px 26px;
  }

  li {
    position: relative;
    display: block;
  }

  .hoverable {
    display: inline-block;
  }

  .selected {
    outline-style: solid;
    outline-width: 1px;
    outline-style: dotted;
  }

  .collapsed > .collapsible {
    display: none;
  }

  .ellipsis {
    display: none;
  }

  .collapsed > .ellipsis {
    display: inherit;
  }
`, ea = ee2.div`
  &:hover > ${Ji} {
    opacity: 1;
  }
`, ta = ee2(((t3) => {
            const [r3, n2] = e2.useState(), o3 = ({ renderCopyButton: r4 }) => {
              const o4 = t3.data && Object.values(t3.data).some(((e3) => "object" == typeof e3 && null !== e3));
              return e2.createElement(ea, null, e2.createElement(Ji, null, r4(), o4 && e2.createElement(e2.Fragment, null, e2.createElement("button", { onClick: i3 }, " Expand all "), e2.createElement("button", { onClick: s3 }, " Collapse all "))), e2.createElement(ce2.Consumer, null, ((r5) => e2.createElement(Hi, { tabIndex: 0, className: t3.className, ref: (e3) => n2(e3), dangerouslySetInnerHTML: { __html: Hs(t3.data, r5.jsonSamplesExpandLevel) } }))));
            }, i3 = () => {
              const e3 = null == r3 ? void 0 : r3.getElementsByClassName("collapsible");
              for (const t4 of Array.prototype.slice.call(e3)) {
                const e4 = t4.parentNode;
                e4.classList.remove("collapsed"), e4.querySelector(".collapser").setAttribute("aria-label", "collapse");
              }
            }, s3 = () => {
              const e3 = null == r3 ? void 0 : r3.getElementsByClassName("collapsible"), t4 = Array.prototype.slice.call(e3, 1);
              for (const e4 of t4) {
                const t5 = e4.parentNode;
                t5.classList.add("collapsed"), t5.querySelector(".collapser").setAttribute("aria-label", "expand");
              }
            }, a3 = (e3) => {
              let t4;
              "collapser" === e3.className && (t4 = e3.parentElement.getElementsByClassName("collapsible")[0], t4.parentElement.classList.contains("collapsed") ? (t4.parentElement.classList.remove("collapsed"), e3.setAttribute("aria-label", "collapse")) : (t4.parentElement.classList.add("collapsed"), e3.setAttribute("aria-label", "expand")));
            }, l3 = e2.useCallback(((e3) => {
              a3(e3.target);
            }), []), c3 = e2.useCallback(((e3) => {
              "Enter" === e3.key && a3(e3.target);
            }), []);
            return e2.useEffect((() => (null == r3 || r3.addEventListener("click", l3), null == r3 || r3.addEventListener("focus", c3), () => {
              null == r3 || r3.removeEventListener("click", l3), null == r3 || r3.removeEventListener("focus", c3);
            })), [l3, c3, r3]), e2.createElement(Ws, { data: t3.data }, o3);
          }))`
  ${Ks};
`, ra = (t3) => {
            const { source: r3, lang: n2 } = t3;
            return e2.createElement(Gi, { dangerouslySetInnerHTML: { __html: jt(r3, n2) } });
          }, na = (t3) => {
            const { source: r3, lang: n2 } = t3;
            return e2.createElement(Ws, { data: r3 }, (({ renderCopyButton: t4 }) => e2.createElement(Yi, null, e2.createElement(Ji, null, t4()), e2.createElement(ra, { lang: n2, source: r3 }))));
          };
          function oa({ value: t3, mimeType: r3 }) {
            return Ue2(r3) ? e2.createElement(ta, { data: t3 }) : ("object" == typeof t3 && (t3 = JSON.stringify(t3, null, 2)), e2.createElement(na, { lang: Ke2(r3), source: t3 }));
          }
          var ia = (e3, t3, r3) => new Promise(((n2, o3) => {
            var i3 = (e4) => {
              try {
                a3(r3.next(e4));
              } catch (e5) {
                o3(e5);
              }
            }, s3 = (e4) => {
              try {
                a3(r3.throw(e4));
              } catch (e5) {
                o3(e5);
              }
            }, a3 = (e4) => e4.done ? n2(e4.value) : Promise.resolve(e4.value).then(i3, s3);
            a3((r3 = r3.apply(e3, t3)).next());
          }));
          function sa({ example: t3, mimeType: r3 }) {
            return void 0 === t3.value && t3.externalValueUrl ? e2.createElement(aa, { example: t3, mimeType: r3 }) : e2.createElement(oa, { value: t3.value, mimeType: r3 });
          }
          function aa({ example: t3, mimeType: r3 }) {
            const n2 = (function(t4, r4) {
              const [, n3] = (0, e2.useState)(true), o3 = (0, e2.useRef)(void 0), i3 = (0, e2.useRef)(void 0);
              return i3.current !== t4 && (o3.current = void 0), i3.current = t4, (0, e2.useEffect)((() => {
                (() => {
                  ia(this, null, (function* () {
                    n3(true);
                    try {
                      o3.current = yield t4.getExternalValue(r4);
                    } catch (e3) {
                      o3.current = e3;
                    }
                    n3(false);
                  }));
                })();
              }), [t4, r4]), o3.current;
            })(t3, r3);
            return void 0 === n2 ? e2.createElement("span", null, "Loading...") : n2 instanceof Error ? e2.createElement(Gi, null, "Error loading external example: ", e2.createElement("br", null), e2.createElement("a", { className: "token string", href: t3.externalValueUrl, target: "_blank", rel: "noopener noreferrer" }, t3.externalValueUrl)) : e2.createElement(oa, { value: n2, mimeType: r3 });
          }
          const la = ee2.div`
  padding: 0.9em;
  background-color: ${({ theme: e3 }) => (0, t2.transparentize)(0.6, e3.rightPanel.backgroundColor)};
  margin: 0 0 10px 0;
  display: block;
  font-family: ${({ theme: e3 }) => e3.typography.headings.fontFamily};
  font-size: 0.929em;
  line-height: 1.5em;
`, ca = ee2.span`
  font-family: ${({ theme: e3 }) => e3.typography.headings.fontFamily};
  font-size: 12px;
  position: absolute;
  z-index: 1;
  top: -11px;
  left: 12px;
  font-weight: ${({ theme: e3 }) => e3.typography.fontWeightBold};
  color: ${({ theme: e3 }) => (0, t2.transparentize)(0.3, e3.rightPanel.textColor)};
`, pa = ee2.div`
  position: relative;
`, ua = ee2(ds)`
  label {
    color: ${({ theme: e3 }) => e3.rightPanel.textColor};
    text-overflow: ellipsis;
    white-space: nowrap;
    overflow: hidden;
    font-size: 1em;
    text-transform: none;
    border: none;
  }
  margin: 0 0 10px 0;
  display: block;
  background-color: ${({ theme: e3 }) => (0, t2.transparentize)(0.6, e3.rightPanel.backgroundColor)};
  border: none;
  padding: 0.9em 1.6em 0.9em 0.9em;
  box-shadow: none;
  &:hover,
  &:focus-within {
    border: none;
    box-shadow: none;
    background-color: ${({ theme: e3 }) => (0, t2.transparentize)(0.3, e3.rightPanel.backgroundColor)};
  }
`, da = ee2.div`
  font-family: ${(e3) => e3.theme.typography.code.fontFamily};
  font-size: 12px;
  color: #ee807f;
`;
          class ha extends e2.Component {
            constructor() {
              super(...arguments), this.state = { activeIdx: 0 }, this.switchMedia = ({ idx: e3 }) => {
                void 0 !== e3 && this.setState({ activeIdx: e3 });
              };
            }
            render() {
              const { activeIdx: t3 } = this.state, r3 = this.props.mediaType.examples || {}, n2 = this.props.mediaType.name, o3 = e2.createElement(da, null, "No sample"), i3 = Object.keys(r3);
              if (0 === i3.length) return o3;
              if (i3.length > 1) {
                const o4 = i3.map(((e3, t4) => ({ value: r3[e3].summary || e3, idx: t4 }))), s3 = r3[i3[t3]], a3 = s3.description;
                return e2.createElement(ma, null, e2.createElement(pa, null, e2.createElement(ca, null, "Example"), this.props.renderDropdown({ value: o4[t3].value, options: o4, onChange: this.switchMedia, ariaLabel: "Example" })), e2.createElement("div", null, a3 && e2.createElement(zs, { source: a3 }), e2.createElement(sa, { example: s3, mimeType: n2 })));
              }
              {
                const t4 = r3[i3[0]];
                return e2.createElement(ma, null, t4.description && e2.createElement(zs, { source: t4.description }), e2.createElement(sa, { example: t4, mimeType: n2 }));
              }
            }
          }
          const ma = ee2.div`
  margin-top: 15px;
`;
          var fa = (init_mobxreact_esm(), __toCommonJS(mobxreact_esm_exports));
          const ga = ee2(Di)`
  &.deprecated {
    span.property-name {
      ${Ai}
    }
  }

  button {
    background-color: transparent;
    border: 0;
    outline: 0;
    font-size: 13px;
    font-family: ${(e3) => e3.theme.typography.code.fontFamily};
    cursor: pointer;
    padding: 0;
    color: ${(e3) => e3.theme.colors.text.primary};
    &:focus {
      font-weight: ${({ theme: e3 }) => e3.typography.fontWeightBold};
    }
    ${({ kind: e3 }) => "patternProperties" === e3 && Y2`
        display: inline-flex;
        margin-right: 20px;

        > span.property-name {
          white-space: break-spaces;
          text-align: left;

          ::before,
          ::after {
            content: '/';
            filter: opacity(0.2);
          }
        }

        > svg {
          align-self: center;
        }
      `}
  }
  ${Pi} {
    height: ${({ theme: e3 }) => e3.schema.arrow.size};
    width: ${({ theme: e3 }) => e3.schema.arrow.size};
    polygon {
      fill: ${({ theme: e3 }) => e3.schema.arrow.color};
    }
  }
`, ya = ee2.span`
  vertical-align: middle;
  font-size: ${({ theme: e3 }) => e3.typography.code.fontSize};
  line-height: 20px;
`, ba = ee2(ya)`
  color: ${(e3) => (0, t2.transparentize)(0.1, e3.theme.schema.typeNameColor)};
`, xa = ee2(ya)`
  color: ${(e3) => e3.theme.schema.typeNameColor};
`, va = ee2(ya)`
  color: ${(e3) => e3.theme.schema.typeTitleColor};
  word-break: break-word;
`, wa = xa, ka = ee2(ya).attrs({ as: "div" })`
  color: ${(e3) => e3.theme.schema.requireLabelColor};
  font-size: ${(e3) => e3.theme.schema.labelsTextSize};
  font-weight: normal;
  margin-left: 20px;
  line-height: 1;
`, Ea = ee2(ka)`
  color: ${(e3) => e3.theme.colors.primary.light};
`, Oa = ee2(ya)`
  color: ${({ theme: e3 }) => e3.colors.warning.main};
  font-size: 13px;
`, ja = ee2(ya)`
  color: #0e7c86;
  font-family: ${(e3) => e3.theme.typography.code.fontFamily};
  font-size: 12px;
  &::before,
  &::after {
    content: ' ';
  }
`, $a = ee2(ya)`
  border-radius: 2px;
  word-break: break-word;
  ${({ theme: e3 }) => `
    background-color: ${(0, t2.transparentize)(0.95, e3.colors.text.primary)};
    color: ${(0, t2.transparentize)(0.1, e3.colors.text.primary)};

    padding: 0 ${e3.spacing.unit}px;
    border: 1px solid ${(0, t2.transparentize)(0.9, e3.colors.text.primary)};
    font-family: ${e3.typography.code.fontFamily};
}`};
  & + & {
    margin-left: 0;
  }
  ${te2("ExampleValue")};
`, Sa = ee2($a)``, Ca = ee2(ya)`
  border-radius: 2px;
  ${({ theme: e3 }) => `
    background-color: ${(0, t2.transparentize)(0.95, e3.colors.primary.light)};
    color: ${(0, t2.transparentize)(0.1, e3.colors.primary.main)};

    margin: 0 ${e3.spacing.unit}px;
    padding: 0 ${e3.spacing.unit}px;
    border: 1px solid ${(0, t2.transparentize)(0.9, e3.colors.primary.main)};
}`};
  & + & {
    margin-left: 0;
  }
  ${te2("ConstraintItem")};
`, Pa = ee2.button`
  background-color: transparent;
  border: 0;
  color: ${({ theme: e3 }) => e3.colors.text.secondary};
  margin-left: ${({ theme: e3 }) => e3.spacing.unit}px;
  border-radius: 2px;
  cursor: pointer;
  outline-color: ${({ theme: e3 }) => e3.colors.text.secondary};
  font-size: 12px;
`;
          Object.defineProperty, Object.getOwnPropertyDescriptor;
          const Ia = ee2.div`
  ${Os};
  ${({ $compact: e3 }) => e3 ? "" : "margin: 1em 0"}
`;
          let Aa = class extends e2.Component {
            render() {
              const { externalDocs: t3 } = this.props;
              return t3 && t3.url ? e2.createElement(Ia, { $compact: this.props.compact }, e2.createElement("a", { href: t3.url }, t3.description || t3.url)) : null;
            }
          };
          Aa = ((e3, t3) => {
            for (var r3, n2 = t3, o3 = e3.length - 1; o3 >= 0; o3--) (r3 = e3[o3]) && (n2 = r3(n2) || n2);
            return n2;
          })([fa.observer], Aa);
          const Ta = ee2(js)`
  table {
    margin-bottom: 0.2em;
  }
`;
          class _a3 extends e2.PureComponent {
            constructor(e3) {
              super(e3), this.state = { collapsed: true }, this.toggle = this.toggle.bind(this);
            }
            toggle() {
              this.setState({ collapsed: !this.state.collapsed });
            }
            render() {
              const { values: t3, type: r3 } = this.props, { collapsed: n2 } = this.state, o3 = !Array.isArray(t3), i3 = Array.isArray(t3) && t3 || Object.entries(t3 || {}).map((([e3, t4]) => ({ value: e3, description: t4 }))), { enumSkipQuotes: s3, maxDisplayedEnumValues: a3 } = this.context;
              if (!i3.length) return null;
              const l3 = this.state.collapsed && a3 ? i3.slice(0, a3) : i3, c3 = !!a3 && i3.length > a3, p3 = a3 ? n2 ? `… ${i3.length - a3} more` : "Hide" : "";
              return e2.createElement("div", null, o3 ? e2.createElement(e2.Fragment, null, e2.createElement(Ta, null, e2.createElement("table", null, e2.createElement("thead", null, e2.createElement("tr", null, e2.createElement("th", null, e2.createElement(ya, null, "array" === r3 ? _15("enumArray") : "", " ", 1 === i3.length ? _15("enumSingleValue") : _15("enum")), " "), e2.createElement("th", null, e2.createElement("strong", null, "Description")))), e2.createElement("tbody", null, l3.map((({ description: t4, value: r4 }) => e2.createElement("tr", { key: r4 }, e2.createElement("td", null, r4), e2.createElement("td", null, e2.createElement(zs, { source: t4, compact: true, inline: true })))))))), c3 ? e2.createElement(Ra, { onClick: this.toggle }, p3) : null) : e2.createElement(e2.Fragment, null, e2.createElement(ya, null, "array" === r3 ? _15("enumArray") : "", " ", 1 === t3.length ? _15("enumSingleValue") : _15("enum"), ":"), " ", l3.map(((t4, r4) => {
                const n3 = s3 ? String(t4) : JSON.stringify(t4);
                return e2.createElement(e2.Fragment, { key: r4 }, e2.createElement($a, null, n3), " ");
              })), c3 ? e2.createElement(Ra, { onClick: this.toggle }, p3) : null));
            }
          }
          _a3.contextType = ce2;
          const Ra = ee2.span`
  color: ${(e3) => e3.theme.colors.primary.main};
  vertical-align: middle;
  font-size: 13px;
  line-height: 20px;
  padding: 0 5px;
  cursor: pointer;
`, Da = ee2(js)`
  margin: 2px 0;
`;
          class za extends e2.PureComponent {
            render() {
              const t3 = this.props.extensions;
              return e2.createElement(ce2.Consumer, null, ((r3) => e2.createElement(e2.Fragment, null, r3.showExtensions && Object.keys(t3).map(((r4) => e2.createElement(Da, { key: r4 }, e2.createElement(ya, null, " ", r4.substring(2), ": "), " ", e2.createElement(Sa, null, "string" == typeof t3[r4] ? t3[r4] : JSON.stringify(t3[r4]))))))));
            }
          }
          function La({ field: t3 }) {
            return t3.examples ? e2.createElement(e2.Fragment, null, e2.createElement(ya, null, " ", _15("examples"), ": "), I2(t3.examples) ? t3.examples.map(((r3, n2) => {
              const o3 = Xe2(t3, r3), i3 = t3.in ? String(o3) : JSON.stringify(o3);
              return e2.createElement(e2.Fragment, { key: n2 }, e2.createElement($a, null, i3), " ");
            })) : e2.createElement(Na, null, Object.values(t3.examples).map(((r3, n2) => e2.createElement("li", { key: n2 + r3.value }, e2.createElement($a, null, Xe2(t3, r3.value)), " -", " ", r3.summary || r3.description))))) : null;
          }
          const Na = ee2.ul`
  margin-top: 1em;
  list-style-position: outside;
`;
          class Ma extends e2.PureComponent {
            render() {
              return 0 === this.props.constraints.length ? null : e2.createElement("span", null, " ", this.props.constraints.map(((t3) => e2.createElement(Ca, { key: t3 }, " ", t3, " "))));
            }
          }
          const qa = e2.memo((function({ value: t3, label: r3, raw: n2 }) {
            if (void 0 === t3) return null;
            const o3 = n2 ? String(t3) : JSON.stringify(t3);
            return e2.createElement("div", null, e2.createElement(ya, null, " ", r3, " "), " ", e2.createElement($a, null, o3));
          })), Fa = 45;
          function Ba(t3) {
            const r3 = t3.schema.pattern, { hideSchemaPattern: n2 } = e2.useContext(ce2), [o3, i3] = e2.useState(false), s3 = e2.useCallback((() => i3(!o3)), [o3]);
            return !r3 || n2 ? null : e2.createElement(e2.Fragment, null, e2.createElement(ja, null, o3 || r3.length < Fa ? r3 : `${r3.substr(0, Fa)}...`), r3.length > Fa && e2.createElement(Pa, { onClick: s3 }, o3 ? "Hide pattern" : "Show pattern"));
          }
          function Qa({ schema: t3 }) {
            var r3;
            const { hideSchemaPattern: n2 } = e2.useContext(ce2);
            return t3 && ((null == t3 ? void 0 : t3.pattern) && !n2 || t3.items || t3.displayFormat || (null == (r3 = t3.constraints) ? void 0 : r3.length)) ? e2.createElement(Ua, null, "[ items", t3.displayFormat && e2.createElement(wa, null, " <", t3.displayFormat, " >"), e2.createElement(Ma, { constraints: t3.constraints }), e2.createElement(Ba, { schema: t3 }), t3.items && e2.createElement(Qa, { schema: t3.items }), " ]") : null;
          }
          const Ua = ee2(ba)`
  margin: 0 5px;
  vertical-align: text-top;
`;
          var Wa = Object.defineProperty, Va = Object.getOwnPropertySymbols, Ha = Object.prototype.hasOwnProperty, Ja = Object.prototype.propertyIsEnumerable, Ya = (e3, t3, r3) => t3 in e3 ? Wa(e3, t3, { enumerable: true, configurable: true, writable: true, value: r3 }) : e3[t3] = r3, Ga = (e3, t3) => {
            for (var r3 in t3 || (t3 = {})) Ha.call(t3, r3) && Ya(e3, r3, t3[r3]);
            if (Va) for (var r3 of Va(t3)) Ja.call(t3, r3) && Ya(e3, r3, t3[r3]);
            return e3;
          };
          const Za = (0, fa.observer)(((t3) => {
            const { enumSkipQuotes: r3, hideSchemaTitles: n2 } = e2.useContext(ce2), { showExamples: o3, field: i3, renderDiscriminatorSwitch: s3 } = t3, { schema: a3, description: l3, deprecated: c3, extensions: p3, in: u3, const: d2 } = i3, h2 = "array" === a3.type || I2(a3.type) && a3.type.includes("array"), m3 = r3 || "header" === u3, f3 = e2.useMemo((() => !o3 || void 0 === i3.example && void 0 === i3.examples ? null : void 0 !== i3.examples ? e2.createElement(La, { field: i3 }) : e2.createElement(qa, { label: _15("example") + ":", value: Xe2(i3, i3.example), raw: Boolean(i3.in) })), [i3, o3]), g3 = x2(a3.default) && i3.in ? Xe2(i3, a3.default).replace(`${i3.name}=`, "") : a3.default;
            return e2.createElement("div", null, e2.createElement("div", null, e2.createElement(ba, null, a3.typePrefix), e2.createElement(xa, null, a3.displayType), a3.displayFormat && e2.createElement(wa, null, " ", "<", a3.displayFormat, ">", " "), a3.contentEncoding && e2.createElement(wa, null, " ", "<", a3.contentEncoding, ">", " "), a3.contentMediaType && e2.createElement(wa, null, " ", "<", a3.contentMediaType, ">", " "), a3.title && !n2 && e2.createElement(va, null, " (", a3.title, ") "), e2.createElement(Ma, { constraints: a3.constraints }), e2.createElement(Ba, { schema: a3 }), a3.isCircular && e2.createElement(Oa, null, " ", _15("recursive"), " "), h2 && a3.items && e2.createElement(Qa, { schema: a3.items })), c3 && e2.createElement("div", null, e2.createElement(Ii, { type: "warning" }, " ", _15("deprecated"), " ")), e2.createElement(qa, { raw: m3, label: _15("default") + ":", value: g3 }), !s3 && e2.createElement(_a3, { type: a3.type, values: a3["x-enumDescriptions"] || a3.enum }), " ", f3, e2.createElement(za, { extensions: Ga(Ga({}, p3), a3.extensions) }), e2.createElement("div", null, e2.createElement(zs, { compact: true, source: l3 })), a3.externalDocs && e2.createElement(Aa, { externalDocs: a3.externalDocs, compact: true }), s3 && s3(t3) || null, d2 && e2.createElement(qa, { label: _15("const") + ":", value: d2 }) || null);
          })), Xa = e2.memo(Za);
          var Ka = Object.defineProperty, el = (Object.getOwnPropertyDescriptor, Object.getOwnPropertySymbols), tl = Object.prototype.hasOwnProperty, rl = Object.prototype.propertyIsEnumerable, nl = (e3, t3, r3) => t3 in e3 ? Ka(e3, t3, { enumerable: true, configurable: true, writable: true, value: r3 }) : e3[t3] = r3;
          let ol = class extends e2.Component {
            constructor() {
              super(...arguments), this.toggle = () => {
                void 0 === this.props.field.expanded && this.props.expandByDefault ? this.props.field.collapse() : this.props.field.toggle();
              }, this.handleKeyPress = (e3) => {
                "Enter" === e3.key && (e3.preventDefault(), this.toggle());
              };
            }
            render() {
              const { hidePropertiesPrefix: t3 } = this.context, { className: r3 = "", field: n2, isLast: o3, expandByDefault: i3, fieldParentsName: s3 = [] } = this.props, { name: a3, deprecated: l3, required: c3, kind: p3 } = n2, u3 = !n2.schema.isPrimitive && !n2.schema.isCircular, d2 = void 0 === n2.expanded ? i3 : n2.expanded, h2 = e2.createElement(e2.Fragment, null, "additionalProperties" === p3 && e2.createElement(Ea, null, "additional property"), "patternProperties" === p3 && e2.createElement(Ea, null, "pattern property"), c3 && e2.createElement(ka, null, "required")), m3 = u3 ? e2.createElement(ga, { className: l3 ? "deprecated" : "", kind: p3, title: a3 }, e2.createElement(Li, null), e2.createElement("button", { onClick: this.toggle, onKeyPress: this.handleKeyPress, "aria-label": `expand ${a3}` }, !t3 && s3.map(((e3) => e3 + ".​")), e2.createElement("span", { className: "property-name" }, a3), e2.createElement(Pi, { direction: d2 ? "down" : "right" })), h2) : e2.createElement(Di, { className: l3 ? "deprecated" : void 0, kind: p3, title: a3 }, e2.createElement(Li, null), !t3 && s3.map(((e3) => e3 + ".​")), e2.createElement("span", { className: "property-name" }, a3), h2);
              return e2.createElement(e2.Fragment, null, e2.createElement("tr", { className: o3 ? "last " + r3 : r3 }, m3, e2.createElement(zi, null, e2.createElement(Xa, ((e3, t4) => {
                for (var r4 in t4 || (t4 = {})) tl.call(t4, r4) && nl(e3, r4, t4[r4]);
                if (el) for (var r4 of el(t4)) rl.call(t4, r4) && nl(e3, r4, t4[r4]);
                return e3;
              })({}, this.props)))), d2 && u3 && e2.createElement("tr", { key: n2.name + "inner" }, e2.createElement(Ri, { colSpan: 2 }, e2.createElement(Ni, null, e2.createElement(Ml, { schema: n2.schema, fieldParentsName: [...s3 || [], n2.name], skipReadOnly: this.props.skipReadOnly, skipWriteOnly: this.props.skipWriteOnly, showTitle: this.props.showTitle, level: this.props.level })))));
            }
          };
          ol.contextType = ce2, ol = ((e3, t3) => {
            for (var r3, n2 = t3, o3 = e3.length - 1; o3 >= 0; o3--) (r3 = e3[o3]) && (n2 = r3(n2) || n2);
            return n2;
          })([fa.observer], ol);
          Object.defineProperty, Object.getOwnPropertyDescriptor;
          let il = class extends e2.Component {
            constructor() {
              super(...arguments), this.changeActiveChild = (e3) => {
                void 0 !== e3.idx && this.props.parent.activateOneOf(e3.idx);
              };
            }
            sortOptions(e3, t3) {
              if (0 === t3.length) return;
              const r3 = {};
              t3.forEach(((e4, t4) => {
                r3[e4] = t4;
              })), e3.sort(((e4, t4) => r3[e4.value] > r3[t4.value] ? 1 : -1));
            }
            render() {
              const { parent: t3, enumValues: r3 } = this.props;
              if (void 0 === t3.oneOf) return null;
              const n2 = t3.oneOf.map(((e3, t4) => ({ value: e3.title, idx: t4 }))), o3 = n2[t3.activeOneOf].value;
              return this.sortOptions(n2, r3), e2.createElement(ds, { value: o3, options: n2, onChange: this.changeActiveChild, ariaLabel: "Example" });
            }
          };
          il = ((e3, t3) => {
            for (var r3, n2 = t3, o3 = e3.length - 1; o3 >= 0; o3--) (r3 = e3[o3]) && (n2 = r3(n2) || n2);
            return n2;
          })([fa.observer], il);
          const sl = (0, fa.observer)((({ schema: { fields: t3 = [], title: r3 }, showTitle: n2, discriminator: o3, skipReadOnly: i3, skipWriteOnly: s3, level: a3, fieldParentsName: l3 }) => {
            const { expandSingleSchemaField: c3, showObjectSchemaExamples: p3, schemasExpansionLevel: u3 } = e2.useContext(ce2), h2 = e2.useMemo((() => i3 || s3 ? t3.filter(((e3) => !(i3 && e3.schema.readOnly || s3 && e3.schema.writeOnly))) : t3), [i3, s3, t3]), m3 = c3 && 1 === h2.length || u3 >= a3;
            return e2.createElement(Mi, null, n2 && e2.createElement(Ti, null, r3), e2.createElement("tbody", null, d(h2, ((t4, r4) => e2.createElement(ol, { key: t4.name, isLast: r4, field: t4, expandByDefault: m3, fieldParentsName: Number(a3) > 1 ? l3 : [], renderDiscriminatorSwitch: (null == o3 ? void 0 : o3.fieldName) === t4.name ? () => e2.createElement(il, { parent: o3.parentSchema, enumValues: t4.schema.enum }) : void 0, className: t4.expanded ? "expanded" : void 0, showExamples: p3, skipReadOnly: i3, skipWriteOnly: s3, showTitle: n2, level: a3 })))));
          }));
          var al = Object.defineProperty, ll = Object.defineProperties, cl = Object.getOwnPropertyDescriptors, pl = Object.getOwnPropertySymbols, ul = Object.prototype.hasOwnProperty, dl = Object.prototype.propertyIsEnumerable, hl = (e3, t3, r3) => t3 in e3 ? al(e3, t3, { enumerable: true, configurable: true, writable: true, value: r3 }) : e3[t3] = r3, ml = (e3, t3) => {
            for (var r3 in t3 || (t3 = {})) ul.call(t3, r3) && hl(e3, r3, t3[r3]);
            if (pl) for (var r3 of pl(t3)) dl.call(t3, r3) && hl(e3, r3, t3[r3]);
            return e3;
          }, fl = (e3, t3) => ll(e3, cl(t3));
          const gl = ee2.div`
  padding-left: ${({ theme: e3 }) => 2 * e3.spacing.unit}px;
`;
          class yl extends e2.PureComponent {
            render() {
              const t3 = this.props.schema, r3 = t3.items, n2 = this.props.fieldParentsName, o3 = void 0 === t3.minItems && void 0 === t3.maxItems ? "" : `(${it2(t3)})`, i3 = n2 ? [...n2.slice(0, -1), n2[n2.length - 1] + "[]"] : n2;
              return t3.fields ? e2.createElement(sl, fl(ml({}, this.props), { level: this.props.level, fieldParentsName: i3 })) : !t3.displayType || r3 || o3.length ? e2.createElement("div", null, e2.createElement(Qi, null, " Array ", o3), e2.createElement(gl, null, e2.createElement(Ml, fl(ml({}, this.props), { schema: r3, fieldParentsName: i3 }))), e2.createElement(Ui, null)) : e2.createElement("div", null, e2.createElement(xa, null, t3.displayType));
            }
          }
          var bl = Object.defineProperty, xl = Object.defineProperties, vl = Object.getOwnPropertyDescriptor, wl = Object.getOwnPropertyDescriptors, kl = Object.getOwnPropertySymbols, El = Object.prototype.hasOwnProperty, Ol = Object.prototype.propertyIsEnumerable, jl = (e3, t3, r3) => t3 in e3 ? bl(e3, t3, { enumerable: true, configurable: true, writable: true, value: r3 }) : e3[t3] = r3, $l = (e3, t3, r3, n2) => {
            for (var o3, i3 = n2 > 1 ? void 0 : n2 ? vl(t3, r3) : t3, s3 = e3.length - 1; s3 >= 0; s3--) (o3 = e3[s3]) && (i3 = (n2 ? o3(t3, r3, i3) : o3(i3)) || i3);
            return n2 && i3 && bl(t3, r3, i3), i3;
          };
          let Sl = class extends e2.Component {
            constructor() {
              super(...arguments), this.activateOneOf = () => {
                this.props.schema.activateOneOf(this.props.idx);
              };
            }
            render() {
              const { idx: t3, schema: r3, subSchema: n2 } = this.props;
              return e2.createElement(Bi, { $deprecated: n2.deprecated, $active: t3 === r3.activeOneOf, onClick: this.activateOneOf }, n2.title || n2.typePrefix + n2.displayType);
            }
          };
          Sl = $l([fa.observer], Sl);
          let Cl = class extends e2.Component {
            render() {
              const { schema: { oneOf: t3 }, schema: r3 } = this.props;
              if (void 0 === t3) return null;
              const n2 = t3[r3.activeOneOf];
              return e2.createElement("div", null, e2.createElement(Fi, null, " ", r3.oneOfType, " "), e2.createElement(qi, null, t3.map(((t4, n3) => e2.createElement(Sl, { key: t4.pointer, schema: r3, subSchema: t4, idx: n3 })))), e2.createElement("div", null, t3[r3.activeOneOf].deprecated && e2.createElement(Ii, { type: "warning" }, "Deprecated")), e2.createElement(Ma, { constraints: n2.constraints }), e2.createElement(Ml, (o3 = ((e3, t4) => {
                for (var r4 in t4 || (t4 = {})) El.call(t4, r4) && jl(e3, r4, t4[r4]);
                if (kl) for (var r4 of kl(t4)) Ol.call(t4, r4) && jl(e3, r4, t4[r4]);
                return e3;
              })({}, this.props), xl(o3, wl({ schema: n2 })))));
              var o3;
            }
          };
          Cl = $l([fa.observer], Cl);
          const Pl = (0, fa.observer)((({ schema: t3 }) => e2.createElement("div", null, e2.createElement(xa, null, t3.displayType), t3.title && e2.createElement(va, null, " ", t3.title, " "), e2.createElement(Oa, null, " ", _15("recursive"), " "))));
          var Il = Object.defineProperty, Al = Object.defineProperties, Tl = (Object.getOwnPropertyDescriptor, Object.getOwnPropertyDescriptors), _l = Object.getOwnPropertySymbols, Rl = Object.prototype.hasOwnProperty, Dl = Object.prototype.propertyIsEnumerable, zl = (e3, t3, r3) => t3 in e3 ? Il(e3, t3, { enumerable: true, configurable: true, writable: true, value: r3 }) : e3[t3] = r3, Ll = (e3, t3) => {
            for (var r3 in t3 || (t3 = {})) Rl.call(t3, r3) && zl(e3, r3, t3[r3]);
            if (_l) for (var r3 of _l(t3)) Dl.call(t3, r3) && zl(e3, r3, t3[r3]);
            return e3;
          }, Nl = (e3, t3) => Al(e3, Tl(t3));
          let Ml = class extends e2.Component {
            render() {
              var t3;
              const r3 = this.props, { schema: n2 } = r3, o3 = ((e3, t4) => {
                var r4 = {};
                for (var n3 in e3) Rl.call(e3, n3) && t4.indexOf(n3) < 0 && (r4[n3] = e3[n3]);
                if (null != e3 && _l) for (var n3 of _l(e3)) t4.indexOf(n3) < 0 && Dl.call(e3, n3) && (r4[n3] = e3[n3]);
                return r4;
              })(r3, ["schema"]), i3 = (o3.level || 0) + 1;
              if (!n2) return e2.createElement("em", null, " Schema not provided ");
              const { type: s3, oneOf: a3, discriminatorProp: l3, isCircular: c3 } = n2;
              if (c3) return e2.createElement(Pl, { schema: n2 });
              if (void 0 !== l3) {
                if (!a3 || !a3.length) return console.warn(`Looks like you are using discriminator wrong: you don't have any definition inherited from the ${n2.title}`), null;
                const t4 = a3[n2.activeOneOf];
                return t4.isCircular ? e2.createElement(Pl, { schema: t4 }) : e2.createElement(sl, Nl(Ll({}, o3), { level: i3, schema: t4, discriminator: { fieldName: l3, parentSchema: n2 } }));
              }
              if (void 0 !== a3) return e2.createElement(Cl, Ll({ schema: n2 }, o3));
              const p3 = I2(s3) ? s3 : [s3];
              if (p3.includes("object")) {
                if (null == (t3 = n2.fields) ? void 0 : t3.length) return e2.createElement(sl, Nl(Ll({}, this.props), { level: i3 }));
              } else if (p3.includes("array")) return e2.createElement(yl, Nl(Ll({}, this.props), { level: i3 }));
              const u3 = { schema: n2, name: "", required: false, description: n2.description, externalDocs: n2.externalDocs, deprecated: false, toggle: () => null, expanded: false };
              return e2.createElement("div", null, e2.createElement(Xa, { field: u3 }));
            }
          };
          Ml = ((e3, t3) => {
            for (var r3, n2 = t3, o3 = e3.length - 1; o3 >= 0; o3--) (r3 = e3[o3]) && (n2 = r3(n2) || n2);
            return n2;
          })([fa.observer], Ml);
          var ql = Object.defineProperty, Fl = Object.defineProperties, Bl = Object.getOwnPropertyDescriptors, Ql = Object.getOwnPropertySymbols, Ul = Object.prototype.hasOwnProperty, Wl = Object.prototype.propertyIsEnumerable, Vl = (e3, t3, r3) => t3 in e3 ? ql(e3, t3, { enumerable: true, configurable: true, writable: true, value: r3 }) : e3[t3] = r3;
          class Hl extends e2.PureComponent {
            constructor() {
              super(...arguments), this.renderDropdown = (t3) => {
                return e2.createElement(ws, (r3 = ((e3, t4) => {
                  for (var r4 in t4 || (t4 = {})) Ul.call(t4, r4) && Vl(e3, r4, t4[r4]);
                  if (Ql) for (var r4 of Ql(t4)) Wl.call(t4, r4) && Vl(e3, r4, t4[r4]);
                  return e3;
                })({ Label: ms, Dropdown: ua }, t3), Fl(r3, Bl({ variant: "dark" }))));
                var r3;
              };
            }
            static getMediaType(e3, t3) {
              if (!e3) return {};
              const r3 = { schema: { $ref: e3 } };
              return t3 && (r3.examples = { example: { $ref: t3 } }), r3;
            }
            get mediaModel() {
              const { parser: e3, schemaRef: t3, exampleRef: r3, options: n2 } = this.props;
              return this._mediaModel || (this._mediaModel = new Yn(e3, "json", false, Hl.getMediaType(t3, r3), n2)), this._mediaModel;
            }
            render() {
              const { showReadOnly: t3 = true, showWriteOnly: r3 = false, showExample: n2 = true } = this.props;
              return e2.createElement(li, null, e2.createElement(ui, null, e2.createElement(ai, null, e2.createElement(Ml, { skipWriteOnly: !r3, skipReadOnly: !t3, schema: this.mediaModel.schema })), n2 && e2.createElement(pi, null, e2.createElement(Jl, null, e2.createElement(ha, { renderDropdown: this.renderDropdown, mediaType: this.mediaModel })))));
            }
          }
          const Jl = ee2.div`
  background: ${({ theme: e3 }) => e3.codeBlock.backgroundColor};
  & > div,
  & > pre {
    padding: ${(e3) => 4 * e3.theme.spacing.unit}px;
    margin: 0;
  }

  & > div > pre {
    padding: 0;
  }
`, Yl = (H2().div`
  background-color: #e4e7eb;
`, H2().ul`
  display: inline;
  list-style: none;
  padding: 0;

  li {
    display: inherit;

    &:after {
      content: ',';
    }
    &:last-child:after {
      content: none;
    }
  }
`, H2().code`
  font-size: ${(e3) => e3.theme.typography.code.fontSize};
  font-family: ${(e3) => e3.theme.typography.code.fontFamily};
  margin: 0 3px;
  padding: 0.2em;
  display: inline-block;
  line-height: 1;

  &:after {
    content: ',';
    font-weight: normal;
  }

  &:last-child:after {
    content: none;
  }
`), Gl = H2().span`
  &:after {
    content: ' and ';
    font-weight: normal;
  }

  &:last-child:after {
    content: none;
  }

  ${Os};
`, Zl = H2().span`
  ${(e3) => !e3.$expanded && "white-space: nowrap;"}
  &:after {
    content: ' or ';
    ${(e3) => e3.$expanded && "content: ' or \\a';"}
    white-space: pre;
  }

  &:last-child:after,
  &:only-child:after {
    content: none;
  }

  ${Os};
`, Xl = H2().div`
  flex: 1 1 auto;
  cursor: pointer;
`, Kl = H2().div`
  width: ${(e3) => e3.theme.schema.defaultDetailsWidth};
  text-overflow: ellipsis;
  border-radius: 4px;
  overflow: hidden;
  ${(e3) => e3.$expanded && `background: ${e3.theme.colors.gray[100]};
     padding: 8px 9.6px;
     margin: 20px 0;
     width: 100%;
    `};
  ${K2.lessThan("small")`
    margin-top: 10px;
  `}
`, ec = H2()(bi)`
  display: inline-block;
  margin: 0;
`, tc = H2().div`
  width: 100%;
  display: flex;
  margin: 1em 0;
  flex-direction: ${(e3) => e3.$expanded ? "column" : "row"};
  ${K2.lessThan("small")`
    flex-direction: column;
  `}
`, rc = H2().div`
  margin: 0.5em 0;
`, nc = H2().div`
  border-bottom: 1px solid ${({ theme: e3 }) => e3.colors.border.dark};
  margin-bottom: 1.5em;
  padding-bottom: 0.7em;

  h5 {
    line-height: 1em;
    margin: 0 0 0.6em;
    font-size: ${({ theme: e3 }) => e3.typography.fontSize};
  }

  .redoc-markdown p:first-child {
    display: inline;
  }
`;
          function oc({ children: t3, height: r3 }) {
            const n2 = e2.createRef(), [o3, i3] = e2.useState(false), [s3, a3] = e2.useState(false);
            return e2.useEffect((() => {
              n2.current && n2.current.clientHeight + 20 < n2.current.scrollHeight && a3(true);
            }), [n2]), e2.createElement(e2.Fragment, null, e2.createElement(ic, { ref: n2, className: o3 ? "" : "container", style: { height: o3 ? "auto" : r3 } }, t3), e2.createElement(sc, { $dimmed: !o3 }, s3 && e2.createElement(ac, { onClick: () => {
              i3(!o3);
            } }, o3 ? "See less" : "See more")));
          }
          const ic = H2().div`
  overflow-y: hidden;
`, sc = H2().div`
  text-align: center;
  line-height: 1.5em;
  ${({ $dimmed: e3 }) => e3 && "background-image: linear-gradient(to bottom, transparent,rgb(255 255 255));\n     position: relative;\n     top: -0.5em;\n     padding-top: 0.5em;\n     background-position-y: -1em;\n    "}
`, ac = H2().a`
  cursor: pointer;
`, lc = e2.memo((function(t3) {
            const { type: r3, flow: n2, RequiredScopes: o3 } = t3, i3 = Object.keys((null == n2 ? void 0 : n2.scopes) || {});
            return e2.createElement(e2.Fragment, null, e2.createElement(rc, null, e2.createElement("b", null, "Flow type: "), e2.createElement("code", null, r3, " ")), ("implicit" === r3 || "authorizationCode" === r3) && e2.createElement(rc, null, e2.createElement("strong", null, " Authorization URL: "), e2.createElement("code", null, e2.createElement("a", { target: "_blank", rel: "noopener noreferrer", href: n2.authorizationUrl }, n2.authorizationUrl))), ("password" === r3 || "clientCredentials" === r3 || "authorizationCode" === r3) && e2.createElement(rc, null, e2.createElement("b", null, " Token URL: "), e2.createElement("code", null, n2.tokenUrl)), n2.refreshUrl && e2.createElement(rc, null, e2.createElement("strong", null, " Refresh URL: "), e2.createElement("code", null, n2.refreshUrl)), !!i3.length && e2.createElement(e2.Fragment, null, o3 || null, e2.createElement(rc, null, e2.createElement("b", null, " Scopes: ")), e2.createElement(oc, { height: "4em" }, e2.createElement("ul", null, i3.map(((t4) => e2.createElement("li", { key: t4 }, e2.createElement("code", null, t4), " -", " ", e2.createElement(zs, { className: "redoc-markdown", inline: true, source: n2.scopes[t4] || "" }))))))));
          }));
          function cc(t3) {
            const { RequiredScopes: r3, scheme: n2 } = t3;
            return e2.createElement(js, null, n2.apiKey ? e2.createElement(e2.Fragment, null, e2.createElement(rc, null, e2.createElement("b", null, j2(n2.apiKey.in || ""), " parameter name: "), e2.createElement("code", null, n2.apiKey.name)), r3) : n2.http ? e2.createElement(e2.Fragment, null, e2.createElement(rc, null, e2.createElement("b", null, "HTTP Authorization Scheme: "), e2.createElement("code", null, n2.http.scheme)), e2.createElement(rc, null, "bearer" === n2.http.scheme && n2.http.bearerFormat && e2.createElement(e2.Fragment, null, e2.createElement("b", null, "Bearer format: "), e2.createElement("code", null, n2.http.bearerFormat))), r3) : n2.openId ? e2.createElement(e2.Fragment, null, e2.createElement(rc, null, e2.createElement("b", null, "Connect URL: "), e2.createElement("code", null, e2.createElement("a", { target: "_blank", rel: "noopener noreferrer", href: n2.openId.connectUrl }, n2.openId.connectUrl))), r3) : n2.flows ? Object.keys(n2.flows).map(((t4) => e2.createElement(lc, { key: t4, type: t4, RequiredScopes: r3, flow: n2.flows[t4] }))) : null);
          }
          const pc = { oauth2: "OAuth2", apiKey: "API Key", http: "HTTP", openIdConnect: "OpenID Connect" };
          class uc extends e2.PureComponent {
            render() {
              return this.props.securitySchemes.schemes.map(((t3) => e2.createElement(li, { id: t3.sectionId, key: t3.id }, e2.createElement(ui, null, e2.createElement(ai, null, e2.createElement(fi, null, e2.createElement(Si, { to: t3.sectionId }), t3.displayName), e2.createElement(zs, { source: t3.description || "" }), e2.createElement(nc, null, e2.createElement(rc, null, e2.createElement("b", null, "Security Scheme Type: "), e2.createElement("span", null, pc[t3.type] || t3.type)), e2.createElement(cc, { scheme: t3 })))))));
            }
          }
          var dc = (e3, t3, r3) => new Promise(((n2, o3) => {
            var i3 = (e4) => {
              try {
                a3(r3.next(e4));
              } catch (e5) {
                o3(e5);
              }
            }, s3 = (e4) => {
              try {
                a3(r3.throw(e4));
              } catch (e5) {
                o3(e5);
              }
            }, a3 = (e4) => e4.done ? n2(e4.value) : Promise.resolve(e4.value).then(i3, s3);
            a3((r3 = r3.apply(e3, t3)).next());
          }));
          function hc(e3, t3) {
            return dc(this, arguments, (function* (e4, t4, r3 = {}) {
              const n2 = yield ye2(e4 || t4);
              return new mc(n2, t4, r3);
            }));
          }
          class mc {
            constructor(e3, t3, r3 = {}, n2 = true) {
              this.marker = new Ht(), this.disposer = null, this.rawOptions = r3, this.options = new W2(r3, fc), this.scroll = new ii(this.options), ti.updateOnHistory(Wt.currentId, this.scroll), this.spec = new Do(e3, t3, this.options), this.menu = new ti(this.spec, this.scroll, Wt), this.options.disableSearch || (this.search = new si(), n2 && this.search.indexItems(this.menu.items), this.disposer = (0, de2.observe)(this.menu, "activeItemIdx", ((e4) => {
                this.updateMarkOnMenu(e4.newValue);
              })));
            }
            static fromJS(e3) {
              const t3 = new mc(e3.spec.data, e3.spec.url, e3.options, false);
              return t3.menu.activeItemIdx = e3.menu.activeItemIdx || 0, t3.menu.activate(t3.menu.flatItems[t3.menu.activeItemIdx]), t3.options.disableSearch || t3.search.load(e3.searchIndex), t3;
            }
            onDidMount() {
              this.menu.updateOnHistory(), this.updateMarkOnMenu(this.menu.activeItemIdx);
            }
            dispose() {
              this.scroll.dispose(), this.menu.dispose(), this.search && this.search.dispose(), null != this.disposer && this.disposer();
            }
            toJS() {
              return dc(this, null, (function* () {
                return { menu: { activeItemIdx: this.menu.activeItemIdx }, spec: { url: this.spec.parser.specUrl, data: this.spec.parser.spec }, searchIndex: this.search ? yield this.search.toJS() : void 0, options: this.rawOptions };
              }));
            }
            updateMarkOnMenu(e3) {
              const t3 = Math.max(0, e3), r3 = Math.min(this.menu.flatItems.length, t3 + 5), n2 = [];
              for (let e4 = t3; e4 < r3; e4++) {
                const t4 = this.menu.getElementAt(e4);
                t4 && n2.push(t4);
              }
              if (-1 === e3 && s2) {
                const e4 = document.querySelector('[data-role="redoc-description"]'), t4 = document.querySelector('[data-role="redoc-summary"]');
                e4 && n2.push(e4), t4 && n2.push(t4);
              }
              this.marker.addOnly(n2), this.marker.mark();
            }
          }
          const fc = { allowedMdComponents: { [dt2]: { component: uc, propsSelector: (e3) => ({ securitySchemes: e3.spec.securitySchemes }) }, [ht2]: { component: uc, propsSelector: (e3) => ({ securitySchemes: e3.spec.securitySchemes }) }, [mt2]: { component: Hl, propsSelector: (e3) => ({ parser: e3.spec.parser, options: e3.options }) } } }, gc = ee2(mi)`
  margin-top: 0;
  margin-bottom: 0.5em;

  ${te2("ApiHeader")};
`, yc = ee2.a`
  border: 1px solid ${(e3) => e3.theme.colors.primary.main};
  color: ${(e3) => e3.theme.colors.primary.main};
  font-weight: normal;
  margin-left: 0.5em;
  padding: 4px 8px 4px;
  display: inline-block;
  text-decoration: none;
  cursor: pointer;

  ${te2("DownloadButton")};
`, bc = ee2.span`
  &::before {
    content: '|';
    display: inline-block;
    opacity: 0.5;
    width: ${15}px;
    text-align: center;
  }

  &:last-child::after {
    display: none;
  }
`, xc = ee2.div`
  overflow: hidden;
`, vc = ee2.div`
  display: flex;
  flex-wrap: wrap;
  // hide separator on new lines: idea from https://stackoverflow.com/a/31732902/1749888
  margin-left: -${15}px;
`;
          Object.defineProperty, Object.getOwnPropertyDescriptor;
          let wc = class extends e2.Component {
            render() {
              const { store: t3 } = this.props, { info: r3, externalDocs: n2 } = t3.spec, o3 = t3.options.hideDownloadButtons, i3 = r3.downloadUrls, s3 = r3.downloadFileName, a3 = r3.license && e2.createElement(bc, null, "License:", " ", r3.license.identifier ? r3.license.identifier : e2.createElement("a", { href: r3.license.url }, r3.license.name)) || null, l3 = r3.contact && r3.contact.url && e2.createElement(bc, null, "URL: ", e2.createElement("a", { href: r3.contact.url }, r3.contact.url)) || null, c3 = r3.contact && r3.contact.email && e2.createElement(bc, null, r3.contact.name || "E-mail", ":", " ", e2.createElement("a", { href: "mailto:" + r3.contact.email }, r3.contact.email)) || null, p3 = r3.termsOfService && e2.createElement(bc, null, e2.createElement("a", { href: r3.termsOfService }, "Terms of Service")) || null, u3 = r3.version && e2.createElement("span", null, "(", r3.version, ")") || null;
              return e2.createElement(li, null, e2.createElement(ui, null, e2.createElement(ai, { className: "api-info" }, e2.createElement(gc, null, r3.title, " ", u3), !o3 && e2.createElement("p", null, _15("downloadSpecification"), ":", null == i3 ? void 0 : i3.map((({ title: t4, url: r4 }) => e2.createElement(yc, { download: s3 || true, target: "_blank", href: r4, rel: "noreferrer", key: r4 }, t4)))), e2.createElement(js, null, (r3.license || r3.contact || r3.termsOfService) && e2.createElement(xc, null, e2.createElement(vc, null, c3, " ", l3, " ", a3, " ", p3)) || null), e2.createElement(zs, { source: t3.spec.info.summary, "data-role": "redoc-summary" }), e2.createElement(zs, { source: t3.spec.info.description, "data-role": "redoc-description" }), n2 && e2.createElement(Aa, { externalDocs: n2 }))));
            }
          };
          wc = ((e3, t3) => {
            for (var r3, n2 = t3, o3 = e3.length - 1; o3 >= 0; o3--) (r3 = e3[o3]) && (n2 = r3(n2) || n2);
            return n2;
          })([fa.observer], wc);
          const kc = ee2.img`
  max-height: ${(e3) => e3.theme.logo.maxHeight};
  max-width: ${(e3) => e3.theme.logo.maxWidth};
  padding: ${(e3) => e3.theme.logo.gutter};
  width: 100%;
  display: block;
`, Ec = ee2.div`
  text-align: center;
`, Oc = ee2.a`
  display: inline-block;
`;
          Object.defineProperty, Object.getOwnPropertyDescriptor;
          let jc = class extends e2.Component {
            render() {
              const { info: t3 } = this.props, r3 = t3["x-logo"];
              if (!r3 || !r3.url) return null;
              const n2 = r3.href || t3.contact && t3.contact.url, o3 = r3.altText ? r3.altText : "logo", i3 = e2.createElement(kc, { src: r3.url, alt: o3 });
              return e2.createElement(Ec, { style: { backgroundColor: r3.backgroundColor } }, n2 ? (s3 = n2, (t4) => e2.createElement(Oc, { href: s3 }, t4))(i3) : i3);
              var s3;
            }
          };
          jc = ((e3, t3) => {
            for (var r3, n2 = t3, o3 = e3.length - 1; o3 >= 0; o3--) (r3 = e3[o3]) && (n2 = r3(n2) || n2);
            return n2;
          })([fa.observer], jc);
          var $c = Object.defineProperty, Sc = Object.getOwnPropertySymbols, Cc = Object.prototype.hasOwnProperty, Pc = Object.prototype.propertyIsEnumerable, Ic = (e3, t3, r3) => t3 in e3 ? $c(e3, t3, { enumerable: true, configurable: true, writable: true, value: r3 }) : e3[t3] = r3, Ac = (e3, t3) => {
            for (var r3 in t3 || (t3 = {})) Cc.call(t3, r3) && Ic(e3, r3, t3[r3]);
            if (Sc) for (var r3 of Sc(t3)) Pc.call(t3, r3) && Ic(e3, r3, t3[r3]);
            return e3;
          };
          class Tc extends e2.Component {
            render() {
              return e2.createElement(ue2, null, ((t3) => e2.createElement(ki, null, ((e3) => this.renderWithOptionsAndStore(t3, e3)))));
            }
            renderWithOptionsAndStore(t3, r3) {
              const { source: n2, htmlWrap: o3 = (e3) => e3 } = this.props;
              if (!r3) throw new Error("When using components in markdown, store prop must be provided");
              const i3 = new Ur(t3, this.props.parentId).renderMdWithComponents(n2);
              return i3.length ? i3.map(((t4, n3) => {
                if ("string" == typeof t4) return e2.cloneElement(o3(e2.createElement(Ds, { html: t4, inline: false, compact: false })), { key: n3 });
                const i4 = t4.component;
                return e2.createElement(i4, Ac({ key: n3 }, Ac(Ac({}, t4.props), t4.propsSelector(r3))));
              })) : null;
            }
          }
          var _c = require_classnames();
          const Rc = ee2.span.attrs(((e3) => ({ className: `operation-type ${e3.type}` })))`
  width: 9ex;
  display: inline-block;
  height: ${(e3) => e3.theme.typography.code.fontSize};
  line-height: ${(e3) => e3.theme.typography.code.fontSize};
  background-color: ${(e3) => e3.color || "#333"};
  border-radius: 3px;
  background-repeat: no-repeat;
  background-position: 6px 4px;
  font-size: 7px;
  font-family: Verdana, sans-serif; // web-safe
  color: white;
  text-transform: uppercase;
  text-align: center;
  font-weight: bold;
  vertical-align: middle;
  margin-right: 6px;
  margin-top: 2px;

  &.get {
    background-color: ${({ theme: e3 }) => e3.colors.http.get};
  }

  &.post {
    background-color: ${({ theme: e3 }) => e3.colors.http.post};
  }

  &.put {
    background-color: ${({ theme: e3 }) => e3.colors.http.put};
  }

  &.options {
    background-color: ${({ theme: e3 }) => e3.colors.http.options};
  }

  &.patch {
    background-color: ${({ theme: e3 }) => e3.colors.http.patch};
  }

  &.delete {
    background-color: ${({ theme: e3 }) => e3.colors.http.delete};
  }

  &.basic {
    background-color: ${({ theme: e3 }) => e3.colors.http.basic};
  }

  &.link {
    background-color: ${({ theme: e3 }) => e3.colors.http.link};
  }

  &.head {
    background-color: ${({ theme: e3 }) => e3.colors.http.head};
  }

  &.hook {
    background-color: ${({ theme: e3 }) => e3.colors.primary.main};
  }

  &.schema {
    background-color: ${({ theme: e3 }) => e3.colors.http.basic};
  }
`;
          function Dc(e3, { theme: t3 }, r3) {
            return e3 > 1 ? t3.sidebar.level1Items[r3] : 1 === e3 ? t3.sidebar.groupItems[r3] : "";
          }
          const zc = ee2.ul`
  margin: 0;
  padding: 0;

  &:first-child {
    padding-bottom: 32px;
  }

  & & {
    font-size: 0.929em;
  }

  ${(e3) => e3.$expanded ? "" : "display: none;"};
`, Lc = ee2.li`
  list-style: none inside none;
  overflow: hidden;
  text-overflow: ellipsis;
  padding: 0;
  ${(e3) => 0 === e3.depth ? "margin-top: 15px" : ""};
`, Nc = { 0: Y2`
    opacity: 0.7;
    text-transform: ${({ theme: e3 }) => e3.sidebar.groupItems.textTransform};
    font-size: 0.8em;
    padding-bottom: 0;
    cursor: default;
  `, 1: Y2`
    font-size: 0.929em;
    text-transform: ${({ theme: e3 }) => e3.sidebar.level1Items.textTransform};
  ` }, Mc = ee2.label.attrs(((e3) => ({ className: _c("-depth" + e3.$depth, { active: e3.$active }) })))`
  cursor: pointer;
  color: ${(e3) => e3.$active ? Dc(e3.$depth, e3, "activeTextColor") : e3.theme.sidebar.textColor};
  margin: 0;
  padding: 12.5px ${(e3) => 4 * e3.theme.spacing.unit}px;
  ${({ $depth: e3, $type: t3, theme: r3 }) => "section" === t3 && e3 > 1 && "padding-left: " + 8 * r3.spacing.unit + "px;" || ""}
  display: flex;
  justify-content: space-between;
  font-family: ${(e3) => e3.theme.typography.headings.fontFamily};
  ${(e3) => Nc[e3.$depth]};
  background-color: ${(e3) => e3.$active ? Dc(e3.$depth, e3, "activeBackgroundColor") : e3.theme.sidebar.backgroundColor};

  ${(e3) => e3.$deprecated && Ai || ""};

  &:hover {
    color: ${(e3) => Dc(e3.$depth, e3, "activeTextColor")};
    background-color: ${(e3) => Dc(e3.$depth, e3, "activeBackgroundColor")};
  }

  ${Pi} {
    height: ${({ theme: e3 }) => e3.sidebar.arrow.size};
    width: ${({ theme: e3 }) => e3.sidebar.arrow.size};
    polygon {
      fill: ${({ theme: e3 }) => e3.sidebar.arrow.color};
    }
  }
`, qc = ee2.span`
  display: inline-block;
  vertical-align: middle;
  width: ${(e3) => e3.width ? e3.width : "auto"};
  overflow: hidden;
  text-overflow: ellipsis;
`, Fc = ee2.div`
  ${({ theme: e3 }) => Y2`
    font-size: 0.8em;
    margin-top: ${2 * e3.spacing.unit}px;
    text-align: center;
    position: fixed;
    width: ${e3.sidebar.width};
    bottom: 0;
    background: ${e3.sidebar.backgroundColor};

    a,
    a:visited,
    a:hover {
      color: ${e3.sidebar.textColor} !important;
      padding: ${e3.spacing.unit}px 0;
      border-top: 1px solid ${(0, t2.darken)(0.1, e3.sidebar.backgroundColor)};
      text-decoration: none;
      display: flex;
      align-items: center;
      justify-content: center;
    }
  `};
  img {
    width: 15px;
    margin-right: 5px;
  }

  ${K2.lessThan("small")`
    width: 100%;
  `};
`, Bc = ee2.button`
  border: 0;
  width: 100%;
  text-align: left;
  & > * {
    vertical-align: middle;
  }

  ${Pi} {
    polygon {
      fill: ${({ theme: e3 }) => (0, t2.darken)(e3.colors.tonalOffset, e3.colors.gray[100])};
    }
  }
`, Qc = ee2.span`
  text-decoration: ${(e3) => e3.$deprecated ? "line-through" : "none"};
  margin-right: 8px;
`, Uc = ee2(Rc)`
  margin: 0 5px 0 0;
`, Wc = ee2(((t3) => {
            const { name: r3, opened: n2, className: o3, onClick: i3, httpVerb: s3, deprecated: a3 } = t3;
            return e2.createElement(Bc, { className: o3, onClick: i3 || void 0 }, e2.createElement(Uc, { type: s3 }, yt2(s3)), e2.createElement(Pi, { size: "1.5em", direction: n2 ? "down" : "right", float: "left" }), e2.createElement(Qc, { $deprecated: a3 }, r3), a3 ? e2.createElement(Ii, { type: "warning" }, " ", _15("deprecated"), " ") : null);
          }))`
  padding: 10px;
  border-radius: 2px;
  margin-bottom: 4px;
  line-height: 1.5em;
  background-color: ${({ theme: e3 }) => e3.colors.gray[100]};
  cursor: pointer;
  outline-color: ${({ theme: e3 }) => (0, t2.darken)(e3.colors.tonalOffset, e3.colors.gray[100])};
`, Vc = ee2.div`
  padding: 10px 25px;
  background-color: ${({ theme: e3 }) => e3.colors.gray[50]};
  margin-bottom: 5px;
  margin-top: 5px;
`;
          class Hc extends e2.PureComponent {
            constructor() {
              super(...arguments), this.selectElement = () => {
                Us.selectElement(this.child);
              };
            }
            render() {
              const { children: t3 } = this.props;
              return e2.createElement("div", { ref: (e3) => this.child = e3, onClick: this.selectElement, onFocus: this.selectElement, tabIndex: 0, role: "button" }, t3);
            }
          }
          const Jc = ee2.div`
  cursor: pointer;
  position: relative;
  margin-bottom: 5px;
`, Yc = ee2.span`
  font-family: ${(e3) => e3.theme.typography.code.fontFamily};
  margin-left: 10px;
  flex: 1;
  overflow-x: hidden;
  text-overflow: ellipsis;
`, Gc = ee2.button`
  outline: 0;
  color: inherit;
  width: 100%;
  text-align: left;
  cursor: pointer;
  padding: 10px 30px 10px ${(e3) => e3.$inverted ? "10px" : "20px"};
  border-radius: ${(e3) => e3.$inverted ? "0" : "4px 4px 0 0"};
  background-color: ${(e3) => e3.$inverted ? "transparent" : e3.theme.codeBlock.backgroundColor};
  display: flex;
  white-space: nowrap;
  align-items: center;
  border: ${(e3) => e3.$inverted ? "0" : "1px solid transparent"};
  border-bottom: ${(e3) => e3.$inverted ? "1px solid #ccc" : "0"};
  transition: border-color 0.25s ease;

  ${(e3) => e3.$expanded && !e3.$inverted && `border-color: ${e3.theme.colors.border.dark};` || ""}

  .${Yc} {
    color: ${(e3) => e3.$inverted ? e3.theme.colors.text.primary : "#ffffff"};
  }
  &:focus {
    box-shadow: inset 0 2px 2px rgba(0, 0, 0, 0.45), 0 2px 0 rgba(128, 128, 128, 0.25);
  }
`, Zc = ee2.span.attrs(((e3) => ({ className: `http-verb ${e3.type}` })))`
  font-size: ${(e3) => e3.$compact ? "0.8em" : "0.929em"};
  line-height: ${(e3) => e3.$compact ? "18px" : "20px"};
  background-color: ${(e3) => e3.theme.colors.http[e3.type] || "#999999"};
  color: #ffffff;
  padding: ${(e3) => e3.$compact ? "2px 8px" : "3px 10px"};
  text-transform: uppercase;
  font-family: ${(e3) => e3.theme.typography.headings.fontFamily};
  margin: 0;
`, Xc = ee2.div`
  position: absolute;
  width: 100%;
  z-index: 100;
  background: ${(e3) => e3.theme.rightPanel.servers.overlay.backgroundColor};
  color: ${(e3) => e3.theme.rightPanel.servers.overlay.textColor};
  box-sizing: border-box;
  box-shadow: 0 0 6px rgba(0, 0, 0, 0.33);
  overflow: hidden;
  border-bottom-left-radius: 4px;
  border-bottom-right-radius: 4px;
  transition: all 0.25s ease;
  visibility: hidden;
  ${(e3) => e3.$expanded ? "visibility: visible;" : "transform: translateY(-50%) scaleY(0);"}
`, Kc = ee2.div`
  padding: 10px;
`, ep = ee2.div`
  padding: 5px;
  border: 1px solid #ccc;
  background: ${(e3) => e3.theme.rightPanel.servers.url.backgroundColor};
  word-break: break-all;
  color: ${(e3) => e3.theme.colors.primary.main};
  > span {
    color: ${(e3) => e3.theme.colors.text.primary};
  }
`;
          class tp extends e2.Component {
            constructor(e3) {
              super(e3), this.toggle = () => {
                this.setState({ expanded: !this.state.expanded });
              }, this.state = { expanded: false };
            }
            render() {
              const { operation: t3, inverted: r3, hideHostname: n2 } = this.props, { expanded: o3 } = this.state;
              return e2.createElement(ce2.Consumer, null, ((i3) => e2.createElement(Jc, null, e2.createElement(Gc, { onClick: this.toggle, $expanded: o3, $inverted: r3 }, e2.createElement(Zc, { type: t3.httpVerb, $compact: this.props.compact }, t3.httpVerb), e2.createElement(Yc, null, t3.path), e2.createElement(Pi, { float: "right", color: r3 ? "black" : "white", size: "20px", direction: o3 ? "up" : "down", style: { marginRight: "-25px" } })), e2.createElement(Xc, { $expanded: o3, "aria-hidden": !o3 }, t3.servers.map(((r4) => {
                const o4 = i3.expandDefaultServerVariables ? pt2(r4.url, r4.variables) : r4.url, s3 = O2(o4);
                return e2.createElement(Kc, { key: o4 }, e2.createElement(zs, { source: r4.description || "", compact: true }), e2.createElement(Hc, null, e2.createElement(ep, null, e2.createElement("span", null, n2 || i3.hideHostname ? "/" === s3 ? "" : s3 : o4), t3.path)));
              }))))));
            }
          }
          class rp extends e2.PureComponent {
            render() {
              const { place: t3, parameters: r3 } = this.props;
              return r3 && r3.length ? e2.createElement("div", { key: t3 }, e2.createElement(bi, null, t3, " Parameters"), e2.createElement(Mi, null, e2.createElement("tbody", null, d(r3, ((t4, r4) => e2.createElement(ol, { key: t4.name, isLast: r4, field: t4, showExamples: true })))))) : null;
            }
          }
          Object.defineProperty, Object.getOwnPropertyDescriptor;
          let np = class extends e2.Component {
            constructor() {
              super(...arguments), this.switchMedia = ({ idx: e3 }) => {
                this.props.content && void 0 !== e3 && this.props.content.activate(e3);
              };
            }
            render() {
              const { content: t3 } = this.props;
              if (!t3 || !t3.mediaTypes || !t3.mediaTypes.length) return null;
              const r3 = t3.activeMimeIdx, n2 = t3.mediaTypes.map(((e3, t4) => ({ value: e3.name, idx: t4 }))), o3 = ({ children: t4 }) => this.props.withLabel ? e2.createElement(pa, null, e2.createElement(ca, null, "Content type"), t4) : t4;
              return e2.createElement(e2.Fragment, null, e2.createElement(o3, null, this.props.renderDropdown({ value: n2[r3].value, options: n2, onChange: this.switchMedia, ariaLabel: "Content type" })), this.props.children(t3.active));
            }
          };
          np = ((e3, t3) => {
            for (var r3, n2 = t3, o3 = e3.length - 1; o3 >= 0; o3--) (r3 = e3[o3]) && (n2 = r3(n2) || n2);
            return n2;
          })([fa.observer], np);
          var op = Object.defineProperty, ip = Object.getOwnPropertySymbols, sp = Object.prototype.hasOwnProperty, ap = Object.prototype.propertyIsEnumerable, lp = (e3, t3, r3) => t3 in e3 ? op(e3, t3, { enumerable: true, configurable: true, writable: true, value: r3 }) : e3[t3] = r3, cp = (e3, t3) => {
            for (var r3 in t3 || (t3 = {})) sp.call(t3, r3) && lp(e3, r3, t3[r3]);
            if (ip) for (var r3 of ip(t3)) ap.call(t3, r3) && lp(e3, r3, t3[r3]);
            return e3;
          }, pp = (e3, t3) => {
            var r3 = {};
            for (var n2 in e3) sp.call(e3, n2) && t3.indexOf(n2) < 0 && (r3[n2] = e3[n2]);
            if (null != e3 && ip) for (var n2 of ip(e3)) t3.indexOf(n2) < 0 && ap.call(e3, n2) && (r3[n2] = e3[n2]);
            return r3;
          };
          const up = ["path", "query", "cookie", "header"];
          class dp extends e2.PureComponent {
            orderParams(e3) {
              const t3 = {};
              return e3.forEach(((e4) => {
                var r3, n2, o3;
                o3 = e4, (r3 = t3)[n2 = e4.in] || (r3[n2] = []), r3[n2].push(o3);
              })), t3;
            }
            render() {
              const { body: t3, parameters: r3 = [] } = this.props;
              if (void 0 === t3 && void 0 === r3) return null;
              const n2 = this.orderParams(r3), o3 = r3.length > 0 ? up : [], i3 = t3 && t3.content, s3 = t3 && t3.description, a3 = t3 && t3.required;
              return e2.createElement(e2.Fragment, null, o3.map(((t4) => e2.createElement(rp, { key: t4, place: t4, parameters: n2[t4] }))), i3 && e2.createElement(mp, { content: i3, description: s3, bodyRequired: a3 }));
            }
          }
          function hp(t3) {
            var r3 = t3, { bodyRequired: n2 } = r3, o3 = pp(r3, ["bodyRequired"]);
            const i3 = "boolean" == typeof n2 && !!n2, s3 = "boolean" == typeof n2 && !n2;
            return e2.createElement(bi, { key: "header" }, "Request Body schema: ", e2.createElement(ws, cp({}, o3)), i3 && e2.createElement(gp, null, "required"), s3 && e2.createElement(yp, null, "optional"));
          }
          function mp(t3) {
            const { content: r3, description: n2, bodyRequired: o3 } = t3, { isRequestType: i3 } = r3;
            return e2.createElement(np, { content: r3, renderDropdown: (t4) => e2.createElement(hp, cp({ bodyRequired: o3 }, t4)) }, (({ schema: t4 }) => e2.createElement(e2.Fragment, null, void 0 !== n2 && e2.createElement(zs, { source: n2 }), "object" === (null == t4 ? void 0 : t4.type) && e2.createElement(Ma, { constraints: (null == t4 ? void 0 : t4.constraints) || [] }), e2.createElement(Ml, { skipReadOnly: i3, skipWriteOnly: !i3, key: "schema", schema: t4 }))));
          }
          const fp = "\n  text-transform: lowercase;\n  margin-left: 0;\n  line-height: 1.5em;\n", gp = ee2(ka)`
  ${fp}
`, yp = ee2("div")`
  ${fp}
  color: ${({ theme: e3 }) => e3.colors.text.secondary};
  font-size: ${(e3) => e3.theme.schema.labelsTextSize};
`, bp = e2.memo((function({ title: t3, type: r3, empty: n2, code: o3, opened: i3, className: s3, onClick: a3 }) {
            return e2.createElement("button", { className: s3, onClick: !n2 && a3 || void 0, "aria-expanded": i3, disabled: n2 }, !n2 && e2.createElement(Pi, { size: "1.5em", color: r3, direction: i3 ? "down" : "right", float: "left" }), e2.createElement(kp, null, o3, " "), e2.createElement(zs, { compact: true, inline: true, source: t3 }));
          })), xp = ee2(bp)`
  display: block;
  border: 0;
  width: 100%;
  text-align: left;
  padding: 10px;
  border-radius: 2px;
  margin-bottom: 4px;
  line-height: 1.5em;
  cursor: pointer;

  color: ${(e3) => e3.theme.colors.responses[e3.type].color};
  background-color: ${(e3) => e3.theme.colors.responses[e3.type].backgroundColor};
  &:focus {
    outline: auto ${(e3) => e3.theme.colors.responses[e3.type].color};
  }
  ${(e3) => e3.empty ? '\ncursor: default;\n&::before {\n  content: "—";\n  font-weight: bold;\n  width: 1.5em;\n  text-align: center;\n  display: inline-block;\n  vertical-align: top;\n}\n&:focus {\n  outline: 0;\n}\n' : ""};
`, vp = ee2.div`
  padding: 10px;
`, wp = ee2(bi).attrs({ as: "caption" })`
  text-align: left;
  margin-top: 1em;
  caption-side: top;
`, kp = ee2.strong`
  vertical-align: top;
`;
          class Ep extends e2.PureComponent {
            render() {
              const { headers: t3 } = this.props;
              return void 0 === t3 || 0 === t3.length ? null : e2.createElement(Mi, null, e2.createElement(wp, null, " Response Headers "), e2.createElement("tbody", null, d(t3, ((t4, r3) => e2.createElement(ol, { isLast: r3, key: t4.name, field: t4, showExamples: true })))));
            }
          }
          var Op = Object.defineProperty, jp = Object.getOwnPropertySymbols, $p = Object.prototype.hasOwnProperty, Sp = Object.prototype.propertyIsEnumerable, Cp = (e3, t3, r3) => t3 in e3 ? Op(e3, t3, { enumerable: true, configurable: true, writable: true, value: r3 }) : e3[t3] = r3;
          class Pp extends e2.PureComponent {
            constructor() {
              super(...arguments), this.renderDropdown = (t3) => e2.createElement(bi, { key: "header" }, "Response Schema: ", e2.createElement(ws, ((e3, t4) => {
                for (var r3 in t4 || (t4 = {})) $p.call(t4, r3) && Cp(e3, r3, t4[r3]);
                if (jp) for (var r3 of jp(t4)) Sp.call(t4, r3) && Cp(e3, r3, t4[r3]);
                return e3;
              })({}, t3)));
            }
            render() {
              const { description: t3, extensions: r3, headers: n2, content: o3 } = this.props.response;
              return e2.createElement(e2.Fragment, null, t3 && e2.createElement(zs, { source: t3 }), e2.createElement(za, { extensions: r3 }), e2.createElement(Ep, { headers: n2 }), e2.createElement(np, { content: o3, renderDropdown: this.renderDropdown }, (({ schema: t4 }) => e2.createElement(e2.Fragment, null, "object" === (null == t4 ? void 0 : t4.type) && e2.createElement(Ma, { constraints: (null == t4 ? void 0 : t4.constraints) || [] }), e2.createElement(Ml, { skipWriteOnly: true, key: "schema", schema: t4 })))));
            }
          }
          const Ip = (0, fa.observer)((({ response: t3 }) => {
            const { extensions: r3, headers: n2, type: o3, summary: i3, description: s3, code: a3, expanded: l3, content: c3 } = t3, p3 = e2.useMemo((() => void 0 === c3 ? [] : c3.mediaTypes.filter(((e3) => void 0 !== e3.schema))), [c3]), u3 = e2.useMemo((() => !(r3 && 0 !== Object.keys(r3).length || 0 !== n2.length || 0 !== p3.length || s3)), [r3, n2, p3, s3]);
            return e2.createElement("div", null, e2.createElement(xp, { onClick: () => t3.toggle(), type: o3, empty: u3, title: i3 || "", code: a3, opened: l3 }), l3 && !u3 && e2.createElement(vp, null, e2.createElement(Pp, { response: t3 })));
          })), Ap = ee2.h3`
  font-size: 1.3em;
  padding: 0.2em 0;
  margin: 3em 0 1.1em;
  color: ${({ theme: e3 }) => e3.colors.text.primary};
  font-weight: normal;
`;
          class Tp extends e2.PureComponent {
            render() {
              const { responses: t3, isCallback: r3 } = this.props;
              return t3 && 0 !== t3.length ? e2.createElement("div", null, e2.createElement(Ap, null, _15(r3 ? "callbackResponses" : "responses")), t3.map(((t4) => e2.createElement(Ip, { key: t4.code, response: t4 })))) : null;
            }
          }
          function _p(t3) {
            const { security: r3, showSecuritySchemeType: n2, expanded: o3 } = t3, i3 = r3.schemes.length > 1;
            return 0 === r3.schemes.length ? e2.createElement(Zl, { $expanded: o3 }, "None") : e2.createElement(Zl, { $expanded: o3 }, i3 && "(", r3.schemes.map(((t4) => e2.createElement(Gl, { key: t4.id }, n2 && `${pc[t4.type] || t4.type}: `, e2.createElement("i", null, t4.displayName), o3 && t4.scopes.length ? [" (", t4.scopes.map(((t5) => e2.createElement(Yl, { key: t5 }, t5))), ") "] : null))), i3 && ") ");
          }
          const Rp = ({ scopes: t3 }) => t3.length ? e2.createElement("div", null, e2.createElement("b", null, "Required scopes: "), t3.map(((t4, r3) => e2.createElement(e2.Fragment, { key: r3 }, e2.createElement("code", null, t4), " ")))) : null;
          function Dp(t3) {
            const r3 = Oi(), n2 = null == r3 ? void 0 : r3.options.showSecuritySchemeType, [o3, i3] = (0, e2.useState)(false), { securities: s3 } = t3;
            if (!(null == s3 ? void 0 : s3.length) || (null == r3 ? void 0 : r3.options.hideSecuritySection)) return null;
            const a3 = null == r3 ? void 0 : r3.spec.securitySchemes.schemes.filter((({ id: e3 }) => s3.find(((t4) => t4.schemes.find(((t5) => t5.id === e3))))));
            return e2.createElement(e2.Fragment, null, e2.createElement(tc, { $expanded: o3 }, e2.createElement(Xl, { onClick: () => i3(!o3) }, e2.createElement(ec, null, "Authorizations:"), e2.createElement(Pi, { size: "1.3em", direction: o3 ? "down" : "right" })), e2.createElement(Kl, { $expanded: o3 }, s3.map(((t4, r4) => e2.createElement(_p, { key: r4, expanded: o3, showSecuritySchemeType: n2, security: t4 }))))), o3 && !!(null == a3 ? void 0 : a3.length) && a3.map(((t4, r4) => e2.createElement(nc, { key: r4 }, e2.createElement("h5", null, e2.createElement(zp, null), " ", pc[t4.type] || t4.type, ": ", t4.id), e2.createElement(zs, { source: t4.description || "" }), e2.createElement(cc, { key: t4.id, scheme: t4, RequiredScopes: e2.createElement(Rp, { scopes: Lp(t4.id, s3) }) })))));
          }
          const zp = () => e2.createElement("svg", { xmlns: "http://www.w3.org/2000/svg", viewBox: "0 0 24 24", width: "11", height: "11" }, e2.createElement("path", { fill: "currentColor", d: "M18 10V6A6 6 0 0 0 6 6v4H3v14h18V10h-3zM8 6c0-2.206 1.794-4 4-4s4 1.794 4 4v4H8V6zm11 16H5V12h14v10z" }));
          function Lp(e3, t3) {
            const r3 = [];
            let n2 = t3.length;
            for (; n2--; ) {
              const o3 = t3[n2];
              let i3 = o3.schemes.length;
              for (; i3--; ) {
                const t4 = o3.schemes[i3];
                t4.id === e3 && Array.isArray(t4.scopes) && r3.push(...t4.scopes);
              }
            }
            return Array.from(new Set(r3));
          }
          Object.defineProperty, Object.getOwnPropertyDescriptor;
          let Np = class extends e2.Component {
            render() {
              const { operation: t3 } = this.props, { description: r3, externalDocs: n2 } = t3, o3 = !(!r3 && !n2);
              return e2.createElement(Vc, null, o3 && e2.createElement(Mp, null, void 0 !== r3 && e2.createElement(zs, { source: r3 }), n2 && e2.createElement(Aa, { externalDocs: n2 })), e2.createElement(tp, { operation: this.props.operation, inverted: true, compact: true }), e2.createElement(za, { extensions: t3.extensions }), e2.createElement(Dp, { securities: t3.security }), e2.createElement(dp, { parameters: t3.parameters, body: t3.requestBody }), e2.createElement(Tp, { responses: t3.responses, isCallback: t3.isCallback }));
            }
          };
          Np = ((e3, t3) => {
            for (var r3, n2 = t3, o3 = e3.length - 1; o3 >= 0; o3--) (r3 = e3[o3]) && (n2 = r3(n2) || n2);
            return n2;
          })([fa.observer], Np);
          const Mp = ee2.div`
  margin-bottom: ${({ theme: e3 }) => 3 * e3.spacing.unit}px;
`;
          Object.defineProperty, Object.getOwnPropertyDescriptor;
          let qp = class extends e2.Component {
            constructor() {
              super(...arguments), this.toggle = () => {
                this.props.callbackOperation.toggle();
              };
            }
            render() {
              const { name: t3, expanded: r3, httpVerb: n2, deprecated: o3 } = this.props.callbackOperation;
              return e2.createElement(e2.Fragment, null, e2.createElement(Wc, { onClick: this.toggle, name: t3, opened: r3, httpVerb: n2, deprecated: o3 }), r3 && e2.createElement(Np, { operation: this.props.callbackOperation }));
            }
          };
          qp = ((e3, t3) => {
            for (var r3, n2 = t3, o3 = e3.length - 1; o3 >= 0; o3--) (r3 = e3[o3]) && (n2 = r3(n2) || n2);
            return n2;
          })([fa.observer], qp);
          class Fp extends e2.PureComponent {
            render() {
              const { callbacks: t3 } = this.props;
              return t3 && 0 !== t3.length ? e2.createElement("div", null, e2.createElement(Bp, null, " Callbacks "), t3.map(((t4) => t4.operations.map(((r3, n2) => e2.createElement(qp, { key: `${t4.name}_${n2}`, callbackOperation: r3 })))))) : null;
            }
          }
          const Bp = ee2.h3`
  font-size: 1.3em;
  padding: 0.2em 0;
  margin: 3em 0 1.1em;
  color: ${({ theme: e3 }) => e3.colors.text.primary};
  font-weight: normal;
`;
          Object.defineProperty, Object.getOwnPropertyDescriptor;
          let Qp = class extends e2.Component {
            constructor(e3) {
              super(e3), this.switchItem = ({ idx: e4 }) => {
                this.props.items && void 0 !== e4 && this.setState({ activeItemIdx: e4 });
              }, this.state = { activeItemIdx: 0 };
            }
            render() {
              const { items: t3 } = this.props;
              if (!t3 || !t3.length) return null;
              const r3 = ({ children: t4 }) => this.props.label ? e2.createElement(pa, null, e2.createElement(ca, null, this.props.label), t4) : t4;
              return e2.createElement(e2.Fragment, null, e2.createElement(r3, null, this.props.renderDropdown({ value: this.props.options[this.state.activeItemIdx].value, options: this.props.options, onChange: this.switchItem, ariaLabel: this.props.label || "Callback" })), this.props.children(t3[this.state.activeItemIdx]));
            }
          };
          Qp = ((e3, t3) => {
            for (var r3, n2 = t3, o3 = e3.length - 1; o3 >= 0; o3--) (r3 = e3[o3]) && (n2 = r3(n2) || n2);
            return n2;
          })([fa.observer], Qp);
          var Up = Object.defineProperty, Wp = Object.defineProperties, Vp = (Object.getOwnPropertyDescriptor, Object.getOwnPropertyDescriptors), Hp = Object.getOwnPropertySymbols, Jp = Object.prototype.hasOwnProperty, Yp = Object.prototype.propertyIsEnumerable, Gp = (e3, t3, r3) => t3 in e3 ? Up(e3, t3, { enumerable: true, configurable: true, writable: true, value: r3 }) : e3[t3] = r3;
          let Zp = class extends e2.Component {
            constructor() {
              super(...arguments), this.renderDropdown = (t3) => {
                return e2.createElement(ws, (r3 = ((e3, t4) => {
                  for (var r4 in t4 || (t4 = {})) Jp.call(t4, r4) && Gp(e3, r4, t4[r4]);
                  if (Hp) for (var r4 of Hp(t4)) Yp.call(t4, r4) && Gp(e3, r4, t4[r4]);
                  return e3;
                })({ Label: la, Dropdown: ua }, t3), Wp(r3, Vp({ variant: "dark" }))));
                var r3;
              };
            }
            render() {
              const t3 = this.props.content;
              return void 0 === t3 ? null : e2.createElement(np, { content: t3, renderDropdown: this.renderDropdown, withLabel: true }, ((t4) => e2.createElement(ha, { key: "samples", mediaType: t4, renderDropdown: this.renderDropdown })));
            }
          };
          Zp = ((e3, t3) => {
            for (var r3, n2 = t3, o3 = e3.length - 1; o3 >= 0; o3--) (r3 = e3[o3]) && (n2 = r3(n2) || n2);
            return n2;
          })([fa.observer], Zp);
          class Xp extends e2.Component {
            render() {
              const t3 = this.props.callback.codeSamples.find(((e3) => fo(e3)));
              return t3 ? e2.createElement(Kp, null, e2.createElement(Zp, { content: t3.requestBodyContent })) : null;
            }
          }
          const Kp = ee2.div`
  margin-top: 15px;
`;
          var eu = Object.defineProperty, tu = Object.defineProperties, ru = (Object.getOwnPropertyDescriptor, Object.getOwnPropertyDescriptors), nu = Object.getOwnPropertySymbols, ou = Object.prototype.hasOwnProperty, iu = Object.prototype.propertyIsEnumerable, su = (e3, t3, r3) => t3 in e3 ? eu(e3, t3, { enumerable: true, configurable: true, writable: true, value: r3 }) : e3[t3] = r3;
          let au = class extends e2.Component {
            constructor() {
              super(...arguments), this.renderDropdown = (t3) => {
                return e2.createElement(ws, (r3 = ((e3, t4) => {
                  for (var r4 in t4 || (t4 = {})) ou.call(t4, r4) && su(e3, r4, t4[r4]);
                  if (nu) for (var r4 of nu(t4)) iu.call(t4, r4) && su(e3, r4, t4[r4]);
                  return e3;
                })({ Label: la, Dropdown: ua }, t3), tu(r3, ru({ variant: "dark" }))));
                var r3;
              };
            }
            render() {
              const { callbacks: t3 } = this.props;
              if (!t3 || 0 === t3.length) return null;
              const r3 = t3.map(((e3) => e3.operations.map(((e4) => e4)))).reduce(((e3, t4) => e3.concat(t4)), []);
              if (!r3.some(((e3) => e3.codeSamples.length > 0))) return null;
              const n2 = r3.map(((e3, t4) => ({ value: `${e3.httpVerb.toUpperCase()}: ${e3.name}`, idx: t4 })));
              return e2.createElement("div", null, e2.createElement(yi, null, " Callback payload samples "), e2.createElement(lu, null, e2.createElement(Qp, { items: r3, renderDropdown: this.renderDropdown, label: "Callback", options: n2 }, ((t4) => e2.createElement(Xp, { key: "callbackPayloadSample", callback: t4, renderDropdown: this.renderDropdown })))));
            }
          };
          au.contextType = ce2, au = ((e3, t3) => {
            for (var r3, n2 = t3, o3 = e3.length - 1; o3 >= 0; o3--) (r3 = e3[o3]) && (n2 = r3(n2) || n2);
            return n2;
          })([fa.observer], au);
          const lu = ee2.div`
  background: ${({ theme: e3 }) => e3.codeBlock.backgroundColor};
  padding: ${(e3) => 4 * e3.theme.spacing.unit}px;
`;
          Object.defineProperty, Object.getOwnPropertyDescriptor;
          let cu = class extends e2.Component {
            render() {
              const { operation: t3 } = this.props, r3 = t3.codeSamples, n2 = r3.length > 0, o3 = 1 === r3.length && this.context.hideSingleRequestSampleTab;
              return n2 && e2.createElement("div", null, e2.createElement(yi, null, " ", _15("requestSamples"), " "), e2.createElement(Vi, { defaultIndex: 0 }, e2.createElement(Wi.TabList, { hidden: o3 }, r3.map(((t4) => e2.createElement(Wi.Tab, { key: t4.lang + "_" + (t4.label || "") }, void 0 !== t4.label ? t4.label : t4.lang)))), r3.map(((t4) => e2.createElement(Wi.TabPanel, { key: t4.lang + "_" + (t4.label || "") }, fo(t4) ? e2.createElement("div", null, e2.createElement(Zp, { content: t4.requestBodyContent })) : e2.createElement(na, { lang: t4.lang, source: t4.source })))))) || null;
            }
          };
          cu.contextType = ce2, cu = ((e3, t3) => {
            for (var r3, n2 = t3, o3 = e3.length - 1; o3 >= 0; o3--) (r3 = e3[o3]) && (n2 = r3(n2) || n2);
            return n2;
          })([fa.observer], cu);
          Object.defineProperty, Object.getOwnPropertyDescriptor;
          let pu = class extends e2.Component {
            render() {
              const { operation: t3 } = this.props, r3 = t3.responses.filter(((e3) => e3.content && e3.content.hasSample));
              return r3.length > 0 && e2.createElement("div", null, e2.createElement(yi, null, " ", _15("responseSamples"), " "), e2.createElement(Vi, { defaultIndex: 0 }, e2.createElement(Wi.TabList, null, r3.map(((t4) => e2.createElement(Wi.Tab, { className: "tab-" + t4.type, key: t4.code }, t4.code)))), r3.map(((t4) => e2.createElement(Wi.TabPanel, { key: t4.code }, e2.createElement("div", null, e2.createElement(Zp, { content: t4.content }))))))) || null;
            }
          };
          pu = ((e3, t3) => {
            for (var r3, n2 = t3, o3 = e3.length - 1; o3 >= 0; o3--) (r3 = e3[o3]) && (n2 = r3(n2) || n2);
            return n2;
          })([fa.observer], pu);
          var uu = Object.defineProperty, du = Object.defineProperties, hu = Object.getOwnPropertyDescriptors, mu = Object.getOwnPropertySymbols, fu = Object.prototype.hasOwnProperty, gu = Object.prototype.propertyIsEnumerable, yu = (e3, t3, r3) => t3 in e3 ? uu(e3, t3, { enumerable: true, configurable: true, writable: true, value: r3 }) : e3[t3] = r3;
          const bu = ee2.div`
  margin-bottom: ${({ theme: e3 }) => 6 * e3.spacing.unit}px;
`, xu = (0, fa.observer)((({ operation: t3 }) => {
            const { name: r3, description: n2, deprecated: o3, externalDocs: i3, isWebhook: s3, httpVerb: a3, badges: l3 } = t3, c3 = !(!n2 && !i3), { showWebhookVerb: p3 } = e2.useContext(ce2), u3 = l3.filter((({ position: e3 }) => "before" === e3)), d2 = l3.filter((({ position: e3 }) => "after" === e3));
            return e2.createElement(ce2.Consumer, null, ((l4) => {
              return e2.createElement(ui, (h2 = ((e3, t4) => {
                for (var r4 in t4 || (t4 = {})) fu.call(t4, r4) && yu(e3, r4, t4[r4]);
                if (mu) for (var r4 of mu(t4)) gu.call(t4, r4) && yu(e3, r4, t4[r4]);
                return e3;
              })({}, { [ei]: t3.operationHash }), m3 = { id: t3.operationHash }, du(h2, hu(m3))), e2.createElement(ai, null, e2.createElement(fi, null, e2.createElement(Si, { to: t3.id }), u3.map((({ name: t4, color: r4 }) => e2.createElement(Ii, { type: "primary", key: t4, color: r4 }, t4))), r3, " ", o3 && e2.createElement(Ii, { type: "warning" }, " Deprecated "), s3 && e2.createElement(Ii, { type: "primary" }, " ", "Webhook ", p3 && a3 && "| " + a3.toUpperCase()), d2.map((({ name: t4, color: r4 }) => e2.createElement(Ii, { type: "primary", key: t4, color: r4 }, t4)))), l4.pathInMiddlePanel && !s3 && e2.createElement(tp, { operation: t3, inverted: true }), c3 && e2.createElement(bu, null, void 0 !== n2 && e2.createElement(zs, { source: n2 }), i3 && e2.createElement(Aa, { externalDocs: i3 })), e2.createElement(za, { extensions: t3.extensions }), e2.createElement(Dp, { securities: t3.security }), e2.createElement(dp, { parameters: t3.parameters, body: t3.requestBody }), e2.createElement(Tp, { responses: t3.responses }), e2.createElement(Fp, { callbacks: t3.callbacks })), e2.createElement(pi, null, !l4.pathInMiddlePanel && !s3 && e2.createElement(tp, { operation: t3 }), e2.createElement(cu, { operation: t3 }), e2.createElement(pu, { operation: t3 }), e2.createElement(au, { callbacks: t3.callbacks })));
              var h2, m3;
            }));
          }));
          var vu = Object.defineProperty, wu = Object.getOwnPropertyDescriptor, ku = Object.getOwnPropertySymbols, Eu = Object.prototype.hasOwnProperty, Ou = Object.prototype.propertyIsEnumerable, ju = (e3, t3, r3) => t3 in e3 ? vu(e3, t3, { enumerable: true, configurable: true, writable: true, value: r3 }) : e3[t3] = r3, $u = (e3, t3, r3, n2) => {
            for (var o3, i3 = n2 > 1 ? void 0 : n2 ? wu(t3, r3) : t3, s3 = e3.length - 1; s3 >= 0; s3--) (o3 = e3[s3]) && (i3 = (n2 ? o3(t3, r3, i3) : o3(i3)) || i3);
            return n2 && i3 && vu(t3, r3, i3), i3;
          };
          let Su = class extends e2.Component {
            render() {
              const t3 = this.props.items;
              return 0 === t3.length ? null : t3.map(((t4) => e2.createElement(Cu, { key: t4.id, item: t4 })));
            }
          };
          Su = $u([fa.observer], Su);
          let Cu = class extends e2.Component {
            render() {
              const t3 = this.props.item;
              let r3;
              const { type: n2 } = t3;
              switch (n2) {
                case "group":
                  r3 = null;
                  break;
                case "tag":
                case "section":
                default:
                  r3 = e2.createElement(Iu, ((e3, t4) => {
                    for (var r4 in t4 || (t4 = {})) Eu.call(t4, r4) && ju(e3, r4, t4[r4]);
                    if (ku) for (var r4 of ku(t4)) Ou.call(t4, r4) && ju(e3, r4, t4[r4]);
                    return e3;
                  })({}, this.props));
                  break;
                case "operation":
                  r3 = e2.createElement(Au, { item: t3 });
              }
              return e2.createElement(e2.Fragment, null, r3 && e2.createElement(li, { id: t3.id, $underlined: "operation" === t3.type }, r3), t3.items && e2.createElement(Su, { items: t3.items }));
            }
          };
          Cu = $u([fa.observer], Cu);
          const Pu = (t3) => e2.createElement(ai, { $compact: true }, t3);
          let Iu = class extends e2.Component {
            render() {
              const { name: t3, description: r3, externalDocs: n2, level: o3 } = this.props.item, i3 = 2 === o3 ? gi : fi;
              return e2.createElement(e2.Fragment, null, e2.createElement(ui, null, e2.createElement(ai, { $compact: false }, e2.createElement(i3, null, e2.createElement(Si, { to: this.props.item.id }), t3))), e2.createElement(Tc, { parentId: this.props.item.id, source: r3 || "", htmlWrap: Pu }), n2 && e2.createElement(ui, null, e2.createElement(ai, null, e2.createElement(Aa, { externalDocs: n2 }))));
            }
          };
          Iu = $u([fa.observer], Iu);
          let Au = class extends e2.Component {
            render() {
              return e2.createElement(xu, { operation: this.props.item });
            }
          };
          Au = $u([fa.observer], Au);
          var Tu = Object.defineProperty, _u = Object.defineProperties, Ru = (Object.getOwnPropertyDescriptor, Object.getOwnPropertyDescriptors), Du = Object.getOwnPropertySymbols, zu = Object.prototype.hasOwnProperty, Lu = Object.prototype.propertyIsEnumerable, Nu = (e3, t3, r3) => t3 in e3 ? Tu(e3, t3, { enumerable: true, configurable: true, writable: true, value: r3 }) : e3[t3] = r3;
          let Mu = class extends e2.Component {
            constructor() {
              super(...arguments), this.ref = e2.createRef(), this.activate = (e3) => {
                this.props.onActivate(this.props.item), e3.stopPropagation();
              };
            }
            componentDidMount() {
              this.scrollIntoViewIfActive();
            }
            componentDidUpdate() {
              this.scrollIntoViewIfActive();
            }
            scrollIntoViewIfActive() {
              this.props.item.active && this.ref.current && c2(this.ref.current);
            }
            render() {
              const { item: t3, withoutChildren: r3 } = this.props;
              return e2.createElement(Lc, { tabIndex: 0, onClick: this.activate, onKeyDown: (e3) => {
                "Enter" !== e3.key && " " !== e3.key || (this.props.onActivate(this.props.item), e3.stopPropagation());
              }, depth: t3.depth, "data-item-id": t3.id, role: "menuitem", "aria-label": t3.sidebarLabel, "aria-expanded": t3.expanded }, "operation" === t3.type ? e2.createElement(qu, (n2 = ((e3, t4) => {
                for (var r4 in t4 || (t4 = {})) zu.call(t4, r4) && Nu(e3, r4, t4[r4]);
                if (Du) for (var r4 of Du(t4)) Lu.call(t4, r4) && Nu(e3, r4, t4[r4]);
                return e3;
              })({}, this.props), _u(n2, Ru({ item: t3 })))) : e2.createElement(Mc, { $depth: t3.depth, $active: t3.active, $type: t3.type, ref: this.ref }, "schema" === t3.type && e2.createElement(Rc, { type: "schema" }, "schema"), e2.createElement(qc, { width: "calc(100% - 38px)", title: t3.sidebarLabel }, t3.sidebarLabel, this.props.children), t3.depth > 0 && t3.items.length > 0 && e2.createElement(Pi, { float: "right", direction: t3.expanded ? "down" : "right" }) || null), !r3 && t3.items && t3.items.length > 0 && e2.createElement(Vu, { expanded: t3.expanded, items: t3.items, onActivate: this.props.onActivate }));
              var n2;
            }
          };
          Mu = ((e3, t3) => {
            for (var r3, n2 = t3, o3 = e3.length - 1; o3 >= 0; o3--) (r3 = e3[o3]) && (n2 = r3(n2) || n2);
            return n2;
          })([fa.observer], Mu);
          const qu = (0, fa.observer)(((t3) => {
            var r3;
            const { item: n2 } = t3, o3 = e2.createRef(), { showWebhookVerb: i3 } = e2.useContext(ce2);
            return e2.useEffect((() => {
              t3.item.active && o3.current && c2(o3.current);
            }), [t3.item.active, o3]), e2.createElement(Mc, { $depth: n2.depth, $active: n2.active, $deprecated: n2.deprecated, ref: o3 }, n2.badges && (null == (r3 = n2.badges) ? void 0 : r3.map((({ name: t4, color: r4 }) => e2.createElement(Rc, { type: "badge", color: r4, key: t4 }, t4)))), n2.isWebhook ? e2.createElement(Rc, { type: "hook" }, i3 ? n2.httpVerb : _15("webhook")) : e2.createElement(Rc, { type: n2.httpVerb }, yt2(n2.httpVerb)), e2.createElement(qc, { tabIndex: 0, width: "calc(100% - 38px)" }, n2.sidebarLabel, t3.children));
          }));
          var Fu = Object.defineProperty, Bu = (Object.getOwnPropertyDescriptor, Object.getOwnPropertySymbols), Qu = Object.prototype.hasOwnProperty, Uu = Object.prototype.propertyIsEnumerable, Wu = (e3, t3, r3) => t3 in e3 ? Fu(e3, t3, { enumerable: true, configurable: true, writable: true, value: r3 }) : e3[t3] = r3;
          let Vu = class extends e2.Component {
            render() {
              const { items: t3, root: r3, className: n2 } = this.props, o3 = null == this.props.expanded || this.props.expanded;
              return e2.createElement(zc, ((e3, t4) => {
                for (var r4 in t4 || (t4 = {})) Qu.call(t4, r4) && Wu(e3, r4, t4[r4]);
                if (Bu) for (var r4 of Bu(t4)) Uu.call(t4, r4) && Wu(e3, r4, t4[r4]);
                return e3;
              })({ className: n2, style: this.props.style, $expanded: o3 }, r3 ? { role: "menu" } : {}), t3.map(((t4, r4) => e2.createElement(Mu, { key: r4, item: t4, onActivate: this.props.onActivate }))));
            }
          };
          function Hu() {
            const [t3, r3] = (0, e2.useState)(false);
            return (0, e2.useEffect)((() => {
              r3(true);
            }), []), t3 ? e2.createElement("img", { alt: "redocly logo", onError: () => r3(false), src: "https://cdn.redoc.ly/redoc/logo-mini.svg" }) : null;
          }
          Vu = ((e3, t3) => {
            for (var r3, n2 = t3, o3 = e3.length - 1; o3 >= 0; o3--) (r3 = e3[o3]) && (n2 = r3(n2) || n2);
            return n2;
          })([fa.observer], Vu);
          Object.defineProperty, Object.getOwnPropertyDescriptor;
          let Ju = class extends e2.Component {
            constructor() {
              super(...arguments), this.activate = (e3) => {
                if (e3 && e3.active && this.context.menuToggle) return e3.expanded ? e3.collapse() : e3.expand();
                this.props.menu.activateAndScroll(e3, true), setTimeout((() => {
                  this._updateScroll && this._updateScroll();
                }));
              }, this.saveScrollUpdate = (e3) => {
                this._updateScroll = e3;
              };
            }
            render() {
              const t3 = this.props.menu;
              return e2.createElement(cs, { updateFn: this.saveScrollUpdate, className: this.props.className, options: { wheelPropagation: false } }, e2.createElement(Vu, { items: t3.items, onActivate: this.activate, root: true }), e2.createElement(Fc, null, e2.createElement("a", { target: "_blank", rel: "noopener noreferrer", href: "https://redocly.com/redoc/" }, e2.createElement(Hu, null), "API docs by Redocly")));
            }
          };
          Ju.contextType = ce2, Ju = ((e3, t3) => {
            for (var r3, n2 = t3, o3 = e3.length - 1; o3 >= 0; o3--) (r3 = e3[o3]) && (n2 = r3(n2) || n2);
            return n2;
          })([fa.observer], Ju);
          const Yu = ({ open: t3 }) => {
            const r3 = t3 ? 8 : -4;
            return e2.createElement(Zu, null, e2.createElement(Gu, { size: 15, style: { transform: `translate(2px, ${r3}px) rotate(180deg)`, transition: "transform 0.2s ease" } }), e2.createElement(Gu, { size: 15, style: { transform: `translate(2px, ${0 - r3}px)`, transition: "transform 0.2s ease" } }));
          }, Gu = ({ size: t3 = 10, className: r3 = "", style: n2 }) => e2.createElement("svg", { className: r3, style: n2 || {}, viewBox: "0 0 926.23699 573.74994", version: "1.1", x: "0px", y: "0px", width: t3, height: t3 }, e2.createElement("g", { transform: "translate(904.92214,-879.1482)" }, e2.createElement("path", { d: "\n          m -673.67664,1221.6502 -231.2455,-231.24803 55.6165,\n          -55.627 c 30.5891,-30.59485 56.1806,-55.627 56.8701,-55.627 0.6894,\n          0 79.8637,78.60862 175.9427,174.68583 l 174.6892,174.6858 174.6892,\n          -174.6858 c 96.079,-96.07721 175.253196,-174.68583 175.942696,\n          -174.68583 0.6895,0 26.281,25.03215 56.8701,\n          55.627 l 55.6165,55.627 -231.245496,231.24803 c -127.185,127.1864\n          -231.5279,231.248 -231.873,231.248 -0.3451,0 -104.688,\n          -104.0616 -231.873,-231.248 z\n        ", fill: "currentColor" }))), Zu = ee2.div`
  user-select: none;
  width: 20px;
  height: 20px;
  align-self: center;
  display: flex;
  flex-direction: column;
  color: ${(e3) => e3.theme.colors.primary.main};
`;
          Object.defineProperty, Object.getOwnPropertyDescriptor;
          let Xu;
          s2 && (Xu = r2(230));
          const Ku = Xu && Xu(), ed = ee2.div`
  width: ${(e3) => e3.theme.sidebar.width};
  background-color: ${(e3) => e3.theme.sidebar.backgroundColor};
  overflow: hidden;
  display: flex;
  flex-direction: column;

  backface-visibility: hidden;
  /* contain: strict; TODO: breaks layout since Chrome 80*/

  height: 100vh;
  position: sticky;
  position: -webkit-sticky;
  top: 0;

  ${K2.lessThan("small")`
    position: fixed;
    z-index: 20;
    width: 100%;
    background: ${({ theme: e3 }) => e3.sidebar.backgroundColor};
    display: ${(e3) => e3.$open ? "flex" : "none"};
  `};

  @media print {
    display: none;
  }
`, td = ee2.div`
  outline: none;
  user-select: none;
  background-color: ${({ theme: e3 }) => e3.fab.backgroundColor};
  color: ${(e3) => e3.theme.colors.primary.main};
  display: none;
  cursor: pointer;
  position: fixed;
  right: 20px;
  z-index: 100;
  border-radius: 50%;
  box-shadow: 0 0 20px rgba(0, 0, 0, 0.3);
  ${K2.lessThan("small")`
    display: flex;
  `};

  bottom: 44px;

  width: 60px;
  height: 60px;
  padding: 0 20px;
  svg {
    color: ${({ theme: e3 }) => e3.fab.color};
  }

  @media print {
    display: none;
  }
`;
          let rd = class extends e2.Component {
            constructor() {
              super(...arguments), this.state = { offsetTop: "0px" }, this.toggleNavMenu = () => {
                this.props.menu.toggleSidebar();
              };
            }
            componentDidMount() {
              Ku && Ku.add(this.stickyElement), this.setState({ offsetTop: this.getScrollYOffset(this.context) });
            }
            componentWillUnmount() {
              Ku && Ku.remove(this.stickyElement);
            }
            getScrollYOffset(e3) {
              let t3;
              return t3 = void 0 !== this.props.scrollYOffset ? W2.normalizeScrollYOffset(this.props.scrollYOffset)() : e3.scrollYOffset(), t3 + "px";
            }
            render() {
              const t3 = this.props.menu.sideBarOpened, r3 = this.state.offsetTop;
              return e2.createElement(e2.Fragment, null, e2.createElement(ed, { $open: t3, className: this.props.className, style: { top: r3, height: `calc(100vh - ${r3})` }, ref: (e3) => {
                this.stickyElement = e3;
              } }, this.props.children), !this.context.hideFab && e2.createElement(td, { onClick: this.toggleNavMenu }, e2.createElement(Yu, { open: t3 })));
            }
          };
          rd.contextType = ce2, rd = ((e3, t3) => {
            for (var r3, n2 = t3, o3 = e3.length - 1; o3 >= 0; o3--) (r3 = e3[o3]) && (n2 = r3(n2) || n2);
            return n2;
          })([fa.observer], rd);
          const nd = ee2.div`
  ${({ theme: e3 }) => `
  font-family: ${e3.typography.fontFamily};
  font-size: ${e3.typography.fontSize};
  font-weight: ${e3.typography.fontWeightRegular};
  line-height: ${e3.typography.lineHeight};
  color: ${e3.colors.text.primary};
  display: flex;
  position: relative;
  text-align: left;

  -webkit-font-smoothing: ${e3.typography.smoothing};
  font-smoothing: ${e3.typography.smoothing};
  ${e3.typography.optimizeSpeed ? "text-rendering: optimizeSpeed !important" : ""};

  tap-highlight-color: rgba(0, 0, 0, 0);
  text-size-adjust: 100%;

  * {
    box-sizing: border-box;
    -webkit-tap-highlight-color: rgba(255, 255, 255, 0);
  }
`};
`, od = ee2.div`
  z-index: 1;
  position: relative;
  overflow: hidden;
  width: calc(100% - ${(e3) => e3.theme.sidebar.width});
  ${K2.lessThan("small", true)`
    width: 100%;
  `};

  contain: layout;
`, id = ee2.div`
  background: ${({ theme: e3 }) => e3.rightPanel.backgroundColor};
  position: absolute;
  top: 0;
  bottom: 0;
  right: 0;
  width: ${({ theme: e3 }) => {
            if (e3.rightPanel.width.endsWith("%")) {
              const t3 = parseInt(e3.rightPanel.width, 10);
              return `calc((100% - ${e3.sidebar.width}) * ${t3 / 100})`;
            }
            return e3.rightPanel.width;
          }};
  ${K2.lessThan("medium", true)`
    display: none;
  `};
`, sd = ee2.div`
  padding: 5px 0;
`, ad = ee2.input.attrs((() => ({ className: "search-input" })))`
  width: calc(100% - ${(e3) => 8 * e3.theme.spacing.unit}px);
  box-sizing: border-box;
  margin: 0 ${(e3) => 4 * e3.theme.spacing.unit}px;
  padding: 5px ${(e3) => 2 * e3.theme.spacing.unit}px 5px
    ${(e3) => 4 * e3.theme.spacing.unit}px;
  border: 0;
  border-bottom: 1px solid
    ${({ theme: e3 }) => ((0, t2.getLuminance)(e3.sidebar.backgroundColor) > 0.5 ? t2.darken : t2.lighten)(0.1, e3.sidebar.backgroundColor)};
  font-family: ${({ theme: e3 }) => e3.typography.fontFamily};
  font-weight: bold;
  font-size: 13px;
  color: ${(e3) => e3.theme.sidebar.textColor};
  background-color: transparent;
  outline: none;
`, ld = ee2(((t3) => e2.createElement("svg", { className: t3.className, version: "1.1", viewBox: "0 0 1000 1000", x: "0px", xmlns: "http://www.w3.org/2000/svg", y: "0px" }, e2.createElement("path", { d: "M968.2,849.4L667.3,549c83.9-136.5,66.7-317.4-51.7-435.6C477.1-25,252.5-25,113.9,113.4c-138.5,138.3-138.5,362.6,0,501C219.2,730.1,413.2,743,547.6,666.5l301.9,301.4c43.6,43.6,76.9,14.9,104.2-12.4C981,928.3,1011.8,893,968.2,849.4z M524.5,522c-88.9,88.7-233,88.7-321.8,0c-88.9-88.7-88.9-232.6,0-321.3c88.9-88.7,233-88.7,321.8,0C613.4,289.4,613.4,433.3,524.5,522z" })))).attrs({ className: "search-icon" })`
  position: absolute;
  left: ${(e3) => 4 * e3.theme.spacing.unit}px;
  height: 1.8em;
  width: 0.9em;

  path {
    fill: ${(e3) => e3.theme.sidebar.textColor};
  }
`, cd = ee2.div`
  padding: ${(e3) => e3.theme.spacing.unit}px 0;
  background-color: ${({ theme: e3 }) => (0, t2.darken)(0.05, e3.sidebar.backgroundColor)}};
  color: ${(e3) => e3.theme.sidebar.textColor};
  min-height: 150px;
  max-height: 250px;
  border-top: ${({ theme: e3 }) => (0, t2.darken)(0.1, e3.sidebar.backgroundColor)}};
  border-bottom: ${({ theme: e3 }) => (0, t2.darken)(0.1, e3.sidebar.backgroundColor)}};
  margin-top: 10px;
  line-height: 1.4;
  font-size: 0.9em;
  
  li {
    background-color: inherit;
  }

  ${Mc} {
    padding-top: 6px;
    padding-bottom: 6px;

    &:hover,
    &.active {
      background-color: ${({ theme: e3 }) => (0, t2.darken)(0.1, e3.sidebar.backgroundColor)};
    }

    > svg {
      display: none;
    }
  }
`, pd = ee2.i`
  position: absolute;
  display: inline-block;
  width: ${(e3) => 2 * e3.theme.spacing.unit}px;
  text-align: center;
  right: ${(e3) => 4 * e3.theme.spacing.unit}px;
  line-height: 2em;
  vertical-align: middle;
  margin-right: 2px;
  cursor: pointer;
  font-style: normal;
  color: '#666';
`;
          var ud = Object.defineProperty, dd = Object.getOwnPropertyDescriptor;
          class hd extends e2.PureComponent {
            constructor(e3) {
              super(e3), this.activeItemRef = null, this.clear = () => {
                this.setState({ results: [], noResults: false, term: "", activeItemIdx: -1 }), this.props.marker.unmark();
              }, this.handleKeyDown = (e4) => {
                if (27 === e4.keyCode && this.clear(), 40 === e4.keyCode && (this.setState({ activeItemIdx: Math.min(this.state.activeItemIdx + 1, this.state.results.length - 1) }), e4.preventDefault()), 38 === e4.keyCode && (this.setState({ activeItemIdx: Math.max(0, this.state.activeItemIdx - 1) }), e4.preventDefault()), 13 === e4.keyCode) {
                  const e5 = this.state.results[this.state.activeItemIdx];
                  if (e5) {
                    const t3 = this.props.getItemById(e5.meta);
                    t3 && this.props.onActivate(t3);
                  }
                }
              }, this.search = (e4) => {
                const { minCharacterLengthToInitSearch: t3 } = this.context, r3 = e4.target.value;
                r3.length < t3 ? this.clearResults(r3) : this.setState({ term: r3 }, (() => this.searchCallback(this.state.term)));
              }, this.state = { results: [], noResults: false, term: "", activeItemIdx: -1 };
            }
            clearResults(e3) {
              this.setState({ results: [], noResults: false, term: e3 }), this.props.marker.unmark();
            }
            setResults(e3, t3) {
              this.setState({ results: e3, noResults: 0 === e3.length }), this.props.marker.mark(t3);
            }
            searchCallback(e3) {
              this.props.search.search(e3).then(((t3) => {
                this.setResults(t3, e3);
              }));
            }
            render() {
              const { activeItemIdx: t3 } = this.state, r3 = this.state.results.filter(((e3) => this.props.getItemById(e3.meta))).map(((e3) => ({ item: this.props.getItemById(e3.meta), score: e3.score }))).sort(((e3, t4) => t4.score - e3.score));
              return e2.createElement(sd, { role: "search" }, this.state.term && e2.createElement(pd, { onClick: this.clear }, "×"), e2.createElement(ld, null), e2.createElement(ad, { value: this.state.term, onKeyDown: this.handleKeyDown, placeholder: "Search...", "aria-label": "Search", type: "text", onChange: this.search }), r3.length > 0 && e2.createElement(cs, { options: { wheelPropagation: false } }, e2.createElement(cd, { "data-role": "search:results" }, r3.map(((r4, n2) => e2.createElement(Mu, { item: Object.create(r4.item, { active: { value: n2 === t3 } }), onActivate: this.props.onActivate, withoutChildren: true, key: r4.item.id, "data-role": "search:result" }))))), this.state.term && this.state.noResults ? e2.createElement(cd, { "data-role": "search:results" }, _15("noResultsFound")) : null);
            }
          }
          hd.contextType = ce2, ((e3, t3, r3) => {
            for (var n2, o3 = dd(t3, r3), i3 = e3.length - 1; i3 >= 0; i3--) (n2 = e3[i3]) && (o3 = n2(t3, r3, o3) || o3);
            o3 && ud(t3, r3, o3);
          })([xe2.bind, (0, xe2.debounce)(400)], hd.prototype, "searchCallback");
          class md extends e2.Component {
            componentDidMount() {
              this.props.store.onDidMount();
            }
            componentWillUnmount() {
              this.props.store.dispose();
            }
            render() {
              const { store: { spec: t3, menu: r3, options: n2, search: o3, marker: i3 } } = this.props, s3 = this.props.store;
              return e2.createElement(X2, { theme: n2.theme }, e2.createElement(wi, { value: s3 }, e2.createElement(pe2, { value: n2 }, e2.createElement(nd, { className: "redoc-wrap" }, e2.createElement(rd, { menu: r3, className: "menu-content" }, e2.createElement(jc, { info: t3.info }), !n2.disableSearch && e2.createElement(hd, { search: o3, marker: i3, getItemById: r3.getItemById, onActivate: r3.activateAndScroll }) || null, e2.createElement(Ju, { menu: r3 })), e2.createElement(od, { className: "api-content" }, e2.createElement(wc, { store: s3 }), e2.createElement(Su, { items: r3.items })), e2.createElement(id, null)))));
            }
          }
          md.propTypes = { store: le2.instanceOf(mc).isRequired };
          var fd = Object.defineProperty, gd = Object.getOwnPropertySymbols, yd = Object.prototype.hasOwnProperty, bd = Object.prototype.propertyIsEnumerable, xd = (e3, t3, r3) => t3 in e3 ? fd(e3, t3, { enumerable: true, configurable: true, writable: true, value: r3 }) : e3[t3] = r3, vd = (e3, t3) => {
            for (var r3 in t3 || (t3 = {})) yd.call(t3, r3) && xd(e3, r3, t3[r3]);
            if (gd) for (var r3 of gd(t3)) bd.call(t3, r3) && xd(e3, r3, t3[r3]);
            return e3;
          };
          const wd = function(t3) {
            const { spec: n2, specUrl: o3, options: i3 = {}, onLoaded: s3 } = t3, a3 = Q2(i3.hideLoading, false), l3 = new W2(i3);
            if (void 0 !== l3.nonce) try {
              r2.nc = l3.nonce;
            } catch (e3) {
            }
            return e2.createElement(ne2, null, e2.createElement(Ei, { spec: n2 ? vd({}, n2) : void 0, specUrl: o3, options: i3, onLoaded: s3 }, (({ loading: t4, store: r3 }) => t4 ? a3 ? null : e2.createElement(ae2, { color: l3.theme.colors.primary.main }) : e2.createElement(md, { store: r3 }))));
          };
        })(), n;
      })();
    }));
  }
});
export default require_redoc_browser_lib();
/*! Bundled license information:

lunr/lunr.js:
  (**
   * lunr - http://lunrjs.com - A bit like Solr, but much smaller and not as bright - 2.3.9
   * Copyright (C) 2020 Oliver Nightingale
   * @license MIT
   *)
  (*!
   * lunr.utils
   * Copyright (C) 2020 Oliver Nightingale
   *)
  (*!
   * lunr.Set
   * Copyright (C) 2020 Oliver Nightingale
   *)
  (*!
   * lunr.tokenizer
   * Copyright (C) 2020 Oliver Nightingale
   *)
  (*!
   * lunr.Pipeline
   * Copyright (C) 2020 Oliver Nightingale
   *)
  (*!
   * lunr.Vector
   * Copyright (C) 2020 Oliver Nightingale
   *)
  (*!
   * lunr.stemmer
   * Copyright (C) 2020 Oliver Nightingale
   * Includes code from - http://tartarus.org/~martin/PorterStemmer/js.txt
   *)
  (*!
   * lunr.stopWordFilter
   * Copyright (C) 2020 Oliver Nightingale
   *)
  (*!
   * lunr.trimmer
   * Copyright (C) 2020 Oliver Nightingale
   *)
  (*!
   * lunr.TokenSet
   * Copyright (C) 2020 Oliver Nightingale
   *)
  (*!
   * lunr.Index
   * Copyright (C) 2020 Oliver Nightingale
   *)
  (*!
   * lunr.Builder
   * Copyright (C) 2020 Oliver Nightingale
   *)

stickyfill/index.js:
  (*!
   * Stickyfill -- `position: sticky` polyfill
   * v. 1.1.1 | https://github.com/wilddeer/stickyfill
   * Copyright Oleg Korsunsky | http://wd.dizaina.net/
   *
   * MIT License
   *)

react-is/cjs/react-is.development.js:
  (** @license React v16.13.1
   * react-is.development.js
   *
   * Copyright (c) Facebook, Inc. and its affiliates.
   *
   * This source code is licensed under the MIT license found in the
   * LICENSE file in the root directory of this source tree.
   *)

object-assign/index.js:
  (*
  object-assign
  (c) Sindre Sorhus
  @license MIT
  *)

prismjs/prism.js:
  (**
   * Prism: Lightweight, robust, elegant syntax highlighting
   *
   * @license MIT <https://opensource.org/licenses/MIT>
   * @author Lea Verou <https://lea.verou.me>
   * @namespace
   * @public
   *)

mark.js/dist/mark.js:
  (*!***************************************************
  * mark.js v8.11.1
  * https://markjs.io/
  * Copyright (c) 2014–2018, Julian Kühnel
  * Released under the MIT license https://git.io/vwTVl
  *****************************************************)

perfect-scrollbar/dist/perfect-scrollbar.esm.js:
  (*!
   * perfect-scrollbar v1.5.6
   * Copyright 2024 Hyunje Jun, MDBootstrap and Contributors
   * Licensed under MIT
   *)

dompurify/dist/purify.cjs.js:
  (*! @license DOMPurify 3.2.7 | (c) Cure53 and other contributors | Released under the Apache license 2.0 and Mozilla Public License 2.0 | github.com/cure53/DOMPurify/blob/3.2.7/LICENSE *)

classnames/index.js:
  (*!
  	Copyright (c) 2018 Jed Watson.
  	Licensed under the MIT License (MIT), see
  	http://jedwatson.github.io/classnames
  *)

redoc/bundles/redoc.browser.lib.js:
  (*! For license information please see redoc.browser.lib.js.LICENSE.txt *)
*/
//# sourceMappingURL=redoc.js.map
